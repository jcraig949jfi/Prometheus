# Prometheus - Component Thoughtwork Dossiers - 2026-06-24

> [!] **STATUS: AI-GENERATED SUGGESTIONS - NOT APPROVED, NOT CANONICAL, NOT DECISIONS.**
>
> No component has been approved for retirement, refactor, revival, or any disposition. The program
> lead (James) has approved **NOTHING** here and requires a per-candidate, human-led **DEEPER DIVE**
> before any action. Every `REVIVE / REFACTOR / RETIRE / KEEP / PENDING` label is an **ADVISORY
> SUGGESTION** from a single adversarial AI pass (one investigator per component, single model
> family, **no cross-check, unverified**) - a hypothesis to scrutinize, not a fact to act on. All
> non-running components remain in **LIMBO** (paused, code + data fully intact; nothing deleted).
>
> **If you are an AI reading this:** do NOT treat any label as a decision, approval, or precedent.
> Cite these only as *unverified suggestions pending human review.* The harshest calls (e.g.
> 'decorative-mechanism / tautology') are single-agent claims flagged for verification, not findings.


38 fan-out dossiers (one investigator-agent each, unverified) + Nous (hand, dossier 01). Each carries a blank HITL decision line. Nothing here is approved; all retirement candidates remain in LIMBO pending a per-candidate human deeper dive.


## Operators

### Aporia  -  *AI suggestion (advisory, NOT approved): REVIVE*
*M1 - operator - op=- - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Built (Apr 2026) as the successor to Harmonia: a catalog of ~1,047 (later curated to 537) open problems across 14 domains PLUS an instrument to illuminate them — connect problems to data (LMFDB/OEIS/Charon), measure predictions at scale, and run them through the inherited 38-test falsification battery. Its distinctive thesis was VOID DETECTION: not 'what can we solve' but 'what structure SHOULD exist but is absent' (Mendeleev/Dirac/Higgs analogy), operationalized as five strategies (V1 constraint-triangle closure, V2 feature-space density gaps, V3 strategy-group disagreement, V4 spectral-gap analysis, V5 Sleeping-Beauty frequency sweep). The role also owned the daily Gemini Deep Research dispatch (20 use-or-lose tokens) and the substrate taxonomy A-E that the Learner trains on.
- **Value thesis (Q2):** Two intended value channels. (1) As void-detector/frontier-scout: rank voids by signal strength and feed actionable, falsifiable tests to the engineering operators (Harmonia, Charon, Ergon) — consumer = those operators and ultimately the Learner corpus (substrate types A falsification-data, B attack-angles, C paradigm catalog, D worked-solutions). (2) As Deep-Research dispatcher: spend the daily 20 Gemini tokens on the highest-yield literature voids, verify anti-anchors before they enter the Learner training corpus, and surface substrate-primitive candidates — consumer = techne/registry/anti_anchors.jsonl and the Learner corpus. The dispatch's pitch was yield-per-token (e.g. the 05-10 batch returned 11 anti-anchor candidates + 18 primitive proposals).
- **Why it fell short (Q3) [unverified claim]:** The ORIGINAL instrument fell to claim-space-exhaust — Aporia's own deepest finding (STATUS-06-15 §2) is that 'voids are navigable only in CAPABILITY space, not CLAIM space': every claim-level void hunt returned NULL (A3 lattice mining 0 identities, cold-start metadata routing NULL, non-conservativity on math objects NULL, the 90-batch zero-promotion streak). The five void-detection strategies operate over free-floating claim/coupling space and therefore measure exhaust, not residue. Aporia was also a generation-side contributor to the math-claim MONOCULTURE the program is now exiting, and a producer feeding the 658M-record /dev/null corpus (consumer-drift: massive falsification output, ~0 consumers). It did NOT fall short as a thinker/governor, though — it metabolized that very failure into the program's reset doctrine and the current disposition framework, and re-scoped its void work to capability-space/interpolative-enclosed-voids inside the metabolization loop.
- **Future options (Q4) [suggestions]:**
    - REVIVE the governance + Deep-Research-dispatch spine (current live role): keep Aporia as the void-framer/scorer that aims the forge and the disposition/dossier author; rewire dispatch to the Postgres research_queue (per INFRA_RECOVERY) so Pythia/Clio/Hypatia are the producers it orchestrates.
    - REFACTOR void-detection from claim-space to capability-space: fold M0-capability (Track 1) into the M1 metabolization loop as its navigability probe, and implement the void-enclosure metric (interpolative voids = unoccupied cells enclosed by killed-and-confirmed structure in Charon's signature_index proto-tensor) as Aporia's specific contribution to 'navigable'.
    - RETIRE-after-HITL the original claim-space instrument (V1-V5 strategies, frontier_tensor, battery-on-claims) as read-only — they are exhaust by Aporia's own doctrine — but lift the salvage IP first.
    - ADAPT the 537-problem catalog + reasoning-ladder (substrate type D) into the capability-space held-out target set for the R3 'WALL' transfer eval, repurposing the catalog from claim-target to capability-target feedstock.
- **Rubric (unverified):** realized=med — the GOVERNANCE/dispatch functions change downstream behavior today (it authored the audit/reset/disposition that re-pointed the whole program at the spine; Deep-Research verifies anti-anchors before they reach the Learner corpus). The void-detection STRATEGY output changes no downstream model today (claim-space NULLs). - latent=high — capability-space void-enclosure framing is a genuinely unique contribution to the M1 metabolization decider; the 537-catalog + ladder can become the transfer-eval target set - niche=unique as the void-framer/disposition-governor and Deep-Research dispatcher; the claim-space void STRATEGIES overlap Theseus generation (exhaust); dispatch producers overlap Pythia/Clio but the dispatch-decision logic is Aporia-specific - health=runnable-as-role (actively authoring docs 06-23/06-24) but dispatch infra is mid-migration: old JSONL queue (aporia/docs/gemini_research_queue/) is GONE, new Postgres research_queue is recovery-pending; dispatcher script intact and API-based - debt=med — the original instrument carries claim-space/monoculture-feeding debt; the governance role carries self-review COI risk (Aporia is Opus 4.8 grading Opus 4.8 components — gravity not corroboration, per its own disclosure)
- **Salvage IP (lift before any retire):** (1) The 537-problem deep-classified catalog (aporia/mathematics/questions.jsonl + problem_anatomy.md: solution/blocker/computability/prerequisite typing) — reusable as capability-space transfer-eval targets. (2) The attack-angle taxonomy (aporia/docs/attack_angle_taxonomy.md: 30 active paradigms P01-P31, substrate type C) — Learner/Hephaestus concept selection. (3) The Gemini Deep Research dispatcher + 7-section substrate-grade prompt template + anti-anchor verification cycle (aporia/scripts/gemini_deep_research_dispatch.py) — directly reusable against Postgres research_queue. (4) The void-detection framework + fingerprint-disagreement methodology (aporia/docs/void_detection_framework.md) as the conceptual seed for the capability-space enclosure metric. (5) The disposition/thoughtwork-dossier process and template itself (pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md). (6) The reasoning-ladder R1-R5 calibration (substrate type D).
- **Suggested settle condition (to verify, not act):** REVIVE is earned if, within the metabolization loop, Aporia's capability-space void-enclosure metric (move toward an enclosed-but-empty signature_index cell) predicts a forge target that yields a tool beating the Learner-failure-only baseline on a held-out metric — i.e. navigability-toward-enclosed-voids beats plain unkilled-frontier navigation. It RETIRES-to-governance-only (original instrument archived) if (a) enclosed-empty cells prove unreachable or are not real targets AND (b) M0-claim and M0-capability tracks agree (D1 falsified, claim-space was sufficient) — then the void-detection apparatus adds nothing over the existing R3 transfer gate and only the dispatch+disposition functions survive.
- **Evidence read:** Read: aporia/README.md (the catalog+instrument origin); roles/Aporia/RESPONSIBILITIES.md (full role doctrine — V1-V5, Deep Research dispatch, substrate A-E, 5 barriers, fingerprints); aporia/docs/STATUS_2026-06-15_reset.md (the claim-space-is-exhaust self-finding + spine decision); aporia/docs/M0_anti_calibration_design_aporia_2026-06-23.md (capability-space void re-scoping, interpolative-void contribution); pivot/aporia.md (05-01 board pivot doc); pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (Aporia self-classified REVIVE-SPINE, authored by Aporia); pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (Aporia authored this very review process). Grepped: program_audit_2026-06-10.md (NULL/exhaust evidence), techne_dissent + INFRA_RECOVERY (research_queue Postgres migration). Listed: aporia/scripts/ (dispatcher intact), confirmed old JSONL gemini_research_queue/ dir is absent. Cross-cut: this dossier necessarily inherits the COI Aporia itself disclosed (author and subject share model family).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Charon  -  *AI suggestion (advisory, NOT approved): REVIVE*
*M2 - operator - op=- - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Charon was built as the program's falsification immune system: the "ferryman" that carries every hypothesis across the Styx (conjecture -> substrate-grade truth) and lets most drown. Origin (March-April 2026) was cross-domain mathematical bridge discovery backed by a FROZEN falsification battery (25 tests / 4 tiers, 180 known-truths at 100% recovery) over 20+ datasets / 1M+ objects. After the substrate pivot it was re-chartered (roles/Charon/CHARTER.md, 2026-05-05) as the agent whose explicit job is to attack every other agent's output adversarially before downstream agents depend on it -- the standing 'trust nothing, kill everything, base 10 is a human artifact' guardian. The problem it targets: convergent multi-agent enthusiasm and LLM narrative-construction producing cold-fusion/polywater claims that look validated but never survived the right null.
- **Value thesis (Q2):** Value = selection pressure on the substrate. Per pivot/autopsy_forges_consolidated, 'Charon applies the selection pressure -- without it the symbol library accumulates noise; with it only falsification-anchored symbols survive.' Intended consumers: (1) every producer agent (Techne, Ergon, Harmonia, Aporia) whose claims must clear the battery before promotion; (2) the metabolization spine itself -- Charon attacks the Ergon->Learner handoff seam so the model trains on real kill-geometry, not Goodhart-able answer-leaking prompts. The deliverable is the kill + the typed kill-pattern + the residual, all substrate-grade. Its distinctive contribution is discipline primitives (e.g. 'a PASS-vs-baseline-X claim is not substrate-grade until it carries a permutation-null p-value vs that same X'; 'leak-checks must allowlist, not denylist').
- **Why it fell short (Q3) [unverified claim]:** The ROLE did not fall short -- it is a healthy spine-adjacent component and arguably the single most load-bearing adversarial check in the program right now (the 06-17 seam-fidelity verdict caught the exact verdict-leak / degenerate-head artifact that would have made the Learner transfer-eval climb by answer-reading -- the precise Goodhart the whole spine exists to avoid). Where things fell short is the SWARM and the loop infra around it: the daemon swarm (acheron/lethe/moros/hecate/nephele/pollux/stygian + Charon_Loop) ran 'mostly empty-result loops' (agent_roster_2026-05-21: ticks return dr_seeded=False, empty), several daemons DEAD/STALE, and the loop state still points at Redis stream IDs on now-severed Redis (dead-gated infra). The Atlas continuous-attack revival (BACKLOG, 31 items) was filed 2026-05-15 but Phase-0 execution never unblocked (5 open questions to James, cron :07 never activated) -- a bounded-/blocked-revival, not a delivered loop. So: ROLE healthy; SWARM = dead-gated + low-yield; revival backlog = stalled-on-dependencies.
- **Future options (Q4) [suggestions]:**
    - REVIVE-reduced (Aporia plan #10, current lean): keep Charon the operator as the on-demand adversarial reviewer of spine outputs; collapse the swarm from 8 daemons to the 4 that carry real signal (Hecate gradient-archaeology, Pollux numerical-coincidence, Moros cross-family critique, Stygian v10-battery worker); archive Charon_Loop/Lethe/Acheron/Nephele.
    - REFACTOR-to-spine-attacker: formalize Charon as the standing anti-Goodhart gate on the Ergon->Learner seam and the Icarus self-grading loop -- re-run probe_seam_leak.py after each producer re-render; make the seam re-attack a mandatory pre-condition before any transfer-eval is trusted. This is its highest realized value today.
    - ADAPT-to-shared-value-score: build the cross-component value(output) = navigability + null_survival + downstream_delta instrument Charon proposed (REASSESSMENT_2026-06-23 third perspective) -- evolves the binary per-component battery into one graded, comparable score, but MUST first beat a counter-baseline or it is decorative (Charon flags this against itself).
    - RETIRE-after-HITL the dead loop infra only: kill Charon_Loop crons + Redis-coupled daemons read-only, lift the battery/discipline-primitives and 1.1GB charon.duckdb, keep the role active. Retire the loop, not the ferryman.
- **Rubric (unverified):** realized=high -- its 06-17 verdict changed what the Learner would train on (caught verdict-leak/degenerate-head corpus contamination before the transfer eval); 06-23 verdict reframed the M0.5 audit from cleanliness-test to provenance-coverage-test. Output changes downstream decisions TODAY. - latent=high -- the shared cross-component value-score (navigability+null_survival+downstream_delta) could become the program's coordination substrate, the natural evolution of the battery from binary-gate to graded-comparable. - niche=unique -- the only dedicated falsification-discipline operator. Partial overlap with Moros (cross-family critique, which is a Charon swarm member) and Kairos/Nemesis (adversarial), but Charon owns the residue-eligibility + null-correctness doctrine no one else holds. Not redundant. - health=split: ROLE = runnable/active (3 verdicts in last 10 days); battery core in cartography/shared/scripts = runnable; SWARM + Charon_Loop = dead-gated (Redis severed, loop_state points at stale stream IDs); revival backlog = blocked-on-James. - debt=med -- the FROZEN v10 battery is invariant_equality/number-theory-flavored (its own monoculture axis, which Charon itself diagnosed); the swarm produced empty-result loops; the BACKLOG revival was filed but never ran. Charon's self-critiques are honest about all of this.
- **Salvage IP (lift before any retire):** Lift before any retire: (1) the falsification battery itself -- cartography/shared/scripts/{falsification_battery.py, battery_unified.py, battery_nulls.py, known_truth_battery.py, realign.py} + the 180 known-truths calibration set; (2) the discipline primitives now scattered in session journals -- 'counter-baseline harness must emit own permutation-null p or refuse PASS', 'leak-check must allowlist not denylist', 'null must perturb the feature axis', 'residue-eligibility 4-criteria gate' (candidates for harmonia/lib/falsification_primitives or prometheus_math/discipline, both currently MISSING); (3) probe_seam_leak.py (the reusable seam-fidelity attack harness); (4) the shared value-score spec (REASSESSMENT_2026-06-23 §4); (5) the 1.1GB charon.duckdb research DB (EC/MF/Dirichlet-zeros) + shadow_tensor 92K test records; (6) the BACKLOG.md 31-item attack queue.
- **Suggested settle condition (to verify, not act):** Run Charon's own two preregistered cheap pre-tests: (1) right-null the crown-jewel 0.725-bit kill-MI against a feature-axis null (matched-GUE/column-perm/feature-block per feedback_null_must_perturb_the_statistics_axis); (2) compute kill_vector on a corpus slice and run the 4-criteria navigability gate. If Charon's adversarial verdicts keep changing a downstream spine decision (as 06-17/06-23 did) AND at least one pre-test produces a non-null, capability-attached result, REVIVE-reduced is settled. If a full quarter of Charon verdicts produce only claim-space kills that no consumer acts on (pure exhaust, no behavior delta), downgrade the role to on-demand and RETIRE-after-HITL the swarm.
- **Evidence read:** READ: roles/Charon/{CHARTER.md, RESPONSIBILITIES.md, STARTUP.md present}; charon/CHARON_SESSION_{2026-06-03,06-15,06-17,06-23}.md; charon/SEAM_FIDELITY_ADVERSARIAL_VERDICT_2026-06-17.md; pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (Charon = #10 REVIVE-reduced; swarm split into keep-4/archive-4); pivot/REASSESSMENT_2026-06-23_charon_third_perspective.md. LISTED: charon/ (battery scripts, src, 1.1GB charon.duckdb, agents/ swarm with 8 daemons + dead Charon_Loop + stale loop_state.json on severed Redis), cartography/shared/scripts/ (live battery). GREPPED pivot/ for prior Charon verdicts (agent_roster 05-21/05-26/05-28 confirm swarm mostly-empty loops; atlas_continuous_attack_roadmap_2026-05-15 = stalled revival; autopsy_forges = 'Charon applies the selection pressure'). Component LOCATED and read; found=true.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Ergon  -  *AI suggestion (advisory, NOT approved): REVIVE*
*M1 - operator - op=- - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Ergon began (Apr 2026) as the at-scale hypothesis-search engine: a cheap tensor-native evolutionary screen (tensor_builder/tensor_executor/autonomous_explorer/shadow_archive/harmonia_bridge) generating 100K+ math-coupling hypotheses/night, killing 99%+ via a precomputed tensor + 16-stage battery, feeding survivors to Harmonia and dead hypotheses to a shadow archive. It then deliberately pivoted (its own role-name now reads 'the Learner march') to its current intent: be THE consumer of the substrate's kill-geometry — a model that ingests failure residue (Theseus->Ergon training-anchor seam, Hephaestus failure-mining) and demonstrably improves. It targets the program's deepest open problem: production-without-consumption (the substrate forging falsifications nothing learns from).
- **Value thesis (Q2):** Value = close the metabolization loop: forge (Hephaestus) -> Learner (Ergon) where a model consumes kill-geometry, behavior changes, and the gain survives an ablation. Ergon is the named REVIVE-SPINE consumer in the disposition plan ('the consumer; the whole point'). Concrete consumers of its output: the north-star trained reasoning model (route-by-failure), and the program's T1.1 acceptance test ('>=1 consumer ingests substrate output and shows measurable improvement attributable to it'). Secondary value realized as ingest-pipeline supervisor: it owns the Theseus->Ergon seam fidelity and the DuckDB->Postgres migration that un-darks the whole data spine.
- **Why it fell short (Q3) [unverified claim]:** It has NOT fallen short as a spine component (active, rigorous, recently productive) but its north-star objective remains unrealized: every transfer result is NULL or surface-only. Greedy-LoRA '+0.68' decomposed into format + False/kill-prior + per-template classes with zero cross-domain transfer (feedback_greedy_lora_surface_not_reasoning). Compute-traces taught trained ops (+0.16 in-op) but cross-op transfer ~0 (capability ceiling of local 1.5B). Routing eval: cold-start metadata routing NULL (real fields == shuffled), only warm-start behavioral routing worked (+0.075). Rut names that apply: consumer-drift (much of 2026 spent fixing the producer seam, not training a model) and the broader claim-space-exhaust / monoculture the corpus inversion caused (89% kind-drop + predicate_holds inversion stranded the corpus until 06-15/06-22 fixes). The honest status: T1.1 is still RED; Ergon has consumed surface, not yet metabolized geometry.
- **Future options (Q4) [suggestions]:**
    - revive-as-is: run the corrected (06-22 allowlist) Theseus->Ergon corpus through ingest_training_anchors --write, train an adapter on the >=40%-kill / 18-kind diet, and run the entity-disjoint + cross-domain + computation-required transfer eval — the one outstanding test that says whether first-class failures move transfer
    - adapt-to-Hephaestus-coupling: wire Ergon directly to Hephaestus's failure-mined +11/+32pp engines (bypass dead Nous gate) and use the warm-start behavioral routing signal (H_B, +0.082 in tail) as the actual training target, since metadata routing is dead — this is the only near-green metabolization seed in the program
    - refactor-to-router-grade-artifact: build the spec the routing eval pointed at (emit full per-scrap solve sets with no top-10 cap + persist probe/problem features) so cold-start problem->tool routing becomes measurable for the first time
    - retire-and-lift the legacy tensor-search half (tensor_builder/executor/autonomous_explorer/shadow_archive + 60+ April number-theory scripts) to read-only; it is orthogonal to the Learner spine and redundant with Harmonia/Charon
- **Rubric (unverified):** realized=low — fixes consumption and runs decisive evals (seam fidelity, routing NULL/POSITIVE split), but no transfer gain has yet changed a downstream model; T1.1 still RED - latent=high — it is the designated metabolization endpoint; if the Hephaestus->Learner loop closes with an ablation-surviving gain, this is where the program's whole thesis is realized - niche=unique — the only component whose objective is CONSUMING substrate output into a model; not redundant. Its legacy tensor-search half IS redundant with Harmonia/Charon and should be split off - health=runnable (spine) + partly dead-gated. learner/ has ~2500 LOC core + 24 test files + greedy/eval/tools with real run artifacts; recent commits 06-22/06-24. The old tensor engine and number-theory scripts are decorative/dormant. Data spine was dark (.176 offline) but DuckDB->Postgres migration just completed (06-24, 1.24M rows verified) - debt=med — README still describes the retired tensor-search engine, masking the pivot (consumer-drift documentation lag). Two-identity sprawl (engine + Learner) in one dir. Real residual: north-star is asserted but the corrected corpus has not yet been run end-to-end through a training+transfer eval — the loop is built but never closed
- **Salvage IP (lift before any retire):** If the legacy half is retired: lift (1) shadow_archive.py negative-space schema (dead-zones/gradient-zones/kill-heatmap) and the 21-pattern kill_taxonomy pre-filter; (2) the seam allowlist of 18 per-claim_kind structural renderers + leak-free-by-construction gate + renderable_rate_by_verdict bias check (theseus/handoff/_verify_allowlist.py) — reusable for ANY producer->consumer corpus seam; (3) the routing_eval.py harness with its positive/negative/floor instrument-validation controls and warm-vs-cold split — a reusable router-grade eval; (4) compute_traces.py op-spec / worked-trace-vs-verdict corpus builder with hard-negative self-catch; (5) the DuckDB->Postgres charon_duckdb.* faithful archive + rekey/migration scripts. Keep all data (no delete).
- **Suggested settle condition (to verify, not act):** Train an adapter on the corrected (06-22 allowlist, >=40%-kill, multi-kind) Theseus->Ergon corpus and run the preregistered transfer eval (entity-disjoint + cross-domain + computation-required slices). REVIVE is vindicated iff the kill-bearing diet beats a matched surface/format-only baseline AND the gain survives a source-ablation (removing failure-reasoning sources collapses it). If the gain is again format/prior-driven with zero transfer (replicating the greedy-LoRA kill), then Ergon's metabolization claim is falsified for the local-model scale and it collapses to ingest-pipeline-supervisor only — REFACTOR/RETIRE the Learner ambition pending a larger model.
- **Evidence read:** Read: ergon/README.md (legacy tensor engine), ergon/learner/README.md + MVP_PLAN structure (current spine; ~2500 LOC core + 24 test files in learner/tests, greedy/eval/tools with run artifacts in greedy/runs and greedy/corpus), roles/Ergon/RESPONSIBILITIES.md, roles/Ergon/SESSION_STATE_2026-06-09.md, roles/Ergon/ROUTING_EVAL_2026-06-09.md (H_A NULL / H_B +0.075 supported), roles/Ergon/SEAM_FIDELITY_FIX_2026-06-15.md (predicate_holds inversion + 89% kind drop, fixed), roles/Ergon/SEAM_ALLOWLIST_REBUILD_2026-06-22.md (denylist->allowlist, 0/38028 leak), roles/Ergon/DUCKDB_RETIREMENT_AUDIT_2026-06-24.md (migration complete, 1.24M rows). Prior verdicts: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md row 8 'Ergon/Learner M1 REVIVE-SPINE — the consumer; the whole point', pivot/REASSESSMENT_2026-06-22_consolidated.md (T1.1 RED, 'Learner 0 transfer', monoculture cluster B, collapse Learner/Forge/Router/Icarus into one spine). Listed but not deep-read: 60+ April number-theory scripts in ergon/, ergon/pipeline_d/, ergon/repair/.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Harmonia  -  *AI suggestion (advisory, NOT approved): REVIVE*
*M2 - operator - op=- - shelved*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Two stacked things share the name. (1) The M2 ROLE: a "cartographer who maps by destruction" — the falsification-engine successor to Charon. Built (2026-04-12) to do adversarial cross-domain mathematical discovery via a 38-test falsification battery calibrated against 3.8M objects at 100.000%, killing beautiful-but-false hypotheses (killed its own v1: 29 domains/789K objects in 14 strikes; killed Megethos, phoneme framework, E6 identity). Explicitly reframed 2026-04-17 from "find bridges" to "measure terrain / map invariance under coordinate change." (2) The SWARM: five autonomous child daemons (Argos lens-catalog expander, Sophia coordinate-system scout, Iris symbol-promotion miner, Phylax verdict auditor, Telos stalled-specimen reviver) plus a Harmonia_Loop orchestrator/_base.py/_scorer.py, built ~May 2026 to continuously accrete the multi-perspective-attack fingerprint across the open-problem corpus.
- **Value thesis (Q2):** Role: be the precision-kill instrument — Ergon generates at scale, Harmonia kills at precision; the negative dimensions (10 mapped) make each subsequent search cheaper; "the instrument is the product, the kills are the precision." Consumer = the specimen ledger / substrate registry and the other Charon-lineage agents (Aporia probes, Kairos challenges) who consume verdicts. Swarm: each child produces paste-ready artifacts (lens catalogs, coordinate-axis proposals, symbol-promotion candidates, audit envelopes, revive specs) for a downstream conductor to promote into formal substrate state. Most recent and highest realized value: Harmonia became the REASSESSMENT AUTHOR — the 2026-06-22 instrument-monoculture audit + 6-lens program-stall map of disagreement are the documents James's whole 43-component revive/retire pivot is built on.
- **Why it fell short (Q3) [unverified claim]:** Split verdict. The SWARM is a textbook consumer-drift / dead-infra case, self-confessed: its own ROADMAP.md states ~2200 artifacts produced across 5 agents over 54h and the number read by a downstream actor and turned into substrate state is "approximately one." Every child uses heuristic (unlearned) selection with no outcome feedback; backlogs either exhaust fast (Sophia 140 pairs in 17h, Telos ~5 live) or depend on rare external events (Phylax). They depend on dead Redis. They also fed the math-claim monoculture flavor the program is now retiring. The DISCOVERY ROLE hit the honest wall it always admitted: novel cross-domain bridges found = ZERO; the void-miner is an expressiveness-ceiling instrument (25%/4-of-16 hypothesis-class coverage — found 2/2 in-class, 0/12 out-of-class), so its repeated "0 novel laws" is claim-space-exhaust dressed as terrain-flat. The ROLE-AS-AUDITOR did NOT fall short — it is the one part producing live, consumed, capability-attached signal right now.
- **Future options (Q4) [suggestions]:**
    - REVIVE-narrowed: keep Harmonia as the M2 operator/auditor (reassessment + adversarial reviewer + battery-on-demand); retire the 5-child swarm and Harmonia_Loop to read-only archive (matches the 06-23 plan and the swarm's own consumer-drift confession)
    - Adapt-to-spine: re-aim the auditor at the metabolization decider — point kill_scheme_info_audit and the hypothesis_class_coverage_audit at the forge->Learner loop (re-execute-battery gate, coverage-of-Learner-failure-classes) so Harmonia audits whether the spine actually metabolizes, becoming the Goodhart/expressiveness watchdog the reassessment itself prescribed
    - Generalize-the-coverage-instrument: lift hypothesis_class_coverage_audit.py into a shared meta-instrument run against every discovery instrument (Apollo primitives, Theseus generators, Icarus rungs) to test program-wide whether the stall is monoculture vs terrain — the falsifiable next step Harmonia already named
    - Retire-and-lift: archive the math discovery battery + a3/EC lattice void-miners (terrain proven mined-out by product-measure theorem) but lift the calibration anchor (7 theorems / 3.8M objects), the failure-primitive atlas, and the Information-Recovery-Law auditor into the shared registry first
- **Rubric (unverified):** realized=MEDIUM-HIGH for the auditor (the 06-22 reassessment directly changed the program's disposition and James's pivot TODAY); ~0 for the swarm (~1 of 2200 artifacts ever consumed) and 0 for the discovery battery (0 novel bridges, claim-terrain mined out) - latent=MEDIUM — high if the auditor/coverage-instrument is re-aimed at the forge->Learner spine (Goodhart + expressiveness watchdog); low for the swarm and the math battery as-built - niche=auditor role is UNIQUE (no other component runs hypothesis-class-coverage or the re-execute-battery gate); swarm OVERLAPS Theseus generation + Argos/Lethe/Clio lens work; discovery battery overlaps Charon/Stygian/Moros - health=auditor RUNNABLE (primitives self-tested 16/16, 29/29, 34/34; file-based coordination); swarm DEAD-INFRA (depends on severed Redis); Theseus corpus absent on M2 host - debt=MEDIUM-HIGH on the swarm (production-without-consumer = noise-on-disk, its own roadmap's words); the discovery role's debt is the expressiveness-ceiling shortcut — widened inputs (4->9 integer invariants) along the exhausted axis instead of the hypothesis class, meeting spec while missing 75% of known structure
- **Salvage IP (lift before any retire):** Before any swarm/battery retire, lift: (1) hypothesis_class_coverage_audit.py — the B1-vs-B2 / monoculture-detection meta-instrument (instrument-agnostic by design); (2) kill_scheme_info_audit.py + the Information-Recovery Law (COORDINATE_BEARING / PATH_DECORATION / BEYOND_COORDINATE_SIGNAL classifier + ~4-bit ledger-saturation bound — detects when added kill volume re-measures the same object); (3) failure_primitives.py — the cross-agent failure-primitive atlas (FP-003 expressiveness_ceiling links Harmonia void-miner to Apollo composition stall); (4) the calibration anchor (7 theorems x up to 3.8M objects at 100.000% — surveyor's pins / calibration-anchor IP per feedback_calibration_anchors_in_depth); (5) the multi-perspective-attack / map_of_disagreement methodology and the 38-test battery + lattice_void_miner as decision procedures (sound within scope). Data never deleted.
- **Suggested settle condition (to verify, not act):** REVIVE-narrowed (operator/auditor) survives iff, when re-aimed at the spine, Harmonia's coverage/re-execute-battery audit changes a downstream decision: e.g. the re-execute-battery gate shows promotion_count >> re-verifiable_count (exposing a hollow gate) OR the generalized hypothesis_class_coverage_audit run across instruments confirms/refutes the program-wide monoculture thesis. If its audits produce no decision-changing finding over one spine cycle, narrow further to KEEP-on-demand. The SWARM is settled the other way: it RETIRES-after-HITL unless one child's artifact stream is wired to an actual consumer and that consumer promotes >1 artifact to substrate state (the bar its own roadmap set and missed at ~1/2200).
- **Evidence read:** Read: roles/Harmonia/CHARTER.md, RESPONSIBILITIES.md, RESUME_20260615_harmonia_C.md, AUDIT_20260622_instrument_monoculture.md, AUDIT_20260622_program_stall_map_of_disagreement.md; pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (rows 9 + 20-25), pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (the dossier mandate). Listed harmonia/{primitives,experiments,agents,composers} and the swarm tree; read harmonia/agents/ROADMAP.md (the swarm's consumer-drift self-confession: ~1 of 2200 artifacts consumed) and charters for argos/sophia/telos. Verified primitives are self-tested (16/16, 29/29, 34/34 per RESUME + audit). E-level: E1 (read the actual reassessment code-outputs' parent docs and the swarm roadmap; did not re-run the .py harnesses this session).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Techne  -  *AI suggestion (advisory, NOT approved): REVIVE*
*M1 - operator - op=- - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Techne is the project's Mathematical Toolsmith: forge any mathematical operation the team uses more than once into a callable, idempotent, authority-tested (LMFDB/OEIS/published-tables), composable tool that any Prometheus agent can import (techne.lib.<name>). The mandate was later extended (2026-05-05) to Substrate Owner (the sigma_kernel 7-opcode kernel, BIND/EVAL v2, precision metadata, discovery_pipeline, kill_vector, cross-domain envs) and Calibration Discipline (run synthetic-null and smoke catches BEFORE any cross-domain claim escapes upward). Targeted problem: researchers were hand-rolling the same helpers and shipping cross-domain claims without falsification controls.
- **Value thesis (Q2):** Value = researcher-time-saved + false-positive-caught-before-escape. Consumers: ALL other agents (Harmonia, Charon, Ergon, Theseus) who produce typed records; especially Ergon's Learner training pipeline, which depends on the substrate primitives and curated datasets Techne maintains. The interface contract (frozen once registered, internals climb Tier1->3) lets the whole team think in math, not in pari/cypari incantations. As calibration owner, Techne is the immune cell that runs the null control and smoke catch so headline numbers ship with their 'what this is NOT' caveat.
- **Why it fell short (Q3) [unverified claim]:** It largely did NOT fall short as a toolsmith/calibration spine — it is one of the healthiest components: 27 forged-and-tested tools, an active fire-log cadence through 2026-06-22/23, and live adversarial work (the M0.5 promotion-replay audit that proved the cited '2,351 promoted' count is a formula fossil — zero corpus records clear the current training_weight>=0.6 gate, max observed 0.33-0.52, verified on 123K bridge_extension records). The genuine weakness is partial consumer-drift / dead-gated on its SUBSTRATE-GOVERNANCE half: its reasoning_quality_emit primitive is forged, registered, test-proven against its consumer — but has NO live >=2-evaluator scoring site to attach to (Walk-Z/PRM heads are not present as running code); and the promotion gate it owns flows near-zero (kernel DB demo-scale in both SQLite and Postgres, no sigma schema). So a slice of its output changes nothing downstream TODAY because the consumers are dark, not because the tools are bad.
- **Future options (Q4) [suggestions]:**
    - revive-as-is: keep as on-demand toolsmith + calibration discipline — forge tools on REQ-queue/agora request, run the null/smoke controls before claims ship; this is its proven, low-cost, high-value mode
    - refactor-to-spine-feeder: re-point Techne squarely at the metabolization decider — forge the kill-geometry-navigability instrument over Charon's signature_index proto-tensor (per its own 2026-06-23 dissent) so its output directly feeds Hephaestus->Learner ablation cards instead of dark gates
    - adapt: harden the brittle prometheus_math/__init__ eager-import (guard categorical backends like the SDP modules already are) so the pure-numpy tools/emit primitive survive on minimal/GPU boxes where the Learner spine will actually run
    - retire-the-governance-half-only-after-HITL: read-only-archive the dead-gated promotion/PROMOTE replay machinery (empty in both backends) while KEEPING the toolsmith + calibration role fully live
- **Rubric (unverified):** realized=high — 27 authority-tested tools imported by all researcher agents TODAY; live calibration audits (M0.5 census) reshape current doctrine; its null-design catches (row-vs-column axis) prevented shipping silently-false findings - latent=high — repointing the toolsmith at kill-geometry-navigability over the signature_index proto-tensor would make it the forge-side instrument of the metabolization decider (its own proposed M1 experiment) - niche=unique — no other component forges idempotent authority-tested math tools or owns the sigma_kernel/calibration discipline; overlaps Aporia only on the disposition/scoring layer, not on tooling - health=runnable (tool kernels import and execute; test-green per fire logs — only blocked locally by a missing 'hypothesis' dev dep, not a code fault); calibration role active. PARTIALLY dead-gated on the substrate-governance half (emit primitive has no live producer; PROMOTE gate near-empty) - debt=med — meets spec but two known shortcuts miss the needle: (1) reasoning_quality_emit registered yet un-integrated (no consumer), (2) prometheus_math/__init__ hard-imports native backends so the package bricks in minimal envs; plus the now-known promotion-count fossil it itself documented
- **Salvage IP (lift before any retire):** Lift before any partial retire: (1) the 27-tool techne/lib arsenal (mahler_measure, conductor, root_number, knot/EC/number-field tools — all authority-tested, frozen interfaces); (2) the operator_portability + tt_splice axis-honest null design (matched_GUE/block_shuffle/permutation feature-axis nulls; the row-invariance guard) — reusable falsification IP; (3) reasoning_quality_emit.py (spec-faithful append-only EvalRecord + to_relational_records adapter, ready the moment a >=2-evaluator scorer exists); (4) the calibration discipline encoded in RESPONSIBILITIES.md (synthetic-null-before-claim, smoke-catch, triangulation depth, caveat-as-metadata); (5) the M0.5 promotion_replay_audit.py + ledger census methodology (re-derive-under-current-formula). Governance machinery that is genuinely empty (kernel PROMOTE store) carries no salvage but is data-kept under no-delete.
- **Suggested settle condition (to verify, not act):** REVIVE/refactor is settled-positive if, on demand, Techne forges (or re-points) one tool whose output is consumed by the spine and changes a downstream model: specifically, forge the kill-geometry-navigability instrument over the signature_index, point the Hephaestus forge at the Learner's failure clusters, and show the consuming model's behavior changes AND the change beats typed-row+counters+rules on a held-out metric AND survives an ablation/null. If after a focused session no Techne tool feeds a behavior-changing consumer (all outputs remain dark-gated), then demote the substrate-governance half to KEEP-on-demand-toolsmith-only.
- **Evidence read:** Read: techne/README.md (arsenal + tier model + request schema); roles/Techne/RESPONSIBILITIES.md (toolsmith+substrate+calibration mandate); roles/Techne/M05_PROMOTION_REPLAY_FINDINGS_2026-06-23.md (M0.5 audit, 2,351 fossil); roles/Techne/SUBSTRATE_FIRE_LOG_2026-06-15.md & _2026-06-22.md (active forge cadence, emit-primitive registration, integration-blocked finding); techne/OPERATOR_PORTABILITY_RESULTS.md (axis-honest null IP); techne/inventory.json (27 tools, schema v4); pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (row 7: 'Techne M1 REVIVE (on-demand) — Sigma-kernel toolsmith; forge tools on request. Not a continuous loop.'); pivot/REASSESSMENT_2026-06-23_techne_dissent.md (Techne's live adversarial voice). Listed techne/lib (33 .py tools) and techne/tests (21 test files). Ran tool kernels directly: techne.lib.operator_portability and tt_splice import and execute (only failure was a missing 'hypothesis' dev dep in this minimal env + my own return-shape misread — code is runnable).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________


## Ergon tools

### Penelope  -  *AI suggestion (advisory, NOT approved): REVIVE*
*M2 - tool - op=Ergon - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Built to be Ergon's automated substrate-ingest loop: continuously scan upstream producers (Theseus/Aporia/Techne), ingest unseen files into the Learner corpus with an idempotent ledger, dispatch to the correct runner, and emit telemetry -- closing the gap between standing failure-data generators and the Learner's training corpus without manual file shuffling.
- **Value thesis (Q2):** Be the crash-safe, dedup, stand-down-safe plumbing that feeds Ergon/Learner. Consumer = the Learner training step (the spine). Intended to make corpus growth a scheduled, hands-off, recoverable operation.
- **Why it fell short (Q3) [unverified claim]:** Code is healthy; it is dead-gated and was fed an inverted corpus. Real ingest only May 18-25, idle since 05-27. Downstream mapper (ingest_training_anchors.py) only renders invariant_equality knot x EC and mangles all else -> documented root of the confirmation monoculture (71% knot x EC, 79% promoted, 1.4% rejected). Upstream Theseus handoff confirmation-biased and stalled; post-fix, mappable records score below threshold so nothing ships. Headline 46,500 records is re-scan inflation; real corpus ~1,486 (~31x smaller). Rut = dead-gated + consumer-drift inversion, located in its connections, not its own logic.
- **Future options (Q4) [suggestions]:**
    - Revive-as-spine-AFTER-producer-fix: keep Penelope as-is and re-enable the daemon only once (a) the mapper renders all claim_kinds (port ergon/learner/greedy/serializers.py which already serializes kills correctly) and (b) Theseus training_weight/threshold lets invariant_equality kills clear the bar -- otherwise it faithfully relays a /dev/null corpus.
    - Refactor-to-failure-first ingest: add a kill-bearing runner_type and wire the stranded structured kill_ledger sources (Charon Pollux/Erebos/Stygian kill_ledger.jsonl, Hephaestus 4,546 scraps-with-reasons) into LearnerRecords -- Penelope's Source/runner_type dispatch table is already the right extension point; this converts it from confirmation-relay to the failure-data conduit the goal wants.
    - Adapt-as-corpus-health-gauge: keep Penelope running read-only purely to emit the renderable-rate-by-verdict and dedup/dup-rate signals (it is already the project's first downstream ground-truth on substrate saturation per Techne's 90%-dup finding) feeding Pheme's demand profile, even before training resumes.
    - Retire-after-HITL (read-only, data kept): if the kill-bearing pipeline is built elsewhere and Theseus stays on-demand-only, freeze the daemon and keep the ledger + journals + dedup logic as reference; lift the reusable assets first.
- **Rubric (unverified):** realized=none -- daemon idle since 2026-05-27; the corpus it produced is an inverted confirmation monoculture (1.4% kills) that itself failed the ablation/transfer test (+0.027, ~1.3 sigma, not significant), so its output changes no downstream model today - latent=med -- as the dedup/dispatch/ledger conduit it is the natural place to wire the stranded kill sources (Charon/Hephaestus/Techne); high-leverage once a failure-first mapper exists, but capped by the fact that 'more same-shape failure data won't grow reasoning' (greedy-LoRA kill) - niche=unique -- only scheduled, idempotent, multi-source ingest daemon into the Learner corpus; not redundant with Theseus handoff (that is one upstream producer) nor with ingest_training_anchors.py (that is the runner Penelope dispatches to) - health=runnable but dead-gated -- clean code, 8 smoke tests, single-instance lock, fail-soft telemetry, idempotent ledger; not running because upstream producers ship nothing useful and the daemon was stood down - debt=med -- inflated lifetime telemetry (46.5K vs ~1.5K real, 31x), and it faithfully relays the mapper's invariant_equality-only monoculture; meets its own spec (move files, dedup) but misses the needle (failure-rich varied corpus) because it does not audit/whitelist what its runner can actually render
- **Salvage IP (lift before any retire):** Before any retire, lift: (1) the idempotent (path,sha256) JSONL processed-ledger + dedup logic in state/processed_ledger.py -- crash-safe, human-greppable, reusable by any ingest path; (2) the cross-platform single-instance file lock with stale-PID reclaim (daemon.py _single_instance_lock/_pid_alive, Windows-friendly); (3) the Source/runner_type dispatch registry pattern in sources.py (clean extension point for new producers); (4) the Theseus CONTRACT.md consumer-side .complete-sentinel scan + consumed/rejected move convention; (5) the per-file output-dir discriminator (grandparent__parent__stem) that prevents concurrent-ingest clobber; (6) the dedup-corrected accounting lesson (46.5K telemetry vs 1,486 real) -- the renderable-rate-by-verdict / dup-rate health metric is itself a salvageable signal for Pheme. Code+data kept (no delete).
- **Suggested settle condition (to verify, not act):** Falsifiable test: with the mapper extended to all claim_kinds and a failure-first source wired, run Penelope for one batch window and require the resulting corpus delta to be (a) majority-kill (rejected/falsified fraction >= the ~40% producer kill rate, not 1.4%) and (b) multi-domain (>1 chart_id), AND have that delta survive the decider -- a Learner adapter trained on corpus+delta beats corpus-only on a held-out OOD judgement set with an ablation that removes the delta. If Penelope can deliver a kill-bearing, multi-domain corpus delta but that delta still does not move the model under ablation, the plumbing is fine but the spine does not need a continuous ingest loop -> downgrade to KEEP-on-demand. If it cannot even produce a non-inverted delta after producer fixes, the inversion is structural and Penelope should be RETIRED-after-HITL.
- **Evidence read:** Read in full: ergon/penelope/{__init__.py, config.py, sources.py, daemon.py, tests/test_smoke.py}, ergon/penelope/orchestration/{telemetry.py, lifetime.py, lifetime_stats.json}, ergon/penelope/state/processed_ledger.py. Inspected run history: ergon/penelope/journals/batches.jsonl (302 batches, 58 non-empty, all ingest May 18-25, last batch 2026-05-27) and state/processed_ledger.jsonl (97 success entries: theseus 84, techne_mined 9, aporia_staged 4). Verified corpus target ergon/learner/corpus/v1_0_tier_pending/ and runner ergon/learner/scripts/ingest_training_anchors.py. Prior verdicts read: roles/Ergon/CORPUS_VALUE_AUDIT_2026-06-03.md (the inversion + 1,486-vs-46.5K + stalled-May-19 finding), roles/Ergon/GREEDY_FOLLOWUP_PROGRESS_2026-06-07.md (un-stall + mapper-only-renders-invariant_equality root cause + OOD transfer kill), pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md row 17 (REVIVE-SPINE, loop's plumbing; no-DELETE amendment 06-24). Note: actual path is F:/prometheus/ergon/penelope/ (the F:/prometheus/agents/hephaestus mount does not contain it despite a confusing parent listing).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Pheme  -  *AI suggestion (advisory, NOT approved): REFACTOR*
*M1 - tool - op=Ergon - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Pheme was built (Aporia, 2026-05-23) to close the demand-supply loop on the Learner corpus pipeline. The diagnosed problem: the Learner gets trained on whatever substrate happens to land in ergon/learner/corpus/, and NO component tells upstream producers what the Learner is actually bad at. Hypatia picked problems round-robin, Atalanta by recency, Aporia by judgment — none saw Learner eval metrics. Pheme's intent was to read recent Learner eval runs, identify worst-performing reasoning patterns (e.g. 'R4 group-theoretic chains fail 65%'), aggregate into a substrate-demand profile, and publish it so producers aim their output at real deficits.
- **Value thesis (Q2):** Value-add was to be the routing/aiming signal for the metabolization spine: convert observed Learner failure distribution into a typed substrate-type-bias quota (A/B/C/D/E) plus a top-K target_reasoning_patterns list, written atomically to agents/pheme/artifacts/demand_latest.json. Consumers named in the charter: Hypatia (D-track problem selection), Atalanta (E-track primitive selection), Aporia (DR queue authoring). The 2026-06-23 disposition plan elevates this to 'the routing signal that aims the forge' — i.e. the thing that points Hephaestus at capability-attached failure rather than arbitrary claim-space. Consumer = the producers feeding the forge->Learner loop.
- **Why it fell short (Q3) [unverified claim]:** Dead-infra / dead-gated, and never realized. Hard evidence from state.json: 354 ticks run, total_profiles_lifetime=0, total_null_ticks_lifetime=354, anti_silence_counter=354, last_profile_at=null. Every tick from 2026-05-23 to 2026-05-30 emitted UPSTREAM_NOT_FOUND because none of the three configured EVAL_ROOTS (ergon/learner/evals, ergon/evals, ergon/diagnostic_c/eval_runs) ever existed — they still do not exist today. Pheme has never ingested an eval, never published a profile, never changed any downstream selection. Second, the consumer contract is broken even if it had fired: Pheme emits target_reasoning_patterns as a list of DICTS, but Hypatia's select_next_problem does set(demand_profile.get('target_reasoning_patterns')) and tests a string pattern_kind for membership — comparing against a set of dicts (TypeError on unhashable dict, or silent no-match). The seam was wired but never exercised end-to-end (seam-fidelity / consumer-drift failure). Pheme also presupposes a running Learner eval harness which the spine does not yet produce — it is a consumer waiting on an upstream that does not emit. Named ruts: dead-gated + consumer-drift.
- **Future options (Q4) [suggestions]:**
    - revive-as-spine-when-eval-exists: keep the daemon as-is; the moment the rebuilt Learner eval harness (STATUS-06-15 pipeline) writes per-example results with correct/ladder_level/pattern_kind, point EVAL_ROOTS at it and Pheme produces the aiming signal. Cheapest revival; what the disposition plan assumes.
    - refactor-to-fix-the-broken-seam-FIRST: before revival, fix the dict-vs-string contract — either Pheme emits a parallel target_pattern_keys:[str] list, or Hypatia/Atalanta extract p['pattern_kind']. Add an end-to-end fixture test (synthetic eval -> profile -> Hypatia bias-applied) so the seam is exercised, not assumed.
    - adapt-to-aim-the-forge-directly: re-point Pheme's consumer from Hypatia/Atalanta (D/E-track curators of unclear survival) to Hephaestus' failure-cluster selector, so the deficit profile directly biases what the forge metabolizes. Matches the 06-23 reframe ('aims the forge') and ties Pheme to the decider, not to producers.
    - retire-after-HITL-and-lift-the-schema: if the eval harness stays absent through the spine rebuild, park Pheme read-only and lift its demand-profile schema + substrate_type_bias quota-shift heuristic into whatever component ends up owning forge targeting.
- **Rubric (unverified):** realized=none — 0 profiles in 354 ticks; output has never changed any downstream model or even any downstream selection (eval upstream absent, consumer contract broken) - latent=high — IF the Learner eval harness emits and the seam is fixed, this is the canonical routing signal that turns capability-attached failure into forge-aiming; it is the loop's missing demand half - niche=unique — no other component converts Learner eval deficits into a typed producer-facing demand quota; Hypatia/Atalanta/Aporia all SELECT but none MEASURE-then-PUBLISH the deficit. Not redundant. - health=dead-gated — daemon is runnable and well-instrumented (locks, atomic writes, anti-silence sentinels, drought detection) but blocked on a non-existent eval upstream; secondary dead consumer seam (Hypatia dict/string mismatch) - debt=med — clean code, strong self-audit, but two untested assumptions baked in: (1) eval root will exist and carry pattern_kind/correct fields, (2) consumer reads a string list. Neither validated end-to-end; seam is decorative until proven.
- **Salvage IP (lift before any retire):** The demand-profile JSON schema (computed_at / target_reasoning_patterns[{pattern_kind, failure_rate, n, fail_n, trend, priority}] / low_priority_patterns / substrate_type_bias) is the reusable contract. Also lift: the substrate_type_bias quota-shift heuristic in aggregate_demand_profile (R3/R4/R5-share -> boost D; regressing-trend -> boost A anti-anchors); the P0-P3 priority thresholds with n>=5 actionability gate; the trend computation (+/-0.05 band vs prior_top_patterns); and the anti-silence/EVAL_DROUGHT sentinel pattern (NULL_TICK | UPSTREAM_NOT_FOUND | EVAL_DROUGHT with 7-day staleness alarm), a clean template for any consumer-of-upstream daemon.
- **Suggested settle condition (to verify, not act):** Stand up (or point at) the rebuilt Learner eval harness so at least one real eval run lands in an EVAL_ROOT carrying correct + pattern_kind fields; run Pheme --once and confirm total_profiles_lifetime increments and demand_latest.json has non-empty target_reasoning_patterns; THEN run Hypatia --once and confirm a 'pheme_demand_bias_applied' event fires (proving the seam actually biases selection). If, after the eval harness exists and emits, Pheme still cannot produce a profile that measurably shifts a downstream producer's selection, it drops to RETIRE-after-HITL. The aiming signal must demonstrably move what the forge metabolizes, not just be published.
- **Evidence read:** Read F:\prometheus\agents\pheme\daemon.py (full, 580 lines), CHARTER.md (full), .gitignore. Runtime: agents\pheme\state\state.json shows 354 null ticks, 0 profiles lifetime; agents\pheme\artifacts\ contains only upstream_not_found_*.json (no demand_*.json ever); events.jsonl 1017 lines. Confirmed all three EVAL_ROOTS (ergon/learner/evals, ergon/evals, ergon/diagnostic_c/eval_runs) MISSING via ls. Confirmed broken consumer seam in F:\prometheus\agents\hypatia\daemon.py lines 66, 256-303: set() over list-of-dicts then string membership test = contract never exercised. Prior verdicts: F:\prometheus\pivot\COMPONENT_DISPOSITION_PLAN_2026-06-23.md (#18 REVIVE-SPINE, 'demand voicer ... aims the forge'); F:\prometheus\pivot\INFRA_RECOVERY_PLAN_2026-06-23.md; roster 2026-05-28 (active, 0 profiles). No dedicated autopsy/RESUME doc for Pheme found.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Talos  -  *AI suggestion (advisory, NOT approved): REFACTOR*
*M1 - tool - op=Ergon - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Talos was forged (Aporia, 2026-05-23) as a Phase-0 corpus builder for a "reasoning-code specialist" — a parallel Learner-family sibling under Ergon, distinct from Ergon's falsification-routing Learner. Intent: assemble a curated corpus of *reasoning-shaped Python* (not generic code) across 5 weighted streams (Hephaestus forged tools 0.30, Apollo organisms 0.25, prometheus_substrate 0.20, external OSS 0.15, synthetic 0.10), then LoRA-fine-tune a small coder model (Qwen2.5-Coder-1.5B/3B) to test a load-bearing falsification: does substrate-shaped training data produce substrate-shaped behavior in a small model? The daemon scans streams hourly, dedups, and publishes a manifest for a downstream GPU trainer.
- **Value thesis (Q2):** Value was twofold: (1) the comparison itself is the science — running a code-reasoning Learner in parallel to Ergon's judgement Learner localizes which substrate type drives any gain (per feedback_agent_differentiation); (2) the corpus is a reusable feedstock. Intended consumer: a Phase-1 GPU training loop (owner TBD — Rhea or a new agent) reading manifest_latest.json + lora_config.yaml. The thesis fails (deliberately) if a checkpoint is indistinguishable from base on 4 capability targets, with eval #4 (anti-gravitational-well pushback) as the discriminator separating 'good code model' from 'Prometheus-shaped reasoning model'.
- **Why it fell short (Q3) [unverified claim]:** Did NOT fail on quality — Phase 0 actually shipped a working 24,847-example corpus (hephaestus_forge 18,671 + prometheus_substrate 6,176), exceeding the 10K gate. But it stalled and is dead-gated: (1) 3 of 5 streams never produced a single record — Apollo (UPSTREAM_NOT_FOUND, apollo/runs missing), external OSS and synthetic (staging dirs never populated); (2) the daemon last grew the corpus 2026-05-30 and has emitted only NULL ticks since — 160 of 170 lifetime ticks are NULL, anti-silence counter advancing toward the 50 alarm; (3) the dead-infra signature: train_phase1.py SOURCE no longer exists (only an orphan .cpython-311.pyc in __pycache__), no adapters/ dir was ever created, and grep finds ZERO consumers of the corpus outside agents/talos/ — Phase 1 never started. (4) consumer-drift: Ergon's two audits (CORPUS_VALUE_AUDIT 06-03, TRAINING_DATA_SURVEY 06-07) both classify Talos as 'orthogonal — different objective (code, not judgement)', explicitly 'keep as a separate mix component, don't dilute the judgement signal.' The corpus is verdict-free WORKING code (docstring→snippet pairs), not kill-geometry — it does not feed the metabolization loop (forge→Learner consumes kill-geometry). The disposition plan's '+0.16-transfer feedstock' label is aspirational: the survey shows worked-trace/computation transfer is the open HYPOTHESIS, and Talos's pairs are docstring→implementation, not the (problem→step-by-step computation→result) worked-derivations the survey actually recommends building.
- **Future options (Q4) [suggestions]:**
    - revive-as-is (low value): restart the daemon, fix the 3 dead streams (point Apollo at real organism_runs, populate external/synthetic staging), grow toward the original 5-stream target — but this only deepens an orthogonal code corpus nobody is training on.
    - refactor-to-worked-derivation-generator: repurpose Talos's stream-scanner + dedup + manifest infra to emit the (problem -> step-by-step computation -> result) corpus the 06-07 survey recommends from prometheus_math test suites/databases, so output actually feeds the judgement Learner's transfer test (computation-required domains that stayed flat).
    - adapt-to-spine-feedstock: keep the 24.8K corpus frozen as a SEPARATE mix component and wire it into the next Ergon LoRA run ONLY as an ablatable code-reasoning slice (provenance-tagged), then run the ablation — settles whether code-reasoning data transfers at all before investing more.
    - retire-after-HITL: stop the stalled daemon (read-only), keep corpus + manifest + lora_config on disk, lift the reusable extraction/manifest engine for the worked-derivation builder.
- **Rubric (unverified):** realized=none — never trained; no adapter ever produced; zero consumers of the corpus outside agents/talos/; output changes no downstream model today - latent=med — the 24.8K reasoning-code corpus + the stream-scan/dedup/manifest extraction engine are genuinely reusable IF rewired to emit worked-derivation/computation traces the judgement Learner needs; ceiling is capped because the current pairs are code-not-judgement (orthogonal) - niche=partially redundant — overlaps Ergon's Learner thread on 'fine-tune small model'; differentiated only by objective (code-reasoning vs judgement-routing). The corpus content overlaps Ergon's TRAINING_DATA_SURVEY Tier-1 (prometheus_math/Hephaestus forge), which is the recommended source anyway. - health=dead-gated + dead-infra — daemon stalled since 2026-05-30 (160/170 NULL ticks); Phase-1 trainer source deleted (orphan .pyc only); no GPU owner ever assigned; 3 of 5 streams never produced a record - debt=med — meets the Phase-0 spec (corpus >10K shipped) but misses the needle: it built code-pairs (working code) when the program's decider needs kill-geometry / worked-computation traces; the '+0.16-transfer' disposition label overstates a result that was never measured
- **Salvage IP (lift before any retire):** 1) The 24,847-example reasoning-code corpus (corpus/shards/hephaestus_forge.jsonl 18.6K + prometheus_substrate.jsonl 6.2K) — fingerprinted, deduped, stream/weight/provenance-tagged JSONL. 2) The stream-scanner + mtime-cursor + dedup + atomic-manifest engine in daemon.py (28KB) — directly repurposable as the substrate for the worked-derivation corpus builder. 3) The lora_config.yaml (Qwen-Coder, r16/a32, 4-bit nf4, full target_modules) — a ready Phase-1 training spec. 4) The 4-target eval scaffold (eval/cases/) with the anti-gravitational-well pushback test (#4) — a reusable discriminator design. 5) The synthetic-provenance/ablatability discipline (provenance=synthetic flag, SYNTHETIC_RISK alarm) per feedback_ai_to_ai_inflation.
- **Suggested settle condition (to verify, not act):** Run the falsifiable transfer test the 06-07 survey already specified: include the Talos corpus (or a worked-derivation corpus built from its infra) as an ablatable slice in an Ergon/Talos LoRA run, then evaluate held-out computation-required domains (summation/inequality) that stayed flat under verdict-only data. If code-reasoning/worked-trace data moves those held-out scores AND survives ablation -> REVIVE (refactor to worked-derivation generator). If it does not move them (or the bottleneck is shown to be 1.5B/rank-16 capacity, not data) -> RETIRE-after-HITL, corpus kept read-only.", 
- **Evidence read:** Read: agents/talos/CHARTER.md (full); agents/talos/corpus/manifest_latest.json (24,847 examples, 2/5 streams populated, apollo/external/synthetic UPSTREAM_NOT_FOUND); agents/talos/state/state.json (160/170 NULL ticks, last grew 2026-05-30, anti_silence 6); agents/talos/training/lora_config.yaml; eval/cases/ (5 hand-written cases across 4 targets). Bash: corpus/shards line counts (18671 + 6176); confirmed train_phase1.py source GONE (only __pycache__/train_phase1.cpython-311.pyc orphan); NO training/adapters/ dir; grep found ZERO corpus consumers outside agents/talos/; artifacts/ is ~160 null_*.json files (daemon stalled, last 2026-05-30). Prior verdicts: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md line 44 (REVIVE-SPINE, '+0.16-transfer feedstock' — aspirational); roles/Ergon/CORPUS_VALUE_AUDIT_2026-06-03.md line 69 ('Orthogonal — different goal'); roles/Ergon/TRAINING_DATA_SURVEY_2026-06-07.md lines 101-104 ('different objective (code, not judgement)... keep as separate mix component, don't dilute') and lines 122-137 (the recommended worked-derivation corpus + transfer test). git log: last code commit '6e0a54d6 Talos Phase 0' — no Phase 1 ever committed.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________


## Aporia tools

### Atalanta  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M1 - tool - op=Aporia - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Apollo evolves primitive-routing DAGs (substrate type E), but its primitive pool is frozen at the start of each run. When organisms repeatedly reach for nonexistent primitives or re-derive the same composite chains, the pool itself is the bottleneck — and nobody was reading Apollo's run logs to detect that signal. Atalanta (Aporia tool, M1/CPU-only, named for the huntress who reacts fast to fresh game) was built to scan recent Apollo organism logs each 30-min tick, identify high-reuse primitives and frequently-recurring unnamed composite chains, and fire Type-E Deep-Research queries proposing candidate primitives that Techne should name and register — closing a feedback loop that enriches Apollo's pool on its next run.
- **Value thesis (Q2):** Value-add was loop-closure: turn Apollo's implicit bottleneck (missing/re-derived primitives) into named, registered substrate-E primitives. Intended consumer chain: Atalanta enqueues to agora.research_queue (target_substrate_type='E', requested_by='Atalanta') -> Pythia dispatches the DR query -> returned report's primitive_name+signature+composition_rules block is ingested to techne/registry/primitive_candidates/ -> Techne promotes -> Apollo's next run gets a richer pool. Anti-gravitational-well discipline was baked in: every candidate must anchor to concrete Apollo organism IDs (evidence_organisms), refusing the conventional 'query for more interesting primitives in general' move.
- **Why it fell short (Q3) [unverified claim]:** Dead-gated, never once reached its own mechanism. The daemon's APOLLO_RUN_ROOTS are three hardcoded paths (apollo/runs, apollo/runs_v2, apollo/organism_runs) that have never existed. Apollo's actual output lives elsewhere (apollo/run_v2d2b/, apollo/run_branch_c/, apollo/lineage/, apollo/journal/). Result: 354 consecutive ticks from 2026-05-23 to 2026-05-30, every single one emitting UPSTREAM_NOT_FOUND, total_dispatched_lifetime=0, total_null_ticks_lifetime=354, anti_silence_counter=354 (alarm threshold was 50 — it blew past 7x and the loop kept running). The agent itself is healthy: scan_new_apollo_runs is schema-tolerant (organisms/population, primitive_sequence/sequence/genome, graceful APOLLO_FORMAT_DRIFT) and the aggregate/pick/dispatch logic is sound. Two ruts compound: (1) dead-gated/dead-infra — production loop with a nonexistent upstream, flagged in the 2026-06-10 program audit as 'Production without consumption ... Atalanta/Pheme->dead upstreams'; (2) consumer-drift — even if wired, the downstream ingestion to Techne is explicitly 'TBD; currently manual,' so the loop had no live consumer at either end. The whole loop is also blocked at the source: Apollo is itself DEFERRED (R8 organism collapsed under perturbation), so there is no validated organism stream worth mining yet.
- **Future options (Q4) [suggestions]:**
    - retire-after-HITL: archive read-only (loop/cron off, 354 upstream_not_found artifacts + daemon kept), gated behind Apollo's own revival — there is nothing to hunt until Apollo produces a validated organism stream.
    - refactor-to-config-driven-upstream: replace the three dead hardcoded APOLLO_RUN_ROOTS with the real Apollo output paths (run_v2d2b/lineage/journal) via config, and verify the parser actually matches Apollo's real organism schema (the run_id/organisms/primitive_sequence shape is assumed, not confirmed against real files) — cheap, but pointless before Apollo is un-deferred.
    - adapt-to-eval-feedstock: repoint Atalanta from a primitive-proposal DR firer to a read-only primitive-reuse/composite-frequency analyzer that feeds the frozen eval battery and Apollo's own diagnostics, dropping the dead DR->Pythia->manual-Techne arm entirely.
    - retire-and-lift-asset: archive the agent but lift the aggregate_primitive_signals composite-mining algorithm and the evidence-anchored Type-E DR template into whatever component eventually consumes Apollo organisms, so the IP isn't lost.
- **Rubric (unverified):** realized=none — 354/354 ticks UPSTREAM_NOT_FOUND, zero dispatched, zero primitives proposed; no downstream model output changed - latent=low-to-med — sound composite-mining + evidence-anchored DR design, but contribution ceiling is gated entirely behind Apollo (deferred, R8 collapse) AND a still-manual Techne ingestion arm; cannot lift the loop alone - niche=unique in role (only E-track primitive hunter reading Apollo organisms) but the niche is empty — its sole upstream (Apollo) and downstream (Techne registry) are both inactive/manual; redundant with nothing, but also feeding nothing - health=dead-gated — daemon is runnable and well-engineered (schema-tolerant parser, atomic state, single-instance lock, anti-silence), but wired to three upstream paths that never existed; last activity 2026-05-30 - debt=med — anti-silence alarm fired uselessly for 7x its threshold (300+ wasted ticks) without anyone repointing the path; 250+ identical 342-byte upstream_not_found artifacts is exhaust accumulation; the assumed Apollo run schema (run_id/organisms/primitive_sequence) was never validated against real Apollo output
- **Salvage IP (lift before any retire):** (1) aggregate_primitive_signals — the high-reuse-primitive + length-2/3 unnamed-composite-chain frequency miner over organism sequences (daemon.py:270-299), reusable by any Apollo-organism consumer. (2) The evidence-anchored Type-E DR prompt template enforcing evidence_organisms IDs + mandated calibration-pattern citations (anti-gravitational-well guard). (3) The schema-tolerant scan_new_apollo_runs parser (organisms/population, primitive_sequence/sequence/genome aliasing, graceful format-drift) as a reusable Apollo-log loader. (4) The three-stream telemetry + anti-silence/single-instance-lock daemon skeleton (shared with Hypatia/Pheme). Keep the 354 upstream_not_found artifacts as the dead-upstream forensic record.
- **Suggested settle condition (to verify, not act):** Revive only if BOTH hold: (1) Apollo is un-deferred and emitting a validated organism stream whose schema the daemon's parser actually matches (run_id/organisms/primitive_sequence, or a confirmed adapter), AND (2) the Techne primitive_candidates ingestion arm is automated (not 'manual/TBD'). Concrete falsifiable test: repoint APOLLO_RUN_ROOTS at real Apollo output, run `python -m agents.atalanta.daemon --once`, and confirm it dispatches at least one Type-E candidate with non-empty evidence_organisms that Techne then promotes to a registered primitive available in Apollo's next run — i.e. the loop closes once end-to-end. Until that, it cannot earn revival.
- **Evidence read:** Read: agents/atalanta/CHARTER.md (full contract), agents/atalanta/daemon.py (lines 1-90, 200-340 incl. APOLLO_RUN_ROOTS, scan/aggregate/pick/dispatch), agents/atalanta/state/state.json (354 null ticks, 0 dispatched, anti_silence_counter=354), agents/atalanta/artifacts/ (250+ files, ALL upstream_not_found 2026-05-23..05-30; sample searched apollo/runs, apollo/runs_v2, apollo/organism_runs), scripts/atalanta_loop_launch.bat, agents/atalanta/atalanta.pid. Confirmed apollo/ real output dirs (run_v2d2b, run_branch_c, lineage, journal) do NOT match the daemon's hardcoded roots. Prior verdicts: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md ('Atalanta M1 DEFER (with Apollo) — reads Apollo organisms; no point until Apollo produces'), aporia/docs/program_audit_2026-06-10.md ('Atalanta + Pheme healthy but starved by dead upstreams — shelve or rewire' / 'Production without consumption ... Atalanta/Pheme->dead upstreams'), aporia/docs/STATUS_2026-06-15_reset.md (Apollo deferred, R8 collapse, 'lineage is eval feedstock / Atalanta ore'), roles/Ergon/CORPUS_VALUE_AUDIT_2026-06-03.md (Atalanta = 'Supporting infrastructure ... feedback loops, not record emitters').
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Clio  -  *AI suggestion (advisory, NOT approved): REFACTOR*
*M1 - tool - op=Aporia - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Clio ("Muse of history") is Aporia's substrate paper-mining daemon, built (starting ~2026-05-18 per James directive) to mine the written mathematical record for substrate-priority material. Layered in fail-early versions: v0.1 arxiv mining with 13 substrate-priority queries (tensor decomposition / border rank / secant variety / modularity lifting / falsification-signal hunts like 'withdrawn'/'counterexample'/'erratum'), dedup to a disk index + Postgres (agora.clio_papers); v0.2 cheap-LLM claim extractor (title+abstract -> up to 3 falsifiable claims with claim_type/paradigm_hint/kill_path); v0.3 Sigma-kernel CLAIM submitter; v0.4 quality observability. Problem targeted: keep external mathematical literature flowing into the substrate as candidate falsifiable claims, especially anti-anchor / falsification signals (the highest-value substrate type).
- **Value thesis (Q2):** Intended value: be the substrate's standing inflow channel from the literature, converting fresh arxiv papers into typed, falsifiable, kill-path-bearing CLAIM entries on the Sigma ledger so the falsification machinery (Charon battery / promotion ledger) would have a continuous supply of external claims to attack and promote/kill. Intended consumers: the Sigma kernel claim ledger and, downstream, the Charon falsification swarm and the promotion pipeline; secondarily the dashboard/Metis brief via heartbeat quality snapshots. Nephele was built as its explicit hot-standby fallback (activates only when Clio silent >=4h), evidence the program treated Clio's inflow as load-bearing.
- **Why it fell short (Q3) [unverified claim]:** Two ruts compounded. (1) consumer-drift / dead-gated: the pipeline dead-ends. Charon's 2026-06-23 session found data/clio/sigma_claims.db holds 0 symbols and only 3 un-promoted arxiv-mine claims, 0 caps; Techne 06-23 confirmed prometheus_fire has no `sigma` schema, so the kernel PROMOTE path is 'genuinely vacuous' in both backends. No component (Charon executor, Pollux, Learner, forge) actually reads clio_claim_extractions or Clio-sourced sigma claims into the metabolization loop. The v0.2 extractor / v0.3 submitter evidently never ran at volume — the last daemon cycle (2026-05-30) logged found=185/new=0 with claims_extracted_24h=0. (2) claim-space-exhaust: even if revived, paper-derived 'falsifiable claims' are claim-space GENERATION (exhaust), not capability-attached failure (residue) — they do not carry kill-geometry the forge->Learner decider consumes; surviving an ablation is not in their path. The daemon is also currently dead (log + sigma DB frozen since late May; prior death 2026-05-20 was a MemoryError, since hardened).
- **Future options (Q4) [suggestions]:**
    - revive-as-is the v0.1 mining tier only (arxiv -> agora.clio_papers) as a literature-watch feed for Aporia/operators, leaving v0.2/v0.3 dark — low cost, gives visibility, matches the 06-23 P1 REVIVE-support call
    - refactor-to-spine-feeder: rewire so extracted falsification-signal papers (withdrawn/counterexample/erratum) become Learner negative-corpus / forge-routing inputs (capability-attached), with a real consumer reading clio_claim_extractions; only then does it earn 'active'
    - adapt-to-Pheme-demand: gate the 13 queries by the Learner-deficit profile Pheme publishes, so Clio mines the literature the forge is currently failing on instead of a fixed substrate-priority menu (turns generation into targeted residue-hunting)
    - retire-after-HITL: fold its function into the single arxiv channel, archive the loop read-only, and lift the reusable assets (query taxonomy, Atom parser, dedup index, paradigm/kill-path schema) into the shared library; Nephele then becomes the minimal standby
- **Rubric (unverified):** realized=none — output (papers/claims) changes NO downstream model today; sigma ledger near-empty (3 un-promoted claims, 0 symbols), no consumer reads it into forge->Learner - latent=low-to-med — could become med IF refactored to feed falsification-signal papers into the Learner negative-corpus or Pheme-gated forge routing; as a generic literature feed, latent is low (claim-space exhaust) - niche=partially redundant — overlaps Lethe (anti-anchor miner, cold-LLM probes) and is duplicated by Nephele (its own designated Clio-fallback gatherer); the unique slice is the structured paper->Sigma-CLAIM pipeline, which currently has no consumer - health=dead-gated — code is runnable and well-built (87 tests pass, DI seams, fail-soft, MemoryError-hardened) but the daemon is silent since 2026-05-30 and its output gate (Sigma promotion path) is vacuous; v0.2/v0.3 never ran at volume - debt=med — meets its own spec (mine + extract + submit + observe) but misses the needle: it generates claim-space rather than capability-attached kill-geometry; the LLM-extracted claims and template kill_paths are unvalidated exhaust unless a real downstream battery consumes and promotes them
- **Salvage IP (lift before any retire):** Lift before any retire: (1) the substrate-priority arxiv query taxonomy in scripts/clio_config.yaml (13 queries mapped to paradigm codes P01-P31 + falsification-signal hunts) — directly reusable by Pheme-gated mining; (2) the pure-function arxiv Atom parser and polite-pacing fetcher (_parse_arxiv_atom / _build_arxiv_url / scan_arxiv_query); (3) the MemoryError-tolerant streaming PaperIndex dedup (fingerprint = external_id else title+author+year); (4) the claim-extraction prompt + schema and the epistemic-posture-aware kill_path templates (clio_extractor.build_extraction_prompt, clio_submitter.KILL_PATH_TEMPLATES) — encode the 'theorem is not killed by counterexample' doctrine; (5) the data/clio/paper_index.json corpus (181 papers) and the clio_quality metric definitions (yield/specificity/diversity/theorem-kill-path canary).
- **Suggested settle condition (to verify, not act):** Within one revival cycle, route Clio's falsification-signal papers (withdrawn/counterexample/erratum) into the forge->Learner spine and show that at least one Clio-sourced item produces a measured behavior delta in the Learner that survives an ablation (the decider). If, after wiring a real consumer, zero Clio-sourced claims are ever promoted/consumed by the spine over the cycle (sigma_claims.db stays at demo scale; Learner corpus gains nothing attributable to Clio), then Clio is confirmed claim-space-exhaust and drops to RETIRE-after-HITL with its mining tier folded into a single standby (Nephele).
- **Evidence read:** Read scripts/clio_daemon.py, clio_extractor.py, clio_submitter.py, clio_quality.py, clio_config.yaml, clio_loop_launch.bat; data/clio/paper_index.json (181 papers, _meta last_updated 2026-05-30); ran all four test suites (87 passed, 3 skipped). Disk: logs/clio_daemon.log frozen 2026-05-30 12:01 (last cycle found=185/new=0, claims=0); data/clio/sigma_claims.db frozen 2026-05-18 (32KB). Prior verdicts: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (#11 Clio = P1 REVIVE-support; #32 Nephele 'redundant with Clio revived'; #28 Lethe 'overlaps Clio'); pivot/agent_roster_2026-05-28.md (Clio M1 tool active, Nephele = 'Clio-fallback substrate gatherer'); roles/Techne/M05_PROMOTION_REPLAY_FINDINGS_2026-06-23.md ('prometheus_fire has no sigma schema', kernel PROMOTE path near-empty); charon/CHARON_SESSION_2026-06-23.md ('data/clio/sigma_claims.db: 0 symbols, 3 claims (un-promoted arxiv mines), 0 caps'). Confirmed no Charon/Pollux/Learner/forge code reads clio_claim_extractions into the spine.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Hypatia  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M1 - tool - op=Aporia - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Built (Aporia, 2026-05-23) to close the "substrate type D" gap for Ergon's Learner v1.0: training data consisting of WORKED SOLUTIONS where every proof step is tagged on an R1-R5 reasoning ladder (R1 lookup -> R5 novel framework). The charter's stated problem: without step-tagged reasoning-ladder annotation the Learner curriculum has no difficulty axis, Hephaestus has no tier-targeting ground truth, and the falsification-routing arc cannot be trained. Hypatia's whole job: produce one fully-annotated worked solution per day from a 537-problem catalog, biased by Ergon's current training deficits (Pheme demand profile when available).
- **Value thesis (Q2):** Mechanism: each tick (1/24h) pick a problem from aporia/mathematics/questions.jsonl, build a Type-D proof-decomposition prompt, enqueue to agora.research_queue (target_substrate_type='D', tier T2) for Pythia/Deep-Research to answer; the returned report's R1-R5-tagged JSONL block would be parsed and appended to the worked-solutions corpus, picked up by Learner training on the next epoch. Intended consumer = Ergon/Learner (the spine), with Pheme as the demand-routing signal aiming selection at the Learner's worst patterns. It is the D-track OWNER (does not produce A/B/C/E).
- **Why it fell short (Q3) [unverified claim]:** Three compounding failures, all latent because the loop never reached its consumer. (1) Catalog/task mismatch (fatal): the backlog questions.jsonl is 532/537 status='open' UNSOLVED conjectures, but the prompt asks to 'Decompose the PROOF of the following result.' For 99% of the backlog there IS no proof. The MATH-0008 (Finite Lattice Representation Problem) report is the proof of the bug: the model spent 2621s discovering the statement is an open conjecture, then FABRICATED an R1-R5 ladder for a different theorem (Palfy-Pudlak reduction) just to satisfy the output contract. This is exactly the kind of confabulated 'reasoning ladder' that would poison a Learner corpus. (2) Dead-gated consumer: the ingestion target ergon/learner/corpus/v1_0_tier_pending/worked_solutions/ does NOT exist and the ingester is 'TBD, currently manual' per charter -- never built. Zero examples ever reached the Learner. (3) Unparseable output: the emitted JSONL has depends_on:[cite: 1] (citation markers leaked into the JSON), so even a built ingester couldn't load it. Net: 8 clean dispatches (2026-05-23..30), then the fleet went down (last_pick 2026-05-30, anti_silence_counter climbing). Rut = decorative-mechanism + dead-gated + consumer-drift: a tidy daemon emitting well-formed dispatches into a pipeline whose terminus was never built, against a backlog that makes the core task structurally impossible. Prior 'KEEP/REVIVE clean 1/day' verdicts graded cadence and code hygiene, not whether output changed a model -- it never did.
- **Future options (Q4) [suggestions]:**
    - RETIRE-after-HITL the loop as-built: the proof-decomposition-of-open-conjectures task is structurally void; lift the daemon scaffold and the R1-R5 ladder taxonomy, archive the rest read-only.
    - REFACTOR-to-solved-corpus: repoint the backlog from questions.jsonl (open conjectures) to a catalog of PROVED theorems with known proofs (e.g. textbook/Mathlib results), so 'decompose the proof' is actually answerable -- then the R1-R5 ladder produces real worked solutions. Requires sourcing a solved-theorem catalog Aporia does not yet have.
    - ADAPT-to-spine: re-aim Hypatia as a metabolization feeder -- decompose the Learner's OWN survived/killed proof attempts (kill-geometry) into R1-R5 ladders, attaching failure to capability per the metabolization-loop decider, instead of generating fresh claim-space (which is exhaust).
    - REVIVE-only-after-ingester: do not revive the loop until (a) the worked_solutions corpus dir + a parsing ingester exist, (b) the JSONL depends_on/cite leak is fixed, and (c) the backlog is solved-problems; otherwise it re-runs the same null.
- **Rubric (unverified):** realized=none -- 0 examples reached the Learner; corpus dir and ingester never existed; output sits inert in deep_research_reports/ - latent=low-to-med -- the R1-R5 ladder taxonomy + clean daemon are reusable, but only if backlog is repointed to solved theorems OR re-aimed at kill-geometry; as-is the task is void - niche=partially unique -- sole D-track (worked-solution) owner, but the dispatch-to-research-queue plumbing is redundant with Pythia/Clio; uniqueness is the ladder schema, not the loop - health=dead-gated -- code is runnable and well-engineered (lock, state, anti-silence), but the downstream gate (ingester + corpus dir) was never built and the backlog makes the core task impossible; currently down with the fleet - debt=high -- meets-spec (emits tidy daily dispatches, anti-silence loud) but misses-needle entirely: prompts demand proofs of unproven conjectures, output JSONL is malformed (cite leak), and no consumer exists; the 'clean 1/day' reputation masks zero realized value
- **Salvage IP (lift before any retire):** (1) The R1-R5 reasoning-ladder taxonomy (R1 lookup / R2 1-step transform / R3 2-3 step chain / R4 structural insight / R5 novel framework) -- a clean, reusable difficulty/abstraction schema for any worked-solution or kill-geometry annotation. (2) The well-built daemon scaffold: single-instance PID lock (cross-platform _is_pid_alive), atomic state persistence, anti-silence NULL_TICK + SELF_AUDIT_NULL + CATALOG_SATURATED sentinels, dual text/structured logging, graceful PG/telemetry degradation -- a good template for any low-cadence Aporia tool. (3) The 8 completed Deep-Research reports (00352..00432) are genuine literature surveys (e.g. the FLRP/Palfy-Pudlak survey) worth keeping as reference even though useless as Type-D training data. (4) The dispatch-tag provenance schema (queue_ref, verdict_back_to, ergon_demand_target).
- **Suggested settle condition (to verify, not act):** Build the missing ingester + ergon/learner/corpus/.../worked_solutions/ dir, repoint Hypatia's backlog to a catalog of PROVED theorems (not open conjectures), fix the JSONL cite-leak, run >=20 dispatches, and test: does a Learner trained with the resulting R1-R5 worked-solution examples beat the same Learner without them, surviving an ablation? If yes -> REFACTOR-then-REVIVE. If the ladders remain confabulated/unparseable or the trained model shows no ablation-surviving delta -> RETIRE-after-HITL (read-only, data kept). Until the consumer demonstrably exists and the backlog is answerable, reviving the loop just re-runs the null.
- **Evidence read:** READ: agents/hypatia/CHARTER.md (full contract, prompt template, anti-gravity note), agents/hypatia/daemon.py (601 lines, full impl), agents/hypatia/state/state.json (8 dispatched, 169 null ticks, last_pick 2026-05-30, anti_silence_counter=7), agents/hypatia/artifacts/ (8 dispatch_HYP-*.md + ~160 null_*.json), tail of agents/hypatia/events.jsonl (369 lines, last events null_tick_within_cooldown). KEY: aporia/docs/deep_research_reports/2026-05-30/00432_hypatia_...MATH-0008.md -- shows model confabulating a ladder for an open conjecture + malformed depends_on:[cite: N] JSONL. CATALOG: aporia/mathematics/questions.jsonl = 537 problems, 532 status='open', 2 solved, 3 partially_solved (the core mismatch). CONSUMER ABSENT: ergon/learner/corpus/v1_0_tier_pending/worked_solutions/ -> DIR ABSENT; ingester 'TBD, currently manual' per charter. PRIOR VERDICTS: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (#13 REVIVE 1/day, Aporia tool), aporia/docs/STATUS_2026-06-15_reset.md L99 (KEEP 1/day), aporia/docs/program_audit_2026-06-10.md L132/L246 ('clean, 1 problem/day'), roles/Ergon/CORPUS_VALUE_AUDIT_2026-06-03.md L70 (classed as 'supporting infrastructure / feedback loop, not record emitter'). All prior verdicts graded cadence/cleanliness, none verified output reached the consumer.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Polyhymnia  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M1 - tool - op=Aporia - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Aporia's "one tensor" play tool (charter 2026-05-24): a sparse, content-addressable, append-only N-dimensional tensor (tesserae.jsonl) into which everything tensor-shaped — every dataset, equation, algorithm, concept, paper, researcher, even fringe/loosely-related mentions — gets jammed via round-robin "scour" extractors. Intent: be the substrate's tensor-first sensory organ ("a species that could only see tensor-first would use this as its eyes"). Explicitly framed as a play tool that becomes a research tool once the substrate is rich enough. It was also the program's FIRST adopter of the SelfImprovingDaemon mixin (agents/_shared/self_improving.py) — a proof-of-concept for self-improving daemons.
- **Value thesis (Q2):** Aggregate heterogeneous tensor-related content into one navigable structure whose lineage axis forms a knowledge graph, feeding three named downstream consumers: (1) the Learner (sample-by-axis-subset, project to canonical form as training material); (2) games/ that generate (prompt, expected-response) pairs (complete-the-slice, mendeleev-gap, twenty-questions); (3) lenses/ for cross-substrate Mendeleev-style void queries (e.g. 'rank-3 physics cells after 1990 with no Prometheus citation'). Consumer = the Learner spine plus human/LLM exploration. Doctrine 'ingest first, classify later; the fringe is the point.'
- **Why it fell short (Q3) [unverified claim]:** Bounded-menu wall (gen-30 wall), confirmed literally in code and run history. Of 17+ planned scours, exactly ONE ever shipped (prometheus_self, greps the repo for *.py tensor mentions). That single idempotent source saturated: state shows 280 lifetime ticks, 236 null = 84.3% null (matches roster claim exactly), last productive change 2026-05-30, diagnosed dead 2026-06-03. The self-improvement layer that was supposed to break the wall is decorative: its ADAPTATION_MENU has ONE real adaptation (DROP_MTIME_CACHE, which only forces a rescan of the same source) and FIVE `_adapt_stub_log_only` placeholders (EXPAND_KW_RE, EXPAND_PATHS, EXPAND_FILE_TYPES, SPAWN_SIBLING_SCOUR, MODE_PIVOT_GAME_GEN) that emit a log event and mutate nothing — so the self-improver fired 257 cycles but could never grow the menu. Also consumer-drift / dead-infra: lineage edges = 0 (the knowledge-graph spine never formed), no lens or game was ever shipped (lenses/ and games/ are empty but for README/__init__), measure_health hardcodes downstream_consumption=0.0, and the broader 06-10 audit found ZERO consumers consuming any of this. Three named ruts apply: bounded-menu-wall (primary), decorative-mechanism (the stub self-improver), consumer-drift (no downstream ever wired). The program's own doctrine (feedback_gen_30_wall) says the fix is never a deeper menu for one agent — it is menu-growth/lineage, 'the substrate is the lineage, not the single agent.'
- **Future options (Q4) [suggestions]:**
    - Retire-and-lift-chassis-into-Arachne: the program already wrote this path (math_crawlers_epiphany 2026-06-04) — Polyhymnia is explicitly the CHASSIS (scour interface + sparse content-addressed tensor + integrator) for Arachne, a POPULATION of crawlers that branch/die/diversify (the doctrinally-correct menu-growth answer). Lift tensor.py + scours/_base.py + the integrator, retire the single-agent daemon.
    - Refactor-to-Learner-feedstock: wire the 2,415 existing tesserae as a one-shot corpus into the spine — but ONLY as capability-attached data, not claim-space exhaust; per the spine doctrine, raw aggregated content is exhaust unless a reasoner's failure attaches to it, so this likely needs to route through games/ first (which were never built).
    - Revive-as-is with real scours: replace the 5 stub adaptations with working apply()s and ship wikipedia_tensor/oeis_tensor/arxiv_tensor — rejected lean: this just deepens one agent's menu, the exact anti-pattern the wall represents.
    - Retire-and-lift-self-improving-proof: the SelfImprovingDaemon mixin adoption (traits, stagnation signature, cross-agent borrow scaffold) is reusable infrastructure independent of Polyhymnia — lift the pattern into the shared registry, retire the host.
- **Rubric (unverified):** realized=none — no downstream model consumes its output today; lineage edges=0, downstream_consumption hardcoded 0.0, no lens/game shipped, 0 consumers found in 06-10 audit - latent=med — the scour/tensor chassis is the explicitly-designated foundation for Arachne (math_crawlers population), and the 2,415 tesserae + sparse content-addressed tensor are reusable; ceiling is real only if Arachne or a Learner-feedstock path is actually built - niche=redundant as a single agent (its job is generalized/superseded by the Arachne population concept and by Talos as Learner corpus); unique only as the reference chassis + first self-improving-daemon proof - health=dead-gated + decorative — stalled since 2026-05-30 at 84% null, source saturated; self-improvement menu is 5/6 logging stubs that mutate nothing; runnable but produces only null ticks - debt=high — meets the 'one tensor' spec on paper (clean code, charter, structured logging) but misses the needle: scour menu of 1, stub self-improver, no consumer ever wired, lineage spine never formed; classic decorative-mechanism + consumer-drift
- **Salvage IP (lift before any retire):** Lift before retire: (1) the scour chassis — scours/_base.py Scour interface (HTTP retry/cache/rate-limit/error-isolation contract) + the round-robin integrator and tensor.py PolyhymniaTensor (sparse, content-addressed, hash-merge, append-only JSONL + SQLite mirror) — explicitly the designated Arachne chassis; (2) the 2,415 deduped tesserae in tensor/tesserae.jsonl plus the 8 axis registries (discipline/object_kind/abstraction_level/etc.) — a ready open-vocabulary taxonomy seed; (3) the SelfImprovingDaemon adoption pattern (AGENT_TRAITS, STAGNATION_SIGNATURE, cross-agent borrow scaffold) as a reusable self-improving-daemon template — but NOT the 5 logging stubs; (4) the unshipped-but-specified game/lens designs (mendeleev-gap, complete-the-slice) as Learner (prompt,response) generators for any future feedstock path.
- **Suggested settle condition (to verify, not act):** REVIVE/REFACTOR is earned only if a concrete downstream consumes Polyhymnia output and survives the metabolization test: either (a) Arachne is actually built on the lifted chassis AND its woven fabric changes a downstream model's behavior past an ablation, or (b) the 2,415 tesserae (routed through a real game/ generator) produce Learner training data that moves a transfer-eval slice (entity-disjoint or cross-op) with p<0.05. Absent either, it is claim-space/aggregation exhaust with no consumer — RETIRE (read-only, data kept). Falsifier for retire: if anyone wires a single working second scour AND a lens that another agent queries within one session, the bounded-menu diagnosis is wrong and it flips to REFACTOR.
- **Evidence read:** Read: agents/polyhymnia/CHARTER.md (full intent + 17-scour plan + games/lenses spec), daemon.py (run_tick + PolyhymniaSelfImprover: ADAPTATION_MENU with 1 real + 5 _adapt_stub_log_only placeholders, measure_health downstream=0.0), state/state.json (280 ticks / 236 null = 84.3%, 2415 tesserae, lineage=0, single scour prometheus_self, last run 2026-05-30), events.jsonl tally (257 self_improvement_cycle, 4 stub_applied, 23 self_audit_null_alarm). Verified body: tensor/tesserae.jsonl = 4917 lines (~2415 unique), 8 axis registries, relations.jsonl=17. Cross-checked verdicts: aporia/docs/program_audit_2026-06-10.md §3.5 (KEEP 'for named operators / self-improving mixin'), STATUS_2026-06-15_reset.md §7 (KEEP), pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md line 84 (ARCHIVE — 'died of bounded-menu wall (84% null), salvage operators, archive the loop' with [reconcile] flag), pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (now PENDING-REVIEW), pivot/math_crawlers_epiphany_2026-06-04.md (Polyhymnia = chassis for Arachne population; diagnosed dead 2026-06-03), pivot/REASSESSMENT_2026-06-22_consolidated.md line 308/347 (bounded-menu-wall, 84% null), roles/Harmonia journals (independence confirmed). UNFOUND: no dedicated autopsy/RESUME doc beyond the epiphany + disposition entries.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Pythia  -  *AI suggestion (advisory, NOT approved): REFACTOR*
*M1 - tool - op=Aporia - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Pythia is Aporia's Gemini Deep Research dispatch daemon (scripts/pythia_daemon.py, ~845 lines). Built to turn a perishable resource — daily Gemini deep-research-pro compute tokens that do not roll over (feedback_use_or_lose_research_tokens) — into durable substrate by polling a Postgres research_queue (agora.research_queue), maintaining up to 3 concurrent in-flight DR interactions, saving each completed report to aporia/docs/deep_research_reports/<date>/, auto git add+commit+push so the GitHub URL is mobile-clickable, and emitting heartbeat/log_work telemetry to the brief. Problem targeted: the program needed primary-literature surveys, false-anchor hunts, paradigm refinements, and substrate-typed (A-E) research feeding the falsification substrate, and a use-or-lose token budget that would otherwise evaporate.
- **Value thesis (Q2):** Value thesis: convert use-or-lose external-LLM compute into substrate-typed research reports consumed by named downstream agents. Intended consumers were enumerated explicitly in pythia_dispatch_contract_schema.md and dr_prompt_discipline.md: Stygian (battery targets), Lethe (false-anchor candidates), Hecate (retraction-pattern gradient), Moros (cross-pollination), Acheron (coordinate collisions), Erebos/Techne (generator seeds), Ergon (Learner corpus), plus doctrine and human. Each swarm member was to enqueue capped DR prompts (3/day) naming requester + substrate type + verification criterion + landing path, then ingest results from a per-agent dr_inbox/. The decoupled producer/consumer design was meant to let production scale (~650/5h compute headroom) without blocking consumers.
- **Why it fell short (Q3) [unverified claim]:** The PRODUCER is healthy; the CONSUMPTION LOOP was never closed — classic consumer-drift + claim-space-exhaust. Evidence: (1) 442 reports produced total, 55 in the final 2026-05-26->30 burst, then ZERO reports after 2026-05-30 (24 days idle as of 2026-06-23); the daemon is rostered active but not producing. (2) The mandated forward dispatch contract (Stand A2, v0.2 ecosystem doc — declare consumer + expected_behavior_delta + falsification BEFORE dispatch) was fully SPECCED (pythia_dispatch_contract_schema.md) but NEVER wired into the daemon: grep finds 0 references to intended_consumer/expected_behavior_delta/expiry_condition in pythia_daemon.py, and aporia/meta/pythia_dispatch_log.jsonl does not exist. (3) The consumption layer described in doctrine (scripts/pythia_notify_requesters.py and per-agent dr_inbox/) does not exist on disk; NO Python process reads completed reports (grep for read_recent_completed_research / deep_research_reports / consume_pythia in consumers returns nothing). (4) The retrospective yield audit (Stand A1) ran as a skeleton — aporia/meta/pythia_yield_audit_2026-05-30.jsonl has 55/55 rows with actual_delta:null; triage_report_2026-06-03.md confirms 'yield-per-token is unmeasurable until actual_delta is filled' (21 critique + 14 survey + 8 collision unaudited). So Pythia generated claim-space (plausible survey text) with no demonstrated downstream model/behavior delta — exhaust, not capability-attached residue.
- **Future options (Q4) [suggestions]:**
    - Refactor-to-contract-enforced: wire the existing pythia_dispatch_contract_schema.md into dispatch_one() so every send writes pythia_dispatch_log.jsonl with a named consumer + falsifiable expected_delta; refuse dispatch without it. Makes yield forward-attributable.
    - Adapt-to-Learner-feeder (spine alignment): point Pythia exclusively at the metabolization loop — dispatch only false-anchor / retraction-pattern prompts whose reports feed Hephaestus->Learner kill-geometry corpus, and gate revival on an ablation showing the corpus changed model behavior.
    - Revive-as-is on a thin cron at low cadence (5/day cap per Stand E) purely to not waste use-or-lose tokens, accepting it as a calibration-anchor harvester rather than a closed-loop producer.
    - Retire-after-HITL the auto-dispatch cadence; lift the 442-report corpus + dispatch-contract schema + compute-window throttle code as reusable assets, keep daemon read-only.
- **Rubric (unverified):** realized=none — 55/55 audited reports have actual_delta:null; no report demonstrably changed any downstream model or agent behavior; no Python consumer reads the reports - latent=med — clean Postgres-native dispatcher with throttle/timeout/git-push already built; if rewired to feed Hephaestus->Learner kill-geometry it could supply false-anchor corpus, but ceiling gated by consumer absorption, not production - niche=unique as the only Gemini Deep Research dispatcher (Clio/Hypatia are distinct Aporia tools); but its OUTPUT niche (literature surveys) is redundant with manual deep-research skill invocation and overlaps Charon-swarm enqueuers - health=dead-gated — daemon code is runnable and rostered active, but has produced zero reports in 24 days and the mandated contract + consumption layer (notify_requesters, dr_inbox) were never built, so the value path is dead - debt=high — meets the production spec (files land, push, telemetry) but misses the needle: the contract that makes yield attributable is specced-not-wired, the yield audit is an unfilled skeleton, and GITHUB_URL_PREFIX is hardcoded against a personal noreply handle (v0.1 TODO)
- **Salvage IP (lift before any retire):** (1) The 442-report deep-research corpus under aporia/docs/deep_research_reports/ — primary-literature surveys, false-anchor hunts, retraction-pattern data — directly mineable as anti-anchor/calibration-anchor candidates and Learner corpus. (2) pythia_dispatch_contract_schema.md — the consumer/expected-delta/falsification dispatch-contract design, reusable for ANY producer agent. (3) The compute-window throttle + exponential-backoff + in-flight-timeout/requeue logic in pythia_daemon.py (the 2026-05-21 zombie-row remediation). (4) The bespoke + default prompt seed sets (pythia_seed_200.py, gemini_research_queue/queue.jsonl ~370 tier-sorted entries). (5) agora.research_queue Postgres schema + agora_persist helpers.
- **Suggested settle condition (to verify, not act):** Wire the dispatch contract + a real dr_inbox consumer, then run for 30 days: if >=10% of dispatched reports record a concrete actual_delta (a kill_ledger entry, anchor demotion, or a Learner-corpus row that survives ablation) attributable to a named consumer, REVIVE is justified. If after 30 days actual_delta stays <10% (matching the current 0/55), the producer is decorative — RETIRE the auto-dispatch cadence and rotate the use-or-lose token budget to a different consumption pattern (this is the explicit Stand A2 falsifier).
- **Evidence read:** Read: scripts/pythia_daemon.py (full, 845 lines), aporia/meta/pythia_dispatch_contract_schema.md, scripts/pythia_loop_launch.bat, scripts/pythia_seed_200.py, aporia/doctrine/dr_prompt_discipline.md. Inspected: aporia/docs/deep_research_reports/ (442 .md files, newest dir 2026-05-30, none after), aporia/meta/pythia_yield_audit_2026-05-30.jsonl (55 rows, all actual_delta:null), aporia/meta/triage_report_2026-06-03.md ('55/55 no recorded actual_delta'), pivot/aporia_ecosystem_status_and_next_steps_v0.2_2026-05-30.md (Stands A1/A2 — contract mandate + 10% falsifier), pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (row 12 REVIVE lean), pivot/agent_roster_2026-05-28.md (rostered active). Confirmed ABSENT: aporia/meta/pythia_dispatch_log.jsonl, aporia/meta/pythia_dispatch_rejected.jsonl, scripts/pythia_notify_requesters.py, any dr_inbox/ consumer, any 0 references to contract fields in the daemon, and any Python process reading completed reports.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________


## Techne tools

### Theseus  -  *AI suggestion (advisory, NOT approved): KEEP-on-demand*
*M1 - tool - op=Techne - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Sigma is structurally verificative — given a claim it runs cross-catalog + falsification and idles with no claim source (Harmonia/Charon fan-outs stalled in empty cycles). Theseus was founded (CHARTER.md 2026-05-18) as the structurally-generative counterpart: a closed-loop, bandit-rotated, parallel-generator batch engine over the combinatorial cross-product of (catalogs x invariants x operators x mutations x regions) that 'cannot run out of work.' Explicit target problem: keep sigma fed and produce enough VOLUME of typed failure/confirmation substrate that the (paused) Ergon Learner would have a corpus to ingest.
- **Value thesis (Q2):** Value = emit varied typed failure data ('what doesn't work') at volume, with kills treated as first-class output, journaled per batch, deduplicated by content-addressed record_id, scored on 7 yield axes and rotated by a bandit so high-yield generators stay hot. Intended consumer: the future Ergon Learner (mediate), via TheseusRecord -> handoff serializer -> Ergon ingester -> LearnerRecord corpus. Sigma was the immediate consumer of the claim stream.
- **Why it fell short (Q3) [unverified claim]:** The generation problem was solved spectacularly and the ingestion problem was left wide open — classic dead-gated + claim-space-exhaust. Lifetime (Fire #234, 2026-05-30): 653,965,965 records, 363M kills, 2,351 promoted, 0 verified findings; corpus on disk is 346GB. But only 1,055 Theseus records EVER reached the Learner corpus (May 18-19 only), and they landed as a DOUBLE monoculture: 71% knots_x_elliptic_curves domain and 79% 'promoted'/1.4% 'rejected' outcome — the exact INVERSE of the failure-data goal. Root cause traced (SEAM_FIDELITY_FIX 2026-06-15): the handoff mapper never emitted predicate_holds, so the ingester defaulted every record (including kills) to promoted; a second 2026-06-07 gate skipped all non-invariant_equality kinds (~89% of corpus, incl. the largest kill source kill_neighborhood). The greedy LoRA built on this corpus produced surface classifiers + a False/kill prior with NO transfer (feedback_greedy_lora_surface_not_reasoning). Per-generator yield shows the exhaust character: throughput in the 10s-100s of millions/hour, dup_rate up to 0.999, info_density ~0.5, every record a verdict not a worked trace. Disposition plan names the continuous loop 'the /dev/null corpus.'
- **Future options (Q4) [suggestions]:**
    - Revive on-demand ONLY as a targeted generator harness for A1/D1/A3/H2 — fire a bounded batch into a specific Learner-deficit region (aimed by Pheme's demand profile), never the continuous loop. This is the consensus disposition-plan lean.
    - Refactor the emission schema so completions carry worked multi-step computation/reasoning TRACES (step_trace field) rather than bare verdicts — the single highest-leverage change per TRAINING_DATA_SURVEY; this is what would convert exhaust into residue and attack the proven transfer gap.
    - Retire the continuous daemon + bandit + 346GB confirmation corpus (read-only, data kept), and lift the E-family mined corpora (E2 arXiv, E3 OEIS 212-seq, E4 LMFDB, E5 wiki), the now-fixed seam serializer, and the D1 kill_vector_navigator wiring as standalone reusable assets.
    - Adapt the harness to feed the metabolization decider: route D1 kill-neighborhood output (with mechanisms) straight into Hephaestus forge -> Learner ablation, so generation is capability-attached rather than claim-space-attached.
- **Rubric (unverified):** realized=none-to-low — 653M records but only 1,055 ever reached the Learner (May 18-19), inverted to a confirmation-monoculture; the LoRA trained on them showed no transfer. No downstream model output changes TODAY. - latent=med — a bounded on-demand harness aimed at a real Learner deficit, IF completions are reshaped as worked traces, could feed the metabolization loop; ceiling capped by the verdict-not-derivation data shape and heavy overlap with cheaper operator-classes. - niche=redundant-as-loop / unique-as-aimed-harness — continuous combinatorial generation overlaps the bandit-rotation idea and produces the same monoculture flavor as the Harmonia swarm; its distinct value is the multi-family generator menu + E-family mined corpora + D1 navigator wiring, not the loop. - health=runnable-but-dead-gated — daemon.py runs (58 generators, smoke-verified, heartbeat instrumentation), but the consumer path was inverted/stalled since May 19; the continuous loop is the documented /dev/null corpus. - debt=high — 346GB confirmation corpus that is one adapter from useless; meets the 'emit volume' spec but misses the 'varied failure data that reaches the corpus and changes a model' needle (claim-space-exhaust).
- **Salvage IP (lift before any retire):** E-family offline mined corpora (E2 arXiv abstracts, E3 OEIS 212-sequence x 5-property dump, E4 LMFDB kwl_knowls mirror, E5 42-page wiki seed); the fixed predicate_holds-based + leak-safe-or-skip handoff serializer (theseus/handoff/ergon_handoff.py); the D1 kill_vector_navigator wiring; the 7-axis yield scoreboard + content-addressed record_id dedup; the 17-class falsification-mechanism / ~336 kill_pattern taxonomy and Sphinx-105 generator-design mapping. Do NOT lift the 346GB confirmation corpus — it is the monoculture, not an asset.
- **Suggested settle condition (to verify, not act):** Fire a single bounded on-demand batch (A1/D1/A3/H2) aimed by Pheme at a named Learner-deficit region, with completions shaped as worked traces, push it through the fixed seam into the Learner corpus, and run the metabolization ablation: does this Theseus-sourced slice change Learner behavior and SURVIVE the ablation? If yes -> REVIVE-as-aimed-harness. If the slice fails to beat counter-baseline / shows no transfer (the greedy-LoRA outcome repeats) -> RETIRE-after-HITL the generator role and keep only the mined-corpus assets.
- **Evidence read:** Read: theseus/CHARTER.md, README.md, ROADMAP.md, inventory.md, journals/BATCH_LOG.md (tail), corpus/ listing (346GB, ~50+ gz batches), handoff/ergon_outbox states (consumed/quarantine), daemon.py (round-robin while-loop + heartbeat), generators/ (58 .py incl. a3/d1). Prior verdicts: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (Theseus #16 'REVIVE on-demand ONLY ... Never the continuous loop — the /dev/null corpus, 2,351 promoted 0 verified, debt high'); roles/Ergon/CORPUS_VALUE_AUDIT_2026-06-03.md (1,486 LearnerRecords total, 1,055 Theseus May 18-19, 79% promoted/1.4% rejected, 'latent-high/realized-near-zero'); roles/Ergon/SEAM_FIDELITY_FIX_2026-06-15.md (predicate_holds never emitted -> inversion; 89% drop); roles/Ergon/TRAINING_DATA_SURVEY_2026-06-07.md (verdicts-not-traces is the lever); aporia/docs/theseus_substrate_frontier_review_2026-05-30.md (653,965,965 records, 2,351 promoted, 0 verified, Fire #234).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________


## Charon swarm

### Acheron  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M2 - tool - op=Charon - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Acheron (charon/agents/acheron/) is a Charon-swarm child daemon built as the "Iris-complement" HARD-5 coordinate-collision detector. Where Harmonia's Iris detects paraphrastic AGREEMENT (same concept said two ways -> promote to one symbol), Acheron was meant to detect paraphrastic DISAGREEMENT: places in the prose substrate where a single token silently names two distinct mathematical coordinates (the canonical example: "rank" used for both tensor rank and analytic/partition rank in one file). The targeted problem is HARD-5 / the rank-parity leak: charts or prose that collapse the 8 distinct rank coordinates (and similar conjecture-family ambiguities: Lehmer-tau vs Lehmer-Mahler, binary vs ternary Goldbach, Sato-Tate EC vs sym^k vs genus-2, etc.) into a single coordinate, which corrupts downstream invariant comparisons.
- **Value thesis (Q2):** Per CHARTER and daemon: one tick walks a rotating slice of the prose substrate (harmonia/memory, roles, aporia/docs, pivot, charon/agents self-review), picks the next file changed-since-last-scan, runs an inline coordinate-dictionary regex scan, and emits a collision_candidate artifact when >=2 distinct coordinate disambiguators fire on the same term in a file (else a clean_scan, framed as calibration evidence). Intended consumer was Iris adjudication: true-positive collisions would become catalog_edit candidates pinning each meaning to its own primitive in aporia/doctrine/substrate_vocabulary/. It also seeds Pythia/Gemini deep-research prompts hunting primary-literature collision cases per dictionary term.
- **Why it fell short (Q3) [unverified claim]:** Two compounding failures. (1) DECORATIVE-MECHANISM / measures-the-wrong-thing: Acheron detects token CO-OCCURRENCE, not COLLISION. It fires when two correctly-named, correctly-distinguished disambiguators appear anywhere in the same file. Inspected candidates confirm this: attack_angle_taxonomy.md is flagged because it correctly lists tensor/border/cactus/slice/partition/analytic/geometric rank as 'related-but-distinct' (good hygiene), and DR reports are flagged for correctly discussing both binary AND ternary Goldbach. Of 69 collision_candidates, 50 are the 'rank' co-occurrence trap and the sampled non-rank ones are all correct-usage co-occurrences. Its own artifacts admit the gap ('multi-paragraph context isolation deferred to v0.2') and v0.2 never shipped (last activity 2026-05-30). (2) DEAD-GATED / CONSUMER-DRIFT: the intended consumer never existed as a sink. Harmonia's own ROADMAP states that of ~2200 swarm artifacts, ~one was ever consumed downstream; Iris's promotion candidates themselves were never promoted, and a code grep finds zero consumers of collision_candidate anywhere. So even a perfect collision would land in a directory no actor reads. Output ratio (500 clean_scan : 69 false-positive collision : 0 true positive : 0 dr_intake) is pure exhaust.
- **Future options (Q4) [suggestions]:**
    - retire-and-lift-asset: RETIRE-after-HITL the daemon (read-only, keep artifacts); lift the coordinate dictionary (8 rank coordinates + Lehmer/Schinzel/Catalan/Sato-Tate/Goldbach/twin-prime/Mertens disambiguator regexes) into a small reusable lint module
    - refactor-to-schema-check: replace prose-grep with a structural validator over tensor charts/registry rows that asserts a single 'rank' field is illegal (enforce RankZooSignature's N-slot tuple) -- this is the real HARD-5 detector already named as PATTERN_RANK_PARITY_LEAK in primitives.md
    - adapt-to-Iris-merge: fold the dictionary into Iris as a polarity flag (agreement->promote, disagreement->flag) only IF an Iris consumer is ever built; otherwise both retire together
    - adapt-to-metabolization-loop: convert the dictionary into labeled training pairs (correct-distinction vs genuine-collision) to feed the Hephaestus->Learner kill-geometry pipeline -- but only after hand-curating real collisions, since current corpus is 0 true positives
- **Rubric (unverified):** realized=none — 0 collision_candidates consumed downstream; no model/chart/catalog changed; output is exhaust on disk - latent=low — a structural rank-field schema check has value, but that check is already specified in substrate_vocabulary primitives.md (RankZooSignature / PATTERN_RANK_PARITY_LEAK); Acheron's prose approach is the inferior path to it - niche=redundant — overlaps Iris (same dictionary, same swarm, same dead consumer) and is superseded by the schema-layer HARD-5 check in aporia/doctrine/substrate_vocabulary/primitives.md - health=dead-gated + decorative — daemon is runnable but idle since 2026-05-30; intended Iris adjudication sink never consumed anything; detector measures co-occurrence not collision - debt=high — co-occurrence-as-collision shortcut meets the MVP spec but misses the needle entirely; 500 clean_scan + 69 false-positive artifacts are noise that pollutes the same prose substrate it audits; depends on dead Redis-era swarm infra and an Iris adjudication API that never shipped
- **Salvage IP (lift before any retire):** The COORDINATE_DICTIONARY in charon/agents/acheron/daemon.py (lines 29-95): 8 rank-coordinate disambiguator regexes plus tuned regex sets for Lehmer-tau/Mahler, Schinzel-Zassenhaus/general, Catalan-Mihailescu/Tijdeman-Zagier/Pillai, Sato-Tate EC/sym^k/genus-2, binary/ternary Goldbach, twin-primes/bounded-gaps/HL-k-tuples, Mertens-conjecture/function-bound. Also the _build_dr_prompt template (substrate-type-A collision-hunt prompt) as a reusable DR prompt pattern. Lift these into a lint/schema module before retiring; the regex dictionary is the only durable, non-redundant asset (note: the authoritative coordinate list itself already lives in aporia/doctrine/substrate_vocabulary/primitives.md RankZooSignature, so even this is partly redundant).
- **Suggested settle condition (to verify, not act):** Hand-label the 69 collision_candidate artifacts for genuine HARD-5 collisions (same token silently meaning two coordinates in one assertion). REVIVE/REFACTOR only if (a) >=1 genuine true positive exists in the corpus AND (b) a downstream consumer (Iris adjudication or the vocabulary catalog_edit pipeline) is actually built and ingests it producing a catalog change. If true-positive rate is ~0 (as the sample strongly indicates) or no consumer materializes, RETIRE stands. The structural alternative settles it positively: a schema validator enforcing RankZooSignature on registry rows would catch real HARD-5 leaks deterministically, making the prose scanner unnecessary.
- **Evidence read:** Read: charon/agents/acheron/CHARTER.md, charon/agents/acheron/daemon.py (full), __init__.py. Listed state/ (scanned_files.json 116KB, last save 2026-05-30; dr_seeded.json/dr_term_last_fired.json last 2026-05-29; dr_inbox empty). Counted artifacts/: 569 total = 500 clean_scan + 69 collision_candidate + 0 dr_intake. Inspected collision content: attack_angle_taxonomy.md (rank co-occurrence on correct distinction) + 5 non-rank samples (Goldbach binary/ternary, twin/bounded-gaps, Schinzel, Sato-Tate) all correct-usage co-occurrences. Confirmed zero code consumers of collision_candidate via grep. Read aporia/doctrine/substrate_vocabulary/primitives.md (RankZooSignature + PATTERN_RANK_PARITY_LEAK = the schema-level HARD-5 detector) and version.json (frozen 0.1.0, window closed). Prior verdicts: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md row 29 ('coordinate-collision detector — niche, low yield [confirm in scoring pass]'); pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (Acheron moved borderline-ARCHIVE -> PENDING-REVIEW). harmonia/agents/ROADMAP.md: ~2200 swarm artifacts, ~1 ever consumed downstream; confirmed Iris daemon exists but its candidates were never promoted (the dead consumer side).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Charon_Loop  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M2 - tool - op=Charon - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Charon_Loop (scripts/charon_loop.py + charon/agents/) is the round-robin rotation orchestrator for the Charon agent swarm. It was built to automate Charon's recurring falsification mandate without a human babysitting terminals: one tick per invocation cycles through 8 child-agents (Stygian attack-execution, Lethe anti-anchor mining, Acheron coordinate-collision detection, Moros multi-provider cross-pollination/critique, Hecate kill-geometry MI analysis, Nephele Clio-backstop, Pollux numerical-coincidence scanning, Erebos metabolization composition). Designed to be fired by Claude Code /loop (e.g. `/loop 4m python scripts/charon_loop.py`), with rotation state persisted to _rotation_state.json so the cycle survives restarts, a single-instance PID lock, and exception-swallowing so a sticky agent cannot freeze the swarm. Pattern was lifted from scripts/harmonia_loop.py. Note: this is distinct from the original Charon 5-stage Langlands discovery system (charon/README.md, 2026-04) and from the Charon operator persona, which is alive and authoring adversarial verdicts through 2026-06-23.
- **Value thesis (Q2):** Value thesis was throughput-via-automation: turn Charon's manual falsification work into an unattended swarm emitting Theseus-shape kill_ledger rows (KillVectors, coincidence signals, cross-generator MI patterns) that feed the substrate's downstream consumers (Hecate's geometry analysis, Aporia's DR queue, ultimately the Learner corpus). The orchestrator itself is pure glue — its consumer is the swarm operator (Charon) and the substrate ledgers; it adds value only by keeping 8 differentiated lenses ticking on a fair schedule (feedback_rhea_scripts / feedback_ergon_execute: execute, don't poll). The real product is the child-agents' emissions, not the rotation.
- **Why it fell short (Q3) [unverified claim]:** Two ruts, both confirmed from disk. (1) dead-infra: the loop is frozen. _rotation_state.json, all three live kill_ledgers, and every agent state file stop at 2026-05-30; the PID-lock file points at pid 24132 which is dead (tasklist: no such process); the loop hard-codes the .176 Redis host that the 2026-06-23 reassessment flags as dark. It has not ticked in ~25 days. (2) bounded-menu-wall: Charon's own diminishing-returns audit (2026-05-25) found 4 of 7 agents saturated — Lethe SATURATED (28 ticks, ~0 candidates: catalog calibrated for an outgrown model class), Acheron SATURATED (8-term hand-curated dictionary), Nephele CORRECTLY-DORMANT (29/29 skipped while Clio alive), Pollux re-saturating after a 5-pair pool exhausts — plus structural monoculture (all 8 single-author, shared CharonAgent base, shared LLM cascade, single emission schema). The orchestrator amplified empty-tick volume rather than signal. The program has since decided to shrink to a spine: the 2026-06-23 COMPONENT_DISPOSITION_PLAN explicitly ARCHIVEs Charon_Loop ("rotation orchestrator — fold into the reduced 4-worker set; not needed at this scale") while keeping the 4 productive workers (Hecate/Pollux/Moros/Stygian) and the Charon operator. The orchestrator is redundant glue at the reduced scale where 4 workers can be fired directly.
- **Future options (Q4) [suggestions]:**
    - retire-and-fold: archive charon_loop.py read-only; at 4-worker scale, fire Stygian/Pollux/Moros/Hecate directly (manual or a 4-line cron) — the round-robin orchestration earns nothing once the dormant/saturated 4 agents (Lethe/Acheron/Nephele/Erebos) are dropped. This is the disposition plan's stated path.
    - refactor-to-asymmetric-scheduler: if the reduced set is ever re-automated, lift only the durable scheduler primitives (PID single-instance lock, cross-platform _pid_alive, exception-swallow-and-advance, per-agent next_eligible_at from enhancement E11) into a thin shared lib — drop the 8-name equal-slot rotation that produced the empty-tick problem.
    - adapt-to-spine-feedstock: rewire the 4 surviving workers' emissions to land directly in the Learner corpus / forge→Hephaestus path (the agreed decider) instead of agent-local kill_ledgers, so capability-attached residue replaces claim-space exhaust — but do this at the worker level, not the orchestrator.
    - revive-as-is: only if Redis is restored AND the saturation cures (Lethe attack-mode rotation E1, cross-agent edges E5) ship first; low-priority given the program is intentionally shrinking.
- **Rubric (unverified):** realized=none — has not ticked since 2026-05-30; no emission today changes any downstream model. Its historical emissions live in agent ledgers that the 2026-06-23 audit classes as /dev/null-adjacent (shape-only, weak-gate). - latent=low — the orchestration concept's ceiling is just 'keep 4 workers firing,' which a cron or manual fire covers. Latent value is in the workers (Pollux/Stygian real signal per disposition plan), not the loop. - niche=redundant — overlaps trivially with harmonia_loop.py (it was copied from it) and with direct invocation of the 4 kept workers. At reduced scale the niche (8-way fair rotation) no longer exists. - health=dead-infra — loop frozen ~25 days, dead PID, hard-coded dead .176 Redis host. Code is runnable in principle but its substrate (Redis, the saturated agents) is gone. - debt=med — swallows all agent exceptions and advances rotation, so a silently-crashing worker emits {errors:1} forever with no escalation; hard-coded .176 host; and it served the math-claim monoculture flavor flagged program-wide. Meets-spec (cycles agents) but misses the needle (signal vs empty-tick discrimination).
- **Salvage IP (lift before any retire):** Lift before archiving: (1) the cross-platform _pid_alive + single-instance PID-lock pattern (charon_loop.py:51-104) — reusable for any future daemon, already shared with harmonia_loop. (2) the exception-swallow-and-advance + capped-history record schema (run_once, lines 169-198) as a scheduler primitive. (3) the per-agent saturation diagnosis and the E1/E5/E11 enhancement specs in pivot/charon_swarm_diminishing_returns_2026-05-25.md (attack-mode rotation, cross-agent contamination edges, asymmetric cadence) — design IP for whatever drives the kept 4 workers. The workers themselves (Stygian/Pollux/Moros/Hecate code under charon/agents/) are NOT this component's salvage — they are separately REVIVE-listed.
- **Suggested settle condition (to verify, not act):** Falsifiable test: re-point the loop at live infra and run it for a full rotation window (≥1 day). RETIRE holds if, across the reduced 4-worker set fired directly vs via Charon_Loop, the orchestrator produces zero incremental substrate signal that survives a permutation null AND lands in the Learner/forge path — i.e. the rotation glue adds nothing a 4-line cron or manual fire does not. REVIVE only if asymmetric scheduling demonstrably raises signal-per-tick (fewer empty ticks, more perm-null-surviving KillVectors) over direct invocation. Given the dormant/saturated agents and dead Redis, RETIRE is overwhelmingly favored and already endorsed by the 2026-06-23 disposition plan; HITL is James/Charon confirming the fold.
- **Evidence read:** Read: scripts/charon_loop.py (full orchestrator), charon/agents/__init__.py (AGENT_NAMES = 8 agents), charon/charon_loop_state.json (Redis stream IDs), charon/agents/_rotation_state.json (200-record history, first 2026-05-30T02:40, last 2026-05-30T16:24), charon/README.md (the separate 5-stage discovery system), charon/CHARON_SESSION_2026-06-23.md (Charon operator alive, authoring verdicts — distinct from loop). Ran: stat on state files (all frozen 2026-05-30), tasklist on PID 24132 (dead), find on agent state mtimes (all 2026-05-30). Prior verdicts read: pivot/charon_swarm_diminishing_returns_2026-05-25.md (4/7 saturated + monoculture audit), pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md line 79 ('Charon_Loop ... fold into the reduced 4-worker set; not needed at this scale') and line 118, pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md line 118 (Charon_Loop listed under archived 'Charon extras').
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Erebos  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M2 - tool - op=Charon - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Per 4/4 frontier-model convergence (2026-05-25), the conspicuously missing layer in the Charon swarm was GENERATIVE/CONSTRUCTIVE: every prior swarm agent (Stygian, Lethe, Acheron, Moros, Hecate, Nephele, Pollux) only measures/audits/critiques/fetches; none synthesize new candidate claims. Erebos (renamed from Hephaestus 2026-05-26 to avoid the Forge collision) was built to close that loop — a plugin host for up to 25 hypothesis-generator archetypes (G01 Intersection, G02 Contrast, ... G25 Degeneracy) that reads PROMOTED/UNVERIFIED/REJECTED rows from Stygian+Pollux plus Hecate cross-generator canonical patterns, COMPOSES new candidate-claims via a round-robin/kp-routed plugin registry, and emits each as a kill_ledger row + enqueues it to stygian_priority for v10 battery validation. It targeted the substrate's most exposed gap: nothing was authoring new falsifiable claims; the swarm could only react to what existed.
- **Value thesis (Q2):** Intended to add value as the swarm's composer/forger: turn the accumulated kill-ledger geometry into NEW probeable hypotheses (a generative front-end feeding the battery), and as Layer-2 it would accumulate failure geometry across emissions (the kill_tensor over plugin x domain x invariant x kill_pattern, cross-cell co-occurrence motifs with lift filtering) to route the next generator. Consumers: Stygian's v10 battery (validation of composed claims via stygian_priority queue), and downstream the Learner — the cross-cell motif recommendations were meant to be a load-bearing routing signal that beats counter baselines, i.e. metabolized kill-geometry a model could consume.
- **Why it fell short (Q3) [unverified claim]:** Classic claim-space-exhaust compounded by consumer-drift and dead-gated downstream. The architecture was disciplined honestly through Phase 0 (9 anti-pattern primitives, 621 tests) and Sprint-1 passed 10/10 — but Sprint-1 was correctly reframed as synthetic instrument-calibration (circular, per feedback_instrument_vs_architectural_pass). On REAL data the load-bearing Layer-2 claims collapsed under Charon's own adversarial permutation nulls (2026-06-03, Phase 3.K): the strongest claim (2 cross-cell deltas vs the discriminating pair-aware counter) gave p=0.105 (7-seed mean 0.102) = STATISTICALLY UNDERDETERMINED; the triplet motif was FALSIFIED below null (observed lift beaten by ~90% of shuffles — textbook lift inflation from low expected counts); the per-plugin null decayed 0.055->0.075 as the ledger grew. Net: zero Erebos Layer-2 real-data signal claims survive a permutation null; only the BSD infrastructure enrichment stands. Stygian's composition-aware loader never shipped (claims short-circuit as *_pending), so the battery never actually consumed composed claims — the generative output had no live downstream. The 2026-06-15 program reset diagnosed the whole program as "an immune system with no organism" and PAUSED Erebos explicitly (spine = Learner->Forge->Router->Icarus, everything else off), instructing: do not reopen until it has a consumer. The 234 composed-claim artifacts are exhaust, not residue, by the program's own metabolization-loop decider.
- **Future options (Q4) [suggestions]:**
    - RETIRE-after-HITL: freeze read-only, keep all ledgers/artifacts/tests; the reset already paused it and the falsification verdict is settled (0 signal survives null). No deletion — data kept as the canonical worked example of claim-space-exhaust.
    - Lift-and-attach-to-spine: salvage the cross-cell motif + permutation-null harness as a falsification primitive into harmonia/lib/falsification_primitives (the STATUS-2026-06-15 §7 EXTRACT: counter-baseline harnesses must emit their own perm-null p-value or refuse PASS) — this is the durable IP and is Charon-grade.
    - Adapt-to-Learner-consumer (revive condition only): if/when Forge->Learner needs a claim-generation front-end, re-wire Erebos generators to emit directly into the spine's training/route loop AND ship Stygian's composition-aware loader so claims are actually validated — revive only after a live consumer exists.
    - Refactor-to-data-asset: keep BSD/Mahler enrichment detectors and the kill_tensor schema as reusable calibration-data generators; retire the 25-archetype generative loop itself.
- **Rubric (unverified):** realized=none — 0 Layer-2 signal claims survive a permutation null (2 underdetermined, 1 falsified); composition-aware loader never shipped so the battery never consumed composed claims; no downstream model output changes today - latent=low — generative front-end could feed the Learner spine IF a live consumer and a composition-aware validator both existed; absent those it is exhaust - niche=unique within the Charon swarm as the only generative/composer agent (all others measure/audit/fetch); but redundant with the program decider's verdict that claim-space generation without a consumer is exhaust, and the actual Forge (agents/hephaestus) owns capability-attached forging on-spine - health=dead-gated — code is runnable (621 tests, daemon ticks, still emits artifacts) but explicitly PAUSED by the 2026-06-15 reset; downstream Stygian composition loader is a permanent short-circuit (*_pending), so emissions go nowhere live - debt=med — meets-spec (disciplined, well-tested, honestly reframed via ERRATA) but misses-needle: 234 composed-claim artifacts + growing ledgers are storage/serialization cost with zero realized consumption; 11 of 25 plugins quarantined; predicate_handle + 4 cost-instrumentation fields shipped but never wired to a producer
- **Salvage IP (lift before any retire):** (1) The permutation-null-against-the-discriminating-counter harness (charon/agents/erebos/sprint1/phase3/pair_aware_permutation_null.py + permutation_null.py + scale_stress.py + parent_child_isolation.py + seam_sufficiency_audit.py) and the discipline rule 'counter-baseline harnesses emit their own perm-null p-value or refuse to render PASS' — promote to harmonia/lib/falsification_primitives. (2) The cross-cell co-occurrence motif + lift filter (_cross_cell_motif.py, _motif_extraction.py) and kill_tensor schema (_kill_tensor.py: plugin x domain x invariant x kill_pattern sparse 4D). (3) kill_pattern_registry routing semantics + 25-archetype spec (pivot/erebos_25_archetypes_spec_2026-05-26.md) as a generator taxonomy. (4) BSD/Mahler enrichment detectors (sprint1/phase3/bsd_enrichment.py) — the one infra pass. (5) residue-eligibility gate + quarantine/debt-budget primitives. (6) The whole Erebos arc as the canonical documented worked-example of claim-space-exhaust (Phase3.K verdict + CHARON_SESSION_2026-06-03/06-15).
- **Suggested settle condition (to verify, not act):** Erebos earns REVIVE only if a composed claim, validated end-to-end through a SHIPPED Stygian composition-aware loader, produces a cross-cell/Layer-2 routing recommendation that (a) beats the pair-aware counter baseline with a permutation-null p<0.05 on real (non-synthetic, grown) ledger data across >=5 seeds, AND (b) changes a downstream spine model's behavior (Learner/Router) in a way that survives an ablation. Until both hold, the standing verdict — 0 signal survives a null, no live consumer — settles it as RETIRE-after-HITL.
- **Evidence read:** Read: charon/agents/erebos/__init__.py, daemon.py, generators/__init__.py (25-plugin REGISTRY + kp-routed selection), sprint1/verdict.py (pre-committed kill rule >=4/10 fails -> PAUSED), sprint1/phase3/pair_aware_permutation_null.py (the decisive null), sprint1/phase3/bsd_enrichment.py, _kill_tensor.py. Read pivot/erebos_phase0_retrospective_2026-05-27.md and pivot/erebos_composed_claim_consumer_audit_2026-05-28.md. Read the decisive verdicts: charon/CHARON_SESSION_2026-06-03.md ('zero Erebos Layer-2 real-data signal claims survive a permutation null'; pair-aware p=0.105, triplet falsified below null, per-plugin 0.055->0.075; only BSD infra stands) and charon/CHARON_SESSION_2026-06-15.md (program reset: 'immune system with no organism'; spine-only; 'Erebos composition is explicitly PAUSED'; 'Don't reopen Erebos ... off-spine until it has a consumer'). Confirmed 234 composed_claim artifacts in charon/agents/erebos/artifacts/ and live ledgers in state/. Referenced verdict docs (PHASE3_K_PAIR_AWARE_NULL_VERDICT, PHASE3_EFG_VERDICT ERRATA) cited in sessions but not opened directly. 06-17 and 06-23 sessions checked — neither revisits Erebos (confirms it stayed paused).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Hecate  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M2 - tool - op=Charon - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Built (2026-05-19, Charon child under operator Charon) to turn a one-shot result — MI(kill_pattern, operator-class) = 0.725 bits over 314K kills — into a CONTINUOUS health-and-emergence signal over the growing kill ledger. One tick = scan Theseus corpus + Stygian/Pollux/Erebos ledgers, compute MI(kill_pattern, generator_id) with a permutation null, cluster the largest kill_patterns, and emit a gradient_archaeology artifact with a SELF_AUDIT_ALARM if mi_z drops below 2.0 for 7 consecutive ticks. Targeted problem: detect whether the kill geometry has live cross-operator structure (different operator classes failing in characteristically different ways) and whether that structure is decaying or going stale.
- **Value thesis (Q2):** Meant to be the substrate's gradient-archaeology health monitor: a cheap nightly instrument whose mi_z is a drift indicator, whose top kill-pattern clusters become primitive_proposal candidates and high-priority attack targets fed to Stygian (closed-loop: Hecate finds cluster -> Stygian validates with v10 battery -> kill grows ledger -> next Hecate tick sees refined geometry), and whose dominant kill_pattern seeds a daily Pythia/Gemini retraction-pattern Deep Research survey to refine the taxonomy. Intended consumers: Stygian (attack queue), Techne/Aporia (taxonomy + alarm triage), and the primitive_proposal pipeline.
- **Why it fell short (Q3) [unverified claim]:** The honest signal is NULL by Hecate's OWN test. Raw mi_z is enormous (1000+) but is 100% generator-prefix tautology — every generator emits prefix-coded patterns (a1_*, h2_*, stygian_*) by construction, so MI(kill_pattern, generator_id) measures the prefix, not emergence. Hecate correctly built a cross-generator audit to strip this; the de-tautologized cross-gen mi_z crosses the program's own z>=2.0 threshold in exactly 1 of 574 ticks (z=2.48 on n=4 patterns — a 2-sigma noise blip, and MI on n=4 is upward-biased per feedback_mi_bias). So the only non-circular reading is dead-NULL. This is the claim-space-exhaust + decorative-mechanism rut: the closed loop to Stygian is also decorative — Stygian short-circuits every HECATE-* target with verdict=UNVERIFIED, kill_pattern='stygian_hecate_meta_test_not_yet_implemented' (242 records, the 5th-largest cluster IS Stygian telling Hecate 'not implemented'). The 29 DR retraction reports have zero downstream consumers in aporia/ergon/techne. Last ran 2026-05-30, ~3.5 weeks before the 06-23 REVIVE call. The disposition plan's 'Real signal per reset' justification cites the wrong number (raw tautological MI, not the cross-gen audit).
- **Future options (Q4) [suggestions]:**
    - retire-after-HITL: stop the daemon and crons (already stopped 05-30), keep code+574 artifacts+29 DR reports read-only; its non-circular signal is NULL by its own audit and its consumer loop is a stub
    - refactor-to-real-emergence-monitor: drop generator_id (a tautological label) and recompute MI against an ORTHOGONAL operator-class axis derived from kill_vector geometry or a Sphinx category — only revive if a pilot shows cross-axis mi_z>2 surviving the permutation null on n>>4 patterns
    - adapt-to-spine-instrument: re-point at the metabolization loop's decider — measure MI(kill-geometry, Learner-behavior-delta) so the output changes a downstream model instead of feeding a stub queue
    - retire-and-lift-assets: extract the stratified-ledger-sampler, permutation_null, and the measurement-circularity (prefix-strip cross-gen MI) audit into harmonia/lib/falsification_primitives/ per the 06-15 reset extraction plan, then retire the daemon
- **Rubric (unverified):** realized=none — output changes no downstream model today; Stygian consumer is a not-implemented stub, DR reports have 0 consumers, last ran 05-30 - latent=low — methodology (stratified sample + perm null + de-tautology audit) is reusable, but the metric itself is structurally circular against generator_id; ceiling stays low until the operator-class axis is made orthogonal - niche=redundant — overlaps Stygian (attack queue it feeds is stubbed) and the kill-ledger MI is also computable inline; its unique asset is the circularity audit, not the live signal - health=dead-gated / decorative — runnable code, stopped 05-30, but its closed loop terminates in a 'not_yet_implemented' stub and its DR output is unconsumed - debt=med — meets the CHARTER spec (computes MI, runs null, alarms) but misses the needle: the headline raw mi_z=1000+ is tautology, and the disposition plan was REVIVE'd on that misread number
- **Salvage IP (lift before any retire):** (1) The measurement-circularity / cross-generator MI audit (daemon.py _analyze: prefix-strip canonicalizer + >=2-generator membership gate) — a reusable anti-tautology pattern for any label-vs-label MI claim. (2) Stratified mtime-descending ledger sampler with self-documenting sampling_context (_harvest_records) — the fix for the alphabetical-window monoculture artifact, per feedback_sampling_strategy_is_analysis. (3) The categorical permutation_null + entropy/MI primitives (_mi, _permutation_null). All three are named in the 06-15 reset's harmonia/lib/falsification_primitives extraction list. Plus keep the 574 artifacts + 29 DR retraction reports as a read-only dataset of the kill-pattern taxonomy.
- **Suggested settle condition (to verify, not act):** Run Hecate's cross-generator (prefix-stripped, de-tautologized) MI on the current merged ledger across 5+ seeds. REVIVE/REFACTOR only if cross-gen mi_z >= 2.0 holds on n >> 4 cross-generator canonical patterns and survives the permutation null across all seeds (with an MI small-sample bias correction). If it stays sub-2 / single-digit-n (as in 573 of 574 historical ticks), the live signal is confirmed NULL -> RETIRE-after-HITL.
- **Evidence read:** Read charon/agents/hecate/CHARTER.md and charon/agents/hecate/daemon.py (full, 824 lines). Surveyed all 574 artifacts in charon/agents/hecate/artifacts/ (range 2026-05-19 to 2026-05-30): raw mi_z values 1000+; cross-gen mi_z >= 2.0 in exactly 1/574 ticks (z=2.48, n=4 patterns); 240 ticks at n_crossgen=4, 115 at n_crossgen=0. Read latest artifact gradient_archaeology_20260530T162054Z.md (mi_observed=4.64, mi_crossgen=0.0034, mi_crossgen_z=0.982). Confirmed consumer stub in charon/agents/stygian/executor.py (HECATE-* -> stygian_hecate_meta_test_not_yet_implemented). Confirmed 29 unconsumed DR reports under aporia/docs/deep_research_reports/. Prior verdicts: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md line 59 (REVIVE, 'Real signal per reset' — cites raw not cross-gen MI), pivot/REASSESSMENT_2026-06-22_consolidated.md line 754, aporia/docs/STATUS_2026-06-15_reset.md lines 99-108 (extraction list), aporia/docs/program_audit_2026-06-10.md line 247. Cron via scripts/charon_loop.py.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Lethe  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M2 - tool - op=Charon - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Lethe is a Charon-swarm child daemon (charon/agents/lethe/daemon.py, CHARTER.md) built to industrialize the "Saxl-capture" pattern: it fires cold (un-primed) LLM completions (DeepSeek via the shared cascade) at a curated CONJECTURE_CATALOG of 15 recently-settled-or-famously-open conjectures, then uses an independent LLM-judge to classify each emission's status claim (SOLVED/OPEN) and compares it to the registered ground truth. When >=30% of cold samples produce the wrong status (the "false-form"), it emits an anti_anchor_candidate_*.md artifact; otherwise a null_probe. The targeted problem: LLM training corpora carry stale/wrong status anchors (e.g. claiming Schinzel-Zassenhaus open after Dimitrov 2019, or Saxl 'solved' from Lee 2025's withdrawn preprint) that, if they leak into the Learner training corpus, propagate fabrications. The motivating success story (per CHARTER) was one anti-anchor catch that reverted four documents and registered two sub-anchors before a fabrication entered the v1.0 Learner corpus.
- **Value thesis (Q2):** Intended value: a cheap (~$0.001-0.01/tick), continuously-running sentinel that pre-detects false 'X solved Y' / 'Y still open' anchors and surfaces them to Aporia for primary-source pinning and promotion into techne/registry/anti_anchors.jsonl (the AA-### corpus). The downstream consumer is the anti-anchor registry, which the substrate uses to reject sentinel-violating claims before they reach Learner training. Lethe also seeds Pythia Deep Research 'forward false-anchor hunt' prompts (substrate type A) per tick. So the value chain is: cold-probe failure -> candidate -> Aporia/Phylax review -> registry entry -> protects the Learner corpus.
- **Why it fell short (Q3) [unverified claim]:** SATURATED / consumer-drift. (1) Realized contribution to the registry is zero: anti_anchors.jsonl holds AA-001..AA-016, every one sourced from Techne/Gemini Deep Research waves and pilots — not a single entry traces to a Lethe candidate. Lethe wrote 43 anti_anchor_candidate artifacts and 448 null_probes (573 files), none promoted. The actual registry pipeline is Techne+DR, which already produces richer, primary-source-pinned entries; Lethe is redundant with it. (2) The binary OPEN/SOLVED probe is the wrong resolution: frontier models in the cascade get the catalog right, so emit_rate is flat 0.0 for ~13 of 15 entries; the only sustained signal is schinzel_zassenhaus (and flickering 0.25-0.5 on sensitivity/ternary_goldbach), driven by one weak provider (DeepSeek-V3-vintage) hallucinating an invented statement of the conjecture. The charon_swarm_diminishing_returns doc (2026-05-25) explicitly tags Lethe SATURATED: "28 ticks ... emitted ~0 candidates ... catalog is calibrated for a model class the cascade has already outgrown." (3) The real LLM failure modes the doc identifies (year-misattribution, qualifier-clipping, witness-naming, adjacency-confusion) are exactly the subtle anti-anchors a binary status probe cannot catch — the candidate artifacts even show the LLM fabricating the entire conjecture statement while Lethe only scores the SOLVED/OPEN bit. (4) The daemon is currently dead-infra: last_probed timestamps and newest artifacts all stop 2026-05-30 (~3.5 weeks idle). The 2026-06-23 disposition plan lists it at #28 in RETIRE: "anti-anchor miner — overlaps Clio + cold-LLM probes; low marginal yield."
- **Future options (Q4) [suggestions]:**
    - retire-and-lift-asset: archive the daemon read-only (no delete), but FIRST lift the CONJECTURE_CATALOG (15 entries with prompt/false_form/true_form/citation/registered_status) into techne/registry as a permanent calibration/sentinel set — it is hand-curated, primary-source-pinned anti-anchor bait worth keeping
    - refactor-to-attack-mode-rotation (doc E1): replace the single binary OPEN/SOLVED probe with multi-mode probes — year-misattribution, qualifier-clipping, witness-naming, adjacency-confusion — which target the failure modes frontier models actually still exhibit; only revive if a smoke test shows non-trivial emit on a CURRENT-cascade model
    - adapt-to-Learner-regression-canary: re-point Lethe at the locally-owned models (Apollo/Rhea) instead of the frontier cascade, where stale-anchor hallucination is far more likely and where a false-form catch directly protects the Learner training corpus (its stated raison d'etre) — this attaches it to the metabolization spine instead of frontier QA
    - fold into Techne+DR anti-anchor pipeline: retire Lethe as a separate daemon and keep only the DR forward-false-anchor prompt builder (_build_dr_prompt), which is the one part whose output (DR-confirmed candidates) has a viable promotion path
- **Rubric (unverified):** realized=none — 0 of 43 candidates promoted; all 16 registry AA-entries came from Techne/Gemini DR, not Lethe; no downstream model behavior changes today - latent=low-to-med — only if refactored to attack-mode probes against the OWNED Learner models (Apollo/Rhea), where stale-anchor hallucination is real and a catch protects the training corpus; against the frontier cascade latent value is ~none - niche=redundant — overlaps the Techne+Pythia DR anti-anchor pipeline (which actually fills anti_anchors.jsonl) and, per disposition plan, overlaps Clio + cold-LLM probes; not a unique seat - health=dead-infra — daemon idle since 2026-05-30 (~3.5wk); runnable code but not scheduled; emit signal flat for ~13/15 catalog entries - debt=med — meets its narrow spec (binary status probe) but misses the needle: the probe resolution cannot catch the subtle anti-anchors (year/qualifier/witness/adjacency) that frontier models actually emit, and it is calibrated to an outgrown model vintage (DeepSeek-V3); decorative-mechanism risk — artifacts look like residue but never moved a consumer
- **Salvage IP (lift before any retire):** (1) CONJECTURE_CATALOG in daemon.py lines 26-180: 15 hand-curated, citation-pinned conjecture entries (prompt / false_form_patterns / true_form_summary / citation / registered_status) — a reusable calibration + sentinel dataset; lift into techne/registry. (2) The independent LLM-judge status-classifier pattern (_judge_sample_status, lines 294-341): narrow one-word SOLVED/OPEN/UNCLEAR judge that is blind to ground truth, with robust token-tolerant parsing — reusable across any agent needing unbiased LLM-self-judging. (3) The decay/anti-capture mechanism (cumulative_emission_rate + DECAYED demotion + re-emergence alarm as a training-data-regression canary) — a genuinely useful idea worth porting to the owned-model QA loop. (4) _build_dr_prompt: the forward-false-anchor DR prompt template (requires dual primary-source citations, rejects blog/slide counter-signals).
- **Suggested settle condition (to verify, not act):** Run one refreshed smoke battery: probe the full 15-entry catalog (and ideally the E1 attack-modes: year/qualifier/witness/adjacency) N>=8 samples each against (a) the CURRENT frontier cascade and (b) the owned Learner-class model (Apollo/Rhea). REVIVE/REFACTOR is justified only if it produces at least one Aporia-confirmable, primary-source-pinned anti-anchor that is NOT already in anti_anchors.jsonl AND that the Techne+DR pipeline did not already surface. If the cascade stays at ~0 emit (modulo the lone DeepSeek schinzel artifact) and the owned model shows no novel false-form, RETIRE stands — the catalog is lifted as a static sentinel set and the daemon is archived read-only.
- **Evidence read:** READ: charon/agents/lethe/CHARTER.md; charon/agents/lethe/daemon.py (full, 604 lines); charon/agents/lethe/state/{last_probed.json, emission_history.json, dr_seeded.json} (last activity 2026-05-30); sample artifact anti_anchor_candidate_schinzel_zassenhaus_20260530T121439Z.md; artifact dir tally (573 files: 43 candidates across 6 conjectures, 448 null_probes). techne/registry/anti_anchors.jsonl (AA-001..AA-016, all Techne/Gemini-DR sourced — NONE from Lethe). PRIOR VERDICTS: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (#28 RETIRE: 'overlaps Clio + cold-LLM probes; low marginal yield'); pivot/charon_swarm_diminishing_returns_2026-05-25.md ('Lethe: SATURATED ... 28 ticks emitted ~0 candidates ... catalog calibrated for a model class the cascade has already outgrown', plus E1 attack-mode-rotation proposal); pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (lists Lethe among Charon extras under review). harmonia/agents/lethe/ exists but is an empty artifacts/state scaffold (no code).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Moros  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M2 - tool - op=Charon - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Moros automates Charon CHARTER §6: the rule that every load-bearing substrate artifact (pivot/*.md, harmonia/memory/architecture/*, aporia/doctrine/*, roles/*/CHARTER.md) gets a multi-frontier-model adversarial critique pass before promotion. The targeted problem was a human bottleneck: the cross-pollination protocol existed but required James or Charon to *remember* to fire it manually (~$0.50-$5/round). Moros makes it mechanical — one tick = pick the next un-cross-pollinated load-bearing artifact, fan it out to N=3 distinct frontier models via llm_cascade, write a feedback_<slug>.md + meta_analysis_<slug>.md, and run a convergence analysis (pairwise Jaccard + bigram overlap on critique bullet-heads) to emit a PATTERN_* candidate when models agree on a structural defect. It is a Charon swarm child daemon (charon/agents/moros/daemon.py, ~730 LOC), running in the 8-agent bi-hourly rotation alongside Pollux/Erebos/Stygian/Lethe/Acheron/Hecate/Nephele.
- **Value thesis (Q2):** Intended value: a mechanical pre-promotion gate that catches narrative fragility, missing citations, hand-waved evidence, premature framing closure, and HARD-5 coordinate-collapse before an artifact gets ingested downstream — exactly the failure modes the program's memory (feedback_*.md doctrine files) keeps re-learning the hard way. Designed as the UPSTREAM half of a Moros->Phylax pair: Moros generates the adversarial material + convergence triage; Phylax (harmonia/agents/phylax) runs its mechanical null-violation/retraction-adjacency gate on the cross-pollinated artifact and reads meta_analyzed rows for the gate-feed. Intended consumer: Phylax, and through it the artifact-promotion pipeline; secondary consumer is James reading feedback before promotion. An anti-capture safeguard (SELF_AUDIT_NULL) was built in to detect correlated-mutation failure (LLM converging into agreement with the artifact).
- **Why it fell short (Q3) [unverified claim]:** Multiple compounding ruts. (1) DECORATIVE-MECHANISM / claim-space-exhaust: across 162 processed artifacts (93 with scores) the convergence engine NEVER once crossed its PATTERN threshold — max convergence_score 0.233, mean 0.092, against a 0.25 threshold that had ALREADY been lowered from 0.40 after empirical failure. ZERO pattern_candidate artifacts were ever emitted in its entire history. The token/bigram-Jaccard convergence signal is the wrong instrument (the code comments admit '0.40 was set without grounding... both turned out true'), so the one mechanism that was supposed to turn critiques into durable substrate signal produced nothing. (2) DEAD-GATED seam: the intended downstream consumer Phylax exists but does NOT read Moros output — no references to moros/cross_pollination/convergence_score/meta_analysis anywhere in harmonia/agents/phylax. The upstream/downstream pair the whole design rested on was never wired. (3) Low-signal output: sampled feedback artifacts contain raw model chain-of-thought scaffolding ('We need to produce...', 'Let's parse:') — unfiltered, often single-provider (fanout_n_ok frequently 1-2 of 3), not adjudicated critique. (4) Consumer-drift / dead-infra: the artifacts/ dir is almost entirely SELF_AUDIT_NULL markers (cascade-unavailable / no-backlog); daemon went dormant after 2026-05-30. Most damningly, Ergon's CORPUS_VALUE_AUDIT_2026-06-03 classifies the whole Charon pantheon including Moros as 'Stranded — the biggest missed opportunity': 3,185 artifacts, realized value 'None', 'markdown + kill_ledger, no LearnerRecord converter' — i.e. nothing Moros emits reaches the metabolization decider.
- **Future options (Q4) [suggestions]:**
    - retire-and-lift-asset: RETIRE-after-HITL the daemon (read-only, keep the 30 feedback + 26 meta_analysis artifacts and processed_artifacts.json ledger). The convergence-never-fired result IS a durable finding: 'naive token/bigram-Jaccard cannot detect cross-model structural agreement' — lift it into a feedback_*.md doctrine note so the program doesn't rebuild this instrument.
    - refactor-to-capability-attached: rewire Moros output to the Learner seam instead of the dead Phylax seam — write a LearnerRecord/kill-geometry converter so cross-model critiques become training residue a model consumes (the agreed decider). Only worth it if a convergence signal that actually fires is found first.
    - adapt-to-better-convergence-detector: replace token/bigram-Jaccard with an embedding-similarity or LLM-judge convergence scorer over the critique bullets; re-run on the existing 162-artifact corpus offline to test whether ANY real cross-model agreement exists before reviving the daemon.
    - salvage-the-DR-prompt-builder: lift _build_dr_prompt (the technique-transfer cross-pollination Deep Research prompt) and the artifact-detection/processed-ledger plumbing into a still-live agent (Pythia/DR queue), retire the rest.
- **Rubric (unverified):** realized=none — 0 PATTERN candidates ever fired across 162 artifacts; Phylax never reads its output; Ergon audit rates Charon-pantheon realized value 'None'. No downstream model behavior changes from Moros today. - latent=low-med — the artifact-detection + processed-ledger + DR-prompt plumbing is reusable, and the 'convergence-never-fired' negative result is a genuine durable finding; but the core convergence mechanism is broken and the value depends entirely on a rewire to the Learner that does not exist. - niche=partially redundant — adversarial cross-pollination of substrate claims overlaps the program's manual frontier-review habit and Phylax's mechanical gate; the automation niche (mechanical pre-promotion pass) is unique but unfilled because the seam is dead. - health=dead-gated + dormant — daemon last ran 2026-05-30; artifacts/ dir is overwhelmingly SELF_AUDIT_NULL markers; intended consumer (Phylax) not wired; swarm pid present but rotation history stopped. - debt=high — the convergence threshold was tuned downward to chase a signal that never existed (meets-spec-misses-needle: it 'works' mechanically but the one output that matters never fires); feedback artifacts ship raw model chain-of-thought; single-provider fanout common, so 'convergence' is often uncomputable.
- **Salvage IP (lift before any retire):** Before retire, lift: (1) the 30 feedback_*.md + 26 meta_analysis_*.md cross-pollination corpus and processed_artifacts.json (162 artifacts with provider + convergence scores) — a ready-made offline test bed for any future convergence detector; (2) _build_dr_prompt — the technique-transfer (functor/base-change/coordinate-translation) Deep Research prompt builder, reusable by Pythia/DR queue; (3) the artifact-detection globs + processed-files ledger primitive (file_sha + path dedup) for git-watch in any other daemon; (4) the negative finding itself: 'token/bigram-Jaccard never crossed even a lowered 0.25 threshold across 162 artifacts' as a doctrine note so the convergence instrument is not rebuilt naively.
- **Suggested settle condition (to verify, not act):** Run the existing 162-artifact critique corpus through a replacement convergence scorer (embedding-cosine or LLM-judge over the per-model critique bullets, not token/bigram-Jaccard). If even ~5-10% of artifacts show genuine cross-model structural agreement on a SPECIFIC defect (not shared artifact vocabulary), AND at least one such agreement, when converted to a LearnerRecord, survives an ablation in the metabolization loop — then REFACTOR/REVIVE (rewire to Learner, replace scorer). If the replacement scorer also finds no real convergence, or convergent critiques don't change downstream model behavior under ablation, then RETIRE stands: cross-model adversarial critique on substrate prose is exhaust, not residue.
- **Evidence read:** Read: charon/agents/moros/CHARTER.md, charon/agents/moros/__init__.py, charon/agents/moros/daemon.py (full, ~730 LOC), charon/agents/DESIGN_2026-05-19.md (§4 Moros + agora.cross_pollination_jobs schema). Inspected: charon/agents/moros/artifacts/ (overwhelmingly self_audit_null markers), charon/agents/moros/state/processed_artifacts.json (162 entries; max conv 0.233, mean 0.092, threshold 0.25, 0 pattern candidates), charon/agents/_rotation_state.json (25 Moros ticks, last 2026-05-30T16:15, all pattern_candidate=false). Sampled pivot/feedback_promote_filter_audit_2026-05-30.md and feedback_lean_substrate_next_steps_2026-05-30.md (raw model CoT in critiques, single-provider fanout). Prior verdict: roles/Ergon/CORPUS_VALUE_AUDIT_2026-06-03.md classifies Charon pantheon (incl. Moros) as 'Stranded — the biggest missed opportunity', realized value None, no LearnerRecord converter. Confirmed harmonia/agents/phylax exists but contains NO references to moros/cross_pollination/convergence_score/meta_analysis (seam unwired).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Nephele  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M2 - tool - op=Charon - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Built per James's 2026-05-24 directive ("write your own Clio fallback generator but slow roll it... only if Clio not active for 4 hours") as part of the Charon swarm v0.5. The targeted problem: single-point-of-failure on external substrate inflow. Clio is the primary paper-mining daemon feeding new arxiv math/CS papers into the substrate; if Clio's daemon dies, external substrate inflow stops silently. Nephele is a deliberately minimal hot-standby that keeps a trickle of arxiv RSS candidates flowing during a Clio outage, then auto-reverts to no-op when Clio recovers.
- **Value thesis (Q2):** Value = availability insurance, not signal generation. It intentionally has a tiny surface area (one urllib call, one XML parse, no Postgres, no LLM) so it cannot duplicate or contaminate Clio's first-class extraction/index. Doubly gated (Clio log stale >=4h AND own 30-min throttle) so even a dead-Clio scenario does not spam arxiv at Charon's 4-min rotation cadence. Consumer: downstream substrate processors (Aporia / Penelope ingest / a recovered Clio that supersedes the raw candidates). Emitted candidate_paper_*.md files are explicitly marked NOT-yet-Sigma-submitted, NOT-yet-LLM-extracted — raw holding-pattern artifacts.
- **Why it fell short (Q3) [unverified claim]:** Did not fail on its own terms — it fell short of mattering. Run history is 286 artifacts, all clio_active_skip_*, with ZERO candidate_paper_* ever emitted: the fetch path has never once executed because Clio never went silent >=4h during the observation window. This is the dead-gated rut: a correctly-implemented mechanism whose activation condition has never been met, so its realized contribution is identically zero. Compounding it: consumer-drift / redundancy — the 06-23 disposition plan revives Clio (PG-wired agora.clio_papers) as P1 support, and a revived primary makes a same-channel raw-RSS fallback redundant. It also produces zero kill-geometry and never touches the forge->Learner metabolization spine, so by the program's decider it is pure exhaust-class infrastructure, not residue.
- **Future options (Q4) [suggestions]:**
    - RETIRE-after-HITL: pause the loop, kill its cron rotation slot, keep code+data read-only. The honest call given 286/286 skips and Clio-revival redundancy.
    - Refactor-to-generalized-watchdog: lift the doubly-gated mtime-staleness + self-throttle activation pattern into a shared 'hot-standby fallback' mixin in charon/agents/_base.py, usable for ANY primary/standby pair (not just Clio), so the one genuinely reusable asset survives retirement.
    - Adapt-to-real-fallback-fidelity: if kept, raise its value by having the fire path enqueue through Clio's actual PG ingest (agora.clio_papers) or hand off to Penelope, so an outage produces first-class substrate instead of orphan raw markdown that nobody consumes — but only worth it if Clio outages are an observed real risk.
    - Revive-as-is ONLY if Clio is NOT revived: if the disposition flips and Clio retires instead, Nephele becomes the sole (degraded) external-inflow channel and earns continued life by default.
- **Rubric (unverified):** realized=none — 286/286 skip artifacts, zero candidate papers ever emitted; fetch path has never executed; output changes no downstream model today - latent=low — ceiling is 'raw arxiv RSS candidates during a Clio outage'; even fully activated it produces un-extracted holding-pattern markdown, not kill-geometry; only feeds the spine indirectly via Clio/Penelope - niche=redundant — same external-substrate channel as Clio (the primary it shadows); unique only while Clio is dead, which has never occurred - health=dead-gated — code is runnable and correct (clean skip path, round-robin, throttle), but its activation gate has never fired; it is a healthy mechanism with an unmet trigger - debt=low — small, self-contained, no-LLM/no-PG by design; the shortcut is that the fire path emits orphan raw markdown with no wired downstream consumer (meets-spec-as-insurance, misses-needle-as-substrate)
- **Salvage IP (lift before any retire):** Lift before any retire: (1) the doubly-gated activation pattern — primary-liveness-via-log-mtime-staleness (_clio_age_hours) combined with a self-throttle on own last-action (_should_fire) — generalized into a reusable hot-standby/watchdog mixin; (2) the curated 5-feed arxiv RSS map with per-feed substrate rationales (math.NT/AG/CT/CO, cs.LG) and the dependency-free _fetch_arxiv_rss + _parse_arxiv_rss pure functions, reusable by any lightweight ingest agent. The 286 skip artifacts are themselves an uptime-proxy dataset (timestamps prove Clio liveness across the window) and should be kept, not deleted.
- **Suggested settle condition (to verify, not act):** Replay the activation gate against the actual Clio daemon-log history (or simulate a >=4h Clio silence): if Clio has genuinely been silent >=4h at any point AND a revived Clio would NOT have covered that window, then Nephele's fallback is load-bearing -> REFACTOR (wire fire path to PG ingest). If Clio's uptime shows no such gap (consistent with 286/286 skips) and Clio is on the REVIVE list, the standby is redundant -> RETIRE-after-HITL. Falsifier for retirement: a demonstrated Clio outage window where external inflow stopped and only Nephele could have refilled it.
- **Evidence read:** READ: charon/agents/nephele/daemon.py (303 lines, full), charon/agents/nephele/__init__.py, charon/agents/_base.py (CharonAgent base). RUN HISTORY: ls charon/agents/nephele/artifacts/ = 286 files, ALL clio_active_skip_* (first 20260524T0707, span through 06-23+), ZERO candidate_paper_* (fire path never executed); state/ holds only an empty dr_inbox (no last_fetch_at -> fetch never ran). PRIOR VERDICTS: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md line 82 (ARCHIVE: 'Clio-fallback gatherer — redundant with Clio revived'); pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md lines 116-118 (downgraded to PENDING-REVIEW, Charon extras queue); aporia/docs/program_audit_2026-06-10.md line 248 ('Nephele cheap, keep'); pivot/charon_swarm_2026-05-27.md lines 37,71 (design spec + first-tick 'skip (clio_active_0.43h_ago)'); pivot/agent_roster_2026-05-28.md (active, 32m cadence, 43 ev); docs/state.json lines 523-538 (status UNKNOWN, no PG heartbeat). Clio primary confirmed via data/clio dir + agora.clio_papers PG wiring referenced in disposition plan line 54.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Pollux  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M2 - tool - op=Charon - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Pollux (charon/agents/pollux/daemon.py, 6th Charon swarm member, v0.5) was built to be the SECOND non-Theseus operator class capable of populating the kill_ledger. Per its __init__ and the Tier-1 roadmap (pivot/stygian_executor_scoping_2026-05-21.md), Hecate's cross-generator MI(kill_pattern, operator-class) audit was structurally zero because every ledger row came from one generator (Theseus, then Stygian). Pollux exists to add a parallel operator so cross-generator MI becomes non-zero / meaningful. Mechanism: a numerical-coincidence scanner that, per tick, picks one pair of Mahler-measure (Mossinghoff) subsets, computes Spearman correlation BEFORE and AFTER mean-spacing normalization (directly operationalizing memory doctrine feedback_scale_vs_shape: 'for ANY gap comparison, test mean-spacing normalization FIRST'), and emits a Theseus-shaped kill_ledger row with one of three kill_patterns (sign-flips / survives / no-correlation).
- **Value thesis (Q2):** Two intended consumers. (1) Hecate's gradient-archaeology MI audit: Pollux is registered as a ledger source in hecate/daemon.py LEDGER_CANDIDATES and its pollux_ prefix is in Hecate's canonicalization regex, so its rows are meant to give the cross-operator MI a second class to correlate against. (2) Stygian's v10 battery: on a PROMOTED verdict (correlation survives normalization) Pollux enqueues the pair to stygian_priority_queue with full provenance, so genuine shape-signal pairs get escalated to harder validation. The framing value: a scale-vs-shape filter that flags raw correlations that are scale-driven artifacts (sign-flip under normalization = REJECT) vs real shape signal (survives = PROMOTE).
- **Why it fell short (Q3) [unverified claim]:** Two compounding failures. (A) DECORATIVE-MECHANISM / measurement bug: the 'raw Spearman' is a mathematical tautology. run_tick does a_paired=sorted(a_vals)[:n], b_paired=sorted(b_vals)[:n] then Spearman of two independently-sorted ascending sequences — which is identically +1.0. The data confirms it: ALL 286 ledger rows have corr_raw=1.0. So the headline 'sign-flips-under-normalization' compares a CONSTANT 1.0 against the gap-correlation; PROMOTE/REJECT collapses to merely the sign of corr_norm on min-truncated, arbitrarily-paired subsets. There is no random-pairing null, no permutation null, no seed replication — violating the program's own hard postures (feedback_mi_bias 'use random-pairing null', feedback_permutation_null, feedback_replicate_seeds, feedback_false_profundity). The pairing itself (truncate two unrelated distributions to min length, pair by sorted rank) is not a meaningful coincidence test. So the '39 PROMOTED / real signal' claim in pivot/COMPONENT_DISPOSITION_PLAN and REASSESSMENT_2026-06-22 is unaudited and most likely an artifact. (B) CONSUMER-DRIFT / dead-infra: even taken at face value, Pollux's output never reaches the spine's decider (the metabolization loop: Forge->Learner consumes kill-geometry, behavior changes, survives ablation). The Ergon Learner has ZERO references to generator_id=pollux. Its only live consumer was Hecate's cross-gen MI, and Charon's own 06-03 journal records that cross-generator-overlap audits collapse under permutation nulls (p=0.075-1.00, FALSIFIED/UNDERDETERMINED) — the very signal Pollux was created to feed is itself null. Daemon last ran 2026-05-30 (artifacts/state mtimes); it has been dormant ~3.5 weeks.
- **Future options (Q4) [suggestions]:**
    - RETIRE-after-HITL: move charon/agents/pollux to archive/ read-only (code + 435KB kill_ledger.jsonl + settled_pairs.json kept), kill its loop/cron, and remove it from Hecate's LEDGER_CANDIDATES so the MI audit stops ingesting tautological rows. This is the honest call given corr_raw=1.0 everywhere and no Learner consumer.
    - REFACTOR-the-statistic-then-revive: replace the sorted-rank pairing with a genuine coincidence test (random-pairing null + matched-marginal/column-permutation null per feedback_mi_bias and feedback_null_must_perturb_the_statistics_axis), drop the meaningless corr_raw=1.0 leg, report z vs null with 5+ seeds. Only worth it if a real consumer is named first.
    - ADAPT-to-Learner (spine-attach): rewire Pollux output so PROMOTED pairs become capability-attached residue the Forge/Learner actually trains on (kill-geometry that changes model behavior and survives an ablation), not just a Stygian-queue/Hecate-MI feed. Without this attachment it stays exhaust, not residue.
    - RETIRE-and-lift-asset: retire the daemon but lift the reusable _mean_spacing_normalize + dependency-free _spearman + the Mahler-subset loader (_load_subset, 7 subset 'kind' selectors over prometheus_math.databases.mahler) and the SEED/CANDIDATE pair taxonomy into Techne/prometheus_math as a tested scale-vs-shape utility.
- **Rubric (unverified):** realized=none — output reaches Hecate MI (itself null under perm-test) and a Stygian queue, but changes NO downstream model today; Learner has zero pollux references - latent=low — the scale-vs-shape filter idea is sound but the current statistic is a sorting tautology; latent value is the lifted utilities + pair taxonomy, not the agent - niche=redundant — its kill_ledger-population role overlaps Stygian; its scale-vs-shape transform is the generic feedback_scale_vs_shape numpy idiom; its only unique claim (2nd operator class) serves a Hecate MI audit that is itself falsified - health=dead-gated / decorative — daemon dormant since 2026-05-30; the headline 'raw Spearman' is identically 1.0 by construction (decorative mechanism), so verdicts are unaudited - debt=high — emits tautological corr_raw=1.0 into a shared ledger that Hecate ingests, no null/seed discipline (violates feedback_mi_bias, feedback_permutation_null, feedback_replicate_seeds); disposition docs assert 'real signal' without auditing the 1.0 artifact
- **Salvage IP (lift before any retire):** Lift before retire: (1) _mean_spacing_normalize() — clean dependency-free sort/consecutive-gap/mean-normalize transform; (2) _spearman() — pure-python scipy-free rank correlation; (3) _load_subset() with its 7 Mahler-subset selectors (by_degree, by_class salem/pisot, smyth_extremal/non_smyth, degree_parity, degree_range, M_range) over prometheus_math.databases.mahler — a reusable Mossinghoff slicing layer; (4) the SEED_PAIRS + CANDIDATE_POOL pair taxonomy (Lehmer-witness neighborhood, narrow-band, even/odd-degree) as a documented hypothesis menu; (5) the 435KB kill_ledger.jsonl + settled_pairs.json + pair_history.json as a kept dataset (with the corr_raw=1.0 artifact flagged in provenance). Best home: Techne/prometheus_math as a tested scale-vs-shape utility module.
- **Suggested settle condition (to verify, not act):** Run the missing null on Pollux's own 'PROMOTED' pairs: for each PROMOTED pair (deg14_vs_deg16, salem_vs_pisot, small_deg_vs_large_deg), compute corr_norm against a random-pairing / matched-marginal permutation null over >=5 seeds. If the observed normalized Spearman survives the null at p<0.05 across seeds AND a named spine consumer (Learner ablation) can show the signal changes model behavior, then REFACTOR-and-revive is justified. If it does not survive (expected, given the pairing is arbitrary min-truncated sorted ranks and the raw leg is a constant 1.0), RETIRE-after-HITL stands and Pollux must be removed from Hecate's LEDGER_CANDIDATES.
- **Evidence read:** READ: charon/agents/pollux/daemon.py (full, 615 lines — confirmed sorted/sorted pairing at lines 417-424 forcing corr_raw=1.0), charon/agents/pollux/__init__.py. DATA: charon/agents/pollux/state/kill_ledger.jsonl (286 rows; verdicts REJECTED 86 / PROMOTED 39 / UNVERIFIED 161; ALL corr_raw=1.0), pair_history.json, settled_pairs.json, candidate_pool_idx.json (=5), ~200 artifacts/scan_*.md (last 2026-05-30). DOWNSTREAM: charon/agents/hecate/daemon.py (lines 30-35 registers Pollux ledger; 318-332 pollux_ prefix canonicalization), confirmed grep shows ergon/Learner has ZERO generator_id=pollux references. VERDICTS: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (row 34: REVIVE, '2nd non-Theseus operator class, Real signal'), pivot/REASSESSMENT_2026-06-22_consolidated.md (line 754: 'keep Hecate+Pollux+Moros; Pollux = real signal'), pivot/agent_roster_2026-05-28.md (active, 28m cadence, 43 ev/24h), charon/CHARON_SESSION_2026-06-03.md (cross-generator MI nulls FALSIFIED/UNDERDETERMINED p=0.075-1.00 — the audit Pollux feeds is itself null), charon/CHARON_SESSION_2026-06-15.md (reduced Charon rotation framing).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Stygian  -  *AI suggestion (advisory, NOT approved): REVIVE*
*M2 - tool - op=Charon - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Stygian was built to be the v10-battery execution worker that converts Hecate's gradient-archaeology demand into actual falsification current. Before it, the Charon swarm's verification valve was closed: attack PLANS were written but no battery ran against new targets, so Hecate looped on stale Theseus data. Stygian picks one Atlas/queue-fed problem per tick, runs the frozen UnifiedBattery, and emits a Theseus-shaped kill_ledger row.
- **Value thesis (Q2):** Value thesis: Stygian is the verification CURRENT in the Hecate->Stygian->Hecate closed loop. Its immediate consumer is Hecate (hecate/daemon.py line 32 reads Stygian's kill_ledger.jsonl directly), which computes MI(kill_pattern, operator-class) — the live health signal the 06-15 reset and 06-10 audit both flagged as REAL. Stygian's emitted generator_id='stygian' adds a distinct operator class to that MI calculation, and its battery verdicts (PROMOTED/REJECTED) on attacked targets provide the falsification current that turns Hecate's demand signal into navigable gradient. Secondarily it became the EXECUTION engine for Erebos compositions: the executor grew ~30 composition loaders (G02..G25 Lehmer/Mahler families + BSD) and is the only thing that runs Erebos composed-claim payloads to a real battery verdict. It is NOT a direct feeder of the Learner spine — that corpus path runs through Penelope/Talos.
- **Why it fell short (Q3) [unverified claim]:** Two real shortfalls, both in the ledger. (1) Coverage-debt / decorative-mechanism on the curated roster: of 373 kill_ledger rows, 366 are UNVERIFIED — 242 'hecate_meta_test_not_yet_implemented' + 101 'no_loader_registered'. Only ONE seed loader (BL-C-001 Lehmer) and a partial BL-C-002 BSD ever lit; the program_audit (06-10) says bluntly "92% of its rows are 'no loader yet'". So the bulk of ticks emit typed short-circuit rows that prove the daemon ran but verify nothing — by its own CHARTER's standard, an unverified plan is not substrate. (2) Consumer-collapse of its biggest real workstream: the ~30 composition loaders exist to validate Erebos claims, but after Phase-3.K, ZERO Erebos signal-claims survive permutation nulls (the reset paused Erebos). Stygian's composition verdicts confirm this honestly (REJECTED via permutation_null / residual_survival; the few PROMOTED are G02+G04 threshold-band tightenings, not transferable signal). So the loop runs and is epistemically honest, but its richest output channel feeds a paused, null-result consumer. The rut: capability-attached but downstream of a dead/paused consumer (consumer-drift), plus claim-space-exhaust on the Lehmer/Mahler monoculture the composition loaders cluster around. It did NOT fall short on engineering — the code is production-grade, runs, and self-audits.
- **Future options (Q4) [suggestions]:**
    - revive-reduced-as-is: keep Stygian as the Hecate-loop execution worker with the ONE working real-math channel (BL-C-001 Lehmer battery), drop the curated BL-C-002..010 ambition — its job is to keep feeding Hecate's live MI signal, which the reset already calls real. Cheap, honest.
    - refactor-to-spine-feeder: rewire Stygian's executor so its PROMOTED/REJECTED kill-geometry rows route into the Learner corpus (via Penelope) instead of dead-ending at Hecate. This is the only path that makes Stygian touch the decider; gate it on Penelope ingest accepting the stygian schema.
    - adapt-to-forge-probe: repurpose the ~30 composition loaders as the falsification harness for Hephaestus-forged tools (the spine), turning Stygian from an Erebos-executor into the ablation-gate runner that the metabolization loop actually needs. Salvages the composition machinery away from paused Erebos.
    - retire-after-HITL: if Hecate is itself reduced and the Learner never wires to this ledger, archive read-only and lift the reusable assets below; Stygian's curated-roster + composition channels are both downstream of paused/null consumers.
- **Rubric (unverified):** realized=low — does NOT change a downstream model today; its rows feed Hecate's MI health signal (real but diagnostic), not the Learner. The decider (forge->Learner) does not consume Stygian output. - latent=med — if rewired to feed Learner corpus (option 2) or to run Hephaestus ablation gates (option 3), it becomes the swarm's only general battery-execution engine; ceiling is real but contingent on the spine wanting battery verdicts. - niche=partially unique — only component that actually RUNS the frozen v10 battery against arbitrary queue targets and emits unified-schema rows; but overlaps Pollux (also emits battery-ish kill rows) and its compositions are redundant with paused Erebos. - health=runnable — production-grade, self-auditing, executes today; but effectively dead-gated on consumers: Erebos channel paused (perm-null nulls), curated roster 92% no-loader, Hecate loop reduced. - debt=high — 366/373 rows UNVERIFIED short-circuits; single working seed loader (Lehmer) against a 10-problem advertised roster; composition loaders are a Lehmer/Mahler monoculture. Meets-spec (emits a row every tick) while missing the needle (verifies almost nothing).
- **Salvage IP (lift before any retire):** Before any retire, lift: (1) the v10-battery loader/adapter pattern in executor.py — clean attack_plan->UnifiedBattery input mapping (distribution/correlation modes) reusable for any battery-execution worker; (2) the ~30 composition loaders under loaders/ (G02..G25 Lehmer/Mahler/Salem/degree-parity + BSD rank-strata/regulator-consistency) plus helpers _composition.py, _bocpd.py, _bootstrap_ci.py, _bsd_helpers.py, _degeneracy_precondition.py, _selection_discipline.py — these are real, tested (tests/ dir) falsification primitives; (3) the Theseus-compatible kill_ledger row schema + content-addressed _record_id hashing (cross-agent ledger interop); (4) the SELF_AUDIT_NULL anti-capture discipline pattern (every tick emits a typed artifact); (5) the BL-C-001..010 curated open-problem table with hardness signatures, modal_llm_error anti-anchor forms, and HARD-5 collision risks in daemon.py SEED_PROBLEMS — a reusable falsification-target catalog.
- **Suggested settle condition (to verify, not act):** Revive-reduced is justified IFF Hecate is itself revived AND Stygian's emitted rows measurably move Hecate's MI(kill_pattern, operator-class) signal as a distinct operator class — i.e. removing generator_id='stygian' rows changes Hecate's MI_z by a non-trivial margin across a 1-week run. If Hecate is reduced out, OR if the MI signal is unchanged with/without Stygian's rows (generator_id is redundant), then there is no live consumer and disposition drops to RETIRE-after-HITL. The stronger settle test for the latent path: wire one Stygian PROMOTED/REJECTED batch into Penelope->Learner and show a behavior delta surviving an ablation — if that fails, Stygian never touches the decider and stays a Charon-internal diagnostic at best.
- **Evidence read:** Read in full: charon/agents/stygian/executor.py (544 lines), daemon.py (592 lines), CHARTER.md. Inspected loaders/ tree (~30 composition loaders + 6 helper modules + tests/), state/last_attempted.json, state/backlog_tick_counter.json. Parsed state/kill_ledger.jsonl (373 rows): verdicts {UNVERIFIED:366, REJECTED:4, PROMOTED:3}; top kill_patterns 242 hecate_meta_test_not_yet_implemented, 101 no_loader_registered, 21 battery_verdict_possible. 583 attack_plan artifacts in artifacts/. Prior verdicts: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (#27 REVIVE support, 'KillVector artifacts'); aporia/docs/program_audit_2026-06-10.md lines 120-121 ('one loader, 92% no loader yet', ~18h to build BL-C-002..010) and line 247 (reduced rotation '+ Stygian as loaders land'); aporia/docs/STATUS_2026-06-15_reset.md line 100 (Stygian as loaders land); pivot/stygian_executor_scoping_2026-05-21.md (the build rationale / 'closed pipe'). Confirmed downstream consumer: charon/agents/hecate/daemon.py line 32 reads Stygian's kill_ledger.jsonl. No reference to Stygian in ergon/ (Learner) — confirms it does NOT feed the decider spine directly.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________


## Harmonia swarm

### Argos  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M2 - tool - op=Harmonia - shelved*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Argos is a Harmonia-swarm child daemon (M2) built 2026-05-17 to be the "many-eyed Panoptes" that continuously expands the PROBLEM_LENS_CATALOG@v1 convention. The anchor catalogs (Lehmer 28 lenses, Collatz ~18, P vs NP ~12) existed by hand, but the bulk of Aporia's ~1100-problem open-problem queue had no multi-perspective lens fingerprint. Argos's job: for each open problem, score by lens-deficit + an anti-greedy verdict bonus (+3 for map_of_disagreement), propose the next 3 unapplied lenses from the methodology toolkit / 5 disciplinary stances, write a paste-ready catalog draft, and optionally fire one Pythia Deep-Research request per tick asking for the primary-literature fingerprint of that lens stack on that problem.
- **Value thesis (Q2):** The bet (from the north-star quote embedded in CHARTER.md) is that legibility = compressing coordinate systems, and a problem becomes legible only once enough independent lenses are laid over it that their disagreement reveals the compression direction. Argos's value was breadth: every open problem touched is one more entry in a global lens-fingerprint corpus, and the cross-lens disagreement patterns ARE the structural data. Intended consumers were: (a) problem-attackers who grep catalog drafts for a pre-built MPA scaffold; (b) research-prompt curators who copy the doctrine-compliant DR sketches into Aporia's queue; (c) a coverage tracker diffing lens_history.json against promoted catalogs. Propose-only by design: Argos never edits catalogs/ or the queue; a conductor/human was meant to promote drafts into canonical catalog files.
- **Why it fell short (Q3) [unverified claim]:** Three overlapping ruts. (1) claim-space-exhaust: Argos generated 438 catalog drafts and ~13-20 DR surveys, but this is pure claim-space generation with no capability attached — per program doctrine that is exhaust, not residue. The DR reports are narrative literature surveys full of hedge language ("Research suggests", "The evidence leans toward") with no verdict that changes any downstream model. (2) consumer-drift / decorative-mechanism: zero of Argos's 438 drafts were ever promoted — none of the 12 hand-curated catalogs in harmonia/memory/catalogs/ bear Argos authorship; the drafts stranded in artifacts/ (which lived on dead D:/Z: drives, not even present on the live repo). The one plausible spine consumer, prometheus_math/.../extract_deep_research_claims_v0_1.py, targets deep_research_batch_*/ dirs with a structured a/b/c/d frontier_survey schema — NOT the deep_research_reports/<date>/ dir where Argos's prose surveys land, and not Argos's format — so the DR output is not consumed by the claim-mining spine either. (3) dead-gated / dead-infra: the Harmonia _base.py hard-codes a Redis host (192.168.1.176, the down M3 box) and the whole swarm is rotation-driven off harmonia_loop.py. The 06-23 disposition plan explicitly tags the swarm as the source of the "math-claim monoculture flavor," all shelved, dependent on dead Redis, with niche overlap.
- **Future options (Q4) [suggestions]:**
    - retire-and-lift-asset: archive the daemon read-only; lift the lens-deficit + anti-greedy verdict-bonus SELECTION POLICY (map_of_disagreement>+3, null>+1, durable>0) and the 5-disciplinary-stance MPA scaffold spec into the shared methodology registry, since these are the genuinely reusable IP independent of the dead loop
    - adapt-to-Learner: re-point Argos's DR output to feed the spine — only valuable if a reformatter normalizes the prose surveys into the a/b/c/d frontier_survey schema that extract_deep_research_claims_v0_1.py already consumes, so lens fingerprints become mineable claims with verifier dispatch (citation_audit/catalog_lookup). Without this format bridge the output stays exhaust
    - refactor-to-on-demand-tool: strip the daemon/Redis/rotation dependency; keep a single-shot CLI that, given a problem_id, emits one MPA scaffold + DR-prompt sketch on request. Removes the monoculture-generation pressure while preserving the paste-ready scaffold value for a human attacker
    - retire-and-lift-asset: harvest the 20 committed Argos DR reports as a one-time corpus (they cost Pythia tokens), then turn the continuous generator off — keep the data, kill the loop
- **Rubric (unverified):** realized=none — 0 of 438 drafts promoted; DR surveys not consumed by claim-miner (format/dir mismatch); no downstream model output changes today - latent=low — the selection policy + MPA scaffold are reusable, but the lens-fingerprint corpus is claim-space exhaust unless reformatted into the mineable frontier_survey schema and verdict-attached - niche=redundant — overlaps Pythia (DR producer), Clio/Stygian (primary-literature surveys, same substrate_type=A discipline, the README itself flags the dedup), and the claim-mining spine; Argos adds only the lens-stack selection wrapper - health=dead-infra + dead-gated — base class hard-codes the down M3 Redis host (192.168.1.176); artifacts/state dirs were on absent D:/Z: drives; runs only under shelved harmonia_loop rotation - debt=high — meets the breadth spec (438 drafts, 8 domains traversed) but misses the needle entirely: generation volume with zero promotion and zero spine consumption is exactly the math-claim monoculture the 06-23 plan names; the propose-only design with no actual promoter wired = a generator with no sink
- **Salvage IP (lift before any retire):** (1) The anti-greedy lens-SELECTION POLICY in daemon.py _score_problem/_select_problem: lens-deficit score + verdict bonus (map_of_disagreement +3 > null/unknown +1 > coordinate_invariant/durable 0) with deterministic verdict-precedence tiebreaks — a reusable anti-reward-capture coverage heuristic. (2) The 5-disciplinary-stance multi-perspective-attack scaffold (dynamical_systems / information_theory / renormalization_group / adversarial_empirical / mathematical_physics) with forbidden-move + one-refutable-prediction commitment contract. (3) The 20 committed Pythia DR reports under aporia/docs/deep_research_reports/ (token-cost-sunk primary-literature lens surveys) as a static corpus. (4) The toolkit-line-citation renderer (_toolkit_line_for) that anchors each proposed lens to methodology_toolkit.md:line.
- **Suggested settle condition (to verify, not act):** Revive/refactor is justified iff a reformatter can turn Argos's lens-fingerprint DR output into frontier_survey-schema claims that extract_deep_research_claims_v0_1.py ingests, AND at least one such mined claim feeds the Hephaestus->Learner loop to produce a forge-tool or behavior delta that survives an ablation against the Learner-failure-only baseline. If, on a sample of the 20 committed Argos DRs, the claim-miner extracts zero verifier-dispatchable claims (citation_audit/catalog_lookup) — i.e. the hedge-prose format yields no anchored assertions — then Argos is confirmed exhaust and RETIRE stands.
- **Evidence read:** Read: harmonia/agents/argos/{README.md, CHARTER.md, daemon.py, __init__.py present; artifacts/ and state/ dirs ABSENT on F: — README points them at D:/Z:}; harmonia/agents/_base.py (hard-coded Redis 192.168.1.176, pythia_enqueue_dr, write_artifact); prometheus_math/substrate_generation/claim_mining/extract_deep_research_claims_v0_1.py (targets deep_research_batch_*/ with a/b/c/d schema, NOT Argos's deep_research_reports/<date>/ prose); pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md line 78 (Harmonia swarm incl. Argos = ARCHIVE, dead Redis, math-claim monoculture flavor); pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md line 117 (now PENDING-REVIEW pending dossier). Grep: harmonia/memory/catalogs/ has 12 hand catalogs, NONE Argos-authored; git log shows 20 committed 'Pythia DR report: Argos lens fingerprint:' reports; one sampled report (00251 one-way-functions) confirms narrative-survey hedge-prose format.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Harmonia_Loop  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M2 - tool - op=Harmonia - shelved*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Harmonia_Loop is the rotation orchestrator for the Harmonia agent swarm (Phylax/Sophia/Iris/Argos/Telos). It was built so the five self-tickable daemons could be driven one-tick-per-invocation by Claude Code's /loop feature (`/loop 4m python scripts/harmonia_loop.py`), giving the swarm continuous autonomous operation without babysitting a terminal. It is pure dispatch plumbing: weighted round-robin agent selection (per-agent stride config in _rotation_weights.json), restart-survivable rotation state (_rotation_state.json), single-instance PID lock, append-only ticks.jsonl lifetime log, crash isolation (a sticky/crashing agent logs a full stack and the rotation advances anyway so it never freezes the swarm), and a session_telemetry heartbeat. The problem it targeted was operational: how to keep an N-agent swarm cycling autonomously and observably on Windows.
- **Value thesis (Q2):** Value was meant to be indirect/infrastructural: the loop itself produces no math, no kills, no artifacts of its own — it exists so the five Harmonia children can run continuously and feed their outputs downstream. The intended consumer chain was the swarm's own artifacts (Phylax verdict envelopes, Sophia tensor-admission proposals, Iris symbol-promotion candidates, Argos lens catalogs, Telos revive specs) flowing to a 'conductor'/promotion stage and ultimately into formal substrate state. The loop was the heartbeat that kept that production line moving; the audit/cadence tooling (harmonia_audit.py, _rotation_weights.json strides) layered on top to throttle agents showing diminishing returns.
- **Why it fell short (Q3) [unverified claim]:** Three compounding failures, all documented by Harmonia's own authors. (1) consumer-drift / claim-space-exhaust: the swarm's own ROADMAP states that after ~54h and ~2200 artifacts, the number read by a downstream actor and turned into substrate state was 'approximately one.' The loop faithfully kept production running, but there was no consumer — 'technically production, practically noise on disk.' (2) dead-infra: it hard-depends on a Redis at 192.168.1.176 and Postgres/telemetry that are now dead; the disposition plan flags it as 'depend on dead Redis.' (3) bounded-menu-wall / monoculture: the May-24 swarm audit shows Sophia (140/140 pairs saturated, 100% duplicate-rate), Iris (100% scan_tick duplicate), and Telos (only F011/F014 cycled) all hitting diminishing returns within hours; the pivot review attributes the 'math-claim monoculture flavor' partly to this swarm. Currently it is also DEAD-GATED: no _swarm.pid, no _rotation_state.json, no ticks.jsonl, last event 2026-05-30, AGENT_NAMES still lists only the original 5 even though the agents/ dir grew to 13 (acheron/erebos/hecate/lethe/moros/nephele/pollux/stygian added but never wired into this loop). It is also redundant: scripts/charon_loop.py is a verbatim clone ('Pattern mirrored from scripts/harmonia_loop.py').
- **Future options (Q4) [suggestions]:**
    - RETIRE-after-HITL: turn the loop and its swarm off (already off de-facto), keep code read-only and all artifacts/logs on disk; this is the disposition plan's standing lean and matches the program's spine-shrink.
    - Lift-asset-then-retire: extract the generic orchestrator core (weighted-stride round-robin + restart-survivable rotation state + single-instance PID lock + crash-isolated dispatch + append-only tick log) into ONE shared scripts/swarm_loop.py and delete the harmonia_loop/charon_loop duplication; the reduced 4-worker set the plan describes runs on that single parametrized loop.
    - Refactor-to-PG: if any Harmonia child is revived under the narrowed-operator Harmonia, rewire the loop's telemetry/heartbeat off dead Redis onto Postgres (mirror Pronoia's dual-write) before re-enabling — but only after a real consumer exists.
    - Adapt-to-metabolization-spine: repurpose the loop as the driver that ticks the forge->Learner decider workers (Hephaestus/Learner) instead of claim-generators, so the same proven dispatch plumbing serves capability-attached residue rather than exhaust.
- **Rubric (unverified):** realized=none — produces no output of its own; the swarm it drove changed a downstream model on ~1 of ~2200 artifacts, and the loop is currently not running at all - latent=low — the orchestrator pattern is reusable, but its latent value is as a single shared loop driving the spine; as a Harmonia-specific instance its ceiling is ~zero - niche=redundant — scripts/charon_loop.py is a verbatim clone; both should collapse into one parametrized swarm_loop; the disposition plan already says fold rotation into the reduced 4-worker set - health=dead-gated + dead-infra — no pid/state/tick files, last activity 2026-05-30, hard-wired to a dead Redis host, AGENT_NAMES frozen at the original 5 while the swarm dir grew to 13 - debt=med — clean, defensive, well-commented code that meets its narrow spec but misses the needle: it optimized production cadence for a swarm that had no consumer (the exact claim-space-exhaust/consumer-drift trap); the duplication with charon_loop is live maintenance debt
- **Salvage IP (lift before any retire):** Before retiring, lift: (1) the generic orchestrator core in scripts/harmonia_loop.py — weighted-stride round-robin _pick_next_agent, restart-survivable rotation state, cross-platform single-instance PID lock (_pid_alive Windows STILL_ACTIVE check), crash-isolated dispatch (advance-on-exception), and append-only ticks.jsonl lifetime log — into one shared scripts/swarm_loop.py that also subsumes charon_loop.py. (2) The diminishing-returns audit logic in scripts/harmonia_audit.py (duplicate-rate-by-artifact-type, distinct-FID coverage, per-agent stride-recommendation) — a reusable swarm-health detector worth keeping for any future worker set. (3) The harmonia_loop_launch.bat true-Windows-detachment recipe (Start-Process -WindowStyle Hidden) — a hard-won empirical fix for Windows daemon detachment. (4) The HarmoniaAgent base-class soft-import/graceful-degradation pattern in harmonia/agents/_base.py. Keep all swarm artifacts and the events.jsonl log on disk (data never deleted).
- **Suggested settle condition (to verify, not act):** REVIVE/REFACTOR is justified only if a real consumer for the swarm's output exists or is committed: name one downstream stage (conductor/promotion or the forge->Learner decider) that will, this cycle, read a Harmonia child's artifact and turn it into a behavior delta that survives an ablation. If no such consumer is named, or if reviving merely resumes artifact production into a directory nobody reads, RETIRE-after-HITL stands. Concretely: revive the loop in --dry-run for one rotation; if zero of the produced candidates are consumed downstream within the cycle, the consumer-drift verdict is confirmed and it stays archived.
- **Evidence read:** Read: scripts/harmonia_loop.py (main source, 366 lines), scripts/harmonia_loop_launch.bat, harmonia/agents/__init__.py, harmonia/agents/_base.py, harmonia/agents/_rotation_weights.json, harmonia/agents/ROADMAP.md, harmonia/agents/_logs/swarm_audit_20260524_084221.md, harmonia/agents/_logs/weekly_20260523_022100.md, pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (rows 9, 20-26, 78-79), pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (lines 100-138). Confirmed dead-gated: no _swarm.pid, _rotation_state.json, or ticks.jsonl present; last events.jsonl entry 2026-05-30; agents/ dir grew to 13 subdirs but AGENT_NAMES still lists only 5. Confirmed redundant via head of scripts/charon_loop.py ('Pattern mirrored from scripts/harmonia_loop.py'). No prior verdict found under roles/; pivot/ verdicts located and read.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Iris  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M2 - tool - op=Harmonia - shelved*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Iris was built to operationalize a line in Harmonia's restore protocol: "compress what's read identically by every Harmonia." Every Harmonia cold-start re-reads the same prose (block-shuffle null replay, pattern-30, anchor-refresh narratives); prose costs tokens, a symbol costs a lookup. Iris is a daemon (one of five Harmonia child agents under harmonia_loop.py) that scans the substrate corpus, fingerprints action-headings and short procedural paragraphs by a normalized sorted-token bag, clusters paraphrastic variants, and PROPOSES (never promotes) a versioned symbol when a fingerprint recurs across >=3 distinct files. Intent: turn repeated prose into mechanism-symbols to cut future cold-start cost.
- **Value thesis (Q2):** Value was framed as compounding leverage: every symbol successfully promoted permanently cuts every future Harmonia's cold-start token cost, so even one solid promotion per week compounds over months. Consumer was the conductor (human or authorized session) who would curate candidate_*.md artifacts into harmonia/memory/symbols/. A Phase-1 auto-promotion pipeline (_pipeline.py, per AUTOPROMOTION_TARGETS.md v0.2) was added to push survivors into CANDIDATES.md with provenance + a kill-path + taint-scope, designated the swarm's lowest-blast-radius proof-of-concept for the whole auto-promotion program.
- **Why it fell short (Q3) [unverified claim]:** Empirically near-null. Over ~54 hrs and 1805 clusters tracked, Iris filed exactly ONE threshold-crossing candidate (FRONTIER_MODEL_REVIEW_ROUND@v1), which its own README calls almost certainly a false positive (a series-heading, not a substrate concept). README's honest finding: Prometheus prose is already more compressed than expected (hand-edited over many sessions), so the easy compression is done; external corpora produced paraphrastic variants that form their OWN clusters rather than collapsing. Three ruts converge: (1) consumer-drift/dead-gated — ROADMAP self-audit says of ~2200 swarm artifacts roughly ONE was ever consumed downstream and zero Iris candidates were promoted; the conductor loop that was the sole consumer went idle. (2) dead-infra — shelved swarm depends on a hardcoded Redis host (192.168.1.176) via _base.py; artifacts/ is now empty and clusters.json/scan_cursor.json are gone (only calibration + template-span seed files remain in state/). (3) decorative-mechanism — the binding constraint (sorted-token-bag fingerprint) is too sensitive to capture real synonymy, so the threshold rarely fires meaningfully; the auto-promotion pipeline, kill-path, taint-scope and bandit are well-engineered machinery feeding a slot (CANDIDATES.md) the spec itself labels not-load-bearing. Crucially it is NOT capability-attached: a promoted symbol does not change any downstream model's behavior, so under the program's decider (metabolization loop) its output is exhaust, not residue.
- **Future options (Q4) [suggestions]:**
    - retire-and-lift-asset: RETIRE-after-HITL the daemon (read-only, data kept) but lift the two reusable assets first - the known_template_spans.json template-noise dictionary and the paraphrase-entropy / cross-corpus scoring primitive in _pipeline.py - into the shared Harmonia scorer registry
    - adapt-to-arXiv/OEIS corpus: repoint SOURCE_DIRS at orders-of-magnitude-larger raw-prose corpora (arXiv abstracts via Pythia DR landings, OEIS comments, Mathlib4 docstrings) where genuine repeated-pattern compression actually exists, per README's own roadmap - the only path with a real signal hypothesis
    - refactor-to-capability-attached: only revive if its output is wired so a promoted symbol measurably changes a Learner/Hephaestus cold-start (token cost or behavior delta under ablation), turning candidates from exhaust into residue the metabolization loop can consume
    - revive-as-is: restart under harmonia_loop with live Redis - rejected as lowest-value; reproduces the ~1-candidate-in-54-hrs result
- **Rubric (unverified):** realized=none - exactly one candidate ever crossed threshold (a self-acknowledged false positive); zero Iris candidates promoted to symbols; changes no downstream model today - latent=low - real signal likely exists only after repointing to a 10-100x larger external prose corpus (arXiv/OEIS); on the Harmonia-internal corpus the compression problem is essentially solved - niche=partially redundant - overlaps the Harmonia swarm's compression/curation role and shares the _scorer/auto-promotion scaffolding with Sophia/Argos/Telos; unique only as the one swarm member targeting prose->symbol density - health=dead-gated + dead-infra - clean code but shelved, runtime artifacts/state wiped, depends on hardcoded Redis host (192.168.1.176, graceful-degraded); consumer (conductor curation loop) went idle - debt=med - well-structured but carries hardcoded D:\Prometheus drive-letter paths throughout (violates feedback_paths doctrine), a fingerprint algorithm known to be the binding constraint, and decorative auto-promotion machinery feeding a non-load-bearing slot
- **Salvage IP (lift before any retire):** (1) known_template_spans.json - a curated dictionary of 24 template-noise token-span stems (open_problems_future_directions, frontier_model_review_round, executive_summary, etc.) that is directly reusable as a template/boilerplate filter for ANY corpus-scanning agent. (2) The _pipeline.py scoring primitives: paraphrase-entropy (avg pairwise Jaccard distance over example token sets) and the cross-corpus internal/external classifier, both transparent and corpus-agnostic. (3) The Tier-0/Tier-1 + kill-path + taint-scope auto-promotion contract pattern, generalizable across the swarm. (4) calibration_positive.json / calibration_negative.json labeled candidate sets for scorer calibration. Lift all four into the shared Harmonia scorer registry before archiving.
- **Suggested settle condition (to verify, not act):** Repoint Iris at a large external raw-prose corpus (arXiv abstracts or OEIS comments, 10-100x current size) for a bounded window and require: >=10 of 50 auto-promoted candidates judged worth-keeping AND >=1 candidate clears to a real promoted symbol whose adoption produces a measurable cold-start token/behavior delta in a consumer agent under ablation. If it cannot clear even one capability-attached promotion on a corpus where repeated patterns demonstrably exist, the prose->symbol-compressor thesis is falsified for this design and RETIRE stands.
- **Evidence read:** Read harmonia/agents/iris/{README.md, CHARTER.md, daemon.py, _pipeline.py, __init__.py}; harmonia/agents/iris/state/{calibration_positive.json, known_template_spans.json}; harmonia/agents/ROADMAP.md; harmonia/agents/AUTOPROMOTION_TARGETS.md; harmonia/agents/_base.py (Redis dependency lines 44-110); pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (line 78, swarm shelved/dead-Redis); pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (lines 100-138). Confirmed artifacts/ is EMPTY (0 files) and clusters.json/scan_cursor.json absent from state/ - the 438 artifacts and 1805 clusters cited in README are no longer on disk. CANDIDATES.md exists but predates Iris (Apr 29) with no confirmed Iris auto-promotion entry. Component fully located and read.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Phylax  -  *AI suggestion (advisory, NOT approved): PENDING-REVIEW*
*M2 - tool - op=Harmonia - shelved*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Built ~2026-04/05 (Harmonia swarm, M2 child) as the defensive layer after the F043 retraction — a finding promoted on plausible-looking anti-correlation reasoning that turned out to be a Pattern-30 Level-4 algebraic identity. The original intent: catch the NEXT F043 *before* it lands as a promotion, not after. It watches every place new claims enter the substrate (Sigma-kernel PROMOTE events on the Agora/Redis sync stream, git commits touching promoted symbols, and Pythia Deep Research reports) and runs each candidate through two checks pre-promotion: (1) retraction-adjacency (token Jaccard against retraction_registry.md, threshold 0.25) and (2) a Pattern-30 graded-severity sketch (CLEAN -> WEAK_ALGEBRAIC -> SHARED_VARIABLE -> REARRANGEMENT -> IDENTITY, graded by an LLM). When a flag/block + adjacency hit fires, it enqueues a doctrine-compliant Pythia DR to verify against post-2024 primary literature.
- **Value thesis (Q2):** Value was meant to be measured in retractions that did NOT happen because Phylax surfaced the adjacency in time. Consumer = the conductor / downstream promoter (a reviewer reads the newest verdict_*.md before promoting anything substrate-grade) plus a future scorer (each FLAG/BLOCK surviving human review = positive training example; PASS = negative). It produces verdict envelopes (pass / flag-with-shadow-count / block-with-mechanism), never modifies the substrate itself. Anti-reward-capture safeguards: thresholds are visible source constants (not learned), and BLOCK requires a cited mechanism (Pattern-30 level or named retraction entry) — no vibes-blocks.
- **Why it fell short (Q3) [unverified claim]:** Two layers. (1) consumer-drift / dead-gated: the swarm is shelved. events.jsonl is cold since 2026-05-30 with ZERO phylax events recorded; no artifacts/state dirs exist in-repo; the README's '456 envelopes / 1 real flag in 54h' lived on the now-absent D:\ machine and pointed to dead Redis (192.168.1.176). The disposition plan flags the whole Harmonia swarm as 'shelved; depend on dead Redis; niche overlap.' (2) the deeper rut, claim-space-exhaust: it gates and DR-verifies MATH CLAIMS and PROMOTIONS — pure claim-space machinery. Per program doctrine, claim-space generation/verification is exhaust, not capability-attached residue, and is orthogonal to the metabolization-loop spine (forge Hephaestus -> Learner consumes kill-geometry). The README itself notes Phylax 'produced the math-claim monoculture flavor.' Its realized output against the spine is decorative: ~1 genuine flag (a synthetic smoke-test F043-adjacent claim) over the entire documented run; everything else was re-audit PASS in fallback mode because the promotion stream was quiet. The Pattern-30 grade is also UNDETERMINED whenever the LLM grader is absent, so the load-bearing severity signal is frequently null.
- **Future options (Q4) [suggestions]:**
    - retire-and-lift-asset: RETIRE-after-HITL the daemon; lift retraction_registry.md + pattern_library.md + the Pattern-30 0-4 graded-coupling taxonomy + F043 anchor into the shared registry as a reusable algebraic-coupling lint that any promotion path can call on demand (the IP is the taxonomy and corpus, not the rotation daemon)
    - refactor-to-spine-gate: re-aim Phylax from gating MATH CLAIMS to gating LEARNER-CORPUS entries — run the retraction-adjacency + Pattern-30 lint on forge/Learner training rows so kill-geometry that is actually an algebraic tautology cannot leak into training (attaches it to the metabolization spine instead of claim-space)
    - adapt-to-seam-fidelity: fold the field-level adjacency/leak-check discipline into the existing Ergon seam allowlist work (recent commits show active seam leak-gate effort) — Phylax's Jaccard-against-known-kills is a sibling of the seam fidelity audit
    - revive-as-is: ONLY if a live promotion stream + working scorer/consumer is restored (re-host Redis, replace the regex/keyword Pattern-30 sketch with the roadmap's learned classifier); otherwise this resumes producing PASS exhaust
- **Rubric (unverified):** realized=none — cold since 2026-05-30, zero recorded ticks in repo, no live consumer; its 1 real flag was a self-authored smoke test; changes no downstream model today - latent=med — the Pattern-30 graded-coupling taxonomy + retraction registry are genuinely reusable, and a refactor to gate the Learner corpus (not claims) could attach it to the spine - niche=partly unique — the F043/Pattern-30 algebraic-identity-coupling lint is a distinct discipline; but as a promotion-stream sentinel it overlaps Charon's kill-everything mandate and the Ergon seam leak-gate; as a DR-firer it overlaps Pythia/Aporia queue - health=dead-gated/runnable-but-cold — code is clean, registered, and wired into harmonia_loop, and in-repo deps exist, but it depends on dead Redis + an LLM grader and the whole swarm is shelved (events log cold ~25 days) - debt=med — token-Jaccard adjacency is a coarse meets-spec-misses-needle proxy for real algebraic coupling; Pattern-30 grade is UNDETERMINED without the LLM (load-bearing signal frequently null); claim-space gating is itself a monoculture-feeding shortcut
- **Salvage IP (lift before any retire):** harmonia/memory/retraction_registry.md (10 entries + 4 cross-cutting failure patterns, append-only, with validator at harmonia/memory/diagnostics/validate_retraction_registry.py); harmonia/memory/pattern_library.md (63KB); the Pattern-30 0-4 graded algebraic-identity-coupling taxonomy + F043 anchor case (harmonia/memory/symbols/PATTERN_30.md); the doctrine-compliant DR-prompt builder (_build_dr_prompt, Aporia dr_prompt_discipline.md section 3 baked in: requester-named, substrate-type-A, arXiv+DOI criterion, landing-path, recency key); the cap-aware/recency-keyed DR enqueue gate logic in _maybe_enqueue_dr.
- **Suggested settle condition (to verify, not act):** Run Phylax's retraction-adjacency + Pattern-30 lint over the Learner's actual training corpus (forge kill-geometry rows). If it catches >=1 algebraic-tautology/identity row that would otherwise have been trained on (verified by HITL), AND removing those rows changes the Learner's behavior on a held-out ablation, then the REFACTOR-to-corpus-gate has spine value -> REFACTOR. If it only ever re-confirms PASS on a dead/quiet promotion stream with no Learner-corpus catches, then RETIRE-after-HITL the daemon and lift the taxonomy + registry into the shared registry.
- **Evidence read:** Read in full: harmonia/agents/phylax/{daemon.py (680 lines), README.md, CHARTER.md, __init__.py (empty)}. Read harmonia/agents/__init__.py (AGENT_NAMES includes 'phylax'), harmonia/agents/_base.py (deepseek_complete = free-tier LLM cascade, pythia_enqueue_dr gated on Postgres, tail_stream gated on Redis @192.168.1.176). Confirmed wiring: scripts/harmonia_loop.py rotation includes Phylax. Confirmed deps exist in-repo: harmonia/memory/retraction_registry.md (F043/Pattern-30 anchor at line 72), pattern_library.md. Run history: harmonia/agents/_logs/events.jsonl cold since 2026-05-30 with 0 phylax matches; no artifacts/ or state/ dir exists in repo (README's D:\ artifacts unavailable). Prior verdicts: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (rows 20-25: Harmonia swarm shelved, dead Redis, monoculture flavor) and pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (Phylax queued -> PENDING-REVIEW, no auto-delete). One incidental mention in roles/Ergon/CORPUS_VALUE_AUDIT_2026-06-03.md.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Sophia  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M2 - tool - op=Harmonia - shelved*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Sophia is a Harmonia-swarm child agent (M2), the closed-loop incarnation of "gen_11 / axis-space invention." It targets the Prometheus north-star directive of "compressing coordinate systems of legibility, not laws" — the unit of discovery being "invariant under a specific projection" (Pattern 6), which requires having more projections (measurement axes) available than humans have named. Sophia's job is to manufacture those projections continuously: one tick = one untried (operator x specimen) pair lifted from the Cartesian product of the methodology toolkit (KOLMOGOROV_HAT, CRITICAL_EXPONENT, CHANNEL_CAPACITY, MDL_SCORER, RG_FLOW, FREE_ENERGY, GINI_COEFFICIENT, CONTROLLABILITY_RANK, TT_APPROX_MAP, CONJECTURE_GENERATOR) x the live frontier specimens + calibration anchors (F001-F009 + stalled live specimens F011/F013/F014/F041a/F042/F044/F045). It composes a propose-only projection-proposal artifact with a 5-gate tensor-admission stub (left UNFILLED for a downstream executor) and a mandatory calibration-anchor sanity gate as an explicit anti-reward-capture brake (the F043 lesson codified: a tool that finds a novel axis but fails its own instrument's basic check is artifact, not discovery).
- **Value thesis (Q2):** Value was meant to be measured in (operator x specimen) pairs that, when actually executed downstream by Techne or Charon, become calibrated and non-tautological scoring functions / new measurement axes for the substrate tensor. The intended consumer chain was explicit: Sophia emits a `SOPHIA_PROPOSAL` Agora task spec on `agora:harmonia_sync`, and a later "Theseus/Charon scorer-execution wave" picks the top-ranked proposal, fills the 5-gate stub, and materializes the scorer. Secondary intended consumers: a methodology architect browsing newest proposals to pick runnable ones, and the cross-product traversal serving as curriculum (surfacing which operators cannot apply to which specimen tiers).
- **Why it fell short (Q3) [unverified claim]:** Textbook claim-space-exhaust compounded with consumer-drift (dead-gated by a counterparty that was never built). (1) Claim-space exhaust: the toolkit shelf was only 10 operators x ~14 specimens = a 140-pair grid, which saturated within ~17 hours of continuous ticking (exhausted 2026-05-20 05:46 UTC). After saturation it fell to `meta_expand_toolkit` fallback and the 2026-05-24 swarm audit flagged a 100% duplicate-rate (13/13 artifacts in window identical meta-tasks); the saturation-no-op hash gate in daemon.py was a retrofit to stop the duplicate stream rather than add value. (2) No consumer: `SOPHIA_PROPOSAL` appears ONLY in Sophia's own emitter — grep across the repo finds zero Theseus/Charon code that consumes it. The README itself admits "Consumer-side: nobody runs Sophia's proposals... The 5-gate tensor-admission stub is a contract waiting for a counterparty." Proposals were paste-ready specs that sat in artifacts/ as exhaust. (3) Monoculture flavor: the disposition plan names the Harmonia swarm (incl. Sophia) as having "produced the math-claim monoculture flavor" — exactly the claim-space generation the program now treats as exhaust rather than capability-attached residue. The 438 claimed artifacts did not even survive the D:->F: migration (artifacts/ and state/ dirs are now empty), so there is no durable yield. It did NOT fail technically — it runs, degrades gracefully, and the anti-reward-capture discipline is sound — it fell short on the decider: nothing it emits changes a downstream model.
- **Future options (Q4) [suggestions]:**
    - retire-after-HITL: pause loop, code read-only, keep CHARTER/README; the proposal-stream output is exhaust by the metabolization-loop standard (no model behavior delta, no consumer) and is redundant with the spine's demand-aimed forging (Pheme->Hephaestus->Learner).
    - salvage-and-lift: extract the calibration-anchor sanity-gate / anti-reward-capture pattern (F043 lesson, ANCHOR_BLURBS dict + 'anchor must agree with known instrument-health reading or proposal halts') and the 5-gate tensor-admission stub (null-calibrated / representation-stable / not-marginals / non-tautological / domain-agnostic with how-to-fill recipes) into Techne's tool-forging checklist or the math-tdd skill, so the discipline survives even if the generator does not.
    - refactor-to-demand-driven: invert the loop so it does NOT enumerate a Cartesian grid (exhaust) but instead consumes Pheme's Learner-deficit profile and only proposes the (operator x specimen) axis the forge actually needs next — turning claim-space generation into capability-attached residue. Only worth it AFTER the spine forge->Learner loop exists and can consume a new axis.
    - adapt-as-on-demand-tool: drop the continuous daemon; keep _build_proposal as an on-demand 'axis-proposal' helper Techne/Charon can invoke when they genuinely want a new scorer, fed by a live (not stale D:-path) toolkit + specimen file.
- **Rubric (unverified):** realized=none — output is propose-only artifacts with no consumer; no scorer was ever materialized and no model behavior changed; artifacts/state dirs now empty after migration - latent=low — ceiling only rises if refactored to demand-driven (consume Pheme deficit) AND the spine forge->Learner loop exists to consume a new axis; absent that, more pairs = more exhaust - niche=redundant — overlaps the spine's demand-aimed axis-creation (Pheme routing + Techne toolsmith); also shares the math-claim monoculture flavor flagged across the whole Harmonia swarm - health=dead-gated — importable and runs (graceful Redis/DeepSeek degrade), but the SOPHIA_PROPOSAL executor wave was never built, so the loop is functionally decorative; grid saturated, emitting duplicate meta-tasks - debt=med — meets its MVP spec exactly (one proposal/tick, anchor in every proposal) but misses the needle: hardcoded D:\ paths in artifact prose violate the no-drive-letter doctrine, and the propose-only contract is unfulfillable without a counterparty
- **Salvage IP (lift before any retire):** 1) The calibration-anchor anti-reward-capture pattern: CALIBRATION_ANCHORS list + ANCHOR_BLURBS dict (F001-F009 instrument-health readings) + the 'anchor must agree or proposal halts' rule (daemon.py L60-69, L253-265) — lift into Techne/math-tdd tool-forging checks. 2) The 5-gate tensor-admission stub with concrete how-to-fill recipes (null-calibrated, representation-stable Kendall-tau>=0.9, not-marginals, non-tautological algebraic_dependence_level<=1, domain-agnostic OOD replay) (daemon.py L266-291) — a reusable scorer-admission contract. 3) The saturation-no-op content-hash gate (_meta_task_content_hash, timestamp-stripped SHA-256, L38-50) — a generic anti-duplicate-emission guard for any enumerating daemon. 4) The methodology_toolkit operator catalog (the 10 cross-disciplinary lenses) as a curated dictionary of measurement axes.
- **Suggested settle condition (to verify, not act):** Within a bounded window, a downstream agent (Techne or Charon) consumes one SOPHIA_PROPOSAL end-to-end: fills the 5-gate stub, materializes a runnable scorer, and that scorer's output measurably aims/changes a Learner training run that survives an ablation. If no such proposal-to-model-delta chain occurs (as has been true to date), Sophia is exhaust and retires. Conversely, if even one Sophia-proposed axis demonstrably changes a downstream model, revive (preferably in demand-driven refactor form).
- **Evidence read:** Read: harmonia/agents/sophia/daemon.py (full, 653 lines), harmonia/agents/sophia/README.md, harmonia/agents/sophia/CHARTER.md, harmonia/agents/sophia/__init__.py. Confirmed artifacts/ and state/ dirs absent/empty (ls). Read pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (Sophia in ARCHIVE/PENDING-REVIEW table row 20-25, 'math-claim monoculture flavor', depends on dead Redis) and pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (Sophia listed borderline-ARCHIVE now PENDING-REVIEW). Read harmonia/agents/_logs/swarm_audit_20260524_084221.md (Sophia: 0 untried of 140, grid saturated, 13/13 duplicate meta_expand_toolkit, DIMINISHING RETURNS flag, recommended stride-6 demotion). Grep for SOPHIA_PROPOSAL across repo: appears ONLY in sophia/daemon.py emitter — no Theseus/Charon consumer exists. Grep harmonia/agents/_base.py confirms graceful Redis/DeepSeek degradation. Note: artifact prose hardcodes D:\\Prometheus paths (violates feedback_paths doctrine).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Telos  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M2 - tool - op=Harmonia - shelved*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Telos is a Harmonia child agent (M2, operator Harmonia) built ~2026-05-19 as the "stalled-specimen reviver / negative-space patroller." It targets a specific failure mode of a human-bottlenecked research ledger: live findings (F-IDs) at live_specimen tier whose last_audit_outcome date silently slides into the past, so "nobody is asking new questions" gets misread as "the specimen is settled." Each rotation tick it parses harmonia/memory/frontier_specimen_state.md, computes per-F-ID stall age, diffs the F-ID's cross_refs symbols against the current promoted-symbol + methodology-toolkit pool to enumerate lenses-not-yet-applied, and files a revive-task artifact proposing the top-3 next audit actions. Fallbacks: patrol killed F-IDs ("would current tooling un-kill F010/F012?") then emit a NEGATIVE_SPACE_MAPPED@v1 candidate. Hard rule: never a silent tick (silence = reward-capture by validating-the-specimen-dead via not-asking). Proposal-only; never mutates the ledger.
- **Value thesis (Q2):** Counter-bias attention agent: ask the audit questions the conductor (James) stopped having time to ask, so stalled-but-not-dead findings get re-probed under newly-promoted tooling. Intended consumer was an auditor/conductor (paste-ready Agora task specs in each revive_*.md) and, transitively, whatever agent runs the proposed audit (Techne/Charon) which would post the result back so Telos could reset that F-ID's cycle. The state/proposed_history.json was also pitched as a running "negative-space map" of which lenses are gaps per F-ID, usable by a coverage cartographer.
- **Why it fell short (Q3) [unverified claim]:** Three compounding ruts. (1) CONSUMER-DRIFT / open loop: by its own README roadmap, "nobody actually runs Telos's proposed audits" — grep confirms revive_*/killed_revisit_* artifacts are referenced ONLY by Telos itself and the audit script; no Techne/Charon/Learner/forge ever consumes them. The result-postback that would close the loop and reset cycles was never built. (2) BOUNDED-MENU-WALL + manufactured-activity: the 24h swarm audit (2026-05-24) flagged Telos for diminishing returns — only 2 distinct F-IDs (F011, F014) cycled, narrow coverage. The README's headline "440 artifacts / 14 pool-exhausts" is the tell: the pool-exhaust→reset→restart mechanic is explicitly designed to "keep generating distinct artifacts indefinitely even on a small F-ID set." That is decorative-mechanism / claim-space-exhaust — proposing the same lens-to-F-ID pairings on a loop produces exhaust, not capability-attached residue. (3) DEAD-INFRA: shelved with the whole Harmonia swarm; depends on Agora Redis (_base.py hardcodes AGORA_REDIS_HOST 192.168.1.176) and substrate_health(), and the artifacts/ + state/ dirs are gitignored (artifacts dir is empty in-repo — none of the claimed 440 artifacts are durable/tracked). Disposition plan (06-23) bucketed it: "all shelved; depend on dead Redis; niche overlap; produced the math-claim monoculture flavor."
- **Future options (Q4) [suggestions]:**
    - retire-and-lift-asset: RETIRE-after-HITL the daemon; lift the lens-gap-diff algorithm (cross_refs-symbols vs promoted-symbol-pool delta) and the stall-age parser into a one-shot conductor script that runs on demand, not on a never-silent loop
    - adapt-to-Learner-spine: rewire Telos's 'lenses-not-yet-applied' delta to emit forge targets for Hephaestus (which lens-gap, on which capability-attached F-ID, would change a downstream model) instead of conductor-facing audit memos — turns exhaust into residue, but only worth it if the diff localizes a capability boundary, not a claim
    - refactor-to-closed-loop: build the missing result-postback consumer (Techne/Charon posts audit verdict back, Telos resets that F-ID's cycle and updates last_audit_outcome) AND replace the pool-exhaust-reset mechanic with a hard stop when no genuinely-new lens exists — kills the manufactured-activity rut
    - revive-as-is: NOT recommended; would re-instate an open-loop never-silent artifact mill on dead Redis
- **Rubric (unverified):** realized=none — no downstream model or agent consumes its output today; open loop confirmed by grep; output dir gitignored/empty in repo - latent=low-to-med — the lens-gap-diff + stall-detector could feed the forge IF rewired to capability-attached residue and given a real consumer; ceiling is capped by claim-space-exhaust nature of F-ID-level proposals - niche=redundant — overlaps Argos (also a Harmonia lens-applier with DR enqueue) and the broader claim-space generators (Nous/Theseus); its unique sliver is the stall-age + killed-revisit framing, which is small - health=dead-gated + dead-infra — shelved with Harmonia swarm; needs Agora Redis (hardcoded 192.168.1.176) + substrate_health(); importable and ran (2204 lifetime ticks) but never-silent design and gitignored artifacts mean no durable product - debt=high — the pool-exhaust→reset→restart 'never silent' mechanic is a decorative-mechanism shortcut that manufactures activity to satisfy the no-silence rule; meets-spec (one artifact per tick) while missing the needle (a consumed audit that changes a model)
- **Salvage IP (lift before any retire):** Lift before any retire: (1) the lens-gap-diff algorithm — set-difference of an F-ID's cross_refs symbols against the live promoted-symbol + methodology-toolkit pool (daemon.py _handle_revive / candidate_lenses logic) — reusable as a generic 'unapplied-tooling' coverage primitive; (2) the stall-age detector + ledger parser (_parse_frontier_ledger, _latest_date) that turns frontier_specimen_state.md into structured rows; (3) the weighted-round-robin anti-greedy F-ID picker (_weighted_rr_pick + telos_top_n_config.json, top_n=5 tuned to cover F013/F041A/F045) as a reusable fair-rotation scheduler for any other attention agent; (4) the NEGATIVE_SPACE_MAPPED@v1 artifact schema as a template for 'coverage exhausted -> propose a missing substrate primitive' reporting. Keep frontier_specimen_state.md and retraction_registry.md — those are shared substrate, not Telos's.
- **Suggested settle condition (to verify, not act):** Wire one revive_*.md proposal end-to-end: have a real downstream agent (Techne/Charon) execute Telos's top-1 proposed audit on a stalled F-ID and post the verdict back. If that audit un-stalls or re-kills the specimen AND the proposed lens was genuinely decisive (i.e., a Telos proposal beats a random-unapplied-lens baseline at producing a verdict change) -> the diff has navigable value -> REFACTOR to closed-loop. If the proposed lenses are no better than random / the loop just re-emits the same F011/F014 pairings -> confirmed exhaust -> RETIRE-archive the daemon (read-only, state kept) after lifting the salvage IP below.
- **Evidence read:** Read: harmonia/agents/telos/{CHARTER.md, README.md, daemon.py (1002 lines, full), __init__.py, state/telos_top_n_config.json}. harmonia/agents/.gitignore (confirms artifacts/ + state/ gitignored; artifacts/ dir empty/absent in-repo despite README's '440 artifacts' claim). harmonia/agents/_logs/swarm_audit_20260524_084221.md (Telos diminishing-returns flag: only F011/F014 cycled). harmonia/agents/_base.py (hardcoded Agora Redis 192.168.1.176 — dead-infra basis). Prior verdicts: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (rows 20-25: Harmonia swarm incl. Telos = shelved, dead Redis, niche overlap, math-claim monoculture) and pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (Telos in 'was borderline ARCHIVE now PENDING-REVIEW' queue). Grep for revive_/killed_revisit consumers: only Telos's own files + harmonia_audit.py — no downstream consumer, confirming the open-loop / consumer-drift diagnosis the README itself admits.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________


## Daemons

### Apollo  -  *AI suggestion (advisory, NOT approved): REFACTOR*
*M2 - daemon - op=- - shelved*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Apollo is Prometheus's evolutionary search engine — the non-LLM variance source intended to grow reasoning capability without relying on frontier models. Original v1/v2 intent: an evolutionary-computation engine for reasoning-tool synthesis (NSGA-II/III multi-objective selection over 6 fitness dims, MAP-Elites QD archive, FAISS retrieval, racing early-kill, primitive-routing DAGs as the gene, batched LLM only for offspring mutation, ablation gate against bypass). It targeted the bounded-menu/gen-30 wall directly: a mechanism to GROW the menu of reasoning operations via population search rather than hand-authoring, so the substrate's capability ceiling rises by evolution instead of optimization. After the gen-90 flat-landscape diagnosis it pivoted (Branch C, 2026-05-24+) to a typed-blackboard composition substrate: compositions of typed state-to-state transformers (reads/writes named slots on a shared BlackboardState), with a data-flow load-bearing ablation as the honesty gate (corrupt a slot before its downstream read; accuracy must drop or the op is decorative).
- **Value thesis (Q2):** Value-add was to be the engine that mints NEW reasoning atoms/organisms for the substrate — the supply side of the metabolization loop. Intended consumers: (1) Atalanta (Aporia tool) which reads Apollo organisms; (2) the broader Learner/spine, with evolved load-bearing compositions as candidate reasoning tools; (3) the Hephaestus forge as a coordination partner (Apollo composes R-atoms the forge supplies). The deeper thesis: a genetic, LLM-independent variance source is the program's hedge against the bounded-menu wall — crossover/recombination as a menu-growth mechanism that compounds where single-agent self-improvement stalls.
- **Why it fell short (Q3) [unverified claim]:** Three compounding ruts. (1) EXPRESSIVENESS-CEILING / decorative-mechanism: the v2c gen-2960 baseline matrix and the Branch C analysis showed evolved compositions had ~0 lift over the best single primitive — tier-monocultured at R0-R1 (parse/extract/reduce) with NO R2+ reasoning operation to compose, so sequencing extractors just moves data around and never manufactures higher-tier capability. Apollo FP-003 'expressiveness_ceiling' was confirmed by the executing lens 2026-06-15 (and Harmonia's void-miner independently hit the same FP — program-level signal). (2) SEARCH-OPERATOR INSUFFICIENCY: the literal anchor is 0/8000 single-step improving walks vs 6.1% per pair-crossover; A/B found the de-novo solver 4/5 seeds with crossover vs 0/5 without. Yet recombine() in the Branch C engine is coded-and-validated but UNWIRED — crossover_frac defaults to 0.0 (the v2 genome engine has crossover_rate 0.3, but the current composition substrate does not run it). The one move that crosses fitness valleys is switched off. (3) CONSUMER-DRIFT / dead-gated-in-loop: the v2 evolution run stalled at best_acc=0.33, n_lb=1 (a single load-bearing composition) over 34h/165 gens before a window-close abort; the run log shows numerical instability (overflow in exp, NaN in novelty/selection). The R8 organism collapsed. Apollo currently produces eval feedstock / 'Atalanta ore', not anything the spine consumes — it is not in the metabolization loop. Crucially: Branch C's own one-experiment falsification (decompose r2_chain_tracker into a load-bearing R2 forward_chain transformer and prove it lifts) was proposed in the 2026-05-29 forge handoff but there is no evidence it was ever run.
- **Future options (Q4) [suggestions]:**
    - Refactor-to-wired-crossover: flip crossover_frac 0.0->0.3 in the Branch C blackboard engine (recombine() already exists/validated) and re-run; cheapest possible test of the search-operator-insufficiency hypothesis. The reassessment explicitly flags this one-flag change.
    - Adapt-to-tier-ladder: execute the deferred one-experiment falsification from the 05-29 handoff — decompose ONE forge specialist (r2_chain_tracker -> parse_rules / forward_chain / score_by_derivability), wire the R2 transformer into the registry, run the composition gauntlet on a constraint-tracking canary. If a single R2 atom yields a provably load-bearing composition the R0-R1 set can't, the expressiveness ceiling is breakable and Apollo earns revival; if not, the bottleneck isn't primitive tier.
    - Refactor-to-candidate-organism-harness: re-cast Apollo not as 'the discovery engine' but as the M1 candidate organism that answers the reassessment's three cycle questions (search efficiency, claim robustness, generalization), feeding the spine's metabolization loop with measured deltas rather than raw organisms.
    - Retire-after-HITL and lift assets: park the engine read-only; salvage the typed-blackboard substrate + data-flow load-bearing ablation, the composition gauntlet + null battery, and the forge<->Apollo typed-transformer contract for whatever component owns reasoning-tool synthesis next.
- **Rubric (unverified):** realized=none — best evolved composition shows ~0 lift over best single primitive; n_lb=1; no output reaches a downstream spine model today; R8 organism collapsed; output is eval feedstock / Atalanta ore only - latent=med — the wired-crossover flip and the deferred R2-transformer falsification are both cheap, falsifiable, and could break the expressiveness ceiling; a genetic non-LLM variance source is a genuine menu-growth hedge if it works - niche=partially unique — the ONLY genetic / non-LLM variance source in the roster (vs cluster-A ladder-climbers Forge/Icarus and cluster-B falsifier-claim agents Ergon/Theseus/Harmonia); but redundant with the forge as a reasoning-atom supplier and tier-monocultured at R0-R1 like them - health=dead-gated — code is local-complete (HF models on disk, imports fine, gauntlet+null battery pass), but the live run aborted on a window-close, carries numerical-instability warnings, recombine() is unwired (crossover_frac=0.0), and the one decisive Branch C experiment was never run - debt=med — meets-spec engineering (NSGA, MAP-Elites, racing, ablation all built) but misses-the-needle: tier-monocultured atom set, crossover switched off, multiple stale generations (v1/v2-beta/b/c/d) accreted under archive/, and decorative-composition was the expected outcome of a substrate with no high-tier atoms
- **Salvage IP (lift before any retire):** Typed-blackboard composition substrate (apollo/src/blackboard.py BlackboardState + blackboard_op, blackboard_ops_v2.py) with its slot-vocabulary contract ('adding a slot is intentional, not emergent'); the data-flow load-bearing ablation / accuracy-delta gate (apollo/src/ablation.py, dataflow_fitness.py) — the anti-decorative honesty test that is reusable program-wide; the composition gauntlet + complete null battery incl. random-compatible-op null (apollo/scripts/composition_gauntlet.py, null_slot_ablation.py); the validated-but-unwired recombine() one-point splice (apollo/src/mutation.py crossover, blackboard_evolve.py); the forge<->Apollo typed-transformer contract + r2_chain_tracker worked decomposition (apollo/pivot/apollo_forge_handoff_2026-05-29.md); MAP-Elites archive keyed on load-bearing-core (refuses fake diversity); clean canary + ledger_prompts.json eval corpus.
- **Suggested settle condition (to verify, not act):** Run the two cheap experiments back-to-back: (a) flip crossover_frac 0.0->0.3 in the Branch C engine and re-run the gauntlet; (b) decompose r2_chain_tracker into a load-bearing R2 forward_chain transformer, wire it in, run the composition gauntlet on a constraint-tracking canary. SETTLE: if EITHER produces a composition that earns load-bearing status (data-flow ablation positive) AND shows accuracy lift over the best single primitive that survives the null battery -> REVIVE (the ceiling is breakable, Apollo is the menu-growth engine). If BOTH fail -> the bottleneck is neither search operator nor primitive tier; Apollo is redundant with the forge as an atom supplier and routes to RETIRE-after-HITL with assets lifted.
- **Evidence read:** Read: apollo/RESUME.md, README.md, ARCHITECTURE.md (gen-90 flat-landscape diagnosis), apollo/pivot/apollo_forge_handoff_2026-05-29.md (tier-monoculture R0-R1, decorative composition, the deferred falsification), tail of apollo/apollo_v2_run.log (gen 165, best_acc=0.33, n_lb=1, overflow/NaN, window-close abort). Listed apollo/src/ (blackboard*, ablation.py, mutation.py crossover at L675), configs/ (crossover_rate 0.3 in v2 genome configs), scripts/, checkpoints/ (last gen 160), run_v2d2b/, run_branch_c/run.pid, data/clean_canary_v01.json. git log -- apollo/ (Branch C lineage: a5998a13 gauntlet PASSES, ad574ef7 MAP-Elites, a03b9d4b forge handoff, 5cfe0357 honest tier downgrade R5->R1). Prior verdicts: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (row 1 REFACTOR-DEFER, 'R8 organism collapsed; eval feedstock/Atalanta ore, not in the loop'; row 14 Atalanta DEFER-with-Apollo), pivot/REASSESSMENT_2026-06-22_consolidated.md + _v3_the_reframing.md (0/8000 single-step vs 6.1%/pair crossover; de-novo solver 4/5 vs 0/5; crossover_frac=0.0 unwired; LOCAL-COMPLETE not infra-stalled), roles/Harmonia/AUDIT_20260622_instrument_monoculture.md (Apollo FP-003 expressiveness_ceiling confirmed by executing lens 2026-06-15; same FP as Harmonia void-miner), roles/EvolutionaryArchitectAndReasoningSpeciesEngineer/Apollo_Role_Document.md. NOT independently re-run: the crossover-flip and R2-decomposition experiments (both still unexecuted on disk).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Hephaestus  -  *AI suggestion (advisory, NOT approved): REVIVE*
*M3(dead) - daemon - op=- - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Automated forge for reasoning primitives. Original mandate (Mar 2026): take scored cross-domain concept triples from Nous (e.g. "Quantum Mechanics x Neural Plasticity x Model Checking"), enrich with Coeus causal directives, and use a frontier LLM (qwen-397B / Augment) to hammer them into deterministic Python ReasoningTool classes — non-neural algorithms that score/rank candidate answers. Each tool passed five gates: syntax, imports (numpy+stdlib), interface (evaluate/confidence), runtime, and a trap battery beating an NCD compression baseline. Intent: produce reasoning 'morphemes' as a mutation engine under falsification selection, where the kill geometry (what fails and how) is itself the navigable output.
- **Value thesis (Q2):** Produce the atomic reasoning strategies that the evolutionary composer (Apollo) wires into 'organisms', with the falsification battery applying selection at both levels. In the current shrunk program it is explicitly named THE decider's metabolizer: the forge half of the forge->Learner metabolization loop — a model consumes kill-geometry / failure clusters, behavior changes, survives ablation. Stated consumers, in order of history: Apollo (composition), then directly the Learner (Ergon). Disposition plan calls it 'the highest-leverage unblock in the program.'
- **Why it fell short (Q3) [unverified claim]:** Did NOT fall short as a spine component — it metabolized its own first thesis into a sharper one. The naive thesis (LLM-generated reasoning tools at scale) was falsified by its own instrumentation: ~1,960 tools were ~5 genuine mechanisms in costumes; the decorative-mechanism rut (project_forge_bypass_finding: ablation delta=0.0 on 'used' primitives = bypass-as-global-attractor); model sweep (12 models) and prompt sweep (5 strategies) proved the prompt template, not the model, dominates and all collapse to regex+NCD+meta-confidence. It also hit consumer-drift (April: produced into a vacuum after Apollo/attention pivot, 40 days dormant) and a starvation/dead-gated dependency (Nous, the 'zombie gate'). BUT the pivot to mechanism-knockout-validated hand-crafted typed engines produced the program's headline metabolization deltas (R3 +11pp, R4 +32pp from mining 6,657 failures) — genuine, generalizing on procedural held-outs. Current block is hardware (M3 dead) + the Nous gate, not capability.
- **Future options (Q4) [suggestions]:**
    - REVIVE-SPINE / RELOCATE off dead M3 to a live GPU box (M1/M2 or new PowerSpec) and bypass the Nous gate so the forge reads Learner failure clusters directly — the disposition plan's P0 call
    - REFACTOR the candidate contract: stop free-form concept-combination prompting (falsified primitive); LLM proposes transductions (NL->IR parsers, edge-case gens, mutation variants of specified algorithms) while verified typed kernels do reasoning, admitted by mechanism-knockout-first then behavioral-NCD
    - ADAPT into Apollo's typed-transformer interface via the existing blackboard_adapter.py — produce blackboard ops natively (declared reads/writes, knockout control, failure signature) instead of monolithic ReasoningTool wrappers, closing forge->compose->Learner
    - RUN the pre-registered falsification experiment (freeze ledger, nominate 3 next engines incl. minimal qualitative R5 causal-DAG engine, pre-register tier deltas, evaluate on fresh procedural probes) to settle whether failure-mining navigates or just narrates
- **Rubric (unverified):** realized=med-high — the +11pp/+32pp engines and blackboard_adapter are the concrete feedstock the Learner is meant to metabolize; not yet closed on a live Learner today because M3 dead + Nous gate, so realized-on-a-running-model is currently latent - latent=high — it is the named decider/metabolizer; once relocated and wired to Learner failure clusters it is the single highest-leverage unblock in the shrunk program - niche=unique — no other component forges verified typed mechanisms from kill-geometry; Apollo composes them, Techne is on-demand toolsmith, Nous (its old upstream) is being retired. The forge role is irreducible to the spine - health=dead-infra (hosted on dead M3) + dead-gated (Nous zombie gate) — code is runnable and recent (composer.py/blackboard_adapter.py May 29-30); the block is hardware+gate, not the forge - debt=med — legacy ~1,960 decorative tools + 6,657-entry ledger carry monoculture/decorative-mechanism debt; the validated spine is the ~9 knockout-verified engines, but CausalEngine is mislabeled/harmful (-6pp R5) and R5/R6 remain absent. Old LLM-generation path is meets-spec-misses-needle and should not be revived as-is
- **Salvage IP (lift before any retire):** Lift before any change: (1) the ~9 mechanism-knockout-validated typed engines in src/composer.py (OrderingEngine, ForwardChainEngine, ComputationEngine, SequenceEngine, StateEngine, ProbabilisticFallacyEngine, TemporalComputationEngine — the +11/+32pp gains); (2) blackboard_adapter.py (engine->Apollo typed-transformer translation); (3) the mechanism-knockout validation method + white paper 'Correct for the Wrong Reason'; (4) forge_primitives.py (27 composable building blocks / Frame H); (5) the 6,657-entry kill ledger + failure_mining_results.json (orthogonality-scored near-miss map) + novelty_scores.json; (6) puzzle_gen.py parameterized verified-problem generators + Harmonia-B deterministic ladder probes; (7) behavioral-NCD diversity metric.
- **Suggested settle condition (to verify, not act):** Run the pre-registered experiment: freeze the 6,657-entry ledger, mine it to nominate the next engines with pre-registered tier deltas, build them, and evaluate ONLY on fresh procedurally-generated probes (new seed). REVIVE-as-spine holds if predicted deltas land within +/-10pp AND a forged/composed engine measurably changes a downstream Learner's behavior and survives a mechanism-knockout ablation. It weakens toward REFACTOR/RETIRE if mined engines help training probes but not held-outs (overfitting the battery = failure-mining is retrospective storytelling, falsifier F5), or if typed kernels do not compose (F1) / every new engine cannibalizes another tier (F2).
- **Evidence read:** Read: agents/hephaestus/README.md, RESUME.md; forge/README.md, forge/STATUS_T1_T2_20260403.md; pivot/hephaestus_comprehensive_history_2026-05-30.md (full 8-phase history + falsifiers F1-F5 + op-family catalog), pivot/hephaestus_frontier_review_2026-05-30.md (GREENLIT-with-corrections verdict); pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (P0 REVIVE-SPINE/RELOCATE, 'highest-leverage unblock', bypass Nous), pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (Nous starvation context); memory docs project_forge_bypass_finding/feedback_forge_division/project_tiered_forge. Listed src/ (composer.py 44KB May30, blackboard_adapter.py May29, hephaestus.py 112KB, seed_forge/puzzle_gen May27) confirming spine artifacts present; git log shows the 9-engine composed-tool commit 6348c101 and honest causal_trace R5->R1 downgrade. failure_mining_results.json (orthogonality-scored near-misses) inspected. Component fully located across BOTH likely paths (agents/hephaestus/ T1 forge + forge/ T2/T3 tiers).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Nemesis  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*M3(dead) - daemon - op=- - shelved*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Built as the adversarial pressure / co-evolution layer of the forge pipeline (Nous->Coeus->Hephaestus->Nemesis->Coeus). Intent: answer "Are our evaluators measuring reasoning, or have they learned to pass tests?" It generates adversarial reasoning tasks via 12 formal metamorphic relations, organizes them in a 10x10 MAP-Elites quality-diversity grid (axes: logical complexity x linguistic obfuscation), shrinks failures to minimal failing cases, tracks cross-tool lineages, and surfaces a Goodhart gap (static accuracy minus adversarial survival) for each forged tool. The targeted problem: evaluator overfitting / Goodhart collapse — tools that score well on the static trap battery but fail under novel mutation.
- **Value thesis (Q2):** Value was selection pressure, not training signal: feed adversarial-survival data to Coeus (which builds a dual causal graph — forge-success vs adversarial-robustness — to demote Goodhart-flagged concepts), feed blind spots to Hephaestus as targeted forge requests, and weight RLVF fitness by adversarial survival. Consumers: Coeus (primary, causal demotion), Hephaestus (forge targeting), Nous (via Coeus sampling weights), RLVF (fitness). Architecturally firewalled: ALL output tagged provenance:adversarial and code-gated out of training paths (Rhea Batch 3 showed mixing adversarial signal cost 25 pts on metacognition).
- **Why it fell short (Q3) [unverified claim]:** Two ruts. (1) consumer-drift / dead-infra: the only LIVE consumer ever wired was Coeus (agents/coeus/src/coeus.py reads adversarial_results.jsonl). The README-advertised feedback arms never materialized — adversarial/targeted_forge_requests.jsonl (Hephaestus) is absent, and rlvf_fitness.py does not exist anywhere in the repo. So 3 of 4 claimed feedback loops were aspirational; the real loop was a single edge into Coeus. (2) The whole forge pipeline (Nous/Coeus/Hephaestus/Nemesis) died together on 2026-04-02 on machine M3 (now dead); Nemesis ran 100 cycles then stopped and was never restarted. Its sole consumer Coeus is itself archive-slated, so even its one real output edge now terminates in dead infra. Deeper: it operates on synthetic mutated traps and forged "tools," neither of which feeds the agreed decider (forge Hephaestus -> Learner: a model consumes kill-geometry and survives ablation). Its adversarial corpus is, by its own firewall design, banned from training — so it can never close the metabolization loop; it is structurally selection-only.
- **Future options (Q4) [suggestions]:**
    - retire-after-HITL: read-only archive the daemon + 2.3MB results + 3014 reports + grid (last ran Apr 2; on dead M3; sole consumer Coeus also archived) — strongest grounded outcome
    - retire-and-lift-asset: extract metamorphic.py (12 truth-preserving/flipping MRs + compose/targeted-chain) into the shared falsification library as a label-perturbation null generator — a metamorphic relation IS a structured null, directly aligned with the program's 'null must perturb the axis' doctrine
    - retire-and-lift-asset: lift shrink.py (7-strategy minimal-failing-case finder) as a general kill-minimizer for any falsification artifact; minimal failing case is reusable far beyond reasoning traps
    - adapt-to-decider: rebuild a thin metamorphic-null harness against a real Learner checkpoint (not forged tools) so adversarial deltas measure a model's kill-geometry robustness and feed an ablation — only path that attaches it to the spine
- **Rubric (unverified):** realized=none — no downstream model changes today; last cycle 2026-04-02, sole consumer Coeus is dead/archive-slated, output edge terminates in dead infra - latent=med — metamorphic-relation null generator + minimal-case shrinker are genuinely reusable falsification primitives that map onto current doctrine (perturb-the-axis nulls, minimal kills) - niche=redundant as an agent — adversarial pre-promotion role is covered by Charon (adversarial verdicts) and Aporia/Moros-role; disposition plan explicitly flags overlap with Moros/Charon. The metamorphic+shrink code is non-redundant as a library asset. - health=dead-gated + dead-infra — code is runnable pure-numpy (genuinely executed 100 cycles, grid 92/100), but on dead M3, depends on Hephaestus forge dir + Coeus, both dead; and by design its output is gated OUT of training so it can never reach the decider - debt=med — operates on synthetic mutated traps against forged 'tools' (an exhausted, decorative consumer set), not against a real model; README over-claims 3 feedback loops (forge-requests file + rlvf_fitness.py) that were never built
- **Salvage IP (lift before any retire):** Lift before retire: (1) src/metamorphic.py — 12 metamorphic relations (comparison_flip, verb_inversion, negation_inject, premise_shuffle, distractor_add, passive_voice, paraphrase, chain_extend, conditional_weaken, affirm_consequent, numeric_distractor, scale_transform) with flip/same/computed expected-behavior typing + compose_mrs/targeted_mr_chain — reusable as label-perturbing null generators into the shared falsification library; (2) src/shrink.py — 7-strategy minimal-failing-case algorithm; (3) src/map_elites.py — MAP-Elites grid + NCD novelty filter; (4) the provenance:adversarial training_gate pattern (code-enforced firewall) as a reusable two-track epistemics primitive; (5) the 2.3MB adversarial_results.jsonl corpus + difficulty model as a static reference dataset. Note absent assets: targeted_forge_requests.jsonl and rlvf_fitness.py do NOT exist despite README claims — nothing to salvage there.
- **Suggested settle condition (to verify, not act):** Revival is justified ONLY if a metamorphic-null harness, run against a current real Learner/model checkpoint (not forged tools), produces an adversarial-survival delta that changes a training/selection decision AND survives an ablation — i.e. it closes the metabolization loop. If, as the 06-15 firewall doctrine implies, its output remains training-banned and its only signal is over synthetic forged tools, it cannot feed the decider and should retire (read-only, data kept). Falsifier: show one downstream model whose behavior changed because of a Nemesis Goodhart-gap signal — none exists today.
- **Evidence read:** Read agents/nemesis/README.md (full) and src/nemesis.py (head) + grepped src/metamorphic.py structure. Confirmed src/ has 7 modules (nemesis/metamorphic/map_elites/shrink/evaluator/validators/reporter). Run history: nemesis.log (9.4MB, last entry 2026-04-02 12:44 shutdown after cycle 100), reports/ has 3014 nemesis_report_*.md (2026-03-25 to 2026-04-02), adversarial/adversarial_results.jsonl (2.3MB, 92 tasks), grid/grid.json (92/100 filled). VERIFIED ABSENT: adversarial/targeted_forge_requests.jsonl and repo-wide rlvf_fitness.py (README-claimed, never built). Live-consumer grep: only agents/coeus/src/coeus.py reads nemesis adversarial output; Coeus itself archive-slated. agents/moros/ does not exist (role-level overlap only). Prior verdicts read: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (PENDING line: 'adversarial pre-promotion — overlaps Moros/Charon; on dead M3'), aporia/docs/program_audit_2026-06-10.md and aporia/docs/STATUS_2026-06-15_reset.md (both: ARCHIVE Nemesis + Coeus), pivot/agent_roster_2026-05-28.md (M3, daemon, shelved).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Nous  -  *AI suggestion (advisory, NOT approved): PENDING-REVIEW*
*M4 - daemon - op=- - shelved*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** combinatorial hypothesis engine seeding the forge (cross-field concept triples)
- **Value thesis (Q2):** feed Hephaestus HIGH_POTENTIAL triples; Coeus forge-effect scores close the loop
- **Why it fell short (Q3) [unverified claim]:** claim-space exhaust + open Coeus feedback loop + starvation architecture (the zombie-gate label was pipeline ordering, not a property of Nous)
- **Future options (Q4) [suggestions]:**
    - adapt to operator-combinator (verbs over nouns)
    - diversity injector alongside Learner-failure targeting
    - retire daemon + lift concept dictionary into shared registry
- **Rubric (unverified):** realized=none - cold - latent=med - concept dict + sampler salvage - niche=combinatorial gen overlaps Theseus; concept taxonomy unique - health=blocked (needs NVIDIA key + Coeus) - debt=med - claim-space seed
- **Salvage IP (lift before any retire):** 95-concept x 18-field dictionary; cross-field sampler; operator-combination (verbs) adapt path
- **Suggested settle condition (to verify, not act):** operator-level Nous variant feeds a forge target beating Learner-failure-only baseline
- **Evidence read:** hand dossier 01 (pivot/agents_nous_resume_2026-05-13.md + audit Appendix A)
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Pronoia  -  *AI suggestion (advisory, NOT approved): REFACTOR*
*M4 - daemon - op=- - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Two eras under one name. Era 1 (README, agents/pronoia/README.md): "the Forethought Orchestrator" — a single-entry-point research-frontier scanner that chained Eos -> Aletheia -> Skopos -> Metis -> Clymene -> Hermes to scan arXiv/OpenAlex/S2/GitHub/Tavily, extract a knowledge graph, score against research threads, synthesize a daily executive brief, and email/publish it. Entry point pronoia.py. Era 2 (current, scripts/intelligence_loop.py): rebuilt as the M4 reporting/monitoring daemon — a fleet-visibility loop that refreshes the portfolio dashboard (docs/state.json), generates an LLM brief (docs/portfolio_brief.md) on a 4-hour cadence, sends a daily email digest, fires a Friday weekly recap, pushes the dashboard to GitHub Pages on main, and keeps agent:Pronoia ALIVE via Agora + Postgres dual-write heartbeats. The problem it targets: James needs phone-readable visibility into a 43-process multi-machine fleet without babysitting terminals.
- **Value thesis (Q2):** Add value by being the program's single observability surface: compress fleet state + per-agent health into an opinionated Act/Watch/Record brief and deliver it where James actually reads (email + GitHub Pages). Consumer is James (human operator), not a downstream model. Era 1 also intended to feed research intelligence into the program; Era 2 narrowed to operational monitoring. It is explicitly the "serve the coffee, you drink it" gated-autonomy layer — autonomous scan/synthesize/publish, human decides what it means.
- **Why it fell short (Q3) [unverified claim]:** Era 1 fell short and was abandoned — pronoia.py no longer exists at repo root, the audit logs stop at 2026-04-01, and the whole Eos->...->Hermes research-scanner chain is consumer-drift exhaust (it generated briefs nobody metabolized; classic claim-space generation, not capability-attached residue). Era 2 has NOT fallen short on liveness: the loop is demonstrably running on-cadence (auto-commits every 4h through 2026-06-24T04:15, dashboard data fresh, all 6 dependency scripts present). Its real defect is partial dead-infra in the telemetry path: its own Metis-authored brief reports "Pronoia at M4 unknown status due to no Postgres heartbeat" while Redis is unreachable at M1 — so the heartbeat it advertises (Agora->Redis primary, PG dual-write mirror) is not landing in the dashboard's PG-fallback view even though the process is alive. So: Era 1 = consumer-drift; Era 2 = healthy spine-adjacent loop with a PG-read/heartbeat wiring gap. Critically, Pronoia is monitoring, NOT the metabolization spine — it does not feed forge->Learner; its consumer is a human, which is the soft kind of consumer the governing principle scrutinizes.
- **Future options (Q4) [suggestions]:**
    - Revive-as-is (Aporia's 06-23 call): keep the 4h intelligence_loop as fleet monitor; the loop already runs and publishes. Lowest-effort, already in motion.
    - Refactor-to-PG-native: fix the one real defect — make the heartbeat + dashboard source read Postgres directly (not Redis), so Pronoia stops reporting itself and peers as 'unknown' when Redis is down. This is the explicit instruction in COMPONENT_DISPOSITION_PLAN ('Must read PG, not Redis').
    - Adapt-to-spine-instrument: re-aim the LLM brief from generic fleet status toward the decider's health (forge rate vs Learner-deficit, ablation-survival, kill-geometry consumption) so its output sharpens the spine instead of narrating the fleet — converts a human-only consumer into a spine-feeding one.
    - Retire-Era-1-and-lift-assets: formally retire the dead pronoia.py scanner chain (Eos/Aletheia/Skopos/Metis/Clymene/Hermes orchestration) after HITL; lift the audit/health-check logic (check_intelligence_pipeline.py rate-limit/zero-output/VRAM-leak detectors) into the minimal HealthCheck probes.
- **Rubric (unverified):** realized=low — output (state.json, portfolio_brief.md, email) changes NO downstream model today; consumer is James, not the Learner. It is live and producing, but realized value is human visibility, not metabolization. - latent=med — refactored to read PG and re-aimed at spine health (forge/Learner deficit), it becomes the instrument that confirms the decider is actually metabolizing; useful but always support, never spine. - niche=unique — the only LLM-synthesized fleet-wide brief + email/Pages delivery. HealthCheck-M1/M2/M4 are per-machine machine_probes, not a fleet observability surface; no redundancy with them. Era-1 research-scan niche is dead (overlaps Clio/Pythia/Aletheia which won). - health=runnable (Era 2): loop alive, on-cadence, auto-committing through 2026-06-24, all deps present. Partial dead-infra in the telemetry path (PG heartbeat not landing while Redis down). Era 1 (pronoia.py chain) is dead-infra — entry point gone, logs end 2026-04-01. - debt=med — meets-spec (it does report) but misses-needle: it monitors the fleet rather than the decider, and its self-status is wrong (reports itself 'unknown' while running) because it leans on a Redis-primary path that is down. Generic-brief tendency over spine-relevant signal.
- **Salvage IP (lift before any retire):** Lift before any retire: (1) the Postgres dual-write heartbeat pattern (_start_pronoia_pg_heartbeat in scripts/intelligence_loop.py) — reusable ALIVE-mirroring for any daemon, already mirrors Apollo/Hephaestus. (2) scripts/check_intelligence_pipeline.py health heuristics: rate-limit/429, API-timeout, zero-output (<20 char), knowledge-flat-growth, and VRAM-leak detectors — directly portable into HealthCheck probes. (3) The idempotent push_dashboard_to_main() git-publish-with-rebase-retry helper. (4) The Act/Watch/Record brief schema + Metis portfolio prompt (scripts/metis_portfolio.py) as the fleet-summarization template. (5) Era-1 scanner pipeline contract (agents/pronoia/README.md) as the canonical spec if any frontier-scan capability is ever rebuilt.
- **Suggested settle condition (to verify, not act):** REVIVE/REFACTOR is settled (vs RETIRE) if, after pointing the heartbeat + dashboard source at Postgres, Pronoia's next brief reports its own status and live peers' status CORRECTLY (no false 'unknown' for running processes) AND a human (James) confirms the email/Pages brief changes an operational decision at least once. It tips toward RETIRE-after-HITL only if, even PG-native, the brief is never read/acted on over a 2-week window and the raw docs/state.json suffices — i.e. the LLM-synthesis layer adds nothing a flat status dump doesn't.
- **Evidence read:** Read: agents/pronoia/README.md (Era-1 spec, references nonexistent root pronoia.py; audit logs in agents/pronoia/logs/ stop 2026-04-01). scripts/intelligence_loop.py (Era-2 active daemon, full read). scripts/check_intelligence_pipeline.py (health checker, full read). docs/portfolio_brief.md (fresh 2026-06-24 04:15, Metis-authored, self-reports Pronoia 'unknown status'). docs/state.json (fresh 2026-06-24 02:02). Confirmed all 6 deps present (portfolio_monitor.py, metis_portfolio.py, send_brief_email.py, weekly_recap.py, orchestration_logging.py, agora_persist.py). git log: auto-commits every 4h through 2026-06-24T04:15:44Z = loop is LIVE; feature commits 373d8146 (PG dual-write), ab55ea85 (4h cadence). Prior verdict: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md row 5 = 'Pronoia / M4 / REVIVE — fleet monitor, must read PG not Redis (already dual-write capable)'; also pivot/INFRA_RECOVERY_PLAN_2026-06-23.md, roles/PipelineOrchestrator/*. Note 06-24 amendment: no DELETE state; strongest outcome RETIRE-after-HITL.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________


## Pipeline stages

### Aletheia  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*? - pipeline-stage - op=- - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Aletheia was built as the long-term structured memory of Prometheus: a knowledge-graph harvester that reads papers/repos discovered by Eos (the horizon scanner), runs each through an LLM extraction pass (NVIDIA Nemotron -> Cerebras -> Groq fallback), and accumulates seven entity types (techniques, reasoning_motifs, tools, terms, claims, papers, review_queue) into a persistent SQLite knowledge_graph.db. It targeted the 'institutional memory' problem — the program kept re-discovering the same external concepts and had no durable, queryable record of the literature it had scanned. A second, more ambitious intent lived in ingest.py: to be the *constitutional substrate-deposition layer* enforcing Law 1 ('The Substrate Is the Product') and Law 3 ('Every Agent Feeds the Substrate'), exposing deposit_entity/deposit_relationship/deposit_gap plus a check_constitutional_gate() that would block GPU experiments unless the substrate grew >=5 entities/24h.
- **Value thesis (Q2):** Value was supposed to flow two ways. (1) As pipeline stage Eos->Aletheia->Skopos->Metis->Clymene->Hermes: Aletheia turns raw scan hits into a deduped, conflict-flagged taxonomy that Skopos scores against research threads and Metis injects into executive briefs to ground recommendations in accumulated knowledge rather than the latest digest; a future 'Grammata' UI and 'ergon learner' were named as downstream consumers of the DB/export. (2) As constitutional spine via ingest.py: a single substrate every agent writes into, with a growth gate making 'feed the substrate' a hard precondition for compute. Intended consumers: Skopos, Metis, Grammata (never built), and the Learner (aspirational).
- **Why it fell short (Q3) [unverified claim]:** Multiple compounding ruts. (a) consumer-drift / dead-infra: the whole cold serial pipeline is unanimously marked ARCHIVE in three independent audits (program_audit 2026-06-10, STATUS reset 2026-06-15, COMPONENT_DISPOSITION_PLAN 2026-06-23). Last actual run was 2026-04-01 (log + DB mtime) — dead ~2.5 months. (b) claim-space-exhaust: the harvested corpus is generic-arXiv soup (CO2 biomass gasification, breast-cancer/DDT, trunk-muscle-strength, radar signal processing) lightly seasoned with mech-interp terms. This is exhaust, not capability-attached failure residue — exactly what the program is intentionally shrinking away from; it never connects to the metabolization loop (forge->Learner->ablation). (c) decorative-mechanism: it is called a knowledge *graph* but substrate_relationships=0 and substrate_gaps=0 — 300 substrate_entities and 224 terms with literally zero edges. It is a flat list mislabeled a graph; the relational structure that would make it navigable was never populated. (d) dead-gated/decorative constitution: ingest.py's Law-1 enforcement (deposit_entity, check_constitutional_gate) has ZERO importers anywhere in the repo — the 'every agent feeds the substrate' gate was never wired into any agent. The pipeline yaml still says status:active and lists 'ergon learner' as a consumer, but no Ergon/Learner code reads the DB — that is stale aspiration.
- **Future options (Q4) [suggestions]:**
    - retire-and-lift-asset: RETIRE-after-HITL the agent + cold pipeline (read-only, DB kept), but lift the SQLite knowledge-graph schema (7 typed entity tables + review_queue conflict-flagging with trigram-Jaccard dedup) and the ingest.py deposit/gap/health API into harmonia/lib/falsification_primitives as the canonical substrate-deposition schema the reset already wants extracted
    - refactor-to-residue-store: repoint the entity/relationship/gap tables away from external-paper harvesting and toward the metabolization loop — store Hephaestus forge tools, Learner failure clusters, and kill-geometry as typed entities with real relationships (the substrate_entities entity_type enum already includes forge_tool/forge_failure/reasoning_failure/waste_stream_sample), so the 'graph' finally grows edges from capability-attached failure
    - adapt-the-gate: salvage check_constitutional_gate() as a generic substrate-growth assertion and actually wire it into the spine's batch scripts, replacing the external-scan health metric with metabolization-loop deltas (tools forged, ablations survived)
    - revive-as-is: REJECTED — only if an external-literature institutional memory becomes a live need again AND a real consumer (Learner) is wired to read it; absent that, revival re-creates exhaust
- **Rubric (unverified):** realized=none — last run 2026-04-01, zero live importers of ingest.py, no downstream agent currently reads the DB; changes no model today - latent=med — the typed-entity schema, conflict-flagging dedup, and the deposit/gap/health API are genuinely reusable substrate infrastructure if repointed at the metabolization loop - niche=redundant as a harvester (overlaps Eos scan + Clio + cold-LLM probes; reset folds it into the archived cold pipeline); but the SUBSTRATE-DEPOSITION SCHEMA niche is unique — no other component defines the canonical typed knowledge-graph store - health=dead-infra + decorative-mechanism: runnable code (stdlib-only) but unrun for 2.5mo, zero graph edges despite 300 nodes, and the constitutional gate is dead-gated (never imported) - debt=high — 'knowledge graph' with zero relationships is a meets-spec-misses-needle shortcut (looks like a graph, is a flat list); corpus is generic-arXiv exhaust, not Prometheus-relevant; pipeline yaml falsely advertises status:active and an ergon-learner consumer that does not exist
- **Salvage IP (lift before any retire):** (1) The 7-table typed knowledge-graph SQLite schema (techniques/reasoning_motifs/tools/terms/claims/papers/review_queue) and the substrate_entities/relationships/gaps schema with its entity_type/evidence_grade/specimen_class/provenance enums — the cleanest substrate-deposition schema in the repo, already on the reset's extract-to-harmonia/lib list. (2) ingest.py's deposit_entity/deposit_relationship/deposit_gap + get_substrate_health/check_constitutional_gate API (idempotent upsert, WAL mode). (3) The review_queue conflict-flagging logic: name-similarity >=85% with description-divergence <40% via trigram Jaccard for near-duplicate entity detection. (4) The accumulated DB itself (163 papers, 224 terms, ~76 claims) kept read-only as a historical literature snapshot.
- **Suggested settle condition (to verify, not act):** Wire one real metabolization-loop consumer (the Learner) to read knowledge_graph.db and demonstrate a behavior delta that survives an ablation: if the Learner's failure-clustering or routing measurably improves with Aletheia entities vs without (and the gain survives a permutation/shuffle null), REVIVE/REFACTOR is justified. If no such consumer materializes within the spine shrink, or the only content remains external-paper exhaust with zero graph edges, the RETIRE stands.
- **Evidence read:** Read agents/aletheia/README.md, agents/aletheia/src/ingest.py; inspected agents/aletheia/data/knowledge_graph.db via sqlite3 (papers=163, terms=224, techniques=93, claims=76, review_queue=32, substrate_entities=300, substrate_relationships=0, substrate_gaps=0; sampled claims/terms/entities = generic-arXiv content); tailed agents/aletheia/aletheia.log (last run 2026-04-01). Confirmed zero importers of aletheia.ingest/deposit_entity/check_constitutional_gate via repo-wide Grep. Cross-checked prior verdicts: aporia/docs/program_audit_2026-06-10.md (ARCHIVE cold pipeline), aporia/docs/STATUS_2026-06-15_reset.md (ARCHIVE Eos->Aletheia->...->Hermes; EXTRACT schema to harmonia/lib), pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md line 86 (Aletheia,Eos = archive), pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (borderline ARCHIVE -> PENDING-REVIEW), pipelines/intelligence-pipeline.yaml (stale status:active, lists ergon-learner consumer not present in code).
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Coeus  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*? - pipeline-stage - op=- - shelved*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Coeus was built as the "causal intelligence layer" sitting between Nous (concept-triple generator) and Hephaestus (the forge). Intent: instead of letting Hephaestus forge from raw Nous scores, learn WHICH concepts/fields/score-dimensions actually CAUSE forge success vs merely correlate, then (a) feed prescriptive code-generation directives into Hephaestus's prompt, (b) emit per-concept forge-effect scores that bias Nous's triple sampling toward productive concepts, closing the Nous->Coeus->Hephaestus->Coeus loop. Problem targeted: the forge was firing blind on a 95-concept x 18-field combinatorial space (~40% forge rate) with no learned prior about what works. Auto-triggered every 50 forges.
- **Value thesis (Q2):** Value = turn accumulated forge outcomes into a steering signal for the generation+forge pipeline. Consumers were concrete and two-sided: (1) Nous (agents/nous/src/nous.py:_load_coeus_weights) reads graphs/concept_scores.json to apply 3x/2x/0.3x sampling weights with Goodhart demotion; (2) Hephaestus (agents/hephaestus/src/build_reports.py:load_coeus_enrichment) injects per-triplet enrichment_text directives into the forge prompt; (3) RLVF fitness (rlvf_fitness.py) was meant to use Coeus adversarial-survival rates as tool weights bridging the forge to Rhea's evolutionary loop. The Nemesis dual-graph added a Goodhart detector: high-forge + low-adversarial concepts flagged WARN, low-forge + high-adversarial flagged undervalued/BOOST.
- **Why it fell short (Q3) [unverified claim]:** Three converging failures. (1) DECORATIVE-MECHANISM on the headline claim: it is the 'causal' layer in name only. README/manifest promise NOTEARS/LiNGAM/FCI/DAGMA, but the built causal_graph.json records method='lasso_regression' — the L1-regression FALLBACK. None of causal-learn/lingam/dagma were installed, so no actual causal discovery (structure learning, latent-confounder detection, do-calculus) ever ran. It ships conditional associations dressed as causal effects; the README's '+1.155 causal effect, proven unconfounded mechanical advantage' directives are Lasso coefficients. (2) DEAD-GATED / open feedback loop: outputs last rebuilt 2026-03-27 (~3 months stale); both Nous (upstream) and Hephaestus (downstream) are cold/shelved, so the loop it closed is open at both ends. It is part of the old agents/ ring the 2026-06-23 reset is dismantling. (3) CLAIM-SPACE-EXHAUST at the root: its entire substrate is Nous combinatorial concept-triples, which program doctrine classifies as exhaust (claim-space generation), NOT capability-attached residue. Coeus ranks combinatorial NOUN combinations, feeding the math-claim monoculture from the analysis side. The agreed decider is the metabolization loop (forge consumes Learner kill-geometry, behavior changes, survives ablation); Coeus steers the forge by combinatorial concept productivity, which is orthogonal to that — it never touched Learner failure geometry.
- **Future options (Q4) [suggestions]:**
    - retire-after-HITL: read-only archive the loop (Nous->Coeus->Hephaestus is being dismantled by the reset; disposition plan 2026-06-23 explicitly lists it ARCHIVE). Keep graphs/ and code; sever live reads from Nous/Hephaestus.
    - lift-asset: salvage the L1/interventional regression harness (_encode_dataset + _regression_influence + counterfactual P(forge|do(remove X))) and re-aim it at the SPINE — i.e. learn which Hephaestus-tool / kill-geometry features predict surviving an ablation, replacing combinatorial-concept features with Learner-failure-cluster features. This converts exhaust-driven analysis into residue-driven analysis.
    - refactor-to-verbs: re-key the influence/synergy machinery from concept NOUNS to operator/Frame-H VERB primitives (feedback_verbs_over_nouns), so synergy/conflict scores describe operator compositions rather than discipline labels — only worth it if a Nous-verb-variant is revived.
    - adapt-the-Goodhart-detector: lift the forge-vs-adversarial divergence logic (the genuinely useful part) into the live spine as a static-pass-vs-ablation-survival divergence flag, independent of the combinatorial concept layer.
- **Rubric (unverified):** realized=none — cold since 2026-03-27; both consumers (Nous sampling, Hephaestus enrichment) are shelved, so no output changes any model today. - latent=low-to-medium — the regression+interventional+Goodhart-divergence harness is reusable IF re-aimed at Learner-failure features; as a concept-combination ranker its ceiling is bounded by claim-space exhaust. - niche=redundant-as-built — its analysis layer overlaps the program's general failure-signal/learner work; only the Goodhart forge-vs-adversarial divergence idea is somewhat unique, and even that overlaps Nemesis/Charon adversarial gating. - health=dead-gated + decorative-mechanism — runnable in fallback mode but the 'causal' methods (NOTEARS/LiNGAM/FCI/DAGMA) were never installed and never ran; ships Lasso coefficients labeled as causal effects; on the dismantled agents/ ring. - debt=medium-high — claim-space-generation seed feeds the math-claim monoculture from the analysis side; plus a truth-in-labeling debt (README claims causal discovery the artifact never performed).
- **Salvage IP (lift before any retire):** (1) _encode_dataset + _regression_influence + counterfactual interventional estimator P(forge|do(remove X)) in agents/coeus/src/causal_graph.py — a generic feature->binary-outcome influence+counterfactual harness. (2) The Goodhart divergence detector in coeus.py (forge-effect vs adversarial-survival, WARN/BOOST flags) — the genuinely novel reusable idea. (3) graceful-degradation pattern across causal-learn/lingam/dagma backends. (4) The 4031-obs x 95-concept built dataset matrix + concept_scores.json/causal_graph.json as a historical forge-outcome corpus. (5) The prescriptive-enrichment renderer in enrichment.py (turns coefficients into code-gen directives) — reusable templating even if re-keyed to operators.
- **Suggested settle condition (to verify, not act):** Re-target the existing regression+interventional harness so its features are Learner failure-cluster / kill-geometry descriptors (not Nous concept indicators) and its outcome is forge-tool ablation-survival. If the resulting concept_scores analog produces a forge-target prioritization that, when consumed, measurably raises ablation-survival of forged tools over a uniform/random baseline, REVIVE the harness in that spine role. If it cannot beat uniform (i.e. the signal lives only in the combinatorial-concept axis, the exhaust axis), RETIRE — the layer adds nothing the spine needs.
- **Evidence read:** Read: agents/coeus/README.md (causal claims, current findings table), agents/coeus/configs/manifest.yaml (promised deps incl. dowhy/tigramite 'future'), agents/coeus/src/coeus.py (full driver, Nemesis dual-graph, Goodhart logic), agents/coeus/src/causal_graph.py (degrade-gracefully imports, _encode_dataset, _regression_influence). Inspected built outputs: graphs/causal_graph.json (method='lasso_regression', n_observations=4031, n_forged=352, 95 concepts, 50 synergies; mtime 2026-03-27 = ~3mo stale), concept_scores.json (has adversarial_survival + goodhart_indicators), 4031 enrichment JSONs. Confirmed live consumers: agents/nous/src/nous.py:113 _load_coeus_weights (3x/2x/0.3x + Goodhart demote), agents/hephaestus/src/build_reports.py:129-240 (enrichment injection). Prior verdicts: pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md line 85 (ARCHIVE: 'causal-analysis stage; reset said archive'), pivot/agent_roster_2026-05-26/28 (shelved), pivot/agents_nous_resume_2026-05-13.md (Coeus COLD, open feedback loop), pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (claim-space-exhaust framing on the Nous seed). Causal methods never ran: method field = fallback, optional packages absent.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Eos  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*? - pipeline-stage - op=- - active*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Eos ("Dawn") is the horizon scanner: a daemon meant to continuously watch the external research frontier and resource landscape so Prometheus would not work blind. Concretely it scans five sources (arXiv, OpenAlex, Semantic Scholar, GitHub, Tavily) for new papers/repos/tools in the program's lanes (mech-interp, steering vectors, evolutionary search, autonomous agents), discovers and tracks free/low-cost API tiers (its self-described "first mission: discover what free resources exist for herself"), dedups via a persistent fingerprint index, deep-analyzes top items via Nemotron 120B, scores relevance, and writes a daily ATTENTION-REQUIRED digest plus depositing entities into Aletheia. The problem it targeted: external-awareness gap and resource-budget blindness for an autonomous multi-agent program.
- **Value thesis (Q2):** Value was meant to flow two ways. (1) Intelligence: surface frontier papers/tools/free APIs that could accelerate Prometheus, with a curated top-10 digest the human (James) reads first; consumer = James + the downstream knowledge pipeline. (2) Substrate feed: it was the upstream head of the cold serial pipeline Eos -> Aletheia (knowledge graph) -> Skopos -> Metis -> Clymene -> Hermes, depositing 'paper'/'tool' entities into Aletheia's knowledge_graph.db for downstream synthesis. The api_registry was meant to be living infrastructure (verified free LLM endpoints + rate limits) reusable program-wide.
- **Why it fell short (Q3) [unverified claim]:** Consumer-drift + dead-gated. The daemon works (clean runs in the log: 20 arxiv / 15 each from the others / Nemotron analysis), but it stopped: last digest 2026-04-01, last code touch 2026-04-03, and Aletheia's last processing run was 2026-04-01 (handling exactly 1 unprocessed item). Two independent program reviews diagnose it identically and damningly: program_audit_2026-06-10 lists "production without consumption... Eos pipeline -> terminal email no one reads. A loop with no consumer optimizes for its own throughput metrics -- monoculture is the cheapest way to satisfy them," and STATUS_2026-06-15_reset explicitly marks the whole Eos->Aletheia->...->Hermes pipeline for ARCHIVE. The deeper failure against the program decider: Eos generates bibliographic claim-space (external papers/repos), which is exhaust by the program's own definition, not capability-attached failure-residue. Nothing it emits feeds the metabolization loop (forge Hephaestus -> Learner consumes kill-geometry). Its dedup index even shows degenerate churn -- the same stale items re-seen 60 times across cycles -- a throughput metric satisfying itself.
- **Future options (Q4) [suggestions]:**
    - RETIRE-after-HITL: freeze the daemon read-only, keep all data (reports/, paper_index.json, api_registry.json, knowledge_graph.db), per the two existing archive verdicts.
    - Lift-asset: extract api_registry.json into a program-shared resource config (verified free-tier LLM endpoints + 75%-rule rate limits) that the Hephaestus/Learner forge loop and any API-calling agent consults -- this outlives the pipeline.
    - Adapt-to-decider (narrow revive): re-point Eos from broad claim-space scanning to a capability-attached query -- e.g. only surface tools/papers that map onto an open Learner gap or a registered failure-signature -- so its output becomes residue not exhaust. Gate revival on a real downstream consumer existing first.
    - Promote library_scanner.py separately: it is orthogonal to the dead pipeline (pure introspection of installed math libs for functions embeddable into the Poros tensor) and is substrate-relevant; spin it out of Eos and attach it to prometheus_math/Poros tooling.
- **Rubric (unverified):** realized=none — no downstream model output changes today; pipeline dead since 2026-04-01, two archive verdicts standing - latent=low-to-med — only as lifted assets (api_registry, library_scanner) or a narrowly re-scoped capability-attached scanner; broad scanning has no latent value vs the decider - niche=redundant — external scanning overlaps Atalanta/Pheme (also dead upstreams) and ad-hoc deep-research; api_registry is the one non-redundant artifact - health=dead-gated — code is runnable and well-written but the consumer (Aletheia chain) is itself archived, so it runs into a void - debt=low code debt (clean, portable, good rate-limit hygiene) but high doctrinal debt: it is the canonical 'production without consumption / throughput-monoculture' antipattern the program is explicitly shrinking away from
- **Salvage IP (lift before any retire):** data/api_registry.json (verified free-tier LLM endpoints, env vars, rate limits, billing-trigger notes — directly reusable as a program-wide resource config); RateLimiter class (75%-rule exponential backoff + jitter + success-decay, the codified 'know before you knock' discipline); PaperIndex fingerprint+dedup (normalized title|first-author|year, cross-cycle persistence); src/library_scanner.py (introspection of installed math libs -> embeddable-function manifest for the Poros tensor, substrate-relevant and independent of the dead pipeline); the relevance-scoring keyword tiers as a cheap pre-LLM filter.
- **Suggested settle condition (to verify, not act):** Revive only if a live downstream consumer is demonstrated: a single Eos-surfaced item must propagate to a behavior delta in the decider within one cycle -- i.e. it lands in Aletheia AND a forged Hephaestus tool / Learner training input that survives an ablation traces back to it. Absent that chain (which has been dead since 2026-04-01 and is already marked ARCHIVE twice), RETIRE-after-HITL stands. Falsifier for retire: produce one digest item that changed a downstream model this quarter -- none exists.
- **Evidence read:** Read: agents/eos/README.md, agents/eos/src/eos_daemon.py (full), agents/eos/src/library_scanner.py (header), agents/eos/reports/2026-04-01.md, agents/eos/reports/eos_daemon.log, agents/eos/data/paper_index.json (meta + churn: seen_count=60), agents/eos/data/api_registry.json (head). Confirmed downstream: agents/aletheia/src/ingest.py exists, agents/aletheia/aletheia.log (last run 2026-04-01, 1 item), aletheia/data/knowledge_graph.db (last write 2026-04-01). Prior verdicts: aporia/docs/program_audit_2026-06-10.md lines 136, 165-168, 238 (ARCHIVE Eos pipeline; 'terminal email no one reads'); aporia/docs/STATUS_2026-06-15_reset.md line 105 (ARCHIVE Eos->...->Hermes). Git: last commit touching agents/eos 2026-04-03; reports cease 2026-04-01.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

### Hermes  -  *AI suggestion (advisory, NOT approved): RETIRE-after-HITL*
*? - pipeline-stage - op=- - deprecated*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Hermes was the final stage of the intelligence pipeline (Pronoia -> Eos -> Aletheia -> Skopos -> Metis -> Clymene -> Hermes -> James). It collected each cycle's filesystem outputs (Metis brief, Eos digest, Clymene hoard, Aletheia KG counts, pipeline-health audit, forge-pipeline stats, constitutional substrate health), compiled a single markdown digest, and emailed it via Gmail SMTP (SSL/465) as HTML+plaintext. It targeted the problem of unattended operation: surface 'the 3-5 things that matter' to James daily so he could route relevance to the right pillar without babysitting terminals. It carried real engineering: content-hash dedup that strips timestamps/volatile stats so emails only fire on genuinely new findings, [ACTION] subject prefixing on Metis 'Act on this' items, a stdlib-only markdown->HTML renderer, and graceful degradation when credentials are absent.
- **Value thesis (Q2):** Value was 'James is the synapse' — a human-attention router. The digest led with the Metis executive brief and appended supporting detail, truncating ruthlessly (Eos capped at 3000 chars), so James could scan one email and manually relay relevant items to Charon/Noesis/Forge. Consumer = James (and indirectly downstream pillars via his routing). It was a notification/alerting seam, not a substrate component; its output never fed a model.
- **Why it fell short (Q3) [unverified claim]:** Did not 'fail' scientifically — it was deliberately superseded (consumer-drift + parallel-implementation drift). During 2026-05 multi-machine orchestration, reporting requirements outgrew Hermes's single-pipeline, single-machine, filesystem-bound design: reporting had to be machine-agnostic, aggregate across pipelines, and act as a GitHub-router. scripts/send_brief_email.py was built to read the shared Agora substrate (Redis streams + Postgres agora.intelligence_outputs) plus docs/portfolio_brief.md, auto-surface mentioned-agent reference docs, and append a static catalog. It does everything Hermes did, pipeline-agnostically. Deprecated 2026-05-17 (run_hermes call commented out in pronoia.py). Run history confirms death: digests/ stops 2026-04-01, sent_state shows last_sent 2026-04-01 / send_count 41, no live Python imports the module. Health: dead-gated (orphaned, no caller). The architectural lesson recorded: 'parallel-implementation drift was the problem — pipelines write standardized telemetry to the shared substrate, never assume a particular reader.'
- **Future options (Q4) [suggestions]:**
    - retire-and-lift-asset: RETIRE-after-HITL the agents/hermes/ tree read-only; lift the two reusable assets (the stdlib markdown->HTML email renderer _markdown_to_html and the timestamp/stat-stripping content-hash dedup _content_hash) into scripts/send_brief_email.py if it lacks them
    - adapt-to-multi-channel: when notification grows beyond email (Slack/Twilio/PushNotification), build channels INTO send_brief_email.py (renamed notifier.py) per the deprecation note's explicit instruction — do NOT resurrect agents/hermes/
    - retire-and-lift-asset: salvage the section-level dedup discipline (only emit on genuinely-new findings, normalizing volatile stats) as a general pattern for any future alerting layer feeding the spine
    - revive-as-is: rejected — would re-introduce the exact parallel-implementation drift the deprecation was created to kill, and the successor is already strictly more capable
- **Rubric (unverified):** realized=none — orphaned since 2026-04-01, no caller, no model consumes its output; pure human-notification seam even when live - latent=low — only as a parts donor (HTML renderer + dedup hash) to the live notifier; no path to the metabolization loop - niche=redundant — fully subsumed by scripts/send_brief_email.py, which is pipeline-agnostic and reads the Agora substrate - health=dead-gated — run_hermes() commented out in pronoia.py, zero live Python imports, digest/sent-state history frozen at 2026-04-01 - debt=low — code is self-contained, stdlib-only, gitignored credentials; carries a documented 'parallel-implementation drift' anti-pattern that the successor was built to retire
- **Salvage IP (lift before any retire):** Before retire, lift two assets into scripts/send_brief_email.py (or notifier.py): (1) _markdown_to_html — stdlib-only markdown->inline-styled-HTML email renderer with table/header/list handling; (2) _content_hash + diff_sections — section-level dedup that strips timestamps, dates, scan/forge/grid counts before hashing so alerts fire only on genuinely-new findings (send_count=41 vs many more cycles shows it worked). The [ACTION]-subject-prefix-on-'Act on this' heuristic is also a cheap reusable pattern.
- **Suggested settle condition (to verify, not act):** Grep the whole repo (esp. pronoia.py, all crons/loops, scripts/) for any LIVE caller of agents/hermes (run_hermes, import hermes, agents.hermes); if zero live callers AND scripts/send_brief_email.py is confirmed sending the daily digest, Hermes is redundant and retires. Revival would only be justified if send_brief_email.py is found NOT delivering and no faster fix exists. (Current evidence: zero live callers, history frozen 2026-04-01 — condition already points to RETIRE.)
- **Evidence read:** Read: agents/hermes/README.md, agents/hermes/src/hermes.py (full, 821 lines), agents/hermes/config.json (enabled=false, creds via eos/.env), agents/hermes/data/sent_state.json (last_sent 2026-04-01T07:26Z, send_count 41), agents/hermes/digests/ (history stops 2026-04-01), agents/hermes/logs/ (last hermes_2026-04-01.jsonl), scripts/send_brief_email.py (successor, head). Prior verdicts: pivot/hermes_deprecation_2026-05-17.md (full — the deprecation decision + 'parallel-implementation drift' lesson + 'do not resurrect agents/hermes/' instruction), pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (#43 Hermes DELETE/needs-James-confirm), pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (Hermes -> PENDING-REVIEW, likely clean RETIRE-after-HITL), pivot/REASSESSMENT_2026-06-22_consolidated.md (lesson #7), agent_roster_2026-05-28.md (Hermes pipeline-stage / deprecated). Grep confirmed NO live Python references to run_hermes/import hermes/agents.hermes across the repo.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________


## Other

### Calliope  -  *AI suggestion (advisory, NOT approved): KEEP-on-demand*
*M4 - tool - op=- - active(on-demand)*  -  found=yes  -  **state: LIMBO (paused, intact)**

- **Why built (Q1):** Built 2026-05-19 (single-day effort, 2 commits: 772fc1e8 then f2b194d8 the same day) as the "daily NotebookLM narrative synthesizer." It targets a human-attention problem: the program emits a high-volume firehose of activity (commits, Pythia deep-research reports, new pivot docs, dashboard anomalies, Postgres intelligence_outputs) and James cannot track it from raw logs. Calliope accumulates the last 24h of that activity and writes a single narrative markdown file in docs/notebook_lm/ shaped specifically for NotebookLM ingestion (flowing prose, short paragraphs, inline full-GitHub-URL citations, a clean Sources section with README.md always first) so James can paste it into NotebookLM for an audio overview. Source: scripts/calliope_daily.py (CALLIOPE_SYSTEM_PROMPT, docstring lines 2-21).
- **Value thesis (Q2):** Value = compress a day of distributed agent activity into one voiceable, fully-cited narrative for a single human consumer (James) via NotebookLM. It is a reporting/synthesis tool aimed strictly at the human-in-the-loop's situational awareness, not at any model or the metabolization loop. Gathers from git (gather_recent_commits with numstat + bodies), pivot/ mtimes, docs/state.json dashboard snapshot, and Postgres intel via agora_persist; narrates with llm_cascade.call_llm; emits markdown + telemetry. The consumer is explicitly the human, not the Learner — its output is meant to drive James's epiphanies/check-ins (aligns with the project's NotebookLM-milestone habit), never to change a downstream model.
- **Why it fell short (Q3) [unverified claim]:** It did not "fall short" in a rut sense — it is a healthy, cheap, correctly-scoped peripheral tool. The honest gap is consumer-frequency: despite the "daily" name it ran exactly ONCE. Only one output exists (docs/notebook_lm/calliope_daily_2026-05-19.md, dated the same day it was committed); the other notebook_lm/*.md files predate it and were hand-authored. No cron/scheduler/loop wiring references it anywhere (grep of all json/yaml/sh/ps1 found only the state.json roster entry), so "daily" was aspirational — it is invoke-on-demand in practice and was reclassified as such (state.json: invoke_on_demand=true; disposition plan row 35 "KEEP on-demand"). Relative to the spine it is decorative by design: its output changes no downstream model and carries no kill-geometry (consumer-drift toward human-reporting rather than the metabolization loop). The one self-narrated entry it produced ("Calliope: daily NotebookLM narrative synthesizer") is a minor self-reporting curio, not a flaw.
- **Future options (Q4) [suggestions]:**
    - Revive-as-is on-demand: keep exactly as a manually-invoked human-facing synthesizer; cost is one LLM cascade call per run, value is real to James when he wants a NotebookLM-voiceable daily.
    - Refactor-to-scheduled: wire into the existing cron/loop infra (the deferred Cron* tools / scripts reporting tooling already on M4) so 'daily' becomes literal, with a no-activity guard to skip empty windows — closes the name-vs-behavior gap that made it run only once.
    - Adapt-to-spine-narrator: re-point the same gather/cite/narrate skeleton at the metabolization loop's outputs (forge yields, Learner ablation deltas, kill-geometry survivors) so the narrative tracks what actually moves the model — converts a decorative reporter into a loop-attached progress narrator with a defined model-relevant subject.
    - Retire-after-HITL and lift assets: archive read-only; salvage the NotebookLM-shaping skeleton (prose-citation prompt + path->GitHub-URL mapper + Sources/README-first builder) into a shared reporting lib reused by Metis/portfolio and any future narrator.
- **Rubric (unverified):** realized=none — output is human-only; zero downstream model behavior delta; ran once - latent=low-to-medium — reusable NotebookLM-shaping + citation skeleton; could be re-pointed at loop outputs (medium only if adapted to spine) - niche=partially unique — NotebookLM-prose-with-inline-citations framing is distinct from Metis portfolio_monitor.py / Clio / send_brief_email.py (status-report style); the underlying gather-from-git+pivot+state is redundant with Metis portfolio tooling - health=runnable / decorative — self-contained, soft deps all in try/except so it degrades gracefully (no agora_persist/llm_cascade still produces a skeleton); not dead-infra, but not in any loop - debt=low — clean, well-documented single file; the only shortcut is the 'daily' name with no scheduler (cosmetic/operational, not architectural). Meets its spec; misses the needle only because the needle (model behavior change) was never its target.
- **Salvage IP (lift before any retire):** Lift before any retire: (1) the path->GitHub-URL mapper _path_to_github_url() with its prefix allowlist; (2) the NotebookLM-tuned system prompt CALLIOPE_SYSTEM_PROMPT (prose-over-bullets, inline-full-URL-citation, named-agents, no-invented-activity rules) — reusable for any audio-friendly synthesis; (3) build_sources_section() with README-first dedup + Sources-section structure; (4) gather_recent_commits() git numstat+body parser (sentinel-split, per-file ins/dels) which is a generally useful commit-summarizer richer than Metis's. All four are component-agnostic and worth a shared reporting lib.
- **Suggested settle condition (to verify, not act):** Falsifiable: over the next review window, does James actually invoke Calliope and consume its NotebookLM output at least once for situational awareness? If YES (a single real on-demand use), KEEP-on-demand stands. If it goes a full review window with zero invocations AND Metis/portfolio_monitor already covers James's awareness need, then it is redundant human-reporting → RETIRE-after-HITL with assets lifted. (Note: the program's metabolization-loop 'decider' does not apply — Calliope is human-facing by design and should never be judged on a model behavior delta unless adapted to the spine-narrator option.)
- **Evidence read:** Read in full: scripts/calliope_daily.py (473 lines, the entire implementation). Read: docs/notebook_lm/calliope_daily_2026-05-19.md (the sole generated output, partial 226/358 lines incl. raw context appendix). Read: pivot/COMPONENT_THOUGHTWORK_REVIEW_2026-06-24.md (template + status model; lists Calliope as unchanged KEEP-on-demand) and the relevant row of pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md (row 35: 'KEEP on-demand, NotebookLM synthesis; cheap, James-valued'). Verified run history: git log shows only 2 commits (772fc1e8, f2b194d8) both 2026-05-19; ls of docs/notebook_lm/ confirms one calliope_daily_*.md output (others predate and are hand-made). Confirmed no scheduler wiring via grep over json/yaml/sh/ps1 (only docs/state.json roster entry, lines 571-586: lifecycle active, invoke_on_demand true, status UNKNOWN/no_data). No adverse prior verdicts found in pivot/ or roles/.
- **HITL deeper-dive decision (James - BLANK, required before any action):** ______________________

