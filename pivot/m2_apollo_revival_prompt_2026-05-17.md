# M2 Apollo Revival Prompt — paste-ready

**Purpose:** Single-block prompt for the Claude Code session James will spin up on M2 (SpectreX5) when he returns home. Per James's format preference: continuous text, no section breaks, no alternating code blocks. Self-contained — assumes fresh session with no prior context.

**Date drafted:** 2026-05-17 (target paste date: when M2 is back online).

**Companion docs the M2 session will reference (already pushed to main):**
- `pivot/apollo_value_proposition_2026-05-17.md` — why this matters now + falsification conditions
- `pivot/autopsy_apollo_2026-05-13.md` — what Apollo produced last time, why it stopped
- `apollo/RESUME.md` — Aporia's chip-2 revival paths (A/B/C)
- `apollo/ROADMAP.md` — v2.1 priorities (NSGA-III, racing, FAISS, MAP-Elites)
- `roles/EvolutionaryArchitectAndReasoningSpeciesEngineer/Apollo_Role_Document.md` — original role doc

---

## The paste-ready prompt (everything below this line is one block to paste into M2's Claude Code session)

You are running on M2 (SpectreX5). James is reviving Apollo's evolutionary computation engine here per the architecture established across the 2026-05-13 forge autopsies and the 2026-05-17 orchestration conversation. M2 has been offline; the rest of the fleet has moved on without it. M1 hosts Redis at 192.168.1.176:6379 and Postgres at 192.168.1.176:5432 with password "prometheus"; M3 is running Hephaestus's forge with dual-write heartbeats to Redis + Postgres; M4 runs the reporting pipeline that aggregates state across machines. Apollo's last successful run was April 9 (v2_d2b at gen 686) showing positive but unfinished compositional evolution; it stopped because James pivoted attention to substrate-vocabulary work. **IMPORTANT UPDATE (2026-05-17 evening): The Postgres dual-write instrumentation patch has ALREADY BEEN APPLIED to `apollo/src/apollo.py` and pushed to main. You don't need to write the patch; you just need to `git pull` to get it. The patch adds a background Postgres heartbeat thread that emits to `agora.agent_heartbeats` every 60s with the current generation, best fitness vector, ablation deltas, etc. — pre-built so you don't have to author it.** Your job is to pull the latest, verify the patch is present, install dependencies, handle credentials, and launch Apollo on Path A from the autopsy (minimal restart on current Frame H gene library; don't gate on the larger Frame-H-vs-substrate-vocabulary strategic question). Read these prerequisite docs in order before doing anything else, each takes ~5 minutes: pivot/apollo_value_proposition_2026-05-17.md to understand why Apollo matters now and what the falsification conditions are (5K gens with no coalition value or no failure orthogonality or routing collapse or persistent LLM-mutation-drift dominance kills the compositional premise; supportive evidence in any of those gives Apollo cloud GPU and scaling), apollo/RESUME.md for Aporia's three revival paths and the four open questions about why Apollo stopped (attention pivot is most likely; PID file conflict from apollo_v2.pid is fast to test; incomplete NSGA-III change is the risky case), apollo/ROADMAP.md for the v2.1 priority tiers (P0 = NSGA-III + stagnation monitoring; P1 = batch LLM + racing eval + FAISS; defer P2-P3), pivot/autopsy_apollo_2026-05-13.md for what Apollo produced at the April 9 stop (v2_d2b at gen 686 with +0.690 best accuracy margin showing real compositional progress + the LLM-mutation premise failing because drift/seed dominated elites), and roles/EvolutionaryArchitectAndReasoningSpeciesEngineer/Apollo_Role_Document.md for the canonical agent identity and the 10-principle discipline. After those reads, work through this eight-step revival checklist sequentially, reporting DONE / BLOCKED (with reason) / NOT YET at each step. Step 1, environment baseline: confirm Python 3.12 with `python --version`; confirm two GPUs are visible with `nvidia-smi`; confirm M2 can reach M1 Postgres with `python -c "import socket; s=socket.socket(); s.settimeout(3); s.connect(('192.168.1.176',5432)); print('OK')"` and report whether Redis port 6379 is reachable too (it might still be down — that's fine, Postgres is the dual-write fallback). Step 2, sync dependencies: `pip install redis psycopg2-binary` if either is missing; `pip show torch pymoo pyribs` to confirm Apollo's existing deps are still installed. Step 3, persistent env vars: run `[Environment]::SetEnvironmentVariable("AGORA_REDIS_HOST","192.168.1.176","User")`, `[Environment]::SetEnvironmentVariable("AGORA_REDIS_PASSWORD","prometheus","User")`, `[Environment]::SetEnvironmentVariable("AGORA_POSTGRES_HOST","192.168.1.176","User")`, `[Environment]::SetEnvironmentVariable("AGORA_POSTGRES_PASSWORD","prometheus","User")`, `[Environment]::SetEnvironmentVariable("PROMETHEUS_MACHINE","M2","User")`; restart PowerShell so the values inherit. Step 4, credentials: Apollo needs NVIDIA_API_KEY (for the LLM-assisted mutation operator's primary path) and DEEPSEEK_API_KEY (for the 50/50 hybrid that was outperforming pure Qwen on April 9). FIRST check whether apollo/.env or agents/eos/.env on M2 already has these from past runs — if yes, skip to step 5; if no, you need a fresh credential staging push and you should tell James he needs to push these from M1 via the Postgres temp_secrets pattern (table is agora.temp_secrets in prometheus_fire database; key pattern is "apollo_<today's-date>"; same shape the M3 Hephaestus push used). Step 5, PID-file cleanup: if apollo/apollo_v2.pid exists, check whether that PID is actually a running process; if not, delete the file (it's the Path-A blocker hypothesis 3 from the RESUME). Step 6, verify the dual-write instrumentation is present (NOT authoring it — it's already pushed): `git pull --ff-only origin main` to get the patched apollo/src/apollo.py. Then `grep -n "_start_pg_heartbeat\|APOLLO_STATUS\|agora_persist" apollo/src/apollo.py | head -10` should show the heartbeat thread setup near the top of the file plus the APOLLO_STATUS updates inside the generation loop. If the grep returns nothing, the patch didn't land — report to James and stop. If the grep shows the expected lines, the patch is in place and you can proceed. NOTE: the patch is fail-soft — Apollo runs without Postgres heartbeat if agora_persist can't be imported (e.g., scripts/ isn't on sys.path). To ensure it picks up, you need scripts/agora_persist.py to be importable, which means `pip install psycopg2-binary` and `pip install redis` (the latter for cross-pipeline compatibility, though Apollo's hot path doesn't touch Redis directly). Step 7, launch decision and execute: the RESUME's Path A is "minimal restart on current machine — validate imports, clean stale PID, launch." Recommended over Path B (port to M2 as continuous daemon — Apollo is ALREADY M2-native, this isn't needed) and Path C (finish NSGA-III before revival — defer until evidence Apollo runs cleanly under current NSGA-II). Run `python -c "from src.apollo import main; print('import ok')"` first; if that errors, debug the import before launching. Then `cd C:\Prometheus\apollo; $env:PYTHONPATH = "."; python src/apollo.py --config configs/config_v2d2b.yaml` (the 50/50 hybrid config that was outperforming on April 9; or pick another v2 config from configs/ if v2d2b isn't there). Within 30 seconds the startup log should show: GPU 1 loaded with Qwen2.5-Coder-3B (LLM mutation), GPU 2 ready for fitness eval, seed population loaded from gem files, Agora connected as Apollo@M2 OR "Postgres heartbeat thread started" (depending on Redis state). The first 50 generations are warmup (diversity-only); accuracy gate activates at gen 50, ablation gate at gen 100; main loop begins at gen 200+. Step 8, verify and report back: wait 5-10 minutes for at least one heartbeat tick to land, then from M2 run `python -c "import sys; sys.path.insert(0, 'scripts'); import agora_persist, json; row = agora_persist.read_agent('Apollo'); print(json.dumps(row, indent=2, default=str) if row else 'NOT FOUND')"`. Confirm machine=M2, status=online, recent last_heartbeat, status_json with current generation number. The M4 dashboard will pick up Apollo automatically on the next portfolio_monitor cycle since M4 reads from agora.agent_heartbeats when Redis is down. Also report any blockers the original briefing didn't anticipate: model name drift (Qwen2.5-Coder-3B may have been deprecated; check the model loading code and fall back to whatever current Qwen Code model is available), config drift (configs/config_v2d2b.yaml may reference paths that don't exist on M2 anymore — fix relative paths if needed), checkpoint format incompatibility (apollo/checkpoints/ may have older format than the current src/checkpointer.py expects — if so, start fresh rather than resuming from old checkpoint), VRAM exhaustion (if M2 GPUs are smaller than Apollo was tuned for, lower the LLM batch size in the config before reporting failure), and the strategic Frame-H-vs-substrate-vocabulary question (DO NOT gate on this for revival; James decided per the value prop doc that Path A on current Frame H is the right starting point; substrate vocabulary integration is a v2 question after 5K generations of Path A produce evidence). Reply to James with the eight-step report (DONE / BLOCKED / NOT YET per step with one-line evidence), the agora_persist.read_agent('Apollo') output (machine + last_heartbeat + key_metrics fields), and a single sentence on the first observed generation rate (gens/hour) which is the leading indicator of whether Apollo is performing at or below the April 9 baseline of ~15-20K gens/day. James will relay your reply to Aletheia on M4 who will coordinate next steps based on the heartbeat data + first-generation metrics. If Apollo runs cleanly for 24 hours and produces measurable progress past gen 686 (last April 9 stop), the revival is structurally complete and the value-proposition doc's falsification conditions become the next thing to watch — specifically the four supportive criteria (coalition value, failure orthogonality, routing diversity, ablation gate enforcement) which Apollo needs to start demonstrating within its first 5K post-revival generations. If those don't materialize, archive cleanly per the value-prop doc rather than tuning indefinitely; the compositional premise being wrong is a real result.

---

## When to paste this prompt

The prompt above is intended for when M2 is back online AND James has time to drive the M2 session. Typical sequence:
1. James turns M2 on; verifies network reachability
2. James opens a Claude Code session on M2
3. James pastes the prompt above
4. The session walks through the 8 steps autonomously
5. The session reports back to James
6. James relays to Aletheia on M4 for follow-up

## What changes if Redis is back when M2 comes up

The prompt above is robust to either Redis state. If Redis is up, Apollo connects to both Redis AND Postgres (dual-write). If Redis is still down, Apollo connects only to Postgres (still dual-write target, just with one side unavailable temporarily). Either way the M4 dashboard sees Apollo as ALIVE.

## What James needs to decide before pasting

One thing: the credential push. If apollo/.env on M2 already has NVIDIA_API_KEY + DEEPSEEK_API_KEY from past runs (likely, since Apollo was running on M2 April 9), skip credential staging. If not, James needs to either push fresh creds via the agora.temp_secrets pattern (same shape the M3 Heph push used) or set them directly on M2 from his local clipboard.

## What this prompt deliberately doesn't do

- Doesn't gate on the Frame-H-vs-substrate-vocabulary strategic question. That's settled by 5K generations of Path A data, not by pre-discussion.
- Doesn't try to finish v2.1 P0 (NSGA-III) before launching. Defer until Apollo runs cleanly on NSGA-II.
- Doesn't push for cloud GPU rental. That decision waits on the 5K-generation evidence.
- Doesn't try to integrate Coeus or Nemesis. Those are dormant and orthogonal to Apollo's compositional test.

The discipline is: revive narrowly, gather evidence on the falsification conditions, decide whether to scale based on the evidence. Don't over-build the revival.
