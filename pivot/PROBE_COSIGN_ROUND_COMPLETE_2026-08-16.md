# The Metabolization Probe is preregistered and binding — round record

**Date:** 2026-08-16 · **Recorded by:** Hephaestus (Fable seat, M3 — dispatch coordinator,
declared-conflicted residue supplier, non-signing) · **Status:** the preregistration
`pivot/PREREG_METABOLIZATION_PROBE_v1.md` carries **three signatures** and is BINDING.
No arm — pilot included — executes until the §5 condition ledger clears.

**Why this file exists:** the fleet's own §1.6 finding is that a stale index breaks a working
channel. This is the one place a new session can read what happened between 08-12 and 08-16
without reconstructing it from twenty commits.

---

## 1. What was built, in five days, from a standing start

| | |
|---|---|
| 08-12 | Program-wide reassessment; James's phase ruling (heredity frame; probe = Priority #1) |
| 08-12 | Spec v1.0 → Aporia audit → v1.1 → re-check → v1.2 → **frozen** |
| 08-13 | James's peer review → **spec v2.0-FINAL** (causal cutoff; F-answer/F-oracle split; D0–D3 transfer ladder; bounded nulls; F-generic; decomposition arms; statistical plan; diagnostic matrix) |
| 08-13 | Ergon's preregistration drafted (measured before designing; power computed, not asserted) |
| 08-13 | Hephaestus supplier review — SIGN-WORTHY after one material finding |
| 08-13→15 | Ergon: API preflight, $100 cap → $0 lane, soak 449/450 @30RPM, diurnal 354/354, analysis path 34/34 |
| 08-15 | **Ergon adjudication** — all three review items adopted and strengthened |
| 08-16 | **Apollo** wall corpus · **Techne** assembler · **Charon** F-generic/co-sign/F-null · **Harmonia B** third signature + R3 battery |

Serial dispatch: one prompt per role, verified on origin between each. Prompts committed at
`roles/Ergon/KICKOFF_PROMPT_metabolization_probe.md`,
`pivot/KICKOFF_PROMPTS_prereg_cosign_round_2026-08-13.md`,
`pivot/KICKOFF_PROMPTS_probe_suppliers_2026-08-14.md`.

## 2. The adversarial chain — every seat found what the others missed

This is the round's real product. Each finding below was invisible from where the previous
seat stood, and every one made the experiment harder to pass, not easier.

- **Hephaestus (review, 08-13).** D0 packets leak or invert binary answers: a correct prior
  verdict is a free answer, a known-failed verdict on a True/False task is a disclosed
  negation. *Raised by the conflicted supplier against his own residue's scoring.*
- **Ergon (adjudication, 08-15).** Adopted and went past it: traces restate conclusions
  mid-stream, so **every** verdict token is stripped, using the extractor's own frozen regex —
  redactor and scorer cannot drift. Over-stripping accepted on the record (that error
  direction can only shrink Δ). Also caught what neither review nor spec saw: an assembler
  free to choose between two cold reps could select on trace length, a selection effect on
  the measured axis — hence rep-1-only, enforced mechanically.
- **Apollo (corpus, 08-16).** Three wall specs killed by their own telemetry: single-move
  withholding produces no wall while crossover is on (independent ablation-side replication of
  the June crossover result); the `routing_purity` guard is inert under dispatch mode
  (decorative, found by ablation); and **plateau telemetry cannot separate "capability absent"
  from "capability present but mis-wired"** — the program's representational-vs-interface
  disease reappearing as a measurement problem inside Apollo, and a pre-registered risk for
  W1's detector.
- **Techne (assembler, 08-16).** Residue censused rather than sampled: forge ledger 6,276
  scraps → 3,415 eligible → 3,315 carrying obstruction classes. Filed the discrepancy that
  `signature_index` has no REJECTED class instead of silently reinterpreting the prereg.
- **Charon (kill authority, 08-16).** Two material findings visible only from the packets:
  **D3's obstruction class is a renaming of the generator's `claim_kind`** — 80.3% of records
  labelled "asserted-equality-without-executing-computation" carry both executed operands, so
  the label documents a refuted generator proposal, not a solver failing to compute; and
  **every D3 task was getting the same packet** (target-independent, alphabetical selection) —
  a constant packet measures topic-priming, not retrieval. Also rejected F-prom-whole at N=60
  (its cost premise had been measured away; it is the arm separating matrix rows 2 and 3) and
  declared a **twelfth marginal — verdict polarity** — after finding the D3 pool runs 26:1
  negative against a 50/50 task set.
- **Harmonia B (third signature, 08-16).** Re-derived §6.3's partition exhaustively — complete
  and disjoint post-amendment, **genuinely gapped before**. Put the ±5%-matching bias on record
  (digit-wise counting over-counts numeral-dense residue, so F-generic gets more real tokens —
  biasing specificity margin *against* carry). Added **BC-8**: matrix row *selection* gets the
  same underpowered escape the verdict classes have. Calibrated every control two-sided, which
  killed its own first cheat rule (20% clean-world false alarm).

**The pattern worth keeping:** six seats, six findings, zero of them reachable by reading
alone — each required executing something the previous seat had not. That is the operational
form of the fleet's own rule that agreement without independent execution is one measurement
with N pointers.

## 3. State of the gates

**BINDING** — prereg signed by Ergon (driver), Charon (kill authority), Harmonia B (meter
integrity); Hephaestus supplier-only, conflict declared, non-signing.

**Conditions before arms (the §5 ledger):**
- **BC-1** — F-prom-whole N: raise to ≥150, or label the decomposition EXPLORATORY-ONLY and
  bar it from routing the diagnostic matrix. *(Charon; Ergon's remedy to choose.)*
- **BC-2** — D3 selection must be per-task, not constant; D3 relabelled, "same latent
  obstruction" barred from its verdict; D3 reported by source. *(Charon.)*
- **BC-8** — `UNROUTED-UNDERPOWERED` when the whole-arm CI cannot separate matrix rows 2/3.
  *(Harmonia B.)*
- Plus Charon's reporting conditions (strict-screened second read of the primary endpoint;
  signature_index restricted to KILL classes) and R3's four controls demonstrated live.

**Built and green:** analysis path (`ergon/probe/`), packet assembler with R14 armed, F-generic
(clean-room attested), F-null (R7 both layers pass on D3: family-wise 0.150 vs 0.315 calibrated;
classifier 0.512 vs 0.55 ceiling), R3 control battery, Apollo's 28-record wall corpus with
validator-enforced cause-not-fix. **Full suite 129/129.** Solver lane $0, verified under load
and across 30 diurnal hours. `valid=None` landed — the fossil that started the August
reassessment is fixed in code.

**Not yet done, and honestly scoped:** R7 covers D3 only — probe_prepass does not exist yet, so
R7 must be re-run for D0/D1/D2 when their residue exists. The pilot's permitted verdicts are
`PIPELINE_ADMISSIBLE` / `NOT_ADMISSIBLE` plus a directional estimate; at N=120 the MDE is
~12–13pp, so a flat pilot is `INCONCLUSIVE-UNDERPOWERED` by construction and can never route a
diagnostic-matrix row.

## 4. What happens next

One Ergon session: clear BC-1/BC-2/BC-8 → run the pre-pass (creates D0/D1 residue) → R7 re-run
for D0/D1/D2 → R3 controls live → the pilot. Twenty minutes, $0. Then Tier A exit, then Tier B —
the number.

The question all of it exists to answer, unchanged since James's ruling: **when we hand the
system its own dead, does it learn?** — measured as F-prom-retrieved versus F-null, over a
transfer radius D0→D3, with every path by which a thin number could become doctrine now closed
by someone who went looking for it.

---
*Round record. The prereg is the binding instrument; this file is the index. — Hephaestus, M3.*
