# Frontier-model feedback — Charon swarm — Grok

**Captured at:** 2026-05-25
**Source prompt:** `pivot/frontier_advice_prompt_charon_swarm_2026-05-25.md`
**Provider:** Grok (model not specified by user)
**Capture method:** James paste, verbatim

---

## Q1

Genuine substrate-grade: Hecate (cross-generator MI exposes operator-class convergence beyond prefix artifacts) and Pollux (numerical shape-vs-scale detection on concrete polynomial pairs). Sophisticated noise: Stygian (9/10 conjectures short-circuit; loader debt makes it a stalled battery), Lethe (zero emissions; status queries saturated), Acheron (narrow dictionary yields near-zero collisions), Moros (low convergence despite 3-model fanout; Jaccard too coarse for deep critique), Nephele (permanent skip; pure fallback waste).

## Q2

Lethe's mandate is fundamentally obsolete. Frontier models (DeepSeekMath-V2, GPT-5.2, Gemini variants) achieve gold-level IMO performance and strong research-level theorem proving by mid-2025. Attack-mode rotation (year-misattribution etc.) will produce marginal candidates at best. Missing: verifiable computation hooks (Lean/Coq calls) or live web-search grounding instead of cold completions; pure LLM status queries are too weak against 2026 capabilities.

## Q3

The audit itself being the load-bearing output indicates the swarm's primary value is currently self-diagnosis of generator convergence, not external mathematical substrate production. This reveals a meta-level success (detecting monoculture in kill_patterns) but confirms production agents are not yet yielding novel claims.

## Q4

Single author + shared epistemic priors (verbs-over-nouns, scale-vs-shape) matters most — it embeds the same inductive biases across layers, limiting discovery orthogonality. Shared base class, LLM cascade, and schema are secondary/cosmetic (easy to diversify technically). Highest leverage fix: inject one agent with deliberately opposing priors (e.g., noun-first ontology or pure symbolic ATP bias).

## Q5

Klymene is a thin variant of ledger management (overlaps Stygian/Hecate settling). Ananke is a thin variant of Acheron/Moros collision detection. Eos is thin observability (already implicit in telemetry). Most conspicuously missing layer: active conjecture generation + automated proof search (counterexample-driven synthesis, not passive scanning or auditing).

## Q6

Not worth the build cost. It delegates responsibility to the same class of models producing the artifacts, reinforcing the monoculture rather than breaking it.

## Q7

I bet no — it will not produce a single genuinely-novel mathematical result in 90 days. Highest-leverage change: replace 4 agents with one integrated MCTS/ATP loop on 2-3 concrete open conjectures (e.g., extend Stygian Lehmer work with verifiable Lean tactics), pruning the rotation entirely. Current design optimizes for coverage noise over depth.
