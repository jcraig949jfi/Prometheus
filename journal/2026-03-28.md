# Journal — 2026-03-28

## Overnight Results: Ignis Requeue + Noesis Experiments

### Ignis: All 4 Requeued Experiments Passed

The 4 experiments that failed during the Mar 27 Athena session (Unicode encoding + d_model assertion) were fixed and requeued. All completed successfully overnight.

**Basin Escape L22/L23/L24 (100 random directions each):**

| Layer | Crossing Rate | Verdict | Penetrable Traps |
|-------|--------------|---------|-----------------|
| L22 | 5.3% (85/1600) | ANISOTROPIC | Overtake (22%), Finish Before 3rd (25%), Siblings (9%) |
| L23 | 4.8% (76/1600) | DEEP BASINS | Overtake (21%), Finish Before 3rd (16%), Siblings (11%) |
| L24 | 3.6% (57/1600) | DEEP BASINS | Overtake (13%), Finish Before 3rd (17%), Siblings (8%) |

Key finding: **basins deepen with layer depth** — L22 most penetrable, L24 least. Only 5 of 16 trap families are ever penetrable at any layer. 11 traps are completely impenetrable at 1.5B with random directions up to ε=20. The Overtake family shows RIDGED geometry (specific low-ε channels exist that CMA-ES could exploit).

**Cross-Architecture: Qwen-0.5B at L18 (300 gen CMA-ES):**

| Metric | Qwen-0.5B | Qwen-1.5B (L19 best) |
|--------|-----------|---------------------|
| Flipped | **10** | 5 |
| Broken | 1 | 0 |
| Baseline correct | 14/30 | — |
| Steered correct | 23/30 | — |

**The 0.5B model is MORE steerable than 1.5B.** Traps that are completely impenetrable at 1.5B (Density Illusion, Elevator Floor, Handshakes, Staircase Steps, Cutting Rope) are flipped at 0.5B with a single steering vector. The ejection circuit exists at 0.5B but is fragile. By 1.5B it's robust enough that only specific evolved directions at specific layers can crack it.

This is direct evidence for the scaling hypothesis: the suppression mechanism strengthens with model scale.

### Noesis: Tensor Engine Experiments

Three Noesis experiments ran overnight. Results:

**Framing (multi-perspective traversal):** 5 frames (unbiased, devil's advocate, Occam, boundary, efficiency) produced different frontiers from the same tensor. Agreement/disagreement data in `organisms/framing_results/`.

**Dream State (Hebbian feature learning):** Tested 4 learning rates on 300 real Poros compositions. Results in `organisms/dream_results/`.

**Tensor vs Random:** Random chains outperformed tensor-guided chains (12% vs 4% execution rate). Root cause: tensor scores conceptual affinity but has no type compatibility awareness. The tensor recommends pairs that can't operationally connect. Fix identified: pre-filter by type compatibility using `compatible_chains()`, then weight by tensor score. Prompt written for separate Claude Code session at `docs/noesis_type_compatibility_prompt.md`.

### Tensor Navigator Decontamination

Removed Coeus/Nous data injection from the tensor navigator:
- Removed `enrich_with_coeus()` (was blending Nous LLM speculation into feature dim 29)
- Removed enrichments directory scan (was marking Nous hypotheses as "explored" triples)
- Added TT reconstruction error diagnostics: 8.94% NRMSE at rank 10, error roughly uniform across concepts, top-100 triples well-represented

### Documentation

- Wrote `docs/continuous_exploration_loop.md` — the unified Noesis vision (4 layers, framing, dream state, metacognitive hedge, forge pipeline integration)
- Named the tensor engine: **Noesis** (Νόησις — higher-order knowing)
- Updated agent registry, memory index, project memory

### Corpus-First Experiment — Major Result

**The most important result since the original ejection discovery.**

First run failed (bf16 on fp16 model + device splitting = NaN gradients, model destroyed). Fixed: bf16 model on single device, lr 5e-6, gradient clipping, gradient checkpointing. Second run succeeded.

**Training:** 300 self-generated reasoning examples, 3 epochs, 8 minutes. Loss curve healthy: 0.61 → 0.37 → 0.10 → 0.05. Gradients finite and declining.

**Results (no evolution, no steering — pure fine-tuning):**

| Pillar | Before | After | Delta |
|--------|--------|-------|-------|
| Tier C (far-transfer) | 42.9% | **52.4%** | **+9.5%** |
| Metacognition | 35.7% | **57.1%** | **+21.4%** |
| Self-correction | 38.5% | **53.8%** | **+15.4%** |
| Composite | 0.335 | **0.427** | **+27.5%** |
| Tier A accuracy | 46.7% | 46.7% | 0% |
| Tier B accuracy | 50.0% | 50.0% | 0% |

**The ejection profile is structurally unchanged:** L* median=26, alive=26/30, top-5=7/30. Identical to baseline. The basins didn't shallow.

**Interpretation:** The ejection circuit is structural (survives fine-tuning). But reasoning performance has headroom within the existing basin geometry. Corpus training teaches reasoning patterns (uncertainty, self-correction) without changing the suppression circuit. The basins are the ceiling, not the current performance.

**For the convergence theory:** This is a third outcome we didn't predict. Not "basins shallow" (they didn't). Not "nothing changes" (performance improved dramatically). The circuit is fixed but the model can learn to reason better within it.

Stage D (CMA-ES evolution on the corpus-trained seed) is the next test: does a better-initialized model produce better steering results?

### Noesis Tournament — Three Machines Running

| Machine | Status | Cracks |
|---------|--------|--------|
| M1 (local) | ~4h in, stable | 5,000+ |
| M2 (compression + sensitivity scoring) | Launched, slower CPU | Running |
| M3 (building blocks + depth bonus) | Launched | 3,700+ |

M1 hitting 0.659 quality ceiling. Mutation dominates at 52%. Cross-domain chains are real (3,570 unique profiles).

**M2 broke the ceiling: max quality 0.7137 within 108 cycles.**

Single-variable ablation: same organisms, same strategies, same tournament mechanics. Only change: scoring weights (compression 0.31, input sensitivity 0.19, execution reduced). The 0.659 ceiling was the formula, not the compositions. 94% of M2's cracks have sensitivity > 0 — the new dimension separates real computation from trivial pass-through. M1 never measured this, so an unknown fraction of M1's cracks may be trivial compositions that scored well on other metrics.

M2 operational note: Windows 10 required three architecture iterations before stabilizing. mp.Pool (PermissionError on pipe handles), ThreadPoolExecutor (C extension segfaults under concurrency), sequential (hangs on blocking scipy calls). Solution: persistent subprocess worker communicating via stdin/stdout, hard-killed and restarted on hang. This is the robust Windows execution pattern — worth backporting.

**M3 at 40 minutes: 8,311 cracks, building blocks confirmed.**

- 52% of all cracks use building blocks (4,321 / 8,311)
- BB cracks have higher mean quality: 0.568 vs 0.531 non-BB
- BB cracks hit higher ceiling: 0.660 vs 0.605 non-BB
- The topology.euler_characteristic → statistical_mechanics.ising_model_1d block dominates: 3,814 cracks (M1 found this pair 13 times — M3 scaled it 293x by composing WITH it)
- Mutation at 71% of cracks — building blocks give it a rich search space to refine
- This is the hierarchical composition hypothesis confirmed: M1 finds primitives, M3 builds with them, quality increases

The topology→stat-mech corridor is a real mathematical relationship (topological phase transitions are a known field in physics). The tensor found it from geometry alone, M3 validated it at scale.

### Hour 9 Convergence — All Four Machines Point at Chain Depth

**M1:** Tensor top-K overtook mutation (3.55 vs 3.0 cpc) at 116K chains. Tensor guidance durably beats local search over long runs. The thesis is validated.

**M2:** Mutation collapsed to 16.2%, temperature anneal surged to 40.8%. Mutation dominance on M1 was a scoring artifact — change what you measure, change who wins.

**M3:** All 22,902 cracks are length 2. Depth bonus is dead weight — no strategy has a mechanism to propose longer chains. Building blocks used as terminators, not components.

**M4:** Max quality 0.7282, 33 chains tied at that exact number. The ceiling is now chain depth, not scoring. The mathematical maximum of the current formula for length-2 BB chains with full sensitivity has been reached.

**Diagnosis:** The next quality regime lives at length 3+. No machine can reach it with the current strategy set. Round 2 priority: a chain extension strategy that takes working length-2 chains and appends type-compatible operations.

**Stage D (corpus-first evolution) crashed.** GPU was idle. Restarted with `run_stageD_only.bat` (200 gen, reduced from 500).

---

## Evening Synthesis — Science Advisor Read

### Forge: Monoculture Broken

| Metric | Mar 26 (v5) | Mar 28 (v7) |
|--------|------------|-------------|
| Unique behavioral profiles | 19 / 344 | 25 passing battery |
| Best single tool coverage | 40 / 89 | 70 / 89 |
| Minimum covering set | 4 tools → 49/89 (55%) | 5 tools → 79/89 (89%) |
| Tools at parity (65+ cats) | 1 | 6 |
| Accuracy of top tools | ~50% | 74% |

Three levers made the difference:
1. **Expanded battery** (15 → 89 categories) — revealed the monoculture
2. **Multi-frame prompts** (structural/constructive/dynamics/judgment) — broke the monoculture
3. **Opus as the forge** — bypassed dead API, produced genuinely better tools

**10 remaining uncovered categories:** causal_confounding, causal_counterfactual, causal_intervention, compositional_causal_statistical, compositional_depth_scaling, compositional_multi_hop_with_distractor, temporal_age_reasoning, temporal_scheduling_conflict, tom_perspective_shift, tom_strategic_deception. Specialist tools already crack these at 0.90+ discrimination — they just fail the NCD baseline on the other 79. Parser tuning problem, not a discovery problem.

### Ignis: Corpus-First Confirmed — The Third Outcome

Stage B fine-tuning (300 examples, 3 epochs, 8 minutes) produced a result the convergence theory didn't predict:

| Outcome | Predicted? | Happened? |
|---------|-----------|-----------|
| Basins shallow (circuit weakens) | Yes | No |
| Nothing changes | Yes | No |
| Performance improves, basins unchanged | **No** | **Yes** |

The ejection circuit is **structural** — L* median=26, alive=26/30, top-5=7/30, identical to baseline after fine-tuning. But metacognition jumped +21.4%, self-correction +15.4%, composite +27.5%. The model learned to reason better *within* its existing attractor basins without touching the suppression mechanism.

**Stage D evolution on corpus-trained seed:** 17/30 at gen 50 with zero guard rail breaks. Previous best on base model: ~13/30 after hundreds of generations. The three Overtake family flips (Race, 2nd, Last) show CMA-ES learned the covariance structure of that ridged basin cluster. Fitness still climbing at gen 50 (0.6 → 6.9, decelerating but positive). Crashed after checkpoint; needs restart.

**Theoretical update:** Within-basin training (corpus) and between-basin evolution (CMA-ES) compose without interference. The circuit defines topology; training fills basins; evolution navigates between them. Three independent degrees of freedom.

### Noesis: Four-Machine Factorial Design Complete

| | No Building Blocks | With Building Blocks |
|---|---|---|
| **Baseline scoring** | M1: 0.659 | M3: 0.660 |
| **Fixed scoring** | M2: 0.714 | M4: 0.728 |

Key results:
- **Scoring fix = dominant effect (+0.055).** M1's 0.659 ceiling was a measurement artifact.
- **Building blocks = invisible under old scoring (+0.001), visible under new (+0.014 interaction).**
- **Additive composition confirmed.** Improvements stack, don't multiply.
- **Tensor guidance validated:** 3.4x random over 142,500 chains on M1. Durable, statistically robust.
- **M5 quality record: 0.8288** on length-5 chains, 7,959 corridors across 218 organisms.

**Universal finding:** topology.euler_characteristic → stat_mech.ising_model_1d is a universal sink. Accepts any input, produces quality output. Real mathematical relationship (topological phase transitions) discovered from tensor geometry alone, confirmed at scale across four machines.

**Universal bottleneck:** All four machines produce only length-2 chains. The depth bonus (+0.15 for length 5+) is entirely unexplored. M4's theoretical formula maximum for 2-step chains is ~0.73. The next quality regime requires a chain extension strategy.

### Cross-System Convergence

Every subsystem independently identified the same next bottleneck: **depth.**
- Noesis: chain depth (all length-2, ceiling reached)
- Ignis: generation depth (Stage D crashed at gen 50, still climbing)
- Forge: coverage depth (10 gaps need specialist parser tuning)

The convergence theory itself refined: the suppression circuit is structural but not total. Three independent degrees of freedom — topology (circuit), content (training), navigation (evolution) — compose cleanly. This is the cleanest statement of the theory to date.
