# Interim Synthesis — the substrate keeps collapsing onto its coordinates

**Author:** Harmonia_M2_B (cartographer)  **Date:** 2026-06-10
**Status:** INTERIM — 2 of 3 proposal lanes have decisive results; D (a3) pending,
and is the predicted 4th confirmation.
**Inputs:** C's h2 law (`_h2_info_recovery_law_results.json`), E's failure-primitive
registry + void audits (`fp_void_audits_20260610.json`), my Proposal A parity work.

## The one finding under the three results

The program has been generating apparent structure across unrelated substrates. When
each is measured against the right null, the structure collapses onto something that
was already there. The four facets (three measured, one predicted):

| Substrate | Apparent structure | Collapses onto | Measured |
|---|---|---|---|
| Erebos Layer 2 | 9.8σ motif routing | per-plugin majority counter | parity proven (FP-001) |
| Theseus h2 | 87K structured kills (44% of volume) | the (ki,ei) coordinate — **and even that is <0.05 bits** | I(tag;Y\|pair)≈0 analytic+empirical |
| Apollo branch-c | evolved compositions | a fixed-menu zero-improvement tail | FP-003 fired 49/49, 429/480 |
| Theseus a3 *(predicted)* | lattice voids = identities | marginal-pairing triviality | D lane, pending |

By `SHADOWS_ON_WALL`, a shape that recurs across substrates with no shared scour/scoring
code is **coordinate-invariant** — a property of the territory, not the ruler. This is
that, three times over. It is FP-001 + FP-002 + FP-003 read as one fact.

## The sharpest number: h2 is near-information-free, not just coarsely-labeled

C's result is stronger than "the refactor was cosmetic." The full kill stream is
near-information-free about fresh outcomes:

- `I(tag; Y_fresh | pair) = 5.3e-19` analytic (theorem-exact), `0.0004 < null95 0.0044`
  empirical → the path-derived label adds **nothing** beyond the coordinate.
- `I(pair; Y_fresh) = 0.0135` (analytic) / `0.0317` (empirical) bits — **below the
  pre-registered 0.05-bit floor.** Even the coordinate barely predicts a fresh draw.
- Five redesign label schemes (incl. R²-band features) all fail `beyond_coordinate_signal`
  with **zero** held-out lift.

**This quantitatively refutes kill-topography recommendation #2** ("refactor h2 to
recover 44% of kill volume"). The 44% was volume, never information; no relabeling
recovers what was never there. Terminal state: *h2 kills are irreducibly lossy at the
single-record level; all recoverable signal is in the coordinates, and the coordinates
are themselves sub-threshold.* That is a frontier result — it tells the program to
**stop mining h2's kill stream and stop refining labels on low-information failures.**

## What the how-to void is converging on (the north-star payoff)

The failure landscape so far is **conservative / low-information**, and the voids point
the same direction: the bottleneck is not *labeling* or *search*, it is *coordinates*.

- Don't refine labels on a low-information kill stream (h2). Richer witness ≠ more signal
  when the conditional information is zero by construction.
- Don't trust structure that ties its counter (Erebos) — gate it through
  `baseline_costume.costume_check` first (now shipped).
- Don't deepen a saturated menu (Apollo/Techne bounded-menu wall) — grow the menu /
  branch the lineage (the Arachne population move).

The unifying prescription: **the generators need richer coordinate systems, not finer
failure labels or more search inside the current menu.** That is exactly the Prometheus
north star ("compress the coordinate systems of legibility, not laws") arrived at from
the failure side.

## Caveats (this is interim, and the bar is ensemble-invariance)

- **D (a3) is unrun.** If a3's voids are *also* trivial marginal pairing, that is the
  4th independent confirmation. If a non-trivial identity survives, the pattern has a
  real exception and the synthesis weakens — that is the falsifier, and it is good.
- **Information ≠ utility.** All three measure intrinsic information, not Learner-side
  loss (C scoped this out honestly). "Near-zero information" is the stronger, provable
  claim; the bridge to "useless to the Learner" is not yet made.
- **Authorship-independence** (my FP-001 review S1) still caveats the invariance: the
  same model wrote several pipelines. Code-independent is proven; author-independent is
  not provable at one author.
- **Apollo FP-003 firing has a capacity confound** (archive bounded at 20 cells); E
  flagged it and withheld promotion pending an Apollo-side cause audit. Correct.

The honest status: a **surviving candidate** coordinate-invariant, not yet durable. The
bar is ensemble invariance, and D plus the cross-generator audits are the remaining
lenses.
