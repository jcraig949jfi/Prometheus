# Proposal B Results — a3 Lattice Void Mining

**Author:** Harmonia_M2_D
**Date:** 2026-06-10
**Status:** COMPLETE — adversarially audited (7-agent panel, 3 SURVIVE / 3 WEAKENED / 1 REFUTED, all hits accepted & repaired)
**Instrument:** `D:\Prometheus\harmonia\primitives\lattice_void_miner.py` (promoted primitive)
**Validator:** `D:\Prometheus\harmonia\experiments\test_lattice_void_miner.py` (16/16 PASS, incl. factored==direct exact-count regression and certificate-sabotage test)
**Data artifacts:**
- `D:\Prometheus\harmonia\experiments\a3_lattice_sweep_results.json` (3,456 cells, exact)
- `D:\Prometheus\harmonia\experiments\a3_candidate_identities.jsonl` (250 void rows)
- `D:\Prometheus\harmonia\experiments\a3_void_extension_stress.json` (closure-stress, v2)
- `D:\Prometheus\harmonia\experiments\diagonal_void_results.json` (same-object extension)
**Results validator:** `D:\Prometheus\harmonia\experiments\validate_b_results.py`

---

## §0 Headline

**The "voids are the math" thesis is KILLED for a3 — constructively, not by threshold.** Every one of the 250 exact voids in the full lattice carries a machine-verified set-level certificate proving its content is marginal (single-catalog value-set facts), never knot↔EC structure. This was the predicted outcome (Proposal B §4 named it a win condition) and it is now a theorem about the instrument, not an empirical tendency.

**But the engine is alive.** Pointed at the same-object diagonal lattice — where the product-measure argument deliberately fails — it immediately surfaced one literature-adjacent arithmetic law (`torsion | tamagawa_product`, §6) with maximal joint signal. The instrument distinguishes dead from live claim-shapes; that is what it is for.

---

## §1 ROUND 0 — the reconciliation (three denominator errors in one lineage)

| # | Wrong number | Where | What it actually was |
|---|---|---|---|
| 1 | "~324 possible cells" | `D:\Prometheus\pivot\kill_topography_findings_2026-05-29.md:45` | 144. The report assumed ~9 operators; a3 has 6 (operator names contain underscores — `sq_mod_100`, `log2_floor` — and naive underscore tokenization of `a3_func_id_{f}_{g}_{rel}_violated` reconstructs ~9 phantom operators; the panel's forensic lens reproduced both wrong numbers exactly: 9·9·4=324, 143/324=44.1%) |
| 2 | "~44% lattice coverage" | same report | **100% of the violable lattice.** Kill patterns emit only on violation; `(mod_3, mod_3, abs_diff_le_3)` is unviolable for any integers (both ranges ⊂ {0,1,2}, max diff 2 ≤ 3); so exactly 143 of 144 patterns are emittable — and 143 were observed |
| 3 | "20,736 cells" | `D:\Prometheus\harmonia\proposals\2026-06-09\B_a3_lattice_void_mining.md` (and downstream directives) | 3,456. 6·6·6·4·4 = 3,456; the 20,736 was a ×6 arithmetic slip |

**Operator-set history (corrected per panel):** two commits touch `D:\Prometheus\theseus\generators\a3_functional_identity.py` (`1e7cfb63` creation, `a8a276a4` predicate_kind annotation only); the `OPERATORS` dict is byte-identical across both. The kill corpus was generated by exactly the current lattice. *(My pre-registered claim said "single commit" — factually wrong, substance unaffected; logged as a precision violation.)*

**Open precondition (BLOCKING for any tier promotion of the coverage claim):** the "143 observed" count is single-sourced to the same document whose other two numbers were fabricated by a broken tokenizer. The raw kill corpus is absent from this host (`theseus/corpus/` empty; raw scan and topography script absent from git). A Postgres recount — confirm the unique absent pattern string is `a3_func_id_mod_3_mod_3_abs_diff_le_3_violated` — is queued; Postgres at 192.168.1.176 was unreachable this session.

**Failure-primitive nomination (for Harmonia E's atlas):** *denominator drift* — lattice-cardinality arithmetic asserted without a validator, three independent instances in one artifact lineage. Detector: recompute every claimed cardinality from code constants at audit time.

---

## §2 The product-measure theorem (the decision procedure)

a3's `next()` samples knot and EC **independently**, and the exhaustive sweep evaluates the **full cross-product**, so the joint distribution of `(f(ki(k)), g(ei(e)))` is the product of its marginals. Consequences, panel-verified (~140k adversarial fuzz trials, 3 fully independent reconstructions of the void set, 0 mismatches):

1. **The marginal-pairing null is DEGENERATE** — identically equal to the catalog statistic, not just equal in expectation. Proposal B §3.2's central null carries zero discriminating power for this claim shape. (Harmonia B's interim synthesis pre-registered a3 as "the predicted 4th confirmation: voids collapse onto marginal-pairing triviality" — as stated this is true *by construction* and therefore unfalsifiable; the falsifiable refinement is the certificate taxonomy below.)
2. **`hold_rate == 1.0` ⟺ a set-level certificate exists**, complete for the four relations: `equal` ⟺ both sides the same singleton; `equal_mod_2` ⟺ both sides parity-constant, same parity; `divides` ⟺ (0∈A ⟹ B={0}) and every nonzero a | gcd(B); `abs_diff_le_K` ⟺ max(A)−min(B) ≤ K and max(B)−min(A) ≤ K.
3. **Therefore the only possible content of a cross-catalog void is single-catalog marginal facts.** Any "cross-domain identity" reading is a Pattern-30 red flag by construction.

**Blast radius (panel finding, beyond this proposal's scope):** the same theorem wholesale-kills **a2** (Pearson r over independent draws ⟹ population r ≡ 0; every a2 SHADOW_CATALOG survivor is a guaranteed false positive at the test's α) and **a4** (population R² ≡ 0; plus an `ss_tot==0 ⟹ R²=1.0` bug that emits constant-column costumes as "strong fits"). These are substrate-design findings for Theseus, filed here.

---

## §3 ROUND 1–2 — the exhaustive sweep and null cascade

Full lattice, **exact** evaluation over 52 knots × 1000 ECs (factored-histogram evaluation; no sampling anywhere; resolves §5 Q3 of the proposal):

```
cells                3,456
exact voids            250
  T1_PIGEONHOLE         24   (= the single unviolable (f,g,rel) × its 24 (ki,ei) slots)
  T2_CONSTANT_SIDE     173   (dominated by nf_class_number ≡ 1 on its 8 defined knots: "1 divides everything")
  T3_MARGINAL_FACT      53   (ALL are abs_diff_le_3 interval voids on operator-compressed ranges)
near-voids  ≥0.999       0
            ≥0.99        7
            ≥0.95       51
projection cross-check vs the 2026-05-30 sampled pass: 144/144 cells, 0 beyond 4 s.e.
```

**Violation anatomy of the ≥0.99 near-voids (failure shapes, not verdicts):** every violator set is an exact arithmetic stratum — the 100 rank-2 curves (with conductor-band structure: hi/lo violation ratio ≈ 1.7, z≈−2.6 — the known rank–conductor correlation surfacing **and** partly catalog assembly: the EC catalog is quota-stratified, rank counts exactly {0:500, 1:400, 2:100}, in-catalog rank-2 conductor floor 664 vs true minimum 389); the 3 Mazur-tail torsion-{8,10} curves (870.h4, 3885.c5, 7440.j2); the 53 curves with 3|torsion. My pre-registered "nothing arithmetically structured" was an overclaim — the structure is stratified selection + known correlations, and it is *visible*, not absent.

**Known structural blind spots (panel):** with 52 knots × 1000 ECs, a knot-driven violation costs ≥ 1/52 ≈ 0.019 of hold-rate, so the ≥0.99 band cannot contain knot-row-driven near-theorems (verified currently unoccupied in [0.95, 0.99) as well). The two genuine theorems sitting in the marginals — **knot determinant is always odd** (verified 52/52) and **Mazur torsion** — are respectively *structurally inexpressible* as voids (no parity-constant EC partner exists) and expressible only as anonymous interval facts. **Void-mining a product lattice is marginal-fact mining through a keyhole; if you want the math in the marginals, mine the marginals directly.**

---

## §4 ROUND FINAL — closure-stress (the Lean leg, honestly substituted)

Lean/mathlib4 is **not executable on this host** (no `lean`/`lake`; `external_deps/mathlib_repl` is an unbuilt shell) and would be vacuous anyway (certificates are finite-domain statements already machine-verified; det-odd and Mazur are not in mathlib). Substitute: **closure-based falsification by extension** — per-invariant true-population closures (unbounded, or bounded by a *named theorem*) propagated symbolically through each operator, relation re-checked on the closure product:

```
SELECTION_ARTIFACT   209/250   breaks under honest catalog extension
                               (rank cells break at known rank-4 curves; the minimal
                               rank-3 curve 5077a1 sits INSIDE the catalog's own
                               conductor window [39, 9990] but outside the catalog)
THEOREM_BACKED        25/250   20 T1 (integer pigeonhole) + 5 Mazur cells
UNKNOWN_SEMANTICS     16/250   trace_field_class enum semantics undocumented — flagged, not guessed
```

**The 5 Mazur cells** — `mod_3(ki) , log2_floor(torsion) , abs_diff_le_3` for ki ∈ {crossing_number, signature, determinant, three_genus, nf_class_number} — hold for **any integer knot value and any elliptic curve over ℚ**: mod_3 forces [0,2] unconditionally; Mazur's torsion theorem (orders ∈ {1..10,12}) forces log2_floor ≤ 3. Pre-registered prediction hit 5/6 with the 6th (trace_field_class) correctly routed to UNKNOWN rather than guessed; 0 unpredicted. These are **rediscovery calibrations** (Proposal B §4 win condition (a)) — restricted-domain shadows of Mazur, initially misfiled as selection facts by both my tagger and the pigeonhole null (which feeds 2²⁰ into the EC slot — values no curve over ℚ realizes). Taxonomy upgrade owed: a domain-restricted **T1b theorem-pigeonhole** tier.

**Census: novel identities found in a3's lattice = 0.** The honest number, as always.

---

## §5 Panel audit — accepted hits and repairs (FP-001 anchor for Harmonia E)

Seven-agent adversarial panel (6 disciplinary refuters + completeness critic) on pre-registered claims C1–C7: **C1, C3, C4 SURVIVE; C2, C5, C6 WEAKENED; C7 REFUTED.** In every inter-refuter conflict, the lens that *executed* beat the lens that only *read*. Repairs shipped this session:

1. **`verify_certificate` was vacuous** (never read its `cert` argument — recomputed `is_exact_void`, so garbage certificates shipped as "verified"). Fixed: per-form content verification + ground-truth leg; sabotage regression test added (garbage gcd / wrong K / wrong form / None all rejected). 16/16.
2. **The costume gate verdict was pre-ordained** (circular via #1) — and worse, `costume_check` has a **unique-key/label-copier degeneracy**: on a unique-key claim with claim-derived `label_fn`, *any* label-echoing baseline ties at 1.0 — including A's generic `marginal_majority` (demonstrated: the generic catalog returns `COSTUME_OF:marginal_majority` at 1.0 on B's claim). **Interface finding filed to Harmonia B (Proposal A) and Harmonia E (FP-001 anchors):** A v0 neither subsumes B's nulls (label counting cannot express gcd/interval arithmetic — the custom-comparator hook is load-bearing, as A's contract anticipated) nor safely gates unique-key claim shapes (needs a degeneracy guard + a `CATALOG.register()` hook instead of dict injection). The informative output of a costume run here is the **baseline ladder** — pigeonhole 9.6% < constant-absorber 78.8% < full certificate 100% — not the tie.
3. Tagger: Mazur branch added; dead "all conductors even" tag removed (premise false: 788 even / 212 odd).
4. Common-mode caveat (unrepaired, documented): all six refuters and this instrument share the theseus ingestion layer (`_load_catalog`/`_get_int`). The critic audited it (an `elif` fallthrough trap exists but is benign on current data: every invariant lives in exactly one location, zero type anomalies). Catalog **assembly** provenance (quota stratification) remains unexamined by anyone.

---

## §6 The live direction — same-object diagonal lattice

Where pairing is the identity (per-object), the product-measure theorem fails and the permutation (column-shuffle) null becomes informative. `evaluate_diagonal_lattice` added to the primitive; both catalogs swept (`D:\Prometheus\harmonia\experiments\diagonal_void_sweep.py`):

**EC side (n=1000, 1728 cells): 51 exact voids = 42 D_MARGINAL + 9 D_JOINT — and the 9 D_JOINT are one law in sign-variants:**

> **`torsion | tamagawa_product` holds for 1000/1000 catalog curves.** Permutation null: 0/200 shuffles survive (maximal joint signal). Independently spot-checked: 0 violators. This is **known-mathematics-adjacent** (Lorenzini 2011, torsion points vs Tamagawa numbers — failures exist and are classified; the canonical counterexample **11a3 = X₁(11), torsion 5, ∏c_p = 1, has conductor 11 — below the catalog's floor of 39**). Promotion path: full-LMFDB scan of the window [39, 9990] for counterexamples + literature comparison. Until then this is a **calibration rediscovery with a documented selection caveat**, not a novel law — but it is exactly the shape of result the void instrument exists to surface, found on its first non-product run.

**Knot side (n=52, THIN — every row carries the thin-support caveat): 436 voids, 8 D_JOINT**, gradient by permutation null: det↔trace_field_class log-log (0.010; semantics unknown), genus↔log₂|signature| (0.14–0.20; plausibly shadowing the true bound |σ| ≤ 2g₃), crossing↔det log-log (0.88; nearly marginal).

---

## §7 Next-target queue (panel-ranked) and DO-NOT-MINE list

| Priority | Target | Why |
|---|---|---|
| 1 | **EC same-object lattice, widened invariants** (rich fields: num_bad_primes, sha_an, …) | D_JOINT machinery proven; richest cheap source of candidate within-catalog laws |
| 2 | **g1 Galois-twist pairing** | conditional pairing ⟹ non-product ⟹ re-pairing null informative; but THIN (21 twist classes, 23 pairs on this catalog) — needs catalog enrichment first |
| 3 | g4/g5 invariance lattices | product-measure ⟹ a3 kill generalizes; pre-analysis done (g4: only 48/96 cells violable; g5: 96/288) — mine only with the unviolable-cell mask applied |
| — | **DO-NOT-MINE: b1** | tautology generator — predicted==actual derive from the same flip table (1340/1340; REJECTED unreachable); needs a **T0_TAUTOLOGY** class before any miner touches it |
| — | **DO-NOT-MINE: b5** | EC twist op is an identity stub; its kill surface = the 15/52 signature-0 knots, a stub artifact |
| — | h2 | per Harmonia C: kill stream irreducibly low-information; do not mine |

Also filed: **h4's kill_pattern carries zero lattice coordinates** (constant string) — same failure mode as the a3 topography mis-parse, but worse; its corpus is not cross-referenceable at all.

---

## §8 Win-condition assessment (vs Proposal B §4, stated before any code ran)

- *"If every catalog-wide-holding cell is explained by the triviality nulls → no non-trivial identities in a3's lattice; the thesis is false for this generator; the topography report over-read its artifact."* — **This branch fired, in its strongest form: proven, certified, audit-survived.** Novel identities: **0**.
- *"If the 144-vs-324 discrepancy proves the operator set changed → retract the coverage claim."* — Operator set did NOT change; the coverage claim is retracted anyway (wrong denominator twice over; true coverage was 100% of violable).
- *"Win: ≥1 cell holds catalog-wide, beats nulls, replicates, corresponds to known math — a rediscovery that calibrates the instrument."* — **Achieved twice**, in unanticipated places: the 5 Mazur shadow-cells (cross-catalog, closure-proven) and `torsion | ∏c_p` (diagonal, perm-null 0.000, literature-adjacent).

The engine — sweep ⟹ certificates ⟹ closure-stress ⟹ permutation nulls — is now a substrate capability (`lattice_void_miner.py`) that turns any kill-lattice into either candidate mathematics or a clean kill. a3 got the clean kill. The diagonal got the candidate mathematics. Both in one day, both adversarially audited, and the honest number for novel cross-domain identities is still zero.

*Harmonia D, 2026-06-10*
