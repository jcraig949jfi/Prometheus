# Ergon — Next-Step Prompt (2026-05-13)

**Paste-target:** Ergon agent session. Self-contained — agent has memory; prompt gives direction.

---

## Strategic context

Substrate-volume-and-quality remains the gate per `pivot/strategic_pivot_2026-05-11_substrate_volume_first.md`. LoRA training stays paused indefinitely. Tracks 1-3 from `PROMPT_2026-05-11_substrate_first.md` shipped (commit 6ac3f4e8 — training_anchor ingester + episode-emission consumption scaffold + blind-spot probe coverage gate; 363/363 tests pass). Substrate-shaped pipeline pilot fires today (3 prompts: DR-001 / DR-007 / DR-231) — Techne is processing on return. Aporia authored a 20-claim starter batch at `aporia/meta/queue/claim_stack_aporia_starter_2026-05-13.md`; that's design-time input for Techne's claim_runner, not direct Ergon consumption yet.

## Your scope this week

**You will NOT:**
- Trigger LoRA training, Mahler run, or --writeable substrate generation (existing stand-down).
- Request compute time or frontier-API budget (still deferred to ~week 3+).
- Modify the v1.0 pilot LoRA design at `ergon/learner/v1_0_plans/pilot_lora_design_tier_1_corpus.md`.
- Open contract-change windows.

**You will:**

### Track 1 — Consume today's substrate-shaped pilot output (PRIMARY)

Today's substrate-shaped pipeline pilot (DR-001 / DR-007 / DR-231) fires through Aporia's dispatcher; estimated wall-clock 35-50 min. When Techne completes their Track 4 (parse + validate + stage the substrate_blocks), the staged training_anchor blocks should land at `aporia/docs/staged_substrate_blocks/2026-05-13/training_anchor.jsonl`.

When that file exists:

1. Run `python ergon/learner/scripts/ingest_training_anchors.py --dry-run --input aporia/docs/staged_substrate_blocks/2026-05-13/training_anchor.jsonl`. Confirm the ingester recognizes the schema and produces dry-run output without errors.
2. If dry-run passes, run without `--dry-run` to actually emit LearnerRecord-shaped output to `ergon/learner/corpus/v1_0_tier_pending/2026-05-13/`. Inspect 2-3 records manually for shape correctness.
3. **First substrate-shaped-pipeline-to-Learner round-trip evidence.** Document the yield in a short status note to `ergon/STATUS.md` Learner Branch section: how many training_anchor blocks ingested, how many invalid, how many unverified-dropped, any specific failures. This is the substrate-grade behavior delta — first time substrate-shaped pipeline output reaches Ergon's corpus.
4. **Smoke-test the BS-coverage path.** The pilot's DR-007 (LMFDB GL(3) root number) is most likely to produce training_anchor blocks with `trust_tier: ml_predicted` (per AA-019). Confirm the ingester correctly identifies these and routes them per the trust_tier discipline (not into the "analytically_proven" eval split).

If today's pilot does NOT produce training_anchor blocks (e.g., Gemini didn't emit valid YAML, or Techne's validator rejected them all): the ingester smoke-test is moot for this cycle. Document as such; no action needed beyond noting the empty cycle.

### Track 2 — Coordinate on training_anchor v1.1 (bs_coverage)

Techne is shipping v1.1 of `training_anchor_v1.json` adding optional `bs_coverage: List[str]` field per `T-2026-05-11-aporia-to-techne-bs_coverage-schema-approved-as-v1.1`. When the new schema lands:

1. Switch `ingest_training_anchors.py` to read `bs_coverage` explicitly when present (regex-heuristic fallback when absent — both code paths stay live for backwards compatibility).
2. Update `ergon/learner/v1_0_plans/training_anchor_ingestion_spec.md` to document the v1.1 field handling.
3. Re-run BS-coverage probe test to confirm 7/7 still pass with the schema bump.

No work today unless v1.1 ships today. Reactive on Techne's release.

### Track 3 — Episode-emission consumption follow-up

The 2026-05-11 EEC-004 finding (GATE/PROMOTE verification_tier reads decidability-status, not KC/BS calibration tier) maps onto Techne's Dim 3 audit-prep §3 per Aporia ack `T-2026-05-11-aporia-to-ergon-EEC-004-mapping-confirmed`. Techne's Dim 3 instrumentation is the substrate-side fix (~70-150 LOC); no Ergon work required this week.

However: **when Techne's tier_1_claim_runner.py ships and runs Aporia's 20-claim starter batch**, the LearnerRecord output will be the first concrete evidence of how `verification_tier` is currently being populated. Track 3 action: when Techne's runner produces output, inspect 5-10 LearnerRecords to confirm whether `verification_tier` reads decidability-status (current behavior, EEC-004 confirmed) or KC/BS-tier (the calibration-preservation gate's actual need). File a follow-up ticket if the gap is confirmed at runtime.

### Track 4 — Maintain stand-down discipline

No compute. No --writeable. No Mahler. No LoRA training. No frontier-API budget request. No Σ-kernel elaboration. No new opcodes proposed. No contract changes.

The pilot LoRA design stays as-is at `pilot_lora_design_tier_1_corpus.md`. The 4-condition spec is sound; it waits.

## Bounded resources

- File ownership: `ergon/` tree, plus `aporia/meta/queue/ergon_inbox.jsonl` for status updates.
- No compute. No API budget. No contract changes.
- Estimated total work: ~1-2 days reactive (waiting for Techne's pilot processing + claim_runner ship). Track 1 is the substantive piece; Tracks 2-3 are coordination-only.

## What to file when work lands

- Track 1 ingest results: append to `ergon/STATUS.md` Learner Branch section + file `T-2026-05-13-ergon-to-techne-pilot-ingest-results` (P2) summarizing yield. If verification_tier vs KC/BS-tier gap surfaces (Track 3), upgrade to P1.
- Track 2 v1.1 consumption: brief status note in STATUS.md, no inbox ticket needed (low-impact maintenance).

## Substrate-grade observation worth carrying forward

Today's substrate-shaped pilot ingest is the **first end-to-end substrate-shaped-pipeline-to-Learner-corpus round-trip**. Per `feedback_substrate_passive_consumer_warning.md` (substrate at risk of being beautifully falsifying machine forever): if today's pilot succeeds at producing usable training_anchor blocks that Ergon's ingester consumes cleanly, that's the first concrete evidence the substrate-shaped pipeline thesis (5-10x reviewer throughput; arXiv-verify gating fabrications) actually compounds into Learner-corpus growth. If it fails at any stage (Gemini doesn't emit valid YAML, validator rejects everything, ingester finds schema mismatch), the pilot evidence is also informative — we know exactly which stage needs work.

Either outcome is substrate-grade. Document the result truthfully; resist narrative-construction in either direction.

## What you should NOT ask James about

The compute decision for LoRA is deferred to ~week 3+. Do not raise it this week.

End of prompt. Acknowledge receipt; wait for Techne's substrate-shaped pilot processing to complete; then run Track 1.
