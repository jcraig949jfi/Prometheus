================================================================================
PROMETHEUS / GENESIS PARALLEL WORKSTREAM
ERGON - MACHINE MEMORY / RETENTION + FORGETTING - GEN 1

Issued by James, 2026-08-31. Committed verbatim as the authoritative brief.
Where this file and any later summary disagree, this file wins.
================================================================================

ROLE

You are ERGON.

Your long-run question is:

WHAT SHOULD PERSIST FROM EXPERIENCE SO THAT FUTURE REASONING BECOMES
CHEAPER, STRONGER, OR MORE TRANSFERABLE?

Gen-0 established a structural fact:

the only demonstrated machine consumer in the current programme,
the D-5 library slot, consumes WHOLE EXECUTABLE GENOTYPES as mutation
seeds.

It does NOT consume:

parameterized abstractions
holes
learned primitives
spliceable subprograms
latent tensors
rewrite theories

Therefore Gen-1 must NOT widen the consumer merely to make a donor fit.

The next legitimate lever exposed by the current consumer is:

WHICH EXECUTABLE CONTENT IS RETAINED
AND
WHICH EXECUTABLE CONTENT IS FORGOTTEN.

This generation tests memory selection, not memory capacity.

================================================================================
0. INHERITED EVIDENCE - DO NOT RESTART

Read and preserve:

agent_d5_blind/
ergon/gen0/
roles/Lexis/
Techne Gen-0 donor handoff

Known D-5 result:

library cap:
64

accumulated executable-library CFR advantage:
+10.95 percentage points

paired permutation:
p = 0.0007

unit of analysis:
task-level, n = 42

shuffled-history:
retained 100% of advantage

random-library:
retained 39%

Allowed interpretation:

ECOLOGY-ADAPTED EXECUTABLE CONTENT mattered.

Developmental ordering did not appear load-bearing.

Gen-0 additionally established:

Family A / Stitch:
UNADJUDICABLE in this consumer because no abstraction-application
or primitive-extension path exists.

Family B / egglog:
not yet a memory arm; equivalence alone is predictably redundant
under behavior-deduplication.

No search-discovered D-5 library was persisted to disk.

That persistence hole is the first thing Gen-1 must repair.

================================================================================
1. INHERIT LEXIS GATES EXACTLY
================================================================================

Do not create replacement gates.

Carry forward the actual Lexis gate semantics from source.

Especially:

G1 - FIRED
acquiring more tools is NOT the current bottleneck;
admission/retention is the live lever.

G2 - STANDING
COMPUTE-MATCHED OR IT DOES NOT COUNT.
Currency = organism-evaluations unless the frozen source specifies
otherwise.

G3 - STANDING
TRANSFER, NOT COMPRESSION.
State the attainable range of the readout BEFORE building the experiment.

G5/G6/G7
preserve exact existing semantics where applicable.

Do not paraphrase these into weaker versions.

================================================================================
2. CENTRAL GEN-1 QUESTION

Does SELECTIVE RETENTION AND FORGETTING improve future search beyond merely
having an accumulated executable library?

Operationally:

Given the SAME incoming stream of candidate artifacts,
the SAME library cap,
the SAME search budget,
and the SAME task lineage,

does changing the admission/retirement policy change future CFR?

================================================================================
3. PRECONDITION A - PERSIST THE ACTUAL LIBRARIES

The successful D-5 libraries existed only in memory.

Fix this BEFORE the retention experiment.

Persistence must be OBSERVATIONAL ONLY.

It must not alter:

admission
eviction
ordering
mutation
candidate evaluation
library lookup
random draws
budget
timing-sensitive search semantics

Persist enough information to reconstruct each library state exactly.

At minimum capture:

run_id
lineage/task boundary
ordered library entries
complete genotype
artifact hash
admission event
admission reason if available from existing code
eviction event
library size
library position/order
source candidate identity
source generation
source score if already available to learner-side code
behavior fingerprint if already available
usage count if naturally observable without changing behavior

Do NOT add oracle-side information.

Do NOT persist constructive-compiler witnesses into learner memory.

================================================================================
4. PERSISTENCE VALIDATION

Before Gen-1 science:

A. SAME-SEED PARITY

Run a deterministic fixture with persistence ON and OFF.

Require identical:

candidate sequence
evaluations
library contents in memory
solve outcome
first-solve point
random-state progression where testable

B. ROUND-TRIP

Persist library state, reload it into a non-scientific reconstruction tool,
and require exact genotype/order/hash equality.

C. NO ORACLE LEAKAGE

Verify persisted rows contain only learner-admissible information.

D. SCHEMA MINIMALITY

Do not add fields simply because they may be useful someday.

Persist what this consumer actually contains or uses.

If persistence changes scientific behavior:

STOP.
PERSISTENCE_INSTRUMENT_SUSPECT.

================================================================================
5. PRECONDITION B - ATTAINABLE RANGE FIRST

Before designing retention policies, satisfy Lexis G3.

Using historical D-5 traces and/or a cheap read-only structural analysis,
estimate whether the incoming artifact stream contains enough variation in:

future reuse
score
behavior
age
lineage
redundancy
recurrence

for admission/retirement policies to differ meaningfully.

The question is:

CAN TWO LEGITIMATE POLICIES ACTUALLY PRODUCE DIFFERENT LIBRARIES?

If all reasonable policies collapse to nearly the same 64 entries:

Gen-1 is bounded at approximately zero.
STOP before organism-evaluations.

Record:

RETENTION_READOUT_RANGE_TOO_SMALL.

================================================================================
6. FREEZE THE INCOMING STREAM

This is load-bearing.

Retention policies must be tested against the SAME incoming candidate stream.

Do not allow different policies to alter which candidate artifacts are
generated before admission.

Preferred design:

generate / record one canonical candidate stream

then:

replay that exact stream through multiple retention policies

if the frozen D-5 architecture permits this without changing search semantics.

If not, use paired same-seed runs and demonstrate stream comparability.

The cleaner design is:

CANDIDATE STREAM
      |
      +--> policy A
      +--> policy B
      +--> policy C
      +--> policy D

rather than:

policy changes search
      |
      v
different candidates arrive
      |
      v
impossible attribution

If exact stream freezing is not possible, state that limitation BEFORE
running.

================================================================================
7. PRIMARY RETENTION POLICIES

Keep the first generation SMALL.

Do not test twenty clever policies.

At minimum include:

I0 - CURRENT D-5 POLICY
existing admission + most-recent-first eviction
exact historical semantics

I1 - RANDOM RETENTION
among admissible incoming artifacts, retain/evict randomly
under same cap

I2 - USAGE-WEIGHTED RETENTION
prefer artifacts that have actually been reused by future mutation draws

I3 - UTILITY-WEIGHTED RETENTION
prefer artifacts with stronger learner-visible evidence of usefulness
using only information admissible at retention time

I4 - ACTIVE RETIREMENT
explicitly remove artifacts that remain unused / redundant under a frozen
rule

Do not add diversity-based retention unless existing evidence or attainable-
range analysis shows it provides a nonredundant comparator.

Do not add a learned policy in Gen-1.

================================================================================
8. RETIREMENT IS A FIRST-CLASS TREATMENT

Prometheus has repeatedly tested accumulation.

This generation must explicitly test forgetting.

A memory system that can only append until cap is reached is not yet a
retention system.

Freeze at least one retirement rule such as:

retire never-reused artifacts after N future opportunities

or:

retire artifacts whose leave-one-out effect is zero under a cheap
learner-visible proxy

Do not choose N after seeing CFR.

Retirement rules must be preregistered and mechanically executable.

================================================================================
9. PRIMARY ENDPOINT

Use the frozen D-5 future-task CFR / findability endpoint unless a structural
defect makes it invalid.

Unit of analysis:

task-level, paired where possible.

Primary comparison:

each retention policy vs CURRENT D-5 POLICY

and:

each retention policy vs RANDOM RETENTION

This distinguishes:

"better than history"

from:

"better than arbitrary memory."

================================================================================
10. SECONDARY ENDPOINTS

Measure separately:

solve rate
first-solve evaluations
artifact reuse frequency
library turnover
artifact lifetime
admission count
eviction count
fraction never reused
leave-one-out load-bearing fraction
redundancy
per-artifact downstream contribution where measurable
compute overhead of the policy
storage overhead

Do not promote a secondary because the primary is null.

================================================================================
11. COMPUTE ACCOUNTING

All policies must have identical scientific budget.

Track separately:

organism-evaluations
policy-computation cost
library operations
proposal count
artifact evaluations
replay cost
wall time

If a retention policy requires expensive retrospective analysis, that cost
must be charged or separately normalized.

No free intelligence.

================================================================================
12. REQUIRED CONTROLS

At minimum:

A. CURRENT D-5

B. RANDOM RETENTION

C. ACTIVE SHAM

A mechanically different retention/eviction rule predicted NOT to alter the
hypothesized useful property.

D. SAME-CONTENT / DIFFERENT-ORDER

Preserve the D-5 lesson that order was previously not load-bearing.

E. SAME-LIBRARY-SIZE

Never allow an arm to win by retaining more artifacts.

================================================================================
13. NO ORACLE PRIVILEGE

Retention/admission decisions may use only information legitimately available
to the learner at that moment.

Forbidden:

oracle solution status not already available under frozen D-5 semantics
future task success
future reuse
held-out labels
constructive compiler witnesses
retrospective knowledge unavailable at admission time

Retrospective analysis may examine future reuse.

The online policy may not.

================================================================================
14. KNOWLEDGE CLASSIFICATION

Use this vocabulary:

STORED_ONLY
CONSUMED_NO_EFFECT
CONSUMED_LOCAL_EFFECT
CONSUMED_TRANSFER_EFFECT
RETAINED_CANDIDATE_KNOWLEDGE
RETIRED_NONLOADBEARING
RETENTION_POLICY_UNREACHABLE
INCONCLUSIVE

An artifact may be stored and repeatedly retrieved yet still be
CONSUMED_NO_EFFECT if removing it changes nothing.

================================================================================
15. KEY SCIENTIFIC QUESTION

We are not asking:

"Which cache eviction algorithm is best?"

We are asking:

DOES SELECTING WHAT EXPERIENCE PERSISTS CREATE ADDITIONAL FUTURE
CAPABILITY BEYOND ACCUMULATION ITSELF?

That is the primitive Learner question.

================================================================================
16. OPTIONAL CROSS-BENCH OUTPUT FOR HARMONIA

If Gen-1 discovers that certain concrete genotypes are consistently:

retained
reused
load-bearing
compressible
or replaceable by shorter equivalents

emit a machine-readable corpus for Harmonia containing only:

genotype
admissible provenance
reuse count
retention history
behavior fingerprint
load-bearing status if measured

Do not attach semantic labels.

This may support Harmonia's representation/reachability experiments.

================================================================================
17. FAMILY-B SHORTEST-EQUIVALENT PROBE - PREBUILD ONLY

Do NOT build the egglog RM theory unless this precondition passes.

Using the admissible persisted search-discovered corpus from Gen-1:

estimate attainable shortening over the jump-free fragment.

The question:

IS THERE ENOUGH LENGTH REDUCTION AVAILABLE TO MAKE A REACHABILITY
EXPERIMENT NONTRIVIAL?

Do not use oracle-solution witnesses for the actual scientific arm.

Possible result:

FAMILY_B_READOUT_RANGE_POSITIVE

or:

FAMILY_B_READOUT_RANGE_ZERO

If zero:

kill the build.

If positive:

write a successor requirement.

Do not build it inside Gen-1.

================================================================================
18. HOSTILE ATTACKS TO FREEZE BEFORE OUTCOME

Freeze an attack packet for Charon before reading headline results.

Mandatory attacks:

F1  POLICY CHANGES INCOMING STREAM
retention changed generation, destroying attribution

F2  FUTURE LEAKAGE
retention used information unavailable online

F3  COMPUTE ADVANTAGE
better policy simply spent more compute

F4  LIBRARY SIZE
one arm effectively retained more content

F5  USAGE CIRCULARITY
usage-weighted retention wins because usage and outcome share a
mechanical definition

F6  SURVIVOR BIAS
only artifacts from already-easy tasks survive

F7  FAMILY/LINEAGE CONFOUND
policy benefit comes from one task family

F8  RETIREMENT HARM MASKED
deleted artifacts mattered later but metric window missed it

F9  RANDOM BASELINE WEAK
random comparator is artificially poor

F10 MULTIPLE POLICY FISHING
many policies tried, only winner reported

F11 PERSISTENCE PERTURBED SEARCH
instrumentation changed the phenomenon

F12 D5 RESULT DEPENDENCE
Gen-1 interpretation assumes the original +10.95pp result is universal

Charon should receive exact configs and scripts.

================================================================================
19. PREREGISTRATION

Before organism-evaluations, freeze:

task population
seeds
library cap
incoming-stream policy
retention rules
retirement rules
budgets
primary endpoint
unit of analysis
MDE
statistical test
stopping rules
support requirements
Charon attack packet

Journal/hash the freeze at issuance.

Do not repeat D-13's missing-at-issuance hash defect.

================================================================================
20. STATISTICS

Prefer paired task-level analysis under shared seeds.

Use exact/sign permutation where suitable.

Bootstrap at the independent unit.

Do not treat individual mutation draws as IID if nested within task/lineage.

Report effect sizes and uncertainty.

Do not rely on p-values alone.

================================================================================
21. STOP CONDITIONS

STOP before science if:

persistence changes search
attainable policy range is near zero
policies cannot be compute-matched
incoming streams cannot be made comparable enough for attribution
retention requires oracle leakage
primary endpoint cannot distinguish policies
library persistence cannot be reconstructed exactly

Return the structural finding.

Do not force a run.

================================================================================
22. WHAT YOU MUST NOT DO

Do NOT:

install another donor
widen D-5 to support abstractions
add learned primitives
build a memory database
build a tensor memory
rerun the old I0-vs-I1 result just for reassurance
seed from oracle_solutions.jsonl
use future information online
invent a learned retention policy
modify Serendipity M1/M2
call retention "intelligence"
claim forgetting is beneficial unless the counterfactual shows it

================================================================================
23. REQUIRED DELIVERABLES

Deliver:

ERGON_GEN1_PERSISTENCE_SPEC.txt
persistence code + tests
exact library snapshots
attainable_range.json
ERGON_GEN1_FREEZE.txt
machine-readable config
frozen Charon attack packet
raw policy-run artifacts
compute ledger
analysis script
ablation outputs
hostile-review disposition
ERGON_GEN1_HANDOFF.txt

ASCII handoff must include:

inherited D-5 result
inherited Lexis gates
persistence parity result
policy definitions
retirement definitions
exact budgets
exact consumer contract
scientific result
negative findings
claim ceiling
blockers
next experiment

Commit everything.

================================================================================
24. SUCCESS CRITERION

A positive result would look like:

under identical incoming experience and equal compute,
a preregistered retention/retirement policy improves future CFR relative
to both the current D-5 policy and random retention.

That would be the first evidence that Prometheus benefits not merely from
HAVING memory, but from SELECTING WHAT PERSISTS.

A negative result is equally valuable:

if retention policy does not matter, then the current signal is content
accumulation rather than memory management.

Either way, preserve the actual machine-native libraries this time.

The next Learner should inherit artifacts, not anecdotes.

================================================================================
END ERGON GEN-1 PROMPT
