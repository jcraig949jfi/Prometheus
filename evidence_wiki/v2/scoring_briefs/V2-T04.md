# Known relevant priors — V2-T04

These prior findings exist in Prometheus history and bear on this task.
Use them to judge D1 (failure avoidance) and D5 (duplication).

- (core) [REFUTED] Cold-start feature routing is NULL: a held-out scrap's concept/field metadata carries no routing signal over popularity (FEATURE AUC 0.744 vs POP 0.753, and real fields perform the same as shuffled fields), so a tool cannot be routed to problems from its concept labels.
- (core) [SUPPORTED] Warm-start collaborative completion is SUPPORTED and robust: navigable structure beyond popularity exists in the mined-failure solve matrix (COLLAB AUC 0.829 vs POP 0.754, P<0.0001) and survives an adversarial tail check dropping the top-8 popular probes, so the navigable handle for failure residue is behavioral, not semantic.
- (supporting) [NOT_ESTABLISHED] Syntax-only relevance keying failed its frozen capacity gate — mean(PP2 - R2) = -0.0208 with one-sided sign-flip permutation p = 1.0 — with PP2 worse than its own query-shuffled twin and worse than uniform retrieval, so the assay is declared not viable on syntax-only information.

Expected design impact: Metadata-only similarity carried as an expected null; primary features behavioral/executable; warm-start framing