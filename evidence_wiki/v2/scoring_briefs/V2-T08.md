# Known relevant priors — V2-T08

These prior findings exist in Prometheus history and bear on this task.
Use them to judge D1 (failure avoidance) and D5 (duplication).

- (core) [REFUTED] The greedy-LoRA +0.68 gain decomposes into format-following, a False/kill prior, and narrow per-source template classes with no cross-domain transfer and almost no genuine reasoning - a calibrating kill establishing that more failure data of the same shape will not grow the reasoning share.
- (supporting) [SUPPORTED] The greedy-substrate LoRA gain survives a shuffled-label control and entity-disjoint holdout, but decomposition shows genuine mathematical reasoning is the smallest component (~0.10 of the raw +0.68), behind format-following, relation base-rate priors (~0.67) and value memorization (~0.12).

Expected design impact: Preregistered decomposition controls (format-only, prior-only, template/entity-disjoint holdout); success claim gated on surviving decomposition, not headline gain