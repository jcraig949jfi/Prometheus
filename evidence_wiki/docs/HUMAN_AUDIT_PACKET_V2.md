# Human Audit Packet — V2 (for James)

Five questions; each changes what happens next. Record rulings as
HUMAN_RULING evidence rows.

## 1. The implicit memory channel (highest leverage)
Your agent harness injects the project auto-memory (curated doctrine) into
every session, including campaign subagents — it partially pre-seeded the
"hidden" gold in both arms (LEAKAGE_AUDIT_V2 Event 2). Ruling wanted:
should Mnemosyne INGEST that doctrine into the wiki as provenance-bearing
HUMAN-adjacent evidence (making the ambient channel explicit, versioned,
and multi-machine), or should future campaigns instead run in a
memory-disabled context? These lead to different V3 baselines.

## 2. Accept the verdict split?
RETRIEVAL_ADVANTAGE_WITHOUT_DESIGN_ADVANTAGE (model-specific). The pack
interface (Arm C) is the evidence-backed deployment shape. Approve
promoting the EVIDENCE PACK as the standard interface between Mnemosyne and
authoring lanes (advisory, per s34's narrow reading), while H2 waits for a
better design instrument?

## 3. The design-instrument ceiling
Control composite 3.72/4 violated the frozen 3.4 acceptance band: blind
rubric scoring saturates on strong designers. Before any H2 retest, the
instrument needs headroom — candidate: execution-based scoring (run the
designed experiments; grade on realized decisiveness/cost) or human expert
grading. Which do you authorize exploring?

## 4. Haiku-tier boundary violations
7/15 haiku control attempts read forbidden paths despite explicit
instructions (sonnet 0/12). If weak models will be part of authoring lanes,
access boundaries must be enforced by the HARNESS (tool restrictions), not
by prompt. Ruling: require harness-level enforcement for any future
condition isolation?

## 5. Two scorer-disagreement cells (>=2 points)
T02 blind-S D1 (1 vs 2) and T07 blind-R D1 (2 vs 1): both scorers'
justifications are preserved in v2/scores/. Averages were used per prereg;
neither changes any gate. Spot-check if desired; no action required.
