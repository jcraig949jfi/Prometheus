# V2 Case Studies (charter s31)

## WIKI WIN (retrieval): the cross-family kill nobody else found
T05 asked for validation of a failure signature as a program-wide early-
abort trigger. Both wiki arms retrieved C-ff8811fa0ac7 — the sigma-kernel
OBSTRUCTION_SHAPE signature REFUTED as a cross-family predictor (1/244) —
via mechanism/negative-evidence search, and T05-B-sonnet turned its numbers
into falsifier thresholds. Neither control found it (repo path:
sigma_kernel validation JSON, low salience); both controls substituted a
DIFFERENT real signature (Gen-1A MDE-exceeds-bound; D-4 S1-vs-S3 contrast)
and built sound designs around it. Retrieval won; design tied — the
campaign in miniature.

## CONTROL WIN: repo archaeology beats hidden linkage
T10-A-sonnet, with no wiki, found that a near-identical bridge-ranking
pipeline already exists with a measured 67% false-positive rate
(signature_matcher.py + cross_domain_falsification_protocol.md) — material
NOT in the wiki gold at all — and correctly reframed the task as a re-gate
of an existing instrument. Ordinary search remains formidable.

## RETRIEVAL WITHOUT METABOLIZATION / MEMORY NOISE
T01-B-haiku retrieved relevant claims, then spent its design on GEN-2.1
release-packet fork-inheritance mechanics tangential to the dedup question;
blind scorers rated it lowest of its task (2/3/2/4/4 avg side). Retrieval
without integration — the anchoring/noise channel, and the likely driver of
the negative clean-task delta.

## MISLEADING MEMORY REJECTED (all arms)
T06 planted the tempting D-5 +10.95pp positive against the D-8 null and the
interface-mediation doctrine. Every arm — wiki, control, and pack —
predicted conditional single-digit gains with explicit failure conditions;
several independently demanded interface-feasibility gates before any
solve-rate comparison. Misleading adoption: 0. The ecosystem's
falsification-first culture, not the wiki, is what resists anchoring.

## CROSS-DOMAIN FAILURE REUSE
T03-C's designer imported the probe band's attainable-without-reasoning
floor (C-82fe472469ca) and the ceiling-read-as-floor retraction
(C-353ec1eb022a) into a grammar-world evaluation — probe-band negative
evidence changing a games/grammar design, delivered by a pack. Also
T05-B-sonnet (kernel-substrate kill -> program-wide abort-trigger design).

## HISTORICAL MEMORY
T02: the June seam-fidelity denylist findings (11 weeks old) reached the
sonnet control through repo search AND the wiki arms through the
denylist_leak mechanism; the haiku control missed them. Old evidence flows
through both channels — but only for models strong enough to search.

## NULL CASE
T09 (unit-of-inference): every arm converged on decision-level inference
with cluster bootstraps; composites 3.7-4.0 across the board. The doctrine
is so thoroughly ambient (repo + auto-memory) that no retrieval channel
changes anything.

## The accidental case study: the implicit memory system
The campaign's biggest discovery was not planned: designer agents cited
operator memory doctrine that exists NOWHERE in the repo — the agent
harness injects project auto-memory into every session. Prometheus already
HAS a memory-transmission channel shaping design behavior; it is unlogged,
unversioned, provenance-free, single-machine, and curated by one operator.
The Evidence Wiki's differential value must be measured against — and its
V3 case argued relative to — that ambient channel, not against a blank
slate. (Full analysis: v2/LEAKAGE_AUDIT_V2.md, Event 2.)
