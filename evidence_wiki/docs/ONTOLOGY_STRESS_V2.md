# V2 Ontology Stress — Report

Charter s17/s18. No new annotation wave was part of the V2 campaign design
(the memory-advantage experiment consumed the session's agent budget), so
the stress test is limited to what V2 actually exercised:

1. **Boundary rulings encoded (s17)**: algebraic_identity_artifact,
   circular_verification, and calibration_anchor now carry James's
   discriminators as v2 registry rows (HUMAN, charter-provenance,
   supersedes v1, nothing rewritten). The V1 audit-packet questions on
   these two boundaries are thereby RESOLVED; whether the rulings reduce
   annotator disagreement is measurable at the next annotation event and
   is preregistered as the follow-up check.
2. **Retrieval-side stress (implicit)**: mechanism traversal remained the
   discriminating wiki capability (T05's cross-family kill; T03's
   chance-floor family). No mechanism behaved as a dumping ground in
   campaign queries; `projection_equivalence` and `chance_floor_calibration`
   were the most-traversed terms and returned precise families.
3. **Debts unchanged from V1** (still open, still tracked): the
   `null_underpowered` failure-class proposal; the H-008
   circularity-on-a-positive-claim ceiling question; duplicate-pair ruling
   (OQ1 twice). These remain in HUMAN_AUDIT_PACKET_V1/V2.

Verdict: G13 STABLE_NO_NEW_DATA — no evidence of category collapse; the
generalization result from V1 stands unmodified.
