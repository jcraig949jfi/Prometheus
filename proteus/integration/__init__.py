"""Harmonia integration surface for the Proteus Player Foundry.

USE A (allowed): a deterministic source of frozen, semantics-free player specimens.
USE B (NOT qualified): the mutation machinery as a neutral evolutionary operator.

Nothing in this package evolves, scores, classifies or selects players, and nothing in it knows
that worlds exist. See registry.source_qualification() for the limitation that travels with
every registry, and roles/Proteus/HARMONIA_HANDOFF.md for the consumer path.
"""
