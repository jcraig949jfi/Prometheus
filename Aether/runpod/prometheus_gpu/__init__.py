"""Prometheus GPU flight system: package, validate, launch, account.

A seat declares a module and receives packaging, dry-run validation,
cost projection, safe launch, telemetry, artifacts, cleanup and a
receipt -- without publishing a container or learning the RunPod API.

Start at Aether/runpod/PROMETHEUS_GPU_RUNPOD_GUIDE.md.
"""
