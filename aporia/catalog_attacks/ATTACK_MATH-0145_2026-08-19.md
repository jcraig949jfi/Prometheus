# Catalog attack: MATH-0145 — Brumer-Stark calibration trial (Stickelberger instance, 3000 fields)
Date: 2026-08-19 (Aporia loop) | Bucket: A | CALIBRATION TRIAL on a proven theorem (AA-065)
Consumer: trace-vector corpus + instrument-calibration ledger + AA-065

## Design (from triage — Kairos priority 0)
Brumer-Stark is PROVEN (Dasgupta-Kakde 2023; AA-065). This thread is a blind-trial-style
calibration: point the instrument at the data and ask whether it detects the structural
signature of the proven annihilation. Per the triage's own rule: if the instrument fails on a
proven theorem, the INSTRUMENT is miscalibrated.

## Operationalization (simplest proven rung of the Brumer-Stark circle)
For imaginary quadratic K with discriminant D < 0, the classical Stickelberger/analytic
relation — the base instance the Brumer-Stark circle generalizes — predicts the class number
exactly from a bare character sum:
    h = (w / 2|D|) * | sum_{a=1..|D|-1} kronecker(D,a) * a |,  w = 6,4,2 for D = -3,-4,else.
The instrument computes the right side from scratch (own Kronecker-symbol implementation, no
database arithmetic) and compares against the mirror's independently stored class_number.

## Result: 3000/3000 EXACT — zero mismatches
All 3,000 imaginary quadratic fields with |D| <= 20,000 recovered their stored class number
exactly from the character sum. The proven theorem's fingerprint is fully visible to the
instrument; calibration PASSES with no tolerance needed (integer-exact agreement).

## Honest scoping
1. This is the CLASSICAL instance (abelian over Q, imaginary quadratic) — the base of the
   annihilation circle, not the Dasgupta-Kakde frontier (totally real base fields, Ritter-Weiss
   machinery). The full totally-real Brumer-Stark element test requires a Stickelberger-element
   computation harness (Hecke L-values at s=0) that does not exist as database columns —
   filed as the upgrade path, a build not a query.
2. Independence: the class_number column and the character sum are computed by entirely
   different mathematics (LMFDB: algebraic class-group computation; here: analytic sum) —
   agreement is a genuine cross-instrument check, not circular.
3. Mirror trap #7 logged: disc_abs::int overflows (values to 1e10) — use ::numeric.

## Trace-vector record
problem_id: MATH-0145 | tier_probe: calibration-blind-trial | answer_correct: 3000/3000
domain_constraints_detected: [classical-instance-scope, int-overflow-cast]
operations_used: [kronecker-symbol-from-scratch, character-sum, exact-integer-comparison, cross-instrument-independence]
counterexamples_tested: 3000; 0 mismatches
kill_pattern: none (CALIBRATION PASS) | failure_type: none
repair_available: n/a | confidence_calibration: integer-exact; scope limited to classical instance
residue: the calibration-first discipline validated (Kairos priority-0 rule: blind trials
  before discovery tests — this instrument may now be pointed at open annihilation questions
  with a documented pass on the proven base case); trap catalog -> 7 (::int overflow)
