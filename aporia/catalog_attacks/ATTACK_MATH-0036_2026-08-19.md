# Catalog attack: MATH-0036 — Arthur multiplicity (GL(2) strong-multiplicity-one proxy, empirical)
Date: 2026-08-19 (Aporia loop) | Bucket: A | Instrument mode — not an oracle claim
Consumer: trace-vector corpus + kill ledger + triage rescore

## Question and test spec (from triage)
Arthur's multiplicity formula predicts multiplicity one throughout the GL(2) discrete spectrum
(a theorem for GL(2): Jacquet-Langlands / strong multiplicity one; Arthur's endoscopic
classification generalizes it to classical groups). Test spec: systematic verification across
LMFDB mf_newforms. Falsification criterion: ANY form whose multiplicity deviates.

## Operationalization (honest proxy)
Full automorphic multiplicity is not a database column. The testable projection: strong
multiplicity one implies NO two distinct newform (Galois) orbits at the same
(level, weight, character) share a Hecke eigensystem — detectable as trace_hash collisions
within the same (level, weight, char_orbit_index) triple.

## Pre-stated readings
- SUPPORTIVE-NO-COLLISION: zero same-triple collisions.
- CHALLENGE: any same-triple collision (would be instrument-bug-first per doctrine — a
  theorem-violation reading is an instrument bug until it survives the full battery).
- VACUOUS: hash coverage degenerate.

## Result: SUPPORTIVE-NO-COLLISION (within coverage)
- mf_newforms: 1,141,510 rows; trace_hash populated on 217,520 (19.1% — coverage caveat);
  217,519 distinct hashes.
- Same-(level, weight, char_orbit_index) collision groups: ZERO.
- Whole-corpus control: exactly ONE collision group anywhere —
  3381.1.c.b vs 3381.1.o.c (weight 1, level 3381, char orbits 3 vs 15, both dim 16).
  Different nebentypus => NOT a multiplicity-one violation. It is a weight-1 curiosity:
  two dimension-16 Galois orbits in the exotic (Artin) corner with identical trace vectors —
  trace-vector identity across distinct character orbits. Filed as residue (a probe-grade
  object for the collision family: same surface statistics, distinct underlying objects).

## Instrument caveats (load-bearing)
1. trace_hash covers 19.1% of rows — the reading is coverage-local. The uncovered 81%
   (mostly higher-dimensional orbits) is untested by this projection.
2. Trace vectors are ORBIT sums: hash equality is necessary but not sufficient for
   eigensystem equality; hash inequality IS sufficient for distinctness — so zero same-triple
   collisions is a sound supportive reading, while the 3381 pair needs eigenvalue-level
   comparison before any stronger claim.
3. All numeric columns text-typed (mirror-wide trap, see ATTACK_MATH-0026): every
   comparison above used casts.

## Trace-vector record
problem_id: MATH-0036 | tier_probe: catalog-attack-empirical | answer_correct: n/a (instrument)
domain_constraints_detected: [hash-coverage-19pct, orbit-trace-projection, text-typed-columns]
operations_used: [collision-scan, within-triple-grouping, whole-corpus-control, proxy-honesty]
counterexamples_tested: 217,520 hashed forms, 0 same-triple collisions
kill_pattern: none (SUPPORTIVE) | failure_type: none | repair_available: n/a
confidence_calibration: supportive within the 19% hashed subset; silent on the rest
residue: the 3381 weight-1 trace-twin pair (navigable: a natural specimen for the
  same-statistics/distinct-objects probe class) + the coverage boundary itself
