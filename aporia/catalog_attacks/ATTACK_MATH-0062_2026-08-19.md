# Catalog attack: MATH-0062 — Pair correlation vs GUE across L-function families (empirical)
Date: 2026-08-19 (Aporia loop) | Bucket: A | Instrument mode — not an oracle claim
Consumer: trace-vector corpus + kill ledger + triage rescore + GUE paradigm P04 grounding

## Question and test spec (from triage, unblocked)
Montgomery pair correlation / GUE universality across L-function families. The triage marked
this BLOCKED on a DuckDB zeros store — STALE: the local LMFDB mirror's lfunc_lfunctions holds
24,351,376 rows with positive_zeros arrays. The mirror supersedes the dependency; unblocked
same-pass.

## Method (bounded proxy)
Low-zero regime (each L-function contributes its first ~40 zeros), so the statistic is the
ensemble nearest-neighbor spacing after per-L-function MEAN-SPACING NORMALIZATION FIRST
(feedback_scale_vs_shape), first zero dropped (edge), primitive L-functions only.
Discriminator: level-repulsion fraction P(s < 0.3) — GUE ~0.015, Poisson ~0.259.
Samples: degree 1 (Dirichlet): 2,000 L-functions, 76,000 gaps; degree 2: 203 of 2,000
sampled rows carried >= 12 zeros (coverage note), 7,388 gaps.

## Pre-stated readings
- SUPPORTIVE-GUE-CONSISTENT: repulsion present, Poisson excluded, family-consistent.
- CHALLENGE: any family with Poisson-like or intermediate statistics.
- VACUOUS: zeros coverage degenerate.

## Result: SUPPORTIVE-GUE-CONSISTENT (both families, mutually consistent)
    family   | n_lfunc | gaps   | var    | P(s<0.3) | P(s<0.5) | median
    degree 1 |   2000  | 76,000 | 0.158  | 0.0213   | 0.0934   | 0.968
    degree 2 |    203  |  7,388 | 0.162  | 0.0250   | 0.1022   | 0.971
    GUE ref  |         |        | ~0.18  | ~0.015   |          |
    Poisson  |         |        |  1.0   | ~0.259   |          |

Strong level repulsion in both families; Poisson decisively excluded (observed 0.021-0.025 vs
0.259). Cross-family consistency: degree-1 and degree-2 statistics agree within sampling noise —
the universality claim's testable face. Narrative resistance on the small excess over pure GUE
(0.021 vs 0.015): the unfolding is crude (global per-L mean, not local density log(cond*t)/2pi)
and the first-zeros regime sits near the spectral edge where bulk GUE is not the exact law —
the simplest explanation is instrument bias, NOT a deviation claim. A sharper unfolding is the
upgrade path, not a finding.

## Instrument caveats (load-bearing)
1. Fourth mirror encoding trap logged: primitive stored as Python-literal text 'True'
   (not 't'/boolean) — the first run silently matched zero rows. Mirror traps so far:
   text-numerics, text-arrays {..}, bare-except parser, Python-literal booleans.
2. Degree-2 coverage: only ~10% of sampled rows carry >= 12 stored zeros.
3. Crude unfolding (global mean); edge regime; conductor mixing within family.

## Trace-vector record
problem_id: MATH-0062 | tier_probe: catalog-attack-empirical | answer_correct: n/a (instrument)
domain_constraints_detected: [low-zero-edge-regime, crude-unfolding, text-literal-booleans, deg2-coverage-10pct]
operations_used: [mean-spacing-normalization-first, ensemble-gap-pooling, repulsion-discriminator, cross-family-consistency, narrative-resistance]
counterexamples_tested: 83,388 gaps across 2 families; 0 Poisson-like families
kill_pattern: none (SUPPORTIVE) | failure_type: none | repair_available: sharper local unfolding
confidence_calibration: repulsion robust; fine-scale GUE agreement deliberately not claimed
residue: the mirror-encoding trap CATALOG (4 entries, now a checklist for every future attack);
  the stale-blocked-note pattern (triage gates rot — re-verify data dependencies before parking)
