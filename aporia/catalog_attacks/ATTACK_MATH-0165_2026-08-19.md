# Catalog attack: MATH-0165 — Keating-Snaith k^2 moment scaling (Dirichlet family, 400K central values)
Date: 2026-08-19 (Aporia loop) | Bucket: A | Instrument mode — not an oracle claim
Consumer: trace-vector corpus + kill ledger + GUE/RMT paradigm P04 + 00146 KS grounding

## Question and operationalization (blocked note re-verified: STALE)
Keating-Snaith/CFKRS: moments of central L-values grow like c_k (log q)^{k^2}. The triage
marked this blocked on a DuckDB zeros store — stale again (second instance of gates-rot):
the mirror's lfunc_lfunctions stores leading_term = the CENTRAL VALUE for
order_of_vanishing=0 rows. Family form of the test: over degree-1 (Dirichlet) L-functions,
M_k(q) = mean |L(1/2,chi)|^{2k} should scale with exponent k^2 in log q. Testable as the
slope of log M_k against loglog q: predicted slopes 1, 4, 9.

## Pre-stated readings
- SUPPORTIVE-K2-SCALING: fitted slopes ordered and near 1:4:9.
- AMBIGUOUS-TAIL: low moments consistent, high moments under-measured (the direction
  finite-sample tail truncation predicts).
- CHALLENGE: non-convex or inverted ordering (instrument-bug-first).

## Result: SUPPORTIVE at k=1,2; AMBIGUOUS-TAIL at k=3
400,000 central values parsed (0 failures; complex 'a+b*I' format handled explicitly).
    k | fitted slope | KS prediction
    1 |    1.329     | 1
    2 |    3.803     | 4
    3 |    6.677     | 9
k=2 lands within 5% of the predicted 4 — the sharpest reading. k=1 mildly over (narrow
loglog range compresses the regressor; family-average vs t-average constants differ).
k=3 undershoots (6.68 vs 9) in EXACTLY the direction the instrument's known bias predicts:
6th moments are dominated by rare large central values, and a finite insert-order slice
truncates that tail, deflating high-moment growth. Per instrument-vs-architectural doctrine,
the k=3 shortfall is read as instrument bias (predictable direction, observed direction),
not as a KS deviation. Growth is strictly convex in k (ratios 1 : 2.9 : 5.0) — qualitatively
the k^2 signature, quantitatively confirmed at k<=2.

## Instrument caveats (load-bearing)
1. Insert-order 400K slice (sampling-window caveat); loglog q is a compressive regressor.
2. Family-average vs the conjecture's original t-average: the k^2 exponent transfers, the
   constants c_k do not — constants deliberately not tested.
3. Complex-literal parse ('a+b*I') added to the parse repertoire; zero failures.

## Trace-vector record
problem_id: MATH-0165 | tier_probe: catalog-attack-empirical | answer_correct: n/a (instrument)
domain_constraints_detected: [tail-truncation-bias, compressive-regressor, insert-order-slice, family-vs-t-average]
operations_used: [complex-parse, quantile-binning, log-log-regression, bias-direction-prediction, convexity-read]
counterexamples_tested: 400,000 central values; k=1..3 moments
kill_pattern: none (SUPPORTIVE k<=2; AMBIGUOUS-TAIL k=3) | failure_type: none
repair_available: stratified/tail-weighted sampling for high moments
confidence_calibration: k=2 within 5%; k=3 deliberately not adjudicated (bias direction matches instrument prediction)
residue: the bias-direction-prediction pattern — when an instrument's known limitation predicts
  the SIGN of a deviation before measurement, an observed deviation of that sign is instrument
  evidence, not architecture evidence (kills a false KS-violation claim before it forms)
