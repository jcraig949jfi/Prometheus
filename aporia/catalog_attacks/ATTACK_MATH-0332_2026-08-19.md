# Catalog attack: MATH-0332 — Jones polynomial detects the unknot (2,977 knots, internal data)
Date: 2026-08-19 (Aporia loop) | Bucket: A | Instrument mode — calibration-grade consistency check
Consumer: trace-vector corpus + kill ledger + knot cluster (00123/R09-165) + mirror trap catalog

## Question and test spec (from triage)
Open conjecture: the Jones polynomial detects the unknot (no nontrivial knot has J(K)=1).
Verified in literature to ~24 crossings (Thistlethwaite). Our data cannot exceed that bound —
the triage itself flags this as a consistency check / blind-trial adjacent (Kairos downgrade
candidate), and that is how it is read.

## Data reality (trap #8 found)
topology.knots (prometheus_sci): 12,965 rows, ALL with jones_coeffs NOT NULL — but the 9,988
crossing-13 rows hold EMPTY arrays. NOT-NULL EMPTY ARRAYS masquerade as data (count() and IS
NOT NULL both pass; array_length returns NULL). Effective Jones coverage: 2,977 knots,
crossings 3-12 — EXACTLY the triage's stated count. The spec was right; the naive null-check
was wrong. Filed as mirror/internal-data trap #8.

## Pre-stated readings
- SUPPORTIVE-NO-TRIVIAL-JONES: zero nontrivial knots with J=1 in coverage.
- CHALLENGE: any hit (would be world news at <=12 crossings — instrument-bug-first, hard).
- VACUOUS: coverage degenerate.

## Result: SUPPORTIVE-NO-TRIVIAL-JONES + a theorem surfacing as a data law
- Zero nontrivial knots with trivial Jones among all 2,977 with data. Consistent with the
  conjecture and far inside the published 24-crossing bound (no novelty claimed — consistency
  check as designed).
- BONUS INVARIANT (unasked, Smyth-floor pattern): coefficient span tracks crossing number as
  span = c+1 exactly for low crossings (3->4, 4->5, ... 7->8), then avg drops below c+1 as
  crossing number grows (8: 8.67, 12: 12.10) — this is the KAUFFMAN-MURASUGI-THISTLETHWAITE
  theorem (span = c+1 for alternating knots, strictly less for non-alternating) visible as a
  database law, with the deficit measuring the non-alternating fraction. Second instance of
  reverse-calibration: a known theorem surfacing uninvited certifies the data pipeline.

## Trace-vector record
problem_id: MATH-0332 | tier_probe: catalog-consistency-check | answer_correct: n/a
domain_constraints_detected: [empty-array-not-null (trap 8), coverage-3-to-12-crossings, inside-published-bound]
operations_used: [signature-existence-first, trivial-jones-scan, span-vs-crossing-law, reverse-calibration-recognition]
counterexamples_tested: 2,977 knots; 0 trivial-Jones hits
kill_pattern: none (SUPPORTIVE) | failure_type: none | repair_available: ingest cn-13 Jones coeffs (empty arrays)
confidence_calibration: exact within coverage; no claim beyond 12 crossings
residue: trap #8 (empty-array-not-null) joins the checklist; the KMT span law as a free
  pipeline certification; cn-13 Jones data is MISSING-NOT-ABSENT (rows exist, arrays empty) —
  a data-completion task distinct from the Artin-style ingestion gap
