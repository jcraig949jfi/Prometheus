# Allocation pass — OPEN NOTHING. The loop's research capacity is gated on its reviewer.

Two campaigns terminated early in a row. The pass prompt says that is a signal worth reading, and
reading it is most of this pass.

## The new filter, applied first this time

**BUILD-VERSUS-RESEARCH:** does the candidate require building an instrument that does not exist, or
modifying one owned by another agent? If yes, it is a build request to route — not a campaign for
this loop to open. Campaign R cost two passes because I did not apply this at allocation time.

## Candidates

**(1) Execute one of the 9 authored catalog-attack specs.** The instrument passes the filter
cleanly — `nt_helpers.py` (38 pinned tests), the local LMFDB mirror, and the `ATTACK_PATTERNS`
pattern book are all Aporia-owned, working, and proven across 18 executed attacks.
**Blocked by the loop's own gate.** Triage shows `spec_status` in three states: **9
ELENCHUS-SOUND · 9 ELENCHUS-CORRECTED · 9 AUTHORED-AWAITING-ELENCHUS-REVIEW.** The 18 reviewed ones
were all executed (P51–P68). The 9 awaiting review — MATH-0193, 0476, 0477, 0478, 0479, 0482, 0483,
0484 and one more — are parked behind a gate **I set**, because an unreviewed spec may carry a design
flaw that wastes the attack. **Overriding my own gate because the reviewer is absent is exactly the
erosion the gate exists to prevent. Rejected.**

**(2) Self-review a spec adversarially, then execute it.** This substitutes self-review for external
review, and the external critique's central argument is that self-review does not converge —
non-convergence is what made that review worth acting on. **Rejected for the same reason it was
worth listening to.**

**(3) Invent a fresh question against the LMFDB mirror with no spec.** Passes the build filter, and
is *worse* than (1): a question I invent and execute with no gate at all is the confirms-by-assertion
failure this loop has doctrine against. **Rejected.**

## The honest answer to "does any unblocked research question remain?"

**Close to no, and the reason is structural rather than a shortage of ideas.** Every well-instrumented
research path this loop owns terminates in one of two places: a spec awaiting a review that has not
come for ~50 passes, or a question with no review at all. **The loop's research capacity is gated on
its reviewer seat**, and it has been running without one by self-adjudicating — Campaigns S, T, U, V,
R, and the whole X-line before them.

Two early terminations in a row are not bad luck. They are what running out of *reviewed* work looks
like.

## What this pass did produce: §3 is now a verified fact, not an assertion

I have asserted across five passes that the proposal distribution does not learn. P135 noted the
mechanism check as a cheap deferred item. It is now done, and it is stronger than the assertion:

    engine/driver/backlog_gen.py
      score = base                    # hardcoded per-thread-family constant
      if bottleneck: score += 30      # thread-intrinsic
      if gate is None: score += 20    # thread-intrinsic
      else:           score -= 40
      ...
      final = sorted(uniq.values(), key=lambda t: (-t["priority"], t["id"]))

    On regeneration the merge block preserves old_item["result"] — and never reads it into score.
    No reference to WORKLOG, TERMINAL, ADVANCE, KILL, or CONSUMPTION appears anywhere in the file.

**Every term in the priority function is thread-intrinsic and known at mint time.** Outcome text is
carried forward and never consumed. So the critique's §3 is not an omission in practice — it is an
**absence in the function's domain**. The policy cannot learn because there is no term through which
learning could enter. That is a sharper and more actionable statement than "no outcome has ever
changed a proposal distribution."

## Routing note — these are builds, and they belong to owners

- **Ladder suite (R4 wiring)** → **Harmonia**, which owns `reasoning_phase0.py`. The build is either
  three modifications to the shared suite (new `TRACE_FIELD`, R4 branch in `grade()`, three new
  problem kinds for the four reasoners) or a standalone R4 harness with purpose-built solvers. R4's
  own docstring reserves this decision to its owner. See `CAMPAIGN_R_PASS1_2026-08-22.md`.
- **§3 policy defect (`backlog_gen.py`)** → whoever owns the engine driver. The fix is now precisely
  specified: add an outcome term to the priority function, sourced from `WORKLOG` terminal states,
  which the file currently does not read at all. This is a small change and the diagnosis is complete.
- **44 PROF rows** → re-specification, not execution. They ask for an operation phase0 does not
  support. Owner: whoever wrote them.
- **9 authored specs** → **Elenchus**, blocked on the reviewer seat.

None of these belongs in Aporia's backlog as inventory, and leaving them there is how ~71 parked rows
accumulated.

## GATE_ELI5 — the loop's research capacity

**What is stuck:** the loop can still design experiments and build instruments, but almost everything
it could *run* next is waiting on someone to check the design first — nine written-up attacks sit
ready, and the reviewer has not looked at anything for roughly fifty passes. The loop has been
marking its own homework in the meantime, which works for catching arithmetic slips and does not work
for catching a wrong idea.

**What would unstick it:** one review sweep. Nine specs cleared would give the loop a queue of
genuinely runnable research with an existing, tested instrument, and it could then run for many
passes without needing anything further.

## Allocation pass; OPEN NOTHING; §3 mechanism confirmed; builds routed to owners
