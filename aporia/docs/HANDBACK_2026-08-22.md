# Allocation pass — OPEN NOTHING, and hand back. The binding constraint is no longer mine.

Zero campaigns live. The X-line is closed. This pass opens **nothing**, and the reason is not that
the loop ran out of ideas — it is that **every one of the loop's three highest-value next moves is
blocked on something only James can unblock, and none is blocked on my capability.**

## 1. What the loop's other commitments actually are

`CONSUMPTION.jsonl` holds **174 records, 157 of them outside the X-line**. Read properly, almost all
are *completed lanes*, not open ones:

    back-corpus metabolization      614 reports, COMPLETE (420 numbered + 194 recovered)
    catalog attack line             18/18 authored + 16/16 campaign, COMPLETE
    paradigm tier                   30/30, COMPLETE (with a corrected false completion)
    agent autopsy tier              21/21, COMPLETE
    AA-VERIFY registry              72/72 graded, COMPLETE
    salvage lanes (LETHE/IRIS/NOUS/SOPHIA/STYGIAN)   COMPLETE
    INFRA lane                      COMPLETE (P29-P32)

**Backlog: 782 rows, 138 DONE, 644 PARKED — of which 573 are generator-emitted** (CAT-MATH 502,
AA-VERIFY 56, RETRY-RL 15). **~71 genuinely distinct parked threads.** Of those, the ones with a real
named waiting consumer are few, and the good ones are blocked (below). The rest is inventory.

## 2. The external critique, six campaigns later

The review ranked **B > C→D > A > C > E**. Checked against what happened:

- **E (verb lattice)** — killed at P106 as endogenous-ontology-validation. Correctly stayed dead.
- **B (void map)** — executed at P107, one pass, terminal ADVANCE, claim retracted. **Finished as
  scheduled.** The review said B should be *finished*, not become a standing thread. It was.
- **C→D (calibrated signature search)** — this became the X-line. Six campaigns, closed KILL. The
  review's own §7 target (blinded relation recovery → Sleeping Beauties) was executed exhaustively
  on its first half. **The ranking held: it was the right thing to do, and it is now done.**
- **A (operator transport)** — considered at P114 and **rejected on resolvability**: n is fixed at 4
  by the small primes, so 3/4 correct predictions gives p = 0.0508 and the design has an ambiguous
  zone no achievable n can close. Not silently dropped — rejected with arithmetic on the record.
- **C alone** — the review said C has no independent backlog; anchors get built only when D demands
  one. D is now closed, so C has no demand. Correctly dormant.

**The ranking held up.** But §3 — the defect the review said **outranks the target question** — did
not get fixed:

> *"the policy that decides where to look next does not learn. Priorities are assigned at thread-mint
> time and no outcome has ever changed a proposal distribution."*

What P106 metabolized was §4 (campaign discipline: how long to stay on a thread). The **proposal
distribution** is still `backlog_gen.py` sorting by static mint-time priority. Across the X-line I
selected campaigns by reasoning about resolvability each pass — which is a human-shaped policy, not a
mechanism, and it dies with the session. **After 21 campaign-era passes, the review's top-ranked
defect stands.**

## 3. The three highest-value moves, and why each is blocked on James

**(a) Sleeping Beauty sweep — the review's own endpoint.** The X-line's one frozen-validated positive
(relations *are* recoverable from term vectors at ~156× chance) exists precisely to license this. It
is the highest-resolvability candidate available: relations verify **exactly** over ≥20 terms, so one
hit is a discovery and zero is a real null — no inference needed.
**Blocked:** `oeis_sleeping.json.gz` holds 212 curated anchors (Fibonacci, Catalan, primes), not the
68,770 zero-connectivity set. That set needs OEIS **cross-reference** data, and the only OEIS files on
disk are `stripped.gz` (terms) and `names.gz` (names).
**Unblock:** obtain OEIS xref/link data. **Cost to James:** one download. **What it buys:** the
program's north-star application, cheaply and exactly.

**(b) LAD-R12-RUN live leg — a built, verified, never-run instrument.** The R12 grader (conjecture
emission + falsification-test proposal, deterministic AST-whitelist scoring, no LLM judging) passed
17/17 self-tests and a 9-trial mock battery with **full three-way separation** (good 0.24–0.53
conjecture-quality; overfit and naive both 0.000, with overfit's *probes* retaining 0.57–0.65 —
probing and conjecturing skill dissociate).
**Blocked:** the live leg is gated on the standing budget decision, "$0 until ignition, DECISION
pending with James." **Cost:** cents. **What it buys:** the first live reading of whether a frontier
model can emit conjectures that survive a deterministic falsification grader — which is a *decision
about the program*, not another instrument.

**(c) Sweep-5 reviewer seat.** 23 reviews on file, **0 carrying a disposition**. Six campaigns,
3,275 held-out pairs and five doctrine rules have been adjudicated **entirely by checks I chose to
run against myself.** The review's own argument is that non-convergence is what makes external review
worth acting on — and there has been none for ~45+ passes.
**Unblock:** one Elenchus sweep. **What it buys:** the only thing self-review structurally cannot.

## 4. Why opening a seventh campaign anyway would be the wrong call

The review's §6 names the failure signature exactly: *"open-thread debt rising while decision yield
falls — manufacturing motion."* With the X-line closed, the good candidates blocked, and the residue
being generator-emitted inventory, opening something to avoid the appearance of stopping **is** that
failure. The doctrine's own continuation criteria disqualify it: infrastructure without a waiting
consumer does not count, and every unblocked candidate is either inventory or self-serving instrument
work.

**What would have to be true to open something next pass:** any one of (a), (b) or (c) unblocking —
or a genuinely new question that is not a variation on a closed line. Not a new representation. Not a
seventh benchmark.

## 5. What six campaigns established, for the record

**Positive, frozen-validated:** structural relations between OEIS sequences *are* recoverable from
plain term vectors at ~156× answer-set-adjusted chance, with a clean shuffled control.

**Negative, frozen-validated, three times over:** nothing beat a 20-term log-magnitude vector — not a
hand-designed 125-feature behavioral signature, not a learned closed-form linear metric, and not an
oracle-windowed correction. Each null carries an interval excluding the effect its campaign was
powered for.

**Method, which is the durable part:** five doctrine rules now in `LOOP_APORIA.md` — gate design
(thresholds must exceed measurement error), branches must partition, a verdict rule is an instrument,
thresholds fail in both directions, and oracle scoping (an oracle-based null is decisive where an
oracle-based positive is only an upper bound). Plus seven recorded firings of the wrong-population
lesson, the last of which fired in the direction that *costs* a finding rather than flattering one.

**What the program should do next, in my judgement:** unblock (a) and run the sweep — it is the
cheapest path from a validated retrieval method to an actual discovery attempt, and it is what the
whole line was built to serve. Unblock (c) before trusting any of the above further.

## GATE_ELI5 — the three unblocks

**(a) Sleeping Beauties.** *Stuck:* we want to search the ~68,770 OEIS sequences nothing else cites,
but the file we have lists 212 famous sequences instead, and neither of our two OEIS data files says
which sequences reference which. *Unstick:* get OEIS cross-reference data; the search itself is then
cheap and gives a straight count of confirmed finds.

**(b) R12 live leg.** *Stuck:* a grader that scores a model's conjectures and its proposed tests is
built, tested and proven to separate good from bad answers, but it has never been pointed at a real
model because live calls cost money and the budget is $0 pending a decision. *Unstick:* one budget
word; the run costs cents.

**(c) Reviewer seat.** *Stuck:* six campaigns of results have been checked only by me, and 23 filed
reviews have never been dispositioned. *Unstick:* one Elenchus sweep.

## Allocation pass; OPEN NOTHING; handback written; loop stays armed

---

## CORRECTION (P128, 2026-08-22) — unblock (a) was never blocked

**This document's claim that OEIS cross-reference data is not on disk is WRONG.**
`cartography/oeis/data/oeis_crossrefs.jsonl` holds **1,588,669 edges** and has since April. The
zero-connectivity set derives directly: **31,189 sequences with >= 25 terms** that appear as neither
source nor target of any edge.

I searched `prometheus_data/oeis/` and concluded from two files what a repo-wide search answers in
one command. The allocation pass that produced this document listed that exact shortcut as a
self-identified weakness and named this exact falsifier; it fired one pass later.

**Unblock (a) was mine to clear, not James's.** Campaign S opened on it — see
`aporia/search/CAMPAIGN_S_PASS1_2026-08-22.md`. Unblocks **(b)** the R12 budget word and **(c)** the
reviewer seat remain genuinely his.
