--- CONTEXT PACKAGE ---

Condition C. Nearest historical failures, retrieved by surface similarity to the
claim under assessment.

These are past cases only. No defenses, no graph structure, no bypass history,
no failure-family clustering, no edges. Each item stands alone, exactly as it
was recorded.

Retrieval: `{{RETRIEVAL_METHOD}}`, top `{{RETRIEVAL_K}}` by similarity.
Package size: `{{CONTEXT_TOKENS}}` tokens (cap `{{CONTEXT_TOKEN_CAP}}`).

{{NEAREST_FAILURES}}

---

These cases were selected because they look like the present claim. Looking
alike is not being alike. A retrieved case may have been dispositioned wrongly
at the time, and its resemblance to your claim may be entirely superficial.
Record in `context_items_used` only the items that actually changed what you
did.
