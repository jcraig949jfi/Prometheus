{
  "claim_id": "NAV-0038",
  "kind": "navigation",
  "proposition": "Let f be the sequence held by the arena. For every integer n with 1 <= n <= 600, f(n) mod 3301 is not 318.",
  "domain": "integers n with 1 <= n <= 600",
  "quantifiers": "for all n in [1, 600]",
  "hypotheses": [
    "f is an integer sequence; its defining coefficients are sealed",
    "f may be observed only through the metered interface",
    "f satisfies a linear recurrence of order at most 2"
  ],
  "observable": "sample(n) returns f(n) mod 3301; evaluate(n) tests the proposition at n"
}
