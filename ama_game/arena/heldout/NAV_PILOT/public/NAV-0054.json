{
  "claim_id": "NAV-0054",
  "kind": "navigation",
  "proposition": "Let f be the sequence held by the arena. For every integer n with 1 <= n <= 600, f(n) mod 2711 is not 1811.",
  "domain": "integers n with 1 <= n <= 600",
  "quantifiers": "for all n in [1, 600]",
  "hypotheses": [
    "f is an integer sequence; its defining coefficients are sealed",
    "f may be observed only through the metered interface",
    "f satisfies a linear recurrence of order at most 2"
  ],
  "observable": "sample(n) returns f(n) mod 2711; evaluate(n) tests the proposition at n"
}
