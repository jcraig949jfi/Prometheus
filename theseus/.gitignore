# Runtime artifacts — not source
corpus/*.jsonl
corpus/*.jsonl.gz
journals/batches.jsonl
journals/daemon_*.log
journals/handoff_daemon_*.log
journals/demand_*.jsonl

# mathlib import staging artifact (3MB+, regenerable via
# theseus.scripts.mathlib_signature_extractor). Curated top-200
# subset will land as a separate committed artifact.

# Offline caches for literature-mining generators (E2, E4, E5).
# Rebuild via theseus.scripts.fetch_* CLI modules.
cache/arxiv/*.jsonl
cache/lmfdb/*.jsonl
cache/conjectures/*.jsonl

# Bandit history (yield-score persistence across fires). Runtime state.
orchestration/bandit_history.json

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Pytest cache
.pytest_cache/
