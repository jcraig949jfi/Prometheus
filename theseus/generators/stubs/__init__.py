"""Theseus stub generators — scaffolded but not yet implemented.

All 35 stubs live in all_stubs.py. Each declares generator_id,
claim_kind, and description() but returns None from next().

When a stub is filled in, lift its class out of all_stubs.py into a
dedicated module under theseus/generators/ matching the naming
convention used by active generators (e.g. a2_statistical_correlation.py).
Then update theseus/registry.py to import from the new module.
"""
