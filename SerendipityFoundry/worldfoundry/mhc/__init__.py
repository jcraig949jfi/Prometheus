"""mhc -- Microstructure Hadron Collider (Microstructure Observatory) v0 prototype.

A SECOND instrument beside the Bump Detector: maximally sensitive to tiny,
distributed, interaction-dependent, genealogical, or outcome-free reproducible
structure -- without maximizing false discovery.

Core exploit of the substrate: worlds are deterministic and bit-exact
replayable, so
  (1) the null is NEVER "no difference" -- any effective perturbation either
      changes the trace or it does not. The null is NO COHERENT DIRECTION
      across independent (world x seed-block) units.
  (2) causal depth is measured by INTERVENTION, not estimation: perturb one
      observation element, replay the player OPEN-LOOP on the recorded
      observation stream, and see exactly which later actions change. An
      unused memory buffer contributes exactly zero; world chaos contributes
      exactly zero (the world is not re-stepped).

The Observatory never promotes organisms. Observatory encounters are sterile
for evolution. Losers are first-class. The cemetery is the product.

Offline prototype: no service, no port, no evolution, no campaign.
"""

MHC_VERSION = "mhc-0.1"
