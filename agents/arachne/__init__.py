"""Arachne — a population of math crawlers weaving one relationship fabric.

See pivot/math_crawlers_epiphany_2026-06-04.md for the vision + design.

Three non-negotiable epistemic rules (every edge):
  1. provenance — which crawler, which landscape, which operation
  2. operator-typed — the verb, not the noun
  3. born against a null — null_p: would a random crawler make this as easily?
"""
