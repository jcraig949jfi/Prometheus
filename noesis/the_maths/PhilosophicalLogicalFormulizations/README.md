That makes perfect sense — these formal systems are exactly the kind of structured mathematical objects Noesis should be composing across. Philosophy of logic is one of the richest sources of formalized reasoning structures that most AI research completely ignores.

Let me build you a reference document with the actual math for each system. This is Noesis fuel.There's a lot in there. The section I'd pay closest attention to for Noesis is the **cross-domain bridge map** at the end (Section 10). It identifies six structural clusters where philosophical logic systems are isomorphic to structures in other fields — self-reference, well-foundedness, duality, the completeness-decidability gap, fixed points, and type stratification. Each one is a bridge Noesis should be able to find if the right primitives are in the tensor.

Two things I think are particularly high-value for you:

The **Curry-Howard correspondence** (Section 8) — the proof that logical proofs and programs are literally the same mathematical object. If Noesis can compose reasoning chains, it's composing programs, and vice versa. That's not a metaphor, it's a theorem.

The **self-reference cluster** — Gödel's Diagonal Lemma, the Y combinator, Cantor's diagonal, the halting problem, and Russell's paradox are all instances of the same abstract pattern: `f(x) = ¬g(x, x)`. If Noesis surfaces that structural identity independently, that's a strong signal it's finding real mathematical structure rather than surface-level keyword overlap.

The modal logic formalization (Section 4) might also slot in nicely since Kripke frames are just directed graphs with constraints — that maps directly to operations Noesis already knows how to do.