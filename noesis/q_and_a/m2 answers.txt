M2 ANSWERS (Updated — covers both rounds of questions)
======================================================

1. ADAPTIVE FRAMEWORK: Keep the adaptive execution logic. Layer M2 changes on top.
   The adaptive approach is better than flat weights — M1's engineer already improved
   on the prompt spec. Your instinct is right:
   - Keep dynamic execution weight (drops to 0.10 when baseline > 0.5)
   - Re-weight the remaining budget: compression gets the largest share (0.31),
     then sensitivity (0.19), then novelty (0.19), then structure and diversity
     split the rest
   - The key M2 differentiator: compression is PRIMARY, sensitivity is NEW

2. 82 OPERATIONS: That's correct for a fresh clone without the generated/ wrappers
   being loaded. M1 has 580 because the daemon also loads organisms/generated/*.py
   (474 auto-wrapped numpy/scipy/etc functions). Check if the daemon's organism
   loader scans generated/ — if not, you may need to add that. The generated/
   directory should be in the repo from the latest push. If you only see 82 ops
   at startup, the loader isn't finding the generated organisms.

   Look at how M1's daemon loads organisms — there should be a block that loads
   from organisms/generated/ in addition to ALL_ORGANISMS. If it's missing on
   your clone, add it:

   # After loading ALL_ORGANISMS
   gen_dir = Path(__file__).parent / "generated"
   if gen_dir.exists():
       for py_file in gen_dir.glob("*.py"):
           if py_file.name == "__init__.py":
               continue
           # import and load organisms from generated files
           ...

   The generated/*.py files define operations as dicts, not as MathematicalOrganism
   classes. Read one to see the format, then load accordingly.

3. SENSITIVITY — use option (b): add custom_inputs parameter to _execute_chain_direct.
   And YES, only compute sensitivity for chains scoring > 0.4 on the initial pass.
   Don't burn 3x compute on chains that are going to score zero anyway.

4. OUTPUT_HASH FORMAT: You're right, it's a list of dicts not a string. Adapt the
   sensitivity function to compare the FULL list of hash dicts across perturbations.
   Simplest: serialize each result's output list to a string, compare strings.
   If all 3 perturbations produce identical serialized output, sensitivity = 0.
   If all 3 are different, sensitivity = 1. Partial matches = proportional.

5. DB SCHEMA: Yes, add score_sensitivity and score_compression columns. All sub-scores
   should be queryable for the cross-machine autopsy.

6. FRESH RUN: Confirmed. This is NOT resuming M1. Fresh DuckDB, fresh cracks log,
   independent experiment. M2's whole purpose is testing different scoring weights
   against M1's baseline.

Your observations are sharp — you clearly read the code. Use your best judgment on
implementation details. The goal is: does boosting compression + adding sensitivity
break the 0.659 quality ceiling that M1 is stuck at?

GO.
