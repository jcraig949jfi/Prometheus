M4 QUESTIONS
============

I've read the M4 prompt, the M2/M3 Q&A, the full noesis_daemon.py, the building blocks,
the generated organisms, base.py, and the M1 cracks format. Here are my questions:

1. NO PYTHON ON THIS MACHINE. Same situation M3 hit — `python` resolves to the Windows
   Store stub, not a real installation. No conda, no venv, nothing in the usual locations.
   Do you have a Python install I should use, or do I need to install Python + all deps
   (numpy, tensorly, duckdb, psutil, scipy, sympy, networkx, PyWavelets, filterpy, galois,
   powerlaw) from scratch?

2. BUILDING BLOCKS ALREADY PRESENT. The building_blocks/ directory is already in the local
   repo (organisms/building_blocks/) with all 20 .py files matching what's on the share.
   I'll use these as-is. Just confirming — no additional building blocks expected from M3
   beyond these 20?

3. M2'S SCORING: IMPLEMENTATION DETAIL. The current QualityScorer (line 534-545) already
   has the adaptive execution weight logic AND already distributes remaining weight as
   0.31*novelty, 0.19*structure, 0.19*diversity, 0.19*compression. So the existing code
   is ALREADY most of M2's scoring change — the execution weight drops to 0.10 when baseline
   > 0.5, and the remaining 0.80 is split with novelty getting the 0.31 share.

   What M2's scoring adds that ISN'T already here:
   - Input sensitivity (new signal, 0.19 weight)
   - Compression as PRIMARY (0.31 weight, currently novelty has the 0.31)

   My plan: Swap compression and novelty weights (compression gets 0.31, novelty gets 0.19),
   carve out sensitivity's 0.19 from the remaining budget, and add the sensitivity measurement.
   The final non-execution budget split would be:
     compression: 0.31, sensitivity: 0.19, novelty: ~0.19, structure: ~0.15, diversity: ~0.15

   Does that match your intent, or should I redistribute differently?

4. M3'S DEPTH + BB BONUSES ON TOP. The M4 prompt says to add depth_bonus and bb_bonus
   on TOP of M2's scoring. These are additive bonuses (up to 0.15 for depth, 0.10 for BB).
   Since the base quality is already 0-1 clamped, adding up to 0.25 on top could push quality
   well above 1.0 before clamping. Is that the intent — that BB chains with depth can score
   higher than the theoretical max of the base formula? Or should these bonuses replace some
   of the existing weight budget?

5. SENSITIVITY COST + THRESHOLD. M2's answers confirmed: only compute sensitivity for
   chains with initial quality > 0.4. I'll do a two-pass score — first pass without
   sensitivity, and if quality > 0.4, compute sensitivity and re-score. Chains below 0.4
   get sensitivity = 0. This keeps compute reasonable. Sound right?

6. WINDOWS MULTIPROCESSING. M2's answers mention using a "stdin/stdout subprocess worker
   pattern (persistent child process, hard-kill on timeout)" for Windows stability. The
   current daemon uses multiprocessing.Pool which can hang on Windows when workers get stuck.
   Should I implement the stdin/stdout pattern from scratch, or just use the existing Pool
   approach and see if it's stable enough? The existing code already has pool.terminate()
   + restart on timeout, which might be sufficient.

7. DB SCHEMA. I'll add score_sensitivity and score_compression columns (score_compression
   already exists in the schema but I'll make sure sensitivity gets stored too). Confirm?

8. RUN PARAMETERS. Prompt says --hours 30 --batch-size 50. This machine — how much
   CPU/RAM does it have? Should I adjust batch size based on what's available?
