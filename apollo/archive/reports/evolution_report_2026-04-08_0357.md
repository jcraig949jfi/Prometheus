# Apollo Evolution Monitor — 2026-04-08_0357
*Wednesday, April 08, 2026 at 03:57 AM EDT*

## Run Status

| Run | Gen | Best Acc | Med Acc | Ablation | Prims | LLM Calls |
|-----|-----|----------|---------|----------|-------|----------|
| v2_d2 (50/50 Run A) | 331 | +0.390 | +0.300 | 0.640 | 3.0 | 2070 (0% DS) |
| v2_d2b (50/50 Run B) | 347 | +0.380 | +0.320 | 0.640 | 2.0 | 2106 (0% DS) |

## DeepSeek Analysis

## Analysis Report: Apollo LLM Mutation Variants

**1. Comparison**
The data only includes the two 50/50 hybrid runs (v2_d2 and v2_d2b). The pure Qwen-7B (v2_d) and pure DeepSeek-chat (v2_d3) runs are missing from the provided data, making a full comparison impossible. Between the two hybrid runs, performance is nearly identical: both show best accuracy margins around +0.39-0.42 and median margins around +0.30-0.32. No statistically meaningful difference is apparent.

**2. LLM Mutation Survival**
No. The `elite_mutations` dict for both runs shows only `'drift'` and `'seed'` entries. There are no `'llm'` or `'annealed'` keys present. This indicates that **zero LLM-generated structural mutations are currently surviving to elite status**. Likely causes:
- LLM mutations are too disruptive, breaking functional logic.
- The evolutionary pressure (NSGA-III with accuracy and diversity) strongly favors smaller, incremental changes from drift/seed operators.
- LLM mutations may be failing compilation or evaluation silently.

**3. Trajectory Analysis**
Both runs appear to be in a **stable plateau**. Over the last 10 generations, best and median accuracy margins show minor fluctuations but no clear upward or downward trend. The best ablation delta (0.64-0.66) has also remained constant. This suggests the search has converged to a local optimum that current operators cannot escape.

**4. Anomalies**
- **LLM Usage Anomaly**: `llm_alive=0` in health stats and `{'primary': 0, 'alt': 0, 'deepseek': 0}` in LLM stats suggest the LLM mutation operators are **not being selected** by the Adaptive Operator Selection (AOS). The batch counts are high, but these may be from earlier generations or a logging artifact.
- **Archive Saturation**: Both archives are at 500 (capacity), indicating the diversity objective is active but may be limiting further exploration.
- **Low Structural Diversity**: `structs=22-31/50` shows many invalid/compiled-out organisms, reducing effective population size.

**5. Recommendations**
1. **Fix or Replace LLM Operators**: Since they are not producing elites, either:
   - Drastically reduce the mutation magnitude (e.g., restrict to single primitive swaps or minor wiring edits).
   - Implement a validation filter to discard non-compiling or radically destructive changes before evaluation.
   - Replace with a simpler pattern-based or probabilistic structural operator.

2. **Break the Plateau**:
   - Increase the `NCD weight` above 0.5 to force more exploration, even at temporary accuracy cost.
   - Introduce a periodic "shake-up" (e.g., random reintroduction of archived solutions) every 50 generations.

3. **Next Restart Adjustments**:
   - Start with a higher initial mutation rate for LLM operators to ensure they are tested.
   - Log the *actual* operator selection rates per generation to diagnose AOS behavior.
   - Include the missing pure-LLM runs (v2_d and v2_d3) for a complete comparison.

**Conclusion**: The LLM structural mutation experiment is currently failing—the operators are not competitive. The evolutionary search has stalled. Prioritize fixing the mutation operators before drawing conclusions about LLM capability.

## API Usage
- Analysis tokens: 1241 in / 722 out / 1963 total
- DeepSeek balance: $4.19 USD remaining

---
*Auto-generated by evolution_monitor.py*
