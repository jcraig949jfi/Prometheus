# S7 -- THE GATE: readout (2026-09-13)

Order: `roles/Archaeon/prompts/2026-09-12_seasons/S7_ORDER.md` (sha256 e4bbb346...379b). Preregistration
`S7_PREREG_2026-09-13.json` (sha256 7d7f012e...eecc, commit e44574ad9, before the trial). Candidate `archaeon/producer/s7_gated.py`,
CANDIDATE_VERSION 39093752300ebb85 (sha256 over s7_gated.py + s6_endgame.py + s4_producers.py). Rows: `S7_RESULTS_ELIGIBLE_L8/L9`,
`S7_RESULTS_UNIVERSE`, `S7_RESULTS_L10` (all 2026-09-13); verdict `S7_VERDICT_2026-09-13.json` from `S7_VERDICT_2026-09-13.py`.

## 1. S6 preserved

S6's frozen verdict CHEAP_REPAIR_WITH_REGRESSION (ab2d40cef) stands. S7 tested only the composition S6 did not preregister.

## 2. Frozen candidate

GATED_V2W(s) = S6 A_v2w_0(s) if 5 <= N(s) <= 18 else G(s); N from the exact block solver. A composition of the existing
functions (`s6_endgame.produce_refined(..., "v2w", 0.0)` and `s4_producers.produce_G`), nothing rewritten; gate overhead (one
exact inference) charged under its own nested budget and reported per proposal; firing flag `extra["gate"]["active"]`.
Seeds: the S6 seed inputs verbatim, so every pool is identical to S6's and G's census costs replicate S6 exactly (asserted on
every state); the candidate differs from S6 A_v2w_0 by the gate alone.

## 3. Identity outside the gate (instrument invariant)

Tests (`test_s7_gated.py`): on 800 real decision states the candidate equals G exactly (probe, tie class, objective, pool
size) whenever N < 5 or N > 18 and equals A_v2w_0 exactly inside; fossil-order permutations never change the decision; no
signature carries a target, an index or the oracle. In the trial the invariant was re-checked at EVERY candidate decision
against an independent call: L 8 eligible 1375 decisions (354 inside, 1021 outside), L 9 1122 (276 / 846), universe 2095
(455 / 1640), L 10 (see 11) -- 0 violations.

## 4. The seven S6 N 30 canaries

All seven: root decision identical to G (the S6 refinement is shown to differ at that root and is suppressed), census cost
identical to G's (3.1000 x6 at L 8, 3.2667 at L 9; S6 A_v2w_0 had 3.17-3.27 / 3.33). The refinement fires downstream in the
gate (36-41 of ~90 decisions per world) without changing the cost. Bar met.

## 5. Inside-gate comparison (complete census, S6 eligible endgame)

L 8 (258 states, 165 with gap): improved 27, unchanged 230, worsened 1 (evs:914ee3e416304d0d, N 12, +3.2 %); recovery 0.405
(= S6 A_v2w_0 on the same states); aggregate probe reduction 2.1 % (81 probes over 1828 census positions).
L 9 (210 states, 113 with gap): improved 49, unchanged 161, worsened 0; recovery 0.700 (= S6); aggregate 4.1 % (140 probes).
By N: the recovery is the N 6 class (L 8 10.7 of 14.8; L 9 18.7 of 20.7) plus N 9 (all of it) and small pieces at 8, 14, 15;
nothing at N 5, 7, 10, 11, 16; the one loss is at N 12.

## 6. Regression audit and the exact falsifier

Inside-gate bar: EXACT non-regression over L 8 union L 9 -- FAILED by one L 8 state. Anatomy (exact): state
evs:914ee3e416304d0d, N 12, depth 1 under root L8_prod2_a7_b1_uA5_uB1, fossils 00000000/0.25, 00110010/0.375, 11111110/0.625.
G's exact tie class has five probes, ALL with partition shape {6,4,2}, all with the same v2w (25/3), all with Q* = V* = 2.5833
(every one of them is an optimal first probe). The candidate picked a different member than G because v2w is computed in
floating point (8.333333333333334 vs 8.333333333333332) -- a rounding difference of 2e-15 split a mathematically exact tie and
overrode G's lexicographic rule; the candidate's OWN continuation from that probe then costs 2.6667 against G's 2.5833
(V* 2.5833). Two facts follow: (a) the frozen "exact-tie" refinement is not exact as implemented -- v2w must be a rational
(numerator over N) for equal partitions to tie; (b) even with Q*-optimal first probes, a policy's cost depends on its own
continuation, so the exact-tie repair is not monotone in general. This is the same single state S6 A_v2w_0 lost at L 8.
Universe (233 roots): 0 worlds worsened >= 2 % (bar met), 1 worsened at all (L8_prod2_a1_b7_uA0_uB3, N 35, +0.8 % = +1 probe
over 35, from a downstream in-gate decision), 49 improved >= 2 %, aggregate -2.0 % (205 probes over 3286).

## 7. G->O gap recovery (oracle values reused from S6 / S5 rows; provenance recorded)

L 9 0.700, L 8 0.405 -- identical to S6 A_v2w_0 on the same states: the gate changed nothing inside the endgame (by
construction) and only removed the outside-gate regressions.

## 8. Full-universe safety

Universe bar met (0 worlds >= 2 %; aggregate cost 2.0 % below G). The seven S6 regressions are gone; the residual +1/35 world is
an in-gate effect, not a gate leak.

## 9. Work accounting (deterministic units; gate overhead included)

Median units per decision: L 8 G 506 vs candidate 1934 (ratio 3.8), L 9 418 vs 3535 (8.5), universe 274 vs 589; maximum
18,743; refinement invoked on 53 % / 51 % / 40 % of decisions (the rest are outside the gate or single-member windows).
Total incremental units: L 8 10.4 M for 81 probes saved (129 k units per probe), L 9 17.8 M for 140 (127 k), universe 23.1 M
for 205 (113 k). All work bars met (median <= 25 k, ratio <= 10, max <= 100 k). Oracle work: none fresh for L 8/L 9/universe
(reused); L 10 fresh, ledger in the file.

## 10. Leakage / permutation controls

Signature audit, fossil-order permutation (3 permutations x 120 states), per-decision identity audit, census/solver equality
at every state, arm-below-V* scan on every row: all pass. The gate reads N only (one call, one field).

## 11. Generalization / exposure labels

L 8: development set of S5-S6 (exposed to every statistic and rung). L 9: S5 producer-level and S6 confirmatory exposure; the
S7 composition itself was never run on it before this trial. L 10: never run with any producer or statistic before S7; the
same state family, no tuning surface (the candidate cannot change). Result: 110 worlds, 1252 targets, 871 candidate decisions (176 inside the gate, 695 outside), 0 identity violations; worsened 0 (any amount), improved 17, aggregate cost 1.4 % below G (53 probes); sealed oracle completed on all 110 worlds within the 50 M-unit budget (94.7 M fresh units, 177 M memo hits, 5452 s): 36 gap worlds, recovery 0.683. The L 10 bar is met.

## 12. Frozen-rule verdict: GATE_FAILS_TO_ISOLATE

Instrument invariants: all pass (0 identity violations). Canaries: pass. Full universe: pass. Inside-gate exact
non-regression: FAILS on one L 8 state (N 12, +1/12 probes). By the frozen precedence the verdict is GATE_FAILS_TO_ISOLATE.
The improvement bar (L 9 recovery 0.700, aggregate 4.1 %) and the work bars would have passed.

## 13. Production: NOT LICENSED

The rule allowed production only on LICENSED_ENDGAME_REPAIR. Nothing is integrated; the live tick is unchanged.

## 14. The exact falsifier and the disposition

The gate does exactly what it was asked (outside-gate identity, canaries clean, universe clean). What fails is inside: the
frozen refinement's tie definition is floating-point, and a 2e-15 rounding difference breaks a true tie against G's rule in
one state whose downstream cost then regresses. This was not fixed in S7 (the order forbids altering v2w). ARCH-45's residue
(a) -- the N-gate -- is now TESTED and works as a gate; the residue moves one step down: an exact-rational v2w tie test (so
that equal partitions remain ties and fall to G's lexicographic rule) is the single change a successor season would
preregister, with the identical gate, bars and universes. Filed as ARCH-46 residue (a); the near-tie and deep-search
frontiers stay filed as ARCH-46 (b), untouched. W and M remain retirement candidates; not rerun.
