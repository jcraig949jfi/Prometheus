PROMPT -- HARMONIA -- FIRST REAL PROMETHEUS INTEGRATION (2026-09-04)
Preserved per section 20 of the directive. The full directive text was
delivered in-session; its operative requirements are enumerated and
answered point-by-point in REVIEW_PACKET_FIRST_INTEGRATION.txt (items
1-30 map to the directive's required packet contents; sections 0-19 of
the directive are mirrored by: component-state inheritance (packet 2,4),
single question (packet 16), selection rule (packet 3), no-modification
(packet 5), world creation (packet 6), adapter boundary (packet 7),
placement gate (packet 8-9), bounded encounter (packet 10),
checkpoint/restore/replay (packet 11), run/event anchor (packet 12-13),
PEW writes (packet 14-15), no-scores rule (packet 28), independent
read-back (packet 16), identity ledger (packet 17), fail-closed tests
(packet 19), branch discipline (packet 2,25), live-host qualification
(packet 20), failure preservation (packet 21), stop condition + verdict
(packet 1,29).)
