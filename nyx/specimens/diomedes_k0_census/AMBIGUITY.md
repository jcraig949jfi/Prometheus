# diomedes K0 census -- alternative readings, preserved

Cut A (Nyx): zero organs; the module is doctrine + textbook statistics.

Cut B: 'conditional headroom' IS a defensible ORGAN because the SPECIFIC
baseline (marginal action rate, per state, AUC-scored) is a mechanistic
choice with independent behaviour that a consumer (any seat about to make
a state-conditional claim) could run unchanged. Nyx refused it on the
duplicate control; a second Chopper who weights 'independent behaviour
runs' above 'textbook recurrence' would admit it. Preserved.

Cut C: the module is an INSTRUMENT, and instruments are a category the
Chop Shop schema does not have (neither organ nor pressure nor
scaffolding). If the schema forces every instrument into RECURRENCE or
POLICY, that is a schema finding, not a specimen finding. Recorded for the
calibration document; not acted on (charter XV: formats change when a
consumer's failure says so).
