# hypothesis shrinker -- alternative cuts, preserved not merged

Cut A (Nyx, CUT-2): six organs (acceptance, order, adaptive step, choice tree,
standalone shrinkers, coupled-value passes); the class's pass list is a
schedule (POLICY); structural passes are a coupled cluster with the strategies.

Cut B: the Shrinker CLASS is one organ and everything in shrinking/ is its
toolbox. Rejected by Nyx because the toolbox runs without the class (P-a) and
the class does not run without the engine; preserved because the ruling
warns against cutting across valid boundaries for its own sake.

Cut C: the specimen is (shrinker x strategy encoding) and cannot be cut
below that: P-b shows the consumer's outcome is decided by the encoding's
labels and order, not by any pass. Under this cut the transferable products
are c07 (a value minimiser) and the PRESSURE; everything in the class is
ecosystem.

Cut D: the operator's word SHRINKER denotes the standalone package only
(shrinking/), and the class is the ENGINE's shrink phase -- a different
specimen. Under this cut N1 yielded exactly one organ (c07, with c01 and c03
inside it) and the rest belongs to a 'conjecture engine' specimen.

Combinations noticed, NOT built: running c07 directly on Proteus programs
with size_key as left_is_better would be a different minimiser; recorded as
an observation only (anti-reassembly). The pressure asks for the outcome;
Proteus decides how.

## Correspondence recorded 2026-09-12 (not scored as a prediction)

The combination declined above ('c07 run directly on Proteus programs under size_key') is exactly the one Archaeon attempted in #200 and found INTERFACE_INSUFFICIENT: Collection.shrink requires a flat sequence of int-orderable elements; the site holds nested tuples with string operators; no reversible encoding exists and none was built. This was not a preregistered prediction and is not counted as one. It is evidence that preserving the unresolved interface ambiguity, rather than resolving it by building the bridge, was the right disposition.
