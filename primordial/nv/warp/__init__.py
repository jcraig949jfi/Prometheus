"""N3 (builder W): Warp as a numba successor for world kernels.

Runs in its own venv C:/Users/jcrai/lab/nv-venv-w (warp-lang 1.17); gw-venv has no warp.
Inside this package `import warp` is the NVIDIA library (absolute import), not this directory.
"""
