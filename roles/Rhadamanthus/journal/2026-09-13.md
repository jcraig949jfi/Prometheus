# Rhadamanthus journal, 2026-09-12..13 (harvest charter, day 2-3)

Charter: prompts/2026-09-11_harvest/CHARTER_verbatim.md (9ac363786).
Receipt: prompts/2026-09-11_harvest/RECEIPT.md.  Workshop under
engine/necropolis/workshop/ on branch rhadamanthus/tool-harvest-2026-09-11.

- Seven scouts (A-G) returned 351 leads over the live tree, docs, git
  history and dead agents; 433 files import-screened in fresh processes
  (267 OK / 10 FAIL / 156 not attempted because import has side effects).
  Techne delivered its quartermaster inventory (comms #193, 44 rows).
- Registry unit fixed at "meaningfully invokable forensic instrument"
  (charter III): 93 rows in TOOLS.jsonl, generated by registry_source.py
  from the control run, never typed.  Status is DERIVED from the control
  results by the generator and re-checked by validate_workshop.py, whose
  seven negative self-tests all reject their mutations.
- Control harness tests/run_controls.py: 194 cases / 57 groups / 10 kinds.
  Canonical run at 2d97a6c66: PASS 174 / FAIL 8 / INFO 9 / ERROR 3
  (first run at 52ac826b5: 173 / 8 / 10 / 3; the validator self-test
  moved INFO to PASS once the registry existed).
  Nothing was patched to turn a FAIL green (charter IV).  The FAILs are
  the evidence rows for NT-001 (grading oracle answer-key leak, CHEAT x2 +
  CORRUPT) and NT-010 (Pollux sorted-rank identity, SYNTHETIC_NULL +
  PERTURBATION), plus NT-090 (icarus holdout R1 red, undiagnosed).
- Eleven adapters (plus _common) written (LAYER: NECROPOLIS ADAPTER headers; each names
  the original logic it wraps).  Five batteries (charter VII).  Coroner
  contract proposed in CORONER_RUN.md with coroner_run.py enforcing the
  refusals; three rulings left to James (R-CR-1..3).  Not legislated.
- Pollux CR-001 plan written (charter XII) and PRE-KILLED by its own
  positive control: the split-half read in the lower KS tail passes at
  rate alpha for any iid continuous gap law (measured 0.0133 at alpha
  0.00556, 0/30 resolvable).  Filed as PRF-1 with a refile recommendation;
  hitl_status PROPOSED, never executed.
- FRANK-002 blocker found in the ledger, not the packages: 0 / 6,661 rows
  of agents/hephaestus/ledger.jsonl carry judge_version or api_state, so
  the frozen-judge precondition is unattestable from bytes.
- Harness mistakes, recorded: (1) long python heredocs through the shell
  tool fail with "unexpected EOF" and write nothing; now a memory rule
  (write the script file, then run it).  (2) build_consumers.py first
  pass treated importer dicts as strings; every non-empty row errored
  identically, which is how it was caught; fixed and re-run (about 10 s
  per row, 90 rows).
- Disputes with Techne recorded, not resolved: hypothesis and Lean are
  PRESENT under the canonical checkout (H:/Python312) and MISSING /
  HOST_LOCAL under this worktree's python 3.14.  Both readings are kept.
- Commit discipline: two workshop commits by explicit paths on the
  harvest branch; build_consumers.log and __pycache__ excluded; staged
  diff scanned for credentials and drive letters before each commit
  (host paths appear only inside measurement records in
  controls_result.json).
