# CLERIC challenge -- case POLLUX

Court date: 2026-09-14.  Role: Cleric (hostile reader).  Tree (read-only):
D:/Prometheus-worktrees/rhadamanthus-base-role.  Scratch:
C:/Users/James/AppData/Local/Temp/claude/D--prometheus/73954ab7-6db7-4809-b206-7543b299f62b/scratchpad/cleric/
(abbreviated S/ below).  Prior verdicts (dossiers, autopsies, audits) are read at
zero weight; where they report a ledger COUNT I cite them as a record of a count,
not as a verdict.  Every factual sentence carries a tag per COMMON_RULES.

## 0. files_opened / instruments_executed / excluded_by_charter

files_opened (all under the tree unless absolute):
- roles/Rhadamanthus/prompts/2026-09-14_court/case_pollux/COMMON_RULES.md, CLERIC_prompt.md, NECROMANCER_report.md (the three permitted)
- charon/agents/pollux/daemon.py (1-614, full), charon/agents/pollux/__init__.py (1-10)
- charon/agents/.gitignore; charon/agents/_base.py (grep only); charon/agents/__init__.py (grep only)
- charon/agents/erebos/daemon.py (50-70, 87-112, 150-170, 305-335 + grep); charon/agents/erebos/generators/g01_intersection.py, g04_survivor_tightening.py (grep)
- charon/agents/hecate/daemon.py (315-365 + grep)
- charon/agents/stygian/executor.py (215-240); charon/agents/stygian/daemon.py (grep 190-243); charon/agents/stygian/loaders/ (ls)
- ergon/learner/greedy/sources.py (158-204, 384-392); ergon/learner/greedy/corpus/manifest_v1.json (18-30, 56-64)
- scripts/charon_loop.py (77-104, 169-198, 201-242 + git log)
- docs/state.json (Charon_Loop, Stygian, Hecate, Erebos, Pollux blocks, via a stdlib json dump)
- prometheus_math/databases/mahler.py (147-153, 437-468; READ ONLY, never imported)
- prometheus_math/databases/_mahler_data.py (1-60, 2237-2296, 2300-2345, 2356-2515, plus a textual regex census of the 178 curated dict literals; READ ONLY, never imported)
- pivot/COMPONENT_DOSSIERS_2026-06-24.md (286-310, 385-410); pivot/aporia_ecosystem_status_and_next_steps_2026-05-30.md (57); pivot/charon_swarm_diminishing_returns_2026-05-25.md (55-59, 184-189, 252); pivot/charon_swarm_2026-05-27.md (11, 35, 60-93); pivot/INFRA_RECOVERY_PLAN_2026-06-23.md (grep); pivot/erebos_whitepaper_v1_2026-05-27.md (grep)
- aporia/docs/program_audit_2026-06-10.md (118-119, 192 via grep); engine/ledger/AGENT_AUTOPSIES.jsonl (line 18)
- charon/ORCHESTRATION_FORENSIC_MAP_2026-08-31.md, charon/BACKLOG.md, charon/agents/erebos/sprint1/phase3/real_residue_smoke.py, seam_sufficiency_audit.py, charon/agents/stygian/loaders/composition_g16_lehmer_extremum.py (grep "pollux": zero hits in each; see section 2)
- git: `git log` on charon/agents/pollux/daemon.py, _mahler_data.py, charon_loop.py; `git log -1 --format=%B` 8c619443a and 43b094552; `git diff --stat 43b094552 HEAD -- charon/agents/pollux`
- S/recovered/8c619443a793/daemon.py, S/recovered/43b094552462/daemon.py (recovered by instrument)

instruments_executed (my own runs; S/run_instruments.py -> S/instruments.json):
- engine/necropolis/workshop/adapters/git_history_census.py: history() on 11 paths, recover() on the 2 daemon SHAs, deleted_python_files(grep="pollux"), deleted_python_files(grep="charon/agents")
- engine/necropolis/workshop/adapters/consumer_trace.py: trace() on 7 targets
- engine/necropolis/workshop/adapters/literal_verdict_lint.py: lint_paths() on 10 files (9 tree files + recovered c1 daemon)

re-implementations (stdlib, seed 914, S/reimpl_cleric.py -> S/reimpl_cleric.json; S/smyth_pair_literals.py; S/salem_pisot_hybrid.py).  The last two operate on the curated-tier Mahler literals READ from _mahler_data.py source text (not on the mahler.py module, its deepcopy, or the Known180 gz), plus synthetic fill where the gz would be needed; they are labelled RE-IMPLEMENTATION (READ literals) below.

excluded_by_charter (hit by grep/trace, not opened): engine/necropolis/** other than the three adapters (necropolis dossiers and the workshop harvest rows surfaced by consumer_trace for "charon/agents/pollux", "stygian_priority", "charon_loop.py"); roles/** other than the three permitted files; comms/**; C:/Users/James/.claude/**; prometheus_math/databases/_known180_raw.gz (a dataset of mahler.py; never decompressed); every *.env / *Key* / *secret* / *credential* / keys.py path.  No database, Redis, or network access; no EW_DB_HOST set; charon.agents.pollux never imported; nothing run in the background.

## 1. PROPOSITION AUDIT (P1..P25)

P1. CONFIRMED.  [READ daemon.py:414-418] `n = min(len(a_vals), len(b_vals)); a_paired = sorted(a_vals)[:n]; b_paired = sorted(b_vals)[:n]` -- two independent sorts, one truncation, no join key.

P2. CONFIRMED, and the "no exact ties" restriction is vacuous.  [READ daemon.py:218-224] the rank is `sorted(range(n), key=lambda i: vs[i])` with stable order and no tie correction, so on an already-sorted list the rank vector is 0..n-1 whatever the ties.  [RE-IMPLEMENTATION R6] 200 trials with 4-valued and 2-valued inputs (heavy exact ties): corr_raw in [0.9999999999999998, 1.0000000000000002].  [RE-IMPLEMENTATION R1] 400 independent Uniform trials, same interval.  Ties make corr_raw MORE certainly 1.0, not less.

P3. CONFIRMED.  [READ daemon.py:253-268] with cr = 1.0: `no_correlation_observed` needs `not raw_sig` = False, so unreachable; `sign_flips` needs cn < 0 and |cn| < 0.8; `survives` needs cn >= 0.30; else `attenuates`.  [READ daemon.py:427-434] survives -> PROMOTED; sign_flips or no_correlation -> REJECTED; else UNVERIFIED.  Added shape the Necromancer did not state: a PERFECT anti-correlation of the gap sequences (cn <= -0.8) is classified `attenuates` -> UNVERIFIED, i.e. weaker than a mild one.  [RE-IMPLEMENTATION R5d] constant A vs B with monotonically shrinking gaps: cr 1.0, cn -1.0, verdict UNVERIFIED.  Also, n = 10 gives 9 gaps < 10 so corr_norm is None and the verdict is `correlation_unmeasurable` -> UNVERIFIED [READ daemon.py:421-424, 255-256].

P4. CONFIRMED.  [READ daemon.py:127-209] `_load_subset` contains no random, sample, shuffle, or time-dependent call; every kind filters `all_below(...)` or `smyth_extremal()` and converts to float.  [READ mahler.py:457-468] `all_below` is a deterministic filter + sort over MAHLER_TABLE.  [READ daemon.py:417-418] both inputs are sorted before use, so input order is irrelevant.  [EXECUTED git log _mahler_data.py] three commits only, last 12a76bade 2026-05-03, so the table did not change during Pollux's life.  [QUOTED engine/ledger/AGENT_AUTOPSIES.jsonl:18] the ledger census recorded "per-pair distinct-outcome census 9x distinct=1" -- cited as a count, not a verdict.

P5. WEAKENED in detail, conclusion stands.  [RE-IMPLEMENTATION R7] A drawn from a rising density (density ~ (x-1)^3 on [1,1.3]), B = 2.6 - A, n = 100, 200 trials: REJECTED 173, UNVERIFIED 26, PROMOTED 1 (mean cn -0.171).  The Necromancer's T4 (Uniform A) gave `attenuates`; on non-uniform A the same anti-monotone relation gives `sign_flips`.  So the specific yield "attenuates" is a property of the Uniform choice, not of anti-monotonicity; the claim that the statistic does not measure the A-B relation is confirmed, and strengthened: the verdict is a function of the two marginal density shapes only (R2/R3 below).

P6. CONFIRMED, with an n-dependence the report omits.  [RE-IMPLEMENTATION R1] 400 trials, independent Uniform(1,2), sizes 10-400: PROMOTED 11 (2.75 %), REJECTED 211 (52.75 %), UNVERIFIED 178 (44.5 %).  [RE-IMPLEMENTATION R1b] stratified: n in [10,20] PROMOTED 47/300 (15.7 %); [20,50] 16/300; [50,150] 0/300; [150,400] 0/300.  The PROMOTED-from-noise rate is a small-n effect; the pairs with n in the teens (smyth_extremal_vs_rest has n = 16, P15) sit where noise promotes one trial in six.

P7. CONFIRMED.  [READ daemon.py:1-614; grep -i "null|shuffle|surrogate|baseline|random|permut|calibrat"] the only hits are the docstring phrase "null result" (14) and "SELF_AUDIT_NULL" (582); no control of any kind.

P8. CONFIRMED.  [READ daemon.py:120-124] SETTLE_THRESHOLD = 5, CORR_SIGNIFICANT = 0.30, CORR_FLIPPED_DELTA = 0.20; [READ daemon.py:214-215, 424] the n >= 10 floor appears twice (Spearman and n_norm).

P9. CONFIRMED.  [READ daemon.py:301-315] settle requires the last 5 verdicts identical and in ("PROMOTED", "REJECTED").

P10. CONFIRMED.  [READ daemon.py:317-340] `settled_pairs.append(...)` precedes the `candidate_pool_idx >= len(CANDIDATE_POOL)` return; the removal at 336-339 is only on the non-exhausted branch.  [RE-IMPLEMENTATION R8a] 9 pairs all settle-able, 240 ticks: 204 settled rows, the exhausted branch hit 199 times, first at tick 37; every one of the four final pairs is re-appended every 4 ticks.

P11. CONFIRMED.  [READ daemon.py:525] the "CANDIDATE_POOL exhausted -- active rotation shrunk" log fires when `_promote_settled_replace` returns None, and by P10 the rotation is unchanged in that case.

P12. CONFIRMED.  [EXECUTED git_history_census.history] daemon.py: 2 commits, 8c619443a (2026-05-24T03:10:34-04:00) and 43b094552 (2026-05-25T01:46:55-04:00); __init__.py: 1 commit.  [EXECUTED git diff --stat 43b094552 HEAD -- charon/agents/pollux] empty.  [EXECUTED git_history_census.deleted_python_files grep="pollux"] []; grep="charon/agents": [].

P13. CONFIRMED.  [RE-IMPLEMENTATION, textual] the `_spearman`, `_mean_spacing_normalize` and `_classify` function bodies of S/recovered/8c619443a793/daemon.py and HEAD daemon.py compare byte-identical; the sorted/truncate pairing lines and the three thresholds also match.  [EXECUTED git_history_census.recover] c1 sha256_lf d17218e5... (16792 bytes), c2 44270dac... (26307 bytes).  [QUOTED git log 43b094552] the commit message lists rotation, loaders, settle logic, Stygian enqueue; nothing about the statistic.

P14. CONFIRMED.  [EXECUTED git log -- prometheus_math/databases/_mahler_data.py] 48337bc3e, 3f68561da, 12a76bade, all 2026-05-03.  [READ _mahler_data.py:2312-2340] every Known180 row is appended with salem_class True, is_smyth_extremal False; the in-code comment says M < 1.3; [READ 2320-2323] Lehmer at deg 10 is flagged lehmer_witness.

P15. CONFIRMED and sharpened; part of the NEEDS_CORONER it defers is derivable.  [READ _mahler_data.py textual census] curated tier: 178 `"mahler_measure"` literals; 16 with is_smyth_extremal True; 107 with salem_class False; 67 with M in [1.30, 1.50].  [READ _mahler_data.py:2356-2511] arxiv_promoted tier: 16 rows, M literals 1.3022688051 .. 1.556014485, is_smyth_extremal forced False, salem_class default True; 15 of them lie in [1.30, 1.50].  [READ 2320-2323 + INFERRED] Known180 rows are all M < 1.3 and >= Lehmer's 1.17628, so: the 1.30-1.50 side of narrow_band has exactly 82 rows (67 + 15); the extremal side of smyth_extremal_vs_rest has 16; the non-Salem side of salem_vs_pisot has 107.  Hence n_paired = 16 for smyth_extremal_vs_rest and 107 for salem_vs_pisot, derivable without execution, because the other side is larger (~8,600 and ~8,500 rows).  The Known180 counts per degree / band remain NEEDS_CORONER (the gz is a dataset of mahler.py and stays closed).

P16. CONFIRMED.  [EXECUTED ls charon/agents/pollux/] __init__.py, __pycache__, daemon.py only; state/ and artifacts/ absent; charon/agents/_shared_queues/ holds __init__.py and __pycache__ only.  [EXECUTED git_history_census.history] state, artifacts, kill_ledger.jsonl, stygian_priority.jsonl, pollux_survivor.py: 0 commits, not in HEAD.  [READ charon/agents/.gitignore] _rotation_state.json, _swarm.pid, _logs/, _shared_queues/*.jsonl, */state/, */artifacts/.

P17. CONFIRMED.  [READ docs/state.json Pollux block] last_tick_at 2026-05-30T15:55:23.901164+00:00; last_tick_stats verdict UNVERIFIED, corr_raw 1.0, corr_norm 0.0817, kill_pattern attenuates, pair_scanned even_deg_vs_odd_deg, pair_settled_this_tick false, elapsed 0.39; session_started_at 2026-05-26T17:39:27.793918+00:00; session_cycle_id 19ac6398-....

P18. CONFIRMED and strengthened.  [READ docs/state.json] last ticks, all 2026-05-30 UTC: Pollux 15:55:23, Erebos 15:59:24, Stygian 16:03:24, Lethe 16:07:58, Acheron 16:11:58, Moros 16:16:53, Hecate 16:20:54, Nephele 16:24:54; Charon_Loop last_run agent "nephele" started 16:24:54.59, elapsed 0.14, error null; all nine share session_cycle_id 19ac6398 and session_started_at 05-26T17:39:27Z.  [INFERRED] the eight stamps are one full rotation at ~4-min spacing ending on the LAST agent of AGENT_NAMES, and Pollux is the first of the eight; the process stopped between Nephele's tick and the next Stygian tick (due ~16:28:54), during the sleep.  [READ scripts/charon_loop.py:77-104] the PID lock is released only in `finally`; [QUOTED pivot/COMPONENT_DOSSIERS_2026-06-24.md:290-305] "PID-lock file points at pid 24132 which is dead" -- a stale lock means `finally` never ran, i.e. hard termination (kill -9, host reboot, power) rather than an exception or a clean exit.

P19. CONFIRMED.  [READ charon/agents/stygian/executor.py:221-236] `if problem_id.startswith("POLLUX-"): return _emit_short_circuit_row(... kill_pattern="stygian_pollux_survivor_loader_pending", reason="pollux_survivor_loader_not_yet_implemented" ...)`.  [EXECUTED ls charon/agents/stygian/loaders/] 41 files, none named pollux_survivor.py.  [EXECUTED consumer_trace "pollux_survivor"] 33 hits, 0 importers.

P20. CONFIRMED.  [READ ergon/learner/greedy/sources.py:161-202] `gold = False  # kill_ledger entries are, by construction, the ones that did NOT survive`, applied to every row that has corr_raw and corr_norm; `outcome="rejected"`; registered at 388.  [READ manifest_v1.json:21-27, 58-62] pollux yielded 286, skipped 0, by_outcome rejected 286; train by_source pollux 86.  [READ daemon.py:457-465] the ledger payload carries corr_raw/corr_norm in claim_payload as sources.py expects.

P21. CONFIRMED.  [READ hecate/daemon.py:328-333] `_GEN_PREFIX_RE = r"^(stygian|pollux|nephele|moros|acheron|lethe|hecate|a\d+|h\d+|c\d+|f\d+)_+"` stripped before cross-generator membership.  [READ docs/state.json Hecate block] mi_crossgen 0.0034, mi_crossgen_z 0.982, total_records 13392, n_crossgen_kps 4, ledger_found lists charon\agents\pollux\state\kill_ledger.jsonl.

P22. WEAKENED (true as a number, vacuous as evidence).  [EXECUTED literal_verdict_lint on 10 files incl. the recovered c1 daemon] files_scanned 10, n_findings 0 -- replicated.  [READ literal_verdict_lint.py docstring/VERDICT_WORDS] the lint matches a fixed verdict vocabulary; Pollux's return strings are `pollux_*` kill_patterns and the verdict is chosen by a conditional at 427-434, so the instrument cannot see the literal-constant behaviour that matters here (corr_raw is a literal 1.0 in effect, P2) and 0 findings would have been the result even if `_classify` returned a constant `pollux_*` string.

P23. CONFIRMED.  [READ __init__.py:1-10] "only agent in the swarm with operator class != Theseus"; [READ daemon.py:1-29] "second non-Theseus ... (Stygian is the first)"; [QUOTED git log 8c619443a] "6th non-Theseus operator class" and "third operator class after Theseus and Stygian".  Three inconsistent self-descriptions, not two.

P24. CONFIRMED.  [QUOTED git log 8c619443a] "Live verification: deg10_vs_deg12 scan emitted REJECTED with pollux_sign_flips_under_normalization (raw 1.0, normalized -0.2235).  The scale_vs_shape discipline firing on first scan -- exactly what Pollux is for."  Under P2 the raw 1.0 was certain before the scan.

P25. CONFIRMED.  [EXECUTED consumer_trace] "charon/agents/pollux" 1214 hits, 3 importers (charon/agents/_base.py:301 + 2 under excluded paths); "kill_ledger.jsonl" 103 hits, 0 importers; "stygian_priority" 59 hits, 4 importers; "POLLUX_SURVIVOR" 9 hits, all in charon/agents/stygian/daemon.py; the non-excluded consumer files match the Necromancer's list.  Note [READ charon/agents/erebos/generators/g01_intersection.py:40-57, g04_survivor_tightening.py:117-126] G01 requires BOTH Stygian and Pollux substantive pools non-empty; G04 draws from pollux_substantive.

Counts for section 1: CONFIRMED 23 / WEAKENED 2 (P5, P22) / FALLS 0 / UNTESTABLE_WITHOUT_EXECUTION 0 / NOT_EXAMINED 0.  Every numbered proposition was checked against my own read, instrument run, or re-implementation.  The propositions are the report's strongest part; the attacks below land on what the report concluded FROM them.

## 2. EVIDENCE THE NECROMANCER MISSED

2.1 The verdict map is misread in the report body.  [READ daemon.py:427-434] `"PROMOTED" if kp == survives else "REJECTED" if kp in (sign_flips, no_correlation_observed) else "UNVERIFIED"`; [QUOTED git log 8c619443a] "pollux_no_correlation_observed (REJECTED)".  The report's section 2.6 lists no_correlation -> UNVERIFIED.  P3 (correct) contradicts 2.6 (incorrect) inside the same report; the error is harmless to the outcome (the branch is unreachable, P3) but shows the body was not re-read against its own propositions.

2.2 The curated tier is dominated by near-duplicate runs, and that -- not any spacing structure -- decides at least two seed verdicts.  [READ _mahler_data.py textual census] the curated tier holds 29 rows with M in [1.0, 1.0000000000000067] (4 at the literal 1.0, the rest 1.0 + a few ulp), 21 rows at 1.17628081825991xx (Lehmer's number, three at the identical literal), 16 rows at 1.32471795724474xx (Smyth's number, all is_smyth_extremal True, literals differing in the last 2-3 digits), plus blocks of ~10 near 1.618 and ~10 near 1.8393.  These are legitimately distinct polynomials with mathematically identical Mahler measure, encoded with different rounding residues.  Under P1's sorted-truncated pairing the smallest-M block of each side sits at the head of the paired sequence, so `_mean_spacing_normalize` sees runs of 0 .. 6e-15 gaps whose RANKS are the ordering of rounding residues.
  (a) smyth_extremal_vs_rest, computed entirely from READ literals [RE-IMPLEMENTATION (READ literals) S/smyth_pair_literals.py]: A = the 16 Smyth rows, B = the 16 smallest non-Smyth rows (all cyclotomic ~1.0 rows, since Known180 >= 1.176 and arxiv >= 1.3022); n = 16; A gaps 0 .. 6.2e-15, B gaps 0 .. 6.7e-16; corr_raw 1.0, corr_norm -0.0786, kill_pattern sign_flips, verdict REJECTED.  Whatever the historical row said, on this pair the instrument rank-correlated two sequences of floating-point rounding residues of hand-entered/computed literals.  This is MEASUREMENT_ERROR in the strict sense (the instrument reads the encoding of the input, not the quantity) and the report does not offer it.
  (b) salem_vs_pisot [RE-IMPLEMENTATION (READ literals + synthetic fill) S/salem_pisot_hybrid.py]: B = the 107 non-Salem curated literals as read; A = the 21 Lehmer-block literals + 8,431 synthetic values in [1.1763, 1.30] standing in for the unread Known180 rows; n = 107.  200 trials each: uniform fill PROMOTED 185/200 (mean cn 0.376); rising-density fill PROMOTED 144/200; falling-density fill PROMOTED 198/200.  CONTROL: collapse the Lehmer block to one entry -> REJECTED 108 / UNVERIFIED 92, mean cn -0.017.  CONTROL 2: collapse the near-duplicate runs on the B side (n drops to 38) -> UNVERIFIED 180 / PROMOTED 20.  The PROMOTED "survivor signal" on salem_vs_pisot is manufactured by the co-location of two near-duplicate runs (20 Lehmer gaps vs 28 cyclotomic gaps) at the head of both sequences; it is insensitive to what the 8,431 real Salem rows look like and vanishes when either run is deduplicated.  [QUOTED pivot/COMPONENT_DOSSIERS_2026-06-24.md:395-405] names salem_vs_pisot as one of the three PROMOTED pairs (cited as a record of ledger content).
  (c) narrow_band_1.10_1.20_vs_1.30_1.50 [INFERRED]: side A begins with the 21-row Lehmer block; side B (82 rows) begins with 11 arxiv rows at real gaps (1.3022 .. 1.3242) followed by the 16-row Smyth block; index-aligned, A's near-zero run occupies positions 0-19 while B's occupies ~11-26, which predicts a negative rank correlation, i.e. sign_flips / REJECTED -- the verdict the 05-27 memo shows for this pair [QUOTED pivot/charon_swarm_2026-05-27.md:70].  Confirmable only with the Known180 band count (NEEDS_CORONER C-A).

2.3 The statistic is NOT content-free; it is a comparison of quantile-density profiles, which is a different question from the one the daemon names.  [RE-IMPLEMENTATION R2] two INDEPENDENT samples from the same non-uniform density (density ~ (x-1)^3): mean corr_norm +0.22 at every n from 20 to 1000, PROMOTED 28 % at n = 50, 19 % at n = 100, 8.5 % at n = 300, 1 % at n = 1000 (it converges to ~0.23 < 0.30, so PROMOTED becomes rare at large n even though the signal is real).  [RE-IMPLEMENTATION R3] opposite density trends: REJECTED 200/200 at n = 300, mean cn -0.18.  [RE-IMPLEMENTATION R4] same shape, |A| = 50 vs |B| = 5000 (B truncated to its 50 smallest): PROMOTED 30 %, same as untruncated.  So `survives` means "the two subsets' reciprocal-density profiles by quantile trend the same way", `sign_flips` means "they trend oppositely", and the threshold 0.30 is above the large-n plateau of a genuine same-shape signal.  The Necromancer's T1 (Uniform vs Uniform) is the no-trend worst case and cannot distinguish "no content" from "content on the wrong question".  The report's C5 (shuffled-split null) would test the wrong thing: a random split of one subset is a SAME-SHAPE null and by R2 would PROMOTE at a rate comparable to or above the historical pairs -- which would be read as "no signal" when it actually says "the instrument detects marginal shape".

2.4 The "39 PROMOTED is not a multiple of 5" puzzle has an arithmetic solution the report did not attempt.  [QUOTED pivot/COMPONENT_DOSSIERS_2026-06-24.md:399-405; AGENT_AUTOPSIES.jsonl:18] ledger: 286 rows, PROMOTED 39 / REJECTED 86 / UNVERIFIED 161; artifacts deg18_vs_deg20 x54, even_deg_vs_odd_deg x56; PROMOTED pairs = deg14_vs_deg16, salem_vs_pisot, small_deg_vs_large_deg; each pair one outcome.  [EXECUTED git log] c1 committed 2026-05-24T07:10Z, c2 2026-05-25T05:46Z: 22.6 h of c1 (no settle logic, 4 TEST_PAIRS, 7-agent 28-min cycle per both commit messages) = 48.4 ticks = ~12 per pair.  [INFERRED] c1 period: 2 PROMOTED pairs x 12 = 24 PROMOTED, 2 REJECTED pairs x 12 = 24 REJECTED.  c2 period: five settled pairs x exactly 5 rows (deg10 R, deg14 P, salem_vs_pisot P, smyth R, small_vs_large P) = 15 PROMOTED + 10 REJECTED; four unremovable final pairs deg18 (54, UNVERIFIED), even_vs_odd (56, UNVERIFIED), narrow_band (52, REJECTED), lehmer_witness (51, UNVERIFIED).  Totals: PROMOTED 24 + 15 = 39; REJECTED 24 + 10 + 52 = 86; UNVERIFIED 54 + 56 + 51 = 161; rows 48 + 25 + 213 = 286.  All four totals reproduce, and the decomposition requires smyth_extremal_vs_rest to have been REJECTED -- the same verdict my literal computation gives (2.2a).  [RE-IMPLEMENTATION R8b] the settle machine with these per-pair verdicts yields final_active = {deg18, even_vs_odd, narrow_band, lehmer_witness} and 5 rows for each settled pair.  This is a retrodiction with free assumptions (continuous c1 running; c2 state survived the 05-26 restart) and is offered as a falsifiable reading of the record, not a fact; it is checkable in minutes from the ledger (C-B).

2.5 Exhaustion was fast and the record shows the pool was exhausted within about a day of c2, not "~5 weeks".  [RE-IMPLEMENTATION R8c] with settle-able seeds, candidate_pool_idx reaches 5 at tick 34 and the exhausted branch first fires at tick 37 (~20 h at a 32-min cycle; 4 seeds settle at ticks 17-25).  [QUOTED pivot/charon_swarm_2026-05-27.md:70] narrow_band (pool index 3, needing 4 prior settle events = tick ~25) was already being scanned in the 12-hour window reported on 05-27.  [QUOTED pivot/charon_swarm_diminishing_returns_2026-05-25.md:55-59] the "~5 weeks" projection is off by a factor of ~30 for deterministic pairs.  Consequence for the death certificate: Pollux spent roughly its last three days (05-27 to 05-30) re-scanning four pairs it could never settle, with the log claiming the rotation had shrunk (P11).

2.6 The halt is swarm-wide AND mid-cycle AND hard.  (Section 1, P18.)  The report reports "swarm-wide"; it does not report the ordering, the stale lock, or that Pollux was not the last agent to tick.

2.7 Two consumers DID read the Pollux ledger during its life; what they got is quantified in the record.  [READ docs/state.json Erebos block] pollux_substantive_recent 286 at 05-30T15:59Z (7-day lookback), i.e. every row was within Erebos's window; [READ docs/state.json Hecate block] ledger_found includes the Pollux ledger; total_records 13392.  So CONSUMER_ABSENT in the literal sense is false for Hecate and Erebos; what is true is that consumption produced nothing (mi_crossgen z 0.98; no evidence in the tree of a G01/G04 composition that survived anything -- NOT_EXAMINED, the Erebos ledger is gitignored).  The rival should be named CONSUMER_INERT, not CONSUMER_ABSENT (section 3b).

2.8 The author observed the failure signature on 05-25 and repaired the wrong layer.  [QUOTED git log 43b094552] "Pollux 100% deterministic" is listed as a diminishing-returns observation, and the response was patch 4 (settle + candidate pool) plus patch 2 (PROMOTED -> Stygian queue with a loader that "belongs in loaders/pollux_survivor.py as v0.7 work").  Determinism on a static table with a deterministic instrument is not a rotation problem; the pool patch converted 4 constant outputs into 9 constant outputs and the queue patch fed PROMOTED rows to a short-circuit (P19).  This is the historical Frankenstein-that-changed-nothing (3f).

2.9 Files the consumer_trace surfaced that the report did not list carry nothing: charon/ORCHESTRATION_FORENSIC_MAP_2026-08-31.md, charon/BACKLOG.md, charon/agents/erebos/sprint1/phase3/real_residue_smoke.py, seam_sufficiency_audit.py, charon/agents/stygian/loaders/composition_g16_lehmer_extremum.py -- [EXECUTED grep -i pollux] zero hits in each; they were matched on other targets ("charon/agents", "charon_loop.py").  pivot/INFRA_RECOVERY_PLAN_2026-06-23.md:48-50 [QUOTED] classes charon_loop.py among "Redis-host setters ... retired Redis. Dead config" -- relevant to whether a revival could even start (3e).

## 3. ATTACKS

### 3a. DEATH CERTIFICATE (last tick 2026-05-30T15:55:23Z; stopped by swarm-wide halt; DESIGN_ERROR)

Attack: the time is right but the "what stopped it" is under-specified, and the certificate mixes the moment of death with the cause of the disease.
Evidence: [READ docs/state.json] the eight last ticks form one complete rotation ending on Nephele at 16:24:54Z with Charon_Loop.last_run error null; the loop stopped in its sleep after the last agent of the list.  [QUOTED COMPONENT_DOSSIERS_2026-06-24.md:290-305] stale PID lock (pid 24132 dead) and the Redis host ".176 dark".  [READ scripts/charon_loop.py:77-104, 169-198] exceptions inside `run_once` are swallowed and would not stop the loop; the lock is removed only in `finally`.  So the stopper was external and hard (process kill / host event), not any agent's exception and not Pollux's own code.  The certificate should read: "process charon_loop.py (pid 24132) terminated without cleanup between 16:24:55Z and ~16:28:54Z on 2026-05-30; Pollux's last tick 15:55:23Z; never restarted (docs/state.json heartbeat_age 8,997,555 s at 09-11)".  Whether the host was rebooted, the process killed by an operator, or the machine lost power is NOT_EXAMINED (no host logs in the tree).
Outcome: WEAKENED.  Time stands; mechanism of stopping is more specific than the report says and is INFRASTRUCTURE by class; the certificate's cause-class field should carry the halt mechanism separately from the disease.

### 3b. RECONSTRUCTED CAUSE (primary DESIGN_ERROR; rivals CONSUMER_ABSENT, INFRASTRUCTURE)

Attack 1 -- is DESIGN_ERROR actually primary?  Evidence: P1-P6 hold (section 1), so the pairing cannot answer "does the correlation between A and B survive normalization" because there is no correlation between A and B to begin with.  But [RE-IMPLEMENTATION R2/R3] the normalized leg does measure something real (same-vs-opposite quantile-density trend), so the design is not a tautology on the normalized leg; it is the right statistic for a question nobody asked.  DESIGN_ERROR stands as primary, with the correction that the report's "no content" framing (T1 only) is too strong; the design error is a QUESTION-INSTRUMENT MISMATCH, not an empty instrument.  Outcome: STANDS (as class), WEAKENED (as characterised).

Attack 2 -- the strongest rival is not offered.  Evidence: section 2.2 -- the recorded PROMOTED on salem_vs_pisot is reproduced from READ literals under every fill of the unread side and destroyed by deduplicating one block; smyth_extremal_vs_rest is a rank correlation of rounding residues.  This is MEASUREMENT_ERROR: the input table encodes one mathematical value as many ulp-distinct floats and the gap statistic reads that encoding.  It is independent of the pairing bug (fixing P1 with a real join would not fix it; a deduplicated table with the P1 bug intact would still be tautological).  It explains the specific historical verdicts, which DESIGN_ERROR alone does not (DESIGN_ERROR predicts "some verdict"; MEASUREMENT_ERROR predicts WHICH).  Outcome: the report's rival ordering FALLS; MEASUREMENT_ERROR is the strongest rival and is close to co-primary.

Attack 3 -- CONSUMER_ABSENT is misnamed.  Evidence: section 2.7; Hecate and Erebos consumed every row.  What was absent is a consumer that could turn a Pollux row into anything (Stygian short-circuit P19; ergon inverts all labels P20; Hecate MI at noise P21).  Outcome: WEAKENED -- rename to CONSUMER_INERT and demote below MEASUREMENT_ERROR.

Attack 4 -- INFRASTRUCTURE is the cause of the halt, not of the failure.  Evidence: 3a.  Outcome: STANDS as the halt mechanism; correctly not primary.

Attack 5 -- HYPOTHESIS_FAILURE is not available on this record.  Evidence: no ledger, no per-pair n, no execution; the only computable pair (2.2a) says nothing about Mahler measures and everything about rounding.  Outcome: STANDS (the report's refusal to call HYPOTHESIS_FAILURE is right).

### 3c. FAIR test -- argued as strongly as the record allows

The daemon ran 286 scans over 9 pairs on unchanged data with an unchanged statistic [P4, P12-P14], emitted a typed row for every scan [READ daemon.py:457-505], and its verdict rule is stated in code [P3].  The normalized leg is a genuine statistic of the two marginals [R2/R3]: had the two subsets had opposite quantile-density trends it would have said REJECTED with probability -> 1 as n grew (R3: 200/200 at n = 300), and had they shared a trend it would have said PROMOTED at a rate far above the Uniform null (R2 vs R1b at matched n: 28 % vs 5 % at n ~ 50).  Its stated purpose in the record was not Lehmer-adjacent mathematics but instrumentation: [QUOTED git log 8c619443a] "Necessary precondition for Hecate's cross-generator MI to become non-zero", and on THAT question it delivered an answer: rows landed, Hecate consumed them, mi_crossgen stayed at z 0.98 [P21], and Aporia's autopsy could state the mechanism (replicated constants carry no variation).  The 05-25 observation "100 % deterministic" [QUOTED git log 43b094552] shows the author could read the outcome from the record.  Under this reading the experiment was fair for the question "does a second operator class with these emissions move Hecate's MI" and answered NO, and fair for "do these subset pairs share a quantile-density trend" and answered per pair.
Outcome: WEAKENED.  The argument holds only after the question is rewritten to what the instrument measures; on the question the daemon's own docstring names ("correlation ... survives normalization" between the two subsets [READ daemon.py:1-29]) it does not hold.

### 3d. NOT a fair test -- argued as strongly as the record allows

(i) The raw leg cannot vary [P2]; a test one of whose two arms is a constant cannot compare the arms.  (ii) The normalized leg's verdict on the pairs that can be computed from the record is a function of rounding residues and duplicate-run placement [2.2a-b], so the recorded PROMOTED/REJECTED labels are not about Mahler measures at all.  (iii) There is no null, chance floor or calibration [P7]; the 0.30 threshold sits above the large-n plateau (~0.23) of a genuine same-shape signal [R2], so even on the rewritten question the instrument would miss a true positive at large n and promote noise at small n [R1b: 16 % at n in 10-20].  (iv) The PROMOTED arm's only downstream test was a short-circuit that never loaded the pair [P19]; a test whose positive outcome is discarded unexamined cannot be answered in the positive direction.  (v) After ~1 day the rotation was frozen on four unsettleable pairs and re-emitted the same four rows ~50 times each [2.5, 2.4]; 213 of 286 rows carry zero new bits.  (vi) The one acceptance check in the record [P24] verified a value that was guaranteed before the scan.  (vii) The follow-up patch [2.8] treated determinism as a rotation problem.
Outcome: STANDS.  On the question the daemon states for itself, the historical run could not have answered it in either direction.

### 3e. SALVAGE VALUE

Components the Necromancer names as survivable (from its section on salvage; I do not repeat its wording):
- `_mean_spacing_normalize` [READ daemon.py:238-250]: 13 lines; its one non-trivial behaviour is `if mean_gap == 0: return gaps`, which is exactly the path that turns duplicate runs into index-ordered ranks [R5a: two constant vectors -> corr_norm 1.0 -> PROMOTED].  Lifting it without a tie policy lifts the artifact.  Not worth lifting as-is.
- `_spearman` [READ daemon.py:212-235]: no tie correction (average ranks), so it is not Spearman on tied data; every scipy-free alternative in the tree would need the same fix.  Not worth lifting.
- `_load_subset` with 7 selector kinds [READ daemon.py:127-209]: thin filters over `all_below`; its only value is the pair taxonomy, which is where the duplicate-run problem lives (Smyth block, Lehmer block).  Worth lifting only together with a deduplication-by-(rounded M) step, which the loaders do not have.
- The settle/replace machine [READ 301-340]: the exhausted-branch behaviour is a bug (P10/P11) and the whole machine is inapplicable to a deterministic instrument (any settle-able pair settles in exactly 5 ticks; anything else never settles).  Not worth lifting.

Things not named that might be worth lifting:
- The finding itself: the curated Mahler table's encoding (one value, many ulp-distinct literals) as a hazard for every gap-based or spacing-based statistic in the tree -- Stygian's composition loaders over Lehmer bands and Mahler cubes [EXECUTED ls loaders: composition_g02_g04_lehmer_band_high.py, composition_g18_lehmer_degree_band.py, composition_g23_lehmer_degree_decay.py, ...] draw from the same table and are NOT_EXAMINED here.  A table-level dedup-or-flag is the salvageable object.
- The verdict-shape record: "9 pairs, 9 constant outcomes, 286 rows" is a clean measured instance of emission-without-information usable as a calibration case for liveness auditors (Pronoia-class), independent of Pollux's statistic.
- The Erebos `_filter_substantive_recent` seam [READ erebos/daemon.py:87-106]: it counts rows by verdict string with no per-(pair, instrument-version) novelty gate, so 286 constant rows counted as 286 substantive inputs; the gate the autopsy asks for belongs there, not in Pollux.

### 3f. FRANKENSTEIN COUNTERFACTUAL

Most likely to have changed the historical result -- layer: input table / loader; mutation: deduplicate each subset by M rounded to 1e-9 (collapse the cyclotomic, Lehmer, Smyth, 1.618 and 1.8393 blocks to one row each) before pairing, leaving the statistic untouched.  Expected change: [RE-IMPLEMENTATION S/salem_pisot_hybrid.py controls] salem_vs_pisot flips from PROMOTED (185/200) to REJECTED/UNVERIFIED (108/92, mean cn -0.017); smyth_extremal_vs_rest becomes unmeasurable (A collapses to 1 row, n < 10, verdict `correlation_unmeasurable` -> UNVERIFIED) [READ daemon.py:214-215, 255-256]; narrow_band and salem-adjacent pairs lose their head-of-sequence runs.  The "2 survivor signals" [QUOTED 05-25 memo:252] and the "39 PROMOTED" [QUOTED 06-10 audit:118] would not have existed; the c2 Stygian-queue patch would have had nothing to enqueue; the 06-24 dossier's proposed null test would have had no PROMOTED pairs to run on.  Cost: one line in `_load_subset`.  Confidence on direction: high for the two computed pairs, INFERRED for the rest.

Looks like a repair but changes nothing -- layer: statistic; mutation: replace the index-aligned Spearman on gap sequences with a permutation null (the report's C5 shuffled-split null, or the 06-24 dossier's "random-pairing / matched-marginal" null) while keeping the sorted-truncated inputs and the duplicate runs.  Expected change: none that matters -- a within-subset shuffle does not change `sorted(vals)` [P4], so the observed statistic is invariant to it; a split-half null of the same subset is a same-shape null [R2] that reproduces the duplicate runs on both halves and PROMOTES at a comparable rate, so the "z vs null" would report the artifact as null-robust.  The historical analogue is c2 patch 4 [2.8]: it looked like a repair of "100 % deterministic" and produced 5 more constants.

## 4. MY OWN VERDICT

Cause class: DESIGN_ERROR (primary) with MEASUREMENT_ERROR as co-cause, ranked above CONSUMER_INERT (the report's CONSUMER_ABSENT) and above INFRASTRUCTURE (which is the halt mechanism, not the cause).  Grounds: the pairing makes the raw arm constant (P1-P2, replicated) and asks the normalized arm a question it does not answer (R2/R3 show what it does answer); and the two seed verdicts that can be computed from the record are decided by rounding residues and duplicate-run placement in the curated table (2.2), a defect of the measurement that survives any fix of the pairing.

FAIR / UNFAIR: UNFAIR on the question the daemon states for itself (3d); WEAKLY FAIR only on the rewritten question "do the two marginals share a quantile-density trend" and on the instrumentation question "does a second operator class move Hecate's MI" (3c), both of which were answered and neither of which is the hypothesis of record.

Hypothesis status ("a correlation between Mahler-measure subsets that survives mean-spacing normalization carries real shape signal"): UNTESTED by this experiment.  Not falsified, not supported; the record contains no measurement of a correlation between two subsets.

Where I differ from the Necromancer and why:
1. Rival ordering.  The report offers CONSUMER_ABSENT first, INFRASTRUCTURE second, and does not offer MEASUREMENT_ERROR.  I put MEASUREMENT_ERROR first among rivals because it is the only class that retrodicts WHICH verdicts the record shows (2.2, 2.4) and because it is checkable from read literals (done for one pair, controlled for another).  CONSUMER_ABSENT is factually wrong as named (2.7).
2. "No content".  The report's T1 supports "every verdict reachable from noise", which I replicate (P6), but the report slides from that to "the statistic carries nothing"; R2/R3 show it carries marginal-shape information with a threshold set above the large-n plateau.  This changes the salvage answer (3e) and kills the report's C5 proposal (below).
3. Death certificate detail.  Same time, more specific mechanism (3a): mid-sleep hard termination after a full rotation, stale lock, never restarted.
4. The 39-PROMOTED anomaly is not an anomaly under a c1+c2 decomposition (2.4); the report left it as "confounded".
5. Section 2.6's verdict map (2.1) -- a body/proposition contradiction inside the report.

What I do NOT dispute: P1-P25 as facts (23 confirmed outright), the primary cause class, the refusal to call HYPOTHESIS_FAILURE, and the refusal to recommend revival.

## 5. NEEDS_CORONER

C-A (replaces the Necromancer's n_paired deferral in P15).  What: with mahler.py loaded, count Known180 rows per degree (10, 12, 14, 16, 18, 20), per parity, in [1.10, 1.20], and in the "small vs large" degree bands used by CANDIDATE_POOL [READ daemon.py:83-114]; report n_paired for all 9 pairs; report the number of DISTINCT M values at 1e-9 rounding in each subset.  Expected output: 9 x (|A|, |B|, n, distinct_A, distinct_B).  Meaning: distinct << n on a side confirms the duplicate-run mechanism for that pair; distinct == n on both sides for a PROMOTED pair (deg14_vs_deg16, small_deg_vs_large_deg) would mean the artifact does NOT explain that pair and R2's density-trend reading is the live explanation there.

C-B (new).  What: from the historical ledger (charon/agents/pollux/state/kill_ledger.jsonl on M2, if it still exists; 435 KB per the 06-24 dossier), tabulate rows by (pair_name, verdict, corr_norm, batch_id date).  Expected output: 9 rows of (pair, verdict, single corr_norm value, count, first_date, last_date).  Meaning: counts 12/12/12/12 for the four seeds dated 05-24/05-25 plus 5/5/5/5/5 dated 05-25..05-27 plus ~51-56 for four final pairs confirms 2.4 and P4 on real data; any pair with more than one corr_norm value falsifies P4 and my decomposition together.  Also read: corr_norm for smyth_extremal_vs_rest and compare with -0.0786 (2.2a) -- equality to 4 decimals confirms that the historical instrument computed exactly the rounding-residue correlation I reproduced; inequality means the loaded table differs from the source literals (deepcopy/ingest order) and C-A must be rerun on the loaded table.

C-C (new; the decisive one).  What: rerun the Pollux statistic (a copy of the three functions, not the daemon) on all 9 pairs from the loaded table, twice: (i) as-is, (ii) after deduplicating each subset at 1e-9.  Expected output: 9 x 2 (corr_norm, verdict).  Meaning: if every PROMOTED in (i) becomes non-PROMOTED in (ii), MEASUREMENT_ERROR is confirmed as co-primary and the "39 PROMOTED"/"2 survivor signals" are certified artifacts; if any PROMOTED survives (ii), that pair is the only candidate for a real density-trend finding and gets its own case.

C-D (new).  What: the shape-null the instrument actually needs -- for each pair, hold the two marginals fixed and draw B' from a smooth density fitted to B (kernel or quantile-spline) with the same n, 1000 draws; compare corr_norm(A, B) with corr_norm(A, B').  Expected: a z-score per pair that is large only if the historical corr_norm exceeds what B's marginal shape alone produces.  Meaning: separates "same shape" (R2) from "same rounding encoding" (2.2) from noise.  This supersedes the Necromancer's C5.

C-E (new).  What: Erebos ledger (charon/agents/erebos/state/kill_ledger.jsonl on M2) -- count composed claims whose provenance cites a pollux_record_id [READ g01_intersection.py:104] and their verdicts.  Expected: an integer and a verdict histogram.  Meaning: zero confirms CONSUMER_INERT; non-zero with any PROMOTED means a Pollux artifact propagated and a downstream case exists.

Necromancer proposals I judge unnecessary or mis-specified:
- C5 (shuffled-split null): unnecessary as specified.  [P4] within-subset shuffles are invisible to a sorted-multiset statistic, and split-half nulls are same-shape nulls that PROMOTE at the R2 rate; it would certify the artifact.  Replace with C-D.
- Any proposal that executes the daemon or `charon_loop.py` to "reproduce a tick": unnecessary; the statistic is 60 lines and was reproduced on read literals (2.2a); the only thing execution adds is the loaded table, which C-A/C-B/C-C obtain without running the corpse.  [QUOTED INFRA_RECOVERY_PLAN_2026-06-23.md:48-50] the loop's Redis host is retired config, so a live tick would also be an INFRASTRUCTURE experiment.
- Any proposal to recover the M2 process/host logs for the 05-30 halt beyond confirming "hard termination": the mechanism is already bounded (3a) and does not bear on the cause class.

Section-1 outcome counts: CONFIRMED 23 / WEAKENED 2 / FALLS 0 / UNTESTABLE_WITHOUT_EXECUTION 0 / NOT_EXAMINED 0.
Section-3 outcome counts: 3a WEAKENED; 3b attack 1 STANDS-as-class / WEAKENED-as-characterised, attack 2 FALLS (rival ordering), attack 3 WEAKENED, attack 4 STANDS, attack 5 STANDS; 3c WEAKENED; 3d STANDS.
