# Aether journal -- 2026-09-22

Instance: Aether[buckkeep-7a10ca4b] (comms instance tag; host BUCKKEEP).
Model: claude-opus-5-1m (heavy tier).

## Bootstrap pass (base role s1)

The operator woke this seat with "You're @roles/Aether bootstrap" from
the canonical checkout on a HOST THIS SEAT HAS NEVER RUN ON BEFORE.
Everything the seat's own files say about its workspace (M2 / SPECTREX5 /
D: drive) is stale as of this pass; see the anomalies section.

Boot sequence as executed, in order:

1. Workspace guard. The working directory the seat was woken in is the
   CANONICAL checkout (git rev-parse --git-dir == --git-common-dir ==
   C:/Prometheus/.git), so the seat refused to work there
   (WORKING_CONTRACT.md s1). No mutating git operation was performed in
   it; the only git call made there was `git fetch origin` (permitted,
   read-only for the tree). `git pull` was not run at any point.
2. Fetch and record. `git fetch origin` moved origin/main
   13fa220d4 -> c351f00b2 and origin/aphrodite/engine-2026-09-21
   74f857091 -> 4d87b0bd4. Recorded origin/main = c351f00b2.
3. Worktree. Created C:/Prometheus-worktrees/aether-memwall on a new
   task branch aether/aeth01-memwall-2026-09-22 from base SHA 251bc987e
   (the tip of the pushed seat branch
   aether/base-role-adopt-2026-09-19, verified identical to
   origin/aether/base-role-adopt-2026-09-19). Guard re-checked inside
   the new worktree: git-dir C:/Prometheus/.git/worktrees/aether-memwall
   differs from git-common-dir C:/Prometheus/.git. Clean (0 dirty paths
   at creation). The base SHA was taken from the seat branch rather than
   origin/main because the AETH-01 lane's entire history is on the seat
   branch; origin/main is NOT an ancestor of it (31 behind / 32 ahead),
   so integration will need an explicit `git merge c351f00b2` and a
   re-test on the merged tree (WORKING_CONTRACT.md s5) before any push
   to main.
4. Environment. `python -m comms` failed with
   ModuleNotFoundError: No module named 'psycopg2' on this host's
   Python 3.13.5. Installed psycopg2-binary 2.9.13 with pip. That is the
   only environment change made this pass.
5. Comms. EW_DB_HOST=192.168.1.202 set before the first comms call
   (base role s1 step 1: one comms database, on M1). M1 reachable
   (2 of 2 ICMP replies, ~3 ms). `python -m comms instance` returned
   buckkeep-7a10ca4b. `python -m comms boot Aether --model
   claude-opus-5-1m --capabilities aether,age,gpu,physics` returned
   "booted Aether[buckkeep-7a10ca4b] on BUCKKEEP | model
   claude-opus-5-1m (heavy) | 251bc987e @
   C:\Prometheus-worktrees\aether-memwall
   [aether/aeth01-memwall-2026-09-22]".
6. Inbox. `python -m comms sync Aether` at 2026-09-22T17:47:04+00:00:
   0 new, 0 queued, queue length 0. Nothing arrived for this seat.
7. Reading. Base role chain in the mandated order
   (roles/base-role/README.md, NORTH_STAR.md, RESPONSIBILITIES.md,
   WORKING_CONTRACT.md, WAKE_DIRECTIVE.md, INHERITANCE.md rows 11/77),
   then aporia/doctrine/critical_memories.md (HARD-1..HARD-6), then this
   seat's own files (RESPONSIBILITIES.md, STATUS.md, BACKLOG_H0H5.md,
   calibration/LEDGER.md, journal/2026-09-19.md, journal/2026-09-20.md),
   then the lane's documents (Aether/AETHER_DOCTRINE.md,
   Aether/AETHER_CONCEPT.md), then
   Aether/AETH-01/GPU_MEMORY_OPTIMIZATION_HANDOFF_2026-09-22.md (the
   resume point left by the previous session), then
   Aether/AETH-01/RUNPOD_SCALE_RECEIPT_2026-09-22.md, then
   `git log --oneline -30 origin/main` and -25 on the seat branch.
8. Watchdogs. `grep -i aether roles/base-role/MONITORS.md` returns
   NOTHING: this seat owns and feeds no registered standing loop. That
   is consistent with the seat never having launched one; it is recorded
   here rather than left silent (base rule 7). Nothing was running that
   needed feeding.

## Spend safety check (done before anything else executable)

The previous session's last paid action was the A40 scaling run. Its
receipt (RUNPOD_SCALE_RECEIPT_2026-09-22.md) records pod dc21hvohu9f79j
created, used and terminated, ACK_204, absence independently
re-confirmed (ACTIVE_POD_COUNT 0), pod wall time 113 s (about $0.015
against the $3 cap). No live pod is outstanding from the previous
session by that receipt. No RunPod API call was made this pass and no
money was spent this pass.

## Tests on this host

`python -m pytest Aether/test/test_aeth01_gpu_differential.py
Aether/test/test_aeth01_canary_parity.py
Aether/test/test_aeth01_properties.py -q` gave 95 passed in 9.46 s at
251bc987e in the new worktree. These are exactly the three files the
handoff names as the Phase 4 falsification gate, so the gate is known
green on this host BEFORE any kernel edit.

Full suite, `python -m pytest Aether/test -q` at 251bc987e on BUCKKEEP:
**984 passed, 5 skipped, 92 subtests passed in 487.08 s (8 m 07 s)**,
exit code 0. All 5 skips are platform gates, not failures, and each one
names its reason:

    test_aeth01_deployment_package::test_legacy_stubs_refuse_at_runtime
      [launch_pod.sh]      Linux bash runtime; static fail-closed
                           checks run everywhere
      [terminate_pod.sh]   same
    test_aeth01_pod_service::
      test_legacy_watchdog_rejects_zero_and_invalid_timeout_before_command
                           Linux bash wrapper
      test_symlinks_and_nonregular_artifacts_are_not_served
                           POSIX symlinks/FIFOs
      test_timeout_kills_term_ignoring_descendant_not_just_leader
                           real Linux process-group regression

Those five are exactly the tests that exercise the pod-side Linux
behaviour, so on a Windows host they are UNMEASURED, not passed. Any
claim about pod-side watchdog or symlink behaviour made from this host
rests on the on-pod run, not on this suite.

## Anomalies found at boot (none of them caused by this pass)

A-1 THE CANONICAL CHECKOUT HAS A SEAT BRANCH CHECKED OUT. C:/Prometheus
    is the repository's main worktree and its HEAD is
    aether/base-role-adopt-2026-09-19 at 251bc987e, not main (local main
    is at fdfb344b0, 22 behind origin/main). The recent AETH-01 commits
    were authored there. That is a D-23 s1 violation: no seat performs a
    mutating git operation in the canonical checkout, and no seat edits
    source files there. This seat did not create the condition and will
    not "fix" it by running further mutating git commands in the
    canonical checkout; it is reported to the operator as a workspace
    decision.

A-2 TWO WORKTREES SHARE ONE BRANCH. C:/Prometheus-aether is a linked
    worktree pinned to the same branch as the canonical checkout. Its
    working tree is STALE (newest files dated 2026-09-21 06:04; it is
    missing GPU_MEMORY_OPTIMIZATION_HANDOFF_2026-09-22.md,
    REVIEW_PACKET_C1_REPAIR_2026-09-22.md,
    RUNPOD_SCALE_RECEIPT_2026-09-22.md,
    RUNPOD_SMOKE_RECEIPT_2026-09-22.md, aeth01_bench.py,
    aeth01_bench_server.py and aeth01_scale_orchestrate.py), so
    `git status` there reports 7 staged deletions and 5 staged
    modifications that are really "this index is behind the branch ref
    the other worktree advanced". No unique uncommitted work was found
    in it: every reported modification is an older blob already
    superseded on the branch, and the only untracked file is
    _pytest_aether_out.txt (a pytest capture). It is a
    destroy-and-recreate candidate (WORKING_CONTRACT.md s7) but it was
    NOT destroyed this pass: it is not blocking, and destroying another
    checkout is an outward-facing action the operator should authorise.
    SLOW-IS-NOT-CORRUPT does not apply here: no process is writing it,
    the file count is flat, and the divergence is fully explained.

A-3 THE SEAT'S OWN ENTRY FILES ARE STALE, which base rule 5 (CURRENCY IS
    CORRECTNESS) calls a defect even when internally consistent.
    roles/Aether/RESPONSIBILITIES.md and roles/Aether/STATUS.md both
    carry currency 2026-09-19 and still say "charter PENDING", "NO
    lane", "NO science", "resident on M2", and "it changes no code and
    no document outside roles/Aether/". All five statements are now
    false: the seat has an adopted doctrine (Aether/AETHER_DOCTRINE.md,
    currency 2026-09-20, the operator's verbatim intent condensed), a
    named lane (the AGE / aeth01.v1 artificial-physics ecosystem under
    Aether/), three days of committed science, and it is running on
    BUCKKEEP. roles/base-role/INHERITANCE.md rows 11 and 77 still say
    "charter PENDING the operator's discussion" as well. BACKLOG_H0H5.md
    is still the 3-item PROVISIONAL file, below the schema's 20-item
    floor. This pass updates STATUS.md only (below); the
    RESPONSIBILITIES / backlog / INHERITANCE rewrite is proposed as work
    item 4 and not done unasked, because rewriting a seat's charter
    section around a charter is a scope-changing action to confirm
    rather than assume.

A-4 JOURNAL GAP. roles/Aether/journal/ holds 2026-09-19.md and
    2026-09-20.md only. The work of 2026-09-21 and 2026-09-22 (Astra
    closure reviews, cleanup-evidence repair, terminology refactor, the
    RunPod smoke failure and the A40 scaling run) is recorded in
    Aether/AETH-01/*RECEIPT*.md, REVIEW_PACKET_*.md and the commit
    messages, but not in the dated journal the base role mandates (s3).
    This entry does not back-fill those two days: reconstructing them
    from artifacts would be a summary written by a seat that was not
    there, and the primary records exist. The gap is named here so it is
    visible rather than silent.

## Work suggested at boot (base role s1 step 6)

See STATUS.md "next executable action" and the boot receipt given to the
operator in chat.

## AETH-01 memory-wall round, Phases 2 through 4 and 6

Full report: `Aether/AETH-01/GPU_MEMORY_OPTIMIZATION_01_2026-09-22.md`.
Phase 5 (the paid A40 run) is NOT started and is held for the operator.

PHASE 2, measured rather than reasoned. The handoff's byte accounting
was explicitly never validated, so it was not used. Peak per-tick
allocation was measured with tracemalloc, after first proving
tracemalloc can see NumPy's allocations at all (4,000,096 bytes observed
for a 4,000,000-byte array) -- without that check the table would read
zero and look like a win. Baseline at 251bc987e: 241.17 / 240.29 /
240.07 / 240.02 / 240.00 bytes/site at 64/128/256/512/1024, so
**240.00 bytes/site** asymptotically. The A40 receipt's ~261 B/site is
CuPy pool occupancy, a different measurement, and the two are
consistent.

PHASE 3, three changes, each measured separately:
  v1  packed uint64 coordinate grid + np.roll gathers   240.00 -> 216.00
  v2  v1 + in-place mix64_vec                           216.00 -> 201.01
  v3  v2 + narrowed field dtypes                        201.01 -> 113.01
The handoff predicted the hash-chain temporaries were marginal; that
was wrong by 15 bytes/site. Every narrowing is justified from the
VALIDATED domain in oracle_aeth01.py (_require_int puts write_cost,
maintenance_cost and replenish_amount in [0,255]), so energy-write_cost
is in [-255,255] and int16 is exact over the whole legal domain. A
further uint8 narrowing was measured, works, saves ~8 more bytes/site,
and was REJECTED: it is correct only via "the wrapped value is never
read", and 113 already clears the 120 target.

PHASE 4, falsification. Both patched files reproduce the A40 hardware
digests: the unmodified kernel was checked first to prove the oracle
reproduces on this host (7/7 to 1024), then the optimized reference
matched **8/8 including 2048^2** and the bundled canary 7/7 to 1024.
400 randomized differential trials: 400/400 bit-exact. 464 adversarial
cases aimed at the narrowing (domain extremes, the write_cost=255 /
energy=0 / ENERGY-target case that drives transfer_amt to -255,
degenerate and non-square lattices, seed/tick at domain top): 464/464
bit-exact.

CONTROLS. Three defects injected deliberately. Roll-sign flip: caught
(8/464 adversarial, all digests differ). Arbitration priority truncated
to uint32: caught (4/464 adversarial, 75/400 randomized, digests
differ). uint8-instead-of-int16: NOT caught -- and that is the finding,
not a gap, because that change really is semantics-preserving. Recorded
as an inverted control rather than dropped.

REGRESSION GUARD. Added `Aether/test/test_aeth01_memory_footprint.py`
(10 tests): a bytes/site ceiling, a "materially below baseline" check,
the A40 digests for 16..256, and a self-check that fails if tracemalloc
cannot see NumPy -- so the ceiling cannot pass vacuously. Verified it
actually bites: with the kernel reverted to 251bc987e, 4 of its
assertions fail. Instruments committed under
`Aether/AETH-01/instruments/` with a README.

DEPLOYMENT PIN. The full suite failed on
`test_image_manifest_covers_runtime_critical_files_and_is_current`
because image_manifest.json pins the kernel's sha256. That is the pin
working. Regenerated with its own generator: exactly one hash changed
(0b7c89bd... -> f6323456...), installed_dependencies and
immutable_image_digest still null. Any previously built canary image is
now stale.

FULL SUITE after the round: `python -m pytest Aether/test -q` ->
994 passed, 5 skipped, 92 subtests passed in 542.25 s, 0 failed
(984 passed before the round; +10 is the new guard file).

SIDE EFFECT, NOT THE OBJECTIVE. NumPy/CPU tick time improved 2.83x /
2.70x / 2.29x at 256/512/1024. This does not predict GPU behaviour.

PREDICTION FOR PHASE 5 (the falsifier). At 113.01 bytes/site, 16384^2
needs 30.3 GB before pool overhead; two overhead models anchored on the
single available data point give 33.0 GB (multiplicative) and 36.0 GB
(additive), both under 48 GB. The sharper check is at a size already
measured on real hardware: 8192^2 should fall from 16,718 MB to roughly
8.2-9.2 GB of used pool. If it does not, the allocation model is wrong
about CuPy even though the digests are right, and the 16384^2
projection should not be believed.

## Next executable action

Phase 5: one A40 pod, sizes 256..16384, one pod at a time, $3 ceiling,
no pod-creating retries, terminate and re-confirm ACTIVE_POD_COUNT 0.
It spends real money, so it is held for the operator's explicit go.
Nothing else in this round is blocked.

## Pass receipt

commit          3124d1f88 on aether/aeth01-memwall-2026-09-22
push verified   git merge-base --is-ancestor 3124d1f88
                origin/aether/aeth01-memwall-2026-09-22 -> true
base_sha        251bc987e
worktree        C:/Prometheus-worktrees/aether-memwall (guard passed)
files           13 paths, committed by explicit path with a message file
closing sync    2026-09-22T18:33:22Z, 0 new, 0 queued, queue length 0
                (the boot sync at 17:47:04Z was also empty; nothing
                arrived for this seat while the pass ran)
spend           none. No RunPod API call was made at any point.
monitors        none created, so no MONITORS.md row is owed.
NOT pushed to main. origin/main (c351f00b2) is not an ancestor of this
branch; integration needs an explicit merge and a re-test on the merged
tree (WORKING_CONTRACT.md s5), which this pass did not do and did not
claim.

## AETH-01 Phase 5: the A40 qualification run (operator-authorized, $3 cap)

Disposition: **MEMORY_WALL_MOVED**. Full write-up in section 9 of
`Aether/AETH-01/GPU_MEMORY_OPTIMIZATION_01_2026-09-22.md`; raw artifacts
in `Aether/AETH-01/evidence/2026-09-22_optimized_scale_run/`.

CORRECTION MADE BEFORE SPENDING. The authorization quoted the branch
head as `faf567ab8`. That commit predates this round; pinning it would
have downloaded the PRE-optimization kernel onto the pod and benchmarked
the wrong artifact while looking entirely successful. Pinned
`AETH01_COMMIT=084c648274a30a11573b2fd97647a186f6b4ffdc` instead and
verified from the on-pod receipt's own source_hashes that the kernel
that ran was f6323456 (the optimized one).

PREFLIGHT, before any pod existed: read-only inventory check
(ACTIVE_POD_COUNT 0, auth OK); sha256 of all four pinned files compared
local vs git-blob vs GitHub-raw (4/4 MATCH, so the pod's sha256sum gate
could not fail for a line-ending or push-lag reason); a standalone
terminate-and-verify script written first, so an orchestrator killed
mid-run could not leave a pod billing.

RESULTS. Canary on the GPU: PASS, backend cupy, 300/300 matched, 0
mismatches, gpu_kernel_seconds 11.779 (baseline 14.747). Scaling curve
16..16384 plus an attempt at 32768.

  16384^2 = 268,435,456 sites: 7.419936 s/tick, 36,177,599 sites/sec,
  33,038.2 MiB used, 12,459.8 MiB (12.2 GiB) STILL FREE.
  Baseline OOM'd at this size.

  New boundary: AETH01_BENCH_STOP reason=OOM size=32768.
  Peak throughput 36,177,599 sites/sec, up from 21,227,101 (1.704x).

All TEN digests the two A40 runs have in common are byte-identical to
the pre-optimization run. Parity against the CPU oracle PASS at 16, 32,
64, 128 on-pod. 16384^2's digest b3d06be54c1be93c is new because the
baseline could not reach that size.

CRITICAL FALSIFIER PASSED. Predicted before the run: 8192^2 should fall
from 16,718 MB to 8.2-9.2 GB or the allocation model is falsified.
Observed 8,462.2 MiB = 8.87 GB, inside the band, a 1.976x reduction.

THE MODEL IS NOW OVER-DETERMINED, not guessed. Two large sizes give
used(MiB) = 270.2 + 128.000 bytes/site, and the intercept that fit
produces (270.2 MiB) is exactly the floor the four smallest lattices
measured independently. Baseline marginal cost by the same form is
257.000 bytes/site, so the on-GPU reduction is 2.008x against the
2.124x tracemalloc measured off-GPU; the difference is CuPy pool
rounding, and 128.000 exactly is that rounding showing itself. Both
section 6 predictions bracket the 16384^2 truth (32.99 predicted
multiplicative, 35.98 additive, 34.64 GB observed). Model ceiling:
370.5 M sites, about 19249^2, so 16384^2 is the last power-of-two that
fits and the next doubling is a device-count problem, not an allocation
problem.

COST AND CONTAINMENT. Pod 6q9raukca794ou, one pod, created on the first
attempt, no pod-creating retries. Wall 186 s at $0.49/hr = $0.0253,
which is 0.84% of the $3 authorization. Terminated ACK_204; absence
confirmed by the orchestrator AND again afterwards by a separate script
from a clean process, both ACTIVE_POD_COUNT 0. No credential printed,
on argv, or in any artifact; all six committed artifacts were scanned
for credential patterns first. Billing reconciliation is INCOMPLETE and
is not claimed as done: the figure is computed from measured wall time
at the quoted rate, because this client has no billing endpoint.

ONE COSMETIC REGRESSION, reported not hidden. A new NumPy
RuntimeWarning ("overflow encountered in scalar multiply") appears in
canary.log and bench.log, from the in-place mix64_vec on the two numpy
SCALAR pre-mixes; under CuPy the bundled kernel cannot call np.seterr,
so the scalar path is outside the overflow policy the NumPy backend
sets. The wraparound is the intended semantic and the run was bit-exact
throughout. NOT fixed, because the authorization said no kernel edits
during the evidence run.

## Two fleet-level notes from this pass

GITIGNORE DEFECT, FIXED IN MY OWN LANE. Committing the pod's raw
evidence failed because `.gitignore` line 47 (`*.log`) swallowed
canary.log and bench.log -- the raw rows behind the scaling verdict. A
verdict whose ledger is not committed is an assertion (base role s2),
and this is the same defect class the base role's own self-test exists
to catch. Added a narrow negation, `!Aether/AETH-01/evidence/**/*.log`,
with a comment saying why. Not fixed with `git add -f`, which would
have hidden the trap for the next person.

NYX MANIFEST DRIFT IS STILL OPEN, THREE DAYS ON.
`archaeon/tests/test_base_role.py::test_issued_manifests_verify_against
_their_files` still FAILS fleet-wide on
`roles/Nyx/prompts/2026-09-19_asal_pipeline_direction/`
`PLAN_behavioral_cuts_after_replication.md` (manifest dc41abff1393 !=
artifact dbd639ef6ee7). Identical hashes to the report this seat filed
on 2026-09-19 (`roles/Aether/prompts/2026-09-19_nyx_manifest_drift/`),
so nothing has changed. Verified NOT caused by this round: no commit
between 251bc987e and HEAD touches roles/Nyx. Not re-reported, because
the original report stands and duplicating it is noise; recorded here so
the red row is not mistaken for something this round introduced. Every
seat that runs the fleet-wide self-test meanwhile sees a failure it did
not cause. Base role self-test otherwise: 9 passed, 1 skipped.

## Phase A: the uint64 scalar-overflow warning (operator-directed)

Report: `Aether/AETH-01/MIX64_SCALAR_REPAIR_2026-09-22.md`.

CORRECTION FIRST. I reported this warning last pass as a NEW regression
from the in-place mix64_vec. Wrong, and it reached three committed
documents. The pre-round expression form warns identically on a scalar
(1 warning each for old and new form on a scalar; 0 each on an array),
so the baseline A40 run emitted it too. It became visible only because
this round fixed the .gitignore rule that had been swallowing *.log.
That is a change in observability, not behaviour. Row added to
roles/Aether/calibration/LEDGER.md -- the seat's first. The original
claim is annotated in place in the round report, not rewritten.

DIAGNOSIS. NumPy warns on integer overflow for SCALAR ops and is silent
for ARRAY ops. Six of mix64_vec's twelve call sites pass a scalar (the
seed/tick pre-mixes of the three hash domains); the other six pass
lattice arrays. The warning was never about the lattice.

REPAIR. Added mix64_scalar, explicit modulo-2^64 Python-int arithmetic
that cannot overflow rather than overflowing quietly, and pointed the
six scalar sites at it. Constants are SHARED with mix64_vec, not
restated (a test asserts the literals are absent from its source, so
the two mixers cannot drift apart one edit at a time); return type
stays np.uint64 so every downstream broadcast is the same operation; no
warning filter anywhere. Applied character-identically to both kernel
files.

FALSIFICATION. (1) 2,000,014-value randomized 64-bit corpus plus domain
edges and the hash's own constants: 0 mismatches. (2) differential /
parity / properties / footprint: 105 passed. (3) frozen A40 digests 8/8
on the reference, 7/7 on the bundled canary. (4) full suite 1002
passed, 5 skipped, 0 failed (was 994). (5) warning gone, verified by
turning RuntimeWarning into an error with np.seterr also raising, on
the bundled kernel -- AND with a control proving the harness does catch
a deliberate scalar overflow, so "no warning" cannot be confused with
"cannot see warnings". Plus a cross-check against
oracle_aeth01.splitmix64_mix, an independent restatement in the CPU
oracle that imports none of this.

image_manifest.json kernel hash moved f6323456 -> (recorded in the
report). Any pod launched from the Phase 5 commit runs the pre-repair
kernel: identical behaviour, different artifact identity, so
AETH01_COMMIT must be updated for any further run.

## AETH-01 Phase B: first light on one A40 (operator-authorized, $3 cap)

Report: `Aether/AETH-01/FIRST_LIGHT_2026-09-22.md`. Rows:
`Aether/AETH-01/evidence/2026-09-22_first_light/firstlight.jsonl`
(3,024 records, 2.6 MB). Scout that chose the parameters:
`Aether/AETH-01/SCOUT_FINDINGS_2026-09-22.md`.

Run aeth01fl-20260922T201339Z-7df284cb, pod cblvom5ygkh210, pinned
aa3851360. 6 of 6 worlds completed, 30,000 world-ticks over 16,777,216
sites each, 3,006 observatory samples, pod wall 14,859 s, **$2.022** of
$3. Canary PASS 300/300 backend cupy before any science. Terminated
ACK_204, absence confirmed twice.

THE OBSERVATORY DID NOT EXIST and had to be built first (tier-1
quantities from HABITABILITY.md, 18 known-answer tests including a
read-only guard). Before spending anything, a 36-config CPU scout ran
for $0 and found the ECONOMICS.md default regimes all freeze or starve,
which changed the run design from six replicates of one regime to three
regimes x two seeds.

RESULTS.
- The scout's scale-invariance argument held EXACTLY. Regime A freezes
  at 16.7 M sites by the same mechanism as at 16 K: write_density
  0.435418 == starved_density 0.435418, so 7.3 million sites still
  carry opcode=WRITE and not one can act. Activity and template change
  are exactly zero from tick ~400.
- A SECOND CERTIFIED-DEATH CONDITION, absent from HABITABILITY.md: with
  MAINTENANCE_COST=0, REPLENISH_NUMER=0 and every WRITE site starved,
  no site can emit, so no transfer can occur, so no energy can rise, so
  no site can ever emit. Provable, not a trailing window. Proposed for
  the label table rather than edited in unilaterally.
- Replication at this scale is near-exact: every metric agrees between
  seeds to 3-4 significant figures. The disagreement rate HABITABILITY
  asks for is ~0 at 16.7 M sites.
- 86-92% of the A and C runs observed an already-settled state.
  B_balanced never settled and is RIGHT-CENSORED by the run length, not
  by the physics: energy still falling, Gini still rising at tick 5000.
- NO large-scale spatial organization, quantified: 64x64 block-mean std
  1.107 against a pure-sampling-noise prediction of ~1.11. The maps are
  statistically indistinguishable from noise.
- One positive structural signal: payload autocorrelation +0.042 ->
  +0.069, growing all run, while opcode autocorrelation goes NEGATIVE
  (-0.039). Both have the same mundane mechanical explanation (payload
  is copied to neighbours; opcode is overwritten with a random byte).
  Recorded as tier-1 structural resemblance at most; explicitly NOT
  evidence of copying, and no claim-ladder detector was run.
- "FREE COMPUTE" IS NOT CONSERVATIVE. Regime C charges nothing for
  anything and still lost 3.04% of its energy (63.8 M units, 3.81 per
  site, per 5,000 ticks, reproduced across seeds to 0.02 points). The
  only sink left is the lossy transfer law. No AETH-01 regime is
  energy-conserving, and ECONOMICS.md states no loss rate; this is the
  first measurement of one.
- No recurrence, no oscillation, no abrupt regime change anywhere.

TWO DEFECTS OF MINE, both found by launching rather than by checking.
A syntax error in shipped-hash logging killed the orchestrator at
import -- failed CLOSED, no pod, no spend. Then the GATE started the
artifact server AFTER the science, copied from a 2-minute benchmark
where it did not matter; for an hours-long run that meant no progress
visibility and, worse, that terminating mid-run would have destroyed
every sample taken. Fixed to start the server first and wait on it.
One pod (dswvdbbjqkp393) was created and terminated establishing this,
about $0.04, absence confirmed. Its canary passed 300/300 on the Phase
A kernel, which is independent hardware confirmation that mix64_scalar
is bit-exact under CuPy and not only under NumPy.

TOTAL SPEND TODAY: about $2.09 of $3 across all runs.

## Phase B addendum: the operator's full brief arrived (relayed 2026-09-23)

The Phase B instruction I worked from was truncated mid-heading at
"## Preserve surprising". The full text arrived afterwards and named
four things the truncated copy did not. Three were gaps in what I
delivered; one I had complied with by accident of good practice.

FILENAME. The brief mandates
`Aether/AETH-01/FIRST_LIGHT_01_2026-09-22.md`; I had written
`FIRST_LIGHT_2026-09-22.md`. Renamed with `git mv` and rewritten to the
mandated structure (OBSERVED / INTERPRETATION / HYPOTHESES FOR NEXT
ROUND held strictly apart) with every report item the brief lists.

ENGINEERING TELEMETRY. Partly recovered, partly a real gap.
  Recovered from the controller's own 60-second polls, which resolve to
  one sample per 10 ticks: tick latency median 0.477 s/tick, stable
  across all 4.12 h, drift first-to-last decile +0.0199 s/tick
  (+4.07%), which is at the edge of that quantization. Per-world wall
  times 2452-2476 s, a 1.0% spread with the last world no slower than
  the first, bounding cumulative slowdown below 1%. Artifact growth
  linear at 902 bytes/sample, 2.711 MB total against an 8 MB ceiling.
  Zero errors, zero warnings (the Phase A repair holds on hardware),
  the 60 s/tick guard never fired, no OOM.
  NOT RECOVERABLE: GPU memory over lifetime. The runner records no
  mem_used_mb and the pod is gone. The scaling run's 2,318.2 MiB at
  4096^2 and the fitted pool model agree exactly, so the steady value
  is known, but the LIFETIME is unmeasured and flat latency is weak
  evidence rather than a measurement. First item of the next round.

DETERMINISTIC REPLAY SPOT-CHECK. Was missing; now done and committed as
an instrument. For all six worlds: rebuild the initial lattice from the
COMMITTED RECIPE ALONE, advance one tick on this host's NumPy backend,
compare against the digest the A40 recorded under CuPy. 6/6 MATCH,
REPLAY_CHAIN_VERIFIED, at 16.7 M sites. The replay pointer is
executable rather than documentary. What it does NOT establish is
cross-backend agreement over THOUSANDS of ticks -- a 4096^2 CPU replay
to the next committed digest (tick 251) costs about 3 h of CPU. The
brief asked whether the 7-tick qualification stays stable over
thousands of ticks; that question is still open and this run did not
close it. Filed as hypothesis H2 with a cheap test (one 512^2 world,
5,000 ticks, both backends, full digest series).

PRESERVE SURPRISING THINGS. No localized unusual event occurred, so the
capture list was not exercised; but the capability was also not built,
and the report says so. What IS preserved per world: full parameters
and physics seed, a recipe that regenerates the exact initial lattice
and is verified against its own digest before the world starts, 501
samples with per-field change rates, 21 exact sha256 digests at
250-tick intervals, 11 coarse 64x64 maps, final digest. NOT preserved:
full snapshots, per-event traces, any coordinate-level record. Nothing
below the 64-site block scale is recoverable except by replay.

NO TUNING MID-CAMPAIGN: complied. No kernel edit, parameter change,
threshold change or seed selection after the batch started; all six ran
to completion in the declared order, and no world was stopped for being
boring.

NEW MEASUREMENT MADE WHILE CLOSING THESE GAPS. The digest series proves
the regime A freeze independently of any rate: both A worlds hold a
BYTE-IDENTICAL lattice for 4,249 consecutive ticks (18 consecutive
identical sha256 digests, ticks 751 through 5000).

## Terminology audit caught 13 violations, all mine

Running the full suite after the gap-closure commit (cf7e4efea) turned
`test_aeth01_terminology_audit.py::test_active_age_surface_has_no_
unallowlisted_metaphors` RED: 13 deprecated-metaphor hits on the active
AGE surface, every one introduced by this pass's own Phase B code and
documents. The seat built that audit on 2026-09-21 and it did exactly
its job on the seat's next author.

Fixed against COMPUTATIONAL_TERMINOLOGY.md's canonical column, not by
allowlisting: the deprecated heredity term -> configuration/state
transmission and transmitted variation; the deprecated group term ->
ensemble; the deprecated perturbation term -> perturbation (prose and
the scout's local variable and JSON key alike); the deprecated
persistence verb -> persist; the deprecated modification verb ->
modified. `MUT_NUMER`/`mut_numer` were left alone: the contract retains
them explicitly as the legacy serialized identifier.

The 14th hit was the note I wrote explaining the 13th -- it quoted the
deprecated word literally. Reworded rather than allowlisted.

The committed 2026-09-22 scout rows keep the older JSON key. They are
historical evidence (contract class C, do not rewrite), and the scout
now carries a comment saying so, so a reader of those rows is not
confused by the rename.

Full suite after the repair: **1020 passed, 5 skipped, 92 subtests, 0
failed** (462.9 s). The preceding commit cf7e4efea was pushed with this
test red; that is recorded here rather than quietly fixed, and the
lesson is that the full suite belongs before a push, not after it.

