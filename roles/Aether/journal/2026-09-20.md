# Aether journal 2026-09-20 -- fourth ecosystem design capture (pre-charter)

Charter still PENDING (roles/Aether/STATUS.md unchanged by this pass). This
is design-capture work under an Augment Agent session on a laptop off the
Prometheus network (M1-M4 unreachable; no comms boot possible: EW_DB_HOST
192.168.1.202:5432 unreachable from this host, confirmed by TCP test).
Working directly in the checkout, not a worktree, at the operator's
direction for this offline session.

## What happened

1. Operator directive (chat, verbatim, captured in full under
   roles/Aether/prompts/2026-09-20_fourth_ecosystem/RAW_NOTES.md): design a
   fourth Prometheus experimental ecosystem, independent of BEE/NPE/SFE,
   explicitly not borrowing their architecture or mechanisms. No
   implementation yet. Aether's job: organize raw notes into a coherent
   concept, surface open questions, help refine the plan. Eventual target
   Runpod; hard budget $19.93; tiny worlds, short runs, cheap hardware;
   optimize learning per dollar; every remote test must answer a specific
   question, be reproducible, and inform the next test.
2. Wrote roles/Aether/prompts/2026-09-20_fourth_ecosystem/RAW_NOTES.md
   (verbatim capture, first entry) and
   roles/Aether/design/FOURTH_ECOSYSTEM_DESIGN.md (organized concept:
   context, non-goals, constraints, process, 9 open questions, empty
   decisions log, next action).
3. Did not touch roles/Aether/STATUS.md, RESPONSIBILITIES.md or
   BACKLOG_H0H5.md: no charter exists yet, no lane opened, nothing to
   revise there from this pass.

## What was NOT done

- No implementation of any kind for the fourth ecosystem.
- No Runpod account access, no remote test run, no cost incurred.
- No comms boot, no comms sync (unreachable from this host; recorded as a
  known constraint, not chased).
- No commit yet: files exist in the working tree only, pending the
  operator's word on when to commit (raw notes will likely grow over
  several messages before a natural commit point).

## Update, same day: second message, artifact restructure

Operator sent a second, more detailed directive (Aether/notes/2026-09-20_
raw_notes.md, message 2): strict incremental TDD workflow (contract ->
tests -> minimal design -> minimal code -> review -> next increment);
scientific priorities for an artificial-life substrate (determinism where
appropriate, adversarial tests against false discoveries, physics/
observation separation, causal evidence, raw forensic evidence, seeded-
control vs. spontaneous-emergence distinction, no claim from a detector
alone); Runpod spend requires explicit per-run approval with a 5-part
disclosure (question/duration/hardware/max cost/decision criterion);
Claude Sonnet 5 as primary design model, Astra as a later independent
review with a defined review-packet format; Augment Cosmos evaluation as
low-priority background; and a named artifact set (AETHER_CONCEPT.md,
AETHER_SPEC.md, AETHER_TEST_PLAN.md, AETHER_OPEN_QUESTIONS.md,
AETHER_RUNPOD.md, AETHER_DECISIONS.md).

Restructured accordingly, since nothing from the first pass had been
committed yet: removed roles/Aether/design/FOURTH_ECOSYSTEM_DESIGN.md and
roles/Aether/prompts/2026-09-20_fourth_ecosystem/RAW_NOTES.md (first-pass
ad hoc names, superseded); created a top-level Aether/ directory (parallel
to the other ecosystem directories such as vivarium/, techne/,
SerendipityFoundry/, distinct from roles/Aether/ which remains the seat's
own identity/journal/backlog space) holding: notes/2026-09-20_raw_notes.md
(both verbatim messages), AETHER_CONCEPT.md, AETHER_SPEC.md (empty --
nothing frozen yet), AETHER_TEST_PLAN.md (empty -- no milestone yet),
AETHER_OPEN_QUESTIONS.md (16 questions, including a checked-for-
contradictions note: none found between message 1 and message 2),
AETHER_RUNPOD.md (budget ledger, $0 spent of $19.93), AETHER_DECISIONS.md
(7 decisions logged, D-1 through D-7).

## Update, same day: third message, doctrine and working concept

Operator sent AETHER ENGINEERING DOCTRINE (message 3, Aether/notes/
2026-09-20_raw_notes.md): rigor doctrine (correctness over velocity,
falsification over confirmation, causal over resemblance, five-nines
engineering rigor where measurable, never weaken a test); the TDD/
emergence tension resolved (TDD covers controlled/measured things,
emergence is an observation never a test assertion, a detector firing is
never sufficient for a campaign to pass); a working scientific concept
(GPU-native artificial physics with no predefined organism/genome
boundary, central question about evolutionary accessibility under a
different heredity representation); hard candidate constraints (no BIRTH/
ALLOC/organism_id/genome_id/external copying/reproduction reward/
observatory metadata visible to matter); a candidate world (2-D toroidal
lattice), candidate physics (synchronous deterministic ticks) and
candidate instruction set, all explicitly marked non-frozen; a causal
heredity taxonomy (STRUCTURAL_RESEMBLANCE / CAUSAL_CONSTRUCTION /
RECURSIVE_CONSTRUCTION) with six adversarial cases to survive; a telemetry
shape (cheap counters + rolling buffers + triggered high-res capture); a
deferred first experiment (an accessibility-barrier transplant, described
abstractly, existing engines not to be inspected); an expected development
order; and the Runpod economics metric (useful interactions per dollar,
information per dollar).

Wrote Aether/AETHER_DOCTRINE.md (new, the condensed doctrine). Rewrote
Aether/AETHER_CONCEPT.md around the working concept, tagging every
non-frozen section [CANDIDATE]. Reorganized Aether/AETHER_OPEN_QUESTIONS.md
into the operator's 7 categories (scientific ontology, physics, execution/
state, heredity/observatory, falsification/testing, GPU/performance,
Runpod economics) plus a process/meta bucket for leftovers; marked
questions 1, 2, 5, 9, 10 (old numbering) ANSWERED or PARTIALLY ANSWERED
against the new material; added 10 new questions surfaced by message 3
(conflict resolution rule, resource/decay formula, instruction-set v1
scope, exact state layout, RECURSIVE_CONSTRUCTION operationalization,
telemetry trigger definitions, substrate-trustworthy gate criteria,
CPU/GPU tolerance, information-per-dollar quantification, and the
first-transplant sourcing tension). Added D-8 through D-12 to
Aether/AETHER_DECISIONS.md for decisions actually stated as adopted
(doctrine, TDD/emergence resolution, Runpod metric, causal-heredity
taxonomy, Runpod-development-only restriction); annotated D-4 (one
question per probe); did NOT log the hard candidate constraints, candidate
world/physics/instruction-set, telemetry shape, first-transplant sketch or
development order as decisions -- they stay in AETHER_CONCEPT.md tagged
[CANDIDATE], per the operator's explicit instruction not to silently
convert candidates into decisions. AETHER_SPEC.md left empty/unfrozen.

Committed 6a5ba2b56 (this pass's files, local only, not pushed).

## Update, same day: message 4 -- three rulings, AETH-00 candidate

Operator: keep 6a5ba2b56 local; no code yet. Three rulings: (1) GPU-native
means the target implementation is massively parallel, but GPU execution
never defines scientific semantics -- physics gets an implementation-
independent spec and CPU oracle first (candidate GPU mapping: per-cell
parallel eval/propose, then deterministic parallel arbitration/commit,
kept as candidate, not logged as a decision); (2) executable matter is
intentional, no privileged code/data distinction unless a later
experiment deliberately adds one; (3) the first accessibility transplant
reproduces a stated abstract scientific property (information present,
selection pressure present, useful conditional computation inaccessible
by representation topology; incumbent answers before reading the needed
information; viable alternative separated by a zero-fitness valley with
no local 1-2 edit route; a seeded viable witness strongly selectable),
never any existing engine's implementation, and no existing engine code
is inspected. Logged as D-13, D-14, D-15 in AETHER_DECISIONS.md.

Then: draft AETH-00 (still no implementation) -- 4-uint8-field physical
state (opcode/arg0/arg1/payload), 2-opcode ISA (NOP, WRITE), synchronous
snapshot-decode-propose-arbitrate-commit tick, and a collision-arbitration
recommendation. Wrote: AETHER_SPEC.md AETH-00 (UNFROZEN CANDIDATE)
section plus a Substrate qualification philosophy section (categories
only); AETHER_TEST_PLAN.md AETH-00 section (15 candidate tests including
two I added beyond the operator's list: a win-rate uniformity check
across directions, test 13, and a small-lattice toroidal-aliasing case,
test 14, since minimal dimensions can make two different arg0 directions
resolve to the same physical neighbor); AETHER_OPEN_QUESTIONS.md
reorganized entries (questions 6, 8, 9, 10, 16 answered/partially answered
for the AETH-00 slice; question 3's tension resolved; 4 new questions
added: 27 unknown-opcode default flagged by the operator, 28 minimum
lattice dimension policy, 29 exact hash constants, 30 GPU-mapping
validation timing); Aether/AETH-00_REVIEW.md (new: proposed contract,
assumptions, unresolved choices, three arbitration options compared with
a recommendation -- hash-keyed deterministic max-arbitration, a public
splitmix64-style avalanche mix, not sourced from any Prometheus engine --
ways AETH-00 could bias later science, the test list pointer, and what
AETH-00 does not establish).

Recommended arbitration: hash-keyed deterministic max over (seed, tick,
target cell, target field, source cell); rejected fixed spatial priority
per the operator's own reasoning (permanent bias); noted segmented sort
as an equivalent-result, GPU-costlier alternative.

Did not commit this pass: the operator's message 4 did not repeat the
conditional commit authorization from message 3, so files are left
uncommitted pending explicit instruction.

## Update, same day: message 5 -- Astra review reconciliation, rev 2

Operator relayed an independent (Astra) review of AETH-00 with a
meta-ruling (AETH-00 is a replaceable conformance specimen, not the
frozen scientific foundation of Aether -- passing it licenses engineering
progression only) and four STOP-SHIP corrections plus several smaller
fixes. Still no implementation, still no commit.

Applied to AETHER_SPEC.md (now rev 2, semantics_version
`aeth00-candidate-2`): (1) corrected proposal/collision semantics -- one
physical WRITE source emits exactly one proposal; direction resolves a
destination, never multiplies proposals; a collision requires 2+ DISTINCT
physical sources on the same (target cell, field); rewrote the
correction as its own subsection with a proof sketch. (2) Added a
"Complete transition function parameters" section: dimension domain,
coordinate orientation, direction/field encoding, seed type, tick
type/initial value/overflow behavior, invalid-input handling, same-value
writes, untargeted-field preservation, independent per-field arbitration,
and the six-component replay-identity tuple. (3) Removed atomicMax/
packed-key language from the normative arbitration section; moved the
GPU sketch into a clearly non-normative "Deferred implementation
guidance" subsection. (4) Fully specified the SplitMix64-finalizer
arbitration law (exact mix constants, bit packing, <4096-per-axis
supported range, tie-freedom proof) with an explicit non-neutrality
caveat. Added "Known limitation -- no byte synthesis" and "Properties of
this candidate physics (not defects)" sections, and an "Instrumentation"
section requiring proposal_emitted / proposal_won / stored_bits_changed
as three distinct trace events from now on.

Applied to AETHER_TEST_PLAN.md (rev 2, 25 items): repaired test 9 (full
bytes, not hash-only), test 14 (renamed/corrected from "toroidal
aliasing" to "degenerate small-lattice self-targeting" to match the
corrected proposal semantics), test 20 (per-arity/per-direction-pair
diagnostics, replacing the old pooled-only win-rate check); added tests
16-19 (same-value WRITE, inert source as target, independent per-field
arbitration, six-component replay identity); added the five Astra test
families as items 21-25 (exhaustive tiny-torus fixtures, independent
oracle, golden arithmetic vectors, deliberate-faulty-implementation
mutation checks, preregistered statistical-diagnostics protocol).

Rewrote AETH-00_REVIEW.md (rev 2): added Meta-status, a "Material changes
made because of the Astra review" section (7 items), a "WHAT AETH-00
PROVES / WHAT AETH-00 DOES NOT PROVE" section, and a "Remaining blockers
to freezing AETH-00" section (questions 27, 28, 31 unresolved; SplitMix64
law specified but not yet implementation-verified).

Updated AETHER_OPEN_QUESTIONS.md: added a message-5 contradiction check
(none found); upgraded questions 6 and 29 to "candidate fully specified,
not yet frozen"; added question 31 (arbitration coordinate-packing range,
<4096 per axis -- permanent or revisit later).

No changes made to AETHER_DECISIONS.md this pass: nothing was frozen or
newly ruled by the operator in message 5 itself (it relayed Astra's
review and gave instructions to incorporate it, not new operator
rulings distinct from D-13/14/15); the meta-ruling and STOP-SHIP
corrections are recorded in AETHER_SPEC.md/AETH-00_REVIEW.md as
candidate-contract content, consistent with the standing instruction to
log AETHER_DECISIONS.md only for things explicitly frozen or ruled.

Did not commit this pass: operator said "do not commit yet." Files
remain uncommitted, staged for review, on top of local (unpushed relative
to this pass) history at 3581269b8.

Reported per message 5's closing instruction and stopped, awaiting freeze
instructions.

## Update, same day: message 6 -- final freeze pass, AETH-00 -> FROZEN v1

Operator issued final rulings resolving all three remaining blockers and
ordered AETH-00 frozen: Q27 (0x01=WRITE is the sole active opcode; every
other value, including 0x00, is RESERVED_INERT -- preserved byte-for-byte,
never coerced to 0x00, never trapping, still writable); Q28 (transition
semantics defined for 1<=H,W<=2^32-1; ordinary runs require H>=3 && W>=3;
H/W in {1,2} remain supported and are mandatory adversarial fixtures);
Q31 (removed, not revised, the <4096-per-axis packed-key limit -- replaced
with unsigned 32-bit coordinates combined as C(row,col)=(uint64(row)<<32)|
uint64(col), fed through an explicit five-step SplitMix64-finalizer chain
h0..h3->priority, unconditionally tie-free per a proof requiring no
coordinate-range caveat; coordinate tie-break removed as unnecessary, a
duplicate-source collision now treated as an implementation defect, not a
law-resolved case). Replay identity's `semantics_version` tag renamed
`semantics_id`; tick starts at 0, a step from tick==2^64-1 is rejected,
never wraps; row increases south, col increases east; arg0/arg1 mod-4
encodings and toroidal mathematical-modulo wrap confirmed as previously
specified. Semantics frozen under the id `aeth00.v1`.

Applied throughout AETHER_SPEC.md: header/currency marked FROZEN v1;
Instruction set section rewritten (RESERVED_INERT framing, 1-active/
255-inert ratio stated explicitly); WRITE semantics marked FROZEN;
"Complete transition function parameters" rewritten with the frozen
dimension domain, ordinary-run policy, and frozen tick-overflow-rejection
behavior; "Collision arbitration" rewritten with the unbounded C()
construction, the five-step chain, the unconditional tie-freedom proof,
and removal of the coordinate tie-break; "Properties of this frozen
physics" gained the 1-active/255-inert bullet; Instrumentation section's
tag renamed to semantics_id.

Applied to AETHER_TEST_PLAN.md (now 27 items, 25 original + 2 added at
freeze): header marked FROZEN v1/`aeth00.v1`; test 12 and the section
intro's "<4096-per-axis supported range" language removed (arbitration
law now has no narrower range than the transition law's own domain);
test 14 (degenerate small-lattice self-targeting) updated to explicitly
require both H/W=1 AND H/W=2 as mandatory adversarial fixtures, not just
size-1; test 15 rewritten from "unknown-opcode-as-NOP" to "RESERVED_INERT
byte preservation" (covers all 255 inert values, not just 0x00, and
explicit never-coerced/never-traps assertions); test 19 and test 9's
`semantics_version` renamed `semantics_id`; added test 26 (tick-overflow
rejection: stepping from 2^64-1 must raise, not wrap) and test 27
(duplicate-source proposal detection: the harness itself must fail a
deliberately defective implementation that emits 2+ proposals from one
physical source in one contest, distinct from test 24's wrong-winner
check).

Rewrote AETH-00_REVIEW.md: currency/header marked FROZEN v1; "Frozen
contract" section replaces "Proposed contract"; added a "Material changes
made in the final freeze pass" section (8 items: RESERVED_INERT reframe,
dimension policy, unbounded arbitration range, five-step chain rewrite,
tie-break removal, tick-overflow freeze, semantics_id rename, formal
FROZEN marking); "Unresolved choices" replaced with "Choices resolved at
freeze" (all four items resolved); arbitration-options and bias-risk
sections updated to drop the <4096 caveat; PROVES/DOES-NOT-PROVE section
updated (unconditional tie-freedom, not range-limited); exact test list
updated to 27 items; "Remaining blockers" replaced with "None" plus one
explicit non-blocking note (implementation-verification, e.g. golden
vectors, still pending, per normal TDD order, not a freeze blocker).

Updated AETHER_OPEN_QUESTIONS.md: added a message-6 contradiction check
(none found; Q31 resolved by elimination of the limit, not revision);
marked questions 6, 27, 28, 29, 31 RESOLVED/FROZEN referencing D-16
through D-19.

Added D-16 (opcode semantics), D-17 (dimension policy), D-18 (arbitration
construction), D-19 (formal freeze of `aeth00.v1`, replay-identity tuple,
tick-overflow rule) to AETHER_DECISIONS.md.

Still no implementation, no code written. Commit pending as this pass's
next step (documentation-only freeze commit).

## Update, later: AETH-00A committed, AETH-00B production begun (between-instance gap)

Between the freeze pass above and this entry, two things happened that
this journal was not updated for at the time: AETH-00A (independent CPU
oracle, dual-oracle differential harness, golden vectors, mutant
catalog, 27-test executable suite -- see Aether/AETH-00A_RECEIPT.md)
was built and committed as `2d2468965`; then AETH-00B (a from-scratch
production `aeth00.v1` transition implementation,
`Aether/production/aeth00.py`, deliberately a third independently-shaped
"gather" algorithm distinct from both AETH-00A reference oracles' shapes)
was built with its own conformance and differential test files
(`Aether/test/test_production_conformance.py`,
`Aether/test/test_production_differential.py`,
`Aether/test/conftest.py`). The operator message authorizing AETH-00B
by that name is not present in `Aether/notes/2026-09-20_raw_notes.md`
(which stops at message 4); it was evidently given directly in chat and
not appended there. Flagging this as a process gap, not a concern about
the work itself.

## Update, same day: bootstrap verification (3rd instance)

This is a new Claude Code session bootstrapping as a 3rd instance of the
Aether seat (roles/Aether/STATUS.md and RESPONSIBILITIES.md read first;
both still show the pre-charter PENDING state, which is stale relative
to the fourth-ecosystem work above -- consistent with the seat's own
rule that direct operator instruction overrides its pre-charter
self-restriction, and already flagged as a tension in the entries
above). Found AETH-00B's production implementation and test files
present in the working tree but uncommitted. Read
`Aether/production/aeth00.py` in full and both new test files; the
implementation is independently structured from the AETH-00A oracles
(gather vs. scatter), imports nothing from `reference.*`, and matches
AETHER_SPEC.md's frozen `aeth00.v1` text section-for-section per its
own docstrings.

Ran the full suite (`cd Aether/test && python -m pytest -q`, all AETH-00A
and AETH-00B tests together, including the AETH-00B differential
property test at 100,000 Hypothesis examples): **84 passed, 0 failed,
814.78s**. No code changed by this pass; verification only.

Did not commit: no explicit commit instruction has been given to this
instance, and every prior commit in this file's history waited for one.
Reported to the operator and stopped, awaiting either commit
authorization or the next instruction.

## Update, same day: AETH-00B completed and committed

Received the operator's explicit AETH-00B instructions directly in
chat (production CPU implementation, differential qualification,
mutant-sensitivity re-check, trace semantics, benchmark, receipt).
Continuing from the state described above (production implementation
and its first two test files already present, 84 tests green), this
pass:

- found and fixed one real validation gap before commit: `bytes(int)`
  silently builds an int-length zero buffer, so `State(1, 1, 4)` would
  have silently succeeded instead of raising -- `data` being a bare
  `int` is now explicitly rejected, with a regression test;
- ran the required differential qualification at full required volume:
  100,000 randomized WRITE/contest-biased cases (production vs. both
  AETH-00A oracles simultaneously) plus 9 explicit hand fixtures (dims
  1/2/3/ordinary, dense contests, RESERVED_INERT-heavy, same-value
  writes, multi-field contests, high-bit seed/tick) -- 0 failures;
- added trace-semantics tests (3 required event kinds present;
  trace-enabled vs. trace-disabled produce byte-identical next states,
  checked by one explicit fixture plus 3,000 generated cases);
- reran the AETH-00A mutant-kill suite unchanged (23 tests): all
  previously-killed mutants remain killed;
- measured a CPU baseline benchmark (~290-330K cell-steps/second,
  roughly flat across world sizes 4x4 to 128x128; transient script,
  not committed);
- wrote `Aether/AETH-00B_RECEIPT.md`.

Final suite: 85 passed, 0 failed (~13 min, dominated by the 100,000-
case property). No contradiction found in `aeth00.v1`; the only
discrepancy was the bare-int validation gap above, fixed pre-commit,
not a spec ambiguity.

## Update, same day: AETH-01 primordial-physics design sprint

Operator ordered a ~2-3h design sprint for AETH-01 (the first serious
primordial-physics candidate, not another conformance specimen).
AETH-00 stayed frozen and untouched throughout; AETH-00B was not
altered. Produced a full review packet under `Aether/AETH-01/` (12
files): three genuinely different candidate physics families
(instruction-lattice, asynchronous chemistry-like reaction automaton,
mobile-particle dynamic-adjacency automaton -- PHYSICS_CANDIDATES.md),
selected and formally specified the first as `aeth01.v1` (DRAFT, not
frozen) -- AETH-00's lattice/WRITE/arbitration law extended with a 5th
`energy` field, an execution cost, a conservative lossy transfer
mechanism, optional maintenance decay/replenishment, and explicit
domain-separated mutation (PHYSICS_SPEC_DRAFT.md, which also folds in
the representation/accessibility analysis). Also wrote ECONOMICS.md
(three resource regimes), EXPERIMENTS.md (seven init regimes,
spontaneous/seeded separation mechanism), HABITABILITY.md (11-label
phase map, scout-tier campaign design), OBSERVATORY.md (15-metric
catalog with cost/false-positive/tier for each), HEREDITY_REQUIREMENTS.md
(four-tier heredity claim ladder, 10 adversarial cases, novelty/
gravitational-pull data-preservation requirements), ADVERSARIAL_ANALYSIS.md
(22 specific ways this design could fool us, each with a control),
REQUIREMENTS.md (R1-R15 compliance table -- 3 of 15 flagged as
unresolved TENSIONS, not silently resolved -- plus engineering
requirements separated into semantic/numerical/reliability/inference),
GPU_RUNPOD.md (batched-worlds architecture, scout->qualify->deepen->
verify funnel, 13-item scope table), DECISIONS.md (14-entry provisional
ledger, each with rationale/alternatives/hidden prior/falsifier), and
ASTRA_REVIEW_PACKET.md (compact index, no steering of Astra's
conclusions). Ran a cross-reference consistency pass across all 12
files (item-numbered citations between files checked against actual
item numbers) before committing. No implementation, no tests, no
Runpod spend -- design only, per instructions.

## Next executable action

Committed as this pass's final step; report given to the operator.
Awaiting the next instruction (likely an independent Astra review of
AETH-01, or a decision on which named tension/rejected-candidate to
pursue first). AETH-00C and AETH-01 implementation both not started.

## Update, same day: AETH-01 repair, qualification, RunPod canary prep

Operator relayed Astra's independent adversarial review
(`Aether/AETH-01/ASTRA_REVIEW_01.md`, verdict `PROCEED_TO_REPAIR`,
commit `b751992f8`) of the AETH-01 design packet (`5e41c1d67`) and
ordered a repair -> qualify -> RunPod-canary-prepare cycle, explicitly
NOT a re-invention. AETH-00's frozen artifacts untouched throughout.

Read every source doc named by the review, then wrote
`REPAIR_LEDGER_01.md`: all 30 Astra items (S01-S06, M01-M09 with
M02/M03 merged into one entry = 21 headers total, N01-N04, B01-B03,
K1-K8 handled separately) individually dispositioned -- 19 ACCEPT, 2
ACCEPT_WITH_QUALIFICATION, 0 REJECT for the 21 ledger items. The most
consequential repair (S01): the reviewed draft's energy-conservation
identity double-counted `TransferLost`/`OverflowSpillage` against
`TransferAttempted`/`TransferCredited`; replaced with the single
authoritative form `E[t+1] = E[t] + R - X - A + C - D`, with `L`/`O`
kept as derived forensic diagnostics, never separately subtracted.
Repaired the accessibility/mutation description (S03), the scientific
inference/construction-vs-relay separation (S04/M05/M08), provenance/
campaign-selection rules (S05/S06/M06), and replay/instrumentation
sufficiency (M09) across `PHYSICS_SPEC_DRAFT.md`, `EXPERIMENTS.md`,
`HABITABILITY.md`, `OBSERVATORY.md`, `HEREDITY_REQUIREMENTS.md`,
`GPU_RUNPOD.md`, `REQUIREMENTS.md`, `ECONOMICS.md`,
`ADVERSARIAL_ANALYSIS.md`, `DECISIONS.md`, `PHYSICS_CANDIDATES.md`.

Hand-worked kill gates K1 (accounting), K2 (mutation/accessibility), K3
(relay vs. constructed capacity), K6 (provenance composition) per
instruction scope, wrote `KILL_GATES_01.md`: all four PASS, nothing
unresolved; K4/K5/K7/K8 explicitly deferred (each needs a running
sweep/oracle/trace that doesn't exist yet), not silently dropped.
Wrote `AETH01_REPAIRED_FREEZE_CANDIDATE.md` stating the full repaired
`aeth01.v1` semantics (still a CANDIDATE, not frozen).

Implemented `Aether/test/reference/oracle_aeth01.py` (naive CPU oracle
restating the repaired law directly from the freeze-candidate text) and
`Aether/test/reference/gpu_aeth01.py` (whole-grid vectorized NumPy
GPU-shaped implementation, independently restated, not importing the
oracle) plus `provenance_aeth01.py` (K6's `instrument_class` composition
rule, made executable). Wrote `test_aeth01_kill_gates.py`,
`test_aeth01_properties.py` (Hypothesis, including a zero-tolerance
accounting-identity property), `test_aeth01_gpu_differential.py`
(fixtures plus 500 randomized CPU-vs-GPU-shape trials, dims 1-4,
multi-tick). 27 AETH-01-specific tests pass; full suite (including all
inherited AETH-00/00A/00B tests) `python -m pytest Aether/test -q`:
**112 passed, 0 failed**, no regressions.

Built `Aether/runpod/aeth01_canary/`: `README.md`, `Dockerfile` (CUDA
12.4 + CuPy), `aeth01_gpu_kernel.py` (CuPy-if-available/NumPy-fallback
port of `gpu_aeth01.py`), `aeth01_cpu_oracle.py` (verbatim copy of the
oracle), `run_canary.py` (200-trial on-pod differential check, writes
`receipt.json`), `watchdog.sh` (10-minute hard wall-clock kill),
`build_and_push.sh`/`launch_pod.sh` (confirmation-gated, reads no
credentials)/`terminate_pod.sh` (none run), `receipt_schema.json`.
Locally smoke-tested `run_canary.py` on the NumPy fallback path: 200/200
matched, receipt shape validated against the schema, scratch receipt
then deleted (not committed). **No RunPod spend; nothing launched.**

Wrote `REVIEW_PACKET_REPAIR_01.md` as the handoff packet for the next
independent review, summarizing the ledger disposition, kill-gate
results, freeze candidate, test evidence, and the canary package
(inventory, exact command, EXAMPLE-ONLY cost calc, expected receipt,
known unresolved risks), without asking Astra to approve a freeze or a
RunPod launch.

Not committed yet as of this journal entry; commit and push are this
pass's next and final step, per instruction to stop before any RunPod
launch.
