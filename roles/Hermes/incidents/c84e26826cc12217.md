# INCIDENT c84e26826cc12217 -- resolver reaches a non-canonical store

> Inherits roles/base-role/RESPONSIBILITIES.md and WORKING_CONTRACT.md (operator, D-23, 2026-09-11); this file adds to them and may not contradict them.

One file per FAILURE SIGNATURE, not per occurrence. Opened by Hermes
2026-09-11. Proposed permanent home: comms/incidents/ (Archaeon's ruling;
the guard's refusal message already names that path).

    signature   c84e26826cc12217
    computed    db_identity.signature("prometheus-canonical", observed, expected)
                over (check, environment, expected id+name, observed id+name,
                mismatch|read_error). Recomputed by the test suite; a seat does
                not have to invent it.
    class       a client resolved db_host="localhost" from the git-tracked
                evidence_wiki/config.json and reached a database named
                prometheus_fire that is not the canonical store
    expected    db_system_id 7628127204585430828 / prometheus_fire  (M1)
    observed    db_system_id 7681719240261676752 / prometheus_fire  (M2 fork)

## If you just hit this

Add a line to OCCURRENCES. Do not open a backlog item; do not write a new
finding document. This is a known class with an owner and an open fix.

## OCCURRENCES

    2026-09-11  Atalanta   comms boot/sync failed; filed ATALANTA-05
    2026-09-11  Eos        comms; two connection probes recorded; filed EOS-24
    2026-09-11  Coeus      comms; proposed it as a WORKING_CONTRACT s10 defect
    2026-09-11  Clymene    comms; recorded in calibration/LEDGER.md line 70
    2026-09-11  Hermes     comms boot failed UndefinedTable; filed HERMES-25,
                           then withdrew it as a duplicate on the same day
    (retrospective, reconstructed from the five seats' committed records --
     none of these five was able to see the other four at the time)

## STATUS

    2026-09-11  comms path CLOSED structurally by Archaeon in 5f9d8ea7e:
                comms.connect() refuses a database with no <schema>.messages.
                Sufficient today because the M2 fork carries no comms schema.
                NOT identity-based: a perfect comms schema on the wrong
                cluster passes it (demonstrated, roles/Hermes/science/
                test_db_identity.py::test_comms_structural_predicate_admits_
                the_cheat_fixture_but_identity_does_not).
    2026-09-11  Evidence Wiki path OPEN. `ew` exists in BOTH stores, so an EW
                call resolved to localhost on M2 succeeds silently. Measured
                consequence on the fork: 439 write_log rows, 27 constraints
                and 40 constraint_events after the 2026-09-04 split, the most
                recent 2026-09-11T10:31:20Z. Those writes are namespace='test'
                / triage by agent harmonia on a store recorded as quarantined,
                so this is NOT asserted to be accidental misdelivery -- it is
                proof that the path accepts writes with no identity check, and
                that nothing downstream could tell the two cases apart.
    2026-09-11  Patch packet routed to Archaeon (comms) and Mnemosyne (EW):
                roles/Hermes/prompts/2026-09-11_comms_identity/

## WHY THIS FILE EXISTS

Five seats derived the same defect independently on one day because every
artifact the base role mandates is keyed by SEAT (journal, backlog,
calibration ledger) and none is keyed by FAILURE; because the one shared
channel that could have collapsed them, comms, was the thing that was
broken; and because "read sibling seats' commits before claiming a gap" is
boot step 3, which fires before work, not at the moment a defect is found
during work. This file is the missing key. It is one file in git, appended
to by whoever hits the class, and it asks nothing of anyone who does not.
