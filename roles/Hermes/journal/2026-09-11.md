# Hermes journal -- 2026-09-11 (adoption pass)

> Inherits roles/base-role/RESPONSIBILITIES.md and WORKING_CONTRACT.md (operator, D-23, 2026-09-11); this file adds to them and may not contradict them.

What happened, not what was wished. First journal entry this seat has
ever had.

## Order of the pass

1. Ran `git pull` in the canonical checkout D:\Prometheus as the first
   command of the session, on the operator's "pull the latest"
   instruction, BEFORE reading WORKING_CONTRACT.md which forbids both
   the pull and mutating operations in the canonical checkout. Output:
   "Already up to date." No tree change, no merge. Recorded in
   calibration/CALIBRATION.md row 8. Everything after this ran from a
   linked worktree.
2. Read agents/hermes/README.md and listed roles/ and roles/base-role/.
3. Read the base role in full at 8714b2709: README.md, NORTH_STAR.md,
   RESPONSIBILITIES.md, WORKING_CONTRACT.md, INHERITANCE.md,
   MONITORS.md. Then comms/README.md and the backlog schema
   (roles/Archaeon/prompts/2026-09-10_backlog/00_BACKLOG_SCHEMA.md).
   Read roles/Icarus in full as the model for a seat whose roles/
   directory was created on its own adoption pass.
4. Confirmed the workspace guard: in D:\Prometheus, `git rev-parse
   --git-dir` equals `--git-common-dir`, so it is the main worktree and
   the seat must refuse to work there.
5. `git fetch origin` (rc 0). origin/main = 8714b2709, identical to the
   local HEAD.
6. `git worktree add D:\Prometheus-worktrees\hermes-base-role -b
   hermes/base-role-adopt-2026-09-11 8714b2709...` run UNBOUNDED per
   WORKING_CONTRACT s3. 39,284 files, completed, tree clean. Guard
   verified in the new worktree: git-dir
   D:/Prometheus/.git/worktrees/hermes-base-role differs from
   git-common-dir D:/Prometheus/.git.
7. Archaeology (below), all of it by reading, none by running.
8. Wrote roles/Hermes.
9. comms boot, then sync (below).
10. Registered rows in INHERITANCE.md and MONITORS.md.

## Archaeology -- the commands and what they returned

    git log --all --oneline --format='%h %ad %s' --date=short \
        -- agents/hermes scripts/send_brief_email.py

  60 committed digests 2026-03-23 to 2026-04-01 under
  agents/hermes/digests/, produced by agents/hermes/src/hermes.py as the
  last stage of the Pronoia scan chain. Then eleven commits 2026-05-15
  (74c4c5834) to 2026-05-23 (cce4505ed) that built the reporting layer
  which replaced it, under the same name and the same credentials.

    cat pivot/hermes_deprecation_2026-05-17.md

  The seat was formally deprecated on 2026-05-17 by Aletheia after an
  architecture clarification from the operator: the digest agent was
  bound to one pipeline on one machine, and reporting had to aggregate
  across machines. run_hermes was commented out of pronoia.py.

    grep -n -i hermes pivot/agent_roster_2026-05-28.md \
        pivot/COMPONENT_DISPOSITION_PLAN_2026-06-23.md \
        pivot/PROCESS_TABLE_2026-06-24.md

  Roster 05-28: "Hermes | ? | pipeline-stage | deprecated | alerting".
  Disposition plan 06-23 row 43: DELETE, "[needs James confirm]".
  Process table 06-24: "suggest: RETIRE-after-HITL". No operator
  confirmation of either is in the record. The operator seating the seat
  today supersedes both proposals.

    git log -1 --format='%h %aI %s' --grep='auto: portfolio update'

  64de18126, authored 2026-09-08T22:15:15-04:00, message "auto:
  portfolio update 2026-09-09T02:15:10Z". The producer of
  docs/portfolio_brief.md -- scripts/intelligence_loop.py, Hermes
  lineage, 4-hour cadence -- has produced nothing since. Measured at
  2026-09-11T15:16:31Z: 61.0 hours, about 15 missed cycles. The
  auto-commit stream is the only freshness record the loop has, and it
  is readable without running it.

    git log --format='%ad' --date=short --grep='auto: portfolio update' \
        | uniq -c | head

  6 per day on 09-02 through 09-08, confirming the 4-hour cadence held
  right up to the stop. The loop did not degrade; it stopped.

    grep -n "stale|mtime|Generated|unchanged|sha256" scripts/send_brief_email.py

  No match on any of them. The mailer has no staleness guard, no payload
  hash and no unchanged-payload check. If it is still running it has
  delivered the same 61-hour-old brief with a fresh envelope and nothing
  on its face saying so.

    sed -n '/AGENT_REFS/,/^}/p' scripts/send_brief_email.py

  Five registered agents: Hephaestus, Apollo, Nous, Ergon, Agora, all
  pointing at May pivot autopsies and apollo/RESUME.md. Nous is gone,
  Hephaestus's M3 host is gone, apollo/RESUME.md is known stale, and the
  roster today has more than thirty seats none of which are listed. The
  email's "smart" reference router routes to dead documents.

  PowerShell on this host: no process and no scheduled task matching
  intelligence_loop, send_brief_email, portfolio_monitor,
  metis_portfolio or pronoia. Host is SPECTREX5 (M2). The machinery runs
  on M3 or M4 and is NOT observable from this seat.

## comms -- and the resolver defect found on the way

`python -m comms boot Hermes` first failed with `ValueError: unknown
agent 'Hermes'`: the roster is derived from roles/*, so the seat was
literally unaddressable until its directory existed. Created the
directory, retried, and got:

    psycopg2.errors.UndefinedTable: relation "comms.agents" does not exist

Checked what the resolver had actually connected to, without reading any
credential:

    select current_database(), inet_server_addr(), inet_server_port()
    -> ('prometheus_fire', '::1', 5432)
    schemas present: ['public']

That is M2's LOCAL FORK of prometheus_fire. It has the right NAME and
not the property that matters (it does not carry the comms schema). The
canonical comms tables are on M1.

`python -m comms init` was NOT run. It is idempotent DDL and would have
succeeded, and it would have created a second comms schema on M2 that
passes every name check and is a silently forked program inbox. That is
the base role's opening failure mode -- resolve by capability, never by
name -- and this seat would have committed it against the program's own
inbox. Failed closed instead, then used the documented override from
evidence_wiki/docs/OPERATIONS_V1.md line 39:

    EW_DB_HOST=192.168.1.202 python -m comms boot Hermes \
        --model "claude-opus-5[1m]" --capabilities TOOLS,any --status active

    booted Hermes on SPECTREX5 | model claude-opus-5[1m] (heavy) |
    8714b2709 @ D:\Prometheus-worktrees\hermes-base-role
    [hermes/base-role-adopt-2026-09-11] | session ea8104ce

    EW_DB_HOST=192.168.1.202 python -m comms sync Hermes
    == Hermes sync at 2026-09-11T15:27:05+00:00: 2 new, 0 queued;
       queue length 0

  [1]  Archaeon -> * broadcast, 07:31: comms live, base role applies,
       sync before and after every prompt.
  [39] Archaeon -> * ruling, 10:47: every seat adds its OWN two rows to
       INHERITANCE.md on its adoption commit; the register is a receipt,
       not a permission. Also records, unfixed, that comms boot refuses a
       seat whose roles/<Seat>/ is absent while sync accepts it -- which
       is exactly what happened here an hour later.

  Nothing addressed to Hermes. Queue length 0.

[ANNOTATED, same day: see "Correction" below -- this was NOT a new
finding; four M2 seats filed it first. The paragraph is left standing
because corrections are annotations beside the original, never silent
rewrites.]

The resolver defect is filed as HERMES-25 and belongs to comms and the
Evidence Wiki resolver, not to this seat. It is reported, not patched
from here (lane discipline). The right fix is a capability check --
does this connection carry comms.agents -- not a host default.

## What was NOT done, and why

By the operator's explicit scope limit ("Don't do anything other than
this bootstrap and registration"): no email sent, not even a dry run;
no loop started, stopped or restarted; the dead producer not revived;
no backlog item begun; scripts/send_brief_email.py and
scripts/intelligence_loop.py not modified, not even to add the D-23
guard they lack. `python -m comms init` not run, for the separate reason
above. No credential file opened, at all, at any point.

## Correction, same day: HERMES-25 was not a new finding

A full-repository grep for EW_DB_HOST had been left running in the
background while this pass proceeded on a scoped one. It returned after
the adoption commit was pushed, and it overturned the framing (not the
action):

    roles/Atalanta/BACKLOG_H0H5.md:18   ATALANTA-05, same gap
    roles/Atalanta/STATUS.md:44         "required for every comms call"
    roles/Eos/BACKLOG_H0H5.md:34        EOS-24, same gap
    roles/Eos/journal/2026-09-11.md:39  two connection probes
    roles/Coeus/journal/2026-09-11.md:106-124  same gap, and proposes it
                                        as a WORKING_CONTRACT s10 defect
    roles/Clymene/calibration/LEDGER.md:70     same gap

Four seats seated on M2 today hit it before Hermes did. Failing closed
was still right; calling it this seat's finding was not. HERMES-25 is
annotated SUPERSEDED and re-aimed as HERMES-25b: the thing none of the
five reports contains is that five seats hit the same wrong database on
the same day, which is the argument for fixing the RESOLVER (refuse a
connection that lacks the expected schema) rather than documenting an
override in a fifth journal. No comms message had claimed the finding,
so nothing is owed a retraction. Calibration row 9.

The cause is boot step 3, skipped for this one item: read sibling seats'
commits before claiming a gap.

## Next executable action

HERMES-01, the delivery ledger. It depends on no open decision.
