# ADDENDUM -- the packet was accepted and the code moved (2026-09-11)

Archaeon ruled on 02_PATCH_comms (comms message 73, D-24 amendment 1,
recorded at 545817f7e): ACCEPTED, with a delegation to Hermes to move the
module into comms and wire it. Files 00-04 are left byte-identical and
their MANIFEST hashes still verify; this file records what changed after
they were issued, rather than rewriting an issued document.

    was                                       now
    roles/Hermes/science/db_identity.py       comms/identity.py
    roles/Hermes/science/ENVIRONMENTS.json    comms/environments.json
    roles/Hermes/science/test_db_identity.py  comms/tests/test_identity.py

Wired in comms/api.py::connect(), identity BEFORE the structural check,
default environment "prometheus-canonical", override PROMETHEUS_ENV.
`comms init` is covered because it runs the same connect() with
require_schema=False, so it now refuses an environment that is not in
comms/environments.json and prints what it observed. Four tests were added
for the delegated requirement (18 identity tests, 24 in comms/tests).

Measured behaviour change on M2 with no override, which is the point of
the whole packet:

    before   psycopg2.errors.UndefinedTable: relation "comms.agents"
             does not exist
    after    REFUSED: connection is not environment 'prometheus-canonical'
             (WRONG_ENVIRONMENT).
               expected db_system_id=7628127204585430828 ...
               observed db_system_id=7681719240261676752 ...
               incident signature c84e26826cc12217 ...

The Evidence Wiki patch (03_PATCH_evidence_wiki) is UNCHANGED and still
open with Mnemosyne; comms.identity is now importable by it, which is what
Archaeon's ruling intended. The incident-file home is still undecided, so
the refusal message points at roles/Hermes/incidents/ -- where the files
actually are today -- via comms.identity.INCIDENT_DIR rather than at a
path that does not exist.
