====================================================================
APOLLO -- BASE ROLE ADOPTION RECEIPT
2026-09-11
====================================================================

1. WHAT I READ, AND AT WHICH SHA

   base role read at   7466bd6ac (origin/main at fetch time, 11:2x UTC)
   files read in full  roles/base-role/README.md
                       roles/base-role/NORTH_STAR.md
                       roles/base-role/RESPONSIBILITIES.md
                       roles/base-role/WORKING_CONTRACT.md
                       roles/base-role/INHERITANCE.md
                       roles/base-role/MONITORS.md
                       roles/Archaeon/prompts/2026-09-11_workspace/
                         MISSIVE_ALL_SEATS.md, CANONICAL_STATUS_2026-09-11.txt
                       roles/Archaeon/prompts/2026-09-10_backlog/00_BACKLOG_SCHEMA.md
   also read           roles/Apollo/{STARTUP.md, STATUS.txt, CHARTER.md,
                       CHARTER_GEN2_serendipity_20260901.md};
                       apollo/cycles/S1_archive_value/RULING_2026-09-01.md and
                       gate/gate_stackvm-v1_50b5c2327c64.json;
                       integration/SFE_CLIENT_GUIDE.md (route inventory);
                       roles/Archaeon/H0H5_STATUS.md; Charon's (383818522) and
                       Ergon's (772edf15e) adoption commits as templates;
                       roles/Charon/apollo_e9/ (E9 battery delivered, 5097b0c8f).
   NOT read            aporia/doctrine/critical_memories.md in full this pass
                       (the base role cites it; the parts it cites were read
                       through the base role). Listed so it is not implied.

   MY INHERITANCE ROW
     | Apollo | CHARTER.md (already) |
   CHARTER.md carries the banner. CHARTER_GEN2 (the ACTIVE mission
   document) did not; stamped in this pass as an annotation (section 4).
   Apollo had no BOOTSTRAP.md and no RESPONSIBILITIES.md: a fresh session
   would have read STARTUP.md (a Gen-1 frontier log) first. BOOTSTRAP.md
   now exists and is the entry file; STARTUP.md is annotated.

2. WORKSPACE RECEIPT (WORKING_CONTRACT s4)

   worktree_path   F:\Prometheus-worktrees\apollo-base-role
   branch          apollo/base-role-adopt-2026-09-11  (task branch)
   base_sha        7466bd6ac79b5a247f9ac50e355f2244d79883a8
   dirty           false at creation
   main_worktree   false  (archaeon.workspace.receipt() from this tree;
                   the guard predicate is True on the canonical path and
                   False here, checked both ways)
   machine         M1 (SKULLPORT)
   worktree add    39092/39092 files, no timeout (600 s budget)
   merged in       9f6bc3ae8 (named-SHA merge, D-23 s3) after origin/main
                   advanced during the pass; one additive conflict in
                   MONITORS.md (three seats' rows at one anchor), all kept
   comms boot      recorded (base s1.1 as amended at 9f6bc3ae8): heavy
                   tier, capabilities ENGINE,EVIDENCE,H3,TOOLS

   Apollo footprint before this worktree: 0 worktrees, 0 local branches,
   0 scheduled tasks (schtasks), 0 processes, 0 modified or untracked
   files of Apollo's in the canonical checkout, 1 stale pid file
   (apollo/run_branch_c/run.pid, June; tracked; APOLLO-12).

3. CLEAN-UP OF THE CANONICAL CHECKOUT (missive items a-e)

   a. nothing to claim: no Apollo file among the 35 M + 96 ?? in the
      snapshot or in today's `git status` of the canonical checkout.
   b. stale worktrees of mine: none. merged branches of mine: none.
   c. long-running processes: none (Gen-1 Granite server and evolve runs
      are dead since June; nothing to move).
   d. startup refusal on entry points I own: DONE for the three Foundry
      entry points (apollo/serendipity/{s1_campaign,slice_pilot,
      source_viability_gate}.py) via apollo/serendipity/workspace_guard.py,
      which inherits archaeon.workspace (rule 1). Gen-1 scripts under
      apollo/scripts/ and apollo/src/ are dormant and NOT guarded in this
      pass (they write nothing canonical; listed, not hidden).
   e. this receipt.

4. SEAT FILES CHECKED AGAINST THE BASE ROLE; CONFLICTS ANNOTATED, NOT
   REWRITTEN

   CHARTER.md (Gen-1, durable identity)
     SUPERSEDED  "On startup, read STARTUP.md first" (base s1; BOOTSTRAP.md)
     SUPERSEDED  "Keys load via D:\Prometheus\keys.py" (drive letter, s9)
     SUPERSEDED  "Use absolute paths with drive letter in every reference"
                 (the base role names this line explicitly)
     HISTORICAL  "on M2 (dedicated GPU window)"
     NO CONFLICT doctrine section (failure shapes, Goodhart default,
                 search-operator before substrate) -- now constitutional.
   CHARTER_GEN2_serendipity_20260901.md (ACTIVE mission)
     STAMPED     banner added (annotation; it is the active document)
     MEASURED    s0a premise "the next Foundry release": does not hold in
                 the specified form (section 7). s0a itself is unchanged.
     SUPERSEDED  "canonical remote.py lives at D:\ZeusE\..." (drive letter)
     ANNOTATED   s8-9 jobs A-L never installed; DORMANT by ruling; MONITORS
     ANNOTATED   s10 STATUS.txt is the seat's STATUS.md equivalent
     MIGRATION   s10a review packets restate base s4; kept dated
   STARTUP.md
     SUPERSEDED  its role as entry file; annotated as the Gen-1 frontier
                 log with drive-letter references read as repo-relative.
     NO REWRITE  every verdict in it stands as recorded.

5. MANDATORY ARTIFACTS CREATED OR UPDATED

   roles/Apollo/BOOTSTRAP.md                 entry file; base chain first
   roles/Apollo/STATUS.txt                   rewritten; PRESENT/ACTIVE/
                                             PRODUCTIVE/VALID stated
   roles/Apollo/journal/2026-09-11.md        this pass, what was NOT run
   roles/Apollo/BACKLOG_H0H5.md              22 rows in the 09-10 schema,
                                             2 XL (APOLLO-04, APOLLO-22)
   roles/Apollo/CALIBRATION.md               11 rows, Gen-1 through today
   roles/base-role/MONITORS.md               1 row: Apollo jobs A-L + gate,
                                             DORMANT by ruling, NEVER
                                             INSTALLED, productivity stated
   apollo/serendipity/workspace_guard.py     D-23 refusal (inherits)
   apollo/cycles/S1_archive_value/gate/
     GATE_ELIGIBILITY_2026-09-11.json        the measured INDETERMINATE
   roles/Apollo/prompts/2026-09-11_h3_dead_world_control/
     {00_COMMON,ARCHAEON,MANIFEST}.md        delegation, sha256 at issuance
   roles/Apollo/INBOX_MNEMOSYNE_EW_TIMEOUT_2026-09-11.md   defect report

6. SELF-CHECK RESULTS

   archaeon/tests/test_base_role.py     7 passed, 1 FAILED in 10.22 s at
                                        7466bd6ac; 8 PASSED in 10.20 s on the
                                        merged tree (f97deae5c = HEAD + 9f6bc3ae8)
     the failure was PRE-EXISTING on origin/main at 7466bd6ac and not in
     Apollo's tree: roles/Archaeon/prompts/2026-09-11_comms/00_BROADCAST.md
     committed blob sha256 c1f4b664... (LF, 633 bytes) vs its MANIFEST
     44273b26...; not a CRLF difference (LF-normalised hash identical),
     not trailing whitespace (796d2649...). Both file and manifest landed
     in 7466bd6ac. Fixed centrally by Archaeon in 9f6bc3ae8 after Lexis
     and Hephaestus reported it; Apollo's report is kept annotated as a
     third independent observation and was NOT posted (section 8).
   ASCII check (LANG=C.UTF-8 grep -nP '[^\x00-\x7F]') on every new
     Apollo file, the MONITORS row, the gate artifact and this receipt:
     no lines. The three annotated charter/startup files were already
     non-ASCII (em-dashes, Greek) and are left as they were.
   gitignore: roles/Apollo/journal/** is re-included (.gitignore:274);
     prompts/ and the gate artifact are not ignored.
   Guard: is_main_worktree(canonical) = True; (this worktree) = False.

7. FINDINGS THAT WERE NOT THE TASK (all measured, grade M)

   - THE GATE HAS NO ELIGIBLE INPUT. The S1 ruling authorises one Source
     Viability Gate probe "against the next Foundry release". Measured:
       /v0 Foundry :8799  LISTENING pid 23276; service log last start
                          2026-08-30 00:45:20 release=50b5c2327c64 -- the
                          SAME release the gate FAILED on 2026-09-01.
       SerendipityD       last commit ba879da 2026-09-01 "Remove gen2/:
                          relocated to Prometheus as the SFE".
       SFE :8811 /v2      200; schema 8; engine_source_hash 5380cb90...;
                          54 routes, 0 with search/evaluate/burst/organism;
                          sfe/*.py: 0 mentions of stackvm/pyshgp/treegp/
                          map_elites; executors BitString, NKLandscape.
     The successor is an epistemic ledger, not an evolutionary-search
     service; the gate's G1-G4 cannot be computed over it. Verdict written
     as INDETERMINATE (nothing fired; nothing could have fired), never as
     PASS or FAIL. This creates the operator decision APOLLO-04. My
     recommendation: retire scheduled mining now (the ruling's own branch
     for gate FAIL / Q4 unmet); keep the adapter/replay/fossil/gate code
     as dormant on-demand instrumentation; no endpoint substitution.
   - Apollo's credential kit is not at its configured path on M1
     (kit/token/cert all absent; the cert exists elsewhere; the token's
     M1 location is unknown to this seat). The default is a drive-letter
     path for another host's layout: a base s9 violation in Apollo's own
     code, APOLLO-05. No credential was read or printed.
   - /v0 TLS from this client: default negotiation 000 x4 then 401 x2 over
     six tries; TLS 1.2 pinned 401 x3. The service is UP. Recorded because
     my FIRST probe returned 000 and I nearly wrote "the old Foundry is
     gone" (CALIBRATION.md CAL-11).
   - Evidence Wiki: /health answers an empty body; search_evidence times
     out on read at 60 s (three queries). Reported to Mnemosyne; the
     watchdog row in MONITORS.md already says it cannot see this state.
     APOLLO-07/08 (submitting the S1 and E9 findings) wait on it.
   - The S1 dead-world control is sharper than the "19%" the ruling
     quotes: DEAD_random map_elites coverage 0.1875 (12/64) EQUALS the
     live affine world's 0.1875 at the same budget, and its QD score was
     0.8333 vs 0.0833 (noise fills cells with high values). Coverage
     carries no stream information there; QD score was anti-informative.
     Offered to Archaeon as a negative + cheat control for the H3 archive
     policies (APOLLO-02), with a stated falsifier.

8. BASE-ROLE DEFECTS (WORKING_CONTRACT s10)

   - test_issued_manifests_verify_against_their_files was RED on
     origin/main at 7466bd6ac (section 6); fixed at 9f6bc3ae8 before this
     pass pushed. Apollo's evidence matches Lexis's and Hephaestus's.
     Kept as record; not posted.
   - Observation, not a defect: the manifest test covers only
     roles/Archaeon/prompts/*/MANIFEST.md. Other seats' manifests
     (roles/<Seat>/prompts/) are not checked by the self-test; Apollo's
     is verified by hand in section 6 and again after commit.
   - Observation: RESPONSIBILITIES.md s1.2 names BOOTSTRAP / RESPONSIBILITIES
     / CHARTER / METHOD as the entry order. Apollo had none of the first
     two and a STARTUP.md; Charon reported the same shape. Creating
     BOOTSTRAP.md is the seat-side fix; the register could say which file
     is the entry file per seat.

9. FIRST FIVE WORK ITEMS (base s1.6) -- APOLLO-01 done by this commit

   APOLLO-01  commit + push this pass; verify ancestor. (this commit)
   APOLLO-02  post the H3 dead-stream control delegation to Archaeon by
              comms; message id into the journal. blocker none
   APOLLO-03  the eligibility artifact. DONE in this commit.
   APOLLO-04  XL operator decision: retire scheduled mining now, or
              re-scope the gate to a future search-capable engine.
              recommendation: retire now. blocker operator
   APOLLO-05  foundry_creds kit path configuration-driven, no drive
              letter; preflight names the missing piece. blocker none

   XL rows for the operator's queue: APOLLO-04 (mining retirement now vs
   re-scope), APOLLO-22 (seat disposition after retirement: on-demand
   QD/archive instrument owner for H3, or close and hand infra to
   Archaeon; recommendation: keep as on-demand instrument owner).

10. NEXT EXECUTABLE ACTION

   Push; verify `git merge-base --is-ancestor <sha> origin/main`; post
   APOLLO-02 (delegation to Archaeon) and the two reports (Mnemosyne EW
   timeout; Archaeon manifest mismatch) through comms with the committed
   body files; record the message ids in the journal; then APOLLO-05
   from this worktree on a new task branch.

====================================================================
