# Apollo -- BOOTSTRAP (entry file; currency 2026-09-11)

> Inherits roles/base-role/RESPONSIBILITIES.md and WORKING_CONTRACT.md (operator, D-23, 2026-09-11); this file adds to them and may not contradict them.

Resolve and obey the current base-role inheritance chain BEFORE this seat's
local bootstrap: roles/base-role/NORTH_STAR.md, RESPONSIBILITIES.md (boot
sequence s1, eight rules), WORKING_CONTRACT.md (D-23), MONITORS.md, and
`python -m comms sync Apollo` (D-24). Nothing here restates those mechanics.

## Reading order after the base role (Apollo-local)

1. `roles/Apollo/CHARTER_GEN2_serendipity_20260901.md` s0a -- the STANDING
   DISPOSITION (HITL 2026-09-01): scheduled evolutionary mining is SUSPENDED;
   the only authorized action is one Source Viability Gate probe against the
   next Foundry release. Everything else in that charter is dormant scope.
2. `roles/Apollo/STATUS.txt` -- the machine-readable state (the seat's
   equivalent of STATUS.md, as the Gen-2 charter s10 specifies; ASCII).
3. `roles/Apollo/journal/<newest date>.md` -- what the last pass did and did
   NOT run.
4. `roles/Apollo/BACKLOG_H0H5.md` -- prioritised items; the first five are
   today's; XL rows are the operator's queue.
5. `roles/Apollo/CALIBRATION.md` -- the ledger of Apollo's own wrong calls.
   Read it before trusting any Apollo number, including today's.
6. `apollo/cycles/S1_archive_value/RULING_2026-09-01.md` -- the decision tree
   Apollo implements mechanically.

## What is historical (read for identity, never for state)

- `roles/Apollo/CHARTER.md` -- Gen-1 durable identity; its mission is
  superseded by the Gen-2 charter, its verdicts are NOT rewritten.
- `roles/Apollo/STARTUP.md` -- the Gen-1 (Branch C, blackboard) frontier log,
  2026-05 to 2026-08. It is no longer the entry file (annotated 2026-09-11).
- `apollo/RESUME.md`, `apollo/ROADMAP.md` -- stale since 2026-05-15.

## Adoption record

`roles/Apollo/BASE_ROLE_ADOPTION_2026-09-11.txt` -- what was read, at which
SHA, what was measured, what was annotated, what was NOT done.
