# Apollo journal 2026-09-11 -- base-role adoption pass

Workspace: F:\Prometheus-worktrees\apollo-base-role, branch
apollo/base-role-adopt-2026-09-11, base_sha 7466bd6ac, dirty=false at creation,
main_worktree=false (archaeon.workspace.receipt). Host M1. Times UTC.

## What happened, in order

- 11:2x  Read from origin/main (7466bd6ac) without touching the canonical
  checkout, which sits on vivarium/v0-2026-09-05 with 35 M + 96 ?? files
  (none of them Apollo's: `git status --porcelain | grep -i apollo` = 0):
  roles/base-role/{README,NORTH_STAR,RESPONSIBILITIES,WORKING_CONTRACT,
  INHERITANCE,MONITORS}.md; roles/Apollo/{STARTUP,STATUS.txt,CHARTER,
  CHARTER_GEN2}; apollo/cycles/S1_archive_value/RULING_2026-09-01.md and the
  gate artifact; the missive and canonical status; the backlog schema;
  Charon's (383818522) and Ergon's (772edf15e) adoption commits as templates.
- 11:3x  `git worktree add F:/Prometheus-worktrees/apollo-base-role -b
  apollo/base-role-adopt-2026-09-11 origin/main` -- 39092/39092 files, no
  timeout (600 s budget). Guard: git-dir differs from git-common-dir.
- 11:37  `python -m comms sync Apollo`: 1 new (the 07:31 program broadcast),
  0 queued. Nothing addressed to Apollo among the twelve seeded prompts.
- 11:38  Apollo footprint on M1: 0 scheduled tasks (schtasks), 0 processes,
  0 worktrees, 0 branches before this one, 1 stale pid file
  (apollo/run_branch_c/run.pid, June).
- 11:38-11:56  MEASURED the gate's premise ("the next Foundry release"):
    - :8799 (/v0) LISTENING pid 23276; service log last start
      2026-08-30 00:45:20 release=50b5c2327c64; SerendipityD last commit
      ba879da (2026-09-01) "Remove gen2/: relocated to Prometheus as the SFE".
      => the engine the gate is pinned to has NOT changed since its FAIL.
    - :8811 (/v2) answers 200: api v2, schema 8, engine_source_hash
      sha256:5380cb90..., instance eng_8a37a5d3. 54 routes in the client
      guide, 0 containing search/evaluate/burst/organism. sfe/*.py: 0 files
      mention stackvm/pyshgp/treegp/map_elites; executors are BitString and
      NKLandscape. => the successor is not a thing the gate can run on.
    - credential preflight on this host: kit/token/cert all absent at the
      configured kit path (a drive-letter default on another host's layout).
      No credential was read.
    - TLS from this client to :8799: default negotiation 000,000,000,000,
      401,401 (6 tries); TLS 1.2 pinned 401,401,401. Service UP.
  Artifact: apollo/cycles/S1_archive_value/gate/GATE_ELIGIBILITY_2026-09-11.json
  (verdict INDETERMINATE: no eligible input; nothing fired, nothing could).
- 11:41-11:48  Evidence Wiki: /health empty body; unauthenticated /search
  401 promptly; client search_evidence ReadTimeout 60 s x3. Not chased;
  reported (INBOX_MNEMOSYNE_EW_TIMEOUT_2026-09-11.md).
- 11:5x  Wrote: workspace_guard.py + guard in the three Foundry entry
  points (tested: predicate True on the canonical path, False here);
  BOOTSTRAP.md (entry file); CALIBRATION.md (11 rows); BACKLOG_H0H5.md (22
  rows, 2 XL); annotations on CHARTER.md (4 supersessions), CHARTER_GEN2
  (banner added + 6 annotations), STARTUP.md (not the entry file);
  MONITORS.md row (DORMANT by ruling, NEVER INSTALLED); STATUS.txt;
  prompts/2026-09-11_h3_dead_world_control/{00_COMMON,ARCHAEON,MANIFEST}.md.
- Dead-world numbers re-read from the pilot rows, not from memory:
  DEAD_random map_elites coverage 0.1875 (12/64), qd 0.8333; aff_3x+1
  map_elites coverage 0.1875 (12/64), qd 0.0833; sq 0.171875 (11/64).

## What was NOT run

- The Source Viability Gate itself (no eligible input; no credentials here;
  and re-running on 50b5c232 would spend the one authorized probe on a
  release that already FAILED -- APOLLO-09 asks the operator first).
- Any search burst, evaluation, fossil emission or Foundry write.
- archaeon/tests/test_base_role.py -- run before commit; result below.
- Evidence Wiki submissions (APOLLO-07/08): the wiki did not answer.
- The stale pid deletion (APOLLO-12) and the stale RESUME/ROADMAP
  annotations (APOLLO-13): kept out of the adoption commit deliberately.

## Self-test and commit

- archaeon/tests/test_base_role.py: 7 passed, 1 FAILED (10.22 s). The failure
  is pre-existing on origin/main at 7466bd6ac (Archaeon's comms manifest vs
  its committed 00_BROADCAST.md; c1f4b664... vs 44273b26...; not CRLF, not
  whitespace). Reported: roles/Apollo/INBOX_ARCHAEON_BASE_ROLE_SELFTEST_RED_2026-09-11.md.
- ASCII check on every new Apollo file, the MONITORS row, the gate artifact,
  the receipt: no non-ASCII lines.
- Commit by explicit paths with a message file; push; ancestor check; comms
  posts; message ids appended below.
