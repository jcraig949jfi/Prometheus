# Nemesis journal -- 2026-09-11, second pass: NEMESIS-01

> Inherits roles/base-role/RESPONSIBILITIES.md and WORKING_CONTRACT.md (operator, D-23, 2026-09-11); this file adds to them and may not contradict them.

Operator direction: the adoption receipt is sufficient; spend no more
turns on archaeology; give the seat a real instrument and make her attack
it; Eos's scorer is the first specimen. Operator's hard rule, adopted
into the charter this pass:

> No instrument earns trust merely because it rejects negatives. Nemesis
> must attempt to make it ACCEPT A FRAUD.

Built from 742a6c8b3 in D:\Prometheus-worktrees\nemesis-adopt on branch
nemesis/eos-scorer-attack-2026-09-11.

## Order of work

1. Read `agents/eos/src/intake.py` (449 lines) and its tests in full.
2. PREREGISTERED six predictions in their own commit (73e6f46c1) before
   any attack code existed, saying plainly that the predictions were
   informed by the source rather than blind.
3. Built `roles/Nemesis/science/cheatlib.py` (NEM-01, NEM-02, NEM-03 are
   one instrument, not three) with 12 self-controls, including the
   firing fixture constraint 5 demands: the constant responder is pinned
   at 0.674 on the April ledger, so a control that goes quiet fails a
   test instead of going unnoticed.
4. Built and ran the attack. Three runs; the first two were my own
   defects. Wrote FINDING.md and the report to Eos.

## Result

BOUNDED STATEMENT with one scoped DEATH CERTIFICATE. All six predictions
held.

    POP-NULL  empty claims (positive control)      0/30    0.00
    POP-A     ANCHOR frauds                      200/200   1.00
    POP-B     ACQUIRE frauds                       8/8     1.00
    POP-B_POS marker that really exists            0/3     0.00
    POP-C     RESOURCE, fabricated observation    30/30    1.00

Cheapest crossing ANCHOR fraud: 22 characters plus a borrowed path
(1-char token, 20 filler chars, 1-char rationale).

The scoped kill is RESOURCE: the only state the gate settles by itself,
whose authenticity evidence is the self-asserted string
`observed_by == "eos-intake"`. I tested the falsifier that would have
scoped it away -- something upstream authenticating the field -- and it
did not fire.

Second finding, not about fraud: `capability_absent` greps the working
tree, so in a sparse worktree it saw 43 files where the index holds 97
and reported absence in the same words.

## What I got wrong, twice, before I got it right

Run 1: POP-A at 0.92. The 16 refusals were MY falsifier stripping to 19
characters against a >= 20 floor. Not a gate defence.

Run 2: POP-A at 0.00, which reads like a strong gate. A patch of mine had
silently dropped the `referent=` line, so the builder was submitting
empty claims and the gate was correctly refusing them.

A 0.00 from a broken adversary and a 0.00 from a strong instrument are
indistinguishable in the output. That is the precise confusion this seat
exists to prevent and it happened inside the seat's first attack. Fix
committed: `assert_builder_built_something` proves the adversary
populated the fields the sought state requires before the gate runs, with
POP-NULL explicitly exempt. Run 1 is preserved as
`results_run1_generator_defect.json`; nothing was silently rewritten.

Separately my own `shrink` was caught under-reducing by its own positive
control (stopped at 28 where the known minimum was 5: it bounded single
reductions rather than passes). Fixed to run to fixpoint. Both entries go
to CALIBRATION.md.

## What was NOT done

No byte of `agents/eos/**` was modified. No verdict was issued about
Eos's real items. No other instrument was attacked. POP-B is n=8 because
each member costs a 19-second `git grep`; the eligible count is reported
beside the rate rather than the rate being reported alone. POP-B was not
re-run in a full (non-sparse) worktree, and the finding says so.

## Next executable action

Post the report to Eos, then NEM-14: census of which live instruments in
the program publish a chance floor beside their headline number. The
census is the thing that turns one specimen into a lane.
