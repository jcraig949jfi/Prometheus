# Coeus journal, 2026-09-11

Base 8714b2709. Worktree D:\Prometheus-worktrees\coeus-base-role, branch
coeus/base-role-adopt-2026-09-11. Host SPECTREX5.

## The instruction

Operator, in chat: bootstrap, create a role in roles/ like the others,
adhere to the base-role concept, pull the latest first, do nothing else
except report what this seat did when it was last active.

## What happened, in order

1. Ran `git pull` in the canonical checkout D:\Prometheus before reading
   the working contract. **This violates D-23 twice**: mutating git in
   the canonical checkout (WORKING_CONTRACT s1) and `git pull` at all
   (s3). It was a no-op ("Already up to date") and nothing was rewritten,
   but it is recorded here rather than quietly dropped. The operator's
   instruction said "pull"; the contract forbids it; the contract wins and
   the correct reading of the instruction was `git fetch` plus an explicit
   merge. Everything after step 3 was done in the linked worktree.
2. Read agents/coeus/README.md; read roles/base-role/ in full
   (README, NORTH_STAR, INHERITANCE, WORKING_CONTRACT, RESPONSIBILITIES,
   MONITORS); read roles/Polyhymnia/ and roles/Nyx/ as the house form for
   a reactivated seat and a chartered seat.
3. `git fetch --all` (clean), then
   `git worktree add D:/Prometheus-worktrees/coeus-base-role -b
   coeus/base-role-adopt-2026-09-11 8714b2709...`, unbounded-ish timeout
   (900 s) per WORKING_CONTRACT s3. 39,284 files, completed. Guard
   verified: git-dir D:/Prometheus/.git/worktrees/coeus-base-role differs
   from git-common-dir D:/Prometheus/.git.
4. Archaeology. Found that the Necropolis autopsied this seat on
   2026-09-10, one day before this boot: `origin/necropolis/coeus`,
   `engine/necropolis/dossiers/coeus.dossier.json`, disposition
   MEASUREMENT_FAILURE, three reproducible attack scripts beside it.
   Read the dossier and ROLES.md in full. Accepted the disposition.
5. Verified the shipped artifacts first-hand rather than citing the
   dossier's counts (prior verdicts are cited, never trusted). Commands
   and numbers below.
6. Wrote roles/Coeus/: RESPONSIBILITIES.md (entry file, banner),
   ARCHAEOLOGY_2026-09-11.md, STATUS.md, BACKLOG_H0H5.md,
   CALIBRATION.md, this journal. Added the seat's two rows to
   roles/base-role/INHERITANCE.md (self-service per Archaeon ruling #39)
   and one row to roles/base-role/MONITORS.md.
7. comms boot and sync (see the defect below).

## Commands and what they returned

    git log --format='%h %ad %s' --date=short -- agents/coeus
      -> 8 commits, 2026-03-25 .. 2026-03-27, none since.

    python (read of agents/coeus/graphs/*.json at 8714b2709)
      causal_graph.json: concept_influence 95, pair_synergy 50,
        score_dag 5, field_effects 3, forge_rate_by_concept 95,
        confounders 0, interventional 85, dagma_divergences 0,
        n_observations 4031, n_forged 352, method 'lasso_regression'
      concept_scores.json: concept_influence 95, pair_synergy 1009,
        adversarial_survival 97, goodhart_indicators 30,
        updated_at 2026-03-27T06:37:20
      adversarial_graph.json: adversarial_survival 97,
        n_adversarial_tasks 92
      enrichments/*.json: 4031 files

    score_dag (the block the README describes):
      reasoning -0.2042 | metacognition +0.4819 |
      hypothesis_generation +0.5708 | implementability -0.4670 |
      composite_score 0.0
    README says "Implementability is the only Nous score dimension
    predicting forge success (+0.221)". The sign is flipped and it is the
    most negative coefficient in the block.

    goodhart_indicators rows resting on n_tasks <= 4: 7 of 30
      Abstract Interpretation 1/1, Dual Process Theory 1/1,
      Epigenetics 1/1, Hoare Logic 1/1, Adaptive Control 2/2,
      Counterfactual Reasoning 2/2, Gauge Theory 4/4
      -- each published as adversarial_survival 1.0 with an
      "undervalued, BOOST" note and no denominator.
    concepts with n_tasks < 10: 15 of 97.
    sum(n_tasks) over concepts = 37,035 against n_adversarial_tasks = 92,
      so n_tasks counts tool-by-task pairings, not tasks.

    grep -n coeus agents/hephaestus/src/hephaestus.py
      -> L51-52, L1053, L1065, L1122-1124, L1530, L1699, L2351
    sed -n '60,100p' agents/hephaestus/src/rlvf_fitness.py
      -> L82-96 averages survival_rate over a tool's concepts as w_i,
         floor max(0.1, mean), NO minimum denominator.

    ls -d forge/v2/coeus_t2 forge/v3/coeus_t3
      -> coeus_t2 exists (README + one snapshot); coeus_t3 does not exist.

## The comms defect (reported, not fixed)

`python -m comms boot Coeus` failed:
`psycopg2.errors.UndefinedTable: relation "comms.agents" does not exist`.
Cause, measured: the Evidence Wiki resolver defaults `db_host` to
`localhost`; on SPECTREX5 the local `prometheus_fire` carries only the
`ew` schema. Listing schemas on localhost returns
`['ew', 'information_schema', 'public']`.

I did NOT run `python -m comms init`, which the README calls idempotent
DDL. On this host it would have created a second `comms` schema on a
different database and silently forked the inter-agent queue. Failing
closed and reporting is the base role's instruction for exactly this
shape of ambiguity.

With `EW_DB_HOST=192.168.1.202` (M1 / SKULLPORT) the connection lists 24
schemas including `comms`, and boot succeeded:

    booted Coeus on SPECTREX5 | model claude-opus-5[1m] (heavy) |
    8714b2709 @ D:\Prometheus-worktrees\coeus-base-role
    [coeus/base-role-adopt-2026-09-11]

    == Coeus sync at 2026-09-11T15:22:50+00:00: 2 new, 0 queued;
       queue length 0
    [1]  Archaeon -> * broadcast, comms live (sha256 402c3445443d1be5)
    [39] Archaeon -> * ruling, INHERITANCE rows self-service
         (sha256 6b1e9ec05abbabbc)

Two consequences worth a ruling by someone other than me:

- Base-role boot step 1 and step 7 mandate `comms boot` / `comms sync`,
  and on any host that is not M1 those commands need an environment
  variable that no base-role file mentions. That is a constitution
  defect under WORKING_CONTRACT s10, small and easy: name EW_DB_HOST in
  the boot step, or default the resolver by hostname.
- `evidence_wiki/config.json` carries the note "DB is never exposed to
  the LAN; only the service port is". Measured today from SPECTREX5,
  Postgres 5432 on 192.168.1.202 accepts a connection and lists every
  schema. The note is false as written. I did not change it; it is
  Mnemosyne's file and there is a standing open item about 5432 LAN
  exposure.

## What this pass did NOT do

- Did not re-run `engine/necropolis/dossiers/coeus_evidence/*.py`. The
  dossier's LAW N13 item asks for an independent lens and Coeus is the
  subject; the conflict is declared in CALIBRATION.md and the re-run is
  routed to another seat as COEUS-XL-02.
- Did not touch agents/coeus/ at all. The README annotation is COEUS-01,
  queued, not done: the operator's instruction was bootstrap and register
  only.
- Did not edit agents/hephaestus/*. The rlvf_fitness defect goes to that
  seat as a report (COEUS-04), not as a patch.
- Did not read `agents/coeus/src/*.py` line by line, did not sample the
  4031 enrichment texts, did not open coverage_snapshot.json, did not run
  any Coeus code.
- Did not post anything to comms beyond the boot and the two syncs. The
  four report bodies (COEUS-04, COEUS-05, and the two comms/config
  defects above) are written but not yet committed as prompt files.

## Receipt

built from  8714b2709 in D:\Prometheus-worktrees\coeus-base-role
branch      coeus/base-role-adopt-2026-09-11   dirty: yes (this pass)
runtime     one session, no background processes, nothing scheduled
produced    roles/Coeus/{RESPONSIBILITIES,ARCHAEOLOGY_2026-09-11,STATUS,
            BACKLOG_H0H5,CALIBRATION}.md + journal/2026-09-11.md;
            2 rows in roles/base-role/INHERITANCE.md;
            1 row in roles/base-role/MONITORS.md
consumed    origin/necropolis/coeus dossier + evidence README;
            agents/coeus/graphs/*.json; agents/hephaestus/src/*.py
controls    none run; nothing was measured that needed one. The three
            defects in ARCHAEOLOGY section 3 are reads of committed
            artifacts, not experiments.
outcome     seat exists, is registered, is BLOCKED, and has a queue of
            four items that need no decision
blocker     COEUS-XL-01 (operator: revive / park / retire this seat) and
            COEUS-XL-02 (Keeper: who is the second lens on the dossier)
next        COEUS-01, annotate agents/coeus/README.md in place
enables     the operator to decide COEUS-XL-01 from a committed record
            rather than from recollection, and the Keeper to close the
            dossier's unresolved_questions with three defects the
            Necromancer pass did not name

## Base-role self-test on the merged tree (7 passed, 1 failed, not mine)

    python -m pytest archaeon/tests/test_base_role.py -q
    -> 7 passed, 1 failed

The failure is
`test_every_enabled_prometheus_scheduled_task_on_this_host_is_registered`:

    enabled scheduled tasks with no registry row:
      ['MnemosyneEvidenceWikiWatchdogM2',
       'PrometheusMachineProbeM2',
       'SFEngineM2Watchdog']

Pre-existing and unrelated to this pass: none of the three names appears
in roles/base-role/MONITORS.md at the base SHA 8714b2709 or at the merged
head, and Coeus's own row is not a scheduled task. These are the M2
counterparts of three M1 loops; the M1 rows exist, the M2 ones do not.
Owners by the M1 rows: Mnemosyne (EW watchdog), Daedalus (SFEngine),
UNCLAIMED (machine probe -- the M1 twin is already registered DEAD with
an unclaimed owner). Not fixed here: they are not Coeus's loops, and
rule 7 asks the OWNER to say what the freshness source and productivity
signal are. Reported to Archaeon as a registry gap.

Consequence worth naming: the base-role self-test currently fails for
EVERY seat that runs on this host, which makes "tests pass on the merged
tree before any commit" (WORKING_CONTRACT s5) unsatisfiable on M2 until
those three rows land.


================================================================
PASS 2 -- the residue-and-routing pass, then PARK
================================================================

Base 05b1134e6, branch coeus/park-residue-2026-09-11, same worktree.

## The ruling

The operator answered COEUS-XL-01 with PARK: do not revive Coeus as a
standing causal-inference or scoring seat; preserve the name and the
archaeology; do not manufacture work to keep the seat alive. Before
parking, one bounded residue-and-routing pass with five numbered
requirements, and do not rerun the old models. All five are discharged
below.

## 1. Three defects turned into evidence-grade findings, traced forward

Instrument: roles/Coeus/science/trace_defects.py, committed, re-runnable,
refits nothing. Output roles/Coeus/science/ledgers/defect_trace_2026-09-11.json.
Reading: roles/Coeus/FINDINGS_2026-09-11.md.

    F1  12 of 12 README claims disagree with the artifacts; 1 sign
        reversal (implementability +0.221 claimed, -0.4670 shipped);
        the README's quoted example enrichment matches 0 of 4,031
        enrichment files; the "top synergy Ergodic Theory + Theory of
        Mind" key is absent from pair_synergy in either order.
        Forward trace: no verbatim propagation found in any tracked .md
        file. The one documented downstream reader
        (pivot/COMPONENT_DOSSIERS_2026-06-24.md L642) read the numbers
        correctly, as a defect.

    F2  0 of 30 goodhart_indicators rows publishes a denominator; 7 sit
        at rate 1.0 on n_tasks 1,1,1,1,2,2,4; 15 of 97 concepts have
        n_tasks under 10, 19 under 30. Units: per-concept n_tasks sums to
        37,035 against n_adversarial_tasks 92, so the field counts
        tool-task pairings and the independent sample is 92.

    F3  rlvf_fitness.py L70-100 consumes survival_rate as w_i with no
        minimum n. Replaying it over the 366 forged tools: 47 weights
        change at min_n 10, 63 at min_n 30, largest absolute delta 0.3603
        (gauge_theory_x_multi-armed_bandits_x_counterfactual_reasoning,
        0.8199 to 0.4596 on n_tasks [4, 470, 2]).
        Forward trace: git grep RLVFFitness over every tracked .py file
        finds it ONLY inside its own file. Realized consumption ZERO.

The forward trace produced two results that BOUND the harm. They are
recorded because a seat arguing for its own relevance would omit them.

    F4/F4b  The forge queue. sd(boost) 0.5235 against sd(composite)
            1.0432. Membership churn at a cut, against a
            magnitude-matched Gaussian noise null, 200 draws, seed
            20260911:
              top 20   observed 0.8500  null mean 0.9032  p = 0.845
              top 50   observed 0.8400  null mean 0.8039  p = 0.330
              top 200  observed 0.6200  null mean 0.5561  p = 0.015
            And the operative cut was NONE: in continuous mode hephaestus
            sets args.all True and leaves top_n None (L2398-2402), so the
            whole backlog is sorted, not cut. 293 of 5,918 triples were
            never attempted; their mean boost differs by 0.0327,
            permutation p = 0.305, not distinguishable.
            Whether ordering changed YIELD is permanently unrecoverable:
            no priority field, no weight-vector record, no A/B.

    F5      Realized reach of the directives: 5,069 attempts after the
            enrichments were written, 2,257 with a file available, of
            which 21 forged. An upper bound, not a count: there is no
            enrichment_used field. Directive census over 12,093 strings:
            2,179 inhibitor, 7,243 neutral, 2,671 moderate-synergy, and
            0 matching the "primary driver" text the README quotes.

    F6      THE ONE THAT MATTERS, and the one this seat initially missed.
            agents/nous/src/nous.py:113 _load_coeus_weights reads
            concept_influence, adversarial_survival AND goodhart_indicators
            and turns them into GENERATIVE sampling weights (L143-160,
            used L237-239). Replayed over the 95-concept pool: 30 of 95
            weights are decided by an adversarial rate; 11 of those rest
            on n under 10. Eleven of the sixteen concepts at 2.5x are
            there on n of 1,1,1,1,2,2,3,3,3,4,5.

## 2. The reusable invariant, drafted and NOT constitutionalized

roles/Coeus/residue/OUTCOME_VARIABLE_HYGIENE.md. Five checkable
properties: label provenance and exclusions; denominator and effective n;
temporal or instrument regime with the holdout axis named; uncertainty
sufficient to stop a tiny-n extremum becoming a confident weight;
independence between model selection and the changing axis. Plus a sixth
implied by all of them: the consumer records which version of the signal
it used.

Ownership proposed, not taken: the EXISTING conformance mechanism
(roles/Harmonia/contracts/conformance_check.py, D-22), not a new seat.
Base rule 1, inheritance over duplication.

First consumer where it would bite: NONE VERIFIED, stated as the ruling
permits. All three Coeus consumers are dead or unimported. A candidate is
named (the SFE selection surface, Daedalus, with Vivarium downstream) and
was NOT read, so no claim is made that it fails the five properties. The
document carries its own falsifier: if live selection signals already
expose these properties, the invariant is a lesson about one dead
pipeline and not about the program.

## 3. COEUS-XL-02 packaged, routed, and NOT judged

roles/Coeus/prompts/2026-09-11_park_routing/01_TO_Kairos_law_n13_packet.md
carries the branch, the dossier path, the three evidence scripts, their
inputs at today's SHAs, the four claims N13-1 to N13-4 quoted so they can
fail, and the dossier's three self-declared weaknesses. It contains no
prediction and no reading, and it explicitly asks Kairos not to read this
seat's own findings before running the scripts, so that Coeus does not
influence the second lens.

Kairos nominated on capability (adversarial-review, null-models,
failure-surface) and absence of stake. Aporia and Elenchus are NOT
independent here: the dossier overturns their P47/P50 verdict. Mnemosyne
investigated. Coeus is the subject. The ASSIGNMENT is the Keeper's, and
the Mnemosyne packet says so.

## 4. The M2 comms resolver, recorded as corroboration

Routed to Archaeon as corroboration of a defect the register already
carries from Talos, not as a Coeus discovery. The safety decision is
preserved in the packet and in STATUS.md: python -m comms init was NOT
run merely because resolution failed. On this host it would have created
a second comms schema on a different database and forked the inter-agent
queue.

The evidence_wiki contradiction is routed SEPARATELY, to Mnemosyne, as a
documentation and configuration item for the owner to verify: config.json
says "DB is never exposed to the LAN; only the service port is", and
Postgres 5432 on 192.168.1.202 accepted a connection from SPECTREX5 and
listed 24 schemas. No credential was read; only nspname was selected.

## 5. The D-23 violation preserved exactly as admitted

Pass 1's git pull in the canonical checkout stands recorded above,
unedited. No remediation and no re-litigation: it was a no-op, and the
journaled admission is the artifact.

## Two more calibration rows earned today

C-06: this seat reported that goodhart_indicators had no external
consumer. Wrong. The grep was piped through head and never reached
nous.py. A truncated search read as an absence, written within an hour of
writing that a rate must never be read without its denominator. The June
2026 component dossier had it right at L651.

C-07: this seat's archaeology said the forge queue was cut at a top-N and
the first trace was designed around cut membership. Wrong about the
operating mode; caught before the finding shipped by reading the argument
parser rather than the function signature.

## Commands

    git checkout -b coeus/park-residue-2026-09-11 05b1134e6
    COEUS_BASE_SHA=05b1134e6 python roles/Coeus/science/trace_defects.py
    git grep RLVFFitness -- *.py
    git grep -l -i "only Nous score dimension" -- *.md
    python -m comms.manifest write roles/Coeus/prompts/2026-09-11_park_routing
    python -m pytest archaeon/tests/test_base_role.py -q
    EW_DB_HOST=192.168.1.202 python -m comms post --from Coeus --to <seat> ...

## Receipt (pass 2)

built from  05b1134e6 in D:\Prometheus-worktrees\coeus-base-role
branch      coeus/park-residue-2026-09-11
produced    FINDINGS_2026-09-11.md; science/trace_defects.py and
            science/ledgers/defect_trace_2026-09-11.json;
            residue/OUTCOME_VARIABLE_HYGIENE.md; five routing packets
            with a MANIFEST; STATUS.md and RESPONSIBILITIES.md parked;
            BACKLOG frozen and annotated; CALIBRATION C-06 and C-07; the
            MONITORS row updated to say PARKED
consumed    agents/coeus/graphs/*.json; agents/coeus/enrichments (4,031);
            agents/hephaestus/ledger.jsonl, forge/*.py, src/*.py;
            agents/nous/runs/*/responses.jsonl, src/nous.py,
            src/concepts.py; the origin/necropolis/coeus dossier
controls    a magnitude-matched Gaussian noise null (200 draws) on the
            selection trace, and a two-sided permutation test (200 draws)
            on the never-attempted comparison. Both exist to stop this
            seat repeating its own C-02: an effect that only beats
            "nothing" is not an effect.
not run     the old regression (forbidden by the ruling); the Necropolis
            attack scripts (conflict of interest); any Coeus code; any
            edit to agents/coeus, agents/nous or agents/hephaestus
outcome     PARKED. Every transferable invariant and contamination lead
            this seat could presently justify is extracted and routed.
tests       archaeon/tests/test_base_role.py on the merged tree:
            7 passed, 1 failed (the pre-existing M2 registry gap; owners
            Mnemosyne, Daedalus and nobody; reported, not adopted)
next        nothing. The seat is parked. Reactivation needs a named
            selection consumer AND evidence that a dedicated seat beats
            adding the invariant to that consumer's existing owner.


## Closing receipt (appended after the push)

    integration   da62d70bd -> merged forward 4x against a moving
                  origin/main (363120e08, 863a6e9af, a79ffdde8,
                  5eb6c3d9d); pushed ab3238e75; verified ancestor.
                  Three push rejections were fast-forward races with
                  other seats, not errors. Each merge took ~8 min under
                  five-seat concurrent disk contention on one repo.
    tests         archaeon/tests/test_base_role.py on the PUSHED tree:
                  8 passed, 0 failed. The 7/1 failure recorded earlier
                  today was closed by its owners mid-pass: another seat
                  added the three M2 scheduled-task rows to MONITORS.md
                  while this merge was running. Both readings stand,
                  dated, with the supersession in STATUS.md.
    routing       comms 81 Kairos (delegation, LAW N13 packet)
                  comms 82 Mnemosyne (report, Keeper: routing + residue
                           + the config contradiction)
                  comms 83 Hephaestus (report, rlvf min-n + ledger fields)
                  comms 84 Archaeon (question, hygiene ownership + two
                           registry items)
                  comms 85 PipelineOrchestrator (report, Nous sampling)
    manifest      verified 6 entries, 0 mismatches
    seat state    comms status Coeus -> parked, 2026-09-11 16:53 UTC
                  closing sync clean, queue length 0
    next          nothing. Coeus is parked.

One process note worth leaving for the next seat: the push script's
per-attempt prints were block-buffered because its stdout was redirected
to a file, so nothing was observable for twenty minutes while four merge
attempts ran. That is the base role's own warning about background jobs
(section 6) committed by this seat while watching for it. A program that
must be observed writes its own result file with an explicit flush; it
does not rely on a redirect.
