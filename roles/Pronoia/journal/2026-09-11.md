# Pronoia -- journal, 2026-09-11 (adoption pass)

> Inherits roles/base-role/RESPONSIBILITIES.md and WORKING_CONTRACT.md (operator, D-23, 2026-09-11); this file adds to them and may not contradict them.

What happened, not what was wished. The numbers, the commands, the SHAs,
and what was NOT run.

    base_sha        363120e08665af062d40810183624fa23ed19698
    branch          pronoia/base-role-adopt-2026-09-11
    worktree_path   Prometheus-worktrees/pronoia-base-role
    dirty at boot   false
    host            M2 (SPECTREX5)
    canonical       D:\Prometheus (named as evidence about this host)

## 1. The contract breach that opened the pass

First action of the session was `git pull` in the canonical checkout,
acting on the operator's "pull the latest from the repo first" before
D-23 had been read. It ran about two minutes, then was killed on reading
WORKING_CONTRACT.md section 3, which states that such a directive MEANS
fetch-then-worktree and that `git pull` is forbidden outright.

Verified the canonical tree was unharmed before continuing:

    git status --short          -> only evidence_wiki/.gitignore and
                                   evidence_wiki/derived/ (pre-existing,
                                   not mine)
    git rev-parse HEAD          -> 363120e08... (unmoved)
    test -f .git/MERGE_HEAD     -> no merge in progress
    test -f .git/index.lock     -> no index lock

Recorded as PRON-CAL-004. D-23 section 3 already records Atalanta making
the identical mistake earlier today (L-09).

## 2. The conformant sequence

    git fetch origin                    -> no output (already current)
    git rev-parse origin/main           -> 363120e08665af062d40810183624fa23ed19698
    git rev-parse --git-dir             -> .git
    git rev-parse --git-common-dir      -> .git      (equal: canonical, refuse to work here)

    git worktree add D:/Prometheus-worktrees/pronoia-base-role \
        -b pronoia/base-role-adopt-2026-09-11 363120e08...

That checkout took about 30 minutes for 39,496 files and was allowed to
run to completion unbounded rather than killed on a timeout, per D-23
section 3's warning that a killed checkout strands the tree. Guard
re-verified in the new worktree:

    git rev-parse --git-dir         -> D:/Prometheus/.git/worktrees/pronoia-base-role
    git rev-parse --git-common-dir  -> D:/Prometheus/.git      (differ: guard passes)
    git status --short              -> clean
    git ls-files | wc -l            -> 39496

## 3. Era 1 archaeology (pronoia.py, the research-scanning orchestrator)

    git log --oneline --all -- agents/pronoia pronoia.py
    git log --oneline --diff-filter=AD --name-status --all -- pronoia.py

      A  eb17886fa  2026-03-23T09:39:59-04:00  "agents: add Aletheia,
                    Clymene, Hermes, Pronoia + wire full pipeline"
      D  3b3c74bc0  2026-04-23T09:25:09-04:00  "Clean up repo for
                    external visibility"  (24 other root scripts in the
                    same commit)

    git show 3b3c74bc0^:pronoia.py | wc -l          -> 872

THE FILE IS STILL ON THIS HOST, and this was not expected:

    git ls-files --error-unmatch pronoia.py  -> "did not match any file(s)"
    git check-ignore -v pronoia.py           -> .gitignore:174:/pronoia.py
    git cat-file -e origin/main:pronoia.py   -> "exists on disk, but not in origin/main"
    ls -la pronoia.py                        -> 32,165 bytes, mtime 2026-04-11 11:26

So roles/base-role/MONITORS.md is correct that pronoia.py is not in the
tree, and incomplete: a runnable copy survives on disk, invisible to any
seat reading the tree. Its publish_reports() at line 69 does `git add`
over nine globs then commits and pushes to main from its working
directory. NOT DELETED, NOT MODIFIED, NOT RUN on this pass (destructive
and outward-facing; operator's prohibition). Recorded as PRON-02.

### The audit-detector census

    ls agents/pronoia/logs/ | wc -l                -> 42
        (37 audit_*, 4 health_intelligence_*, 1 health_intelligence_enhanced_*)
    date range                                     -> 2026-03-23 .. 2026-04-01

    grep -h '^\*\*Overall' agents/pronoia/logs/audit_*.md | sort | uniq -c
        27  **Overall: UNHEALTHY**
        10  **Overall: DEGRADED**
    grep -l 'Overall: HEALTHY' agents/pronoia/logs/audit_*.md | wc -l   -> 0

    grep -hoE '\[(HIGH|MEDIUM|LOW|INFO)\] [^(]*' agents/pronoia/logs/audit_*.md
        31  [MEDIUM] API error detected
        27  [HIGH] Rate limit or 429 detected
        (no other finding type appears in any of the 37 reports)

    zero_output findings ever                                    -> 0
    knowledge-growth findings ever                               -> 0
    'possible leak' in audit_*.md                                -> 27
    'possible leak' in health_*.md                               -> 0

    grep -h 'GPU memory' agents/pronoia/logs/audit_*.md
        readings include 8909, 8789, 8658, 8619, 986, 964, 953 MiB

Read run_audit() at pronoia.py:455-561 to establish WHY three detectors
produced nothing:
- zero_output is `len(agent_log.strip()) < 20` over captured stdout;
  every agent that ran printed a banner far longer, and agents that did
  not run were recorded SKIPPED and excluded from the loop before the
  check. Eligible count approximately zero across all 37 runs. Not
  quiet: unreachable.
- knowledge growth calls _query_entity_counts(), prints the counts, and
  compares them to nothing. No previous value, no threshold, no finding
  path exists in the code. The README's promise ("flat growth means
  scanner needs tuning") was never implemented.
- the VRAM check runs nvidia-smi after a pipeline made entirely of HTTP
  calls to five external APIs. The multi-gigabyte readings are the Ignis
  training runs sharing the box. 27 of 37 audits carried a finding the
  audited process cannot cause.

And the sharpest evidence, read verbatim from
health_intelligence_enhanced_2026-03-31_191441.md:

    ### PRONOIA
    - **Status**: HEALTHY
    - **Process Running**: Yes
    - **Recent Log Lines**: 0
    - **Errors**: 0
    - **Warnings**: 0

## 4. Era 2 discovery (scripts/intelligence_loop.py)

Found via pivot/COMPONENT_DOSSIERS_2026-06-24.md:600-608, which names
two eras under this seat's name. Verified the attribution rather than
accepting the dossier:

    grep -n -i 'pronoia' scripts/intelligence_loop.py
        3:   "Intelligence Loop -- Pronoia-orchestrator for the
             multi-machine reporting pipeline."
        6:   "...keeping `agent:Pronoia` alive"
        79:  def _start_pronoia_pg_heartbeat(...)
        108: agent_name="Pronoia"
    git log -1 --format='%h %aI %s' -- scripts/intelligence_loop.py
        373d81469 2026-05-23T04:49:14-04:00
        "Pronoia: dual-write heartbeat to Postgres (parity with
         Apollo/Hephaestus)"

roles/base-role/MONITORS.md registers this loop as "OWNER UNCLAIMED
(Hermes lineage code, May 2026)". The evidence says Pronoia. Row claimed
by annotation, not rewrite.

Not running on this host, checked before assuming:

    tasklist /FI "IMAGENAME eq pythonw.exe"   -> no matching tasks
    schtasks /query ... | grep -i intelligence -> nothing
    ls dashboard/intelligence_loop.log         -> absent

### Freshness from git

    git log -1 --format='%h %aI' --grep='auto: portfolio update' origin/main
        64de18126  2026-09-08T22:15:15-04:00  (= 2026-09-09T02:15:10Z in
                                                 the commit subject)
    measured at                    2026-09-11T16:48:58Z
    age                            62.6 h, about 15 missed 4-hour cycles
    cadence before the stop        exactly 6/day on 09-04, -05, -06, -07, -08

It stopped dead. It did not degrade.

### Freshness from the canonical Postgres (M1, EW_DB_HOST=192.168.1.202)

First, a MONITORS.md claim that turned out to be false. The row says the
loop's inputs are "agora.agent_heartbeats and agora.intelligence_outputs
-- May-era tables the current program does not write":

    agora.agent_heartbeats      rows=36     max(last_heartbeat)=2026-09-11 12:57:49-04:00
    agora.intelligence_outputs  rows=15500  max(started_at)=2026-09-11 12:57:06-04:00

Both took writes minutes before the measurement. The input is alive.

Then the finding:

    select agent_name,machine,status,pid,last_heartbeat,
           last_work_attempt_at,last_work_success_at,health
      from agora.agent_heartbeats where agent_name='Pronoia';

      Pronoia | M4 | online | 9620 | 2026-09-11 12:58:14-04:00
              | NULL | NULL | NULL

    rows per day by stage:

      stage                        09-08  09-09  09-10  09-11
      pronoia_portfolio_refresh        6      0      0      0
      pronoia_brief_generated          6      0      0      0
      pronoia_dashboard_pushed         6      0      0      0
      pronoia_email_dispatched         6      0      0      0
      pronoia_cycle_complete           6      0      0      0
      pronoia_weekly_recap             0      0      0      0
      observability_canary            96      6      0      8
      machine_health_m4               24      1      0      2
      agent_started                    0      0      0      1

The rival explanation and what separates it: 09-10 is a total zero day
for every stage including the canary and the machine probe, which
implicates the M4 host. On 09-11 the host is back (canary 8,
machine_health 2, agent_started 1 at 11:03:40) and the heartbeat thread
came back with it, and all five pronoia_* stages are still zero. The
host event explains 09-10 and not 09-11. Nearly stopped at the host
explanation; recorded as PRON-CAL-005.

INDETERMINATE branch, stated: at measurement the restarted process was
1 h 55 m old against an hourly cadence for two of the five stages, so
one missed hour is within normal if those stages fire on a fixed minute.
The four-hourly push missing about 15 consecutive cycles is not within
normal on any reading, and the three heartbeat columns that would settle
it have never been written.

## 5. What was NOT run, deliberately

No loop started. No loop stopped. Nothing on M4 touched (this seat
cannot reach it and did not try). No dashboard refreshed, no brief
generated, no email sent. No scheduled task created or disabled. No file
deleted, including the hazardous untracked pronoia.py. pronoia.py was
never executed. No other seat's document rewritten -- the two MONITORS.md
rows were annotated beside their existing text, per the base role's rule
that corrections are annotations and never silent rewrites. No claim
promoted to any ledger. The Evidence Wiki was not written.

Every database interaction this pass was a SELECT.

## 6. Registry and comms

Added this seat's own two rows to roles/base-role/INHERITANCE.md (role
table and entry-file table) and its rows to roles/base-role/MONITORS.md,
under the Archaeon ruling that a seat adds its own rows and Archaeon is
not the bottleneck.

Manifest written and verified for the operator directive:

    python -m comms.manifest write  roles/Pronoia/prompts/2026-09-11_adoption
    python -m comms.manifest verify roles/Pronoia/prompts/2026-09-11_adoption
        -> verified 1 entries; 0 mismatches
    OPERATOR_DIRECTIVE.md sha256:6537ab39f47f9d322c1cd7a6ea40021415699a1f17779d0a418704cd76e0c532

comms boot and sync are recorded in section 7 below, after the commit,
because `comms boot` refuses a seat whose roles/<Seat>/ is not on the
tree it runs from (INHERITANCE.md, Talos's observation).

## 7. Boot, sync and push

Recorded at the close of the pass; see the receipt in STATUS.md and the
push verification below.
