# 2026-09-24 charter (mission)

The operator's mission for Ananke, verbatim, in chat on 2026-09-24,
directly after the creation receipt: 01_OPERATOR_MISSION_verbatim.md.
It is kept byte-for-byte including its non-ASCII punctuation (curly
quotes, em-dashes, the multiplication sign and section rules): the
verbatim directive outranks the base role's ASCII rule, which governs
base files and paste blocks, and every summary of it (including this
seat's RESPONSIBILITIES.md) is ASCII.

Committed on its own, before any charter rewrite, design, preregistration
or code, so the order is visible in git history.
