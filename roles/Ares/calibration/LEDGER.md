# Ares calibration ledger

Currency: 2026-09-19. Kept because it will be unflattering (base role s2).

One row per call this seat made that later proved wrong, with the
correction and what the seat now does differently. Empty at creation:
the seat has made no calls.

date | call made | what was true | corrected by | changed practice
2026-09-19 | In the creation receipt I said the M2 non-interference rules (Atlas/SIBLINGS.md, the SFE-ledger constraint) "would bind me too" whatever the charter turned out to be | Those rules are the COLLECTORS' constraint only: Atlas and Atlas-M2 shadow the research (indexes, archives) so researchers stay on experiments; they do not bind other seats on M2 | operator, in chat, same day | Do not generalise another seat's lane rule to the host it runs on; a rule binds the role it was written for, and a new seat's constraints come from its own charter
2026-09-19 | DESIGN_C0 s3 preregistered the W2 shuffled floor as 60 "cannot exceed" | Windows persist ~2 steps and the last-reward channel reveals a paying window one step late; shuffled champions reached 75-80 by a reward reflex | the first 18 sweep cells (progress.log 12:45-12:46Z) | Before declaring a control "unexploitable", enumerate every channel that carries the hidden variable with a lag, including reward feedback; write the attainable value of the best REFLEX on each channel, not just the best fixed policy
2026-09-19 | Named the substrate ablation "no_state" (allow_keep=False, reset_each_step=True) as if it removed all within-lifetime memory | It leaves PLASTICITY (R weights) intact; plastic weights persist across steps and are an independent carrier, so a nostate organism scored ~33 on W4 (near cap) by using them | the nostate W4 arm in sweep_c0 (progress.log 12:58Z) beating its own threshold | Report the "no_state" ablation as "no activation memory (keep+cross-step); plasticity intact"; the true "no memory" control is no_state AND no_plasticity together; add that combined arm before any W4-memory claim
2026-09-19 | DESIGN_C0 set the W12 present-mode fitness threshold (31.2) from an attainable estimate of ~80 borrowed from the ABSENT mode | The present world cannot reach 80 because risk is genuinely costly; the real present ceiling is ~30, so W12 fails criterion (i) by construction while its target behaviour (state-dependent risk, gap 1.00) is clearly present | the W12 present runs capping at ~30 vs absent 80 | Derive a pressure mode's attainable range and threshold from THAT mode's own best achievable, never from the mode that removed the pressure; an absent-mode ceiling is an upper bound the present mode may be structurally unable to reach
2026-09-19 | Predicted P-W5 "delayed revelation does nothing" and set a shuffled control assuming a fixed guess scores ~0 | W5 present produced genuine cross-delay memory (dec_acc 1.00 in 2/3, nomem shows both routes needed); the shuffled control scored high because the balanced-seed set balances only the FIRST binary draw and W5's payout is a SECOND draw | the W5 present dec_acc 1.00 with a high shuffled floor | Balance the held-out set on EVERY hidden binary a world uses, not just the shared first draw; a per-world balanced seed set, not one global set
2026-09-21 | Cycle-0 report said the W4 memory mechanism "accreted over ~13 lineage generations" | That counted mutation-chain length (lineage age); the functional test (snapshots intact vs no_memory) shows the memory gap co-emerging with fitness onset in 8/10 W4 lineages; in W4 the memory IS the solution | ares/runs/sweep_c1/*_ancestry.json | Accretion is a functional claim: measure when the ablation gap appears, never how many mutations the chain lists
2026-09-21 | Fixed the second-draw imbalance for W5 (balance_key on token x pay) and declared R2 done | W4/W13 shuffled mode draws a second binary (the SHOWN cue) that balance_key omitted; a cue reflex scores 0.59 on the shuffled set, not 0.50; conservative for criterion (ii), no disposition changed | supp check 2026-09-21 (19/32 agreement) | When a repair is made for one world, grep every world for the same construct (a second rng draw in shuffled mode) before declaring the class of defect closed
2026-09-21 | Preregistered P3 as "arm_nomem_W4 <= 4.0 in 10/10" and P4/P6 counts at 6/10 and 4-7/10 | 8/10 <= 4.0 (max 4.56, all far below the 8.0 threshold); W5 10/10 and W13 9/10 above threshold; every count-prediction this cycle was too pessimistic about the GA on this substrate | disposition.txt | Bounds on a floor arm should be stated relative to the threshold, not a round number; and my priors on search power here are biased low, so predicted counts get widened next time
2026-09-23 | In the cycle-1 close I called the recurrence result "a small, clean instance of the north star's 'mechanisms no human explicitly conceived'" | Recurrent state as memory is textbook; the defensible claim is narrower -- a carrier the EXPERIMENTER did not designate was rediscovered and preferred over the designated one, i.e. the substrate routed around design intent | operator, in chat, 2026-09-23 | Never upgrade "undesignated by me" to "unknown to the field"; state what the experiment controlled (my design intent), not what the literature contains
2026-09-23 | Wrote the cycle-2 GATE B rule as "SUPERIORITY if c1_only_keep reaches threshold in < 5/10" | Without a guard, an apparatus that simply never learns (no arm reaching threshold) scores as evidence FOR recurrence's superiority; the smoke test produced exactly that false OPEN | own smoke test, before any cycle-2 run | Every rule that reads a NEGATIVE result needs a positive-control clause naming the arm that must have SUCCEEDED for the negative to mean anything
