# Ares journal 2026-09-23 -- cycle 2: ATTACK THE MECHANISM (m2-640acfe6)

Seat UNPARKED by the operator for exactly one focused round (verbatim:
prompts/2026-09-23_cycle2_directive/, MANIFEST beside it). Not a new
exploratory cycle: attack the carrier cycle 1 discovered, explain WHY
it wins, and end on the operator's three-gate stopping rule. No cycle
3 may be self-authorised under any outcome.

## Boot
- Worktree guard passed; merged origin/main (2496 sibling commits since
  09-21, none touching ares/). Base a39bc1647 -> prereg ab137f52b.
- comms boot + sync: 0 new. Still no Nyx/Harmonia reading of the W4
  fossil (#518/#519/#535/#536). Not waited on (cycle-1 directive s6).

## Wording correction adopted (operator)
Cycle 1's closing chat line called recurrence "an instance of
mechanisms no human explicitly conceived". Wrong: recurrent state as
memory is textbook. The defensible claim is narrower and better --
a carrier the EXPERIMENTER did not designate was rediscovered and
preferred over the designated one, i.e. the substrate routed around
design intent. LEDGER row; DESIGN_C2 s0 states the corrected claim.

## Instrument repairs (before the prereg commit)
I1 balance_key now (regime, shown) for W4/W13: shuffled sets are
   8/8/8/8, so a cue reflex scores 0.50 there, not 0.59.
I2 ares/carriers.py: edge- and SCC-aware ablation replacing node-only
   attribution. Cuts each carrier CLASS, each SCC and each recurrent
   EDGE. Control: an output-node self-loop organism, which cycle-1
   node ablation reports as having NO ablatable structure, is
   attributed correctly here.
I3 seeds 201-210 and held-out sets from 40000: all lineages fresh, no
   re-evaluated cycle-0/1 lineage anywhere.
I4 carriers.opportunity(): P(one mutation CREATES carrier | absent) --
   the denominator cycle 1 lacked.
I5 snapshots retained.
New substrate flags are CONSTRAINTS or COSTS, never rewards. The
directive's "keep subsidy" is implemented as MUTATIONAL opportunity
(keep_mut_weight), not a fitness bonus, because a bonus would reward a
designated mechanism and violate the charter. Stated in DESIGN_C2 s3.

## Measured BEFORE any GA run (hand-wired organisms, tests/test_carriers)
    world  hand-wired RECUR  hand-wired KEEP   empty
    W4          24.56             33.75         0.00
    W14         20.06             -4.31         0.00
    W15          8.88             11.69         0.00
    W16          6.38              7.00         0.00
(a) The DESIGNATED carrier is BETTER hand-built on W4 than the one
    evolution chose -- so "recurrence won on merit" was improbable
    before a single run, and the accessibility hypothesis is sharply
    losable.
(b) W14 attacks the LEAKY INTEGRATOR, not recurrence (saturation is
    noise-robust). The anti-activation world is W15. Stated in the
    design so the result cannot later be read the other way.

## Own-rule defect caught by the smoke test (before any real run)
GATE B as first written scored "no arm reached threshold" as evidence
FOR recurrence's superiority. Guard added: SUPERIORITY now requires the
only_recur arm to have SUCCEEDED in >= 7/10 first. LEDGER row. General
form: every rule that reads a negative result needs a positive-control
clause naming the arm that must have succeeded.

## Run
- tests 19/19 (10 cycle-0 + 9 new carrier controls).
- `python -m ares.cycle2 all` launched from the worktree at ab137f52b;
  180 GA runs (seeds 201-210) + edge/SCC dissection + causal battery +
  transplant/swap + gates. Results ares/runs/sweep_c2/.

## Results (gates.txt, recheck.json, basin.json, c3_summary.json)
- 190 GA runs (180 preregistered + 10-seed post-hoc c3 arm). All
  present arms at the structural cap 40.0 (W16 9.28/10).
- GATE A OPEN: c2_no_recur 10/10 at cap, plasticity load-bearing 7/10,
  keep 3/10, recurrence 0/10, ttt 20 vs 10. Tax_high substitutes too
  (rec edges median 0.0). W15 builds REDUNDANCY (rec 8/10 AND plast
  7/10, rec edges 2.0 -> 9.0) rather than substituting. No NEW_CARRIER.
- GATE B OPEN on substance, but the rule's NAMED explanation is dead:
  intrinsic superiority killed pre-run (hand-built keep 33.75 >
  recurrence 24.56), gradient killed (p_improve ~0 both), raw
  opportunity killed (8x subsidy did not flip), useful-reachability
  killed by the c3 falsification arm (my prediction >=3/10 lost at
  2/10 despite 7/10 champions carrying keep 0.98). What survives:
  BASIN WIDTH -- keep viable in 3/25 of its range [0.94,0.98] against
  its ceiling, recurrence in 14/23 [2.75,6.0] and saturating.
  Corroborated by c2_recur_unstable (jitter did not dislodge it;
  fastest ttt of any arm) and by keep's long ttt tail (10-90).
- GATE C SHUT after correction: per-pair swap recovery median 0.02,
  >=0.5 in 16/59; portable 4/9. The first printout said OPEN using a
  best-of-9-donors statistic.
- Cycle-1 correction: plasticity 0/10 on W4 does NOT replicate (4/10
  on fresh lineages with edge-aware attribution).
- Two defects in this cycle's own apparatus found and recorded (D1
  held-out selection bias; D2 best-of-N swap statistic). With D1
  removed the shuffled controls sit exactly at the floor: the worlds
  do not leak.
- 4 ledger rows. Report ares/ARES_CYCLE2_REPORT.md; packet
  roles/Ares/REVIEW_PACKET_C2_2026-09-23.txt; STATUS -> PARKED pending
  the operator's decision; export package prepared (report s9).

## Not done
- No cycle 3 self-authorised. The s7 tests are proposals only.
- Evidence-wiki submission still not run (carried since cycle 1).
- Nyx/Harmonia have still not read the fossil; artifacts re-sent, not
  waited on.
