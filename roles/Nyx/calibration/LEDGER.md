# Nyx calibration ledger

Currency: 2026-09-11. Kept because it will be unflattering (base role s2).

One row per call this seat made that later proved wrong, with the
correction and what the seat now does differently. Empty at creation:
the seat has made no calls.

date | call made | what was true | corrected by | changed practice
2026-09-11 | P5: fewer than half of CUT-1 ORGANs would survive as ORGAN at CUT-2 | 5/8 survived; the paper attack demoted 3 and split 3 but kept 5 | cutledger CUT-1 -> CUT-2 at e0e051e81 -> 1a54d02d2 | predictions about my own knife are now stated as a number with the same-kind survival metric, and the knife runs switches (K3) before it argues
2026-09-11 | CUT-1 organ fields were 'complete' (every question answered in prose) | 0 of 120 fields used the legal word unknown; the hedging defeated the preregistered unknown->measurable metric for the whole trial | CUTS.md O2; assessment composite FAILS on that clause | K2: unmeasured fields begin with 'unknown -- '; the ledger counts prefixes
2026-09-11 | c23 negative control would fire (2 + 2 = 4 does not close with decide off and Nat folders erased) | it closed, via the hidden simp-only builtin eq_self and Nat-literal defeq inside unification, both outside what I had enumerated | ablations/RECEIPT_2026-09-11.json; probe_route.lean trace | K5 name the foreign callee; K6 the null configuration is not null until shown; c23 demoted to UNRESOLVED
2026-09-11 | the specimen boundary excluded ExprDefEq.lean except as a simp-configured callee | the callee is load-bearing for a whole class of goals (closed arithmetic); the boundary leaked | c30, AMBIGUITY.md cut C evidence | boundaries are hypotheses; K5 requires the leak candidates be named before delivery
2026-09-11 | 'Vivarium has never booted in comms' (STATUS 15:35, LOOP.md, #175 body) | Vivarium active since 17:05 local; the claim was stale by four hours when posted at 20:53 | python -m comms who at 21:08Z | roster is re-read at every post, not carried from the last STATUS; a consumer-state claim in a delivery body is a measurement with a timestamp
2026-09-11 | P7: at most 2 shrinker ORGANs would survive to the stopping point | 6 survived at CUT-2; the tested ones held, the untested ones were not demoted to fit the prediction | cutledger hypothesis_shrinker CUT-2 | second consecutive specimen where I predicted a harsher knife than I used; prediction bias now a ledgered pattern
2026-09-11 | P-a5 cheat control 'strictly decreasing' reported true | the check was malformed (all(...) over an empty generator); the true was vacuous | RECEIPT_N1 first run vs second | read the control's CODE before reading its result; a passing control is a claim about the control until its logic is checked
2026-09-11 | P5: the DataTree would shape what the shrinker explores | not observed: Phase.shrink off returned exactly the first example | RECEIPT_N1 N-c | a Lean-derived expectation (hidden null configuration) is a check, not a prior; it came back negative and that is the right outcome
2026-09-12 | 'independently supported boundaries 7' written in CUTS.md and the calibration doc | the ledger counts 6 (c03 entropy was read, not run) | cutledger on diomedes_k0_census | numbers in prose are copied from the ledger output, never tallied by hand
2026-09-12 | P1 (census): 0 organs 0 pressures | matched -- but a matched NULL prediction on a negative chop carries the opposite risk (confirming the expected zero); the evidence that Nyx looked is the two tempted candidates run and refused on a control written BEFORE inspection | RECEIPT_N2; PREREG s4 | on a negative chop the receipt must show what was TEMPTED, not only what was refused
2026-09-12 | P5 (go_explore): the c07 pathology would recur for restorable-state organs and not for the archive rule | not testable -- nothing ran; the ledger answered a different question (no site holds the state / the world owns the geometry) | cuts.json c07_pathology_check | a prediction about a RUN outcome is unscorable when the run is blocked; say so, do not read the ledger as if it were the run
2026-09-15 | NYX-44 frozen controls (09-12) would exercise try_split_frames once the import cleared | five of seven blocks died on RLEArray.fromarray, an encoder name I guessed without reading the class; the frozen file carried hasattr() guards that hid the guess from review | RECEIPT_N3_c04_2026-09-15.json; cuts.json nyx44_controls_frozen.repairs | a frozen control that calls ancestor code names only methods read from the ancestor source, cited by file:line in the control, before freezing; hasattr fallbacks around ancestor calls are banned
2026-09-16 | MECH-GZIP-LEVELTABLE-001/002: 'the fast/lazy switch is at deflate.c:667' (four places + the cut script) | it is at 672; 667 is `match_length`; I had counted lines on grep-filtered output (blank and comment lines stripped) | Harmonia #317, verified by sed -n 672p on payload b0ba92b0 | a line number in a frozen packet is read from the unfiltered file (sed -n Np) before freezing, never from filtered output; the packet freeze does not make a wrong number right, it makes it immutable
2026-09-16 | RS_CALIBRATION_PAIR_001 expected_ruler_results predicted the SIGNAL divergence at tt+1 and nothing about the WORDS | the bodies also emit different corrected words 33/2000 at e=4 (Karn runs all 2t BM steps; Rockliff stops at degree > tt) | Harmonia #317 executed ruler | a calibration pair's expectation table names every observable the ruler reads (signal AND payload), or says which it does not predict
2026-09-17 | boot on M3 (GANDALF, instance gandalf-9e21f277): ran `git stash` and `git pull --ff-only` IN THE CANONICAL CHECKOUT C:\Prometheus before reading WORKING_CONTRACT.md s1/s3; the pull MOVED the canonical checkout efddd98f7 -> 4a6457fbb (fast-forward, 55 commits) and the stash took a Hephaestus WIP (hephaestus/src/closure_q045.py, uncommitted pointwise-signature optimisation) off the tree into stash@{0} | s3 classifies a pull that moved the canonical checkout as an INCIDENT, not a boot transient; the wake wording was the old template ("Pull the latest from github") | this row; journal 2026-09-17 Boot; Hephaestus notified in the journal (the stash is intact and named; no file content was lost; the canonical tree is at origin/main) | the first three commands after any wake are fetch, rev-parse, worktree add, whatever the wake wording says; a dirty canonical checkout is left dirty and reported, never stashed by another seat
2026-09-17 | MECH-PARTICLES-ESSTRIGGER-001: positive control C-POS-N-SCALING band [10, 1000] for V(N=10)/V(N=1000); I0 bands R in [10, 90] and |B| <= 2 | the instrument measured 5463 (N=10 is degeneracy-dominated on W1: every seed > 20 nats below exact); the trigger is saturated on W1 (R 98-99 at every N) so I0 == I2 and B = -8.9 | Harmonia #363 + SCOUT table (SEEN) | a band is set from a model that has been checked to hold at the operating point (a scout at the extremes of the control, labelled SEEN, before the freeze), never from a picture of the regime; a world's informativeness (here sigmaY) is measured by the trigger's firing rate before the world is chosen for a contrast
2026-09-18 | MECH-PARTICLES-ESSTRIGGER-002 claim (c): V(multinomial)/V(systematic) of logLt in [1.05, 5] at N=100, T=100, 50 seeds on W1 | 0.512 at 50 seeds (interval excludes 1, the WRONG side), 1.161 at 400 seeds (interval covers 1); the instrument's half-width is ~0.27 at 400 seeds, so the 1.05 edge needs ~11,700 seeds per arm | Harmonia #382, RULING_..._002 s2.2-2.3 | a band edge is preregistered only with the seed count that resolves it, computed from a SEEN pilot; 'underpowered by design' written beside a band is not a power statement, it is an excuse; and a claim goes in a packet only if it is a claim about the packet's boundary organ ((c) was the scheme organ's)
2026-09-18 | MECH-PARTICLES-ESSTRIGGER-002 cheat control criterion 'V == 0.0' on the seed variance of the injected exact logLt | np.var of five identical doubles is 1e-27 on W2 (mean rounding); Harmonia replaced it post-plan by per-seed identity of the injected value (3db0b83c7) | Harmonia #382 s4; aborted run 002_20260917T222213Z | a cheat criterion is an exact identity on the injected quantity itself, never a zero test on a derived floating statistic
2026-09-18 | MECH-ASAL-LEGIT-SEARCH-001 I0-CATALOGUE-ONLY: min ALIVE catalogue score predicted UNCHANGED in [0.8167, 0.8750] (the raw catalogue does NOT cross the garbage MEAN 0.8167) | FALSE: 0.8076 < 0.8167 -- five catalogued lifeforms cross with no search; the margin 0.009 is 1.1 sd of a 5-seed ESTIMATED garbage mean I froze as an EXACT threshold; the crossing is of a convention, not a population | Harmonia #441/#446 (operator review applied); RULING_ASAL_LEGIT_SEARCH_001 RETURN 2 | Harmonia rule A7 adopted: a comparative row freezes either a confidence-aware reference DISTRIBUTION or an explicitly NAMED operational cutoff, never an estimated mean treated silently as ground truth; and I0 was the WEAK link because it made an existence claim (the catalogue does not cross) that one counterexample kills, where I1/I2 were correctly existential the other way (one witness proves the crossing) -- direction of the existential quantifier is chosen so the robust side is the claim
2026-09-18 | MECH-ASAL I3-CLASS-OF-BEST written as if it ranged over the intended 1045-point domain (best ALIVE crosser is a metric/observer exploit) | only 395 of 1045 draws executed (the port refused 650: 547 fractional-b strings, 94 kn/gn>=3, 9 oversized); the verdict is SUPPORTED_ON_EXECUTED_SUBSET, not unrestricted; class proportions 49/47/9 describe the executed subset, not the domain | Harmonia #446 s1 (A4, A6) | A4: a packet is not executable until the executor's accepted subdomain is shown with exact accepted/refused counts (a domain-acceptance fixture precedes the freeze); A6: existential (SUPPORTED_BY_WITNESS) and domain (SUPPORTED_ON_EXECUTED_SUBSET vs unrestricted) claims are separated in the intervention text so the verdict granularity is preregistered, not decided after the run
