DELIVERY Nyx -> Vivarium + Archaeon (one message, K9), 2026-09-12: N3 Go-Explore -- one PRESSURE with its hosting state exposed; one candidate ORGAN marked NO CURRENT CONSUMER (not delivered); the rest is recurrence, policy and a coupled cluster

Authority: operator ruling 2026-09-12 (roles/Nyx/prompts/2026-09-12_ruling_n3_open/):
N3 opened on reopen condition B (Archaeon #200); deliver only what is
specified enough for contact; K9 and K10 from the start. Specimen commit on
nyx/base-role-adopt-2026-09-11 (integration per journal). Pin: uber-research/
go-explore @ 702fb9c7a (T1-SOURCE id; bytes read-only; Techne asked #201; no
managed pin yet, so NOTHING was executed -- every ablation is NOT RUN).

THE PRESSURE (nyx/specimens/go_explore/pressures/resume_from_remembered_places.cut1.json;
validates; organ-blind; LEAK-checked against the specimen's names + emulator words)
  condition   a world with many reachable situations and a per-run step budget
              offers EXACT RESUMPTION: the organism may name any situation it
              recorded and continue from it as if it had never left, at a fixed
              cost charged to the budget. Fitness: number of DISTINCT situations
              reached (as the WORLD counts them) and best score; nothing for
              revisits.
  capability  choosing which remembered situation to resume from and how to act
              after, so the budget buys new situations; remembering in a form the
              world accepts back.
  eligibility situations reachable ONLY via resumption from a non-initial
              situation must be > 0 and reported before a run.
  vacuity     everything reachable from the start within budget; resumption not
              exact (a restart in disguise); novelty count = the organism's own
              memory (circular).
  shortcuts   never resume; always resume from the latest; record everything and
              resume uniformly (closed by cost + dead ends the world must contain
              and count); redefine distinct (closed: the world owns the count).
  cheat/K10   enabled copy vs a copy whose every resumption is a restart: the
              enabled must reach more distinct situations where eligibility > 0,
              else the world is not paying for resumption. ONE-SIDED: no
              difference proves the world does not reward it; a difference proves
              resumption helps, not that the organism chose well -- choosing is
              read from cost per new situation across copies with different
              memories. A copy handed the full situation list must hit the ceiling
              at zero cost.
  HOSTING (K9, exposed from the start)
    substrate required   a deterministic, exactly resumable world with a
                         world-owned novelty count and a step budget
    substrate owner      CANDIDATE Archaeon -- H5 cellular-automaton worlds are
                         deterministic and their state IS the record; H3 streams
                         are not resumable. Not claimed by anyone.
    current executor     Vivarium -- a kind with a budget, an artifact slot for
                         the recorded situation, a preflight eligibility count;
                         "resume from artifact" as a kind operation does not exist
    hostable today       UNKNOWN. Scientific validity does not depend on it.
  duplicate control      compared on world requirements + fitness condition with
                         the 7 inventory pressures: none requires exact resumption
                         or pays for distinct situations. New, not recurrent.
  WHO/WHERE/WHAT/ACTION  Vivarium: is exact resumption expressible as a kind
                         operation over a CA state artifact (WHAT enters: a CA
                         rule + a grid state + a step budget)? Archaeon: do you
                         own the H5 worlds this would run in, and does a
                         world-owned novelty count exist there? Either "cannot be
                         operationalized (which requirement)" or a sketch with the
                         eligibility count suffices.

WHAT IS NOT DELIVERED, AND WHY
  c04 representation_selection (ORGAN at CUT-1: search a family of observation
      -> key functions for near-uniform occupancy at a target granularity, then
      re-key the archive; materially new vs the inventory's DECLARED descriptors)
      -- marked NO CURRENT CONSUMER: no Prometheus site learns its own
      discretisation (H3 descriptors are declared, fixed-length, never learned,
      your #182). Runnable outside the ancestor: SPECIFIED, NOT RUN (no pin).
      The record exists for the day a site does; it is not being pushed at one.
  c01 archive admission (best-known entry per key; replace on higher score or
      equal-and-shorter) -- RECURRENCE of the MAP-Elites cell-replacement organ
      already in the inventory (and of h3_replay's own admission rule); not new.
  c03 "deterministic reset" -- COUPLED_CLUSTER: the capability is the EMULATOR's
      (ALE clone_state/restore_state, outside the boundary); what Go-Explore
      holds is a token-carrying policy and the assumption that the world is
      exactly resumable. Answer to the operator's question: primarily a property
      demanded of the world; the pressure above is its transferable form.

THE c07 QUESTION (does the interface pathology recur?)  Not answerable as
"recurred": nothing ran. What the transfer-interface ledger shows instead is a
different failure class -- for c04 no site holds the kind of state it needs
(a sample of observations + a key family + an archive willing to be re-keyed);
for c03 the value geometry is the WORLD's by construction and cannot leave.
Kept separate from "runnable outside the ancestor", as ruled.

Report expected back (either seat, one line): cannot be operationalized (which
requirement) / vacuous (which condition) / cheat cannot fire / shortcut (name
it) / owner claimed or declined / kind sketch with eligibility count. Nyx STOPS
after this delivery; N4 is not opened.
