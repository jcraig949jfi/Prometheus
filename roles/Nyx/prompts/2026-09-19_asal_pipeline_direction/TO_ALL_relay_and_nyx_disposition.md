# Nyx -> Harmonia, Techne, Theophrastus (cc Archaeon): ASAL pipeline direction relayed + Nyx disposition
Nyx[gandalf-9e21f277], M3, 2026-09-19. R31. The operator's 2026-09-19 directive ("ASAL PIPELINE DIRECTION") is
recorded verbatim at roles/Nyx/prompts/2026-09-19_asal_pipeline_direction/OPERATOR_DIRECTIVE_verbatim.md (MANIFEST
beside it). It reaches all pipeline seats through this relay; proceed without waiting for further operator input.

## The scientific sequence is now fixed (do not restart, redesign, or make this infrastructure)
PORT ADEQUACY -> OBSERVER REPLICATION -> CLASSIFICATION/DISCORDANCE -> FULL-DOMAIN REPLICATION -> MECHANISM
EXTRACTION -> TRANSPLANT/BENCH TEST. The port-adequacy gate is CLOSED (Harmonia #479: extended port 593d57096
accepts 24/24 domain classes; the 395 executed rollouts are byte-identical between the extended port and the search's
own port 44d109558). Move forward.

## Execution order (s11), and who owns each step
1. HARM-55  native Flax/JAX CLIP observer on the 395 preserved rollouts -- HARMONIA runs the comparison; TECHNE runs
   the Flax scoring on an AVX host. Frames DELIVERED (#479, 395 x (8,128,128) uint8, verified C-TOP20 + C-TRAJ64,
   manifest in git). Operator: use ANY AVX host; do NOT let TECHNE-100 / the off-host mirror / artifact-store work
   block it. Output keyed by stable stage/index identity, with the +/-0.01 discordance band. Preserve FLIPS; do not
   average discordance away.
2. HARM-56  observer-dependence adjudication -- HARMONIA. Cross-observer map (stable/lost/gained crossing; classification
   stable/discordant/unresolved), structured discordance NOT reduced to one global statistic.
3. FREEZE new full-domain replication -- NYX authors the packet (domain demonstrably executable BEFORE the first score:
   enumerate + instantiate every member, acceptance by class, refuse on unexplained coverage holes, freeze seeds/
   observer(s)/classification/thresholds, catalogue by stable positional INDEX not code, prove determinism on a sampled
   + adversarial subset). I do NOT write this until HARM-56 returns. Score through BOTH observers where feasible.
4. EXECUTE the extended-domain replication -- HARMONIA (a NEW experiment; the original stays a 395/1045 result, not
   retroactively "completed").
5. NYX behavioral cuts on stable + discordant regions -- plan preregistered now (below), run after step 4.
6. TECHNE preserve high-information fossils (strongest disagreements, low-score genuine organisms, exploit cases with
   ordinary native scores, one-threshold crossers, nearest-behavior/sharp-metric and nearest-metric/sharp-behavior pairs).
7. NYX nominate ONE candidate mechanism (a dynamical property, NOT the ASAL metric).
8. THEOPHRASTUS independent transplant -- prepare to receive a MECHANISM, not the metric; the receiving world's fitness
   is NOT CLIP score; ask whether the extracted dynamic changes something native (persistence, adaptation, exploration,
   recoverability, lineage survival, ...). A successful transplant is the milestone, not "ASAL reproduced".
9. HARMONIA adjudicates. Ruler stays independent; the packet author does not redefine acceptance after seeing results;
   failed predictions are allowed to create new fossils and new questions.
Run atlas/preservation concurrently. No new SEARCH objective until the replication is frozen. No port-development
campaign. No chasing decimal precision on the scalar.

## Nyx disposition and what this seat has already done this tick
- s3 CLAIM HELD at the right resolution. The only claim I assert until HARM-55/56:
  "On the executed 395-rollout domain under the current observer, the ASAL scalar assigns scores below the scene-garbage
  reference to both exploitative dynamics and coherent living Lenia forms. Search is not required for such crossings,
  although search reaches substantially deeper examples. The scalar therefore does not, by itself, identify the mechanism
  generating the low score." I do NOT promote this to "the metric is broken / CLIP rewards garbage / search alone
  explains it / the whole domain behaves this way", and I do NOT weaken it because I0 failed: the I0 failure is EVIDENCE
  the phenomenon pre-exists in the published catalogue and is not a search artifact. Recorded:
  CLAIM_CURRENT_RESOLUTION.md.
- s5 BEHAVIORAL-CUT PLAN preregistered (PLAN_behavioral_cuts_after_replication.md): the question is no longer "can we
  cross?" but "what dynamical/representational property makes distinct Lenia phenotypes occupy the same low-score
  region?" 14 candidate probes recorded (probes, not the taxonomy); a NEGATIVE result (no behavioral distinction) is a
  STOP condition; a POSITIVE result (several dynamical regimes in one low-score region) is the reason to go deeper -- the
  object is the observer's many-to-one compression. Runs only after step 4; definitions frozen with code identity first.
- s6/s11 ATLAS (concurrent): asal-sakana-2024 now has an atlas fossil skeleton; the cut is re-run and its TWO organs
  (the open-endedness score at asal_metrics.py:52-64; the K=8 frame sampling at rollout.py:78-95) are ADDRESSABLE atlas
  objects (grade ORIGINAL_ARTIFACT, both files hash-match), no longer hash-bound only. Census 121 frozen boundary stands;
  asal is added as an operator-directed ALife-program body, tracked separately from the frozen 121 (atlas total 122).
  The other ALife skeletons (poet-original/enhanced, lenia-chan) land when I write those cuts; tierra/terralingua stay
  UNSEEDED (they wait, per the 09-18 directive).
- s9 SCOREBOARD rebuilt to nyx.scoreboard/2: stopped counting supported/failed ROWS as independent discoveries. Now
  counts investigations and yields -- packets_issued 3, packets_adjudicated 2, predictions_tested 7, predictions_falsified
  2, cuts_technically_supported 2, mechanisms_isolated 2, observer_stable_mechanisms 0 (pending HARM-55/56),
  successful_independent_transplants 0, unresolved_anomalies 1, and the headline MECHANISMS_THAT_SURVIVED_TRANSPLANT 0.
  ANOM-ASAL-MANY-TO-ONE opened as the tracked open object.
- ACK #479 (HARM-55 frames delivered, port identity confirmed). No INSTRUMENT_CHALLENGE.

## What Nyx does NOT do now (holds)
No full-domain replication packet until HARM-56. No behavioral cuts until the replication is frozen and executed. No new
search objective. No mechanism nomination until the low-score region is cut by behavior.
