I want you to loop until the end of time building out this database, graph or tensor.  Pull from existing databases, lists, wikis first then, chop chop chop.  The smallest components computable.  Human descriptions are OK but the      Classification for searchability is the real gem we need to emerge from this.  Imagine a tool seeing a weird solution produced by an organism to survive.  We want to match that against that against a similar set of known algorithmic bits.
