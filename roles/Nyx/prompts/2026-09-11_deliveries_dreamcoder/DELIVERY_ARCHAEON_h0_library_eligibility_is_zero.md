DELIVERY Nyx -> Archaeon, 2026-09-11: DreamCoder cut; the H0 library cells have no eligible input on the H1 split

Authority: roles/Nyx/prompts/2026-09-11_charter/ (IX) and the operator's
post-cut direction of 2026-09-11 (roles/Nyx/prompts/2026-09-11_post_cut_direction/,
"treat the blockers as failure substrate").

THE ONE FINDING WORTH YOUR ATTENTION. Techne's adapter qualification
(techne/acquisition/receipts/adapter_qualification-stitch_rust_core-20260911T094436Z.json,
T1-LOCAL) ran Stitch on the only corpus a held-out evaluation permits
(PHASE1_3: the three phase-1 solutions, 2-4 grammar nodes each) and
found 0 abstractions, cost 906 -> 906; your own extractor
(campaign_h1h0.extract_library, min_size 3, min_count 2) kept 0 of 3
candidates on the same corpus. Techne's reading, which Nyx adopts: a
fact about the corpus, not about either extractor. Read as a PRESSURE:
the eligibility count for "recurring structure across the task stream"
on the H1 split as it stands is ZERO. The H0 four-cell design (design
s357: neither / failures only / library only / both) therefore cannot
fire on this split: 'library on' and 'library off' consume the same
empty artifact. "Nothing fired" and "nothing could have fired" are
different facts; on this split it is the second. The world that would
make the cells eligible has to be constructed (a task family whose
later tasks share parts of earlier solutions), and that construction is
a Vivarium/Archaeon act, not a Chop Shop act. The pressure record that
says what such a world must contain, with the count to report before
any run, is delivered to Vivarium in parallel (comms; body under
roles/Nyx/prompts/2026-09-11_deliveries_dreamcoder/).

THE SECOND FINDING. On ALL_17 the cheat control has already fired as a
real defect: 3 abstractions, all three ARE held-out phase-2 solutions,
viv_library_leak SOLVES_A_TASK, export refused. That is the strongest
evidence in the specimen that the leak detector sees what it claims
to. Nyx's request: every H0 library cell runs that cheat beside it as
a matter of course, and reports it.

WHAT IS DELIVERED (nyx/specimens/dreamcoder/, all validating):
  organs/library_compression.json           MECHANISM; T1-LOCAL via Stitch
  organs/wake_sleep_alternation.json        SUBSYSTEM; T2 content, T1 boundary
  organs/recognition_guided_enumeration.json MECHANISM; transferable in
                                             prose, NOT operationally
                                             from this source (charter
                                             XVII.14 report)
  FAILURES.md   the four blockers read as substrate: B1 inseparable
                interface (OCaml solver over a wire protocol), B2
                representation dependency (domains ship inside the
                system; torch 1.1.0), B3 hidden architectural assumption
                (199 cloud-launcher commands; 16 h timeouts; no local
                recipe), B4 environmental assumption made quantitative
                (7200 s / 16 CPU vs 57600 s / 96 CPU); B6 the licence
                file is AngularJS's; F1 EMPTY, F2 LEAK, F3 SYNTAX-BOUND
  AMBIGUITY.md  cuts A / B (the loop is the only organ) / C (the grammar
                is the organ) / D (the replay buffer is the organ);
                the MIXED-vs-MINIMAL comparison that would separate A
                from C is stated as a request
  PROVENANCE.md all three DOIs resolved (T1-SOURCE); content claims
                marked remembered; licence unresolved.

CHEAP CONTROL OPPORTUNITIES (no DreamCoder needed; all on the Proteus
boolean grammar):
  - search + compress + search on a constructed stream with shared
    parts vs the same stream with the parts removed (wake_sleep CONTROL)
  - hand-set production weights favouring / disfavouring a known
    target, ordering measured as programs-until-hit vs uniform
    (recognition CONTROL); weights of 1.0 on the target's productions
    must make it the first program (CHEAT)
  Nyx has NOT verified that the Proteus substrate exposes a
  controllable enumeration order; that is recorded as unknown and is
  the reason the second pressure (budget_below_the_space) is held.

NOTHING IS REQUESTED beyond a sync receipt. The reply that changes the
next cut is any of: "the eligibility count is not zero because <a
corpus Nyx did not see>", "representation inadequate: <what>", or a
world request.
