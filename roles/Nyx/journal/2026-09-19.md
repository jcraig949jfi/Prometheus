# Nyx journal -- 2026-09-19 (instance gandalf-9e21f277, M3/GANDALF)

## Operator directive "ASAL PIPELINE DIRECTION" -- move the pipeline forward from the first verdict
Recorded verbatim at roles/Nyx/prompts/2026-09-19_asal_pipeline_direction/OPERATOR_DIRECTIVE_verbatim.md (MANIFEST
beside it), relayed to all pipeline seats (#487, reply to #479). The fixed sequence: PORT ADEQUACY -> OBSERVER
REPLICATION -> CLASSIFICATION/DISCORDANCE -> FULL-DOMAIN REPLICATION -> MECHANISM EXTRACTION -> TRANSPLANT. Port-
adequacy gate CLOSED (Harmonia #479: extended port 593d57096 accepts 24/24 classes; 395 rollouts byte-identical to the
search port). The scientific target changed from "can we cross?" to "what dynamical/representational property makes
distinct Lenia phenotypes occupy the same low-score region?" -- the observer's many-to-one compression.

## Nyx actions this tick
s3 CLAIM HELD at the 395-executed-domain resolution (CLAIM_CURRENT_RESOLUTION.md): the ASAL scalar puts both exploit
turbulence AND coherent living Lenia below the garbage reference; search not required, deepens; the scalar does not by
itself identify the generating mechanism. NOT promoted to "metric broken / CLIP rewards garbage / search alone / whole
domain". NOT weakened by the I0 failure -- that failure is evidence the phenomenon pre-exists in the catalogue.

s5 BEHAVIORAL-CUT PLAN preregistered (PLAN_behavioral_cuts_after_replication.md): 14 candidate probes (probes, not the
taxonomy), two strata (stable + discordant regions), definitions frozen with code identity + A5 definedness before any
score is read. Runs ONLY after the full-domain replication is frozen and executed. Negative result (no behavioral
distinction) = STOP condition; positive (several dynamical regimes in one low-score region) = go deeper.

s6/s11 ATLAS (concurrent preservation): created the atlas fossil skeleton for asal-sakana-2024 (census.fresh from the
now-existing specimen record; the blanket census.run was NOT used because it would also seed tierra + terralingua, which
wait). Re-ran nyx.atlas.cuts.asal_sakana_2024 -- fixed the invalid ancestry relation 'original' -> 'algorithm_from'
(same class of validator error as batch4) -- and the TWO organs attached (COARSE, 2 accepted). migrate populated grade
ORIGINAL_ARTIFACT, both boundary files hash-match. The ASAL organs are now ADDRESSABLE atlas objects, no longer hash-
bound only. Validator 122/0. build: inspected 121 (COARSE 72, DEEP 45, ORGAN0 4, NOT_CUT 1); accepted_organs 475.

s9 SCOREBOARD rebuilt to nyx.scoreboard/2 (build.py; supersedes /1 row-counting). Stopped counting supported/failed
ROWS as independent discoveries. Now investigations + yields, read from structured fields on the packets_issued rows:
  packets_issued 3 | packets_adjudicated 2 | predictions_tested 7 | predictions_falsified 2 |
  cuts_technically_supported 2 | mechanisms_isolated 2 | observer_stable_mechanisms 0 (pending HARM-55/56) |
  successful_independent_transplants 0 | unresolved_anomalies 1 | MECHANISMS_THAT_SURVIVED_TRANSPLANT 0 (the headline).
Superseded packets (particles-001) excluded from active counts. Opened ANOM-ASAL-MANY-TO-ONE in the ledger. Added
nyx/tests/test_scoreboard.py (2 tests: /2 shape + internal consistency). Tests 25 pass.

## Holds (do not do yet)
No full-domain replication packet until HARM-56. No behavioral cuts until the replication is frozen + executed. No new
search objective. No mechanism nomination until the low-score region is cut by behavior. HARM-55 Flax scoring is
Techne/Harmonia on an AVX host, not this seat.

## Next for this seat
POET's two mechanisms (environment discard / minimal-criterion boundary; PATA-EC over world x contemporary population
with the basis-population ablation) -- bodies landed; write those cuts + their skeletons. Then the Avida ancestry cut
(A5 definedness, A9 ancestry). Author the full-domain replication packet the moment HARM-56 returns.

## Second operator directive, "NEXT PIPELINE DIRECTION" (2026-09-19b) -- mechanism ledger, probe freeze, first POET packet
Recorded verbatim at roles/Nyx/prompts/2026-09-19b_next_pipeline_direction/; relayed #495. Read Techne #488: HARM-55
STAGED -- the original-observer column is BIT-IDENTICAL over all 395 (max |diff| 0.0), proving the delivered frames +
script are exactly the pipeline that produced the search result, so the Flax run changes the observer and nothing else;
the Flax column is DEFERRED on execution placement (M3 is a 2008 i7-920 without AVX; M1/M2 expose no remote exec), not
on science. Harmonia #479 earlier closed the port-adequacy gate (24/24 classes; 395 rollouts byte-identical).

s2 A MECHANISM IS NOT A PACKET -- my review question 9.1 answered NO. Built nyx/atlas/mechanisms.py (schema
nyx.mechanism_ledger/1: validator + counts + CLI) over nyx/atlas/gates/MECHANISMS.json. Every mechanism carries its own
id, lineage, minimal executable organ + boundary, proposed behavior, predicted intervention, a FALSIFIER the validator
requires non-empty, evidence AND counterevidence packets, observer dependence (using Harmonia's A/B/C vocabulary so
HARM-56's disposition lands straight in), transplant history, disposition. Six registered. The decoupling is already
load-bearing: one ASAL packet carries TWO mechanisms, and that same packet is both evidence (I1/I2) and counterevidence
(I0) for one of them.

s9+s2 SCOREBOARD -> nyx.scoreboard/3: mechanism counts now come from the ledger and are over UNIQUE mechanism_ids.
MECHANISMS_THAT_SURVIVED_TRANSPLANT cannot be claimed without a SUPPORTED transplant row (validator-enforced, tested).
  packets 4 | adjudicated 2 | predictions tested 7 | falsified 2 | cuts supported 2 | mechanisms 6 | isolated 6 |
  observer-stable 0 | observer-dependence unresolved 2 | anomalies 1 | SURVIVED TRANSPLANT 0.

s2 DISCOVERY MACHINERY PREPARED, ASAL hold retained. nyx/atlas/probes.py: 14 declared probes + an open-channel
descriptor, all numpy-only and observer-independent by construction. FROZEN by source sha256 53f63df5..., exercised
ONLY on synthetic fixtures (17 tests; no ASAL/Lenia/replication artifact consulted). Two channels: DECLARED and OPEN,
with open-channel output explicitly a NOMINATION not evidence. p06 mirrors the ASAL score's form in PIXEL space so a
gap between it and the ASAL scalar localises the effect in the OBSERVER. One real defect found and fixed while
building: autocorrelation on non-negative intensities never drops (cosine ~0.75 even between independent random
fields), so identity half-life could not separate static from noise; fixed by mean-centering before normalising.

s2 STOP-CONDITION CORRECTED. My 9.2 was wrong: "no separating axis" establishes only PROBE_BATTERY_FOUND_NO_SEPARATOR,
never homogeneity. Do not kill the branch on it. Recorded in the plan.

s2 POET, one mechanism to a ruling. Cut nyx/atlas/cuts/poet_original_2019.py, COARSE, 3 organs: the two-sided minimal
criterion (pass_mc 264-268, applied twice on different thetas); capacity discard by AGE (remove_oldest 343-352) against
a novelty archive that delete_optimizer NEVER prunes -- an active FIFO window with a permanent memory; and the novelty
estimator (novelty.py:18-62). Reading the estimator found three code-level properties: argsort()[:k] silently returns
fewer than k so the statistic CHANGES CHARACTER at archive size k (whole-archive dispersion below, local density at and
above); normalize=False is hardcoded so the norm vector [8,8,8,3,3] declared in that very function is never applied;
and absence is identical to zero-presence. Froze MECH-POET-NOVELTY-ESTIMATOR-001 (291a22ed) with four EXACT rows, all
SCOUTED (SEEN) before freezing and disclosed as confirmatory: I1 identity at every archive size 1..20; I2 ratio 8/3 =
2.666667 (exactly 1.0 if the norm were applied); I3 distance 0.0; I4 zero increases over sizes 5..40 (series fell
2.8900 -> 0.2360). novelty.py runs STANDALONE on M3 with numpy alone, so this is adjudicated on the FOSSIL'S OWN BYTES
and does not queue behind HARM-55's placement problem. Posted #494.

RECORD DEFECTS reported to Techne (#494): (a) 46 lineage_relations across 24 records use "target" where the other 112
use "to" -- all 22 ASAL rollout capsules, poet-original-2019, terralingua-data-abundant-exp-1; it broke census.py with
KeyError 'to'. I made the reader accept both (_rel_to) and did NOT normalise Techne's records. (b) poet-original-2019's
nyx_handoff block is entirely empty, so its grade reads UNKNOWN despite source_type ORIGINAL_AUTHORITATIVE_RELEASE and
a pinned commit; the packet records UNKNOWN honestly rather than inferring a grade from the string.

Atlas: poet-original-2019 skeleton created (targeted census.fresh again, not the blanket run). 123 fossils validate
0 problems; inspected 122. Tests 45 pass. Holds unchanged: no replication packet until HARM-56, no behavioral cuts
until it executes, no new search objective, no transplant nomination yet.
