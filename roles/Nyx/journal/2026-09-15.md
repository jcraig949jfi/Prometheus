# Nyx journal 2026-09-15 -- instance m1-509ff7bc

## Boot (16:12Z)

Worktree F:\Prometheus-worktrees\nyx-base-role, branch nyx/base-role-adopt-2026-09-11, base 43d84dac5
(ancestor of origin/main; main 22 commits ahead, none under nyx/ or roles/Nyx/). Session opened in the
canonical checkout; no work done there. `python -m comms boot Nyx --model claude-opus-5 --capabilities nyx`
-> Nyx[m1-509ff7bc] on SKULLPORT. `python -m comms sync Nyx`: 0 new, queue #235 (atlas charter) #241
(THEO-REQ-003, composition; not Nyx's). No new prompt addressed to Nyx since 09-14. MONITORS.md lists
nothing Nyx owns or feeds. Hashes: atlas PROMPT_verbatim 7da74389 matches; charter manifest 2/2.

Suggested work (base role s1.6): (1) NYX-44 rerun, pre-authorized -- artifact: receipt; (2) ATLAS PASS 01
Stage A, 25 remaining sampled fossils -- artifact: nyx/atlas cuts; (3) atlas first-pass return -- blocked
on (2). Started (1).

## NYX-44 (go_explore c04)

Pre-run: controls sha256 398da889 (frozen); image prometheus-fossil-goexplore:min present; harvest verify
go-explore-uber-2022 tree 67bfdebe matches; robustified file digest 24af115c (86 files).

Run 1, unaltered (16:15Z): `wsl -d Ubuntu docker run --rm -e PYTHONDONTWRITEBYTECODE=1 -v <pin>/tree:/pin:ro
-v <worktree>/nyx/specimens/go_explore/ablations:/w prometheus-fossil-goexplore:min python
/w/n3_c04_controls.py /pin`. IMPORT OK (the 09-12 loky block is gone). Blocks 5 6 7 2 1: AttributeError
RLEArray has no fromarray. Block 4 p0 0/200, p0.5 109/200.

Read the ancestor: RLEArray (import_ai.py:131-149) has __init__/to_np/tobytes/frombytes; the environments
store RLEArray(gray) and try_split_frames decodes frombytes (goexplore.py:467). The control guessed the
encoder name behind hasattr() guards. Demonstrably malformed. Ledgered in cuts.json and calibration LEDGER
and committed 9a46e93c2 BEFORE the repair. Repair: ge.RLEArray.fromarray(f) -> ge.RLEArray(f); repaired
sha256 6ce4ed61.

Repaired runs: r1 (16:16Z) receipt LOST -- my `for r in 1 2` loop variable was empty after the Git Bash ->
wsl -> sh -c hop, both copies went to one name, r2 overwrote r1. Not reconstructed. r3-r5 re-run from a
script file (ablations/diagnostics/nyx44_reruns_r3_r5.sh), 4-5 s each.

  run  pos_k4  neg  cheat_n  cheat setting  t0.02  t0.5  dyn off/on  prob_override
  r2   4       1    200      7x25 pix 17    4      102   200/4       0 / 103
  r3   4       1    8        1x3 pix 8      4      4     200/4       0 / 92
  r4   4       1    4        1x2 pix 30     4      102   200/4       0 / 91
  r5   4       1    8        3x1 pix 2      4      122   200/4       0 / 100

Frozen kill criterion ("if n_parts ~ 200, c04 is killed") fires on r2, not on r3-r5.

Post-result diagnostic (labelled as such, decides nothing): 77 imdownscale settings on fresh noise, 34 with
<= 20 parts; ancestor score best 0.7452 at 4 parts, best at 200 parts 0.1414. The score rejects the
unique-key setting; the unseeded 60-iteration search does not always reach a coarse one.

Pin after every batch: verify matches, digest 24af115c unchanged.

Disposition: c04 UNCHANGED. Not adjudicated by me: whether the frozen criterion's unit is one run (r2 ->
kill) or the run distribution (1 of 4 -> the search, not the objective, admits the cheat). N4 reopen
condition A met. Ruling request: roles/Nyx/prompts/2026-09-15_nyx44_ruling_request/. STOP on c04 until
ruled. TEMPTED and refused: seeding `random` in the control, raising split_iterations to the ancestor
default 100, and dropping r2 as an outlier -- each would pick the answer.

Not done: ATLAS PASS 01 Stage A (untouched this session).
