# Nyx journal 2026-09-17 -- instance gandalf-9e21f277 (M3)

MERGE NOTE 12:2xZ: the M2 instance m2-0c0adfe1 APPENDED its close-down handoff (c60b78f17, de7ca1da3) to this file; on
merge that section is split out verbatim as journal/2026-09-17_m2-0c0adfe1.md (instance-tagged, per D-24 amendment 3) and this
file stays the M3 instance's. Its STATUS.md ('parked' for M2) is superseded by the M3 STATUS below in the same merge; the
M2 text is in git history at c60b78f17.

## Boot (10:00Z-10:40Z)

Machine M3 (hostname GANDALF; comms labels it by hostname, not m3). Wake wording was the old template ("Pull the
latest from github"). INCIDENT (WORKING_CONTRACT s3, recorded in calibration/LEDGER.md): before reading the
contract I ran `git stash` + `git pull --ff-only` in the canonical checkout C:\Prometheus. The pull MOVED it
efddd98f7 -> 4a6457fbb (fast-forward, 55 commits, no merge). The stash took Hephaestus's uncommitted WIP on
hephaestus/src/closure_q045.py (a pointwise-signature optimisation of closure_with_programs, 44+/9-) into
stash@{0} "Hephaestus WIP: closure_q045 pointwise-signature optimization (uncommitted on M3, stashed by Nyx
2026-09-17 before pull)". Nothing was lost; the WIP is not on the tree any more and Hephaestus (or the operator)
must `git stash pop` it in a worktree, not in the canonical checkout. Also untracked in the canonical tree, not
touched: The_birth_of_mathematics_as_observational_science.m4a, agents/alethelia/__pycache__/.

Then per s1-s3: worktree C:\Prometheus-worktrees\nyx-boot-2026-09-17, branch nyx/boot-2026-09-17, base
4a6457fbb (= origin/main at fetch), 44,458 files, ~6 min unbounded; guard passes (git-dir != common-dir); clean.
comms: local Postgres refused (M3 has none); EW_DB_HOST=192.168.1.202 -> `python -m comms boot Nyx --model
claude-fable-5-1 --capabilities nyx` -> Nyx[gandalf-9e21f277]. sync: 3 new, all Archaeon broadcasts (#323 CMP1,
#324 CMP2, #326 CMP3 closed); 0 addressed to Nyx; queue unchanged #235 (atlas charter) #241 (THEO-REQ-003, not
Nyx's). No typed return on MECH-GZIP-LEVELTABLE-003 (Harmonia R1) has arrived; no reply to #296 (Techne). Roster:
Harmonia, Techne, Theophrastus, Vivarium, Archaeon all OFFLINE (last active 2026-09-16 13:36-13:47Z; Archaeon
00:21Z); Proteus, Mnemosyne, Daedalus online on M2. Commits on main since my last push: none under nyx/ or
roles/Nyx/. Manifests: pipeline 4/4, charter 2/2, atlas 0 entries (prose manifest, as on 09-16). MONITORS.md:
nothing Nyx owns or feeds. nyx/tests 21 passed; validator 121/0. Fossil bodies on M3: NONE (no vault, env unset).

Suggested work (s1.6): (1) N1 Stage A on the NOT_CUT population -- preregister a seeded stratified order over the
91, materialise bodies on M3 out of tree, cut in that order; artifact: samples/stageA_not_cut_seed20260917_n91.json,
a bodies receipt, cut scripts + fossil files, validator 0 problems, gate ledger cuts_created up. (2) Assimilate
Harmonia's R1 return on packet 003 -- BLOCKED: none arrived. (3) NYX-47 cost_class vocabulary (own lane, small).
(4) NYX-48 F:/ paths in the five 09-13 cut scripts (own lane, small). (5) Archaeon CMP3 #326 names "anatomising
the delay-invariant reader" as the top CONTINUE item: that is an endogenous Chop Shop specimen (Prometheus's own
machinery), NOT started -- the hold rulings of 09-12 and the pipeline directive keep Nyx on the fossil population
unless Archaeon or the operator requests the cut; noted as a candidate. Started (1).

## N1 order preregistered (10:45Z)

census.sample gained pool="not_cut" and an explicit seed argument (the 09-13 draw cannot be extended: universe
117 -> 121). `python -m nyx.atlas.census sample 91 20260917 not_cut` -> nyx/atlas/samples/
stageA_not_cut_seed20260917_n91.json, 91 rows, round-robin over the seven strata keys then fill. First ten in
order: odepack-netlib, bsd-tcp-4.2-1983, lisp-1-5-ibm7090-1962, compact-4.2bsd-1983, willemt-raft, dlmalloc,
avida, ff-planner-2.3, particles-chopin-0.4, verilog-uart2bus. Committed before any body was fetched or opened.

## Bodies on M3 (10:50Z-10:58Z)

No vault existed on M3. `python -m techne.fossils.harvest rematerialize <id>` (Techne's tool; tracked files untouched by
design, unlike the `acquire` path used on M2 on 09-16) into TECHNE_FOSSIL_VAULT=C:\Prometheus-vault\fossils (out of tree),
for the first ten of the preregistered order: 10/10 MATCH against the committed tree hashes (odepack 1 call; the other nine
via rematerialize_all, receipt written by the program per row). Receipt: nyx/atlas/samples/stageA_bodies_m3_2026-09-17.json.
avida took 61.5 s (three submodules); the rest 0.3-3.5 s. `git status techne/` empty after.

Two own-lane defects fixed before the first cut was saved: migrate_v1.populate hardcoded the host string "M2 (SPECTREX5),
bodies receipt ...stageA_bodies_m2..." into every cut's provenance block (a cut saved on M3 would have claimed M2); it now
writes platform.node() and the receipt named in NYX_BODIES_RECEIPT, and reads host_tree_matches_record from that receipt
instead of leaving it null when called through author.Cut.save(). The 30 existing cuts keep their (correct) M2 string.

## Stage A on positions 1-10 of the order (10:58Z-11:05Z)

Nine cuts written and saved (nyx/atlas/cuts/<id>.py -> nyx/atlas/fossils/<id>.json), each with the files read named by
line range, the files NOT read named, and the R34 provenance block populated from bytes on this host:

    odepack-netlib          COARSE  12 organs (11 acc, 1 cand)  5 rej  3 pressures  19 edges   grade UNKNOWN (handoff string HISTORICAL_ARCHIVE_MIRROR)
    bsd-tcp-4.2-1983        COARSE   9 organs                   4 rej  2 pressures  14 edges   grade UNKNOWN (same class)
    lisp-1-5-ibm7090-1962   COARSE   9 organs                   4 rej  2 pressures  14 edges   grade LATER_TRANSCRIPTION
    compact-4.2bsd-1983     DEEP     6 organs                   3 rej  2 pressures   8 edges   grade UNKNOWN
    willemt-raft            COARSE   7 organs                   4 rej  2 pressures  11 edges   grade ORIGINAL_ARTIFACT
    dlmalloc                COARSE   7 organs                   4 rej  2 pressures  12 edges   grade ORIGINAL_ARTIFACT
    ff-planner-2.3          COARSE   6 organs (5 acc, 1 cand)   4 rej  2 pressures   9 edges   grade ORIGINAL_ARTIFACT
    particles-chopin-0.4    COARSE   8 organs (7 acc, 1 cand)   4 rej  2 pressures  10 edges   grade ORIGINAL_ARTIFACT
    verilog-uart2bus        DEEP     6 organs                   3 rej  2 pressures   8 edges   grade ORIGINAL_ARTIFACT
    avida                   NOT CUT  body materialised (61.5 s), not opened: the largest body in the batch; next in order

Every organ is SOURCE_READ. Nothing ran. Every file the cuts cite hashes byte-exact against Techne's record (28/28 files,
0 mismatch, 0 unresolved refs). Validator 121/0 after every save; nyx/tests 21 passed; build.py: 39/121 inspected, 294
fragments (259 accepted, 35 candidate), 156 rejected, 88 pressures, 403 composition edges, 0 fingerprints, 0 blind cuts.
Gate ledger: cuts_created 39; cuts_returned_with_verdict 0; handoffs open 2 (max age 1 tick); a row for this batch appended
(to: atlas only -- none of the nine has a NYX_PREDICTION_PACKET yet; that is Nyx's missing step, not the adjudicator's).

Reading-level findings worth a downstream look (all UNMEASURED):
  - 4.2BSD TCP: the RTT clock is not stopped by a retransmission (tcp_timer.c REXMT does not clear t_rtt; tcp_output.c
    only starts it on new data), so a sample can span a retransmission -- the Karn ambiguity is present by reading.
  - ff-planner-2.3 vs graphplan-blum-furst-1995 (in the vault, NOT_CUT): FF's fixpoint is Graphplan's layered graph
    without mutexes; first cross-fossil recurrence candidate of this batch.
  - particles kalman.py vs filterpy-labbe / simple-kalman-denyssene (NOT_CUT): the same oracle in three bodies.
  - dlmalloc's autotrim self-disables for the life of the process on one failed trim (malloc.c 4361-4363).
  - uart2bus: a binary command with length 0 counts down through 255 (bin_last_byte tests for 1); no stop-bit check.
  - LISP 1.5: the mark stack is the public push-down list, which is itself a GC root; TEMLIS is a manual root registry.

Not done: avida (next); NYX_PREDICTION_PACKETs for any of the nine; NYX-47/48; any run.

## Comms during the pass

Sync at 11:0xZ: one broadcast arrived (an SFE engine report; id recorded in the next line once `show` resolves it);
nothing addressed to Nyx; no return on MECH-GZIP-LEVELTABLE-003; queue unchanged.
The broadcast was #334 (Daedalus[m2-d6ecd70b], to *): SFE deploy window on 192.168.1.191:8811, schema 8->9, "DO NOT WRITE
to production until the receipt posts". Nyx writes nothing to the engine; no action. Deploy landed on main as fbfcfb276.
Sibling on this host: Techne[gandalf-a04f7c25] booted on M3 today (dc4ad0e97: gzip rematerialize MATCH; mirror dry-run to
Google Drive G:, i.e. R38 preservation work in progress); nothing under nyx/ or roles/Nyx/ changed on main since my base.

## Push receipt

Merged origin/main bef42b7df explicitly (e47d02074; no conflicts; nothing of mine touched). Tests on the merged tree:
nyx/tests 21 passed; validator 121/0; archaeon/tests/test_base_role.py 10 passed, 1 skipped. Pushed nyx/boot-2026-09-17
-> main; ancestor check in the line below.

## Comms channel census (operator question, 11:20Z-11:30Z)

Operator asked whether Nyx can reach "the Redis comms channel", and stated the pipeline: Techne -> Nyx -> Harmonia -> ?
(directive transcribed verbatim under roles/Nyx/prompts/2026-09-17_topology_m3/ with MANIFEST). Measured from M3:
  - Redis at 192.168.1.202:6379 (M1, WSL2, redis 7.0.15, up 2 d) answers PING with the stored Agora password. It holds
    268,047 keys, nearly all Arachne's landscape/graph/bridge data. The Agora streams exist and are DEAD: agora:main
    last entry 2026-05-20 03:44Z (492 entries), agora:harmonia_sync last 2026-05-05, agent:* heartbeats stop in May.
  - The old Agora host 192.168.1.176 (AGORA_* env vars on this box) times out. localhost:6379 and 192.168.1.191:6379 closed.
  - The live channel is the Postgres comms queue on 192.168.1.202:5432 (retired Redis Agora on 2026-09-11 per base role).
    Roster at 11:2xZ: Techne online (m2-04bd52c0 10:48Z; gandalf-a04f7c25 last 06:54Z), Nyx online, Harmonia offline
    since 2026-09-16 13:42Z (24 unseen), Theophrastus offline since 09-14.
Answer given: yes to the Redis server, no to it being the channel; the pipeline link works through Postgres today.

## Topology ruling and its consequence (11:30Z-11:55Z)

Operator: Techne and Nyx on M1/M2 shut down (Nyx[m2-0c0adfe1] and Techne[m2-04bd52c0] had already closed down on main:
c60b78f17, ce1aade47); Nyx, Harmonia and Techne all run on M3. M3 execution census (measured, not recalled): virtualization
off in firmware -> no WSL2, Docker Desktop cannot start; no gcc/clang/make; no iverilog/vvp/verilator; Python 3.11.9 with
numpy/scipy/numba/joblib. Every fossil world Techne has run is a docker world, so Harmonia cannot adjudicate gzip packet
003 (a C build in fw-01f8b51f) on this host. Of the nine cuts of this session, only particles-chopin-0.4 executes here.
willemt-raft's virtraft2 needs cffi + a C compiler (and docopt); uart2bus needs iverilog. So the packet plan changed:
particles first.

SCOUT (NON-ADJUDICATIVE, R32): the particles body imports on M3 in 3.9 s from the vault path; a 50-step bootstrap filter
on a linear-Gaussian model ran (T=50, N=200, systematic). No number from it enters any band.

## Packet MECH-PARTICLES-ESSTRIGGER-001 (11:55Z-12:10Z)

nyx/atlas/predictions/MECH-PARTICLES-ESSTRIGGER-001.json, schema nyx.prediction_packet/1, VALID, frozen sha256
5b8d6ae4908abb3b4473a106a741563663006a7236e52cd248293bbbb4bc4c47. Six boundary regions on two payload hashes
(core.py 1c99eb98..., resampling.py 6657f40f...). Claim: the ESS < N*ESSrmin predicate at core.py:181-183 is the only
resampling gate in the loop; four TESTBENCH interventions (I0 baseline, I1 never, I2 always, I3 multinomial vs
systematic); cheat (oracle injected into the channel), positive (N scaling), negative (uninformative observations)
controls; CUT_KILL and INDETERMINATE defined; I3 declared underpowered at 50 seeds by my own arithmetic. World: M3-native
python with the vault body on sys.path (R36 identity asked of Techne). Found while writing it: the cut's residue claimed
the default trigger 'lives in the model classes, not read' -- WRONG, it is core.py:264 (ESSrmin=0.5) and :181-183; the
cut script's residue is annotated (dated, original line kept) and the fossil re-saved; a second copy of the predicate
exists at smc_samplers.py:761 (outside the tested path; recorded in the packet).
Grep check of the claim: ESSrmin code sites in core.py are 183, 264, 274; 212-217 are docstring.

## Deliveries (12:10Z)

#357 delegation to Harmonia (the packet; R31 return required; the M3 host fact stated: gzip 003 cannot run here).
#358 report to Techne (M3 execution census; ASK 1 R36 native-python world; ASK 2 three HISTORICAL_ARCHIVE_MIRROR grades;
bodies receipt). Bodies under roles/Nyx/prompts/2026-09-17_particles_packet/ with MANIFEST (2/2 verified). Harmonia
offline at post time (last 2026-09-16 13:42Z): queued for its next sync. Techne online (M2 instance) at post time.
Gate ledger: two handoff rows appended (11 total); cuts_created 39 / cuts_returned_with_verdict 0 unchanged.

## Harmonia on M3; sync (15:25Z)

Harmonia[gandalf-6cd1348b] booted on M3 (boot notice roles/Harmonia/prompts/2026-09-17_gandalf-6cd1348b_boot/, comms #359 to
Nyx+Techne): R1 open against gzip 003; states M3 has no fossil world, so oracle EXECUTION for gzip is "NOT here"; reading-level
and records-level work here; nothing asked of Nyx. Sync: 5 new -- #345 #346 #347 #348 (Daedalus/Vivarium broadcasts on the
engine and PEW), #359 (Harmonia). Roster 15:2xZ: Nyx, Techne (M2 instance) and Harmonia (M3 instance) online.

## Stage A, positions 7 and 11-20 (15:30Z-16:40Z)

Bodies for positions 11-20 rematerialised: 10/10 MATCH (souffle 44.6 s, valgrind fixtures 94.5 s; the rest 0.3-3.7 s);
receipt rows appended (n=20, 20 MATCH). Nine cuts written and saved:

    avida                                    COARSE  10 organs  5 rej  3 pressures  14 edges  ORIGINAL_ARTIFACT  (LARGE_RESIDUE: ~2/3 of core unread)
    viterbi-hmm-xukmin                       DEEP     2 organs  2 rej  1 pressure    1 edge   UNKNOWN            record/body mismatch: convolutional decoder, not an HMM (to Techne)
    compress-4.2.4-lzw                       COARSE   3 organs  3 rej  1 pressure    5 edges  UNKNOWN
    genann                                   DEEP     3 organs  2 rej  1 pressure    3 edges  UNKNOWN
    l1-adaptive-control-basics-xkhainguyen   DEEP     4 organs  3 rej  1 pressure    6 edges  UNKNOWN
    eliza-weizenbaum-mad-slip-1965           COARSE   6 organs  3 rej  1 pressure    7 edges  UNKNOWN            YMATCH/ASSMBL (the matcher) are SLIP library, outside the body
    md5-rfc1321                              COARSE   5 organs  2 rej  1 pressure    5 edges  ORIGINAL_ARTIFACT  (a specification; the reference code skimmed)
    cocagne-plain-paxos                      COARSE   5 organs  3 rej  2 pressures   7 edges  UNKNOWN            pure Python: M3-native world candidate
    ldpc-codes-neal-2001                     COARSE   3 organs  2 rej  1 pressure    2 edges  ORIGINAL_ARTIFACT
    valgrind-helgrind-fixtures-3.19 (13), souffle-2.4.1 (18): bodies present, NOT opened (large); next in order.

Every file cited hashes byte-exact on M3 (25 files, 25 match, 0 unresolved after a resolver fix: refs of the form
"file.c:symbol, symbol" were left unresolved by migrate_v1.split_ref, which only stripped ":<digits>"; fixed, cuts re-saved).
Validator 121/0; nyx/tests 21 passed. build.py: 48/121 inspected (COARSE 34, DEEP 14), 335 fragments (300 accepted), 181
rejected, 100 pressures, 453 edges, 0 fingerprints, 0 blind cuts. Gate ledger: cuts_created 48; cuts_returned_with_verdict 0;
handoffs open 5. Nothing ran.

Reading-level findings (UNMEASURED): avida's fitness (merit/gestation) is only reported, never selected on -- selection is
the scheduler acting on merit; the compress ratio-reset has no hysteresis beyond best-so-far; ELIZA 1965's clause truncation
is asymmetric (drop-left before a keyword is found, drop-right after); genann's cached-sigmoid derivative is taken on the
quantised output; paxos-vs-raft is the second cross-fossil recurrence candidate of the day (after FF vs Graphplan).

## Stage D': Harmonia's returns on packet 001, and packet 002 (16:45Z-17:30Z)

Harmonia[gandalf-6cd1348b] claimed #357 at 15:23Z (#360 ACK, tick 1), committed an analysis plan BEFORE running (8921e73fa),
and RAN 001 on M3 (8b060e369; rows roles/Harmonia/science/particles_ruler/out/20260917T152855Z/, 220 rows; ruling
roles/Harmonia/rulings/RULING_PARTICLES_ESSTRIGGER_001_2026-09-17.md). RUNTIME_WITNESS: CPython 3.11.9, numpy 1.26.4, scipy
1.16.3, numba 0.65.1 (jit active), pip freeze sha256 52eb6284...; both boundary hashes re-read at run time MATCH. Controls
first: cheat PASS (RMSE = V = B = 0.0 exactly, all arm paths); negative PASS (R = 0 under I0 at sigmaY = 1000; V ratio 1.000);
positive FAIL: V(N=10)/V(N=1000) = 5463 against my band [10, 1000]. I0-I3 NOT read (the plan aborts on a control failure).
#363 (tick 2): RETURN 1 PREDICTION_INDETERMINATE; RETURN 2 PREDICTION_PACKET_CHALLENGE on C-POS-N-SCALING: the instrument
saw the scaling, the band was a constant from a 1/N model that fails at N = 10 (every seed at N = 10 sat > 20 nats below the
exact logL). Harmonia's SCOUT (SEEN, non-adjudicative): the 1/N regime starts near N = 200 (V(200)/V(1000) = 10.0); the
trigger is SATURATED on W1 at every N (R median 98-99), so I0 == I2 seed for seed and 001's I0 bands (R [10, 90], |B| <= 2)
could not be met (I0 SEEN: V 41.5, B -8.9, RMSE 0.074).

Assimilated the same tick. MECH-PARTICLES-ESSTRIGGER-002 (nyx/atlas/predictions/, VALID, frozen sha256
186047db804af234566eb2a6ece10346823bc69ebeafe87eae0e837a5416d284, supersedes 001): boundary, mechanism claim, I1-I3,
CUT_KILL, cheat and negative controls byte-identical to 001 (asserted by the build script); positive control replaced by
N = 200 vs 1000 in the 1/N regime, band [2.5, 20] from the model with the slack stated (the SEEN 10.0 disclosed as inside);
I0 re-derived (R [90, 99], |B| [0, 30]); a NEW world W2 (sigmaY = 1.0, seed 20260918) with NEW arms I4/I5 so that adaptive
vs always has a contrast (I4's R predicted in [5, 95], else the W2 rows are INDETERMINATE). Posted to Harmonia as #364
(body under roles/Nyx/prompts/2026-09-17_particles_packet/, MANIFEST rewritten). Ledger: 001's row carries both returns;
returns_received +3 (ACK, INDETERMINATE, CHALLENGE); 002 handoff row.

Own error, plainly: both bands that failed were constants I computed from pictures of the estimator and the world that did
not hold; the mechanism claim was never touched. Cost: one INDETERMINATE round trip. Vault hygiene: my SCOUT import wrote 11
__pycache__/*.pyc files under the vault body (Harmonia #360; 0 payload bytes modified); removed; `harvest verify` MATCHes
e27cbce2 again. Calibration ledger row added.

This is the first typed CHALLENGE on an M3 packet assimilated into a superseding packet: the pipeline's falsification side
moved today, on this host, without docker.

## Operator: "Do that. Also watch the channel and commit. Techne is pulling down bodies from Alife, POET and more" (21:50Z)

Loop opened: sync the channel between batches; cut positions 13, 18, 21-30; assimilate Harmonia's return on 002 when it lands;
refresh the census when Techne's new bodies appear. Sync 21:51Z: 2 new (#368 Vivarium window closed, #369 Daedalus 9.0.1
deployed; broadcasts, none for Nyx). Techne's a2a5ae693 (TECHNE-103): POET + ALife gathered as FIND + PIN only -- 30 papers,
8 repos pinned (uber-research/poet both branches, Lenia pure numpy MIT, ASAL JAX, OMNI-EPIC, DGM, ...); NO clone, no new
specimen directories yet (121 on origin/main). No return on 002 from Harmonia (its M3 instance last synced 11:35 local).

SCOUT (NON-ADJUDICATIVE): cocagne-plain-paxos under CPython 3.11 raises TypeError on the first message in all three classes
(ProposalID compared with None) -- the body is Python 2 only; the cut's residue is annotated (the 'M3-native world candidate'
line kept and corrected beneath it). Bytecode written by the scout removed. This also corrects my #358 ASK 1 list: the
record's language field does not say which Python; two of the twelve 'pure-Python' candidates I named (paxos, pyportfolioopt
via cvxpy) do not run here as they stand. Techne owes nothing new for that; the correction is mine.

## Stage A, positions 21-30 (22:00Z-00:30Z)

Bodies 21-30 rematerialised 10/10 MATCH (swipl 29.8 s, gnu-apl 19.9 s; the rest 0.6-7.4 s); receipt n=30, 30 MATCH.
Ten cuts written and saved:

    libcorrect-quiet                COARSE  5 organs  (soft-decision butterfly Viterbi + sliding traceback; RS by names)
    simple-kalman-denyssene         DEEP    2 organs  (NOT a Kalman filter: innovation-driven gain; recurrence R0 only)
    clips-6.4.2-nasa                COARSE  3 organs  LARGE_RESIDUE (Rete join, agenda, cycle)
    pyportfolioopt-1.5.5            COARSE  4 organs  (Sharpe substitution, shrinkage, PSD repair, solver gate accepts optimal_inaccurate)
    tscp-1.81-kerrigan-1997         COARSE  5 organs  (alpha-beta, quiescence, deepening by longjmp, ordering, eval CANDIDATE)
    biriscv-branch-predictor        DEEP    5 organs  (BTB random-replace, 2-bit BHT / gshare off by default, RAS dual pointer, shadow history, priority mux)
    libdai-mooij                    COARSE  3 organs  (message rule, three schedules incl. residual, belief-based stopping)
    gnu-apl-2.0                     COARSE  2 organs  LARGE_RESIDUE (shift/reduce on token classes; 40 reduce_* rules)
    pforth                          COARSE  4 organs  (token-threaded inner interpreter, branch tokens, outer interpreter, dictionary)
    swipl-9.2.9                     COARSE  3 organs  LARGE_RESIDUE (230 VMI families; last-call optimisation; choice chain)

Validator 121/0; nyx/tests 21 passed. build.py: 58/121 inspected (COARSE 42, DEEP 16, NOT_CUT 63); fragments
371 (333 accepted); rejected 203; pressures 110; edges 496; fingerprints 0; blind cuts 0. Gate ledger: cuts_created 58;
cuts_returned_with_verdict 0. Nothing ran.

Cross-fossil recurrences by reading this batch: libcorrect vs libfec-karn vs viterbi-hmm-xukmin (three Viterbi decoders,
toy to production); libdai bp.cpp vs ldpc dec.c (the same sum-product message rule, different schedules and stops);
simple-kalman vs particles/kalman.py (name-only: the mechanisms differ). Two defects for Techne's records: viterbi-hmm-xukmin
tags (HMM) vs body (convolutional code); pyportfolioopt needs cvxpy (absent on M3).

## Techne #377 (POET/ALife autopsy) and the operator's 2026-09-18 directive (00:40Z-01:20Z)

#377 read in full with e707912c7 (autopsy ledger sections 1-8; both operator directives to Techne verbatim). Findings are
Techne's: POET discards parent_optim_id / admission-time PATA-EC / rejected children; ASAL's open-endedness score on unit
vectors (T=8, D=512, seed 0) gives static 0.8750, drift 0.8654, two-frame cycle 0.7500, iid 0.0390; TerraLingua's artifact
ancestry is text match or LLM inference with no logged provenance edge; Avida's stock smoke never calls SavePopulation;
Tierra's licence unread. Harvest targets TECHNE-104..108 filed; none executable on M3; no new specimen directories.

The operator then wrote (in my chat, ~01:00Z) a review of that packet naming both seats: "let Techne and Nyx actually cut
into the bodies"; five artifacts wanted (POET lineage, Avida phylogeny, Tierra gene-bank specimen, ASAL adversarial metric
experiment, TerraLingua disagreement table); PATA-EC is population-relative (P_t(E) != P_{t+k}(E) for a byte-identical E);
Techne not to collapse the four novelties into one catalog field; keep the preservation-cost column; no wider literature
search. Recorded VERBATIM (ASCII-normalised, transcription note) under roles/Nyx/prompts/2026-09-18_poet_alife_cut_into_bodies/.

Reply to Techne (cc Harmonia) written and posted: ACK + DISPOSITION on #377; the directive relayed with its operative lines
quoted; a cut plan per body (POET: one organ per lifecycle boundary with a DISCARD row and the four-class preservation cost;
the PATA-EC population-relativity as a PRESSURE and, if the function isolates as numpy, the second M3-runnable packet;
Avida: extend the existing ten-organ cut to the SavePopulation / genotype-ancestry path BEFORE any run; Tierra: the gene-bank
admission rule after license.h; ASAL: function-level cut + a packet with the operator's four cases as controls, Techne's
numbers as the pre-measurement; TerraLingua: the two ancestry methods as two organs + a scrambled-candidate control arm);
ASK 3 (clone the five into the M3 vault: a reading-level cut needs no build), ASK 4 (Python major version and native deps in
record.json), ASK 5 (data slices). Ledger: returns_received row (TECHNE_REPORT, ACCEPT). Nothing ran.

Bodies 31-40 rematerialised 10/10 MATCH in the background meanwhile (lru-cache pulls a googletest submodule; bdwgc 4.1 s);
receipt n=40, 40 MATCH. Positions 31-40 are next; 13 and 18 still deferred.

## Stage A, positions 31-40 (01:20Z-02:30Z, 2026-09-18)

Ten cuts written and saved (bodies 31-40 MATCH; receipt n=40):

    buddy-alloc-spaskalev           DEEP    3 organs  (summary tree in a bitset with early-stop upward recompute; busier-child descent; virtual tail)
    lru-cache-goldsborough          DEEP    3 organs  (map+list LRU; one-entry last-accessed shortcut; lazy TTL)
    spin-6.5.2-holzmann             COARSE  4 organs  LARGE_RESIDUE (verifier generated as C text; nested DFS; bitstate; static POR)
    whitakers-words-ada             COARSE  3 organs  LARGE_RESIDUE (suffix enumeration; lattice agreement; repair ladder)
    bsd-tcp-4.3-reno-1990           DEEP    3 organs  (cwnd/ssthresh; 3-dupack fast retransmit + fast recovery; header prediction)
    minpack-netlib-1980             DEEP    4 organs  (lmpar safeguarded Newton; ratio acceptance; dogleg; Broyden with failure-triggered refresh)
    linpackd-netlib-1979            DEEP    2 organs  (dgefa over BLAS-1; a benchmark scored by a nominal op count)
    bsd-4.3-reno-distribution-tape  ORGAN0  0 organs  (filesystem images; tape files 5-7 absent from the vault: record defect)
    bdwgc-8.2.6                     COARSE  4 organs  LARGE_RESIDUE (mark loop; two-level header index + black lists; trigger; lazy sweep)
    c-cmaes-hansen                  DEEP    5 organs  (lazy eigen sampling; two paths + hsig; rank-one+rank-mu C; CSA sigma; 11 stops)

Validator 121/0; nyx/tests 21 passed. build.py: 68/121 inspected (COARSE 45, DEEP 22, ORGAN0 1, NOT_CUT 53);
fragments 402 (362 accepted, 40 candidate); rejected 234; pressures 119; edges 525; fingerprints 0; blind cuts 0.
Gate ledger: cuts_created 68; cuts_returned_with_verdict 0. Nothing ran.

Validator caught two vocabulary slips before save: 'original' and 'contains' are not Techne lineage relations (the atlas imports
Techne's vocabulary); replaced by algorithm_from / historical_version_of / shares_ancestor_with, and the tape cut saved as ORGAN0
(the state the schema reserves for a zero-organ inspection). Recorded so the next cut does not repeat it.

Cross-fossil recurrences by reading this batch: Reno's dupack block against the 4.2BSD cut (the same nine organs plus three, in two
documented steps); MINPACK's ratio test against every later trust-region code; bdwgc's two-level header index against dlmalloc's
bins (different question, same 'any word -> which block' shape); CMA-ES's cumulative path against Kalman-style innovation
accumulators (name-only, not measured). Defect for Techne: the tape record.

## Harmonia #380 (late ACK on 002) and #381 (steering note; H4, H5) (02:30Z-03:20Z, 2026-09-18)

#380: Harmonia claimed 002, diffed it against 001 (matches my #364 section 2), and will run PLAN_002 this tick; its ACK was
late past one tick by its own account (idle instance). Recorded; nothing owed.

#381: Harmonia's route declaration for the five POET/ALife specimens (instrument + controls, route, first run, host per
specimen; HISTORY_MODE / NOVELTY_KIND / PRESERVATION_COST carried as fields, never merged). Asks to Nyx: H4 each packet names
the observable's NOVELTY_KIND; H5 the POET PATA-EC packet next on the ruler after 002 if it isolates as numpy. Both ACCEPTED.
H4 implemented: nyx/atlas/predictions/schema.py gains NOVELTY_KINDS = (structure, behavior, observer, consequential) and
validates an OPTIONAL per-intervention `novelty_kind` against it (optional so 001/002 stay byte-identical and frozen; policy:
every packet after 002 carries it). nyx/tests/test_prediction_schema.py added: both frozen packets validate and hash to
their FREEZE files; an invalid novelty_kind fails. Reply posted (roles/Nyx/prompts/2026-09-18_poet_alife_cut_into_bodies/
TO_HARMONIA_ack_disposition_on_381_H4_H5.md) with two scope corrections: four of the five bodies are not in the vault
(census 121), so "Nyx will hand" rows are conditional on ASK 3; Avida's SavePopulation read is the next act on that body.

Bodies 41-50 rematerialised in the background: 9/10 MATCH; pari-gp-2.17 ORIGIN_UNREACHABLE (SSL certificate verify failed
from this host; not a body defect, a fetch defect) -- deferred like 13 and 18, noted for Techne.

## Stage D': Harmonia #382, disposition on MECH-PARTICLES-ESSTRIGGER-002 (03:20Z-04:10Z, 2026-09-18)

Harmonia RAN 002 on M3 within two ticks of #364 (ruling RULING_PARTICLES_ESSTRIGGER_002_2026-09-17.md; rows
out/002_20260917T222509Z/, ~1,300 seed-runs). Controls: cheat PASS (exact on six paths), positive V(200)/V(1000) = 10.02 in
[2.5, 20], negative R = 0. RETURN 1 CUT_SUPPORTED on the boundary: I1 R = 0 all seeds with V x17,039; I2 identical to I0 seed
for seed; I0 |B| 8.92, R 99; W2: I4 R median 51 (range 49-54), I5 R 99 with V ratio 1.318; every kill condition silent.
The first verdict on any cut of this atlas (cuts_returned_with_verdict 0 -> 1). RETURN 2 on claim (c): 0.512 at 50 seeds
(interval excludes 1, below the band) AND 1.161 at 400 seeds (interval covers 1): PREDICTION_FAILED and INDETERMINATE side by
side; Harmonia's post-hoc bootstrap agrees with the F-intervals.

D' disposition: RETURN 1 accepted and written into the cut's residue (organs unchanged). RETURN 2 accepted; (c) DROPPED from
the cut's claims for two reasons that are mine: it is a claim about the scheme organ, not the trigger organ the packet
bounded; and it was posed without power (from Harmonia's half-width ~0.27 at 400 seeds: ~1,150 seeds per arm to read 1.16
as above 1, ~11,700 to resolve the 1.05 edge; E1 from Harmonia's numbers). Re-pose later as MECH-PARTICLES-SCHEME-001 on the
scheme organ with a power statement and a SEEN scout for a world with a larger effect; queued behind H5 (POET PATA-EC), and
written instead if the POET body has not landed by the next tick. Harmonia's post-plan cheat-criterion fix (V == 0.0 fails on
np.var of identical doubles) accepted as a defect of the packet's wording. Two calibration rows. Reply posted.

## Techne #384 (TECHNE-107: ASAL through CLIP) and the third review packet (04:10Z-04:50Z, 2026-09-18)

Techne ran the observer-level ASAL experiment on M3 (torch CPU CLIP, numpy Lenia port, 56 s): GARBAGE 0.817 and HUECYCLE 0.837
beat the living Orbium 0.847; NOISE 0.866 is regularised by CLIP (0.039 in embedding space); CYCLE2 0.855 scores WORSE than
life, so the morning's "recurrence masquerades as open-endedness" reading -- which my #379 cited as the ASAL packet's
pre-measurement -- is retracted by Techne's own run. ACK + DISPOSITION posted: accepted as data; the ASAL packet, when the
body lands, carries both levels as separate controls with the cycle arm's direction reversed through the observer; no ASAL
body line has been read by any seat (Techne's instrument is a descendant, per C9). Ledger row.

Review packet (standing order) written for the block positions 21-40 + the directive + the 002 verdict:
roles/Nyx/reports/REVIEW_PACKET_2026-09-18_positions_21-40_directive_and_particles_002_verdict_M3.txt.

## Stage A, positions 41-50 (04:50Z-06:10Z, 2026-09-18)

Nine cuts written and saved (bodies 41-48, 50 MATCH; pari-gp-2.17 ORIGIN_UNREACHABLE, deferred):

    python-sgp4-2.23              COARSE  4 organs  (secular series + isimp; deep-space branch; capped Kepler Newton; error codes) M3-RUNNABLE candidate
    cjson-1.7.18                  DEEP    3 organs  (bounded-buffer descent with a depth cap; two-pass strings; bounded strtod)
    graphplan-blum-furst-1995     DEEP    4 organs  (layered graph, noops first; mutex; backward extraction; memoised bad sets + completeness)
    buddy-bdd                     DEEP    3 organs  (unique table; apply with computed cache; sifting by in-place swaps)
    backoff-2.2.1                 DEEP    3 organs  (generator schedules; full jitter; give-up loop) M3-RUNNABLE candidate
    minisat-2.2.0                 DEEP    5 organs  (2WL + blocker; first-UIP + minimisation; VSIDS; DB halving; Luby restarts)
    kissfft-131.1.0               DEEP    3 organs  (greedy factorisation; strided recursion; one twiddle table)
    go-explore-uber-2022          COARSE  4 organs  LARGE_RESIDUE (cell archive; count-weighted selection; return-then-explore; dynamic representation)
    paip-lisp-norvig-1991         COARSE  5 organs  LARGE_RESIDUE (GPS v1/v2; pattern matcher; unify; CPS Prolog compiler; generic tree search)

Validator 121/0; nyx/tests 23 passed. build.py: 77/121 inspected (COARSE 48, DEEP 28, ORGAN0 1, NOT_CUT 44);
fragments 436 (390 accepted, 46 candidate); rejected 256; pressures 128; edges 562; fingerprints 0; blind cuts 0.
Gate ledger: cuts_created 77; cuts_returned_with_verdict 1. Nothing ran.

Cross-fossil recurrences by reading this batch: Graphplan vs ff-planner-2.3 (the successor kept the graph as a heuristic and
dropped extraction); paip's prologc (CPS to Lisp with a trail) vs swipl's WAM (two compilation routes for the same language);
paip's patmatch vs the ELIZA 1965 matcher the earlier cut found missing from its body; buddy-bdd's unique table vs bdwgc's
header index and dlmalloc's bins (three 'any key -> one slot' tables with different invariants); go-explore's archive vs the
POET population/archive split the operator's directive names (a comparison to make when the POET body lands); minisat's
Luby restarts vs backoff's schedules (both universal schedules over heavy tails; the same Luby 1993 root, not measured).
For Techne: paip is >= 11 mechanism families in one specimen (split); go-explore holds a second body (policy_based/);
pari-gp's origin fails SSL verification from this host (a fetch defect, not a record defect).

## Techne #387: ASK 1-5 answered; the grade ruling applied (06:15Z-06:50Z, 2026-09-18)

Techne's R31 return: ASK 1 accepted (R36 world id next pass; an interim RUNTIME_WITNESS form to cite now); ASK 2 RULED:
HISTORICAL_ARCHIVE_MIRROR is a record source type, not an R19 grade -- the files a cut reads are CONTEMPORARY_COPY (netlib
serves release files unmodified; unix-history-repo imports tape bytes verbatim) with a tree-level RECONSTRUCTION caveat for
reassembled git histories; ASK 3 accepted (Tierra's license.h read: free copying, no commercial use, modified versions
documented; landings next pass, POET first); ASK 4 accepted + TECHNE-109 (backfill); ASK 5 accepted with ETags. Techne also
notes the M1 instance saw #358 and never answered (shut down) -- its late ACK is recorded as Techne's.

Applied: nyx/atlas/migrate_v1.py grade_from_record returns CONTEMPORARY_COPY for source_type HISTORICAL_ARCHIVE_MIRROR and
grade_basis_from_record writes the ruling's citation and caveat into every such cut; the 13 cut fossils with that source
type re-saved (grades UNKNOWN -> CONTEMPORARY_COPY); validator 121/0; 23 tests. The ruling named three records; I applied
the reasoning to all 22 archive-mirror records and said so in the reply, offering to narrow it if that overreaches. The
STATUS defect line "the Techne-grade map does not translate HISTORICAL_ARCHIVE_MIRROR" is closed. Reply posted with two
record notes for Techne's next pass (paip is >= 11 mechanism families; go-explore holds policy_based/ as a second body)
and the pari-gp SSL fetch defect.
