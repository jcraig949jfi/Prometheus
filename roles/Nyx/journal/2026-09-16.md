# Nyx journal 2026-09-16 -- instance m2-0c0adfe1

## Boot (11:45Z)

Machine M2 (SPECTREX5). Canonical checkout D:\Prometheus: `git fetch origin` (no change), origin/main =
ccb26df01. Worktree D:\Prometheus-worktrees\nyx-m2-0c0adfe1-boot-2026-09-16, branch
nyx/m2-0c0adfe1-boot-2026-09-16, created from that SHA (43,352 files, ~2 min, unbounded timeout); guard passes
(git-dir != common-dir). No work in the canonical checkout.

`python -m comms instance` REFUSED (WRONG_ENVIRONMENT, signature c84e26826cc12217: M2's local Postgres is the
quarantined fork) until EW_DB_HOST=192.168.1.202 was set -- the known class in roles/Hermes/incidents/; not a new
report. `python -m comms boot Nyx --model claude-opus-5 --capabilities nyx` -> Nyx[m2-0c0adfe1]. `sync`: 0 new,
queue #235 (atlas charter) #241 (THEO-REQ-003, not Nyx's). No NYX-44 ruling has arrived. 67 commits on main since
1736cad05, none under nyx/ or roles/Nyx/. MONITORS.md: nothing Nyx owns or feeds. Charter manifest 2/2 verified;
atlas MANIFEST verified 0 entries (it is a prose manifest, not comms.manifest format -- the sha256 in its text matches
the 09-15 journal's record; not re-hashed today).

Suggested work (s1.6): (1) materialise Stage A bodies on M2; (2) Stage A on the 25 remaining sampled fossils; (3)
build.py + first-pass return; (4) c04/N4 held pending ruling; (5) path portability of new cut scripts. Started (1).

## Bodies on M2 (11:55Z)

The fossil vault is host-local (Techne README); M2 had none. `python -m techne.fossils.harvest acquire` for all 30
sampled fossils into TECHNE_FOSSIL_VAULT=D:\Prometheus-vault\fossils (out of tree; the default would be under the
canonical checkout, forbidden by s1). acquire rewrites Techne's tracked record.json / UPSTREAM_HASHES.txt; each was
reverted with `git checkout -- techne/fossils/specimens/<id>` + `git clean -fdq` after the fetch, then
harvest.drift() run against the COMMITTED record. Result: 30/30 fetched, 28 match, 2 differ. libfec-karn: 127/127
files match after LF->CRLF (Techne's hash is of a CRLF checkout; M2's clone is LF; system gitconfig core.autocrlf=true
on both). corewar-redcode: 31 exact, 15 match after LF->CRLF, 4 MISSING = tree/**/__pycache__/*.pyc (build products
hashed inside upstream/ on M1). Receipt: nyx/atlas/samples/stageA_bodies_m2_2026-09-16.json. `git status techne/`
empty after. Reported to Techne in the first-pass return, section 9a. Not repaired by Nyx.

## Stage A (12:00Z-12:30Z)

25 cut scripts written and run (nyx/atlas/cuts/<id>.py -> nyx/atlas/fossils/<id>.json), in sample order:
linux-tcp-congestion (15 organs; two systems in one record), minisom (7), libfec-karn (14), dmtcp-3.1.2 (10),
lmdb-0.9.31 (8), verilog-rr-arbiter (5, DEEP), fftpack-netlib (5), minisat-1.14-2006 (9), padasip-1.2.2 (10),
radamsa-0.6 (10), corewar-redcode (6), erfa-2.0.1 (7), ssw-smith-waterman (7), cobol-programming-course-omp (6),
tinycc (7), gzip-1.2.4-1993 (7), concurrencykit-0.7.2 (7), hopfield-takyamamoto (4, DEEP), python-control (6),
eliza-anthay-1966 (6), tiny-aes-c (6, DEEP), leveldb-1.23 (7), sqlite-3.49.1-amalgamation (7,
CUT_INSTRUMENT_INSUFFICIENT), hashicorp-memberlist-0.5.4 (6), reed-solomon-rockliff-1991 (7, DEEP). Evidence refs
in new scripts are host-independent (vault:<id>/upstream/...); the five 09-13 scripts still carry F:/ paths
(unchanged today; s9 defect noted, not chased). author.show() now resolves the body via techne.fossils.vault.body_dir
instead of parsing an F:\ string out of the record.

Nothing ran. Every organ is SOURCE_READ except 4 METADATA (padasip preprocess/detection, ck hazard pointers) and the
3 prior EXECUTED/INTERVENED. Validator: 121 files, 0 problems, after every save.

## Census projection defect (12:36Z)

Techne's `superseded` lineage relation has no fixed direction across records (17 edges read; both readings present).
The census had projected it into KNOWN_SUCCESSORS as if directed. census.py now labels it "direction UNFIXED ...;
note: <the record's note>". Re-run: 121 refreshed, cuts untouched (idempotent for whole_system). Reported to Techne
(return 9b).

## Artifacts and return (12:35Z-12:50Z)

nyx/atlas/build.py assembles the eleven directive artifacts into nyx/atlas/out/ (every row a projection; counts
computed). nyx/atlas/recurrence/stageA_reading_candidates_2026-09-16.json: 15 candidates (4 R0, 8 R1, 3 R2, 0 R3+),
5 test sets, 2 calibration pairs. DEPTH_MAP: 30/121 inspected, 224 fragments, 192 accepted, 121 rejected, 69
pressures, 298 composition edges, 0 fingerprints, 0 blind cuts, 0 measured coverage cells.
nyx/atlas/FIRST_PASS_RETURN_2026-09-16.md: the ten sections, pure ASCII, 80 columns. Delivery body under
roles/Nyx/prompts/2026-09-16_atlas_first_pass_return/ with MANIFEST; posted to Techne (id in DELIVERIES below).

Own defect found while building: 12 of 69 pressure cost_class fields are free text (the charter's vocabulary is
CPU-scale / model-inference / unknown); 8 are in today's scripts, 4 in the 09-13 ones. Not rewritten today; listed
in the return; backlog NYX-47.

## Not done

c04 / N4 (held; no ruling). Stage C (docker/WSL presence on M2 not checked). Blind cuts (0). The 91 NOT_CUT fossils.
The 09-13 cut scripts' F:/ paths. Techne's records (not Nyx's to edit).

## Delivery

comms #296 (report, to Techne) 12:58Z: the first-pass return; DELIVERIES row in nyx/atlas/DELIVERIES.md. Techne[m2-04bd52c0] booted today (e6fab5e29) but was OFFLINE at post time (last active 07:56Z per `comms who`, 30-min window): the report is queued for its next sync. Vivarium and Archaeon online on M2.

## Push receipt

d8472ef90 pushed to origin/main after two explicit merges (de6adc493, efce13853); `git merge-base --is-ancestor` verified. Base-role self-test on M2: 1 RED (VivariumConsumerM2 registry row), already reported by Herakles in de6adc493; nyx/tests 21 passed; validator 121/0.

## Commits (this worktree)

a98614f92 boot + bodies + linux-tcp; 9119205ef 8 cuts; (cobol batch); (hopfield batch); (aes batch); 051b4464f
leveldb/sqlite/memberlist/rs; then census fix + build + recurrence + return + journal + STATUS (this commit). SHAs of
the middle commits are in `git log`; the push receipt is at the end of this file.

## Mechanism Archaeology Pipeline (13:10Z-14:20Z)

Operator directives received in chat: Founding Charter, Amendment 2, Amendment 3 (Amendment 1 NOT received). Transcribed
verbatim under roles/Nyx/prompts/2026-09-16_mechanism_archaeology_pipeline/ with MANIFEST. Nyx commented on each in
chat (points: symmetric returns, prediction schema, world normalisation, intervention levels, preservation gate,
MKW-1 interim readout); Amendment 3 adopted all of them and authorised N1-N4.

Corrected in chat first: "M2 has no docker" was WRONG -- Docker Engine runs inside WSL Ubuntu-24.04 under systemd with
Techne's images present (prometheus-fossil-c:bookworm, gcc:4.9, ...); my check had looked for a Windows-side binary.
tunnel.py hardcodes distro "Ubuntu" (here "Ubuntu-24.04"); Techne's harvest uses the default distro and is fine.

N2: nyx/atlas/predictions/schema.py (schema nyx.prediction_packet/1; canonical bytes; freeze refuses a changed packet)
and MECH-GZIP-LEVELTABLE-001.json, frozen sha256 861dded070d138e35c56d5cc05cf76de7053117c3aba24aa51e350d8ebe10619.
While writing it: grep for `level` in the fossil found THREE consumers, not one -- deflate.c:667 (compr_level <= 3 ->
deflate_fast) and trees.c:987 (flush heuristic gated on level > 2) besides the table. The cut's sentence "no other code
changes with the level" was wrong; annotated in the organ record and the cut script (original text kept; script marked
DO NOT RE-RUN). The packet's decisive intervention I2 flattens the table AND forces both switches; CUT_KILL is a
surviving level difference > 0.002 ratio with both controls passing. Predictions carry direction + bands; controls:
pre-compressed cheat (ratio >= 0.99 everywhere), positive (level 1 vs 9 differ >= 0.01 ratio and in time).
N3: nyx/atlas/calibration/RS_CALIBRATION_PAIR_001.json (Rockliff -> Karn; R-ID-1/2 identical, R-DIV-1 divergent).
N4: nyx/atlas/migrate_v1.py + author.Cut.save hook + validator: cut.provenance (grade, source object, payload manifest,
per-file payload hashes read on this host vs Techne's recorded). 30 cuts: 98 files hashed, 87 match, 10 mismatch (the
known CRLF class), 12 refs unresolved (receipts/prose). 8 fossils grade UNKNOWN (Techne's handoff string lacks the
marker). Re-running hopfield's cut script proved a fresh cut populates it. Validator now REQUIRES it: 121/0.
N1: RESPONSIBILITIES s5a; STATUS rewritten; gate ledger nyx/atlas/gates/LEDGER.json reported by build.py
(cuts_created 30 / cuts_returned_with_verdict 0 / handoffs open 4).
Deliveries: #309 to Harmonia (packet + pair; delegation; R31 return required), #310 to Techne (R39 identities).
Techne offline at 12:58Z; Harmonia online (2 instances on SFE work) -- lane currency under the pipeline not established.

Not done: Stage A/B on the 91 NOT_CUT (next); NYX-47/48; tunnel.py distro name (moot under R32 unless a SCOUT run).

## Returns assimilated (17:25Z-17:45Z, Stage D')

Closing sync brought #302 and #308 from Techne (both sent BEFORE my #310, read after). #308: FOSSIL_PACKET.json for
gzip exists (VALID; BODY_EXECUTABLE; FOSSIL_WORLD_ID fw-01f8b51f...; RUNTIME_WITNESS M2 5d33974496e0; scaffolding = build
flags only), with CUT_ID / NYX_PREDICTION_PACKET null for me. Techne graded the tarball CONTEMPORARY_COPY, not
ORIGINAL_ARTIFACT -> packet 001 is wrong on that field; 002 written with supersedes = 001 (grade + Techne ids only;
predictions untouched), frozen sha256 a30f3348d198c12471d2bad3bc847b41e3092b0c93fb2c751ef0a30080c821f8. Both keys
filled in Techne's packet at Techne's request; Techne's validator VALID. migrate_v1 now prefers a FOSSIL_PACKET grade.
TECHNE-80 (47e13ef65) re-pinned the 11 CRLF records: my 30 cuts now hash 97/98 byte-exact (was 87/98); 9a closed.
Typed return to Techne #312 (ACCEPT on #308; #310 closed by #308; Amendment 3 R38 flagged: preservation = M1 + M2 +
origin does not satisfy R25 per Amendment 3; PRESERVATION_STATUS still null; operator decides TECHNE-65). `comms done 308`.
Gate ledger: first return received (from Techne); cuts_returned_with_verdict still 0 (that needs Harmonia).

## Harmonia's first typed returns (18:00Z-18:20Z, Stage D')

Harmonia[m2-038758c6] #316 ACK / #317 DISPOSITION, both at tick 1 (lane currency established: RESPONSIBILITIES s8,
HARM-37..43). RETURN 1: RS_CALIBRATION_PAIR_001 CUT_SUPPORTED -- ruler EXECUTED in fw-01f8b51f, 2000 trials/e,
R-ID-1 0/2000 mismatches, R-ID-2 8000/8000, R-DIV-1 divergent 1865/2000, controls clean; plus a finding my table did
not predict (33/2000 different words at e=4). RETURN 2: MECH-GZIP-LEVELTABLE-002 PREDICTION_PACKET_CHALLENGE -- the
switch is deflate.c:672, not 667. Verified by me on the bytes (sed -n 672p; 667 is match_length; lm_init ends 342;
trees.c 897/909/927 under #ifdef FORCE_METHOD only). Cause: I took line numbers from grep-filtered output. Packet 003
frozen 5dbf46a208ed3a99441529246ca2fcfd53eb31f1f7ad856159d549ebd918891a (672 everywhere; FORCE_METHOD row + INDETERMINATE
precondition; symbol_end_line 342); 001/002 immutable; cut record and script annotated a second time; two calibration
ledger rows; Techne's packet key -> 003 (validator VALID). Responses #319 (Harmonia), #320 (Techne). R1 opens on 003.
This is the pipeline's first typed challenge Nyx -> assimilated, on evidence bound to bytes; per Harmonia it is not yet
the amendment's "first required return" (that needs an intervention effect outside the boundary, at R2).

## Vocabulary follow-through (18:40Z)

Merging main for the push brought Techne 1bb9965b4 (superseded -> superseded_by / supersedes, migrated edge by edge:
my 9b, closed) and dd9a395b3 (R22 capability matrix as world data). The merge conflicted on FOSSIL_PACKET.json (my key
-> 003 vs Techne's matrix rewrite); resolved on Techne's version with my key re-applied; Techne validator VALID. The
push went out with my validator RED (17 fossils carrying the retired 'superseded' relation) -- pushed before checking;
recorded here as a defect of this pass. Fixed in the following commit: census re-run (TECHNE_RECORD edges refreshed),
PRED includes 'supersedes', KNOWN_SUCCESSORS = superseded_by, three SOURCE_READ edges migrated with dated notes
(gzip, tiny-aes -> supersedes; leveldb -> superseded_by). Validator 121/0; ancestry graph: superseded_by 13,
supersedes 6, superseded 0.
