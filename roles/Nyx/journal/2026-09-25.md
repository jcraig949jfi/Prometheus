# Nyx journal -- 2026-09-25 (instance gandalf-9e21f277, M3/GANDALF)

## Pre-reboot save
Operator called a restart with a context reset and asked for a resume record, a commit, a push, and
any branches merged. The seat had been down since 2026-09-19; origin/main had advanced 2965 commits.

State found on waking: branch nyx/boot-2026-09-17 clean apart from twelve nyx/atlas/out/*.json whose
only diff was the build timestamp (10:08Z -> 10:09Z) and the resulting artifact hashes -- rebuild
noise from a duplicate build, discarded. Nothing of mine was unmerged; the branch fast-forwarded to
origin/main. Twenty new comms messages, eighteen after my last post.

MY DEFECT, six days live on origin/main. Four seats (Atlas #497 and #557, Crius #516, Aether #524)
reported the fleet base-role self-test RED on a Nyx prompt manifest. Cause: on 09-19 I amended
PLAN_behavioral_cuts_after_replication.md with the two-channel split and the 9.2 correction, and
never regenerated roles/Nyx/prompts/2026-09-19_asal_pipeline_direction/MANIFEST.md. Verified the
mismatch (manifest dc41abff1393 != artifact dbd639ef6ee7), regenerated it, and swept every Nyx
prompt directory: all verify 0 mismatches. Recorded as the first trap in the resume record, because
the failure mode is invisible from inside this seat -- the check that catches it runs elsewhere.

Pipeline state, unchanged in substance while I was down: no POET ruling on
MECH-POET-NOVELTY-ESTIMATOR-001 (291a22ed re-derives exactly; still the one open packet) and no
HARM-56 disposition, so every hold stands. HARM-55 is blocked only on execution placement -- Techne's
harness is complete and gained an --rgb224-dir anchor mode (#496), the original-observer column is
bit-identical over all 395, and the Flax run is delegated to the M2 Harmonia instance m2-ca1148a0 on
an AVX host (#485). HARM-56's contract is frozen before any native score, with disposition A/B/C by
written rules; my mechanism ledger already speaks that vocabulary so the answer drops straight into
MECH-ASAL-OE-SCORE.observer_dependence.

Debt I am carrying across the reboot, and should not have: Ares sent three messages asking this seat
to interpret the W4 hidden-regime fossil (#518 cycle 0, #535 cycle 1 at ten lineages, #538 cycle-2
export) and I answered none of them in six days. Ares is parked and says explicitly it is not
waiting, which makes it non-blocking but not excusable -- interpreting a fossil mechanism, judging
atlas match or novelty, and finding the smallest sufficient sub-circuit is exactly this seat's job.
The ask is worth doing properly rather than quickly: the mechanism replicates as a FUNCTION across
ten lineages but is idiosyncratic as a STRUCTURE (the GATE+MAX pair was rediscovered zero times in
seven independent runs; the carrier is a self-loop in 6/10 and a longer recurrent cycle in 4/10), and
Ares says a ten-lineage comparison would be more useful than a reading of seed 3 alone. Captured in
full in the resume record so the next session can start on it cold.

Also noted: Techne's lineage-relation key drift has GROWN, not been resolved -- 80 relations now use
"target" against 112 using "to" across 168 specimens (it was 46 vs 112 when I reported it in #494).
My census reader tolerance (_rel_to) is therefore load-bearing and must not be removed. Reported
again in the pre-reboot notice.

Wrote roles/Nyx/RESUME_2026-09-25.md: boot commands and env, frozen-artifact hashes, the scoreboard
and mechanism ledger, what is in flight on other seats, the holds, my debts, the traps that have
actually bitten this seat, open questions I raised and never had answered (is a confirmatory packet
admissible; is "a mechanism == a cut organ" the same mistake one level down), next actions in order,
and a file map. Added a branch note: do NOT merge the local harm55-frames-transfer orphan, which
carries ~52 MB of frames for transfer to an AVX host. Banner added to STATUS.md pointing at it, and
the auto-memory index updated so the pointer survives the context reset.

Verified at close: HEAD == origin/main == a9301f23c, nothing unmerged, tree clean, 123 fossils
validate 0 problems, 6 mechanisms validate, probe battery 53f63df5 frozen and intact, 45 tests pass.
Fleet notified (#572).


## Post-reboot session (instance gandalf-226cd218)

Booted per the resume record: fetch + explicit merge (already up to date), EW_DB_HOST set,
comms sync 0 new after #571, inbox max id 571. No POET ruling, no HARM-56 disposition, so every
hold in section 4 of the resume record stands. Validators green on first run: 123 fossils 0
problems, 6 mechanisms 0 problems, probe battery 53f63df5 intact, scoreboard unchanged, 45 tests.
The tree was dirty after the tests, as the resume record warned: twelve out/*.json with only the
built timestamp, the derived hashes, and handoffs_open_max_age_ticks 3 -> 9 (the open POET packet
aging). Restored with git checkout. The heredoc trap bit once more (an apostrophe inside a
docstring silently killed the whole command); the file tool was used for every script after that.

Then the largest debt: the Ares W4 reading. Method, in the order Ares asked for it: describe by
replay before naming; reduce by substitution and edge cuts; search the atlas; hand-build the
cheapest equivalent; and run one instrument over all ten cycle-1 lineages. The first thing the
edge list showed was that the record was wrong at the level of topology: there is no edge
7 -> 13, so the seed-3 carrier is a three-node ring 13 -> 7 -> 15 -> 13 that passes THROUGH
output node 15, not a GATE<->MAX two-cycle. Node ablation never removes output nodes, which is
the blind spot Ares had already named in cycle 2, and it hid one third of the ring.

Replay: ring at (8, ~7.5, 8) for regime 0 from step 2 on; at (-1.56, -0.76, ~-2.7) for regime 1
from step 0. Loop gain 3.92 per tick, bistable through the +-8 clip. Both GATE ops have their
condition port unwired (0 > b, always true): they are identities. The MAX with one port unwired is
max(x, 0) - 0.76, a rectifier that fixes the LOW floor; MAX -> ADD still scores 40.0, so it is
dispensable. Removing hidden nodes 8, 9, 10 together: 40.0. Bypassing 7 with one edge: 40.0.
Collapsing the whole ring to a single self-loop on output 15: 40.0. A hand-built two-edge
organism (output self-loop >= 1.5, cue edge) reaches the cap at 15 of 15 grid points, and
lineage 9 is exactly that circuit, so the GA did not miss it; lineage 3 accreted neutral nodes
(duplicate_node made 10 from 8 at generation 15) with no parsimony pressure to remove them.

Synthetic drive on the ring: ignition needs a 3-step cue of at least -0.5 from the zero initial
state (the world gives -1.0 +- 0.2); a reset needs +3, which the world never produces; a -1 cue
at steps 20-22 from the settled LOW state does NOT ignite; a one-step cue leaves the ring in a
period-3-tick rotating orbit with actions 2,1,1,2,1,1. So the "first three steps" sensitivity is
an artefact of the substrate starting every lifetime at zero.

Ten lineages, same instrument (SCC inventory from ares/carriers.py plus a latch test: state at
t=39 per regime, drift after t=10, at-clip flag): 10/10 hold the bit as the sign of a saturated
positive-feedback loop with per-step gain above 1; 8/10 have the output node inside the loop and
read the loop state directly as the action; the sign of the cue weight is fixed by which output
the loop latches (8/8 traced); the ops around the loop are identities-with-bias and carry no
information about the mechanism (the one non-identity, MUL in seed 4, takes its gain from the
constant channel). A substrate quirk: with two ticks per step a negative self-loop (seed 1,
-1.42) is a positive one at step boundaries. Cutting every recurrent edge floors 10/10.

Two predictions written before the run, both held. (a) The loop-gain basin measured on the
EVOLVED seed-3 ring is viable from s = 0.8 up to a gain of 106: wide, flat, open-ended, which was
the measurement Ares had only on hand-wired carriers. (b) All ten W4 champions score at the floor
on W16 (variable-delay cue), present indistinguishable from shuffled, because each loop reaches
a default clip state within ten steps and a cue of +-1 cannot flip a saturated loop (verified
after the run by driving seeds 1, 4, 9 with late cues: nothing moves).

Atlas: novel as an organ. The nearest registered organs are gain-1 saturating accumulators
(arduino-pid clamped integral, biriscv 2-bit counter) and picorv32's clocked latch of instruction
flags; none is a regenerative loop with gain above 1. Known as an engineering motif outside the
atlas (the seed-6 form is literally two cross-coupled output nodes). Name offered only after the
description: a saturating positive-feedback latch, set by the cue, read at the output. I cannot
register it: Stage A is frozen and the Ares organisms are not Techne specimens.

Apparatus committed as nyx/readings/ares_w4_reading.py (reads ares/ only, ~2 minutes, every
number in the reading re-derives). Reading posted to Ares and Harmonia as a reply to #535.

Not done today: THEO-REQ-003, the UNKNOWN-grade ask to Techne, the Avida ancestry cut,
poet-enhanced-2020. Next in order: Avida ancestry cut (A5 definedness fixture first), then
THEO-REQ-003.
