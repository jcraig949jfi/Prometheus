# Metis backlog (schema: roles/Archaeon/prompts/2026-09-10_backlog/00_BACKLOG_SCHEMA.md)

Currency: 2026-09-11, second revision (re-premised by operator ruling
the same day). SEASON 1 rows are first and are what the seat starts the
moment the season prompt lands. The seating-pass rows below them are
kept visible with their disposition marked; superseded rows are
annotated, never deleted.

Format: ID | item | lane | milestone | size | blocked_on | evidence of done

## Season 1 -- composition (rows added 2026-09-11 on the ruling)

METIS-S1-01 | Get the operator's disambiguation of "Erebos transport": the transport protocol (Phase-0 handshake/WebSocket) or "Erebos composition" (0 signal passes survive nulls, 1 infra pass stands), which is the thread that fits a composition season | EVIDENCE | alpha | S | operator, one line | the answer recorded in roles/Metis/ledgers/EPISODE_PRECONDITION_2026-09-11.md and the episode set frozen
METIS-S1-02 | Freeze the episode set and its train/test split BEFORE any rule is written, in its own commit so the order is in git history | EVIDENCE | alpha | S | METIS-S1-01 | a preregistration commit that PRECEDES every specimen commit, naming which episodes fit and which test
METIS-S1-03 | Reconstruct each episode's pre-decision evidence state from COMMIT DATES, not document contents, since every surviving record was authored with the outcome known | EVIDENCE | alpha | M | METIS-S1-02 | one committed evidence-state file per episode, each row carrying the commit SHA and date that proves the evidence existed before the decision
METIS-S1-04 | Build the decision specimen: evidence in, ranked experiment candidates with explicit reasons and vetoes out, deterministic, no LLM in the decision path | ENGINE | alpha | M | METIS-S1-03 | the executable committed with its replay harness and its three controls (negative, positive, cheat)
METIS-S1-05 | Replay the specimen against the held-out episodes and report the failure shape, including the case where it does worse than the best single channel | EVIDENCE | alpha | M | METIS-S1-04 | the replay rows committed with the verdict, and the per-channel baseline beside the composed one
METIS-S1-06 | Build the correlated-evidence control on greedy-LoRA specifically: show whether a rule that counts agreeing channels scores that episode MORE confident the more wrong it is | EVIDENCE | alpha | M | METIS-S1-04 | the measured confidence-vs-correctness curve for that episode, committed with rows
METIS-S1-07 | Build the instrument-error control on Apollo specifically: show whether the rule treats the April llm_alive=0 artifact as an early true positive | EVIDENCE | alpha | S | METIS-S1-04 | the rule's output on the April evidence state, with the artifact's fix commit (2026-05-22) named
METIS-S1-08 | Extract the VETO primitives as the season's durable output, separately from any ranking | EVIDENCE | beta | M | METIS-S1-05 | a committed veto list, each veto naming the episode that motivates it and the observation that would retire it
METIS-S1-09 | Report the two design risks to the operator BEFORE building: five episodes cannot both fit and test a rule; outcome-aware records bias all five the same way | EVIDENCE | alpha | S | none | the risks stated in the season receipt and either accepted, bounded, or the set widened
METIS-S1-10 | Declare the conflict of interest in writing: this seat proposes the rule, builds the specimen, and scores it | EVIDENCE | alpha | S | none | the declaration in calibration/CALIBRATION.md and an independent attacker named (Kairos, Charon or Nemesis) before the verdict ships

## Seating-pass rows (dispositions marked 2026-09-11)

METIS-01 [RULED: NO -- re-routed as METIS-01R, the loop still needs an owner and it is not this seat] | Decide whether Metis claims the unowned portfolio-brief producer chain (scripts/metis_portfolio.py + the intelligence_loop hop that fires it), which has been dead since 2026-09-09T02:15Z with two declared consumers waiting | EVIDENCE | program | XL | operator decision NEW: does the Metis seat own the dashboard brief's CONTENT, given Hermes owns delivery and explicitly declines the content (HERMES-XL-2) and MONITORS.md row 27 reads OWNER UNCLAIMED | a decision row in archaeon/docs/expansion/DECISIONS.md and an owner name in MONITORS.md rows 26-27
METIS-02 [RULED: RE-PREMISED to composition] | Decide whether the seat is re-premised around the MEASURED/CHANGED/STALE/ABSENT/REFUSED typing rule in RESPONSIBILITIES.md, or stays PARKED with its residue archived | EVIDENCE | program | XL | operator decision NEW: re-premise or park, on the evidence in ARCHAEOLOGY_2026-09-11.md | an operator ruling committed under roles/Metis/prompts/ and the seat state changed in STATUS.md
METIS-03 | Determine which code actually produced docs/state.json for the last eight days, given no tracked file emits its "observability" block and the producing host is recorded only as "M3 or M4" | ENGINE | alpha | M | host access to M3/M4, which this seat does not have; routed to the operator and to whoever holds those hosts | a committed note naming the producing file and host with the command that read it, or an explicit UNLOCATED verdict with the hosts that were checked
METIS-04 | Decide the owner of scripts/intelligence_loop.py (the Pronoia-lineage orchestrator that fires the producer, the brief and the mailer) | ENGINE | program | XL | operator decision NEW: PipelineOrchestrator lineage code with no live seat; Hermes owns one hop, Metis another, nobody owns the loop | an owner in MONITORS.md and a pinned-worktree receipt per WORKING_CONTRACT.md s6, or an explicit RETIRED annotation
METIS-05 | Send Eos and Archaeon the measured counterexample showing byte-identity is an insufficient no-op detector one hop downstream of an LLM (8 of 8 briefs byte-distinct, same three actions in six consecutive briefs) | EVIDENCE | alpha | S | none | a committed prompt under roles/Metis/prompts/ with its MANIFEST, posted via comms, and Eos's or Archaeon's reply recorded
METIS-06 | Report to Archaeon that the "pull the latest first" wake directive has now produced the same WORKING_CONTRACT s1+s3 violation twice in one day (Atalanta L-09, then this seat), making it a constitution defect rather than two seats' carelessness | EVIDENCE | alpha | S | none | a committed report under roles/Metis/prompts/ posted to Archaeon, and either a reworded directive at its source or a recorded refusal
METIS-07 | Re-premise or retire docs/manual_status.json as an authoritative override: it is 116 days stale, has no freshness gate, and the producer's own prompt tells the model to trust it as ground truth | ENGINE | alpha | S | METIS-01 (do not repair code the seat may not own) | a committed decision plus, if kept, a freshness gate with a test that fails when the file is older than its window
METIS-08 [DEMOTED: house rule, not charter; the season is not brief production] | Build the claim-set no-op detector: every brief item typed NEW / UNCHANGED-SINCE <date> / RESOLVED, with "unchanged for N days" printed instead of the item | ENGINE | alpha | M | METIS-01, METIS-02 | the detector committed with its three controls (negative, positive, cheat) and the controls' rows, replayed against the 8 archived analyst briefs to show it would have fired on days 2..6
METIS-09 | Replace every optimistic absent-key fallback in the seat's code with fail-loud or ABSENT, and add a schema-version check that refuses an unrecognised input schema | ENGINE | alpha | S | METIS-01 | the committed diff plus a regression test that feeds a schema_version 3 state.json with no infra_status and asserts the brief prints ABSENT, not "(state.json reports up)"
METIS-10 | Put an input-freshness header on every brief: each input, its generated_at, its age at brief time, and STALE in the title when all inputs exceed their windows | ENGINE | alpha | S | METIS-01 | a generated brief carrying the header, and a test that a brief built from the 2026-09-09 state.json is titled STALE
METIS-11 | Put the eligible count beside the printed count on every brief ("3 of 47 printed, 44 suppressed by <named rule>") | ENGINE | alpha | S | METIS-01 | a generated brief carrying both counts and the named suppression rule
METIS-12 | Measure whether any brief has ever changed a decision -- the seat's own productivity signal under base rule 8 -- by asking the one human reader and by checking whether any committed artifact cites a brief | EVIDENCE | alpha | M | none, but the answer may be zero and that is the result | a committed ledger of brief -> decision links with its eligible count, or an explicit ZERO with the search that produced it
METIS-13 | Register every Metis-lineage loop in MONITORS.md with its input, freshness source, dormancy threshold, alarm route and productivity signal | EVIDENCE | alpha | S | none | the rows committed in roles/base-role/MONITORS.md (DONE on the seating pass; this row closes when a seat owns them)
METIS-14 [HELD by operator ruling until the season establishes whether this seat has an instrument worth exposing] | Answer Talos's TALOS-10 all-seats consumption-contract question; the provisional answer is NONE, because a corpus of 24,847 Python functions is not an input to a state-compression instrument | LIT | alpha | S | none; queued behind the seating pass by the operator's scope limit | a committed reply under roles/Metis/prompts/ posted to Talos via comms, recorded in Talos's CONSUMER_SEARCH ledger
METIS-15 | Decide the fate of agents/metis/ (the March analyst): retire the code, keep the briefs as archaeological material, or re-premise the Eos-digest hop | LIT | program | XL | operator decision NEW, and Eos's own re-premise ruling (EOS-01 lineage) | an annotation on agents/metis/README.md and a state in STATUS.md, with the briefs preserved either way
METIS-16 | Archive the dashboard brief history (docs/briefs/portfolio_brief_*.md) with an LF manifest, the way Eos archived its digests, so the reporter's residue survives a host change | EVIDENCE | alpha | S | the history lives on the producing host, which is METIS-03 | the archive directory committed under roles/Metis/archive/ with a verified MANIFEST
METIS-17 | Replay the claim-set detector (METIS-08) against the 2026-09 dashboard briefs to measure how many of them were reworded repeats | EVIDENCE | beta | M | METIS-08, METIS-16 | a committed count with its eligible count, and the raw per-brief rows
METIS-18 | Route the prometheus_llm migration for any surviving LLM hop in this seat's code, so no hand-rolled provider cascade remains | TOOLS | alpha | S | METIS-01, METIS-15 | the committed diff showing the cascade removed and prometheus_llm called, with a test
METIS-19 | Write the three controls (negative, positive, cheat) for whatever brief generator survives, and publish the pre-registered prediction before running them | EVIDENCE | beta | M | METIS-01, METIS-02 | a preregistration commit that PRECEDES the results commit in git history, plus the three controls' rows
METIS-20 | Define the brief's own dormancy alarm: who is told, and how, when a brief has not been produced for two cycles -- the failure that went unnoticed for 61 hours | ENGINE | alpha | S | METIS-01 | the alarm committed with its route, and a test that fires it against a 61-hour-old brief
METIS-21 [MOOT: the shape does not survive; the season's deliverable is an executable specimen, not a brief] | Decide whether the Act / Watch / Record shape survives the re-premise, or whether the typing rule replaces it | EVIDENCE | beta | S | METIS-02 | the decision recorded in RESPONSIBILITIES.md with the reason
METIS-22 | Ask Elenchus whether the shadow-review block in the dashboard brief is still the surface it wants its reviews read on, given the producer is dead and its own file still names it | EVIDENCE | alpha | S | none | a committed question under roles/Metis/prompts/, posted, and Elenchus's reply recorded

METIS-01R | Route the dead, unowned portfolio-brief producer chain to an owner or an explicit RETIRED annotation, now that Metis has been ruled out of that lane | ENGINE | program | XL | operator decision + Archaeon; NOT Metis's to write | an owner named in MONITORS.md rows 26-27 or a RETIRED annotation, either way with the two declared consumers (Hermes mailer, Elenchus shadow-review surface) told
METIS-23 | Add a staleness gate and an incoherence gate to metis.py's context loader so a 163-day-old self-contradicting Skopos report cannot enter brief context undated (Skopos defect 01, comms #87; accepted, not fixed) | ENGINE | alpha | S | METIS-15 (the March analyst's fate is undecided; patching a dormant pipeline is resurrection wearing a bug-fix costume) | the committed diff plus a test that feeds a 163-day-old report and asserts the age is stated in the context block or the source is dropped as ABSENT
