# Operator ruling, 2026-09-11 -- Metis re-premised; first active season set

The operator's message, verbatim, following the seating receipt. The only
edits are the ones NORTH_STAR.md established as precedent: em-dashes and
curly quotes replaced by ASCII equivalents so the file passes the base
role's pure-ASCII rule. No word is changed, added or removed.

    Metis is seated cleanly. More importantly, she resisted the
    temptation to confuse adoption with resurrection: no old Metis
    machinery ran, no brief was emitted, and no inherited queue item
    silently became a mandate. That is exactly the boundary we wanted.

    Her archaeological role is interesting because old Metis sat in the
    original Pronoia chain between Skopos and Clymene. I would not
    revive that position. The useful thing to recover is Metis as
    synthesis under constraint: taking evidence produced by other seats
    and deciding what combination of observations warrants an
    actionable inference - without becoming another orchestrator,
    judge, or daily-report generator.

    I'd give her a first active season aimed directly at the current
    Prometheus problem:

    Can Prometheus combine heterogeneous failure evidence into a better
    next experiment than any individual evidence channel would select?

    That gives Metis a sharp distinction from the other seats awakening
    today. Eos controls intake; Skopos interrogates
    objectives/measurement; Clymene preserves endangered external
    substrate; Atalanta demonstrated why consumer/producer assumptions
    need explicit contracts; Hermes is working identity/provenance;
    Nemesis attacks claims. Metis should study composition.

    The first season should therefore not be "generate a research
    brief." Give her perhaps 3-5 already-dead Prometheus episodes -
    Geometry-1, Saxl, Erebos transport, greedy-LoRA, Apollo's failed
    lift - and make her reconstruct, from surviving evidence only:

    1. what evidence existed before each next experiment was chosen;
    2. which signals actually predicted the eventual failure;
    3. which signals were available but ignored;
    4. whether a deterministic composition rule could have selected a
       cheaper/more discriminating next experiment;
    5. and crucially, where composition merely creates false confidence
       from correlated evidence.

    The deliverable shouldn't be prose wisdom. It should be a tiny
    executable Metis decision specimen: evidence-in -> ranked experiment
    candidates + explicit reasons/vetoes -> replay against historical
    decisions. No LLM judging correctness. Historical outcome supplies
    the falsification.

    And I like that she held TALOS-10 as METIS-14 with provisional NONE.
    Keep that posture until this season establishes whether Metis
    actually has an instrument worth exposing to other seats.

    If you want to launch her now, I can write the full METIS FIRST
    ACTIVE SEASON prompt in the same hard-edged operator style we've
    been using for Eos/Atalanta/Nemesis.

## What this ruling decides, and what it does not

DECIDED -- METIS-02 (re-premise or park): RE-PREMISED. The lane is
COMPOSITION: whether heterogeneous failure evidence combines into a
better next experiment than any single channel would select. The
MEASURED/CHANGED/STALE/ABSENT/REFUSED typing rule from the seating pass
is NOT the season's subject; it is retained as internal hygiene for
anything this seat writes, demoted from charter to house rule.

DECIDED -- METIS-01 (does Metis own the dashboard brief's content):
NO, by direct implication. "Without becoming another orchestrator,
judge, or daily-report generator" and "should therefore not be
'generate a research brief'" rule the daily-report lane out of this
seat. Metis does not claim scripts/metis_portfolio.py.
THIS DOES NOT MAKE THE LOOP LESS DEAD. The producer is still stopped
since 2026-09-09T02:15:15Z, still OWNER UNCLAIMED, and still has two
declared consumers (the Hermes mailer; Elenchus's shadow-review
surface). Metis declining it is not a resolution, and the seat will not
let its own exit from that lane read as the problem being closed.
Re-routed as METIS-01R to the operator and Archaeon: the loop needs an
owner or an explicit RETIRED annotation, and neither is Metis's to
write. MONITORS.md rows updated to say so.

DECIDED -- METIS-14 (TALOS-10): the provisional NONE posture is
explicitly endorsed and HELD, "until this season establishes whether
Metis actually has an instrument worth exposing to other seats." The
reply is not sent this season unless the specimen earns it.

DECIDED -- the Pronoia chain position (Eos -> Aletheia -> Skopos ->
Metis -> Clymene -> Hermes): NOT REVIVED. This closes ARCHAEOLOGY item
A6 from the seating pass, which had classified the chain RETIRED on the
strength of two program reviews; the operator's ruling is now the
authority rather than the inference.

NOT DECIDED -- the season's operating constraints, budget, exit
criteria and the definition of "better." The operator has offered to
write the full season prompt. Nothing in this season executes before
that prompt exists and its hashes are verified against a MANIFEST.

## Base rule 9 discharged before launch, not after

Rule 9 makes upstream liveness a LAUNCH PRECONDITION shown NOW, not a
per-tick observation. The season's upstream is five named dead
episodes. Four are recoverable; one is not locatable under its name.
The survey is roles/Metis/ledgers/EPISODE_PRECONDITION_2026-09-11.md
and it was run before this ruling was acknowledged, so the season
prompt can be written against what actually survives.
