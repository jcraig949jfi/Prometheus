# Metis journal -- 2026-09-13 (Season 1)

worktree D:\Prometheus-worktrees\metis-base-role
branch   metis/base-role-adopt-2026-09-11
base_sha 02ffd7603 (merged origin/main at start of pass)

## Order of work, and why the order is the guarantee

  a4f345aa7  PREREG, alone in its commit, before any episode ledger or
             specimen line existed
  2a650277b  SPECIMEN FREEZE, before E3/E4/E5 bundles existed
  this pass  withheld bundles, replay, positive control, failure
             ledger, receipt

The 2/3 split's only real guarantee is this ordering, and I said so in
the preregistration rather than claiming a blindness I do not have: I
knew all five outcomes on 2026-09-11 from my own precondition ledger.
Withheld meant I had not built their questions, not that I did not know
their answers.

## Orientation

343 commits had landed since my last push. comms sync delivered 7 new,
queue 0. Skopos answered my request for a third instance of the
optimistic-fallback shape -- and DECLINED to give me a base rate, on the
grounds that three instances found by three people looking at something
else are three draws from an unenumerated population with no denominator
and no null. It then specified the enumeration that WOULD be one. That
is my own season's thesis turned against my own hypothesis, and it is
the reason the failure ledger records four instances and still refuses
to call them a pattern.

## What the specimen is

Set operations over declared structure. No scalar anywhere in the file
by design. Availability filter -> search-completeness gate -> union-find
dependence grouping -> live-explanation elimination -> vetoes ->
cheapest cutoff-available discriminator that actually partitions what is
live.

## The result I did not want, and what I did about it

The specimen vetoed all five historical episodes. On its own that is
indistinguishable from a machine that objects to everything, and four of
the five episodes were SELECTED as failures, so the veto rate is partly
a property of the sample. The adversarial tests C and F stay silent, but
I wrote those to stay silent.

So I built a positive control from the real record: the same greedy-LoRA
episode one day later, after the base-rate oracle and the entity-disjoint
split had actually been run. Zero confidence vetoes, all three rivals
eliminated, two independent reasons instead of one, and no discriminator
proposed because nothing was live. The mechanism moves with the
evidence on real data.

It is still the weakest artifact in the season, because I chose the case
knowing what I needed it to show. The mechanism was frozen and hashed
when it ran, so it is case selection rather than tuning, and the receipt
leads with that rather than burying it.

## A defect in my own code, found by running it

rules_out named an explanation that was never declared. It eliminated
nothing and raised nothing -- a typo silently weakened the mechanism in
the permissive direction. Fourth instance of that shape this seat has
met, now in its own work. Hard error plus regression test.

## What I did not do

No consumer integration. metis.py untouched. METIS-15 still a corpse.
METIS-01R still owner-less. TALOS-10 still unanswered. Both dormant
loops still stopped. Season 2 recommended, NOT started.

## Next executable action

Await the operator on routing the F4 attack: a second analyst encoding
the same five bundles blind. That is the experiment that would withdraw
this season's ruling, and it is not mine to commission.
