HARMONIA -- ADJUDICATE THE DOSSIER, THE C3-2 READOUT, AND THE NEXT SCALE
(operator 2026-09-10, via Archaeon; see 00_COMMON.md for authority)

State: your rulings are applied (order-only relabel, the exact null gate,
n=12 blocks for H0). Archaeon delivered the D3 dossier
(archaeon/docs/h0h5/D3_LIVE_DOSSIER_2026-09-10.json) and the partial
readout (archaeon/docs/h0h5/C3_2_READOUT.md, 86/150: null 18/18
IDENTICAL under your gate; ICC(1) 0.995 all rules / 0.908 excluding
structural zeros; 40 of 40 random rules 0.0 under both criteria).

DELIVER, each as a committed file on main
1. D3 LIVE ADJUDICATION: the two UPPER fires (both survive v1, k_nearest,
   40 units, no single-row driver; rows and neighbours with unit ids and
   coords are in the dossier) and the 15 LOWER fires that SURVIVE d3.v1
   on the live corpus (v0 24 fires, v1 18). Your four checks per upper
   fire; for the v1 lowers, whether a second mechanism is present or the
   corpus genuinely has low-dispersion regions. Say what, if anything,
   changes in d3.v1.
2. C3-2 READOUT RULING (final when Archaeon marks it COMPLETE, provisional
   now): the null gate verdict; what ICC 0.995 / 0.908 settles for the
   unit question and U1; and the attainable-range fact -- the random arm
   is constant zero under `stable` and `at_T`, so the D3-over-C3 regions
   have zero within variance in that arm and the historical-vs-random
   contrast is a floor. Rule whether cs-c3-2 answers its declared
   questions as issued, and which questions it cannot answer.
3. THE NEXT SCALE, BEFORE C3-3 IS DESIGNED: Herakles is asked to define a
   per-cell criterion with an attainable range for random tables. Declare
   the analysis that criterion must support (unit, ICC, D3 regions with
   eight independent rules) and the eligibility count it needs, so
   Archaeon's next corpus is sized from your rule and not from the
   previous corpus's numbers.
4. H1/H0 PHASE 2 ANALYSIS FILE: the transport contrast (fresh vs
   random_pack, 12 targets, paired) and the four H0 cells under
   QR-1.1.0, with the second-seed diagnostic replicate defined (does
   phase 2 need a second seed_root row per cell, or is the replicate
   within the existing rows?). Archaeon issues phase 2 as soon as
   Vivarium publishes the artifacts; declare before that lands.

REPORT: SHA on main and path per item.
