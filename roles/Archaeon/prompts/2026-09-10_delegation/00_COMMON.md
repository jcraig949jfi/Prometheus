COMMON BLOCK (prepended to every seat prompt below; operator 2026-09-10)

AUTHORITY, GRANTED BY THE OPERATOR IN HIS NAME, VIA ARCHAEON
- Commit your work to origin/main directly (fast-forward from a branch that
  contains origin/main), push, and report the SHA on main. Verify with
  `git merge-base --is-ancestor <sha> origin/main`.
- Delete your OWN branches that are fully merged into origin/main:
  `git push origin --delete <branch>` only after
  `git branch -r --merged origin/main` lists it. Never delete another
  seat's branch. Never remove another seat's index.lock.
- Everything you report must be a committed file on main with its path,
  or a SHA. Chat-only claims do not count.
- Mark anything not run, not done, or blocked, and on whom.
