TECHNE -- RUSTUP GRANTED, STITCH ROUTE, H3 ON THE REAL STREAM, BRANCH
(operator 2026-09-10, via Archaeon; see 00_COMMON.md for authority)

State: your tools are on main; the pyribs adapter is qualified; the
stitch Rust core route is viable and blocked on a toolchain; the packet
JSONs are on no machine here (Archaeon searched) and remain the
operator's to paste; Archaeon's H3 stream format is at
archaeon/docs/h0h5/H3_STREAM_FORMAT.md and its replay harness at
archaeon/producer/h3_replay.py (three bounds, fixed assay ref, parents
earlier, duplicates recorded).

DELIVER
1. RUSTUP -- authority granted in the operator's name: install rustup
   with the gnu target (MinGW cc/link present), record the disk footprint
   you measure and the exact versions, in
   techne/acquisition/RUST_TOOLCHAIN_2026-09-10.md. Then build
   stitch_rust_core at 0ef5ec7f1709 and run the Rust-vs-Python route
   comparison on ONE fixed input file (your finding: the in-tree
   nuts-bolts.json differs from your fixture by formatting only -- pin
   one, say which). Commit the receipt. DreamCoder: state whether the
   same toolchain unblocks it, and nothing more today.
2. H3 ON THE REAL STREAM: when cs-c3-2 completes (Archaeon posts
   archaeon/docs/h0h5/C3_2_READOUT.md marked COMPLETE), build the adapter
   from Archaeon's Candidate records to your STREAM_CONTRACT_V0 (the diff
   is listed in H3_STREAM_FORMAT.md; adopt your structured assay_ref at
   the seam, derive lineage kind from parent_ids), replay the 150-row
   stream through your pyribs adapter AND Archaeon's four policies under
   the same caps, and report the three bounds and the retained-id
   agreement per policy. Note in advance: 120 of the 150 candidates score
   0.0, so the stream is degenerate on the objective axis; report what
   the archives do with ties at scale rather than tuning anything.
3. PACKET JSONs: nothing to do until the operator pastes them; keep the
   reconciler ready. One line confirming the command still runs on the
   synthetic stand-in.
4. BRANCH: origin/techne/h0h5-tools-2026-09-09 is fully merged into
   origin/main. Delete it (`git push origin --delete
   techne/h0h5-tools-2026-09-09`) and confirm.

REPORT: SHA on main and path per item; exact commands; footprint number.
