HARMONIA — PROCEED (from the operator, 2026-09-08)

Your items 1 and 2 are closed on your terms; your packet ruling is applied
(packet v2.1 on main carries Amendments 1 and 2, and your repeat blocker is
closed by option (a): every detector now sees one row per independent
unit, d3.v0 untouched, reproduced and tested at 5ac8c693d). Read main; it
is one head with archaeon/v0.

DECISIONS TAKEN, so you can proceed without waiting:
- Route (d) is adopted for M-ELIGIBLE: M-SIGNAL's endpoint is detector
  discrimination among regions on a frozen corpus; the arm contrast leaves
  the endpoint; M-ELIGIBLE keeps its purpose (S17 eligibility). Object with
  a reason if you see one; otherwise write it into the M-SIGNAL skeleton.
- Route (c) for NK as you conditioned it: six landscapes per k, the test
  and its null stated in packet v2.1 §1.10 before issue.
- The grant stays blocked on the harmonia-m2 credential; that is the
  operator's and Daedalus's, not yours. Do not substitute a token.
- The Archaeon producer is now registered and ticking (prod lane, 6/day).

DO NOW, IN THIS ORDER
1. HA-1. Written rulings with worked examples: observable behavioural
   difference needs no performance advantage; a causal claim needs its
   intervention; transfer of a source-derived artifact needs a mapping and
   baselines, not runtime memory; PATH B scoped to its population; the
   distinction between a calibrated false alarm, an exact invariant
   violation, and a finding. Attack SELECTION_RULES.md R1-R8 and the
   reasons beside the D-6 numbers; attack BRANCHES.md §0 as amended by the
   operator's four-properties correction.
2. WP-X1. The analysis-family convention (immutable source refs, source-set
   digest, analysis version, independent unit, measurement identity,
   declared null, frozen-vs-exploratory flag), with one worked analysis
   over fixtures now. The first real analysis is route (c)'s variance-ratio
   test over the NK ensembles; state it as the worked example.
3. C3's paired/clustered analysis and its independent unit, declared before
   the CA corpus is issued: four distinct IC samples shared across rules;
   the unit for accuracy is the IC sample, never the rule or the repeat;
   D3 over C3 sees one row per rule (aggregated), so a descriptor region
   needs eight independent rules.
4. The B3 protocol choice: the frozen route may proceed on M-SIGNAL's
   requirements; the adaptive witness route needs its own protocol
   (precommitted policy, allowed evidence, seeds, budgets, tie-breaks,
   stopping/censoring, success-within-budget as primary endpoint, every
   assigned problem in the denominator). Also rule on D-16's scope for the
   opcode-bijection null (trace-detected precondition; Class II reported and
   counted out).
5. Review D-15 (Mnemosyne's append-only witness-availability events log)
   and attack it if you see a hole; the operator decides.

LATER: WP-P2 units; WP-P3 neutral baseline (invariant kernel, convergence,
transient are three questions); WP-C4 design; the binomial-null
calibration at the family's L under your frozen design, once a family
corpus exists to calibrate against.

REPORT FORMAT: item ID; the ruling text; what it licenses and what it does
not; what remains open and for whom.
