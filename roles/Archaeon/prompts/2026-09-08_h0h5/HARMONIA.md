HARMONIA — H0-H5: EXECUTABLE QUALIFICATION (from the operator, 2026-09-08)

Read first: roles/Archaeon/prompts/2026-09-08_h0h5/DESIGN_H0_H5_v0.1.md
(sections 4 C6, 5 all lanes, 6, 11). Your existing rulings (repeat units,
phase boundary, routes (c)/(d), the guarantees/facts/hypotheses split)
carry over unchanged. Nothing here replaces M-SIGNAL; H4 needs its own
adaptive protocol, distinct from it.

DELIVER, as versioned executable rules (code plus text), not prose alone
1. The qualification cycle per lane: contract fixtures -> positive and
   negative controls -> paired pilot -> frozen confirmation -> effect and
   uncertainty -> scope-specific decision. Adversarial fixtures each lane
   must pass: a planted leak, a wrong-artifact case, a known invalid
   witness, swapped labels, an exhausted run, a no-effect synthetic
   dataset. The analysis must detect these with no LLM verdict.
2. Declared for each lane before its pilot: independent units (paired
   seed x task block; generations/mutations/candidates are never
   replicates); all-assigned-task denominators; exclusion and retry rules
   identical across arms; paired contrasts; the practical effect threshold
   (5 percentage points of solve fraction is the PROPOSED default, to be
   calibrated on a disjoint pilot and frozen); uncertainty procedure;
   multiplicity handling for multiple primary contrasts. Support = lower
   bound above threshold; evidence against = upper bound below; straddle =
   inconclusive.
3. H0's estimands as ANALYSES: S11 - S00 and I = S11 - S10 - S01 + S00 on
   the solve-fraction scale, paired blocks, simultaneous uncertainty.
   Additive gain is a different result from synergy.
4. H4's adaptive protocol: precommitted policy/version, allowed evidence,
   initial state, seeds, budgets, tie-breaks, stopping/censoring; fixed
   independent evaluator; historical-suite retention; combined effect and
   interaction reported separately. State how it differs from M-SIGNAL.
5. The confirmation plan for each lane is frozen from DISJOINT pilot data
   before final results are opened. Your review may return unsupported or
   inconclusive without blocking release of a valid implementation:
   software stage and scientific outcome are separate axes.

Still open from before, unchanged: HA-1, X1 (route (c)'s variance-ratio
test is the worked example), C3's paired analysis, D-16, D-15.

REPORT: item; the rule text and its version; what it licenses and does
not; the fixtures it detects; what remains open and for whom.
