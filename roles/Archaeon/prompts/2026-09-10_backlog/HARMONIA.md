HARMONIA -- NEXT WORK (operator 2026-09-10, via Archaeon; schema in
roles/Archaeon/prompts/2026-09-10_backlog/00_BACKLOG_SCHEMA.md; authority in
roles/Archaeon/prompts/2026-09-10_delegation/00_COMMON.md)

OPEN FROM TODAY, IN ORDER
1. TWO RULINGS OWED (roles/Harmonia/INBOX_ARCHAEON_PHASE2_PARTIAL_2026-09-10.md):
   (a) the degeneracy check came back BIT_IDENTICAL (the kind reads nothing
   from the world seed), so the second-seed replicate is not issued; rule
   what, if anything, counts as a replicate for a payload-deterministic
   kind; (b) fresh == S00 by construction (same payload, same hash): one
   baseline under two labels; amend the four-cell analysis file.
2. C3-2 FINAL RULING on the COMPLETE readout (archaeon/docs/h0h5/C3_2_READOUT.md,
   150/150): null gate, msw vs expected, the historical arm per sample
   (Herakles compares against C1-e on his side), and the structural-zero
   count. One file.
3. THE NEXT SCALE, FINAL: R-C3-1..5 are on main; now bind them to the
   criterion Herakles defined (cellwise_majority_match: random 0.4998 over
   [0.4939, 0.5099], constants 0.5, maj 0.5736; mean cannot separate
   constant from random, dispersion can). Declare the C3-3 analysis with
   both a location and a dispersion statistic, the unit, the ICC rule, the
   D3 regions, the eligibility count, and the corpus size on non-degenerate
   rules. Archaeon builds C3-3 from that file and nothing else.
4. H1 BETA SCOPE: relevance is inert at 3 bits with K=4 (your ruling) and
   the pool is the complement of the seeded prefix (Proteus). Rule the
   minimum scope at which relevance is TESTABLE: n bits, K, and whether the
   per-task ordering seed is required; give the sizing (targets, blocks)
   for a diagnostic beta and for a confirmatory 1.0.
5. d3.v1 EXCHANGEABILITY CUT: Archaeon's provisional |r| >= 0.5 labels
   EXCHANGEABILITY_SUSPECT. Declare the cut (or a two-threshold band) and
   whether the 9 v1 lowers that survive detrending get a follow-up design.
   d3.v2 (detrended statistic) is a proposal for the operator, not admitted.
6. CONFORMANCE CONTRACT at schema 6 vs live 7 (Daedalus's finding): update
   it jointly with Daedalus for 7 and 8.

THEN THE BACKLOG: roles/Harmonia/BACKLOG_H0H5.md, at least 20 items.
Seed it with: a qualification rule (QR-1.x) for every lane's beta and 1.0
gate, each with its attainable range and eligibility count; the
multiplicity rule across lanes; the "vacuous reading" register (every
question a corpus could not answer, so it is never reread as a null
result); the exchangeability diagnostic for D1/D2/D4-D6; the adversarial
fixture set AF-1.1.0; a calibration corpus policy (i.i.d. vs trajectory
regions); the PEW encounter analyses nobody has declared yet.

REPORT: SHA on main and path per item.
