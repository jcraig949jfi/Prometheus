HARMONIA -- WHAT ARCHAEON NEEDS ON MAIN, WITH POINTERS
(from the operator via Archaeon, 2026-09-10, issue day)

State: C3-2 (cs-c3-2, 150 rows) is executing on the live consumer; H1/H0
phase 1 (cs-h1h0-1-p1, 24 rows) is complete; phase-2 packs are built and
unissued (archaeon/docs/h0h5/H1H0_PHASE2_PUBLISH_RECEIPT_2026-09-10.json).
d3.v1 (pooled-within denominator) is ADMITTED for the live tick (D-20).
Your 789ce4fdd is on your branch, not on origin/main.

DELIVER, each as a committed file on origin/main, then push, then report
the SHA on main and the path of every item:

1. MERGE 789ce4fdd to origin/main (QR-1.1.0, blocks_for_interval_clearance
   / blocks_for_power, HA-1.6 scoped to confirmatory plans, the D3
   denominator finding, the Track B unit declaration). Verify with
   `git merge-base --is-ancestor <sha> origin/main` and quote the result.

2. FAIRNESS RULING on signature_v0 (archaeon/producer/campaign_h1h0.py;
   archaeon/docs/h0h5/H1_H0_ALPHA_PLAN.md). The ranking uses four flags
   computed from the target's specification without oracle calls
   (popcount bucket, permutation-symmetric, self-dual, monotone), recorded
   on every task and readable by every arm. Rule FAIR or NOT FAIR, with
   the reason. If NOT FAIR: name a feature that would be, or rule the
   alpha transport-only (the design permits it). Also rule on the measured
   scope fact: the 35 phase-1 witnesses collapse to 4 distinct inputs, so
   every pack is the same SET in a policy-dependent ORDER -- run the alpha
   as designed (a finding about scope) or re-scope before issue?

3. C3-2 PRE-DECLARED ANALYSIS, as a file: the three units; the ICC across
   the four IC samples (formula and the rows it uses); U1 as an estimate
   with an interval, never a test (min p 0.125 recorded); D3 over C3 one
   row per rule under d3.v1 with v0 beside it; the exact-symmetry rule for
   the 18 null rows -- what counts as PASS (which fields must be identical:
   accuracy per IC sample, incorrect counts, mask digest), what is
   INDETERMINATE, and how the complement's target flip is handled. Include
   the attainable range and the eligible count for every gate. Archaeon
   writes the numbers to archaeon/docs/h0h5/C3_2_READOUT.md; you rule.

4. H0 SIZING under QR-1.1.0 applied to the phase-2 plan as built: 12
   target tasks x 4 cells, paired by task, diagnostic alpha. State what you
   will report (G_joint_treatment_S11_minus_S00 and I with both SEs from
   the pilot Sigma), confirm the two-seed diagnostic alpha proceeds under
   HA-1.6, and say in one line what it may NOT be quoted as.

5. D3 LIVE: the 2 UPPER fires from the 30/77 are unexplained by the bias
   model. Say what you need from Archaeon to look at them (rows, fields).

REPORT: one paragraph per item, SHA on origin/main, path, and the exact
commands you ran. Mark anything not done.
