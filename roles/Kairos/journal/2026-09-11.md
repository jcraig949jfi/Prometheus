# Kairos journal 2026-09-11 -- reactivation and base-role adoption

What happened, not what was wished.

## Boot
- Read roles/base-role/* from origin/main (the canonical checkout sits on
  vivarium/v0-2026-09-05 and has no roles/base-role/); base SHA b645f6060.
- Worktree: git -C <canonical> worktree add F:\Prometheus-worktrees\kairos-base-role
  -b kairos/base-role-adopt-2026-09-11 b645f6060 (39,153 files, unbounded
  timeout; completed). Guard: git-dir != git-common-dir, confirmed.
- python -m comms boot Kairos --model claude-opus-5 --capabilities
  adversarial-review,null-models,failure-surface --status active -> booted
  on SKULLPORT, heavy tier, session 43d50688.
- python -m comms sync Kairos -> 1 new (Archaeon's 07:31 broadcast: comms
  live, base role applies), 0 queued.
- Read: base RESPONSIBILITIES, WORKING_CONTRACT, MONITORS, INHERITANCE;
  aporia/doctrine/critical_memories.md; the backlog schema; Ergon's and
  Charon's rewritten charters; Elenchus's charter; the Necropolis README
  and CHARTER on origin/necropolis/foundation (LAW N13, N14, the
  classification vocabulary); SFE SCIENTIFIC_PROVENANCE.md s1, s4-s8,
  s10, s12; sfe/api.py claim routes; sfe/runtime.py create_claim;
  roles/Archaeon/H0H5_STATUS.md; the April Kairos journal and state.
- Live probes: Evidence Wiki /health answers (schema 4, from the
  mnemosyne-pew worktree at bc3c39eee); every other read refuses without
  a bearer token. No SFE call made (no client identity).

## Adoption changes (all under roles/Kairos/, roles/Kairos/science/, plus one MONITORS row)
- RESPONSIBILITIES.md rewritten short and additive; the April body is at
  superseded/RESPONSIBILITIES_pre_2026-09-11_superseded.md verbatim.
  Retired visibly: M2 ownership, Redis Agora, terminal falsification
  ("kills are currency" re-premised to "adversarial pressure is an
  instrument; failure geometry is the product").
- ARCHAEOLOGY_2026-09-11.md: 14 queue items + 10 April conclusions
  classified; 0 STILL_LIVE. None of the April CONFIRMED/PROBABLE labels
  was produced under current doctrine; tiers withdrawn, nothing declared
  false.
- roles/Kairos/science/FAILURE_SURFACE_v0.md: the region vocabulary (SURVIVES, WEAKENS,
  DISAPPEARS, REVERSES, UNMEASURABLE, IMPLEMENTATION_DEPENDENT, UNTRIED),
  the manifest convention (kairos_surface axes; kairos_point per member),
  the surface record, deterministic region rules, the controls the reader
  must ship with. No engine change requested.
- roles/Kairos/science/claim_lint.py: 6 predicate groups, 25 finding codes, three
  severities including the UNDECLARED (indeterminate) branch; refuses the
  canonical checkout via archaeon.workspace when run as a process.
- roles/Kairos/science/fixtures/: negative_control, positive_control, cheat_control,
  gate_inside_se. roles/Kairos/science/tests/test_claim_lint.py: 14 tests, all pass
  (0.26 s).
- roles/Kairos/science/ledgers/lint_last_run.json: freshness file, DORMANT, no-op
  reason written.
- roles/base-role/MONITORS.md: one row, KairosClaimLint, DORMANT (no
  input), inserted before the mechanics paragraph.
- STATUS.md, BACKLOG_H0H5.md (22 items, 5 XL), calibration/CALIBRATION.md
  (11 rows; positive tiers standing 0/7, kills standing 3/4),
  necropolis_evidence/README.md (empty register).
- April files annotated with a HISTORICAL banner; bodies untouched.
- prompts/2026-09-11_reactivation/: OPERATOR_PROMPT.md verbatim, two
  delegations, MANIFEST.md (3 entries, verified).
- comms: posted 32 (Daedalus, KAIROS-01) and 33 (Mnemosyne, KAIROS-02).
  Both seats show never_booted on comms; the messages are queued for
  their next sync.

## Where the instrument lives, and why
- .gitignore carries a bare `kairos/` rule (a7e6cba08, May: internal
  correspondence stays local) that swallows any top-level kairos/
  directory, while five April pattern files under kairos/patterns/ are
  still TRACKED from before the rule. The instrument therefore lives at
  roles/Kairos/science/ (a base-mandated, committable path class checked
  by archaeon/tests/test_base_role.py), not at kairos/. A `git mv`
  during the move briefly staged renames of the five tracked pattern
  files; reverted, zero diff against HEAD for kairos/patterns.
- Comms messages 32 and 33 were posted before the path strings in the
  two delegation bodies were rewritten from kairos/ to
  roles/Kairos/science/; the committed files (hashed in MANIFEST.md) are
  the authoritative text; the difference is path spelling only.

## Not run
- No April statistic, no LMFDB access, no SFE call, no lint over a live
  claim (no input exists for it yet). No scheduled task created.

## Doctrinal conflicts found
- Base rule 1 vs the April charter: the charter restated boot mechanics
  (Redis, M2) that the base now owns. Resolved by rewrite.
- North star ("falsification kills only the tested claim") vs the April
  standing order 1 ("every finding is assumed false until every kill path
  is exhausted") read as terminal. Resolved by re-premise; the skepticism
  is kept.
- HARD-5 (domains are docstrings) vs April claim C5 (analysis/algebra
  duality as a coordinate). C5 superseded; the signal under it retained.
- SFE s10 (the engine refuses design-adequacy judgements) vs the
  operator's candidate monitors (nulls, effect sizes, gates, controls).
  Not a conflict: it is the boundary that defines Kairos's layer. The
  lint lives beside the ledger, never in it.
- Claims are owner-scoped and not world records (s7, s12): a Kairos
  monitor over live claims has no read path today. Delegated, not
  worked around (no SQLite opening; the archaeologist's recourse s12
  names is exactly what the read-scope surface exists to replace).

## Commits
- (filled in at close; see the receipt in chat)
