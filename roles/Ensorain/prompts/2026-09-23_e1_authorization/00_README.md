# E1 authorization, received 2026-09-23 (third operator message)

01_OPERATOR_E1_AUTHORIZATION_verbatim.md is the operator's reply to the
E0 review packet, verbatim (non-ASCII punctuation and LaTeX kept as
pasted). Rulings it carries, as the seat reads them (the verbatim wins):

R1. E0 is FROZEN permanently as a negative result; no learner rescue.
R2. E1 is authorised as a hostile, cheap falsifier: one small world where
    fine structure is causally necessary (delayed locks, latent-context
    disambiguation, tight tolerance, unseen regions from the same
    factors, cross-context factor reuse).
R3. Charged batch consolidation for EVERY arm; temporary workspace
    counted against a separately declared scratch allowance.
R4. Decisive metric: (held-out transfer utility + future compute saved +
    survival/puzzle utility) / persistent parameters. The operator names
    this "essentially the remainder of the s12 idea" -- the seat treats it
    as the operative s12 measure from now on (ENSORAIN-14 resolved by
    this ruling; the literal s12 text is still missing).
R5. GOVERNING gate: if any non-TT memory comes within 10% of the best TT
    at matched persistent memory and matched compute budget, B stands.
R6. Control: latent-order oracle TT with the same learning machinery must
    clear the positive-control threshold before TT-specific claims are
    scored.
R7. Control: world-basis rotation/permutation transplant.
R8. No GPU on the critical path, no Runpod, no scale, no societies.
