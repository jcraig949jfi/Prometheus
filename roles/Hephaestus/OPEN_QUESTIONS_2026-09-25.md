# Open questions for the operator (re-ask verbatim after the next bootstrap)

Currency: 2026-09-25, written at session close before the M2 reboot. Each question
names the decision it unblocks. Default-if-silent is stated so the seat can work.

## From the 2.0 boot packet (2026-09-19)

Q1. Accept the re-premise -- boundary certifier (closure gauntlet over ecology
    substrates) + mechanism assay (knockout on evolved organisms) + Master Smith
    re-premised to ASSAY what evolution forged, not to mint -- as the working 2.0
    charter, to be written AFTER HEPH-32 returns evidence? Default: proceed with
    HEPH-32 as the test; write nothing charter-level until it returns.
Q2. Which ecology substrate first for HEPH-32: the SFE VM opcode set (C4 cliff,
    measured) or Proteus graph_organism.v1? Recommendation and default: SFE VM.
Q3. Mint Queue: keep the four packets inert at zero compute and open MINT-0005
    only from a registry event, or retire the packet mechanism? Default: keep inert.

## From the cross-pollination replay (2026-09-19 .. 09-23)

Q4. Close the temporary mission on the finding "size buys mechanism existence,
    not mechanism quality; the prompt architecture caps every model; paired vs
    1.0 originals 2-1 for the originals"? Default: closed; results stay on file.
Q5. Authorize ONE Master Smith inspect-and-revise cycle on XP-009 targeting R2
    (0.225 on every session tool; the claude -p 0.52 tool had R2 0.60), recorded
    as a separate with-feedback arm? Charter s3: operator-only. Default: NOT run.
Q6. Fund GPT-6 Astra (openrouter credits on the existing key)? Without it the
    replay is Fable-vs-cheap, not a frontier comparison. ~10 packets x 3 stages
    x ~8k tokens. Default: not run.
Q7. The 1.0 validator admits constant-output tools (the 14B reproduced the class
    today). Add a constant-output detector (all candidate scores equal -> reject)
    to agents/hephaestus/src/validator.py as a REPAIRED instrument, or leave the
    1.0 tree frozen? Default: frozen; detector lives in xpol_2026 only.

## Standing

Q8. HEPH-22 fate of the M3 station doc; HEPH-17/ruling 6 already answered.
