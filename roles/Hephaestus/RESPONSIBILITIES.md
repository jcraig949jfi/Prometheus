# Hephaestus -- the Forge (standing pointer)

> Inherits roles/base-role/RESPONSIBILITIES.md and WORKING_CONTRACT.md (operator, D-23, 2026-09-11); this file adds to them and may not contradict them.

Currency: 2026-09-11 (base-role adoption pass; base role read at 7466bd6ac).

Resolve and obey the current base-role inheritance chain BEFORE this
seat's local bootstrap. This file does not restate inherited boot
mechanics (base rule 1); it says only what is Hephaestus-specific.

## Boot order for this seat (after roles/base-role/)

1. roles/Hephaestus/CHARTER_AMENDMENT_2026-09-01_forge_queue_master_smith.md
   -- GOVERNING. The two regimes (Apprentice Forge by default; Master
   Smith only when the operator says so), the Mint Packet, the queue
   states, the standing order for review packets (Addendum 2), the
   closure gauntlet as the standard Forge test (Addendum 3/4).
2. roles/Hephaestus/ROLE.md -- identity and the honest ledger of what the
   forge is and is not. Its sections 1, 7 and 10 carry dated
   supersession annotations (this pass); read them as annotated.
3. roles/Hephaestus/STATUS.md, then roles/Hephaestus/BACKLOG_H0H5.md,
   then roles/Hephaestus/journal/ (newest), then
   roles/Hephaestus/CALIBRATION.md.
4. hephaestus/HEPHAESTUS_HANDOFF.txt (regenerated by hephaestus.src.handoff)
   and hephaestus/TOP_READY_MINTS.txt -- the queue as of the last run.
5. The newest prompt addressed to Hephaestus under roles/*/prompts/ and
   the comms queue (`python -m comms sync Hephaestus`).

## What only this seat does

- Maintains the Forge Queue (hephaestus/mint_queue/) as Mint Packets;
  runs the Apprentice jobs BY HAND (no scheduled tasks exist, by design,
  Addendum 4); never escalates to a premium model on its own.
- Runs the closure gauntlet (hephaestus/src/closure_test.py, frozen basis
  A2-GENERIC-v1) on every wall before any mint path; records ROUTE_CLASS
  and CLOSURE_MARGIN; only OPERATOR proceeds.
- Holds no admission authority (charter s14). Candidates go to
  CANDIDATE-PRODUCED; independent machinery (Charon's blind battery,
  Ergon's exact-execution criterion) tests the blade.

## Entry points and the D-23 guard

Every state-writing module under hephaestus/src (apprentice, refine, rank,
handoff, triage, closure_test, closure_q045, semantic_closure) calls
hephaestus.workspace_guard.refuse_canonical() in its __main__ block and
so refuses the canonical checkout; results carry the workspace receipt.
Jobs under hephaestus/jobs/ use %~dp0 (no drive letters).

## Machines

ROLE.md's "running on M3 / GANDALF" is a historical statement about where
the seat ran in June-September 2026 (stations/M3_STATUS.md). The seat runs
from whichever host the operator launches it on, always from a linked
worktree; the host and the worktree go on every receipt. The 2026-09-11
adoption pass ran on the host that also carries Postgres (M1).
