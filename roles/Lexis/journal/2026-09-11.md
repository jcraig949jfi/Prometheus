# Lexis journal -- 2026-09-11

> Inherits roles/base-role/RESPONSIBILITIES.md and WORKING_CONTRACT.md (operator, D-23, 2026-09-11); this file adds to them and may not contradict them.

What happened, not what was wished. Numbers, commands, SHAs, what was NOT run.

## Boot (base-role adoption pass)

- Found the canonical checkout parked on vivarium/v0-2026-09-05 with 111 dirty
  entries belonging to other seats; touched nothing there. No Lexis file among
  them (git status --short | grep -i lexis: empty), so missive item a is empty
  for this seat.
- Created the worktree: `git -C <canonical> worktree add <worktrees>/lexis-base-role
  -b lexis/base-role-adopt-2026-09-11 origin/main`, run unbounded (39,071 files,
  contract s3). Base SHA b1ccc9a2188024ed854f666e5c1652e72d114aa3. Guard:
  git-dir F:/Prometheus/.git/worktrees/lexis-base-role != git-common-dir; 0 dirty.
- Read in full: roles/base-role/{README,NORTH_STAR,RESPONSIBILITIES,WORKING_CONTRACT}.md,
  INHERITANCE.md (Lexis row: ROLE.md, banner already present at line 3),
  archaeon/tests/test_base_role.py, the workspace missive and backlog schema,
  D-23 amendments 3-5, Charon's adoption receipt, Elenchus's STATUS/BACKLOG,
  Charon's CALIBRATION.md. Own files: ROLE.md, SUMMARY.md, handoff/LEXIS_G7_HANDOFF.md,
  the HC-T01 archaeology pass (adjudication commit body, addendum, gaps ledger).
- origin/main moved to 7466bd6ac (comms queue, D-24) while reading; merged by
  named SHA: `git merge --no-edit 7466bd6ac`. Read the diff to the base role
  (boot step 7 is now "sync your inbox"; cross-seat messages go through comms).
- `python -m comms sync Lexis` at 2026-09-11T11:39:28Z: 1 new (Archaeon broadcast,
  sha256 402c3445443d1be5...), 0 queued, queue length 0. No cadre prompt for Lexis.

## Property checks (verify the property, never the label)

- Closeout prompt hash. First hashed the whole file and got 981e7f3b..., not the
  stated 91fbd86b.... The file's own header says the hash is over bytes after the
  marker line: `git show HEAD:...PROMPT_CLOSEOUT_2026-09-01.txt | sed -n '6,$p' | sha256sum`
  = 91fbd86bfeb73af2c0c65838a4a5345424fb0469dfdff31e90cb4ad156f7a35d. MATCH.
  (Logged in CALIBRATION.md: the first check was aimed beside the claim.)
- HC-T01 directive and two subprompts: body and whole-file hashes both match
  L_PROMPT_PROVENANCE.jsonl (1016564181..., 52f54e0b..., 4a6c8a7d...). MATCH.
- `python roles/Lexis/handoff/verify_handoff.py` at HEAD: VERIFY_HANDOFF: PASS,
  exit 0, every frozen number re-asserted (charon dE 40/42, 15/42 unrecognised,
  pair +4/+4/+4, 9 home regressions, L* = 4/3, 5-seed sweep present). LEX-04 done.
- apollo/ has 16 commits since 2026-08-25 (Gen-2 pivot, S1 gate). The ceiling's
  precondition (congruence audit) has NOT been re-run at HEAD; that is LEX-06, next.
- Reopening criteria: none fired. roles/Apollo/STATUS.txt still says
  "Task2 state-injection OWED" (2026-09-01T07:42Z); no commit since 09-01 touches
  the fixture or the pair (git log -- roles/Lexis: only the base-role stamp).

## Guard (missive item d; LEX-03)

- New roles/Lexis/workspace_guard.py inheriting archaeon.workspace (Ergon's pattern).
- Patched the `__main__` block of the 13 scripts that write into the tree
  (11 instruments + handoff/build_handoff.py + handoff/consumer_utility.py):
  4 lines each, main() untouched. candidate_primitives.py (the pair's pinned
  source) NOT touched, so the manifest's hash pin still holds (verify PASS above).
- Positive control: verify_handoff.py (runs build_handoff.py as a subprocess
  through the guarded main block) still PASS.
- Live control from this worktree: refuse_canonical('smoke') returned
  {branch: lexis/base-role-adopt-2026-09-11, main_worktree: False}.
- Cheat control: monkeypatched archaeon.workspace.receipt to return
  main_worktree=True with allow_canonical_override=True; refuse_canonical raised
  CanonicalCheckoutRefused (override ignored, as configured). Restored; real
  main_worktree False.
- NOT demonstrated: a live refusal from the canonical checkout, because that
  checkout is on a branch that predates archaeon/workspace.py and mutating it is
  forbidden. The base self-test's tmp-repo case covers the predicate.

## Seat files (LEX-01, LEX-02, LEX-11)

- New: STATUS.md, BACKLOG_H0H5.md (26 rows, 7 XL), CALIBRATION.md (16 rows from
  written record), this journal, BASE_ROLE_ADOPTION_2026-09-11.txt.
- ROLE.md: adoption annotation block after the banner (currency, migration of the
  s7 git bullet, non-normative memory-slug citations, no-touch constraint kept,
  machine = compute-free, guard). Nothing below the block rewritten.
- SESSION_2026-08-25.md line 3: drive-letter annotation (historical record kept).
- L_ARCHAEOLOGY_GAPS.jsonl: AG-05 and AG-06 gained status_2026-09-11 = CLOSED by
  annotation (RA-1/RA-2 executed at d51d1fa82 after the rows were written).
  Original status fields kept.

## Prompts written (LEX-05; base s4 write-the-prompt-when-blocked)

- prompts/2026-09-11_state_injection/APOLLO.md (delegation: run Task 2 over the
  fixture, arms A/B/C, readout_only apart, then consumer_utility with Apollo's
  payoffs; or rule the blackboard substrate retired). sha256 d22390fa....
- prompts/2026-09-11_state_injection/ARCHAEON_manifest_defect.md (report, below).
- Both to be posted on comms after the commit that carries them (body is a
  committed file first); ids recorded in the next journal entry.

## Base self-test on the merged tree

- `python -m pytest archaeon/tests/test_base_role.py -q`: 1 failed, 7 passed.
  FAILED test_issued_manifests_verify_against_their_files:
  roles/Archaeon/prompts/2026-09-11_comms/00_BROADCAST.md, manifest says
  44273b26..., committed blob hashes c1f4b664.... Reproduced on origin/main at
  7466bd6ac directly (git show 7466bd6ac:<path> | sha256sum), so it is not
  caused by this pass. No-newline and double-newline variants do not match either.
  Reported to Archaeon (file above); constitution defect per s10, fixed centrally.
- The four tests this seat's STATUS relies on (mandatory paths committable,
  banner present, scheduled tasks registered, base files ASCII) pass: 4 passed.

## Not run

- LEX-06 congruence audit at HEAD (next). LEX-07 cheat control for g5/bundle.
  LEX-08 evidence-wiki submission (service reachability not checked this pass).
- No instrument that produces a number was run except verify_handoff.py.
