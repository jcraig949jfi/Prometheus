# Techne journal -- 2026-09-17

## Boot (instance GANDALF gandalf-a04f7c25, model claude-fable-5-1) -- first Techne boot on M3

Operator wake line: "You're @roles/Techne running on M3 instead of M1 or M2. bootstrap. let's
talk about how you can work on M3 when all your files are elsewhere." The deliverable of this
pass is the M3 host census and the plan; no charter work was started.

- Host is GANDALF (env PROMETHEUS_MACHINE=M3; LAN 192.168.1.161), Windows 10 Home 19045, 8 logical
  CPUs, 24 GB RAM. Canonical checkout here is C:\prometheus (git-dir == git-common-dir; refused
  for work). No D:\Prometheus, no F:\Prometheus. C: has 231 GB free.
- `git fetch origin` only; origin/main = 4a6457fbb (HEAD there already equal; the checkout had not
  been pulled by anyone -- ?? entries only). Worktree
  C:\prometheus-worktrees\techne-m3-boot-2026-09-17, branch techne/m3-boot-2026-09-17, from that
  SHA; guard passes (git-dir C:/Prometheus/.git/worktrees/... differs from common C:/Prometheus/.git);
  44,458 files checked out with an 1800 s budget (contract s3), 0 missing.
- techne.workspace.receipt() from the canonical checkout: main_worktree True, tool_cache
  C:\Prometheus\vault\techne_tools (absent), tool_cache_versioned False.
- `python -m comms boot Techne --model claude-fable-5-1 --capabilities any` needed
  EW_DB_HOST=192.168.1.202 (comms/README.md 49-51); there is NO local Postgres on M3 (the comms
  tests' default localhost:5432 is refused), so unlike M2 nothing can be reached by mistake.
  RC 0: "booted Techne[gandalf-a04f7c25] on GANDALF ... 4a6457fbb @ C:\prometheus-worktrees\...".
  Instance tag form on this host is gandalf-<8> (comms lowercases the hostname; m1/m2 are the only
  named labels). The AGORA_* env vars on this host point at 192.168.1.176:5432/6379; both ports are
  closed; they are April residue, not a live service.
- `comms sync Techne` at 10:39Z: queued 0, unseen 0 after sync. Since the M2 instance's last sync
  (2026-09-16 13:36) five messages arrived, none addressed to Techne: Nyx #320 (gzip packet key ->
  MECH-GZIP-LEVELTABLE-003), Nyx #321 (Amendment 3 path + sha), Archaeon #323/#324/#326 (CMP1, CMP2,
  SFE campaign 3 closed). Nyx #310 (delegation) was answered on M2 (1bb9965b4); #312/#316/#317 are
  reports already assimilated.
- `comms who`: Techne 1/6 instances online (this one), Nyx online ON THIS SAME HOST
  (gandalf-9e21f277, C:\prometheus-worktrees\nyx-boot-2026-09-17), Proteus / Mnemosyne / Daedalus
  online on M2, Archaeon and Nestor offline. So M3 hosts two heavy seats today.
- Newest prompt naming this seat: roles/Nyx/prompts/2026-09-16_mechanism_archaeology_pipeline/
  AMENDMENT_3.md, blob sha256 4f93cb12e846...db33 == the sha Nyx gave in #321 (verified from
  `git show origin/main:<path>`). Amendment 2's MANIFEST verifies (1 entry, 0 mismatches).
  Amendment 3 items that land on Techne: R35 ancestry vocabulary (SUPERSEDES / SUPERSEDED_BY /
  DERIVES_FROM / TRANSCRIBES / RECONSTRUCTS / FORKS_FROM / INDEPENDENT_LINEAGE -- record.py today
  has only superseded_by / supersedes), R36 FOSSIL_WORLD_MANIFEST canonicalization (packet.py's
  fossil_world_id() hashes dockerfile sha + package manifest sha + probes; no canonical-JSON
  manifest, no fixtures for ordering / CRLF / witness invariance yet), R37 intervention levels in
  the capability matrix (WORLDS.json has 13 verbs, no levels), R38 preservation gate (TECHNE-65,
  operator), R39 birth-complete pilot record. None of these needs a body or a world to execute.
- Sibling commits since 09-16 touching techne/ or roles/Techne: only Nyx's (#313 reply, the gzip
  packet writes at my request) and my own M2 merges. Nobody has claimed a Techne gap.
- MONITORS.md: no loop owned or fed by Techne. Nothing to feed.
- Doctrine aporia/doctrine/critical_memories.md unchanged since 7cb418d1d (2026-05-08).

## The M3 host census -- what is here, what is not, what cannot be here

TRACKED (present, identical to origin/main): everything. 121 specimen records, recipes, receipts,
harnesses, the cartography campaign store (genomes/claims/holes jsonl), WORLDS.json, the packets.
"All my files are elsewhere" is true only of the gitignored classes below.

NOT HERE (host-local by design; where they are):
  121 fossil bodies (~1.05 GB)     M1 vault (11 of them the CRLF copies) + M2 vault (121/121 verified)
  8 world images (docker)          M1 + M2 (rebuilt on M2 09-16; 0/8 bit-identical to M1)
  vault/techne_tools (locks)       M1 + M2 (M2: 3/5 import, 2 refused by Smart App Control)
  cartography/*/data (gitignored)  M1 (D: store on the 09-01 campaign host)
  keys / .env                      agents/hephaestus/.env is here (May); no repo-root .env
NETWORK FROM M3: M1 Postgres 192.168.1.202:5432 OPEN (the comms host); M1 8377 (Evidence Wiki API)
  CLOSED; M1 445 open but \\192.168.1.202\Prometheus not a share; M2 445 open but
  \\SPECTREX5\prometheus_share and \\192.168.1.191\prometheus_share both refuse this identity (the
  same one-identity ACL measured from M2 on 09-16); 192.168.1.176 dead.
CANNOT BE HERE WITHOUT AN OPERATOR ACT: virtualization is DISABLED IN FIRMWARE
  (Win32_Processor.VirtualizationFirmwareEnabled False, HypervisorPresent False). WSL2 default
  version 2 and the WSL/VMP features are enabled, but `wsl -l` reports no distributions and a
  distro cannot start without VT-x; Docker Desktop 29.1.3 is installed and its engine pipe is
  absent. So the 92 container-runner specimens, the 8 worlds, simh, and every native build
  (no gcc / clang / cl / make / cmake / cargo on this host) are out of reach here -> TECHNE-102.
INTERPRETER: CPython 3.11.9 (M2 runs 3.14.4). Present: numpy 1.26.4, scipy, sympy, mpmath, z3
  5.1.0, numba, Cython, ribs 0.11.0, tensorly 0.9.0, networkx, pydantic, SQLAlchemy, psycopg2 2.9.12,
  redis client. Absent: snappy (test_alexander_polynomial cannot collect), cvxpy / clarabel / scs,
  cypari2, hypothesis not checked. Smart App Control: policy key present with no value (not the
  M2 posture; unverified beyond that).
GOOGLE DRIVE: Google Drive for desktop (GoogleDriveFS) is mounted as G: (presents as FAT32, 279 GB
  used / 219 GB free) with G:\My Drive\Prometheus already there (46 files, 100 MB, April-May
  documents plus serendipity-foundry-0.1.1.zip of 08-28). It is a cloud failure domain distinct
  from M1, M2 and the LAN -- the class R38 asks for -> TECHNE-100 (operator names it or not).

## Two smokes, so the plan has numbers

1. `harvest rematerialize gzip-1.2.4-1993` on M3: MATCH; `harvest verify` = tree
   d89ed3f8c3083e99847253fd0f53576da67754eee54241e1362e747f1c721719 matches. Vault root resolved to
   C:\Prometheus\vault\fossils (the canonical checkout's gitignored vault, as on M1 and M2); 1.1 MB
   on disk after. The every-later-host verb works on a third host; the full 121 is TECHNE-99 (M2
   measured 1,049 MB in 230 s of fetch).
2. `harvest mirror --dest "G:\My Drive\Prometheus\fossil_mirror" --dry-run`: same_volume False,
   WOULD_COPY 1 (gzip, 942,686 bytes), BODY_MISSING_ON_THIS_HOST 120. Receipt
   techne/fossils/mirror/mirror-20260917T104508Z.dryrun.json committed as the measurement. Nothing
   was copied.

## Tests on this interpreter (merged tree 3f2920d6a, see below)

- comms/tests with EW_DB_HOST set: 23 passed, 8 skipped (throwaway schema on the canonical
  cluster; without EW_DB_HOST the 5 DB tests error on localhost:5432 refused -- expected here).
- test_fossils + test_fossil_packet + test_fossil_git_pin_eol + comms: 404 passed, 8 skipped.
- test_fossil_isolation (5) + test_fossil_environment end-to-end (1): 6 FAIL on M3, and the reason
  is an instrument defect of mine, not the host: the skip guard asks shutil.which("bash") and gets
  C:\Program Files\Git\usr\bin\bash.EXE (works: 5.2.21, MINGW64), but harvest._shell runs
  ["bash", "-lc", ...] through subprocess, and CreateProcess searches System32 BEFORE PATH, so the
  process that runs is C:\Windows\System32\bash.exe -- the WSL launcher -- which prints "no
  installed distributions" and exits 1 (reproduced from Python: bare "bash" rc 1, explicit Git
  bash rc 0). On M1 and M2 the System32 bash IS a WSL distro, so the two resolutions agree by
  accident. Label (a bash exists) versus capability (this bash runs the recipe): base-role
  "capability over labels", again at one layer down -> TECHNE-101. Not fixed in a boot pass.
- techne/tests whole: not run to completion (collection stops at test_alexander_polynomial:
  snappy missing; the -x run was a collection count only).
- NOT run: `harvest verify --all` (120 bodies absent here; it would report a fact about the host).

## What M3 is good for, stated once

M3 is a records-and-network host: full tree, the canonical comms queue, Python with the numeric
stack, cloud storage mounted, no worlds, no compilers. That is exactly the shape of Amendment 3's
Techne items (R35, R36, R37, R39 fields) and of the two preservation moves (TECHNE-99 rematerialize
all 121 here; TECHNE-100 mirror to G: if the operator names it). Anything that executes a body in
a world stays on M1/M2 and is routed there by comms, with the M3 receipt saying so.

## Work items suggested (base-role boot step 6), in order

1. TECHNE-101 fix: resolve the native shell by capability probe to an absolute path; typed refusal
   NATIVE_SHELL_UNAVAILABLE; 6 tests pass-or-skip on M3; cheat control feeds the probe the stub.
2. TECHNE-99: rematerialize all 121 on M3 + verify --all -> REMATERIALIZE_M3_2026-09-17.json.
3. TECHNE-100 (operator, one line): is G:\My Drive\Prometheus\fossil_mirror the R38 destination?
   If yes: mirror + mirror-verify from M3 after (2); PRESERVATION key in the gzip packet turns
   from null to a receipt; the gzip lineage leaves NON_CANONICAL / PRESERVATION_GATE_OPEN.
4. R36 canonical FOSSIL_WORLD_MANIFEST + fixtures (ordering, CRLF/LF, witness, version) --
   pure Python, host-independent by construction, no docker needed to write or test it.
5. R35 ancestry vocabulary in record.py with the 17 measured edges re-classified; MKW-1 condition 1
   cannot be counted until this exists.

## Commits this pass

- Merge of origin/main 3f2920d6a into the branch was a fast-forward (no local commits before it).
- This journal, four backlog rows (TECHNE-99..102), the mirror dry-run receipt, and the review
  packet REVIEW_PACKET_TECHNE_M3_BOOT_2026-09-17.txt, committed by explicit path with a message
  file; pushed branch:main only after origin/main is an ancestor; SHA verified with
  `git merge-base --is-ancestor`. The backlog's counts paragraph was NOT regenerated (it is the
  09-11 fence count and says so); a recount is owed at the next backlog edit.

## Addendum 10:40 local -- "Docker we can run here": measured, and no

Operator: "we can't run wsl here. Docker we can though. This machine doesn't support WSL. It's
old, old cpu, old bios." Verified the property, not the label:
- Started Docker Desktop 4.57.0 (installed 2026-01-26) and polled `docker info` for 240 s: the
  engine pipe never appeared. The dashboard renderer crashed (exit 143) at 14:37:31Z and the
  backend exited 143 ("sending to Dashboard: stopped").
- Docker Desktop's OWN backend state, from com.docker.backend.exe.log line 3 (2026-01-26, the
  day it was installed) onward: {"docker":"stopped","dockerAPI":"stopped",
  "hasNoVirtualization":true,"mode":"linux"}. %LOCALAPPDATA%\Docker\wsl\main is EMPTY and
  dates from the install; there is no vm-data directory. No Docker engine has ever run on this
  host. Docker Desktop on Windows 10 Home has one backend, WSL2, and WSL2 needs the same VT-x
  the CPU/BIOS does not offer; "Docker installed" is a label, "engine runs" is the property.
- Processes I started were stopped afterwards; nothing was changed on the host.
- Consequence unchanged: TECHNE-102 stands with this evidence attached. The one route to a
  world FROM M3 is a REMOTE engine: M2 runs docker.io 29.1.3 inside WSL2 (TECHNE-81); a
  `docker -H ssh://<user>@192.168.1.191` (or a guarded TCP socket) would let M3 drive M2's
  worlds, but harvest._shell invokes `wsl.exe -e bash -lc "docker run ..."` and would need a
  `remote-docker` runner plus the body present on M2's side. Not built; filed as the option
  under TECHNE-102, operator's call whether M2's engine is exposed at all.
