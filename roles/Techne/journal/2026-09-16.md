# Techne journal -- 2026-09-16

## Boot (instance SPECTREX5 m2-04bd52c0, model claude-opus-5)

- Host is SPECTREX5 (m2), NOT SKULLPORT: canonical checkout D:\Prometheus (git-dir == git-common-dir,
  refused for work). `git fetch origin` only; origin/main = ccb26df01 (HEAD there already equal).
  Worktree D:\Prometheus-worktrees\techne-boot-2026-09-16, branch techne/m2-boot-2026-09-16, from
  that SHA; guard passes (git-dir differs from git-common-dir); 43,352 files checked out.
- `python -m comms boot Techne --model claude-opus-5`: FIRST attempt REFUSED with
  WrongEnvironment (db_system_id 7681719240261676752 = the M2 fork) -- incident class
  c84e26826cc12217, known; an occurrence line belongs in roles/Hermes/incidents/ but that file is
  Hermes's lane, so it is recorded HERE and not edited there. Second attempt with
  EW_DB_HOST=192.168.1.202 (comms/README.md line 49-51): RC 0, "booted Techne[m2-04bd52c0]".
- `comms sync Techne`: 0 new, 0 queued, queue length 0 (11:46Z). No prompt newer than batch 12.
- Batch 12 charter hash 773121688f... re-hashed from the committed blob: matches journal 09-13:464.
- Sibling commits since 09-13 touching my lanes: only my own 09-14/09-15 boots and 171414967
  (arsenal matrix). Nobody has claimed a Techne gap.
- MONITORS.md: no loop owned or fed by Techne. Nothing to feed.
- Doctrine aporia/doctrine/critical_memories.md unchanged since 7cb418d1d (2026-05-08).
- THIS HOST: 0 vault bodies (D:\Prometheus\vault\fossils does not exist), no docker, no simh,
  WSL2 Ubuntu 24.04 present. Batch 12 P6 (LISP 1.5 / Spacewar on simh) is therefore not runnable
  from here without re-acquiring the bodies; deferred, not dropped.
- NOT run: `harvest verify --all` (no bodies here; would report 121 BODY MISSING, which is a fact
  about the host, not the vault).

## Work items suggested (base-role boot step 6) and what happened to each

1. TECHNE-65 destination side -- DONE (this host IS \\SPECTREX5\prometheus_share).
2. MANIFEST.md for prompts batch 09-12 -- DONE.
3. #245 RQ-4 on harvest receipts -- DONE.
4. Batch 12 P6 on simh -- deferred (bodies on SKULLPORT).
5. Batch 13 -- blocked on an operator charter.

## Unit 1 -- TECHNE-65 measured from the destination (techne/fossils/mirror/TECHNE65_DESTINATION_SIDE_2026-09-16.md)

Measured on SPECTREX5 ~11:50Z: share prometheus_share -> C:\prometheus_share EXISTS (cartography/,
lmfdb_local/ from April; 31,880 files; no fossil_mirror/ yet); LanmanServer Running/Automatic;
SMB2 on, SMB1 off, signing required; firewall SMB-In enabled for Public and the active profile IS
Public; IP 192.168.1.191; 756 GB free on C:. Share ACL: SPECTREX5\prometheus Full -- the ONLY
entry; local user prometheus Enabled, PasswordRequired, LastLogon 2026-05-15. Loopback Test-Path
by name, by IP and by localhost as spectrex5\james: Access is denied x3. No SMB sessions.

Reading: the share is real and independent (other machine, other volume); it is gated to one
identity, and that alone reproduces every 09-13 symptom seen from SKULLPORT (stale mapping,
"does not exist" to an unauthenticated probe, net view 1702, port 445 open). Nothing changed:
granting share access or handing out that account's credential is the operator's. The operator
decision is now one line (map as SPECTREX5\prometheus by IP, or Grant-SmbShareAccess). The mirror
itself still runs FROM SKULLPORT because that is where the bodies are.

## Unit 2 -- manifests (TECHNE-77)

`python -m comms.manifest write` then `verify` on prompts/2026-09-13_batch09, 10, 11, 12: RC 0 x4.
Hashes b7e70d4f.., 2c28f471.., 8b7ae291.., 77312168.. each equal the git blob
(`git show origin/main:<path> | sha256sum`) and the journal stamps at 09-13 lines 186/284/385/464.
Each MANIFEST.md says it was issued 2026-09-16, three days late. The batch 09-12 charters were
never unverifiable -- the stamps were in the journal -- but the tool path now works.

## Unit 3 -- RQ-4 on run receipts (TECHNE-76)

- techne/fossils/vault.py: getenv() records the NAME of every environment variable the package
  reads (ENV_READS); the one existing read (TECHNE_FOSSIL_VAULT) goes through it.
- techne/fossils/harvest.py: RUN_RECEIPT_SCHEMA = techne.fossil.run_receipt/2;
  pip_freeze_lines() from importlib.metadata (sorted, de-duplicated name==version);
  environment_fingerprint() = interpreter {implementation, version, executable_basename},
  pip_freeze_sha256, pip_freeze_n, env_vars_read (names), env_vars_exported_to_recipe (BODY,
  HARNESS); validate_run_receipt() returns defects (schema/1 = "predates", never a defect);
  run() fills the block AFTER the run and REFUSES to write a receipt that fails its own check;
  `receipt-check [--out F]` censuses every tracked receipt.
- techne/tests/test_fossil_environment.py, 12 tests, all pass: positive (a planted distribution
  moves the hash), negative (unchanged env agrees), cheat x3 (block missing / "NAME=value" in the
  names list / non-sha256 hash -- each FAILS validation), interpreter-missing fails, legacy /1
  reported not failed, unknown schema fails, source discipline (no os.environ read outside
  vault.py; the two hits under batches/ and a specimen harness are recipe-side setdefault, not the
  driver), census has no defects, end-to-end native run writes /2 and the tmp vault PATH VALUE is
  absent from the written receipt.
- Baseline census techne/fossils/RECEIPT_CENSUS_2026-09-16.json: n=263 carries_rq4=0
  predates_rq4=263 defective=0. Coverage rises only as bodies are re-run on the vault host.
- Sample fingerprint here: CPython 3.14.4, 145 distributions, one env var read.
- Tests on the merged tree: test_fossil_environment + test_fossil_isolation + test_fossils
  391 passed in 5.1 s. Whole techne/tests minus slow: 701 passed / 8 failed / 10 skipped plus 5
  collection errors -- ALL 13 are this host lacking hypothesis, chipfiring and python-sat
  (verified identical with my change stashed: 8 failed in 0.11 s). Not mine; not repaired (they
  are M2 environment debt, and pip-installing into the shared interpreter is not a boot-pass act).

## Not done

- Batch 12 P6 (LISP 1.5 on simh i7094; Spacewar on simh PDP-1): needs the bodies; a re-acquire
  from origin by tree hash on this host would double as a test of the "reconstructible from origin"
  guarantee, and is the next unit if this instance continues.
- Hermes incident file occurrence line (their lane); reported here.

## Operator, 11:5xZ: "M1 is running a different ecosystem for now; SFE runs on M2 now."

Consequence for this lane: every physical asset (121 bodies, 8 world images, the mirror source)
is on M1; the consumers are now here. M2 facts: 0 bodies, no docker (Windows or WSL2), WSL2
Ubuntu 24.04 present, global core.autocrlf=true. Interpreter here lacks hypothesis, chipfiring,
pysat, cvxpy, clarabel, scs, cypari2 (arsenal gaps on M2; not installed by me).

## Unit 4 -- `harvest rematerialize` (committed 662f707fc BEFORE the run)

acquire() is a FIRST-host verb: it rewrites pins. rematerialize is the every-later-host verb:
fetch to a staging dir from the recorded origin, hash, compare to the tracked list; MATCH
installs upstream/, DRIFT keeps the fetch at upstream.drifted/ and installs nothing,
ORIGIN_UNREACHABLE leaves nothing; record.json / UPSTREAM_HASHES.txt never written.
_fetch_artifacts factored out of acquire() verbatim. 8 controls (positive, idempotence, two
cheats -- origin serving different bytes with and without an archive pin -- negative, no-origin,
per-row census flush, acquire unchanged).

RUN 1 over all 121 (techne/fossils/REMATERIALIZE_M2_2026-09-16.json): 109 MATCH, 12 DRIFT,
0 ORIGIN_UNREACHABLE; 1,049 MB fetched in 230 s of fetch time. Every origin still serves.

The 12 drifts, classified file by file against the kept fetch (scratch classifier; results in
REMATERIALIZE_M2_2026-09-16_run2_drift_classes.json):
  RECORD_IS_CRLF   the recorded hash equals sha256(fetched with LF->CRLF). The RECORD is a
                   Windows-converted copy; the fetch is upstream bytes. 11 specimens, all git:
                   avida 2389/2868, c-cmaes 14/19, corewar 15, espresso 252, femtolisp 112,
                   go-explore 92, ldpc 91, libfec 127, pforth 122, picorv32 246, xv6 82.
  RESIDUE_IN_RECORD the hash list names build products the origin never had (.o, .a, .pyc,
                   binaries, pforth.dic/pfdicdat.h): corewar 4, espresso 50, femtolisp 24,
                   go-explore 4, pforth 40 -- bodies hashed AFTER an in-place build (pre-09-12).
  FETCH_SMUDGED    lapack-reference 70/6776: the reverse -- record LF, fetch CRLF. MY defect:
                   vault.git_pin bound -c core.autocrlf=false to `git clone` only; the fetch +
                   checkout of a pin that is not default HEAD re-smudged the changed files under
                   the host's autocrlf=true. Fixed: the clone's LOCAL config now persists
                   autocrlf=false / eol=lf and fetch/checkout carry the flags.
                   techne/tests/test_fossil_git_pin_eol.py: 4 tests; with the fix stashed 3 of
                   4 FAIL (cheat, host-independence, persistence) -- the control bites.
RUN 2 after the fix (REMATERIALIZE_M2_2026-09-16_run2.json): 110 ALREADY_PRESENT_VERIFIED
(lapack re-fetched: MATCH, 6776/6776, verify True), 11 DRIFT, all RECORD_DEFECT_ONLY.

Reading. The guarantee "reconstructible from the recorded origin + verifiable by hash" held for
110 of 121 on a second host; it FAILED for 11 and in every one of those the origin is fine and
the RECORD is wrong (a CRLF-converted, sometimes build-dirtied copy was hashed on M1). Those 11
bodies on M1 are the Windows copies; their receipts were run on those copies. The repair is a
re-pin with the old hash list kept beside as superseded -- a tracked-file write, done as its
own unit, not silently.

NOT done: docker in WSL2 (asked; host change), P6 simh, the re-pin (next).

## Unit 5 -- TECHNE-80: the 11 CRLF records re-pinned (instrument ac032a0f4, before use)

`harvest repin` classifies every recorded file against the byte-exact fetch and repairs ONLY
when the whole drift is RECORD_IS_CRLF + NOT_IN_ORIGIN (0 ADDED, 0 FETCH_IS_CRLF, 0 OTHER);
4 controls incl. a real edit hidden under the CRLF costume (REFUSED) and a file the record
lacks (REFUSED). Dry classification of the 11 first: all eligible, 0 OTHER, 0 ADDED over
3,586 recorded files. Then the 11 repins, one tracked receipt each
(specimens/<id>/receipts/repin-<id>-20260916T*.json), old hash list kept beside as
UPSTREAM_HASHES.superseded-20260916.txt, record.hashes_superseded carrying the old block,
reason and receipt path. Counts: avida 2389 CRLF; c-cmaes 14; corewar 15 + 4 residue;
espresso 252 + 50; femtolisp 112 + 24; go-explore 92 + 4; ldpc 91; libfec 127; pforth
122 + 40; picorv32 246; xv6 82.
After: `harvest verify --all` on M2 = 121/121 match, 0 differ, 0 missing
(VAULT_INTEGRITY_M2_2026-09-16.json). M2 is now a complete, byte-exact second host.
CONSEQUENCE, stated: on M1 these 11 bodies are the CRLF copies and now fail verify; every
receipt before today for them ran on that copy (its tree hash is in hashes_superseded). On M1
`rematerialize` will report them ALREADY_PRESENT_DRIFTED (upstream/ is present there); the
repair on M1 is delete upstream/ then rematerialize -- NEVER `restore`, which would put the
superseded bytes back. Noted in TECHNE-80.

## Loop tick 1-2 -- TECHNE-81 docker on M2, and P8 measured for real (instrument 5f2fc3f31 before use)

Operator "Yes. Loop. Keep exploring." read as approval of TECHNE-81. docker.io 29.1.3 installed
inside WSL2 Ubuntu 24.04.5 via `wsl -u root` (sudo needs a password; root via wsl.exe does
not), `systemctl enable --now docker`, user added to the docker group; hello-world ran;
control images removed afterwards.

techne/fossils/worlds.py: `build` census + `probe-compare`; 5 controls (build cheat: a
nonexistent package -> BUILD_FAILED with the apt error; build positive: dpkg manifest sha256
identical across two builds; probe cheat: a receipt claiming "Windows 3.1" -> PROBE_DIFFERS;
probe positive; reference integrity). harvest._shell gains readonly= (unused by the final
probe path; kept).

WORLD BUILD (techne/fossils/WORLD_BUILD_M2_2026-09-16.json): 8/8 BUILT in 26-68 s each,
0/8 same image id as M1 (as classified), base debian:bookworm-slim resolves today to
...fe64d6990e4b07ea4171, i386 ...78d2fedc3aeb9d100e46, python:3.7 ...e4a308ad86c001cf028b;
dpkg manifests recorded (191/199/322/179/158/353/197/206 packages) -- M1 never recorded its
manifests, so the package-level comparison starts from today.

PROBE COMPARE (WORLD_PROBE_COMPARE_M2_2026-09-16.json): the 92 docker-runner specimens'
93 probe commands re-run in the rebuilt worlds and diffed against each specimen's latest M1
receipt: 89 IDENTICAL, 4 DIFFER, and every one of the 4 is a HOST fact the probe happened to
print (nproc 16 -> 28 x3; uname -r 6.6.87 -> 6.18.33 WSL2 kernel). Every toolchain version
string -- gcc/g++ 12.2.0-14+deb12u1, glibc 2.36-9+deb12u14, gfortran, iverilog, yosys,
octave, gnat, gnucobol, sbcl, guile, python, go, simh a1f57fa3 -- is byte-identical.
First pass had 21 IMAGE_MISSING_HERE (registry tags python:3.11-slim x15, python:2.7-slim,
golang x2, gcc:4.9 never pulled here) -> pulled, re-run, 88+4. A harness defect found and
fixed on the way: probes were run on a read-only mount of the raw body, so a recipe whose
workdir is build/ (bsd-4.3) could not cd and `cd X && first; rest` silently skipped its first
probe command; probes now run in the same disposable copy `harvest run` stages (control
changed to workdir=build so the staging is exercised).

READING (P8, three days after the images were built): REBUILDABLE_NOT_BIT_EXACT is now a
number -- 8/8 rebuild, 0/8 bit-identical, 71/71 comparable specimens see the same toolchain.
This is a statement about a 3-day gap on one distro snapshot; it says nothing about a year.
The package manifests recorded today are what the NEXT rebuild is compared against.
Side finding: 4 recipes print host facts (nproc, uname -r) in their toolchain probe; those
probes measure the host, not the world. Not changed (recipes are preserved bodies' adjuncts;
a mask in probe-compare would hide the fact). Noted for the next recipe review.

## Loop tick 3 -- TECHNE-82/34: the committed locks reproduced on M2 (instrument afa345adb before use)

techne/scripts/reproduce_locks.py (every-later-host verb; acquire re-resolves and REWRITES
locks, this never does): --require-hashes --no-deps from the committed file, interpreter-tag
refusal, import verification, a tampered-hash cheat control on every run. paths.tool_cache()
now defaults under the CANONICAL checkout (it defaulted under the task worktree). Offline
test caught a vacuous green (every lock refused -> 0 == 0 -> "REPRODUCED"); fixed.

RUN 1 under py -3.12 (receipt installation-lock_reproduction-20260916T141254Z): env
h0h5_tools created fresh at D:\Prometheus\vault\techne_tools; hypothesis 6.165.10, pytest
9.1.1, z3 5.0.0.0 REPRODUCED (hashes install, import works, version == pin); pyribs 0.12.0 and
stitch 0.1.29 INSTALLED_IMPORT_FAILS. Cheat control CONTROL_FAILED: the tampered lock
installed rc 0 -- because every package was already present and pip says "already
satisfied" without checking a hash. Instrument defect, mine; fixed with --ignore-installed
on the control (pypi.install_locked gains ignore_installed=).
RUN 2 (receipt ...20260916T141431Z): same 3/2, cheat REFUSED_AS_EXPECTED, status PARTIAL.
Both receipts kept.

WHY THE TWO IMPORTS FAIL -- a HOST POLICY, not the locks: "DLL load failed ... An Application
Control policy has blocked this file". Windows Smart App Control is ON on SPECTREX5
(HKLM\SYSTEM\CurrentControlSet\Control\CI\Policy VerifiedAndReputablePolicyState = 1);
CodeIntegrity/Operational events 3077/3033 at 10:13 name the two blocked files:
  numpy\random\_pcg64.cp312-win_amd64.pyd (numpy 2.5.3, the pyribs lock's closure)
  stitch_core\stitch_core.cp312-win_amd64.pyd (0.1.29)
both "did not meet the Enterprise signing level". The same log shows numba's
_dispatcher.cp314 blocked on 08-31 in the live 3.14 interpreter. So on M2 any wheel whose
compiled extension is unsigned or not yet reputable is unloadable, whatever its hash says.
This is the operator's security posture (Smart App Control can be switched off but never
back on without reinstalling Windows) -> TECHNE-85, operator decision, not touched.
Consequence for the loop: TECHNE-82's original reading ("13 techne tests cannot run on M2
for missing packages") was the label; the property is that the packages INSTALL and their
.pyd is REFUSED at load. The tests would still fail after a pip install.

## Loop tick 4 -- batch 12 P6: LISP 1.5 (1962) toward execution -- WORLD RECONSTRUCTED, BODY NOT ASSEMBLED

DEMAND: lisp-1-5-ibm7090-1962 (Bourguignon's card-deck transcription of the 7090 LISP 1.5
assembly; Makefile names asm7090 2.2.2, lnk7090 2.1.3, utils 1.0.5, s709; README says the
deck assembles with asm7090 2.1.4 + the body's asm7090.patch). FIND: David G. Pitts'
IBM 709x toolchain at https://www.cozx.com/dpitts/ibm7090.html, current releases asm7090
2.3.2, lnk7090 2.2.3, utils 1.1.15, s709 2.4.4 (MIT, LICENSE.txt in each tarball, (c) 2024).
PIN: sha256 of the four tarballs in techne/fossils/environment/fossil-ibm709x-toolchain.Dockerfile
(d60cdd5b.., d421dac5.., 04372a48.., 86d3f896..). EXECUTE: image prometheus-fossil-ibm709x:bookworm
built on M2 (asm7090, lnk7090, s709, obj2bin, txt2bcd, bcd2txt, ... in /usr/local/bin).
CONTROL: the body's own Makefile step `lisp15.src` (cards padded to 80 columns) runs; asm7090
2.3.2 assembles the 11,114-card deck and reports 752 errors, 0 warnings, lisp15.out written.
The errors: 741 "Undefined symbol" + 11 (ISS)/(IUU); every undefined name is a $-prefixed
cross-namespace reference ($ERROR, $CONS, $ZERO, $PRINT ...) and the assembler reports the
name with its characters SHIFTED (ERROR->EROOR, ZERO->ZROO, CONS->CNSS, SAVE->SVEE), i.e.
2.3.2 mis-parses the `$label` notation the README documents for the 2.1.4-era assembler.
Option sweeps (-f on/off, -m, -j, -x, -w) leave the count at 752 (983 without -f). The older
asm7090 releases are not served on cozx.com; the Wayback Machine was OFFLINE ("Internet
Archive services are temporarily offline") at 14:25Z, so the 2.2.2 / 2.1.4 pins could not be
recovered this tick. RECEIPT: recipe.json written for the world (probe / cards-to-src /
assemble / a wang.job run stage that is written but UNEXERCISED); `harvest run` receipt
run-lisp-1-5-ibm7090-1962-20260916T142124Z.json (the first schema/2 receipt: carries the RQ-4
block), classification BLOCKED_DEPENDENCY, body preserved. Record.environment annotated with
world_status WORLD_RECONSTRUCTED_BODY_NOT_ASSEMBLED. WORLD_IMAGES_M2_2026-09-16.json captured
(a host-suffixed snapshot; the 09-13 file keeps M1's identities).
P3 vocabulary: BODY_RECOVERED_WORLD_NAMED -> WORLD_RECONSTRUCTED (toolchain exists and runs),
not WORLD_EXECUTABLE (the deck does not assemble in it), not BEHAVIOR_REPRODUCED.
Next: (a) Wayback for asm7090-2.2.2 / 2.1.4 when it is back; (b) failing that, a small patch
to 2.3.2's `$` handling, kept beside the body's own asm7090.patch as scaffolding, never in the
deck; (c) Spacewar! on simh pdp1 (the other P6 body). NOT done: lnk7090 was not needed
(absolute assembly); s709 was only probed, never run with a tape.

## Loop tick 5 -- batch 12 P6: Spacewar! 2B (25 Mar 1962) RUNS in emulation; LISP 1.5 still blocked

Wayback retry for asm7090-2.2.2/2.1.4: back up but HTTP 429 on every query at 14:49Z; a Fedora
COPR (sharkcz/danny) packages only 2.3.2. LISP 1.5 stays TECHNE-86.

PDP-1 WORLD (techne/fossils/environment/fossil-pdp1-toolchain.Dockerfile ->
prometheus-fossil-pdp1:bookworm): open-simh `pdp1` at the same pinned commit as the VAX world
(a1f57fa3, headless, no Type 30 display) + Bob Supnik's MACRO-1 cross assembler from
simh/simtools (crossassemblers/macro1, commit 2d9a2d96) + python3 for the harness.

THE EXPEDITION, in the order the errors fell (macro1 -r on the raw transcription: 151 errors):
  151 -> 111  T3  the source's single ONE-LINE `define<TAB>mult Z ... term` (line 90) makes
              macro1 swallow the next ~200 lines as macro body; expanded to multi-line.
  111 ->  21  T4  the assembler's SYSTEM macros (swap, ioh, spq, count, index, setup, init)
              are not in the 1962 text -- they were on the MACRO FIO-DEC system tape. The
              nearest surviving copy (June 1963, masswerk, sha256 8ff11791..) is inserted
              after the title line; T5 an `init` -> `initialize` alias for the spelling the
              1962 program uses. (Later found unnecessary in principle: macro1 folds
              undefined names to a macro by 3-char prefix -- kept, exact match is cleaner.)
   21 ->  12  T7  the STAR TABLES (labels 1j..4q, Peter Samson's "expensive planetarium")
              are a separate tape, absent from the 25 Mar text; appended from Landsteiner's
              2014 RECONSTRUCTION spacewar_2b_2apr62.txt (sha256 8e60bd3a.., "not an
              authentic source code" by its own header) -- two provenance grades, named.
              Plus the two title lines that became code, commented.
   12 ->   0  T6  one `ranct \ran, sar 9s, sar 4s, \src` call expanded by hand (macro1 splits
              macro arguments on spaces); T8 `\ran` -> `\qran`: macro1 resolves an UNDEFINED
              symbol to any macro sharing its first three characters (lookup(), "MACRO returns
              last defined n-x match") and a `\variable` is undefined until `variables`, so
              `\ran` became the macro `ranct`/`random` ("misplaced symbol" x5). Reproduced in a
              6-line isolate before touching the harness.
  Measured and NOT applied: `define` indentation, macro-name indentation, terminate->term
  (macro1 accepts all three; error count unchanged).
RESULT: sw2.rim 14,026 bytes sha256 ddca90afda897a9675cf0fd8926dc68adc926fb48cfe7e2e2a06ecf2c9723b86.
Loaded in simh pdp1: word 6 = 720004 = `tyi` (PDP-1 manual opcode, an oracle the assembler did
not supply); 2000 steps run (PC 0504); then "Infinite I/O wait state, PC 003774 (IOH)" -- 3774
is NOT in the listing: it is code the program's own OUTLINE COMPILER wrote at run time
(`plinst (ioh`), i.e. the 1962 program generated its display routine and executed it, then waited
on the display completion pulse that a scope-less machine never sends. The historically correct
behaviour. Receipt run-spacewar-pdp1-1962-20260916T150305Z.json RUNNABLE_EMULATED, body
preserved (schema/2, RQ-4 block). Cheat control (manual, not a receipt): the source truncated at
line 1300 -> "1 detected error", no tape, pdp1 "File open error" -- the gates see failure.
Vocabulary (charter P3): BODY_RECOVERED_WORLD_NAMED -> WORLD_EXECUTABLE. Not BEHAVIOR_REPRODUCED:
that needs a display build (simh USE_DISPLAY / SDL under a virtual framebuffer) -> TECHNE-87b.
1960s runnable count: was 0 of 11 pre-1970 bodies runnable; now 1.

## Loop tick 6 -- TECHNE-87b: Spacewar! draws (BEHAVIOR_REPRODUCED)

fossil-pdp1-display.Dockerfile = the pinned pdp1 world + libsdl2-dev (open-simh's makefile
compiles its video layer and the Type 30 DPY device when SDL2 is present) + xvfb/xauth +
imagemagick -> prometheus-fossil-pdp1:display. `show dpy` now answers (was "Non-existent
device"). Under `xvfb-run -a -s "-screen 0 1024x1024x24" pdp1 run_display.ini` (set dpy
enabled; load sw2.rim; 2000 + 300k + 300k steps; screenshot after each 300k): the program no
longer stops at IOH; PC 002110 (JDA 233) then 004044 (IOH) still running; two 512x512 frames.
Looked at frame2 with my own eyes: the heavy star (asterisk) at centre, the wedge at lower
left, the needle at upper right. harness/frame_metrics.sh gates declared before the run:
lit pixels in [20, 20000] = FRAME_HAS_CONTENT (85 and 120), sha256 differ = FRAMES_DIFFER.
Recipe: run 1 keeps the headless world's behaviour as the NEGATIVE control (DPY disabled ->
"Infinite I/O wait state (IOH)"); run 2 the display. Receipt
run-spacewar-pdp1-1962-20260916T151039Z.json RUNNABLE_EMULATED ok, body preserved. Frames
kept: specimens/spacewar-pdp1-1962/behaviour/frame{1,2}_20260916T151039Z.png (312 / 340
bytes; sha256 5357da5c.. / b9b95e14.. match the receipt's produced list).
Frame bytes VARY between runs (my manual run gave a different frame2 sha with 120 lit
pixels too): the program's random-walk state differs per run; the behavioural coordinate is
the lit-pixel band and frame-to-frame change, not a fixed bitmap. Said in the record.
P3 vocabulary: WORLD_EXECUTABLE -> BEHAVIOR_REPRODUCED. Batch 12's "DID ANY NEW PAIR PRODUCE
A BEHAVIORAL DATASET?" now has one behavioural coordinate on a 1962 body; a PAIR still needs
a second body (masswerk's own reconstructions, or Spacewar 3.1 / 4.x) -- batch 13 material.

## Loop tick 7 -- TECHNE-86: LISP 1.5 ASSEMBLES; the 752 errors were the assembler's bug, not the deck's

Root cause, found by reading asm7090 2.3.2's scanner (scanner.h, cases 3/12 "qualified symbol"):
    iiicp = strchr(symbol, '$'); *iiicp = 0; iiicp++;
    strcpy (qualsym, symbol);  strcpy (symbol, iiicp);      <- overlapping strcpy
Overlap is undefined behaviour; glibc 2.36's strcpy shifts the tail, which is exactly the
ERROR->EROOR / ZERO->ZROO / CONS->CNSS mangling: every `$label` (741 of them) was looked up under
a wrong name. It "worked" on the author's older libc. Patched to memmove in the WORLD (Dockerfile
sed at build; patches/asm7090-2.3.2-overlapping-strcpy.patch carries the text and the evidence;
python3 added to the image for the harness). No older asm7090 needed after all; Wayback (429)
becomes irrelevant. The deck assembles: 0 errors, lisp15.out 153,900 bytes.
ORACLE: the body ships lisp15.lst, Bourguignon's listing from 2.1.4 + his patch. harness/
compare_listing.py compares (location, generated word) pairs: 5793 vs 5793; 5752 identical;
38 where the reference printed 00000 for a cross-header symbol (its README says 2.1.4 "prints
'0' in the generated words for symbols under different headers") and this world resolved it;
3 DISAGREE: 00353 WEF WEFA 01220 vs 01200, 00355 BSR BSRA 01220 vs 01200, 00731 XEC $RDS 77666
vs 00350. Which side is right is decidable only against the 1962 listing PDF in the body
(LISP1.5-Bonnie-sBirthdayAssembly.pdf); not done this tick; the test gate FAILS until it is.
RUN: obj2bin -> tape; txt2bcd wang.job -> card tape; s709 -m7090 with every unit the Makefile
names; `lt B7` does nothing, `lt 27` (channel B unit 7 as digits) loads and transfers: PC 01002
= inside the deck's card-reading routine (B8/B9, "IS END OF RECORD INDICATOR ON"). No printer
output. s709 core-dumps if stdin is /dev/null without -c (recorded). The 1962 self-loading
tape's operating convention on s709 2.4.4 (IBSYS-mode default) is the open question.
RECIPE gates (declared): assemble = ASSEMBLED_0_ERRORS; test = LISTING_WORDS_MATCH (fails, 3
words); run = TAPE_LOADED_AND_TRANSFERRED and JOB_PRODUCED_OUTPUT (the second fails).
Receipt run-lisp-1-5-ibm7090-1962-20260916T151959Z.json BUILDS_BUT_NOT_RUN /
TECHNE_SMOKE_HARNESS_FAIL, body preserved. Two earlier receipts from this tick were DELETED
before commit (151921Z: my gate looked for a summary line asm7090 does not print at 0 errors;
151940Z: my run gate called "loader transferred" RUNNABLE, an overclaim) -- they measured my
gates, not the deck; the record's receipt list was edited to match. Said here so the deletion
is on the record.
P3 vocabulary: WORLD_RECONSTRUCTED -> WORLD_EXECUTABLE + BODY ASSEMBLES; not BEHAVIOR_REPRODUCED.

## Loop tick 8 -- the 3 disputed LISP 1.5 words adjudicated against the 1962 listing: this world is right, the 2.1.4 reference was wrong

The scan LISP1.5-Bonnie-sBirthdayAssembly.pdf (in the body, 224 pages) carries an OCR text
layer (pypdf) and its PAGE numbers match the transcription's PAGE comment cards. Page 10:
"00352 0772 00 0 01200 REW REWA", "C0353 0770 00 0 01200 264 WEF WEFA", "00355 0764 00 0 01200
266 BSR DSRA"; page 16: "00731 0522 00 0 00350 529 XEC bRDS SELECT TAPE". Rendered both pages
(pdftoppm in a throwaway container, body mounted read-only) and read them by eye: 01200,
01200, 00350 -- exactly this world's words; the reference listing's 01220 / 01220 / 77666 were
2.1.4's errors. (The 1962 listing distinguishes RDS/WRS = 01220 from REW/WEF/BSR = 01200.)
harness/reference_errata.json records the three with page, card, OCR line and both values;
compare_listing.py now accepts IDENTICAL / COMPLETED (ref 00000) / ERRATA (must equal the 1962
word) and fails on anything else. Result: 5793 words, 5752 identical, 38 completed, 3 via
errata, 0 differ -> LISTING_WORDS_MATCH. Cheat: one altered word (00731 -> 00351) -> DIFFER, rc 1.
Receipt run-lisp-1-5-ibm7090-1962-20260916T152519Z.json: BUILDS_BUT_NOT_RUN,
TECHNE_SMOKE_HARNESS_PASS, run stage still NO_PRINTER_OUTPUT.
So the assembled tape is, word for word, the 1962 program (plus the loader region, which the
2.1.4 reference also covers). What remains for BEHAVIOR_REPRODUCED is only the s709 boot
convention. Also noted: the transcription's own README asked for exactly this check ("get a
listing as identical as possible") and it now has a tool for it.

## Loop tick 9 -- the WORLD runs LISP 1.5; the 1962 deck's tape still does not boot; loop stops

s709-2.4.4's tarball ships a working LISP 1.5 operating setup: runlisp, lisp.cmd (`sw3`, `lt a4`),
LISPTAPE.BIN (195,992 B, dated 2008, provenance not stated), fact.lsp / ffact.lsp / propcal.lsp
(the Wang propositional calculus -- the same algorithm as the deck's wang.job). Under those
conventions (a1 sysin, a2 sysppt, a3 sysout, a4r system tape, b3 systmp; job = TAPE cards +
TEST + program + STOP + FIN) his tape prints "EVALQUOTE OPERATOR AS OF 1 MARCH 1961",
FACTORIAL (10) -> 3628800, and propcal traces TH1/TH2 *TRUE* to END OF LISP RUN. Now the
recipe's WORLD POSITIVE CONTROL (harness/world_control_pitts_lisp.sh; material installed in the
image under /usr/local/share/ibm709x/lisp/). It proves the emulator and the conventions; it
says nothing about the body's tape.
The body's tape under the same conventions: `lt a4` leaves PC 00001 (nothing boots); `lt 14`
gives PC 72560 for BOTH tapes (an s709 path I did not chase). Structure: LISPTAPE.BIN =
[1482-byte bootstrap record][194,508-byte core image][2 marks]; ours = ONE 56,598-byte
scatter-load record (obj2bin, BOM/IOCP framing). The 1962 deck was loaded by its own card
loader (LOAD at 77724) and then SET UP into the running system that a system tape captures;
obj2bin's output is the object, not that image. How LISPTAPE.BIN was produced is not in the
tarball. That is the whole remaining gap for BEHAVIOR_REPRODUCED, and it is a research step
(Pitts' site / the CTSS kit notes / the deck's own SETUP path), not an afternoon.
Receipt run-lisp-1-5-ibm7090-1962-20260916T153230Z.json: BUILDS_BUT_NOT_RUN,
TECHNE_SMOKE_HARNESS_PASS (world control + listing words), run FAIL (NO_PRINTER_OUTPUT).
One receipt (153158Z) deleted before commit: it measured a Dockerfile edit that had not
applied (the control files were absent), not the deck.
HARVEST_QUEUE candidate noted: Pitts' LISPTAPE.BIN as a specimen of its own (a 2008
redistribution / LATER build of LISP 1.5 with unknown build path) -- valuable as the pair for
this body once its provenance is asked of him.
LOOP STOPS here on its own rule: every remaining item is the operator's (TECHNE-65 share
access, TECHNE-85 Smart App Control, a batch 13 charter) or a research step (TECHNE-86 SETUP).

## Operator: Mechanism Archaeology Pipeline -- Founding Charter (in chat, "comment on this concept") and Amendment 2

My comment (chat, 16:0xZ) argued: world reconstruction is homeless between Techne and Harmonia;
scaffolding needs a ledger not a prohibition; oracles have provenance grades; the reproduction
state is a lattice; tensor `w` must be measured not named; the runtime is a capability matrix;
search recovery PROJECTS; C1 needs physical preservation; verify the instrument first; the
Harmonia seat collision; pre-register the kinship wager. Amendment 2 adopted all of it (R16-R30,
C11). Both texts committed verbatim under prompts/2026-09-16_amendment2/ with a MANIFEST
(OPERATOR.md sha256 815e84b3...). Amendment 1 never reached this seat -- said in RESPONSIBILITIES.

LAUNCH STEP 1 (Techne, gzip) DONE: techne/fossils/packet.py + FOSSIL_PACKET.json for
gzip-1.2.4-1993 = FOSSIL-GZIP-001, every R30 key present from birth (Techne's filled,
downstream ones null with owner). FOSSIL_RAW_ID d89ed3f8.. verified on M1 (09-13) and M2
(today, re-fetched from origin); FOSSIL_WORLD_ID fw-01f8b51f479199e106bb85ed = sha256 over
(Dockerfile sha256, dpkg manifest sha256, toolchain probes) -- two image witnesses (M1
faeec837, M2 5d339744); scaffolding ledger: ONE applied transformation (build flags -O1
-fcommon -std=gnu89 -w, reversible, no source change), MEASURED_AND_REJECTED empty and SAID
to be empty for lack of a record, not for lack of trials; C11 controls named; TECHNE_STATE
BODY_EXECUTABLE (not rounded: no contemporary oracle attached); REQUIRED_STATE_FOR_HANDOFF
BODY_EXECUTABLE (Nyx Stage C + Harmonia R1 need an executing, intervenable body; the oracle
is what R1 builds); R25: two VERIFIED copies in distinct failure domains + live origin.
Validator cheats (8 tests): round-up without evidence, state below handoff, image digest as
world id, one failure domain, missing downstream key, absent rejected-list -- all refused.
M2 receipt run-gzip-1.2.4-1993-20260916T162024Z (schema/2): ROUNDTRIP OK, gzip -9 21,267 B,
body preserved.
