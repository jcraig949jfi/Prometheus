# Techne journal -- 2026-09-12

Currency: 2026-09-12. Instance m1-8c399e8f (one instance running). Boot per roles/base-role:
fetched, recorded origin/main 3f31cbcac, worktree F:\Prometheus-worktrees\techne-pass-0912 on
techne/pass-2026-09-12 (git-dir != git-common-dir), `python -m comms boot Techne --model
claude-opus-5`. Sync: 1 new (Archaeon msg 213, ruling, copy: c07 RESIDUE / NO CURRENT CONSUMER;
c01 DUPLICATE_CAPABILITY at K=1; nothing for Techne to do), queue length 0. Monitors: Techne owns
no row in roles/base-role/MONITORS.md; the harvest is operator-fed batches, not a scheduled loop,
so there is no dormancy to expose yet (a `verify --all` at each batch start is the closest thing,
and is now written into HARVEST_QUEUE.md).

The 2026-09-11 journal ran through unit 7 (batch 03) and the batch 04 commit (4b251a7b0) landed
without a journal unit. Unit 8 below is that backfill, written from the commit and the Nyx report,
not from recall.

## Unit 8 (BACKFILL): fossil harvest batch 04 -- machinery under pressure (landed 4b251a7b0)

Operator charter verbatim at roles/Techne/prompts/2026-09-12_batch04/OPERATOR_CHARTER.md (+
MANIFEST). 16 specimens, 14 runnable, 2 SOURCE_ONLY by build weight (linux-tcp-congestion kernel
modules; do-mpc CasADi/IPOPT), pinned + tree-hashed with no fake receipt. Vault 57.

    contention    willemt-raft (157 CuTest assertions), linux-tcp-congestion (SRC; CUBIC+BBR pair)
    control       arduino-pid (converge + destabilise receipt), python-control (LQR), do-mpc (SRC)
    estimation    simple-kalman, filterpy (adversarial pair noise<->estimator), viterbi-hmm
    memory        lru-cache (evict + thrash receipt), buddy-alloc (coalesce)
    hardware      verilog-rr-arbiter, verilog-generic-fifo, verilog-uart2bus (iverilog)
    connectionist genann (XOR backprop), minisom (SOM), hopfield (associative recall)

Deviations recorded per specimen (Arduino.h shims; behavioral generic_dpram + bounded SC
testbench for the FIFO; observation testbench for the arbiter; CLinkedListQueue vendored as a raft
companion; python-control/filterpy via PYTHONPATH; hopfield runs the repo's own class). Report:
roles/Nyx/INBOX_TECHNE_FOSSIL_BATCH04_2026-09-12.md. Fossil tests 175 passed. The commit says
"8 build-in-place git bodies cleaned back to the pin after runs" -- which is the defect unit 9
measures at vault scale.

## Unit 9: vault integrity -- 23 of 57 preserved bodies had drifted; isolation + restore

First act of the pass, before any batch 05: `harvest verify` over all 57 specimens.

    census (before)   57 specimens: 34 match, 23 DIFFER, 0 bodies missing
                      techne/fossils/VAULT_INTEGRITY_2026-09-12.json (rows per specimen)
    shape             every one: files ADDED under upstream/tree by in-place builds (.o, .lo,
                      .plo, binaries, .pyc; glpk +588, bdwgc +220, gnu-prolog +184, clips +169);
                      0 files REMOVED anywhere; 5 MODIFIED, all configure rewrites (gnu-prolog
                      gp_config.h, gprolog_cst.h, doc/copyright.tex; zlib Makefile, zconf.h);
                      0 CRLF in any modified file
    the label lied    each of those bodies has a run receipt saying PASS; the preservation
                      property was false underneath it (base-role rule 2)

Instrument repair (commit 7c5bfcb08, tests before bodies):
- `harvest run` stages <vault>/<id>/work/ = copy of upstream/ + harness/ + empty build/, runs the
  recipe there (all three runners; $BODY resolves in the copy so no recipe changed), then
  re-hashes the PRESERVED upstream/ and writes tree_sha256_after + body_preserved into the
  receipt; prints BODY PRESERVED / BODY DIRTIED. `"in_place": true` opts out and is labelled.
- `harvest verify --all [--out]` = the census as rows. `harvest restore <id>` deletes added rows,
  recovers modified/missing rows from the body's own git pin (`-c core.autocrlf=false`) or by
  sha256 CONTENT from the archives kept beside the tree, reports UNRECOVERABLE otherwise, and
  writes a tracked restore receipt with the rows.
- techne/tests/test_fossil_isolation.py 8 passed: negative control (clean recipe verified in
  both modes), positive control (in-place build dirties; detector names tree/product.o added
  and tree/hello.txt modified), the fix (copy verified while products exist in work/), fresh
  work per run, restore-by-content, UNRECOVERABLE stays unverified with nothing fabricated,
  no-op restore, census counts. test_fossils.py 175 passed.

Restore, all 23 (receipts techne/fossils/specimens/<id>/receipts/restore-<id>-20260912T1751*Z.json):

    deleted 1518 build products in total; restored_from_git 0; restored_from_archive 5
    (gnu-prolog 3, zlib 2); UNRECOVERABLE 0; every one VERIFIED
    census (after)    57/57 match -- techne/fossils/VAULT_INTEGRITY_2026-09-12_after_restore.json

Positive control on the real vault: re-ran tscp-1.81 (wsl runner) and picosat-965 (docker
runner) under the new isolation -- both PASS their recipes and both print BODY PRESERVED
(receipts run-...-20260912T175210Z / T175221Z). NOT run: the other 55 recipes; the isolation
is proven on two runners and the synthetic controls, not on every specimen.

Docs: HARVEST_QUEUE.md tooling row closed with the rule "verify --all at every batch start";
PRESERVATION.md gains the run-isolation guarantee; BACKLOG rows TECHNE-63 (batch 04 backfill),
TECHNE-64 (this), TECHNE-65 (off-host mirror, XL, operator decision).

Not done: batch 05 (thin axes after batch 04: contention/retry RUNNABLE, Baum-Welch training,
runnable MPC, failure specimens, binary-only path). Not done: any decomposition.
