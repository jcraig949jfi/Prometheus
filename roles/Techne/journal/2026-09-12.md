# Techne journal -- 2026-09-12

Currency: 2026-09-12. Instance m1-8c399e8f (one instance running). Boot per roles/base-role:
fetched, recorded origin/main 3f31cbcac, worktree F:\Prometheus-worktrees\techne-pass-0912 on
techne/pass-2026-09-12 (git-dir != git-common-dir), `python -m comms boot Techne --model
claude-opus-5`. Sync: 1 new (Archaeon msg 213, ruling, copy: c07 RESIDUE / NO CURRENT CONSUMER;
c01 DUPLICATE_CAPABILITY at K=1; nothing for Techne to do), queue length 0. Monitors: Techne owns
no row in roles/base-role/MONITORS.md; the harvest is operator-fed batches, not a scheduled loop,
so there is no dormancy to expose yet (a `verify --all` at each batch start is the closest thing,
and is now written into HARVEST_QUEUE.md).

The 2026-09-11 journal ran through unit 7 (batch 03) and the batch 04 commit (4b251a7b0) landed
without a journal unit. Unit 8 below is that backfill, written from the commit and the Nyx report,
not from recall.

## Unit 8 (BACKFILL): fossil harvest batch 04 -- machinery under pressure (landed 4b251a7b0)

Operator charter verbatim at roles/Techne/prompts/2026-09-12_batch04/OPERATOR_CHARTER.md (+
MANIFEST). 16 specimens, 14 runnable, 2 SOURCE_ONLY by build weight (linux-tcp-congestion kernel
modules; do-mpc CasADi/IPOPT), pinned + tree-hashed with no fake receipt. Vault 57.

    contention    willemt-raft (157 CuTest assertions), linux-tcp-congestion (SRC; CUBIC+BBR pair)
    control       arduino-pid (converge + destabilise receipt), python-control (LQR), do-mpc (SRC)
    estimation    simple-kalman, filterpy (adversarial pair noise<->estimator), viterbi-hmm
    memory        lru-cache (evict + thrash receipt), buddy-alloc (coalesce)
    hardware      verilog-rr-arbiter, verilog-generic-fifo, verilog-uart2bus (iverilog)
    connectionist genann (XOR backprop), minisom (SOM), hopfield (associative recall)

Deviations recorded per specimen (Arduino.h shims; behavioral generic_dpram + bounded SC
testbench for the FIFO; observation testbench for the arbiter; CLinkedListQueue vendored as a raft
companion; python-control/filterpy via PYTHONPATH; hopfield runs the repo's own class). Report:
roles/Nyx/INBOX_TECHNE_FOSSIL_BATCH04_2026-09-12.md. Fossil tests 175 passed. The commit says
"8 build-in-place git bodies cleaned back to the pin after runs" -- which is the defect unit 9
measures at vault scale.

## Unit 9: vault integrity -- 23 of 57 preserved bodies had drifted; isolation + restore

First act of the pass, before any batch 05: `harvest verify` over all 57 specimens.

    census (before)   57 specimens: 34 match, 23 DIFFER, 0 bodies missing
                      techne/fossils/VAULT_INTEGRITY_2026-09-12.json (rows per specimen)
    shape             every one: files ADDED under upstream/tree by in-place builds (.o, .lo,
                      .plo, binaries, .pyc; glpk +588, bdwgc +220, gnu-prolog +184, clips +169);
                      0 files REMOVED anywhere; 5 MODIFIED, all configure rewrites (gnu-prolog
                      gp_config.h, gprolog_cst.h, doc/copyright.tex; zlib Makefile, zconf.h);
                      0 CRLF in any modified file
    the label lied    each of those bodies has a run receipt saying PASS; the preservation
                      property was false underneath it (base-role rule 2)

Instrument repair (commit 7c5bfcb08, tests before bodies):
- `harvest run` stages <vault>/<id>/work/ = copy of upstream/ + harness/ + empty build/, runs the
  recipe there (all three runners; $BODY resolves in the copy so no recipe changed), then
  re-hashes the PRESERVED upstream/ and writes tree_sha256_after + body_preserved into the
  receipt; prints BODY PRESERVED / BODY DIRTIED. `"in_place": true` opts out and is labelled.
- `harvest verify --all [--out]` = the census as rows. `harvest restore <id>` deletes added rows,
  recovers modified/missing rows from the body's own git pin (`-c core.autocrlf=false`) or by
  sha256 CONTENT from the archives kept beside the tree, reports UNRECOVERABLE otherwise, and
  writes a tracked restore receipt with the rows.
- techne/tests/test_fossil_isolation.py 8 passed: negative control (clean recipe verified in
  both modes), positive control (in-place build dirties; detector names tree/product.o added
  and tree/hello.txt modified), the fix (copy verified while products exist in work/), fresh
  work per run, restore-by-content, UNRECOVERABLE stays unverified with nothing fabricated,
  no-op restore, census counts. test_fossils.py 175 passed.

Restore, all 23 (receipts techne/fossils/specimens/<id>/receipts/restore-<id>-20260912T1751*Z.json):

    deleted 1518 build products in total; restored_from_git 0; restored_from_archive 5
    (gnu-prolog 3, zlib 2); UNRECOVERABLE 0; every one VERIFIED
    census (after)    57/57 match -- techne/fossils/VAULT_INTEGRITY_2026-09-12_after_restore.json

Positive control on the real vault: re-ran tscp-1.81 (wsl runner) and picosat-965 (docker
runner) under the new isolation -- both PASS their recipes and both print BODY PRESERVED
(receipts run-...-20260912T175210Z / T175221Z). NOT run: the other 55 recipes; the isolation
is proven on two runners and the synthetic controls, not on every specimen.

Docs: HARVEST_QUEUE.md tooling row closed with the rule "verify --all at every batch start";
PRESERVATION.md gains the run-isolation guarantee; BACKLOG rows TECHNE-63 (batch 04 backfill),
TECHNE-64 (this), TECHNE-65 (off-host mirror, XL, operator decision).

Not done: batch 05 (thin axes after batch 04: contention/retry RUNNABLE, Baum-Welch training,
runnable MPC, failure specimens, binary-only path). Not done: any decomposition.

## Unit 10: BATCH 05 AFTER RESET (operator charter, committed verbatim 9e4e08794)

Charter roles/Techne/prompts/2026-09-12_batch05/OPERATOR_CHARTER.md, sha256 a04c22d5... (MANIFEST).
Three jobs: prove preservation, harvest the thin pressure axes, deepen lineages.

### 1. Preservation (before any acquisition)
    opening gate      `harvest verify --all` = specimens 57 matches 57 differs 0 missing 0
    second source     techne/fossils/SECOND_SOURCE_CHECK_2026-09-12.json -- the two archive-content
                      restorations re-checked against INDEPENDENT origins:
                      gnu-prolog-1.5.0: gprolog.org re-fetch AND ftp.gnu.org mirror both byte-identical
                        to the recorded archive (670642b4...); all 3 restored files found by content
                        in both -> MATCH
                      zlib-1.3.1: GitHub tag re-fetch byte-identical to the recorded archive; zlib.net/
                        fossils/zlib-1.3.1.tar.gz (independent packaging) holds both restored files by
                        content -> MATCH; zlib.net/zlib-1.3.1.tar.gz itself is 404 (moved to fossils/),
                        recorded ORIGIN_UNAVAILABLE for that one URL only
    isolation re-runs 8 more real recipes (linpackd native, tinyscheme wsl, zlib docker, minisat gcc:4.9,
                      corewar py2.7, verilog-rr-arbiter iverilog, hopfield python, gnu-prolog docker)
                      + tscp and picosat from unit 9 = 10 real recipes; every one PASS from a clean
                      body, BODY PRESERVED, verify matches afterwards. gnu-prolog's build is a real
                      41 s WAM->native bootstrap producing 186 files in work/, none in upstream/.
    state dependency  none observed: every recipe rebuilt from the clean copy
    body drift        0 during the batch (every new receipt carries body_preserved=true; closing
                      census below)

### 2. Harvest -- 33 specimens acquired (techne/fossils/batches/batch05{,_recipes,_finalize}.py)
    runnable 30 (+ do-mpc reclassified SOURCE_ONLY -> RUNNABLE_CONTAINER), SOURCE_ONLY 3 (the BSD
    TCP kernel sets), 0 blocked. New world prometheus-fossil-legacy:bookworm (GNAT 12, gprbuild,
    GnuCOBOL 3.1.2, bwBASIC 2.20, SBCL, + Bas 2.5 built from a pinned tarball beside the Dockerfile;
    gnu-smalltalk is not packaged in bookworm -> Smalltalk deferred). Numbers per specimen in
    roles/Nyx/INBOX_TECHNE_FOSSIL_BATCH05_2026-09-12.md; the ones worth the journal:
      lmdb crash: writer _exit() mid-txn -> 1000 entries, key1200 absent, RECOVERY OK
      leveldb crash: 500 acknowledged unsynced puts survive via log replay -> count=2500
      odepack: MF=21 reaches t=4e10; MF=10 dies at t=0.1109 with ISTATE=-1 after 500 steps
      pathfinder: pan: invalid end state at depth 4, errors: 1 (the 1997 reset, in 12 states)
      biriscv npc: 320 branches, bimodal late mispredicts 10, gshare 0
      arbiter: RR 4/4/4/4 vs priority 16/0/0/0
      backoff: 1000 clients, attempt 6: 1 distinct wait without jitter, 839 with
      cjson vs radamsa: 200 cases, parsed 131, rejected 69, crashed 0 (ASan+UBSan)
      compact vs gzip on one 217,301-byte sample: 131,897 (39.3% off) vs 21,267 (90.2% off)
      particles PF vs Kalman RMSE 0.0144; emcee mean err 0.021 / cov 0.028 / acceptance 0.555
      vollib: 196 well-conditioned points round-trip to 9e-10; 60 ill-conditioned identified
      sgp4: 45 tests OK (tcppver.out), accelerated=True; erfa: make check 2/2
      umdhmm: trained scaled log-lik -1030.93 vs generating -1040.12 (0.9%); unscaled = -INF
      do-mpc: |x| 3.58 -> 0.17 by step 39 under the +-0.5 bound, kick at 20 visible and recovered
    Discovery outside HARVEST_QUEUE.md: 25 of 33 (76%; the queue named the axes, not the bodies).
    Small mechanisms (>= 3 required): pybreaker, backoff, tinyekf, forencich arbiter (4).

### 3. Depth
    TCP     4.2BSD (1983, KNOWN-BAD: congestion collapse) -> 4.3-Tahoe (1988, REDESIGN) -> 4.3-Reno
            (1990, SUCCESSOR), 13 netinet files each from unix-history-repo, beside linux CUBIC/BBR
    compression  compact (1980-83 adaptive Huffman, LOSER) -> compress/ncompress (LZW) -> gzip 1.2.4
            (1993, patent-forced REDESIGN) -> zlib 1.3.1; the loser runs only as -m32 -Dvax
    SAT     zChaff 2007 (LOSING RIVAL) + MiniSat 1.14 (PREDECESSOR) beside 2.2.0 and picosat
    ECC     libcorrect (portable SUCCESSOR of the blocked libfec-karn), beside Rockliff RS
    HMM     UMDHMM (train) beside viterbi-hmm-xukmin (decode); Lisp: PAIP programs beside two Lisp machines
    failed branches preserved: 4.2BSD TCP, compact, zChaff, TinySTM (partly superseded design)

### 4. Deviations and honest edges (all in the recipes' notes and receipts)
    - compact-4.2bsd: segfaults unmodified on x86-64 (K&R implicit-int malloc); built -m32 -static
      -Dvax (the source keys byte order on the vax/pdp11 symbols). Exit status 1 after success is
      the 1983 convention; the round trip is judged by cmp.
    - zchaff: -include cstring/cstdlib/cstdio/climits -fpermissive via CFLAGS (2007 C++ vs modern
      libstdc++), no source edited.
    - umdhmm: the mirror's nrutil.c has a macOS include path; a one-line shim header in the harness.
      t2.100.seq carries an R= header the 1.02 reader does not parse (segfault) -> test.seq used.
    - radamsa: the Owl Lisp compiler ol-0.1.19.c.gz is a pinned COMPANION artifact (the Makefile
      would wget it).
    - emcee: SETUPTOOLS_SCM_PRETEND_VERSION=3.1.6 (a tarball has no .git). pyportfolioopt: the PyPI
      sdist ships no tests/resources -> seeded synthetic 3-factor market. python-sgp4: GitHub tags
      stop at v2.14 -> PyPI sdist 2.23 is the origin.
    - vollib: the first oracle (|sigma_iv - sigma| everywhere) was wrong -- 60 grid points have
      vega <= 1e-6 where sigma is not identifiable; the oracle now separates them and adds a
      price round trip (2e-16). do-mpc: the first criterion (<10% by step 19) was unattainable
      under the +-0.5 input bound; moved to the end of the horizon, recorded.
    - tinyekf: the first oracle read ekf.csv (mean-subtracted) and saw a 3 m radius; corrected to
      the raw stdout positions.
    - basic: SUPER STAR TREK parses under neither interpreter; its run status is that refusal.
    - cobol: CBL0001..CBL0012 refused by GnuCOBOL (PROGRAM-ID without period), every -std tried;
      SRCHBIN Not Found (EBCDIC key vs ASCII literal); CBL0014 S0C7 silently computed.
    - NIST COBOL-85 suite: ORIGIN_UNAVAILABLE (nist.gov redirect, Wayback 403, SF 404); the false
      body (HTML) and its specimen dir were removed before any commit.
    - Instrument: recipe commands each run in their own container, so apt/pip and the step that
      needs them must share ONE command (compact, py_recipe fixed accordingly). hash_tree now skips
      unstat-able symlinks (autotools build-aux on Windows). Nested harness files are hashed.
    - Flake: one cJSON ASan run spun 665 s and was killed; the two identical runs before and after
      took <1 s. Not diagnosed; per-command timeouts 120/300 s now.
    - whitakers-words: the Ada tools build; dictionary generation via make hung once for 19 min at
      wakedict (0-byte DICTFILE.GEN) while the same command run by hand finished in <60 s; the Ada
      sorter over the 39k-line stem list is the long pole. Status at commit time in the record.

### 5. Consumer access
    catalog   techne/fossils/catalog.py: `python -m techne.fossils.catalog [--json --out F]` and
              enumerate_fossils(); fields fossil_id, canonical_name, lineage, human_purpose, version,
              era, language, domain, run_status, test_status, source_type, license_spdx, tags,
              tree_sha256, record_path, recipe_path, receipts, body_present_on_this_host, body_path.
              Verified from this host (90 fossils, 79 runnable at that moment, 90 bodies) and with
              TECHNE_FOSSIL_VAULT pointed at an empty dir (90 fossils, 0 bodies): records are
              visible without bodies. Tracked snapshot techne/fossils/CATALOG.json (--no-body-check).
    homonym   the "Archaeon saw ZERO fossils" report is archaeon/fossils.py reading SFE/PEW
              EXPERIMENT fossils from the engine ledger in a pinned worktree with no
              config.local.json (archaeon/docs/OPERATIONS.md) -- a different object from this vault.
              Not a vault defect; not Techne's lane to fix; named in the catalog docstring.
    mirror    `harvest mirror --dest D [--dry-run]` built and tested (copy by tree hash, verify,
              refuse drift, refuse in-repo destination); NOT run against a real destination
              (TECHNE-65, operator). One host remains the preservation dependency.
