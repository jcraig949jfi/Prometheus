# Techne journal -- 2026-09-17

## Shutdown of Techne[m2-04bd52c0] on M2 (operator: "close this agent down on M2")

WHERE I AM. Every unit of 2026-09-16 is on origin/main; last work commit 34cb66fbc (merge
d8e36cc42); nothing uncommitted except this entry. Loop stopped 17:36Z after charter tick 2.
The worktree D:/Prometheus-worktrees/techne-boot-2026-09-16 (branch techne/m2-boot-2026-09-16)
is removed by this pass; the branch was never pushed as a branch (every push was
branch:main fast-forward) and is deleted locally. Stale remote origin/techne/pass-2026-09-12
deleted if merged (checked below in the shell, recorded in the shutdown receipt in chat).

IN PROGRESS (next pass, in order; every row in roles/Techne/BACKLOG_H0H5.md):
  R20   recovery-project search (the batch-13-shaped harvest): Bourguignon, Landsteiner, Pitts,
        CHM SPG, bitsavers, simh/simtools; target list with provenance grades BEFORE any download
        (Donor Foundry: DEMAND -> FIND -> PIN -> EXECUTE -> CONTROL -> RECEIPT -> CONSUME).
  TECHNE-89  packets for the other 118 bodies (3 exist); the scaffold census is their ledger input.
  TECHNE-98  retire the 15 decorative build accommodations as recorded acts (C9), one receipt each.
  TECHNE-86  LISP 1.5: the SETUP step from assembled deck to bootable system image (ask Pitts how
        LISPTAPE.BIN was built; or the deck's own card-loader path).
  TECHNE-93/94  linux-tcp-congestion split; des-reference oracle label (Nyx #310).
  TECHNE-96  Amendment 3 has never reached this seat (Nyx cites R31, R34-R39).
OPERATOR-HELD: TECHNE-65 share access (one line), TECHNE-85 Smart App Control on M2,
  MKW-1 freeze (posted to Theophrastus, #311), a batch 13 charter if wanted.
PILOT: FOSSIL-GZIP-001 packet carries Techne's identities + Nyx's CUT_ID and prediction packet;
  step 3 is Harmonia's (R26). No Techne action pending on it.

FILES THAT EXIST ON M2 ONLY (host-local, gitignored, all RECONSTRUCTIBLE, none unique):
  D:/Prometheus/vault/fossils/            121 bodies, 3.3 GB, re-fetched from origins 09-16 and
                                          verified 121/121; a second copy of M1's vault (11 of the
                                          121 are the byte-exact bodies M1 does NOT have -- M1's
                                          copies of those are the superseded CRLF versions).
  D:/Prometheus/vault/fossils/_donors/ibm709x/   Pitts' four toolchain tarballs + LISPTAPE.BIN
                                          (inside s709-2.4.4.tar.gz) + masswerk's macro library
                                          and Spacewar reconstruction, with SHA256SUMS.txt --
                                          copied out of the session scratchpad at shutdown. These
                                          are the R25 "unique recovery artifacts" whose only
                                          other home is one personal website each; the Dockerfile
                                          re-downloads them by sha256 while cozx.com is up.
  D:/Prometheus/vault/techne_tools/       h0h5_tools venv (py3.12, 533 MB) from the committed
                                          locks; rebuildable by techne/scripts/reproduce_locks.py.
  WSL2 docker images                      11 prometheus-fossil-* worlds (8 rebuilt + pdp1,
                                          pdp1:display, ibm709x), ~9 GB; rebuildable from the
                                          committed Dockerfiles (pins by sha256/commit); their
                                          dpkg manifests and image ids are committed
                                          (WORLD_BUILD_M2_2026-09-16.json, WORLD_IMAGES_M2_*).
  docker.io itself in WSL2                installed 09-16 (TECHNE-81); a host change, not a file.
  Session scratchpad                      C:/Users/James/AppData/Local/Temp/claude/... -- nothing
                                          there that is not in the vault or in git.
NOT on M2 only: every receipt, packet, census, ledger, journal and prompt is in git.
