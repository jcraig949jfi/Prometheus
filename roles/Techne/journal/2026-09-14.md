# Techne journal -- 2026-09-14

## Boot (instance SKULLPORT m1-2104792f, model claude-opus-5)

- `git fetch origin` only; origin/main = 347e5ea4c. Worktree
  F:/Prometheus-worktrees/techne-pass-0914, branch techne/pass-2026-09-14, from that SHA.
- `python -m comms boot Techne --model claude-opus-5` recorded from the worktree.
- Batch 12 (02ffd7603) verified ancestor of origin/main. Charter sha256 773121688f...
  re-hashed from the committed blob: matches the journal stamp. Vault 121 bodies, 1960s 11,
  1950s 3.
- Inbox: #244 and #245 from Rhadamanthus (seen).

## #244 question 2 -- Lean REPL discoverability from a non-canonical worktree

Answer, measured rather than recalled:
- agents/_shared/external_tools/locate.py resolves $PROMETHEUS_EXTERNAL_DEPS -> <worktree>/external_deps
  -> <canonical checkout>/external_deps (parent of --git-common-dir), testing for repl.exe.
- From F:/Prometheus-worktrees/techne-pass-0914 on SKULLPORT: root F:\Prometheus\external_deps,
  repl.exe present; test_03_lean_repl_handshake.py 1 passed in 10.4 s. So consumers do NOT
  need to run from the canonical checkout -- on a host whose canonical checkout holds the build.
- The registry's failing reading names D:\Prometheus-worktrees\rhadamanthus-base-role. D:\Prometheus
  does not exist on SKULLPORT. That reading was taken on another host (m2), whose canonical
  checkout has no build. HOST_LOCAL is the right status; the note "measured on SKULLPORT" in #244
  does not match the path in the row's own error text. Reported, not edited.
- NOT measured: m2's disk. Remedy on any other host is a build there or PROMETHEUS_EXTERNAL_DEPS
  pointing at one; no code change is owed by Techne.

## #245 (RQ-1..19)

Read. Gaps, not instructions. Techne-relevant: RQ-10 (NT-064 fossil isolation battery, NT-072,
NT-075 need author-nominated defect shapes for Keeper controls), RQ-4 (receipts emit interpreter
version / pip-freeze hash / env-var names -- harvest receipts partly do), RQ-11/12 per-row.
Not yet acted on; ranked in the boot suggestions.
