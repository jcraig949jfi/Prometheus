# Techne journal -- 2026-09-13

Currency: 2026-09-13. Instance m1-8c399e8f. Worktree F:\Prometheus-worktrees\techne-pass-0912 on
techne/pass-2026-09-12, base 3f31cbcac, merged forward through the day. Batch 06 (operator charter
roles/Techne/prompts/2026-09-12_batch06/, sha256 f5260cceb8, committed verbatim 9e4e08794-era).

## Batch 06 -- UNLOCK, FAILURE MACHINERY, PRESSURE, CULTURES

Charter's sharper purpose: unlock existing depth, acquire missing failure machinery, add pressure
diversity to deep lineages, enter a few empty cultures. 15-25 new is enough; the OLD-fossils-made-
useful number matters. Opening `verify --all` = 90/90; closing = 105/105, 0 drift.

### TECHNE-65 mirror -- STOPPED, reported
Z: (\\SPECTREX5\prometheus_share) is NOT independently reachable from SKULLPORT today: the mapping is
remembered-not-connected, the UNC does not resolve, and F:\SPECTREX5\prometheus_share is a local
April copy on the vault's OWN volume that a dropped backslash mistakes for the share (SMB port 445
answers; net view fails 1702). Per the ruling: stop, do not fake redundancy. Built instead:
`harvest mirror` (same-volume guard; refuses a destination on the vault's drive), `mirror-verify`
(re-hash mirrored bodies vs records + MIRROR_INDEX), and a destructive-negative control on a
disposable test mirror (3 bodies COPIED_VERIFIED; one byte flipped -> MIRROR_DIFFERS; source
untouched; test mirror destroyed). techne/fossils/mirror/TECHNE65_Z_INDEPENDENCE_2026-09-13.md.
ONE HOST remains the preservation dependency (105 bodies).

### Phase 1 -- UNLOCK
- TECHNE-70 i386 world (prometheus-fossil-i386, a real i386 Debian userland): libfec-karn goes
  BLOCKED_PLATFORM -> RUNNABLE (its own configure selects the MMX/SSE/SSE2 Viterbi; BER 180/409600
  at Eb/N0 3.0 dB, 17163/409600 at 1.0 dB). compact-4.2bsd moved to the same world (no per-run apt,
  no -m32 -static hack).
- TECHNE-69 SIMH world (prometheus-fossil-simh = open-simh vax780 built from source a1f57fa37;
  Debian's 3.8.1 cannot boot the 780 from tape and spins on stdin EOF -- both recorded). Three tape
  specimens preserved (4.2/4.3/Reno; the Tahoe tape is BROKEN in the TUHS archive). Tooling:
  simh_mktape.py, boot42 (Neozeed's 4.2 console boot, sha a7bacc51), stage 1/2/3 expect scripts.
  The 4.3BSD GENERIC kernel of 6 Jun 1986 BOOTS ("4.3 BSD UNIX #1 ... karels@monet"), single-user
  shell reached, xtr restores 317 root files -> RUNNABLE_EMULATED. BOUNDARY: the multi-user install
  (/usr tar-extract from emulated tape) exceeded ~1 h at `mt fsf 3` and was cut; the TCP netstat
  experiment (stage 3, preserved in the harness) is not reached in-budget (TECHNE-69b: prebuilt
  disk or faster host). 4.2 and Reno tapes preserved+hashed, same procedure, not individually booted.

### Phase 2 -- REAL CONCURRENT FAILURE (3, all executing)
go-runtime-deadlock-fixtures-1.22 (SimpleDeadlock/LockedDeadlock/LockedDeadlock2/GoexitDeadlock/
GoexitExit -> "all goroutines are asleep - deadlock!", exit 2; MainGoroutineID panics = the
not-a-deadlock control). valgrind-helgrind-fixtures-3.19 (tc01/tc05 race, tc13_laog1 lock-order
inversion, tc09 bad unlock; native run exit 0 nothing visible, under helgrind the planted error is
reported; .stderr.exp the oracle). glibc-rwlock-writer-starvation-2.36 (default PREFER_READER: 1
writer acquisition under 8 readers/3s; PREFER_WRITER_NONRECURSIVE: 4,950,536 -- the documented
starvation, measured).

### Phase 3 -- DISTRIBUTED SURVIVAL (4, four assumptions)
dmtcp-3.1.2 (crash-only checkpoint/restart: counter killed at 8 resumes at 7; the uid-mismatch on
the mounted work/ fixed by a container-local dir). hashicorp-memberlist-0.5.4 (asynchronous SWIM
gossip failure detection; probe/suspect/dead tests pass in golang:1.24). redlock-py-redis-2014
(lease mutex; mutual exclusion while valid OK, and TTL<work -> two holders = Kleppmann 2016,
observed). cocagne-plain-paxos (quorum consensus; 214 tests OK in python:2.7).

### Phase 4 -- ADAPTIVE CONTROL (3; new Octave world)
pid-autotune-hirschmann (Astrom-Hagglund relay autotune: two kettles -> different Ziegler-Nichols
gains, Kd 169 vs 69). indirect-self-tuning-regulator-liaosteve (RLS + pole placement: estimate
within 0.033 by sample 20; end-of-run drift to 0.0997 = covariance wind-up under lambda 0.8, kept
as data). l1-adaptive-control-basics-xkhainguyen (L1 architecture: estimates the d=100 pulse at
98.02). Octave plotting shadowed by harness no-op stubs; a runner evals the L1 script's trailing
function block first (MATLAB-vs-Octave order); .m sources untouched.

### Phase 5 -- INFERENCE
libdai-mooij (belief propagation vs exact JTree on the sprinkler network -- LOOPY, so BP matches
Cloudy/Sprinkler/Rain exactly and differs on WetGrass 0.5985 vs 0.6471, the documented inexactness).
particles PMMH added (OLD FOSSIL MORE USEFUL: particle MCMC over stochastic volatility; chain moves,
acceptance 0.38; parameter recovery not asserted at 400 iters).

### Phase 7 -- CULTURES (1 of 3)
gnu-apl-2.0 (array language; +/iota 100 = 5050 and the 5x5 outer-product table, no loops). Smalltalk
not in bookworm; Icon/Erlang/gforth/SWI apt-ready; SNOBOL source -- all still absent (honest).

### Phase 8 -- PRESSURE (old fossils, behavioral spread)
SAT: 7-regime workload x picosat + minisat-2.2.0; agree on every result; effort 0..2519 dec / 0..3863
conflicts; PHP(7,6) and 150-var transition are the hard points. COMPRESSION: gzip/zlib/bzip2/ncompress
x text/random/repeat/zeros; all round-trip OK; ncompress (LZW) EXPANDS random to 1.27x while gzip/zlib
~1.0, bzip2 ~1.006; bzip2 best on text. techne/fossils/pressure/{SAT,COMPRESSION}_PRESSURE_2026-09-13.json.

### CATALOG improvement
catalog.py now exposes without the body: ancestry, human_environmental_pressure, observability,
oracle_backed, intervention_ready, known_historical_disposition (from tags), mirror_available (from
a configured off-host MIRROR_INDEX; no_mirror_configured until TECHNE-65 lands). CATALOG.json = 105
fossils, 95 runnable.

### NOT done / boundaries (honest)
Phase 6 scientific extremes: 0 new (still only erfa + sgp4, both astro). Phase 9: no dedicated new
bad-design trio (redlock contested + the batch-05 losers carry dispositions). Phase 10 binary: NOT
attempted this pass. Phase 11 small-strange: NOT done. TCP multi-user/experiment: boundary (69b).
These are in HARVEST_QUEUE.md "Remaining holes after batch 06" with candidates.

### Deviations / my own errors, corrected in the open
open-simh (Debian simh too old); container-local dmtcp dir (uid mismatch); go 1.24 for memberlist;
python 2.7 for paxos; five oracles mis-specified on first write and fixed with the reason recorded
(STR end-vs-sample-20; L1 window vs one-sample lag; autotune Kp-vs-Kd; libDAI tree-vs-loop; PMMH
recovery-vs-movement); Go control (GoexitExit is a deadlock too, not the control). Every recipe ran
in the disposable copy; every batch-06 receipt says BODY PRESERVED.

--------------------------------------------------------------------------------

# ROUND 07 (autonomous loop) -- breadth + depth by coverage

Opened on 108-verify (105 batch06 + this round's 3). Loop: verify -> coverage map ->
identify thin/absent frontiers -> harvest -> make runnable -> preserve -> test ->
verify --all -> receipt -> land -> reassess.

### Coverage read that drove the digging
COVERAGE_MAP_2026-09-13.json showed several DOMAINS entirely absent (theorem-proving,
computer-algebra/number-theory, bioinformatics) and estimation/control with NO failed
branch. Round 07 entered the three absent domains and added one failed branch to
estimation and one decompressor-failure dataset to compression.

### NEW (3, all RUNNABLE_CONTAINER, oracle-backed)
- eprover-2.6 -- E superposition prover; proves TPTP GRP237-1/BOO006-1 (SZS Unsatisfiable);
  first theorem-proving fossil; execution model = saturation search for the empty clause.
- pari-gp-2.17 -- PARI/GP 2.17.4; factor(2^67-1)=193707721*761838257287, prime(1000)=7919;
  first computer-algebra fossil; a stack-based exact-arithmetic interpreter.
- ssw-smith-waterman -- striped SIMD Smith-Waterman; optimal local score of an exact-
  substring pair; first bioinformatics fossil.

### DEPTH (2 datasets over EXISTING fossils, no new specimens)
- DECOMPRESS_CORRUPTION_2026-09-13.json: gzip/bzip2/ncompress under byte-flip + truncation.
  gzip -> CRC error exit 1 (partial then refuse); bzip2 -> block integrity exit 2; LZW 1985
  -> exit 0, WRONG output, silent (no integrity check). Clean cases reproduce the original.
- ESTIMATION_DIVERGENCE_2026-09-13.json: filterpy KalmanFilter, CV model vs a manoeuvring
  target, four Q. Q=1e-6: gain 0.036, reported sigma 0.40, actual error 299 (overconfidence
  525x) = the divergent "smug" filter; Q=1 matched: tracks (ratio 0.83). Failed branch +
  control both recorded.

### Bug found + fixed in the open
1. run_decompress_corruption.py first produced all-fail rows: the C build image has NO
   python3, so the heredocs that wrote the workload + corrupted bytes silently failed.
   Rewrote workload/flip/truncate in pure coreutils (seq/sed, dd/od, head). Real data now.
2. pari-gp-2.17 classified BUILDS_BUT_NOT_RUN twice: the run located the gp binary with
   `find . -name gp -type f -perm -u+x`, which matched a TPTP-style test DATA file
   src/test/32/gp, not the binary; then the top-level ./gp symlink was a stale/broken
   wrapper in the staged copy. Fixed to target the freshly built Olinux-*/gp-dyn with
   LD_LIBRARY_PATH; run now PASS (RUNNABLE_CONTAINER, oracle matched). gp is proven to run.

### Close
verify --all = 108/108 match, 0 differ, 0 body_missing. CATALOG.json = 108 (98 runnable,
78 oracle-backed). Coverage refreshed. Nyx handoff: roles/Nyx/INBOX_TECHNE_FOSSIL_BATCH07.
Bodies preserved on every run (disposable_copy isolation). Reassessing for Round 08 (edges
beyond CS canon: pre-1970 historical worlds, digital-evolution/artificial-life -- both thin).

--------------------------------------------------------------------------------

# ROUND 08 (autonomous loop) -- THE EDGES (beyond the CS canon)

Opened on the 108-verify. Theme: historical worlds + unusual computational cultures, where the
coverage map is thinnest (pre-1970 programs; digital-evolution/genetic-programming n=1).

### NEW (1, RUNNABLE_CONTAINER, oracle-backed)
- eliza-anthay-1966 -- ELIZA (Weizenbaum 1966), the origin of the chatbot; keyword
  decomposition/reassembly over text, no model of meaning. A pre-1970 historical world + a new
  culture (conversational AI). Reproduces the exact 1966 CACM dialogue: "Men are all alike."->
  "IN WHAT WAY", then "CAN YOU THINK OF A SPECIFIC EXAMPLE", then the pronoun-reflected "YOUR
  BOYFRIEND MADE YOU COME HERE". Embedded self-tests (1966 CACM + 1965 MIT archive) pass with 0
  failures. Faithful reconstruction (FAITHFUL_PORT), not the original MAD-SLIP binary.

### DEPTH (1 dataset over an EXISTING fossil -- artificial life)
- COREWAR_TOURNAMENT_2026-09-13.json: the vault's corewar-redcode MARS (Core War, 1984). Imp/
  Dwarf/Gemini/Mice/Core Clear round-robin, 8000-cell core, 15 rounds/pair. Survival is NON-
  TRANSITIVE: Dwarf>Gemini 15-0, Gemini<Mice 0-14, Mice~Dwarf (ties), Imp almost never wins
  outright but is nearly unkillable (Imp vs Core Clear = 15 ties). Raw counts; no ranking.

### ATTEMPTED UNLOCK -> HONEST BOUNDARY (avida)
avida stays NOT_ATTEMPTED. It BUILDS (AVIDA_DISABLE_BACKTRACE=1, cmake
-DCMAKE_POLICY_VERSION_MINIMUM=3.5, make avida -> 9.9 MB binary; reproduced 3x) and LAUNCHES, but
SEGFAULTS at ancestor injection. Root cause is a PRESERVATION GAP: the fossil body has NO submodule
bodies -- libs/apto (required) is empty, only .gitmodules is preserved. I built against apto HEAD
(the contemporaneous pin is not in the body); mismatched apto + modern toolchain is the likely
crash. Not modernised for a PASS (charter). Logged: specimens/avida/BUILD_ATTEMPT_2026-09-13.md.
Finding of the round: git fossils with submodules were acquired WITHOUT the submodule bodies --
a general acquisition gap. Backlog: re-acquire with submodules + gitlink SHAs pinned.

### Close
verify --all pending (expected 109/109). CATALOG.json = 109; coverage refreshed. Nyx handoff:
roles/Nyx/INBOX_TECHNE_FOSSIL_BATCH08. Round 08 is lighter than 07 (1 new + 1 depth) -- the avida
unlock consumed the build budget and returned a preservation-gap finding instead of a fossil.

--------------------------------------------------------------------------------

# BATCH 09 -- LOSERS, LOST WORLDS, AND PRESERVATION DEBT

Charter roles/Techne/prompts/2026-09-13_batch09/OPERATOR_CHARTER.md sha256
b7e70d4fdfc90f1234094edfada65ca4bbdb326c639b0236f538e85fdf28a505
Opening gate: verify --all = 109/109 match, 0 differ, 0 missing.

### P1 -- THE SUBMODULE PRESERVATION GAP IS REPAIRED (and it was causal)
git_pin now takes init_submodules; acquire() fetches submodules ONLY when the record declares
them ("submodules": "required") and ONLY at the commits the superproject pins -- never implicitly,
never at HEAD. That last word was the whole avida bug: I had cloned apto at HEAD.
New instrument: preservation_of / preservation_check / preservation_census, CLI
`harvest preservation [id|--all]`. Body axis (submodules, git-lfs, artifact pinning) and recipe
axis (network fetch) are separate; status is the WORSE of the two, so nothing reads as preserved
on a tree hash alone.
CONTROLS PASS: positive OK; submodule emptied -> FAIL MISSING; submodule off its pin -> FAIL
pin-drift; body restored byte-identically. The control's FIRST run mutated one gtest header,
because a bare `git checkout` CRLF-converts on Windows -- caught by the tree-hash assertion, then
fixed by forcing core.autocrlf=false/core.eol=lf. An instrument that only ever says OK proves
nothing; this one was made to fail twice on purpose and once by accident.
7 regression tests added (test_preservation.py); 382 fossil-scoped tests pass.

AVIDA IS UNLOCKED. Re-acquired at its ORIGINAL pin 47f13dadb (not HEAD -- no modernisation) with
libs/apto + backward-cpp at the superproject's gitlinks. It now builds OFFLINE (the image already
ships cmake/g++/make, so the recipe fetches nothing) and runs: from one hand-written ancestor the
population reaches 1068 organisms, mean generation 22.96, 808 births by update 300.
RUNNABLE_CONTAINER. The segfault was never an avida bug -- it was the missing submodule, which
means the preservation defect was CAUSALLY responsible for the failed unlock. tasks.dat stays
all-zero at 300 updates; recorded, not interpreted.

### P8 -- PRESERVATION DEBT CENSUS (the defect was not unique)
First census over-reported 59 fossils as unpinned because it read source_origin.artifacts; the
authoritative pin lives in hashes.artifacts (the batch scripts overwrite source_origin and drop
sha256/commit_resolved -- a separate latent defect, now documented). Corrected: 18.
109-fossil census found a SECOND incomplete body, not avida: lru-cache-goldsborough was missing
tests/googletest. Both repaired. Closing census over 117: SELF_CONTAINED 97,
FULLY_PINNED_EXTERNALS 2, UNPINNED_EXTERNAL_DEPENDENCY 18, KNOWN_INCOMPLETE 0.
Network-fetch debt: 18 recipes fetch at build/run time. avida's was ELIMINATED this round (it now
builds with no apt and no submodule clone). The other 17 are named in the census, not fixed.

### P7 -- HISTORICAL DISPOSITION IS NOW A FIRST-CLASS, EVIDENCE-GATED FIELD
record.historical_disposition = {state, failure_reasons, superseded_by, rival_of, evidence[]}.
States: ACTIVE / SUPERSEDED / ABANDONED / FAILED / LOSING_RIVAL / OBSOLETED_BY_ENVIRONMENT /
LEGAL_OR_PATENT_DISPLACED / HISTORICAL_ONLY / UNKNOWN. validate() REFUSES any non-ACTIVE/UNKNOWN
state carrying zero evidence entries, and every evidence entry needs a kind + what the source
actually says. An LLM cannot label something a loser here without a citation; that is enforced by
the schema, not by my good intentions. coverage.py gained a failure_reason view.

### P2 -- LOSERS (8 new specimens, 5 runnable, every disposition cited)
PAIR, dense linear algebra (poor_scaling / superior_rival):
  linpack-netlib-1979 SUPERSEDED -> lapack-reference. LINPACK's own benchmark is its own oracle:
    it solves a system whose exact solution is all ones and returns x(1)=x(n)=1.00000000E+00 with
    residual 1.39e-14 at machep 2.22e-16. RUNNABLE.
  eispack-netlib-1976 SUPERSEDED -> lapack-reference. Graded against exact mathematics: the 3x3
    second-difference matrix has spectrum 2-sqrt2, 2, 2+sqrt2; EISPACK returns 0.585786, 2.000000,
    3.414214. RUNNABLE. Took four acquisition passes to become complete -- rs.f pulls in tred1,
    tqlrat, pythag, epslon and tql1 through a branch the driver never calls but the LINKER does.
    A body missing a dependency its own control flow never reaches is still an incomplete body.
  lapack-reference kept as the WINNER so the comparison exists. SOURCE_ONLY this round.
PAIR, block cipher (security_weakness):
  des-reference OBSOLETED_BY_ENVIRONMENT -> tiny-aes-c. Evidence: NIST withdrew FIPS 46-3 (2005);
    EFF Deep Crack (1998) exhausted the keyspace in days. RUNNABLE (16-round Feistel round trip).
    The oracle is honest about its limit: this body is file-oriented with PKCS5 padding and has no
    raw single-block entry point, so no FIPS known-answer vector is run. DES's failure was never
    its round function; it was 56 bits, which no round trip can reveal.
  tiny-aes-c ACTIVE, the winner, graded on NIST SP 800-38A vectors. RUNNABLE.
SOLO md5-rfc1321 SUPERSEDED (security_weakness, incorrect_assumptions). RUNNABLE.
SOLO spdylay SUPERSEDED by the standard it became (RFC 7540). SOURCE_ONLY.
SOLO openssl-1.0.1f-heartbleed FAILED (CVE-2014-0160). SOURCE_ONLY BY INTENT -- preserved so the
  missing bounds check can be read in its original context; never built, and no exploit in the vault.

### P6 -- A FAILURE DATASET NOBODY WAS LOOKING FOR
Making MD5 runnable surfaced something better than the round's plan.
RFC 1321's own reference implementation declares `typedef unsigned long int UINT4` -- true on 1992
machines, false on LP64. ENVIRONMENT_ASSUMPTION_2026-09-13.json runs the IDENTICAL extracted bytes
(same sha256, verified) in two worlds:
  sizeof(unsigned long)=4  -> every digest matches RFC 1321 A.5
  sizeof(unsigned long)=8  -> MD5("") = e4c23762ed2823a27e62a64b95c024e7, not d41d8cd9...
It compiles clean, runs clean, exits 0, and is silently wrong. No warning, no crash. The fossil is
therefore run in the vault's 32-bit world -- its ORIGINAL environment, the same precedent as libfec
and compact -- and NOT patched. Note what this means for the fossil's OTHER failure: every vector
in its self-test passes in the 32-bit world, and the 2004 collision break is invisible to all of
them. A document that grades itself cannot detect either of its own deaths.

### NOT DONE (named, not hidden)
P3 pre-1970 worlds: NOT attempted. 1950s still 3, 1960s still 7 -- unchanged.
P4 BSD TCP / TECHNE-69b: NOT attempted this round.
P5 remaining unlocks: swipl, souffle, xv6 NOT attempted (avida done). NOT_ATTEMPTED is now 1.
LINPACK-vs-LAPACK rate dataset: NOT done -- lapack-reference is acquired but unbuilt, so the
  measurable half of the pair's documented reason is still missing.
TECHNE-65: Z: still unreachable. SINGLE_HOST_RISK UNCHANGED -- 117 bodies on one host.

### Close
Closing verify --all, catalog 117, coverage refreshed, census refreshed.
Loser axis 15/109 -> 21 non-ACTIVE of 117 (6 of them explicitly evidence-backed; the other 15 are
legacy tag-based with state UNKNOWN and should be migrated to the P7 field).

--------------------------------------------------------------------------------

# BATCH 10 -- TRUST THE VAULT BEFORE EXPANDING IT

Charter roles/Techne/prompts/2026-09-13_batch10/OPERATOR_CHARTER.md sha256
2c28f471c33abc5216ef50a2fafb080e401c26fbd482905749dd8f76de7a46c6
Opening gate: verify --all = 117/117 match, 0 differ, 0 missing.
ZERO new specimens. That was the point.

### DEVIATION FOUND AT THE START (mine, reported before anything else)
origin/main was at 30e5fc1b9 while local main was at 5896133a0: batch 09 AND the three autonomous
rounds had been committed and "landed on main" LOCALLY and never pushed. Every "landed" claim in
those returns meant local main only. The branches had also diverged (9 Archaeon commits). Merged
origin/main, tested the merged tree (406 pass), pushed 30e5fc1b9..21ab6a4c5, and verified each
prior carrier is now genuinely an ancestor of origin/main. Fixed, but it was wrong when claimed.

### P1 -- the pin instrument, fixed at the SOURCE
Batch 09 patched the READER. The defect was the WRITER: every batch main() rebuilt source_origin
from a literal and destroyed the pin acquire() had written there. Measured BEFORE, in a
preregistration filed before the change: 21 git artifacts pointing at a moving "HEAD" while the
exact commit was known, 239 url artifacts with sha256 dropped. 260 pin facts destroyed, all still
recoverable from the receipt.
That is worse than a census mis-read: a record saying commit: HEAD asks a future re-acquisition to
trust a moving reference -- exactly what P7 requires us to answer NO to.
merge_artifact_pins + all 9 batch mains + a backfill of the 260 (nothing invented; only identifiers
the record already proves). PREREGISTERED PREDICTIONS: P-A lost pins -> 0 CONFIRMED; P-B the census
must NOT move (97/2/18/0) CONFIRMED -- the reader already read the receipt, so this defect was never
about the counts; P-C idempotence CONFIRMED. The erroneous 59-fossil census is preserved as an
INSTRUMENT FAILURE, with mechanism, how it was caught, and what it would have cost.
6 controls; falsifiability verified by disabling the fix and watching C3 fail.

### P2 -- one authoritative implementation, decided on BEHAVIOUR
_shell/_clip/_expect_ok were byte-identical. acquire's live copy was strictly newer (the shadowed
one predated submodule preservation entirely -- everything acquired before batch 09 ran through
code that COULD NOT preserve a submodule). verify: 0 disagreements across all 117 specimens; the two
differ only in what they SAY. Which behaviour produced existing evidence: verify_all/restore/mirror
all call drift() directly, so every "117/117" figure came from the drift path and receipts were
never affected. Kept the submodule-aware acquire and the drift-based verify.
The removal regressed once -- it deleted module-level ARCHIVE_SUFFIXES sitting after the stale
acquire -- and test_fossil_isolation caught it immediately. An AST before/after comparison confirms
nothing else was lost. test_instrument_hygiene now fails on any shadowed top-level definition in any
fossil module, with a control proving the detector fires.

### P3 -- the loser count SHRANK under evidentiary scrutiny
15 legacy claims entered. CONFIRMED 5, RECLASSIFIED 4, NOT_A_LOSER 4, DEMOTED TO UNKNOWN 2.
Evidence-backed loser fraction: 15/117 = 12.8% (batch 09 claimed 21 non-ACTIVE; six did not survive).
Three things worth more than the number:
- the mechanical tag rule was counting WINNERS as losers. "successor"/"predecessor" are
  RELATIONSHIPS, not dispositions; lapack-reference and tiny-aes-c were being flagged by the same
  rule while being the rivals that won.
- a documented failure MODE of current software is not a historical disposition. glibc's writer
  starvation is real, documented in the man page, reproduced in the fossil's own receipt -- and
  glibc still ships it. That is behaviour, which the vault already records as behaviour.
- odepack's "pathology" is STIFFNESS: a property of the problem that makes a non-stiff method
  exhaust its step budget. Mathematics behaving correctly, not software that failed.
redlock stays UNKNOWN because Kleppmann vs antirez was never settled; declaring a winner would be
my judgement. tinystm stays UNKNOWN because I cannot cite a source establishing STM was superseded
by a named rival -- the CACM 2008 critique documents an argument, not an outcome.

### P4 -- supersession became a number
LEVEL3_FRACTION: the share of matrix element-references issued through Level-3 BLAS, counted with
ld --wrap so NEITHER BODY IS EDITED. Same matrix, same answer to ~1e-14, preregistered sizes:
  LINPACK  0.0000 at every n (only daxpy/dscal/idamax)
  LAPACK   0.8403 -> 0.9120 as n goes 64 -> 1024
  at n=1024 LINPACK moves 3.60e8 element-references against LAPACK's 1.19e7 = 30.2x
P-1, P-2, P-4 CONFIRMED. P-3 FALSIFIED: I predicted a null at n=64 because dgetrf should fall back
to unblocked dgetf2 at the block size. It does not -- modern reference LAPACK factors the panel
RECURSIVELY (dgetrf2) and issues dgemm/dtrsm on sub-blocks even at n=64. Recorded with the reason.
Three failed links on the way, each a real fact: a fossil that uses only Level-1 BLAS cannot even be
INSTRUMENTED the same way as one that uses Level-3.

### P5 -- generalising the instrument, not the specimen
env_assumption_census scans every preserved body with narrow patterns. 117 scanned, 43 carry at
least one, 14 carry the EXACT MD5 defect class (a fixed width asserted onto `long`) -- and the
scanner finds md5-rfc1321 itself, which is the validity check.
harvest.run gained image=/persist=: the same preserved body and recipe in a DIFFERENT world, never
persisting, so a foreign-world probe cannot overwrite a real classification. Validated on the
control: md5 probed at 64-bit reproduces the wrong digests while its record still reads
RUNNABLE_CONTAINER with its original receipts.
The first world-change run reported "positive control held: FALSE" -- because I had set the other
world to i386 while md5 is NATIVE to i386, so it compared a world against itself. My own control
caught my own bug. Fixed to choose the world that actually differs.

### P6 -- NOT ATTEMPTED, explicitly
Pre-1970 acquisition is gated behind P1-P5 by the charter's own ordering, and P1-P5 consumed the
round. 1950s still 3, 1960s still 7 -- unchanged for the second batch running. Not attempted is the
honest status; a rushed acquisition now would violate "do not lower provenance standards merely to
populate a decade". TECHNE-69b (TCP) deliberately untouched per the charter.

### P7 -- preservation closure
Census unchanged at 97 SELF_CONTAINED / 2 FULLY_PINNED_EXTERNALS / 18 UNPINNED_EXTERNAL_DEPENDENCY
/ 0 KNOWN_INCOMPLETE. Ranked queue built for the 18: 0 OF 18 can be reconstructed without trusting a
moving head, and 0 of 15 pip recipes pin a version. Top of queue is python-sgp4, emcee, filterpy,
pid-autotune, python-control -- all oracle-backed with FLOATING NUMERICAL dependencies. filterpy is
the fossil the Kalman-divergence dataset was measured on, so that dataset's numbers rest on an
unpinned numpy/scipy. For these fossils the BODY is pinned and verifies; the WORLD is not -- the same
failure class P5 demonstrated.

### TECHNE-65
Z: still unreachable. SINGLE_HOST_RISK UNCHANGED, 117 bodies, one host.
