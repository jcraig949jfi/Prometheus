TECHNE — BATCH 05 AFTER RESET

You have just repaired a real preservation defect.

Do not treat that as a side note.

The vault only matters if its bodies remain exact.

This round therefore has THREE jobs:

1. VERIFY the preservation layer independently where cheap.
2. HARVEST the thinnest behavioral-pressure axes.
3. DEEPEN selected lineages rather than adding only singletons.

OPENING GATE

Before acquiring anything:

python -m techne.fossils.harvest verify --all

Expected:

specimens 57
matches   57
differs   0
missing   0

If this fails:

STOP HARVEST.
REPAIR PRESERVATION FIRST.

Do not acquire new fossils into a vault whose invariants are false.

SECOND-SOURCE RESTORE CHECK

For the two tarball-backed specimens involved in archive-content restoration:

independently re-fetch the exact authoritative release where still
available;
hash the release independently;
compare restored files against BOTH:
    the pre-drift recorded hashes
    the independently re-fetched origin artifact

If origin has disappeared:

record ORIGIN_UNAVAILABLE;
retain the archive-backed restoration result;
do not weaken the prior VERIFIED claim;
explicitly state that second-source verification was impossible.

Do not re-fetch every specimen merely for ceremony.

This is a targeted confidence check because the preservation machinery itself
was just repaired.

RUNNER ISOLATION CHECK

Select a diverse sample of at least FIVE real fossils and re-run under the new
disposable-copy isolation.

Include different execution modes where available:

native
docker/container
historical toolchain
HDL
Python or interpreted runtime

After every run:

body_preserved must be true;
verify(id) must still match.

If any recipe depends on historical in-place residue:

report RECIPE_STATE_DEPENDENCY;
do not "fix" it silently.

BATCH 05 HARVEST

Target:

15-25 specimens.

This batch is NOT about raw count.

Bias toward the axes that remain thin after Batch 04.

PRIORITY A — RUNNABLE CONTENTION / RETRY / RECOVERY

Current weakness:

only raft is strongly runnable;
Linux TCP CUBIC/BBR are source-only.

Acquire runnable implementations of several different failure/retry regimes.

Seek:

Ethernet exponential backoff
TCP retransmission / congestion control
randomized lock backoff
database transaction retry
circuit breaker / retry queue
lease/heartbeat timeout
failure detector
checkpoint / restart
journal or log recovery
distributed mutex
leader election

At least FOUR runnable specimens if practical.

Prefer multiple human domains.

Do not satisfy this with four network retry libraries.

PRIORITY B — FAILURE PATHOLOGIES

The vault still lacks enough explicit failure behavior.

Acquire or preserve runnable examples of:

deadlock
livelock
starvation
race condition
retry storm
congestion collapse
unstable feedback
cache thrashing
allocator fragmentation
numerical divergence
oscillatory scheduling

At least THREE.

Prefer REAL historical implementations, regression fixtures, or before/after
versions over synthetic toys.

For each preserve:

trigger
observed failure
corrected descendant if available
source provenance

Do NOT repair the failing fossil itself.

PRIORITY C — ESTIMATION / LEARNING UNDER HIDDEN STATE

Fill the missing estimator families:

particle filter
MCMC
EKF or UKF
Baum-Welch / HMM training
belief propagation

At least THREE runnable.

Prefer compact implementations whose state evolution can later be observed.

PRIORITY D — RUNNABLE MPC / ADAPTIVE CONTROL

Batch 04 has:

PID runnable
LQR runnable
do-mpc source-only

Obtain at least ONE runnable model-predictive controller and ONE genuinely
adaptive controller if practical.

Do not substitute another fixed-gain controller.

Preserve:

plant/model
disturbance or setpoint
resulting control trace
oracle/reference if available

PRIORITY E — CONCURRENCY

Acquire computational machinery that coordinates simultaneous actors.

Seek:

semaphores
monitors
lock-free queues
wait-free structures
work stealing
transactional memory
deadlock avoidance
deadlock detection
concurrent schedulers

At least THREE distinct mechanisms.

A pathological counterpart is especially valuable.

PRIORITY F — ADVERSARIAL PAIRS

Increase paired pressure/response fossils.

Acquire at least TWO new pairs.

Examples:

malformed input <-> parser recovery
corruption <-> decoder
fault injection <-> recovery
contention <-> arbitration
noisy process <-> estimator
malware trace <-> scanner
fuzzing <-> sanitization

Use lawful, safe, archival or simulated materials.

No live offensive capability harvesting.

PRIORITY G — LEGACY COMPUTATIONAL CULTURES

The vault still needs more machinery from languages and environments whose
problem-solving style differs from modern C/Python ecosystems.

Acquire actual runnable systems or meaningful software in:

COBOL
Ada
BASIC
Lisp family
APL family
Smalltalk
Forth
older FORTRAN
Prolog/WAM

At least THREE additions from currently thin cultures.

Prefer:

real business/scientific/control/problem-solving programs

over:

hello-world examples.

PRIORITY H — HARDWARE DEPTH

Hardware exists now, but remains thin.

Acquire small decision machinery rather than another CPU.

Seek:

branch predictor
cache controller
NoC router
packet scheduler
priority arbiter
credit/backpressure controller
sorting network
hazard controller

At least TWO runnable under HDL simulation.

Capture meaningful state/decision traces.

PRIORITY I — SCIENTIFIC EXTREMES

Add at least TWO specimens from precision-heavy science.

Candidate domains:

accelerator physics
beam optics
orbital mechanics
telescope control
fusion/plasma
detector reconstruction
interferometry
high-precision timing
signal reconstruction
numerical relativity

Use public research/reference/simulation source only.

The aim is machinery shaped by severe numerical or physical constraints.

PRIORITY J — FINANCIAL / ECONOMIC COMPUTATION

Add public, lawful implementations from distinct financial problem families:

option pricing
portfolio optimization
risk
volatility
order-book simulation
execution simulation
forecasting
fraud detection

At least TWO if acquisition is straightforward.

Do not use profitability as an oracle.

DEPTH REQUIREMENT

Choose at least THREE existing vault lineages and deepen them.

Suitable candidates include:

TCP congestion-control evolution
compression
SAT solving
garbage collection
allocator lineage
numerical libraries
Prolog/WAM
Lisp implementations
error-correcting codes
artificial life

For each deepened lineage, acquire something that adds a meaningful:

predecessor
successor
rival
fork
failed branch
historical redesign

Do NOT collect version tags with no computational distinction.

ONE FAILED BRANCH PER DEPTH LINEAGE

For at least TWO of the deepened lineages, actively seek a:

superseded design
losing competitor
known-bad version
abandoned branch

Human success history is selection-biased.

Preserve losers when they are obtainable.

DISCOVERY QUOTA

At least 25% of Batch 05 candidates must come from OUTSIDE the current
HARVEST_QUEUE.

Search unknown territory.

Use:

old research indexes
university archives
historical source collections
competition proceedings
obscure package archives
papers with forgotten code
archived personal research pages
source references
old FTP mirrors
museum / preservation catalogs

The queue is guidance.

It is not the universe.

SMALL-MECHANISM HARVEST

Acquire at least THREE compact fossils where the interesting machinery is
small.

Examples:

retry controller
finite-state machine
scheduling rule
cache replacement rule
voter
arbiter
propagator
estimator update
adaptive step controller
backoff rule

Exact ancestry still required.

Small fossils matter because Nyx may eventually compare them directly to
small evolved circuits.

BINARY-ONLY PATH

Still optional.

Exercise only if a genuinely worthwhile lawful specimen naturally appears.

If used:

preserve exact binary
hash it
identify architecture
disassemble
decompile where lawful and useful
preserve raw + recovered representations
record tool/version/config
mark:
    RECOVERED REPRESENTATION — NOT ORIGINAL SOURCE

Do not hunt proprietary binaries just to close the checkbox.

ARCHAEON VISIBILITY

The prior downstream report that Archaeon saw ZERO fossils remains important.

Before Batch 05 closes, establish the current state.

Minimum requirement:

Archaeon or its supported interface can enumerate the fossil records

OR:

exact blocker is identified and recorded.

Do not equate host-local body storage with record invisibility.

A consumer should at minimum be able to know:

fossil_id
lineage
human purpose
version
run status
record location

If this still fails, report it as a first-class infrastructure defect.

BODY MIRROR

TECHNE-65 remains an operator decision.

Do not invent a storage destination.

But prepare the mirror mechanism so that once destination is named, bodies can
be copied by immutable tree hash with:

source hash
destination hash
verification receipt
no git inclusion

The vault presently has one host as a preservation dependency.

Keep that visible.

MEMORY.md

Do not mass-edit the shared memory index.

Route the oversize/truncation issue.

Keep Techne’s additions minimal until a dedicated compaction owner acts.

BATCH CLOSE GATES

Before finalizing:

verify --all

must again report full agreement.

Also report:

how many real recipes re-ran under isolation;
whether any body drifted;
whether any recipe depended on prior in-place residue;
whether second-source restoration checks matched.

RETURN FORMAT

Return FIVE sections.

1. PRESERVATION
    opening census
    closing census
    second-source checks
    isolation re-runs
    body drift
    unrecoverable artifacts
    mirror status
2. BREADTH
    new specimens
    new lineages
    new languages
    new execution models
    new domains
    new pressure regimes
    discovery-outside-queue fraction
3. DEPTH
    lineages deepened
    predecessors/successors/rivals
    failed branches
    historical redesigns
4. THIN AXES

Explicitly answer:

WHAT REMAINS NEAR-EMPTY?
WHAT HAS BREADTH BUT LITTLE DEPTH?
WHAT HAS DEPTH BUT LITTLE PRESSURE DIVERSITY?

5. CONSUMER ACCESS
    Archaeon record visibility
    Nyx handoff
    exact blockers if any

DO NOT

Do not decompose fossils.

Do not name organs.

Do not classify behavioral commonality.

Do not score intelligence.

Do not prune because an LLM thinks two systems “basically do the same thing.”

Do not modernize historical algorithms for convenience.

Do not let PASS receipts substitute for preservation verification again.

FINAL ORDER

THE VAULT IS NOW REAL ENOUGH THAT ITS OWN INTEGRITY MATTERS AS MUCH AS
ACQUISITION.

PROVE THE BODIES ARE STILL THE BODIES.

THEN KEEP DIGGING.

GO WIDER.

GO DEEPER.

PRESERVE THE LOSERS.

PRESERVE THE WEIRD ONES.

AND MAKE SURE THE NEXT AGENT CAN ACTUALLY SEE WHAT YOU BROUGHT HOME.
