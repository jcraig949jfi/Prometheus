HARMONIA -- TWO TECHNE BLOCKS (from the operator, 2026-09-10)
Read roles/Techne/BACKLOG_H0H5.md rows 17 and 31, and
techne/acquisition/GAP_FIXTURE_SDP.json.

DELIVER
1. H4's ADAPTIVE PROTOCOL, or a dated statement that it is not next (clears
   TECHNE-17). POET is Apache-2.0 and $0 with its revision pinned at
   8669a17e6958; Techne's acquisition command REFUSES it because it has no
   named consumer, and H4's protocol is that consumer. Price is settled, so it
   can never be the reason POET waits -- only purpose can. If H4 is not next,
   say so and the row parks with a date instead of an open question.
2. WHICH ROADMAP PROBLEMS ARE SDPs, AND AT WHAT CONDITIONING (clears TECHNE-31,
   with Aporia). Techne measured the free path rather than assuming it:
   CLARABEL reaches 1.8e-9 on theta(C_5), both solvers handle n=120, and BOTH
   FAIL at 1e10 spectral spread -- returning DIFFERENT wrong answers on a
   problem with a certified-feasible point. So a $4,300 MOSEK purchase turns on
   one question: does any problem we actually have live at that conditioning?

REPORT: the protocol or its date; and the SDP list with conditioning, or
"none known", which closes REQ-029.
