TECHNE — FOSSIL HARVEST BATCH 06

Batch 05 established:

90 preserved fossil bodies
90 catalogued lineages
breadth across 23 languages
meaningful historical depth in several families
preservation verification that now measures the body
host-neutral catalog enumeration
zero unresolved body drift

Batch 06 should NOT attempt another general expansion immediately.

This round has a sharper purpose:

UNLOCK EXISTING DEPTH
ACQUIRE MISSING FAILURE MACHINERY
ADD PRESSURE DIVERSITY TO DEEP LINEAGES
ENTER A FEW STILL-EMPTY COMPUTATIONAL CULTURES

The question is no longer:

"Can Techne find more algorithms?"

Clearly yes.

The question is:

"What evidence is still missing from the fossil record?"

OPENING PRESERVATION GATE

Run:

python -m techne.fossils.harvest verify --all

Expected:

90 specimens
90 match
0 differ
0 missing

Any failure stops acquisition until dispositioned.

TECHNE-65 — MIRROR

Operator ruling:

use the Z: share as the first off-host fossil mirror,
keyed by immutable tree hash,

PROVIDED the destination is physically/storage-wise independent of the
SKULLPORT vault.

Before copying:

prove destination is outside the repository;
prove destination is not merely another path onto the same local volume.

Then mirror all bodies using the already-built mechanism.

Require:

source tree hash
destination tree hash
byte/tree equality
mirror receipt
specimen -> tree-hash mapping

Run a destructive-negative control against a disposable test mirror:

corrupt one mirrored file
    ->
verification MUST fail.

Do not modify source bodies.

If Z: is not actually independent storage, STOP and report that fact rather
than pretending redundancy exists.

PHASE 1 — UNLOCK EXISTING FOSSILS

Before substantial new harvesting, attack the blocked execution environments.

1. TECHNE-69 — SIMH / HISTORICAL TCP WORLD

⸻

Highest priority.

The vault already contains:

BSD 4.2 TCP
BSD Tahoe
BSD Reno

but all three are SOURCE_ONLY.

That is historical DEPTH without experimental behavior.

Build the smallest preserved historical environment capable of exercising
those implementations without modernizing their source.

SIMH/VAX or another defensible historical execution path is acceptable.

Target a reproducible experiment that can expose at least:

normal transfer
packet loss
delayed acknowledgement or constrained link
retransmission/congestion response

Do NOT begin by trying to emulate an entire historical Internet.

Find the minimum faithful environment that makes the fossil observable.

If full execution remains disproportionate:

establish the exact boundary;
preserve every successful intermediate step;
classify honestly.

2. TECHNE-70 — I386 WORLD

⸻

Restore executability for fossils blocked specifically by historical x86
assumptions.

First target:

libfec / Karn lineage

and any other existing fossil that becomes runnable essentially for free in
the same world.

Do not turn this into a universal retro-computing project.

PHASE 2 — REAL CONCURRENT FAILURE

The vault still lacks enough REAL executable failures involving multiple
actors.

Acquire at least THREE specimens covering distinct phenomena:

deadlock
race
livelock or starvation

Strongly prefer:

historical bug
upstream regression test
actual library/runtime fixture
real before/after repair

over a newly written demonstration.

For each preserve:

failing version
trigger
observed trace
corrected version/commit where available
provenance

Do NOT repair the failing fossil.

A model-checking counterexample is useful but does NOT by itself satisfy this
requirement.

At least TWO of the three must actually execute concurrent machinery.

PHASE 3 — DISTRIBUTED SURVIVAL MACHINERY

Acquire runnable implementations of at least FOUR distinct mechanisms from:

failure detector
distributed mutex
lease
leader election
checkpoint/restart
journal recovery
replicated log recovery
heartbeat timeout
transactional retry

Raft already exists.

Do not satisfy this section with four Raft-like implementations.

Prefer different underlying assumptions:

synchronous-ish
asynchronous
crash-only
timeout-based
lease-based
quorum-based

PHASE 4 — ADAPTIVE CONTROL

The vault has control.

It does not yet have enough machinery that changes its own control behavior
based on observed performance.

Acquire at least TWO provenance-backed runnable adaptive controllers.

Strong targets:

MRAC
self-tuning regulator
recursive-least-squares controller
gain scheduling with measured adaptation
adaptive filtering tied to control

At least one must expose:

changing plant or disturbance
    ->
adaptation
    ->
altered control parameters/state

Do not substitute:

PID with a different fixed gain
LQR
ordinary MPC

for adaptive control.

PHASE 5 — INFERENCE GAPS

Acquire runnable specimens for:

belief propagation
particle MCMC or related particle-MCMC lineage

If strong specimens are available, also consider:

loopy BP
expectation propagation
smoothing
factor-graph inference

Prefer compact observable implementations.

Do not collect another basic Kalman filter merely because it is available.

PHASE 6 — SCIENTIFIC EXTREMES

Current representation is too shallow.

Acquire at least THREE specimens across DIFFERENT scientific regimes.

Priority order:

accelerator / beam optics
fusion / plasma
interferometry
numerical relativity
precision timing
high-energy detector reconstruction

Favor code shaped by:

extreme tolerances
stability constraints
noisy measurement
feedback
long integrations
ill-conditioned systems

Record the actual human constraint.

Do not infer Nyx organs.

PHASE 7 — NEGLECTED COMPUTATIONAL CULTURES

Acquire at least ONE meaningful runnable specimen from each obtainable gap:

APL
Smalltalk

Then search for ONE additional computational culture that the existing
23-language vault barely represents or does not represent at all.

Potential examples:

Erlang
Occam/CSP
SNOBOL
Icon
Mercury
Self
PostScript
dataflow language
array language other than APL

Do not collect languages for syntax diversity alone.

The specimen should exercise a distinctive execution/problem-solving model.

PHASE 8 — PRESSURE DIVERSITY IN EXISTING DEEP FAMILIES

DO NOT add another member to these families until their existing fossils are
tested under more than one pressure regime:

SAT
COMPRESSION

SAT

Current problem:

four CDCL-family fossils
same two tiny CNFs

Build a small shared fossil-level workload containing materially different
pressures, for example:

satisfiable easy
unsatisfiable easy
structured hard
random
pigeonhole/cardinality style
incremental if supported
resource-bound case

Do not score which solver is “best.”

Record behavior:

result
time/steps where available
conflicts/decisions if exposed
memory if cheap
failure/timeout

The goal is behavioral spread.

COMPRESSION

Current problem:

multiple codecs
primarily text compression ratio

Expose the existing codecs to several pressures:

highly compressible text
incompressible/random data
repeated binary structure
streaming/chunked input
truncated/corrupted stream
memory-constrained case if naturally supported

Record:

success/failure
ratio
round-trip integrity
corruption response
streaming behavior
resource signal where cheap

Do not add another codec until this exists unless it represents genuinely new
machinery.

TCP

If the historical TCP world becomes runnable, exercise the SAME controlled
pressure family across:

4.2
Tahoe
Reno

This may become one of the highest-value depth datasets in the vault.

Keep ancestry interpretation separate from measured response.

PHASE 9 — BAD DESIGNS / ABANDONED MACHINERY

Acquire at least THREE fossils selected specifically because history rejected,
superseded, or exposed serious limitations in them.

Different reasons preferred:

poor scaling
instability
brittleness
incorrect assumption
resource explosion
security failure
inferior rival
architectural dead end

Require documented historical evidence for the disposition.

Do not use LLM opinion as evidence that something was “bad.”

PHASE 10 — ONE LAWFUL BINARY FOSSIL

The binary-only path has remained open for several rounds.

This batch should ATTEMPT it, but not fabricate a candidate.

Find one historically/scientifically meaningful publicly redistributable or
otherwise lawfully analyzable binary for which original source is genuinely
unavailable or materially incomplete.

Possible classes:

historical language implementation
old scientific utility
abandoned shareware computational tool
archived research executable
historical solver

Do NOT choose malware merely because binaries are easy to find.

Do NOT choose proprietary software requiring bypass of protections.

Required preservation:

original binary
source URL/archive provenance
cryptographic hash
architecture
file format
observed execution if safely possible
disassembly
decompiler output where lawful/useful
exact tool versions/configuration

Label every recovered artifact:

RECOVERED REPRESENTATION — NOT ORIGINAL SOURCE

If no defensible specimen is found:

report NO LAWFUL HIGH-VALUE CANDIDATE FOUND.

That is preferable to quota gaming.

PHASE 11 — SMALL STRANGE MACHINERY

Continue breadth, but mechanically.

Acquire 5-10 small fossils from unrelated domains where the core executable
mechanism is compact.

Target things like:

scheduler
controller
predictor
arbiter
retry policy
replacement policy
voting mechanism
estimator update
parser recovery loop
adaptive numerical step rule
reconciliation algorithm
distributed timeout rule

These should come from real ancestry.

No requirement that they be famous.

DO NOT LET TECHNE BECOME THE EXPERIMENTER

The SAT/compression/TCP pressure work above exists to make preserved fossils
observable and comparable.

Techne may:

establish execution
construct reproducible shared inputs
record raw behavior

Techne must NOT:

infer behavioral equivalence
cluster fossils
name recurring organs
decide which mechanism is fundamental
claim convergence
classify intelligence

Nyx and later atlas machinery own those questions.

CATALOG IMPROVEMENT

For every fossil, ensure the tracked catalog can expose without the body:

fossil id
ancestry
human purpose
human environmental pressure
run status
observability
oracle status
intervention readiness
known historical disposition
body availability
mirror availability

Do not stuff interpretive prose into these fields.

BATCH SIZE

New fossils:

approximately 15-25

is enough.

Do NOT hit 30 merely because Batch 05 hit 33.

This batch can be scientifically successful with fewer new specimens if it
turns existing SOURCE_ONLY/depth fossils into experimental objects.

SUCCESS COUNTS

Report separately:

NEW FOSSILS

and:

OLD FOSSILS MADE MORE USEFUL

The second number matters now.

CLOSE GATE

Before close:

verify --all

must show:

all registered specimens match;
zero missing;
zero unacknowledged drift.

Then verify the off-host mirror if TECHNE-65 was executed.

RETURN

1. PRESERVATION
    opening/closing census
    mirror result
    isolation violations
    drift
2. UNLOCKED FOSSILS
    TCP
    i386
    any other previously blocked bodies
3. NEW BREADTH
    concurrency failures
    distributed survival
    adaptive control
    inference
    scientific extremes
    computational cultures
    bad designs
    binary fossil
    small strange machinery
4. NEW DEPTH
    historical lineages
    before/after/failure branches
5. PRESSURE EXPANSION
    SAT
    compression
    TCP if runnable
6. REMAINING HOLES
    near-empty
    broad-but-shallow
    deep-but-behaviorally-undertested
    source-rich-but-unrunnable

FINAL ORDER

DO NOT CONFUSE MORE FOSSILS WITH MORE KNOWLEDGE.

THE VAULT HAS ENOUGH MASS TO START DEVELOPING STRUCTURE.

UNLOCK WHAT WE ALREADY OWN.

FIND THE MISSING FAILURE MODES.

FIND MACHINERY THAT ADAPTS.

FIND SYSTEMS THAT SURVIVE OTHER SYSTEMS FAILING.

MAKE OLD LINEAGES RUN.

THEN KEEP DIGGING.
