================================================================================
FOSSIL VAULT -- AUTONOMOUS THREE-ROUND RETURN (ROUNDS 06, 07, 08)
================================================================================
Prepared:      2026-09-13   frozen (all three rounds landed on main)
Seat/Instance: Techne  m1-8c399e8f   worktree techne-pass-0912
Carriers:      R06 (prior session)  R07 8bfd6bc32  R08 28ab2ec27  -> main
Artifacts:     techne/fossils/CATALOG.json (109), COVERAGE_MAP_2026-09-13.json
               techne/fossils/pressure/{SAT,COMPRESSION,DECOMPRESS_CORRUPTION,
                 ESTIMATION_DIVERGENCE,COREWAR_TOURNAMENT}_2026-09-13.json
               roles/Nyx/INBOX_TECHNE_FOSSIL_BATCH0{6,7,8}_2026-09-13.md
               roles/Techne/journal/2026-09-13.md
Headline:      Vault 90 -> 109 (0 drift, 109/109 verify). 19 new specimens, 4
               old worlds unlocked, 5 behaviour/pressure datasets. Breadth is
               still gettable but the cheap canonical domains are mostly filled;
               what remains skews to losers, pre-1970 worlds, and hard builds.

--------------------------------------------------------------------------------
A. VAULT
--------------------------------------------------------------------------------
                          specimens  match  differ  missing  runnable
  open (start of R06)            90     90       0        0      TBD
  close (batch 06)              105    105       0        0       95
  close (end of R08)            109    109       0        0       99
  oracle-backed 79   with recorded historical disposition 15
Integrity: every run this loop used a disposable work copy; every receipt says
BODY PRESERVED; verify --all = 109/109 at each round close. Tests 343 passed.
Single-host risk UNCHANGED: TECHNE-65 off-host mirror is built but STOPPED (Z:
is not independent storage); 109 bodies still live on one host.

--------------------------------------------------------------------------------
B. BREADTH GAIN (new specimens: 19 across the loop)
--------------------------------------------------------------------------------
R06 (15): 3 BSD tapes; go-deadlock, helgrind, glibc-rwlock (concurrent failure);
  dmtcp, memberlist, redlock, plain-paxos (distributed survival, 4 assumptions);
  pid-autotune, self-tuning-regulator, L1-adaptive (adaptive control); libDAI
  (inference); gnu-apl (an array-language culture).
R07 (3): entered 3 ABSENT domains, all RUNNABLE + oracle-backed --
  eprover-2.6 (first theorem-proving fossil; saturation search for the empty
  clause), pari-gp-2.17 (first computer-algebra fossil; exact-arithmetic stack
  interpreter), ssw-smith-waterman (first bioinformatics fossil; SIMD S-W).
R08 (1): eliza-anthay-1966 (first conversational-AI fossil; Weizenbaum 1966,
  keyword transform with no model of meaning) -- an EDGE: a pre-1970 historical
  world and a new culture, via a faithful reconstruction (FAITHFUL_PORT).
Net: 4 domains that were entirely ABSENT at loop start now hold a runnable,
oracle-backed specimen (theorem-proving, computer-algebra, bioinformatics,
conversational-AI).

--------------------------------------------------------------------------------
C. DEPTH GAIN (5 behaviour/pressure datasets over EXISTING fossils)
--------------------------------------------------------------------------------
R06  SAT_PRESSURE          picosat/minisat over shared multi-regime workloads.
R06  COMPRESSION_PRESSURE  4 codecs round-trip + ratio spread over shared inputs.
R07  DECOMPRESS_CORRUPTION gzip/bzip2/ncompress under byte-flip + truncation:
       gzip -> CRC error exit 1 (partial then refuse); bzip2 -> block-integrity
       exit 2; LZW-1985 -> exit 0 with WRONG output, silent (no integrity check).
R07  ESTIMATION_DIVERGENCE filterpy KalmanFilter vs a manoeuvring target: the
       "smug"/overconfident failed branch (Q=1e-6 -> gain 0.036, reports sigma
       0.40 while actual error is 299 = 525x overconfidence) + a tracking control
       (matched Q=1, ratio 0.83).
R08  COREWAR_TOURNAMENT    corewar-redcode MARS (1984): Imp/Dwarf/Gemini/Mice/
       Core Clear round-robin; NON-TRANSITIVE survival (Dwarf>Gemini 15-0,
       Gemini<Mice 0-14, Imp nearly unkillable but rarely wins outright).
Areas deepened: SAT, compression, estimation (added its first failed branch),
artificial life. All are raw behaviour; no ranking asserted (Nyx's call).

--------------------------------------------------------------------------------
D. WORLD RECOVERY (old fossils made runnable / worlds restored)
--------------------------------------------------------------------------------
R06 UNLOCKED 4: libfec-karn (BLOCKED_PLATFORM -> SSE2 Viterbi decodes noisy
  codewords), compact-4.2bsd (-> genuine i386 world), the 4.3BSD kernel (SOURCE
  -> boots on an emulated VAX-11/780, root FS restored), particles (+ a PMMH run).
R07/R08 further hardened OLD fossils by putting them under pressure (the datasets
  in C drive filterpy, the codecs, and the Core War MARS already in the vault).
R08 ATTEMPTED, HONEST BOUNDARY: avida (flagship digital-evolution) stays
  NOT_ATTEMPTED. It builds + launches but SEGFAULTS at ancestor injection --
  a PRESERVATION GAP: the body has no submodule bodies (libs/apto empty; only
  .gitmodules preserved), so the contemporaneous apto pin is lost and a modern
  apto crashes it. Not modernised for a PASS. Logged; backlog = re-acquire with
  submodules pinned. (specimens/avida/BUILD_ATTEMPT_2026-09-13.md)
STILL SOURCE_ONLY / boundary: the BSD TCP netstat-under-loss experiment
  (TECHNE-69b, emulated-VAX multi-user install exceeds budget); swipl, souffle,
  xv6, linux-tcp-congestion (heavy or platform-bound builds).

--------------------------------------------------------------------------------
E. COVERAGE MAP (mechanical, techne/fossils/coverage.py)
--------------------------------------------------------------------------------
Multi-axis counts over the 109 tracked records; a fossil is counted in every
cell it belongs to. Sparsity is a TRANSPARENT RULE, not a judgement of value:
per domain +1 for each of {<=2 specimens, <=1 decade, <=1 language, 0 runnable,
0 failed/superseded}. This is coverage of the SOURCE CORPUS only -- NOT organs,
equivalence, or a taxonomy of cognition (those are Nyx's).

By decade (specimens): 1980s 26, 2010s 25, 2000s 20, 1990s 18, 1970s 7,
  1960s 7, 1950s 3, unknown 3.  -> pre-1970 remains the thinnest era.
Sparsest domains (mechanical, top): digital-evolution (n1,run0 -- avida not
  unlocked), genetic-programming (n1,run0), then a long tail of n=1 singletons
  (pid, mark-sweep, branch-prediction, burrows-wheeler, ...).
Disposition axis: only 15 / 109 carry a recorded historical disposition
  (failed/superseded) -- the LOSERS axis is the emptiest first-class axis.

--------------------------------------------------------------------------------
F. ACQUISITION FRONTIER
--------------------------------------------------------------------------------
IS THE LANDSCAPE ACTUALLY GETTING SPARSE?
  Not yet -- but the character of what remains has changed. Each of the last two
  rounds still found WHOLE ABSENT DOMAINS on the first look (R07: theorem-proving,
  computer-algebra, bioinformatics; R08: conversational-AI). So the domain
  frontier is not exhausted. What IS thinning is the supply of EASY canonical
  targets: the cheap "clone-a-repo-and-run" software is largely in. What is left
  concentrates in three harder bands -- (1) pre-1970 historical worlds (few
  survive as runnable artifacts; need faithful reconstruction or emulation),
  (2) LOSERS / failed branches (15/109; deliberately unfashionable to preserve),
  (3) heavy / submodule'd / platform-bound builds (avida, swipl, souffle, xv6,
  the BSD-TCP experiment). Breadth is still gettable; it now costs more per
  fossil, and the vault should expect a lower fossils-per-hour rate.

WHERE SHOULD TECHNE DIG NEXT (ranked)?
  1. LOSERS. The emptiest axis (15/109). Deliberately acquire superseded/
     abandoned approaches WITH their disposition (e.g. failed congestion-control
     variants, dead language runtimes, retracted algorithms). Highest marginal
     value for Nyx's failed-branch reading.
  2. FIX THE SUBMODULE ACQUISITION GAP, then unlock. avida revealed that git
     fossils were acquired without submodule bodies. Re-acquire with gitlink
     SHAs + submodule bodies pinned, then unlock avida (digital-evolution
     runnable), and attempt swipl / souffle / xv6.
  3. PRE-1970 WORLDS. 1950s=3, 1960s=7. ELIZA shows the faithful-reconstruction
     pattern works; extend to more period programs (early interpreters, games,
     numerical routines) with pinned provenance.
  4. TECHNE-69b: the emulated-VAX BSD-TCP netstat-under-loss experiment -- still
     the single highest-value networks depth dataset -- needs a prebuilt disk or
     a faster host.
  5. TECHNE-65: an off-host mirror destination. One host is still the sole
     preservation dependency for all 109 bodies; this is a standing risk, not a
     breadth item, and outranks new acquisition if a reachable destination appears.

--------------------------------------------------------------------------------
G. LIMITATIONS (honest)
--------------------------------------------------------------------------------
- All oracles are Techne's own; no second seat has verified them.
- R08 is lighter than R07 (1 new + 1 depth): the avida unlock consumed the
  round's build budget and returned a preservation-gap finding, not a fossil.
- Several recipes install from the network at build/run time (apt/pip/git
  submodule clone); the pinned tarballs/commits are the reproducible core, but
  apto for avida is fetched unpinned (hence the segfault).
- Coverage/sparsity are mechanical counts, deliberately NOT a theory of what the
  machinery means. Decomposition, organs, equivalence remain Nyx's.
================================================================================
END OF PACKET
================================================================================
