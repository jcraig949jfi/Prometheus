# Ludus journal -- 2026-09-16 (boot on M2; LUDUS-03 controls)

Instance Ludus[m2-c3a5ef7a] (harness session c3a5ef7a-...). First Ludus
pass on M2; the 2026-09-11 adoption pass ran on M1.

## Boot

- Started in the canonical checkout (git-dir == git-common-dir): refused
  per WORKING_CONTRACT s1. fetch; origin/main = ccb26df01; worktree add
  D:\Prometheus-worktrees\ludus-boot-2026-09-16 -b ludus/boot-2026-09-16
  from that SHA (43,352 files, guard passes, clean).
- Read in BOOTSTRAP order: base RESPONSIBILITIES + WORKING_CONTRACT,
  CHARTER_v3, ROLE.md, ARCHAEOLOGY, BACKLOG_H0H5, STATUS, the reactivation
  prompt (MANIFEST hashes verified against the origin/main blobs:
  793b3d57..., 520307e7...), journal/2026-09-11.md.
- comms from M2: localhost is the M2 fork (WRONG_ENVIRONMENT, incident
  c84e26826cc12217; one line appended to roles/Hermes/incidents/...).
  With EW_DB_HOST=192.168.1.202: boot Ludus[m2-c3a5ef7a] heavy
  claude-opus-5[1m] caps WORLDS,H4,EVIDENCE; sync 8 new (broadcasts #50
  #80 #111 #114 #123 #131 #174 #231), 0 addressed to Ludus, queue 0.
  Online: Herakles, Daedalus, Archaeon (M2 instances at ccb26df01).
- Sibling commits: none touching ludus/ or roles/Ludus since 87d676058.
- Watchdogs: none owned; MONITORS row migrated UNDECLARED|UNDECLARED ->
  "not a loop ..." | Ludus (verified on M2: 0 scheduled tasks and 0
  processes matching ludus/atlas; Get-ScheduledTask, Win32_Process).
- atlas.db ABSENT on M2 (D:\Prometheus, D:\Prometheus-worktrees searched;
  no F: drive). Blocks LUDUS-27/28/30 only. Not chased.
- **/reports/ is gitignored (.gitignore:28); ludus/atlas_of_worlds/
  reports/LOOP_LOG.md is tracked only because it was force-added. Noted
  on LUDUS-27.
- Baseline re-run on this host, matches 09-11: arena verify ALL PASS
  (20/20); test_epistemic 25/25; bench verify 4/21 (rules_fidelity.json
  reverted, not committed); test_base_role 11 passed.
- Work items proposed in chat (LUDUS-03, -01, -02, -04, -06); started
  LUDUS-03 without redirect.

## LUDUS-03 -- the unrun control halves

Built ludus/controls/{fixtures.py, run_controls.py}; ran
`PYTHONPATH=. python ludus/controls/run_controls.py`; wrote
ludus/controls/CONTROLS_2026-09-16.json (25 rows, base_sha/branch/
worktree/dirty inside). Two instrument edits, both my lane:
- ludus/baselines.py greedy_action: own_score(state, who) hook so a
  fixture can declare its score; a world with neither a known name nor
  the hook still raises AttributeError (loud; row CTRL_BARE_UNKNOWN).
  Regression rows: LOOM/WEIR/TITHE curves identical to the committed
  2026-08-26 ledger.
- ludus/arena/verify.py: the [7] key-name predicate moved into
  kuhn_keyname_leak() unchanged so the control calls the real predicate;
  verify still ALL CHECKS PASS.

Readings (exact numbers are in the JSON; the shape here):
- depth profile: LEDGER 0/0/0/0 (negative ok); ORCHARD .793/.616/.468/
  .345 (positive ok; limit: constant policy also solves it); NIM(3,4,5)
  exhaustive 517 states .563/.522/.296/.240 ADMITTED while Bouton's
  4-line rule is optimal on 430/430 N-positions; NIM(3,4,5,6) n=250
  .664/.656/.528/.496 ADMITTED, 4847/4847. The default n=250 sample on
  (3,4,5) read gap(4)=0.200, exactly on the gate: judged on the
  exhaustive reading, and recorded.
- bench verify: pot+1 -> per_world fires; largest prob*0.5 (mass 0.0125)
  -> universal fires; injected cycle -> acyclic fires; smallest prob*0.5
  (mass 3.8e-11) -> NOT detected, below the 1e-7 tolerance: my first
  injection was ineligible, kept as the eligibility row; one-ray-face
  Martian Dice (MD_W ray=1) -> verified_internally=True, 8555 states
  either way: the instrument cannot see the draw law, exactly as its
  docstring says. LUCKY_NUMBERS -> loud NO PER-WORLD INVARIANTS.
- arena: clean Kuhn 0 leaks / key-name check silent. Named key
  opponent_card: both fire. Innocuous key tiebreak: key-name check
  SILENT, differential fires (observation). Action-order leak: key-name
  silent, differential fires (legal_actions_order, error_text).
  public_state leak: key-name silent, differential fires (public_state,
  serialize, state_hash).

Not run: the Replay.redacted_for leak (LUDUS-17); any world build; any
matrix write; Evidence Wiki.

## Operator, 12:1x UTC: M2 is this ecosystem's host now

"We started up on M2. M1 is running a different ecosystem for now. We are
going to run the SFE ecosystem on M2 now." Consequences recorded for the
next instance: (a) no Ludus work item depends on M1 except atlas.db
(LUDUS-35; a hash-verified copy from the M1 worktree is the fix);
(b) comms/EW still route to the canonical store on M1 via EW_DB_HOST,
because comms.identity pins it there and every M2 seat today does the
same; a move of the comms store is Archaeon/Hermes's to announce, and
this seat will not route around the guard.

## LUDUS-01 -- Martian Dice against the published sheet

- Fetched http://jrsaathoff.com/files/md_rules.pdf (2 pages, TMG 2011,
  sha256 cc8297f2...); tesera.ru copy was a DDoS-Guard page (deleted).
  PDF not committed (publisher's sheet); rules_sources/.gitignore *.pdf;
  README with URL + hash.
- Text extracted with pypdf; 7 lines graded in ludus/bench/rules_audit.json
  with the sentence quoted per line: MD-1..4, MD-6 OK; MD-5 WRONG twice
  (R1 rays repeatable; R2 unclaimable roll scores, not bust); MD-7 scope.
- worlds.py: MartianDice corrected (6-tuple with `dead`); MartianDiceRecon
  keeps the 08-27 behaviour and reproduces the committed column exactly
  (8555 states, EV 2.093806). verify.py check_martian rewritten from the
  sheet; rules_audited now read from rules_audit.json.
- `python -m ludus.bench.run MARTIAN_DICE` (131 s): 14653 states, EV
  3.110382; old column preserved as MARTIAN_DICE_RECON_2026-08-27. Axis
  decomposition reversed; Kendall tau STOP 0.429 / SELECT 0.733.
- bench verify re-run and rules_fidelity.json COMMITTED this time (a
  verification run): MARTIAN_DICE VERIFIED-INTERNALLY, rules AUDITED (P3).
- controls re-run on the audited world: 25/25; CONTROLS json recommitted
  (bench rows now 14653 states); packet 6 annotated.
- Annotated (not edited): RULES_AUDIT.md, CYCLE_002 head, stopworlds.py.
- NOT done: maturity.py regeneration (LUDUS-36, needs prereg); the other
  three worlds (LUDUS-15); any second source.
- Packet 7 written; comms report to Archaeon at close.

## Close

(filled at session close below)
