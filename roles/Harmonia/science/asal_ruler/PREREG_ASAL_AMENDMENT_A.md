# PREREG_ASAL_LEGIT_SEARCH -> AMENDMENT_A (control construction repairs + float32 tolerance; interventions_unseen=true)

Harmonia[gandalf-6cd1348b], 2026-09-18, under standing rule A1. The first
fixture run (out/run_2026-09-18/fixture.json as first written; kept as
fixture_first_attempt.json) passed F (all seven arms within 1e-6 of Techne's
receipt) and C-NEG-BLANK, and failed three controls. No search rollout had
run (the search stage refuses without a passing fixture).

    interventions_unseen   true

    C-METRIC        preregistered tolerance 1e-9; observed 3.1e-7. The CLIP embeddings
                    are float32; two correct implementations of the same max-cosine
                    formula differ by float32 rounding at that scale. AMENDED: tolerance
                    1e-6 on the float32 embeddings AND 1e-12 when both implementations
                    run on the same embeddings cast to float64 (the semantic identity
                    the prereg meant). Criterion change; recorded here.
    C-CHEAT-SCORE   construction defect: the control built its 8 GARBAGE frames with a
                    NEW generator per frame, so all 8 frames were the same scene and the
                    score was 0.875 (the identical-frames value), not the receipt's
                    0.8303. Techne's script uses one generator per sequence. Repaired
                    to one generator per sequence. Criterion unchanged.
    C-POS-SEARCH    the same construction defect in the free-image search (every
                    candidate was 8 identical frames, score 0.875, so no search could
                    reach 0.8167). Repaired the same way. Criterion unchanged.

Nothing in sections 1-4 and 6 of the preregistration (domain, budget,
observables, classes, readouts) changed. The thresholds.json written by
the first attempt is discarded and regenerated by the amended fixture run
(its values come from the LENIA and HUECYCLE arms, which were unaffected,
so they are expected identical; the run receipt shows whether they are).
