# PREREG_HARM56_OBSERVER_DEPENDENCE -> AMENDMENT_A (native anchors, two views, disposition A/B/C; before any native score exists)

Harmonia[gandalf-6cd1348b], 2026-09-19, on the operator's second directive
of the day (prompts/2026-09-19_asal_pipeline_direction/OPERATOR_DIRECTIVE_2_
next_verbatim.md, s1). No native-observer score exists anywhere in the
program at the time of this amendment; interventions_unseen = true. The
torch thresholds of packet 001 are NOT moved by anything below.

## A. Native control anchors (added input)

The scoring host scores, through the same native Flax path and from
frame sets generated by THIS seat's fixture code exactly as the seven-arm
fixture generated them (float32 224x224x3 in [0,1], fed to the observer
without further quantisation, as the torch fixture fed them):

    required     GARBAGE seeds 0..4 (5 sets), LENIA = Orbium rollout, STATIC (Orbium frame 0 x 8)
    preferred    CYCLE2, HUECYCLE, DRIFT_SYN, NOISE seeds 0..4, CHEAT (8 identical scenes)

Delivered as transfer/harm55_anchors/<ARM>.npy + MANIFEST_anchors.json
(sha256 each; the torch fixture score beside each arm, from fixture.json).
Scorer input: the anchors are RGB already; the scorer must NOT pass them
through the grey->RGB path. (Techne's harness gains one input mode; no
other change.)

## B. Two views, reported side by side, never merged

    VIEW 1  LOCKED THRESHOLDS   the frozen torch boundaries (catalogue 0.8076, garbage
                                mean 0.8167, 2-sd 0.7999); only the observer changes;
                                every crossing state, A-F question, probe and the
                                discordance table of the original contract are VIEW 1.
                                This tests TRANSPORT of the existing claim.
    VIEW 2  NATIVE ANCHORS      the same organisms placed relative to the NATIVE garbage
                                mean and sd (5 seeds), the native Orbium score and the
                                native STATIC value: for each rollout, score_flax minus
                                native garbage mean in native garbage sd units; crossing
                                of the native garbage mean and of native (mean - 2 sd);
                                position relative to native Orbium. This tells what the
                                native representation MEANS. It never replaces VIEW 1.
    SCALE CHECK                 native STATIC must equal (T-1)/T = 0.875 (the metric's
                                identity value; observer-independent). If native GARBAGE
                                and Orbium shift together (a calibration shift) VIEW 1
                                discordance is read against VIEW 2 before any disposition.

## C. Disposition (one of three; correlation is never the rule)

    A  OBSERVER-STABLE PHENOMENON      catalogue crossings (the five), genuine-life crossings
                                       (incl. S2_135) and deep-search crossings (torch bottom
                                       ten) SUBSTANTIALLY survive under VIEW 1 AND under VIEW 2:
                                       operational rule -- in each of the three subsets at
                                       least half the torch crossers cross the native garbage
                                       mean (VIEW 2) and at least half cross the locked mean
                                       (VIEW 1). -> full-domain replication.
    B  STRUCTURED OBSERVER DEPENDENCE  particular classes / lineages / phenotypes SYSTEMATICALLY
                                       gain or lose crossing: a probe P1-P5 subset in which the
                                       flipped fraction exceeds the population flip rate by a
                                       factor >= 3 with n >= 5, under either view. -> replication
                                       AND the discordance itself becomes a mechanism target
                                       (its keys go to Nyx and Techne as objects).
    C  RECONSTRUCTION-SPECIFIC         the crossings disappear under the native observer
       PHENOMENON                      (VIEW 2: fewer than one in ten torch crossers cross the
                                       native garbage mean, across all three subsets) WHILE the
                                       native controls behave coherently (STATIC 0.875; native
                                       GARBAGE below native Orbium; native NOISE above native
                                       GARBAGE). -> stop the present ASAL scalar claim; preserve
                                       the discrepancy as a fossil; no rescue campaign.
    otherwise                          UNRESOLVED with the failing control or missing subset
                                       named (e.g. native controls incoherent: the native path
                                       itself is the object, not the phenomenon).
    The replication is frozen ONCE, after this disposition, only under A or B.

## D. Coverage is a gate (replication precondition)

The replication's domain is enumerated as an explicit member list; Techne's
acceptance census (machine-readable, one row per member, EXECUTABLE or
EXCLUDED with a named reason) must account for 100% of members before the
freeze; a member neither executable nor explicitly excluded blocks the freeze.
This seat's `--stage accept` re-runs the census independently and the two
must agree row for row.

## E. What did not change

Everything in the original contract (columns, crossing states, classification
stability, A-F by named subsets, band +/- 0.01, probes P1-P5, controls C-SELF /
C-HASH / C-STATIC / C-ORDER, rule A6 vocabulary).
