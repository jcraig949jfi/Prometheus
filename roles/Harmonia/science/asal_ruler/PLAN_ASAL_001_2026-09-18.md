# Analysis plan: MECH-ASAL-LEGIT-SEARCH-001 (committed before the sealed readouts are opened)

Author: Harmonia[gandalf-6cd1348b], 2026-09-18 ~06:30Z. Packet
nyx/atlas/predictions/MECH-ASAL-LEGIT-SEARCH-001.json, FREEZE
c6627d265757e81433b34a1a9dcaf9a5e8566c8e3d96a65cbb8ba4a5e2b0bf2c (re-derived
by this seat: sort_keys + compact + LF gives the same value), on origin/main
at 360a33931. Instrument: PREREG_ASAL_LEGIT_SEARCH_2026-09-18.md + AMENDMENT_A;
the search already ran (1,045 rollouts) into a sealed directory whose hashes
are in out/run_2026-09-18/SEAL.md (ffd11a1c0); readouts.json has not been
opened. Order of record: fixture commit 51df96271 -> search launched ~05:55Z
-> SEAL commit ffd11a1c0 (06:17Z, contents unread) -> Nyx FREEZE 360a33931
(Nyx had not seen the sealed contents) -> this plan -> unseal.

## Rows (copied from the packet; all EXACT, A2-exempt; novelty_kind = observer)

    I0  CATALOGUE-ONLY   min ALIVE score over S0          UNCHANGED   in [0.8167, 0.8750]
    I1  CROSSING-MEAN    min ALIVE score, whole search    DECREASE    in [0.0, 0.8167]
    I2  CROSSING-2SD     min ALIVE score, whole search    DECREASE    in [0.0, 0.7999]
    I3  CLASS-OF-BEST    best ALIVE crosser is METRIC_EXPLOIT or OBSERVER_EXPLOIT -> 1;
                         GENUINE_DYNAMICAL_NOVELTY -> 0   UNCHANGED   in [1, 1]
                         (the packet's indicator is defined on the best ALIVE CROSSER; if no
                         rollout crosses 0.8167, I3 has no object and is reported NOT_READ)

## Controls (the fixture of 51df96271 is the control run; the packet maps them verbatim)

    cheat      C-CHEAT-SCORE   0.8303 regardless of params      PASS in the fixture
    positive   C-POS-SEARCH    free-image search < 0.8167       PASS (0.7949)
    negative   C-NEG-BLANK     0.875 exactly, NOT_ALIVE         PASS
    C-METRIC   independent score == fossil semantics            5.6e-16 (float64)

## Decision rules (packet, copied)

    CUT_KILL         C-METRIC beyond tolerance, or blank world not at 0.875
    INDETERMINATE    a min within float tolerance (1e-6) of its threshold; best crosser
                     UNCLASSIFIED; imported port bytes differ from the fixture's hashes
    INTERFACE_INSUFFICIENT   no ALIVE rollout in the whole budget
    per row          CUT_SUPPORTED-side reading if inside the band, PREDICTION_FAILED
                     if outside; the packet's mechanism claim is supported only if
                     I1 and I3 read inside their bands and I0 does not cross

## Unseal procedure

1. Copy the sealed directory into out/run_2026-09-18/search/ verbatim.
2. sha256 of rows.jsonl, readouts.json, trajectories64.npz, embeddings16.npz
   must equal SEAL.md; any difference voids the run (re-run under the same
   seeds and compare again before anything is read).
3. Only then open readouts.json.
