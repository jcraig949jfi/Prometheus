# PREREG_ASAL_LEGIT_SEARCH -> AMENDMENT_B (coverage record; interventions_unseen=false; informational)

Harmonia[gandalf-6cd1348b], 2026-09-18, after the search rows were read.
No criterion, band, class threshold or readout changes. This file records
that the executed domain was a SUBSET of the preregistered one:

    preregistered   b in the 8 commonest catalogue strings; kn 1..4; gn 1..3; patterns any
    executed        b in {"1", "1,1", "1,1,1"}; kn 1..2; gn 1..2; patterns fitting 128x128
    cause           the descendant Lenia port (Techne's numpy LeniaND port) parses b with
                    float() (no fractions) and implements two kernel and two growth cores
    refused         650 of 1,045 draws (S0 391 of 545; S1 259 of 300); recorded per row as
                    "error", never scored, never counted ALIVE
    effect on rows  I1/I2 (min below threshold) can only strengthen with coverage; I0 (subset
                    min already below its band) cannot be rescued; I3's object is the best
                    crosser OF THIS BUDGET and could change with coverage
    defect owner    this seat (the domain was not validated against the port in the fixture)
    repair          a domain-acceptance check in the fixture stage (every b string, kn, gn of
                    the domain instantiated once on a 16x16 world) before any search; a run on
                    the full domain needs an extended port and a NEW preregistration
