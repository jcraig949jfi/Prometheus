# Analysis plan: MECH-PARTICLES-ESSTRIGGER-001 (committed before any adjudicative run)

Author: Harmonia[gandalf-6cd1348b] (M3 GANDALF). Seat charter s4: the plan
is committed before the analysis runs. No adjudicative row had been
executed when this file was written; the only prior execution on this
host is Nyx's declared SCOUT (non-adjudicative) and I reuse none of it.

## Object under adjudication

    packet        nyx/atlas/predictions/MECH-PARTICLES-ESSTRIGGER-001.json
    freeze        5b8d6ae4908abb3b4473a106a741563663006a7236e52cd248293bbbb4bc4c47
                  (re-derived here: sha256 of json.dumps(sort_keys, separators=(',',':')) + LF;
                  the packet's schema.py canonical_bytes)
    read from     branch nyx/boot-2026-09-17 @ 5f07df17a (not on origin/main at plan time)
    body          C:\Prometheus-vault\fossils\particles-chopin-0.4\upstream\tree\particles-0.4
                  record tree e27cbce2...; on this host +11 .pyc under particles/__pycache__
                  (SCOUT residue), 0 payload files modified
    boundaries    core.py  1c99eb9884fc49a45c7e02e24d0d897c92e2300111385160628b97fe313ffb3c  MATCH on M3
                  resampling.py 6657f40f1a7cafecdfe958bdd0fb2403b3f06cc31ced294abc1eebcfc0f24e18  MATCH on M3

## World (the packet's M3-native world, made concrete)

- A DISPOSABLE COPY of upstream/tree/particles-0.4 (ignoring __pycache__) is
  placed first on sys.path; PYTHONDONTWRITEBYTECODE=1; the vault body is
  never imported or written. The ruler asserts particles.__file__ is under
  the copy and re-hashes core.py and resampling.py FROM THE IMPORTED
  MODULES' __file__; a mismatch aborts (packet: INDETERMINATE).
- RUNTIME_WITNESS recorded: python version, `pip freeze` sha256 and line
  count, numpy/scipy/numba/joblib versions, whether resampling.inverse_cdf
  is a numba dispatcher (a plain function means numba fell back: packet
  INDETERMINATE), platform.node().
- Model: particles.kalman.LinearGauss() with the body's defaults
  (rho 0.9, sigmaX 1.0, sigmaY 0.2, sigma0 = sigmaX/sqrt(1-rho^2)).
  ONE dataset: np.random.seed(20260917); x, y = model.simulate(T=100).
  Saved as data.npz beside the ruler with its sha256. Same y for every arm.
- Oracle: particles.kalman.Kalman(ssm=model, data=y).filter(); exact means
  m_t = filt[t].mean; exact logL_T = sum(logpyt). Exact for this model.

## Arms (packet section 3; TESTBENCH level, constructor parameters only)

    arm    N     resampling    ESSrmin   seeds
    I0     100   systematic    0.5       1..50
    I1     100   systematic    0.0       1..50
    I2     100   systematic    1.0       1..50
    I3     100   multinomial   0.5       1..50      (vs I0)
    I3x    100   multinomial   0.5       1..400     ADDED, as the packet invites ("raise to 400");
    I0x    100   systematic    0.5       1..400     reported BESIDE the 50-seed reading, never instead

Per seed: np.random.seed(seed) immediately before constructing and running
SMC(fk=Bootstrap(ssm=model, data=y), N, resampling, ESSrmin,
collect=[Moments()]); pf.run().
Per-seed observables: logLt_hat = summaries.logLts[-1]; R = number of True
in summaries.rs_flags (t=0 is False by construction); per-step means from
summaries.moments[t]['mean'].

Aggregates per arm: V = sample variance (ddof=1) of logLt_hat over seeds;
B = mean(logLt_hat - logL_T); RMSE = sqrt(mean over t and seeds of
(mean_hat - m_t)^2); R min / median / max over seeds.

## Controls, run FIRST; any failure aborts the reading (charter s5)

    C-CHEAT-ORACLE-IN-THE-LOOP   same code path, per-step means replaced by m_t and
                                 logLt_hat by logL_T, for EVERY arm ->
                                 RMSE == 0.0, V == 0.0, B == 0.0 exactly
    C-POS-N-SCALING              I0 settings at N=10 and N=1000, 50 seeds each ->
                                 V(10)/V(1000) in [10, 1000] and RMSE(10) > RMSE(1000)
    C-NEG-UNINFORMATIVE          model with sigmaY = 1000.0, SAME y (the observations
                                 carry no information either way; choice preregistered
                                 here), I0 and I1 settings, 50 seeds ->
                                 R == 0 in every seed under I0; V(I1)/V(I0) in [0.5, 2.0]
                                 (welcome, not a substitute for the two mandatory ones)

## Decision rules (copied from the packet; nothing added, nothing relaxed)

    I0   |B| in [0, 2]; R in [10, 90] informative, not a kill row
    I1   R == 0 in every seed (else CUT_KILL); V(I1)/V(I0) in [5, 1e6]
    I2   R in [98, 99] in every seed (else CUT_KILL); V(I2)/V(I0) in [0.5, 3]
    I3   V(mult)/V(syst) in [1.05, 5]; 95% interval of the variance ratio by the
         F distribution (df 49, 49; for I3x df 399, 399); interval covering 1.0 ->
         INDETERMINATE for that reading
    CUT_KILL         both mandatory controls pass AND (R > 0 in any seed under I1,
                     or R outside [98, 99] in any seed under I2,
                     or V(I1)/V(I0) < 2 with RMSE(I1)/RMSE(I0) < 2)
    INDETERMINATE    any control fails; hash mismatch at run time; numba absent;
                     < 50 seeds; dataset differs between arms; I3 interval covers 1

Return typing (R31): CUT_SUPPORTED if no kill condition fires and I1/I2
read inside their bands; PREDICTION_FAILED on (c) alone if I3 reads below
1/1.05 with adequate power; PREDICTION_INDETERMINATE for any row in the
indeterminate list, named. A reading outside a band that is NOT a kill
condition (e.g. I0 |B| > 2, I2 V ratio outside [0.5, 3]) is reported as
PREDICTION_FAILED for that row with the number, and the boundary claim is
adjudicated on I1/I2 alone.

## What is NOT decided by this plan

Nothing about the SIZE of effects beyond the packet's bands. If the rows
were seen before a rule was written, the rule is void; every rule above
was written before rows existed (this file's commit precedes ruler.py's
first run receipt in git history).
