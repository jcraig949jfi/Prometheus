# Analysis plan: MECH-PARTICLES-ESSTRIGGER-002 (committed before any adjudicative run of 002)

Author: Harmonia[gandalf-6cd1348b]. Supersedes PLAN_2026-09-17.md for packet
002 only; everything not restated here is inherited from that plan (staged
copy, PYTHONDONTWRITEBYTECODE, RUNTIME_WITNESS, hash re-read at import,
observables V/B/RMSE/R, aggregation, F-interval for variance ratios).

    packet   nyx/atlas/predictions/MECH-PARTICLES-ESSTRIGGER-002.json
    freeze   186047db804af234566eb2a6ece10346823bc69ebeafe87eae0e837a5416d284 (on origin/main)
    diff     vs 001 by dict: boundary, mechanism_claim, I1, I2, I3, cut_kill, cheat and
             negative controls IDENTICAL; positive control, I0 bands, W2/I4/I5,
             indeterminate list, corrections, supersedes changed (Nyx #364 s2 confirmed)

## Worlds

    W1  LinearGauss defaults (sigmaY 0.2), T=100, data seed 20260917 -- as 001 (same data.npz
        sha256 as the 001 run, asserted by the ruler)
    W2  LinearGauss(sigmaY=1.0), T=100, its OWN dataset, numpy seed 20260918, saved as
        data_W2.npz beside the run; its own Kalman oracle (exact means, exact logL)

## Controls, run FIRST, abort on mandatory failure

    C-CHEAT-ORACLE-IN-THE-LOOP    every arm code path (W1 arms with W1 oracle, W2 arms with W2
                                  oracle), 5 seeds each -> RMSE == V == B == 0.0 exactly
    C-POS-N-SCALING-IN-REGIME     W1, I0 settings, N=200 vs N=1000, 50 seeds ->
                                  V(200)/V(1000) in [2.5, 20.0] AND RMSE(200) > RMSE(1000)
                                  (mandatory). DISCLOSED by both seats: the SEEN scout gave 10.0.
    C-NEG-UNINFORMATIVE           W1 with sigmaY=1000, same y, I0 and I1 settings, 50 seeds ->
                                  R == 0 every seed under I0; V(I1)/V(I0) in [0.5, 2.0] (welcome)

## Arms (50 seeds, seeds 1..50; I3x/I0x at 400 seeds beside, as before)

    I0  W1 N=100 systematic 0.5     |B| in [0, 30]; R in [90, 99] every seed (informative row)
    I1  W1 N=100 systematic 0.0     R == 0 every seed (else KILL); V(I1)/V(I0) in [5, 1e6]
    I2  W1 N=100 systematic 1.0     R in [98, 99] every seed (else KILL); V(I2)/V(I0) in [0.5, 3]
    I3  W1 N=100 multinomial 0.5    V(I3)/V(I0) in [1.05, 5]; F-interval covering 1 -> INDETERMINATE
    I4  W2 N=100 systematic 0.5     R MEDIAN over seeds in [5, 95] -- read FIRST among W2 rows;
                                    outside -> I5 INDETERMINATE (W2 gave no contrast), boundary
                                    claim untouched; report V, B, RMSE against W2's oracle
    I5  W2 N=100 systematic 1.0     R in [98, 99] every seed; R(I5) == R(I4) seed for seed ->
                                    INDETERMINATE; V(I5)/V(I4) in [0.5, 3.0] supports claim (b)

## Decision rules (packet 002, copied)

    CUT_KILL      both mandatory controls pass AND (R > 0 any seed under I1, or R outside
                  [98, 99] any seed under I2, or V(I1)/V(I0) < 2 with RMSE(I1)/RMSE(I0) < 2)
    INDETERMINATE cheat or positive control fails; payload hash mismatch; numba absent;
                  < 50 seeds; dataset differs between arms of one world; I3 interval covers 1;
                  I4 median R outside [5, 95] (W2 rows only); W2 dataset not saved
    return        CUT_SUPPORTED if no kill fires and I1/I2 read in band; PREDICTION_FAILED on
                  the named row for a non-kill out-of-band reading (I0, I2 V-ratio, I3 with
                  power, I5); PREDICTION_INDETERMINATE for listed items, named

The W1 I0 row is SEEN (scout N=100). Its rule above is the packet's; no
rule in this plan was written after seeing a W2 or I1-I3 adjudicative row.
