# Harmonia[m2-038758c6] journal, 2026-09-16

Instance tag m2-038758c6 (M2 SPECTREX5, harness session
038758c6-2210-4d29-8492-f1f69d463240, bridge session
session_01APeC753MberhMK4TGqo6jD). Convention: roles/Harmonia/INSTANCES.md.

## Boot (base-role s1, in order)

- Woken in the canonical checkout (git-dir == git-common-dir). No pull.
  `git fetch origin`; origin/main = b3e62a959 (Techne, LISP 1.5 runs).
  HEAD of the canonical checkout was ccb26df01 and was not touched.
- `python -m comms instance` REFUSED from the default environment:
  WRONG_ENVIRONMENT, observed db_system_id 7681719240261676752 (the M2
  local fork), expected 7628127204585430828 (M1 canonical). Known class,
  incident c84e26826cc12217 (roles/Hermes/incidents/). Set
  EW_DB_HOST=192.168.1.202 for every comms call this session; tag derived
  m2-038758c6.
- Worktree: D:/Prometheus-worktrees/harmonia-m2-038758c6-boot, branch
  harmonia/m2-038758c6-boot-2026-09-16 from b3e62a959; 43,575 files,
  unbounded timeout, clean (0 status lines). Guard passes.
- `comms boot Harmonia --model claude-opus-5 --capabilities
  scientific-audit,qualification,conformance-contract,rulings`: booted,
  heavy tier. `comms who`: no other Harmonia instance online (m2-f541bed9
  last sync 09-14 07:12, m2-54a6d694 09-14 06:50, m1-486e595f 09-11 15:25).
- `comms sync Harmonia` 15:50Z: 6 new, 0 queued, queue length 0.
  #269 Daedalus: M2 engine relocated and UP (twin eng_906356f7, build
       726275da); M1 8811 not answering; my staged contract reads DRIFT on
       engine_instance_id only against M2, CONFORMANT 68/68 with the twin id.
  #270 Daedalus RULING: production is the LEDGER eng_8a37a5d3 wherever it
       runs; the twin is never promoted; move plan steps 1-4, step 3 is mine
       (promote_candidate_contract.py against M2 after the ledger lands).
  #282 Archaeon: C3-3 preflight re-run under option B, G1-G6 computed, GO
       TRUE, NOT issued (ARCH-02 operator); #260 dossier BLOCKED (engine
       unreachable + no B1 credential on M2); #259 findings -> d3.v3 later.
  #288 Vivarium: #270 accepted, (b) withdrawn, 5 rows HELD, M2 consumer
       PREPARED not launched; one operator gate (tokens) on its side.
  #295 Daedalus: #223 spec_hash/committed_seq (+include_spec) on
       /v2/read/observations CODE_FIXED adb7952ff, NOT deployed; asks me to
       say if my contract model disagrees that it is contract-unaffected.
  #301 Vivarium relays OPERATOR RULING: Q1 (a) M1 ledger carried to M2;
       tokens copied as a file; no replacement identity minted; "Harmonia:
       promote the staged contract against 192.168.1.191".
- Read: base README/INHERITANCE/WAKE/RESPONSIBILITIES/WORKING_CONTRACT,
  aporia/doctrine/critical_memories.md, seat RESPONSIBILITIES.md,
  INSTANCES.md, STATUS.md, BACKLOG_H0H5.md, journal 2026-09-14_m2-f541bed9,
  `git log --oneline -25 origin/main`. No prompt addressed to Harmonia under
  roles/*/prompts/ newer than 09-14 (git log --name-only since 09-14, filtered).
- MONITORS.md: no Harmonia-owned or Harmonia-fed row (seat file s2 says the
  same). Nothing to feed.

## Live state measured at boot (not inferred)

    https://192.168.1.191:8811/v2/version   200  eng_906356f7fb1da180131f9290
                                                 hash 726275da..., schema 8,
                                                 source_commit ccb26df01
    https://192.168.1.202:8811/v2/version   curl exit 28 (timeout, 8 s)

So the ledger move (Daedalus #270 step 1-2) has NOT happened; my step 3 is
not executable yet. What IS executable now is the precheck of step 3.

## Work item 1: step-3 precheck on the M2 twin (executed)

Directory roles/Harmonia/contracts/candidates/726275da9c8d/m2_precheck_2026-09-16/

    promote_candidate_contract.py, candidate unmodified, --base 192.168.1.191
        [PASS] candidate hash is live      726275da == 726275da
        [FAIL] same ledger                 live eng_906356f7, candidate eng_8a37a5d3
        [PASS] same schema                 8 == 8
        REFUSING ... Nothing written.      exit 2
        sha256 of contracts/sfe_contract.json identical before and after.
    conformance_check.py, candidate unmodified, --base 192.168.1.191
        DRIFT, 1 breaking check: engine_instance_id; every other check PASS
        (68 live / 68 contract, 0 added, 0 removed; 30 GET routes scoped).  exit 1
    conformance_check.py, SCRATCH copy with engine_instance_id := twin
        CONFORMANT 68/68, 30/30 scoping.                                   exit 0

This reproduces Daedalus #269 s3 from a second instance with the seat's own
tools, and it is the NEGATIVE control for step 3: the promote refuses on the
ledger identity alone, so after the swap the same command reads 0 iff
/v2/version reports eng_8a37a5d3 with hash 726275da and schema 8. The
scratch copy is named *_SCRATCH.json and carries a note; it is never a
promotion input.

## Work item 2: #295 contract-model answer (decided from the generator)

generate_sfe_contract.py records per route: method, path, path_params,
required_query (= parameters with in=query AND required=true),
required_body, requires_session_key. Optional query flags do not enter;
response bodies are not modelled at all (HARM-35, open, blocked on Daedalus
A0: the engine declares no response models). So `include_spec` (optional)
and the two new response fields are INVISIBLE to the contract, which is
what "unaffected" means here and also its limit: the contract cannot
detect that the change shipped either. Independent of that, the build hash
changes (Daedalus quotes sha256:4dbcd3fd...), so that build needs its OWN
candidate contract before it can be promoted; promote refuses on hash.
Trap T1 (POST experiments response has no spec_hash) is unchanged by #223.

## Work item 3: Archaeon #282 G1/G2 independent recompute (executed)

roles/Harmonia/science/c3_3_option_b_g1g2_recompute.py, ledger at
science/ledgers/c3_3_option_b_g1g2_recompute_2026-09-16.txt. Shares no code
with archaeon/producer/campaign_c3_3.py: masses from Binomial(128,1/2) as
exact Fractions over the declared edges; G2 by inclusion-exclusion AND by a
sequential-binomial DP, which agree to 3e-15.

    masses      max |diff vs Archaeon| 5.6e-17; sum exactly 1
    G2 @180     0.8277184844225   (Archaeon 0.8277184844225445; diff 9e-15)
    G2 @120     0.1184184351298   (Archaeon 0.1184184351298047; diff 4e-15)
    min corpus  176 (175 -> 0.7925, 176 -> 0.8000147)
    controls    negative 120 fails; positive 180 passes; cheat (pc04 starved
                to 0.001) -> 3.2e-11

G1 and G2 stand as computed. G3-G6 are read from Archaeon's JSON (not
recomputed here: G3 is arithmetic on the same masses, G4 is a seeded MC
whose two conventions both sit below the 0.80-complement region of concern,
G5/G6 are executor facts). Note on G4: the readout's eligibility floor
(MIN_REGION_N = 8) selects the 0.109 row; my #255 quoted 0.131 under n >= 2.
The readout must print which floor it applied; the two differ by 0.022.
Note on the margin: 176 clears 0.80 by 1.5e-5 -- exact, so not a
SE-vs-gate problem, but 180 is the declared constant and it clears by 0.028.

## Suggested work items at boot (base s1.6), in order

    1  step-3 precheck on M2                       DONE above; rows committed
    2  #295 answer                                 DONE above; posted with 1
    3  #282 G1/G2 recompute + ack to Archaeon      DONE above; posted
    4  HARM-30 shared-arm correlation in
       paired_contrast (S, no blocker)             next; artifact: the function
                                                   + test on the fresh/S00 case
    5  HARM-31 standing-rules index (S)            after 4; roles/Harmonia/
                                                   STANDING_RULES.md
    blocked: step 3 proper (operator lands M1 data dir; Daedalus swaps);
             HARM-18/19 (operator: d3.v2 admission, C3-3 issue); #260 dossier
             (Archaeon, on the same move).

## Ship 1

- Merged origin/main 39068c1a0 (Vivarium THEO-REQ-002) into the branch; no
  conflict. archaeon/tests/test_base_role.py: 11 ERROR at setup without
  EW_DB_HOST (fixture connects to localhost = the M2 fork, WRONG_ENVIRONMENT);
  11 passed with EW_DB_HOST=192.168.1.202. Same class as the comms refusal at
  boot; recorded, not chased (the fixture is Archaeon's).
- Shipped cec0e5d74 (fast-forward 39068c1a0..cec0e5d74; ancestor verified).
  Posted #305 (report -> Daedalus, Archaeon, Vivarium, Nestor) and #306 (ack
  -> Archaeon, Nestor). Sync after: 0 new, 0 queued.
- Next: HARM-30 (shared-arm correlation in paired_contrast), then HARM-31.

## Amendment 3 received from the operator in chat (~17:30Z); #309 from Nyx

- Operator pasted "PROMETHEUS MECHANISM ARCHAEOLOGY PIPELINE, Amendment 3" into
  this session. Transcribed verbatim (ASCII) to
  prompts/2026-09-16_mechanism_archaeology/AMENDMENT_3_as_received_by_Harmonia.md;
  diffed against Nyx's transcription (roles/Nyx/prompts/2026-09-16_mechanism_
  archaeology_pipeline/AMENDMENT_3.md, manifest verified 4/4): 413/413 lines
  identical after punctuation normalisation. Founding Charter and Amendment 2
  read from Nyx's copies; Amendment 1 not on record at either seat. Amendment 2
  also at roles/Techne/prompts/2026-09-16_amendment2/OPERATOR.md (same hash
  815e84b3...).
- origin/main moved: a1dd1458c Daedalus launched SFE production on M2 on its
  OWN ledger under an operator chat ruling and landed a regenerated contract
  in roles/Harmonia/contracts/ (my lane, operator clearance cited). My step 3
  (promote 726275da) is moot; HARM-43 opened to re-verify from here.
  f8b84265e / c1592a5c1 Nyx executed N1-N4; packet 002 supersedes 001.
- sync 17:26Z: 1 new, 1 queued -- #309 Nyx delegation (packet 001 + RS pair;
  R31 ACK <= 1 tick). `comms claim Harmonia 309` -> CLAIMED. ACK posted #316.

## Ruler calibration (N3) -- executed, not read

- No gcc on the Windows host or in the WSL distro; Techne's world
  prometheus-fossil-c:bookworm is present in WSL docker. Bodies in the M2
  vault D:/Prometheus-vault/fossils (Nyx's 09-16 acquire). rs.c 0cbf100e...;
  Karn's nine files hashed in out/bodies.sha256 (LF on this host).
- science/rs_ruler/ruler.c: rs.c #included with main renamed, Karn's three
  _int.c compiled as objects, one driver; positions mapped i <-> 14-i;
  outcomes from the output word. build_and_run.sh runs pair / self-karn /
  self-rock / mismap at 2000 trials, seed 20260916.
- Results in RULING_RS_CALIBRATION_PAIR_001_2026-09-16.md s1-s3. Surprise:
  33/2000 word-level divergences at e=4 (Karn's BM runs 2t steps, Rockliff's
  stops at degree > tt). Blind spot: mismap e=0 reads identical because Karn
  silently accepts syndrome (s0,0,0,0,0,0) -- traced with a throwaway driver
  in the container (not committed; the reasoning is in the ruling s4).
- Two throwaway debug drivers were compiled in the container against the
  built objects and deleted; nothing in the vault was written (mounted :ro).

## Packet 002 checks (R33) -- the challenge

- Re-hashed 002 canonically: a30f3348... == .FREEZE; 001 861dded0... ==;
  schema validate: valid both.
- deflate.c payload hash in the vault == b0ba92b0... (the packet's). 225-245
  is the table; 286 is lm_init (closes at 342, packet says 356); trees.c:987
  is the flush gate. deflate.c:667 is `int match_available = 0;`; the switch
  `if (compr_level <= 3) return deflate_fast();` is at 672. The cut script
  (line 2) and the packet (four places) say 667. I2 cannot be executed as
  written -> PREDICTION_PACKET_CHALLENGE (new typed return under R31).
- grep for `level` across the sources: trees.c:897/909/927 read it only under
  #ifdef FORCE_METHOD (not the default build) -> noted as build-config scope.
- Techne's FOSSIL_PACKET.json already carries FOSSIL_WORLD_ID fw-01f8b51f...,
  RUNTIME_WITNESS, HOST_CAPS_ID, SCAFFOLDING_LEDGER, PRESERVATION; Nyx's
  anticipated DEFER-on-Techne does not arise.

## Lane currency

- RESPONSIBILITIES.md s8 (second lane), BACKLOG HARM-37..43, STATUS block.
- Disposition file prompts/2026-09-16_mechanism_archaeology/RETURN_NYX_309_DISPOSITION.md
  (return 1 CUT_SUPPORTED/ACCEPT; return 2 PREDICTION_PACKET_CHALLENGE/CHALLENGE).
