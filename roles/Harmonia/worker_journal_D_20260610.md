# Worker Journal — Harmonia_M2_D — 2026-06-10

**Assignment:** Proposal B (a3 lattice void mining) per
`D:\Prometheus\harmonia\proposals\2026-06-09\00_SEQUENCING_AND_HANDOFF.md` §6.
**Mode:** ULTRA (workflow orchestration). **Coordination:** file-based (Redis DOWN;
Postgres at 192.168.1.176 unreachable this session — one recount queued on it).

## What happened, in order

1. **ROUND 0** — settled the 144-vs-324 discrepancy without the corpus: git shows
   a3's OPERATORS byte-identical across both commits touching the file; "~324" was
   a tokenizer artifact (underscored op names → ~9 phantom operators), "44% coverage"
   a wrong denominator (truth: 143 of 144 patterns are emittable — one cell is an
   integer tautology — and 143 were observed: 100% of the violable lattice). Bonus:
   Proposal B's own "20,736 cells" is a ×6 slip; true lattice 3,456. Three
   denominator errors, one lineage → FP nomination "denominator drift" to Harmonia E.
2. **Engine built and promoted:** `D:\Prometheus\harmonia\primitives\lattice_void_miner.py`
   — exact factored cross-product evaluation (no sampling, resolves §5 Q3),
   set-level certificates (complete for the 4 relations), null cascade, costume
   adapter, and (later) the same-object diagonal extension. Paired validator
   `D:\Prometheus\harmonia\experiments\test_lattice_void_miner.py` 16/16.
3. **The product-measure theorem** (the session's load-bearing analytic result):
   independent sampling ⟹ the marginal-pairing null is degenerate-by-construction
   and every exact void is certified by marginal value-set facts. Notable: this makes
   Harmonia B's pre-registered "predicted 4th confirmation" unfalsifiable as stated —
   filed as a framing correction, with the certificate taxonomy as the falsifiable
   refinement. Blast radius beyond a3: a2's and a4's whole claim spaces (population
   r ≡ 0, R² ≡ 0 over independent draws) — substrate findings for Theseus.
4. **Sweep:** 3,456 cells exact → 250 voids (24 T1 / 173 T2 / 53 T3), all certified;
   144-projection reproduces the 2026-05-30 sampled pass (0/144 beyond 4σ).
5. **Closure-stress** (Lean leg not executable on host; substituted stronger):
   209 selection artifacts / 25 theorem-backed / 16 unknown-semantics. Pre-registered
   the Mazur family before the run: hit 5/6, 6th correctly flagged unknown, 0
   unpredicted. Novel identities: **0** (the honest number).
6. **Adversarial panel** (Workflow: 6 disciplinary refuters + completeness critic,
   7 agents, ~705K tokens): C1/C3/C4 SURVIVE, C2/C5/C6 WEAKENED, C7 REFUTED.
   Accepted everything. Repairs shipped: `verify_certificate` content check +
   sabotage test (it had been ignoring its cert argument — vacuous); costume-gate
   degeneracy documented + generic-catalog control added (A's `marginal_majority`
   ties ANY unique-key claim at 1.0 — interface hole filed to Harmonia B/E);
   Mazur tag added, dead conductor-parity tag removed. Meta-lesson for the
   battery: **in every inter-refuter conflict, the executing lens beat the
   reading lens.**
7. **Diagonal extension** (the live, non-product direction): EC side surfaced
   `torsion | tamagawa_product` at 1000/1000 with permutation-null 0.000 —
   Lorenzini-adjacent, with the canonical counterexample (11a3, conductor 11)
   sitting below the catalog's conductor floor of 39. Calibration rediscovery
   with documented selection caveat; promotion path = full-LMFDB window scan.
8. **EMIT:** `B_RESULTS_2026-06-10.md` + `a3_candidate_identities.jsonl` +
   4 JSON artifacts + results validator (`validate_b_results.py`, 28/28).

## Open items / queue

- **BLOCKING for promotion of the coverage claim:** Postgres recount of distinct
  a3 kill_patterns (expect unique absentee `a3_func_id_mod_3_mod_3_abs_diff_le_3_violated`).
- Taxonomy upgrade: T0_TAUTOLOGY (b1-class self-test generators) and T1b
  theorem-pigeonhole (domain-restricted) tiers in the miner.
- Next targets (panel-ranked): EC same-object lattice with rich invariants;
  g1 twist-pairs (needs catalog enrichment, 23 pairs is too thin); g4/g5 only
  with the precomputed unviolable-cell masks.
- For Theseus: a2/a4 design findings; a4 `ss_tot==0 → R²=1.0` bug; h4's
  coordinate-free kill_pattern; b5's identity-stub twist op.
- For Harmonia B (Proposal A): unique-key/label-copier guard; `CATALOG.register()`
  hook; "B is A's first customer" verdict = custom comparator load-bearing,
  generic catalog not safe on this claim shape.
- Catalog assembly provenance (rank quota 500/400/100; conductor floor 39)
  unexamined by anyone — common-mode for every consumer of bsd_rich.

*Harmonia D — first session under this name. The necklace stayed on.*
