---
role: Harmonia_M2_C
date: 2026-06-10
mode: ULTRA (multi-agent orchestration)
directive: James — own Proposal E; push past "repaired the wound" to "the
  principle that governs the wound"; iterate-or-kill to convergence
arc: Proposal E executed end-to-end + the Information-Recovery Law extracted,
  cross-generator evidence gathered, auditor primitive promoted
---

# sessionC worker journal — 2026-06-10

## What was asked vs what was found

Asked: backfill h2's 87K opaque kills and test whether the new structured
subclasses carry signal — then generalize to a law of when finer failure
labeling recovers vs repaints.

Found: the question dissolves one level deeper than the proposal framed it.
The corpus is absent on this host (daemon paused 2026-05-29), but schema
archaeology made backfill provable anyway (legacy payload identical →
backfill = pure function, shipped + validated 99.94% byte-parity, with the
0.06% residue being *production's* bug, not the backfill's). The signal
question split cleanly into transmission (label) vs availability (mechanism):
h2's labels now transmit fine; its mechanism has ≈ 0.01 bits/kill to
transmit. Saturation of the entire mechanism: ≈ 4 bits. 87K records,
≤ 4-bit object.

## The law (working theory)

1. Labels transmit only coordinate content; path-derived components carry
   exactly zero conditional information (theorem under independence;
   confirmed empirically on production code: CMI 0.0004 vs null95 0.0044).
2. Available info per record = I(coords; fresh outcome) — fixed by mechanism,
   untouchable by labeling (DPI).
3. Ledger value saturates at Σ_cells H(outcome|cell); volume past saturation
   is re-measurement.
Scope: requires evaluation independence given coordinates — 13/14 audited
generators hold it; d4 violates it (corpus-state-coupled eval), and that
boundary is *detectable* (BEYOND_COORDINATE_SIGNAL verdict).

One law, three kill-topography findings unified (volume-uniformity =
saturation; a3-lattice-is-real = deterministic mechanism + coordinate labels;
h2-black-hole = starved label over empty mechanism).

## Discipline record

- **Pre-registration before measurement** (h2_info_recovery_prereg.md):
  binding thresholds for Q1–Q5; all verdicts read against them; stopping rule
  honored (S3/S4 rounds both null → stop; analytic decisiveness noted).
- **Self-dissent executed:** circularity trap (Y from the same instrument) →
  alternative-Y robustness run (source instrument a4@30: same magnitude);
  volume-vs-info trap → quantified, it IS the headline; plug-in MI bias →
  analytic computation alongside empirical with permutation nulls.
- **Lens panel:** L1 information-theoretic, L2 decision-theoretic (0.000 lift
  everywhere), L3 structural (theorem + code audit). Three frames agree →
  coordinate-invariant tier for the h2 anchor claims. My Q4 costume-gate leg
  was RETRACTED as corroboration mid-session: Harmonia B's adversarial review
  (REVIEW_C_h2_backfill_by_B.md, S1) showed my claim construction was the
  baseline's own definition — tautology, zero evidential weight. Accepted
  without argument; tail-then-act caught the review pre-commit. The z=0.0 was
  in my own output and I had read it as "degenerate" without following
  through to "degenerate-by-MY-construction" — the reviewer finished the
  thought I dropped.
- **Calibration anchor in the fan-out:** h2 included as known-truth control;
  the audit agent independently rediscovered the index-shift bug I'd found
  reading the code — lens calibrated before trusting the other 13 rows.
- **Ownership respected:** FP registry is Harmonia E's lane — FP-002 numbers
  + proposed FP-004 emitted as a handoff packet in E_RESULTS §6, not edited
  into their file.

## Surprises (the gradient, not the verdicts)

1. The corpus being GONE was the session's first real finding — and it
   *improved* the experiment: regenerating through production code gave full
   experimental control (labels and fresh evaluations from the same oracle).
2. The latent index-shift bug fires in production at 0.03–0.06%, 100%
   concentrated on nf_class_number pairs — found because the parity check
   was pre-registered as exact (100% or report shapes). A 99.9% threshold
   would have hidden it.
3. payload_rederivable = true for ALL 14 generators. The schema-retention
   disaster Proposal E feared does not exist; the lint becomes invariant-
   pinning, not repair. The substrate's hygiene was better than its
   reputation — the labels were the weak layer, and only the labels.
4. d3 is the purest pathology found: hundreds of distinct labels, zero
   coordinate bits. FP-002's detector is structurally blind to it (healthy
   entropy). Proposed FP-004 covers exactly this blind spot.
5. a4 — h2's own parent — has the same 24-pair space and emits ONE kill
   string. The next h2-shaped hole was upstream of h2 all along.

## Artifacts

All in E_RESULTS §7 (full paths). Net new: 1 primitive
(kill_scheme_info_audit.py, self-tested + dogfooded), 3 experiment harnesses,
3 results JSONs, 1 pre-registration, E_RESULTS_2026-06-10.md, this journal.
Workflow run wf_23fde6d8-86a (14 agents, ~458K tokens).

## Open / next

- FP-004 registration + FP-002 anchor update → Harmonia E (packet in
  E_RESULTS §6).
- h2 tag-misattribution 2-line fix (R2) — trivial, but touches Theseus
  production code while the daemon is paused; left for the Theseus owner
  with the exact mechanism documented.
- d4 state-coupling: the law's boundary case, classified not measured — the
  natural next empirical anchor.
- Coordinate-starvation fixes (a4/d1/h4 labels) — R3, one-line pattern each.
- If the historical corpus resurfaces on another host: run the shipped
  backfill + emit the 24-row profile table (§4) for the Learner.
