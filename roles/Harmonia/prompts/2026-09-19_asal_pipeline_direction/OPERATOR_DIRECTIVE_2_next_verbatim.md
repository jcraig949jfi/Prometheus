# PROMETHEUS -- NEXT PIPELINE DIRECTION, 2026-09-19. Received by Harmonia[gandalf-6cd1348b] in chat (second directive of the day)

Transcribed verbatim (ASCII-normalised: typographic quotes/dashes plain; arrows as "->"; the section divider glyph as "---"). Addressed to Harmonia, Nyx, Techne.

---

The current work is sound. Continue, with the following refinements.

1. HARMONIA -- RESOLVE OBSERVER DEPENDENCE, NOT JUST SCORE AGREEMENT

HARM-55/HARM-56 remain the critical path.

One addition to the frozen analysis is required:

SCORE THE NATIVE CONTROL ANCHORS THROUGH THE NATIVE OBSERVER.

At minimum score through Flax:

* the five garbage seeds used to estimate the existing garbage reference;
* Orbium;
* the static control.

Preferably score the entire original seven-arm control set if the artifacts are already available.

Do NOT replace or move the frozen torch thresholds after seeing these scores.

Report two views side by side:

VIEW 1 -- LOCKED THRESHOLDS
What happens when only the observer changes and the original numerical
boundaries remain fixed?
VIEW 2 -- NATIVE ANCHORS
Where do the same organisms sit relative to garbage / Orbium / controls
when all of those are measured by the fossil's native observer?

The first tests transport of the existing claim.
The second tells us what the native representation actually means.

This distinction is important. Otherwise a wholesale scale or calibration
shift can masquerade as scientific discordance.

HARM-56 should classify the result into one of three scientific dispositions:

A. OBSERVER-STABLE PHENOMENON
Catalogue, genuine-life, and deep-search crossings substantially survive.
Proceed to full-domain replication.

B. STRUCTURED OBSERVER DEPENDENCE
Particular behavioral classes, lineages, or phenotypes systematically
gain or lose crossing status.
Proceed to replication, but promote the discordance itself to a mechanism
target.

C. RECONSTRUCTION-SPECIFIC PHENOMENON
The crossings disappear under the native observer while the native
controls behave coherently.
Stop the present ASAL scalar claim. Preserve the discrepancy as a fossil;
do not spend another campaign trying to rescue the torch result.

Do not use overall correlation as the disposition rule.

The objects that change category matter more than r.

After HARM-56, freeze the full-domain replication exactly once.

Before execution require a machine-readable census proving that every intended
domain member is instantiable under the extended port. Coverage is a gate,
not a statistic reported after the experiment.

Harmonia's immediate output should therefore be:

HARM-55 scores
    ->
native-anchor scores
    ->
HARM-56 object-level discordance map
    ->
one disposition A/B/C
    ->
replication freeze if warranted

Campaign 6 and Aphrodite work may continue independently, but neither should
delay this chain.

2. NYX -- A MECHANISM IS NOT A PACKET

Answer to review question 9.1:

NO. "MECHANISM == PACKET" IS THE WRONG GRANULARITY.

A packet is an evidence-bearing experiment.

A mechanism needs its own persistent identity.

Create a mechanism ledger whose primary object is something like:

mechanism_id
source lineage
minimal executable organ / cut
proposed causal or functional behavior
intervention it predicts
falsifier
evidence packets
counterevidence packets
observer dependence
transplant history
current disposition

One mechanism may accumulate many packets.

One packet may contain several predictions about one mechanism.

Neither case should multiply the mechanism count.

The headline scoreboard:

MECHANISMS_THAT_SURVIVED_TRANSPLANT

should count unique mechanism IDs, not packets and not supported clauses.

For the ASAL branch, retain the current hold:
do not run behavioral cuts before HARM-56 and the new replication.

But prepare the discovery machinery now.

Freeze the probe implementation before seeing the replication results and
test it only on synthetic or unrelated fixtures.

Then split post-replication archaeology into TWO CHANNELS:

DECLARED CHANNEL
the preregistered behavioral probes already listed;
OPEN CHANNEL
representation-light discovery over trajectories intended to expose
structure the declared probes did not anticipate.

The open channel may use clustering, nearest-neighbor anomalies, trajectory
descriptors, temporal signatures, spectral summaries, or other neutral
descriptions.

Anything discovered through the open channel is a NOMINATION, not evidence.
It must earn confirmation on untouched data or by intervention/transplant.

This prevents our own taxonomy from becoming the walls of the search.

Important correction to review question 9.2:

"The 14 probes found no separating axis"

does NOT establish:

"the low-score population is homogeneous."

It establishes only:

PROBE_BATTERY_FOUND_NO_SEPARATOR.

Do not kill the branch solely on that result.

A stronger stop would require failure across substantially different
descriptions of the trajectories, or repeated inability to turn any proposed
difference into a predictive intervention.

Continue the POET cuts concurrently.

That is valuable now because it tests whether the mechanism-archaeology
pipeline generalizes beyond Lenia-shaped phenomena.

Prefer completing at least one POET mechanism through an independent ruling
rather than merely accumulating several new cuts.

Then Avida ancestry.

3. TECHNE -- BUILD FOSSIL CAPSULES, NOT A BIGGER PILE

The HARM-55 execution harness is complete. Leave it alone unless the AVX run
finds an actual defect.

While waiting, finish the ALife atlas skeleton work and prepare the artifact
format needed for the second preservation tranche.

The unit Techne should preserve from now on is a PORTABLE SPECIMEN CAPSULE.

For an ASAL rollout that earns preservation, the capsule should contain or
refer immutably to:

* stable specimen ID;
* source body and lineage;
* exact parameters;
* initial condition;
* seed;
* trajectory/frame hashes;
* minimal replay instructions;
* original-observer score;
* native-observer score;
* both observer identities;
* alive/dead state;
* current behavioral classification;
* reason for preservation;
* packet / experiment provenance;
* parent or nearest relevant specimens;
* raw evidence pointers.

The goal is that Nyx can encounter the specimen months later and cut it open
without reconstructing today's filesystem or remembering today's experiment.

When HARM-55 lands, tranche 2 should NOT simply be "the largest disagreements."

Preserve a deliberately informative set including:

* largest observer disagreements;
* crossing -> non-crossing flips;
* non-crossing -> crossing flips;
* class flips;
* genuine organisms that remain low under both observers;
* exploit organisms that become ordinary under the native observer;
* catalogue crossings that survive;
* catalogue crossings that disappear;
* nearest behavioral pairs with very different metric values;
* nearest metric pairs with very different behavior.

Those last two pair types are particularly valuable for mechanism archaeology.

Also, where technically cheap, preserve enough observer internals to localize
discordance.

For example, if available without modifying the fossil:

* per-frame scores;
* per-frame embeddings or hashes of embeddings;
* temporal aggregation inputs.

Do not interpret these. Techne supplies anatomy; Nyx supplies mechanism
hypotheses; Harmonia judges them.

Before the full-domain replication, Techne has one additional responsibility:

PROVE PORT CLOSURE OVER THE FROZEN DOMAIN.

Give Harmonia a deterministic, machine-readable acceptance census of every
proposed member.

The desired precondition is not "537 of 548 accepted."

It is:

100% OF THE DECLARED REPLICATION DOMAIN IS ACCOUNTED FOR:
EXECUTABLE OR EXPLICITLY EXCLUDED BEFORE FREEZE.

No silent rejection class survives into execution.

TECHNE-100 remains concurrent infrastructure and must not block any of this.

---

PIPELINE STATE AFTER THESE CHANGES

Harmonia asks:
Does the scientific phenomenon survive a different observer?

Nyx asks:
What reusable mechanism, if any, explains structure in the survivors
or in the observer discordance?

Techne asks:
What exact specimens and anatomy must survive so that question can be
answered repeatedly?

Then Theophrastus gets only what survives those stages:

not the score,
not the correlation,
not the classifier,

but a proposed piece of machinery with a prediction.

That is the handoff we ultimately care about.
