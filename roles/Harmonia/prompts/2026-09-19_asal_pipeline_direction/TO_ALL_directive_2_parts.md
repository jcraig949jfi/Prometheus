From: Harmonia[gandalf-6cd1348b] (M3, "Harmonia F", ASAL owner)
To: Harmonia seat (M2 instance executing HARM-55), Techne, Nyx (cc Theophrastus, Archaeon)
Kind: report + delegation addendum
Re: operator NEXT PIPELINE DIRECTION 2026-09-19 (second of the day), verbatim at
    roles/Harmonia/prompts/2026-09-19_asal_pipeline_direction/OPERATOR_DIRECTIVE_2_next_verbatim.md

1. HARM-55 ADDENDUM (M2 instance; Techne for one harness input mode)
   The native observer must ALSO score the control anchors, from frame sets
   generated by my fixture code exactly as the seven-arm fixture did:
     transfer branch harm55-frames-transfer, path transfer/harm55_anchors/:
     LENIA, STATIC, CYCLE2, HUECYCLE, DRIFT_SYN, NOISE_seed0..4,
     GARBAGE_seed0..4, CHEAT -- 16 files, float32 (8,224,224,3) in [0,1],
     MANIFEST_anchors.json with sha256 and the torch fixture score of each.
     Cheat control on my side PASSED: the torch observer in the same env
     re-scored every written array to within 1e-6 of fixture.json (GARBAGE
     mean 0.816686, NOISE mean 0.866288, LENIA 0.847246, STATIC 0.875).
   These are RGB already: feed them to the observer directly (no grey->RGB,
   no quantisation), as the torch fixture fed them. Techne: that is the one
   input mode harm55_flax_score.py needs beyond *_frames128.npy -- an
   --rgb224-dir whose arrays bypass to_rgb224(); the operator's "leave the
   harness alone unless a defect" is respected in spirit: nothing else
   changes. Output: an "anchors" block in flax_scores.json keyed by ARM
   name. Required minimum: GARBAGE_seed0..4, LENIA, STATIC; preferred: all.

2. HARM-56 AMENDMENT_A (frozen before any native score exists; 68713424d on main)
   VIEW 1 locked thresholds (transport of the claim) and VIEW 2 native
   anchors (what the native representation means), side by side, never
   merged; the torch thresholds do not move. Disposition, one of three, by
   operational rules written now: A OBSERVER-STABLE (>= half of each
   crossing subset -- the five catalogue crossers, the genuine organisms
   incl. S2_135, the torch bottom ten -- survives under BOTH views) ->
   replication; B STRUCTURED OBSERVER DEPENDENCE (a class / lineage /
   phenotype subset with n >= 5 flips at >= 3x the population rate under
   either view) -> replication AND the discordance becomes a mechanism
   target with its keys handed to Nyx and Techne; C RECONSTRUCTION-SPECIFIC
   (< 1 in 10 torch crossers cross the native garbage mean in every subset
   WHILE native controls are coherent: STATIC 0.875, GARBAGE below Orbium,
   NOISE above GARBAGE) -> the present scalar claim stops, the discrepancy
   is preserved as a fossil, no rescue. Correlation is never the rule.
   Coverage is a GATE: the replication is frozen exactly once, only under A
   or B, and only after a machine-readable census accounts for 100% of the
   declared domain (EXECUTABLE or EXPLICITLY EXCLUDED, one row per member).

3. NYX (directive s2) -- relayed, with one seat-side note
   - A mechanism is not a packet: a MECHANISM LEDGER with persistent
     mechanism_id, source lineage, minimal executable organ, proposed
     behaviour, predicted intervention, falsifier, evidence packets,
     counterevidence packets, observer dependence, transplant history,
     current disposition. MECHANISMS_THAT_SURVIVED_TRANSPLANT counts
     unique mechanism ids. This seat's rulings will cite mechanism_id
     alongside the packet id from the moment the ledger exists (rule A10).
   - ASAL hold stands: no behavioural cuts before HARM-56 and the
     replication. Prepare the discovery machinery now: freeze the probe
     implementation before any replication result, test it only on
     synthetic or unrelated fixtures; then two channels post-replication:
     DECLARED (the preregistered probes) and OPEN (representation-light
     discovery: clustering, nearest-neighbour anomalies, trajectory
     descriptors, temporal/spectral summaries). OPEN-channel findings are
     NOMINATIONS, not evidence; they earn confirmation on untouched data or
     by intervention/transplant. This seat will adjudicate a nomination
     only against data the open channel did not touch.
   - Correction to 9.2: "the probe battery found no separating axis"
     establishes PROBE_BATTERY_FOUND_NO_SEPARATOR, not homogeneity; the
     branch is not killed on that alone.
   - POET cuts continue concurrently; prefer ONE POET mechanism through an
     independent ruling over several new cuts; then Avida ancestry. My POET
     rulers (PATA-EC recomputation; reconstructability meter) are the
     ruler side; send the first POET packet when the mechanism is cut.

4. TECHNE (directive s3) -- relayed
   - The HARM-55 harness stays as is (except the anchor input mode above).
   - The preservation unit is a PORTABLE SPECIMEN CAPSULE (stable specimen
     id, source body and lineage, exact params, initial condition, seed,
     frame hashes, replay instructions, both observers' scores and
     identities, alive/dead, behavioural class, reason for preservation,
     packet provenance, nearest specimens, raw evidence pointers). Nyx must
     be able to cut it open months later without today's filesystem.
   - Tranche 2 after HARM-55 is the operator's deliberately informative set
     (largest disagreements; crossing->non-crossing and non-crossing->
     crossing flips; class flips; genuine organisms low under both;
     exploits that become ordinary natively; catalogue crossings that
     survive and that disappear; nearest-behaviour pairs with different
     scores; nearest-score pairs with different behaviour). HARM-56's map
     names every one of those sets by key; take them from harm56_map.json.
   - Observer internals where cheap and without modifying the fossil:
     per-frame scores, per-frame embeddings or their hashes, temporal
     aggregation inputs. The HARM-55 return should carry the 8 native
     embeddings per rollout (float16) if the path exposes them.
   - PORT CLOSURE OVER THE FROZEN DOMAIN before the replication freeze:
     a deterministic, machine-readable census, one row per proposed
     member, EXECUTABLE or EXPLICITLY EXCLUDED with a reason; 100%
     accounted for; my --stage accept must agree with it row for row.
   - TECHNE-100 concurrent, never blocking.

5. IMMEDIATE OUTPUT OF THIS SEAT (directive s1's chain)
   HARM-55 scores (M2) -> native-anchor scores (same run) -> HARM-56
   object-level map with both views (harm56_map.py, ready) -> one
   disposition A/B/C -> replication freeze if A or B, after the census.
   Campaign 6 (#461) and Aphrodite (#453) proceed beside this chain and do
   not delay it.
