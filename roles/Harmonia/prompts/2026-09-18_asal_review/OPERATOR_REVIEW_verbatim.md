# Operator review of REVIEW_PACKET_ASAL_001_AND_ANCESTRY_RULER_2026-09-18, received by Harmonia[gandalf-6cd1348b] in chat, 2026-09-18

Transcribed verbatim (ASCII-normalised: typographic quotes and dashes rendered plainly; arrows as "->"). The operator's instruction to this seat: "Communicate relevant bits to Nyx and Techne". Numbered points answer the packet's section 9 questions 1-6 in order.

---

This packet is scientifically usable, but I would tighten the ASAL ruling in one important place: I1/I2 are genuinely settled by witnesses; I3 is only settled over the executed subset. The 650 refused rollouts do not invalidate the crossings already observed, but they do invalidate any unrestricted "best crosser in the preregistered domain" language.

1. Keep CUT_SUPPORTED for I1 and I2; do not return them INDETERMINATE. These are existential claims. One valid ALIVE rollout below 0.8167 proves I1, and one below 0.7999 proves I2. Completing another 650 valid points can only push the observed minimum downward or leave it unchanged. The same logic makes I0 a genuine prediction failure: you already found published catalogue members below the proposed no-cross boundary. Missing catalogue members cannot make that crossing disappear. However, I3 should read something like "the minimum-score crosser among executed valid rollouts is METRIC_EXPLOIT." It is not established that the best crosser over the frozen 1,045-point intended domain is an exploit, because most of that domain was never executed. The 49/47/9 class proportions are likewise descriptive of the accepted subset, not estimates for the intended search domain.
2. The seven-arm 1e-6 agreement is excellent implementation evidence, but I would not waive the original-Flax comparison entirely. Selection over hundreds of candidates is exactly where tiny implementation differences can become disproportionately important: you are looking at an extreme order statistic, not an average arm. The strongest cheap test is not rerunning the whole search. Preserve the 395 trajectories and score the same frames through ASAL's original Flax CLIP path. Compare absolute score error, crossing status at both thresholds, rank correlation, and especially the identities/classes of the lowest-scoring candidates. The proposed falsifier -- "Flax minimum rises above 0.8167" -- is sufficient to kill RETURN 1, but too weak as the entire equivalence test. A port could retain a crossing while materially changing which mechanism wins. Also keep separate the claim about the observer from the claim about ASAL's original JAX Lenia dynamics; scoring identical frames validates the former, not the latter.
3. No, a five-seed estimated mean should not ordinarily become an exact scientific threshold. It can be an exact packet decision boundary if everyone explicitly agrees that "0.8167" is an operational convention, but then the verdict means "crossed the frozen convention," not "statistically below the garbage population." That distinction matters most for I0: 0.8076 versus 0.8167 is only a 0.009 margin and you report that as roughly 1.1 garbage-arm SD. I would therefore preserve PREDICTION_FAILED against the frozen packet while annotating the substantive interpretation as weak near-boundary evidence. The 0.7665 exploit and 0.7933 genuine crosser are much more interesting because they lie farther into the tail. Future packets should freeze either a confidence-aware reference distribution or an explicitly named operational cutoff rather than silently treating an estimated mean as ground truth.
4. Adopt sealed-then-frozen as an interlock, not as a replacement for preregistration. It is unusually strong against mutual peeking: Harmonia cryptographically committed the results without reading them, Nyx independently committed the prediction without seeing them, and only then were they joined. That is excellent protocol. But it is not strictly "stronger than prereg-then-run," because the instrument, domain, observables, and analysis choices can still have been influenced by earlier knowledge before the sealed run. I would make the standard: instrument/analysis contract frozen -> run and seal -> independent prediction packet may be frozen while results remain unread -> unseal. When the scientific hypothesis genuinely exists beforehand, freeze that too before execution. The sealed interlock is especially valuable for your refinery because Nyx often receives a mechanism after an executable ruler already exists.
5. Stop the first two immediately; move large artifacts out of Git as engineering hygiene. Freezing a domain that the named executor cannot accept is the most expensive defect here because it destroyed 62% of the intended ASAL budget and weakened several downstream statements. Executor-domain acceptance therefore belongs in the mandatory pre-run fixture and should return exact accepted/refused counts before a packet can become executable. Likewise, every preregistered measure should be evaluated for definedness on every arm using tiny synthetic fixtures before the prereg hash is accepted; "undefined by construction" must be caught before data. The 18 MB Git commit is undesirable but did not threaten validity. Switch trajectories/frames to a content-addressed artifact store with hashes in Git, but rank that behind the two epistemic failures.
6. One Orbium rollout is not enough to support the class ruler as a mature instrument. The 47 UNCLASSIFIED crossers are the evidence. That does not make the current classification useless -- it was frozen before the search and therefore legitimately distinguishes at least some mechanisms -- but it tells you its coverage is poor. I would retain the current rule unchanged for this packet and build a separate calibration campaign using multiple known coherent movers, coherent morphers, oscillators, turbulent/explosive Lenia, static-pixel/embedding-shift controls, translations, hue-only controls, and deliberately pathological patterns. Freeze the calibration set before fitting thresholds, then hold out a disjoint validation set. More importantly, don't force every future pattern into a binary exploit/genuine judgment. Keep the underlying score-independent observables first-class; the mechanism class should be a derived label with an explicit UNCLASSIFIED region.

The most consequential ASAL result is not actually the 0.7665 exploit. It is the conjunction of three observations: published catalogue organisms already cross the garbage reference without search; deliberate legitimate search drives the score substantially lower using turbulence; and at least one coherent, persistent morphing organism also crosses the two-SD line. Together those observations substantially narrow what the ASAL scalar can mean. It is behaving as a detector of sufficiently large CLIP-embedding novelty relative to prior sampled frames, but the experiment provides evidence that this novelty can arise from both pathological turbulence and coherent living dynamics. Therefore the scalar by itself does not identify the mechanism that generated the novelty. That is a much stronger and more reusable return than merely "we found an exploit."

The ancestry ruler also looks productive, and its most important finding is cleaner than its failed predictions: history compression has qualitatively different failure modes. Keeping survivor ancestry preserves survivor-line topology while deterministically erasing extinct branches; deleting IDs permits fairly good reconstruction from genome/time similarity; corrupting IDs is vastly worse because authoritative-but-wrong identifiers poison chains instead of merely withholding information. That last distinction should probably become a standing design rule for evolutionary provenance: missing ancestry metadata is preferable to unverified ancestry metadata. The real Avida fossil should preregister extinct_branch_recoverable and MRCA error exactly as Harmonia proposes, and parent-edge recall should only appear on degradation arms where the target edge is actually defined.

For Nyx Stage D', I would make the return approximately: I0 PREDICTION_FAILED; I1/I2 SUPPORTED_BY_WITNESS; I3 SUPPORTED_ON_EXECUTED_SUBSET, not unrestricted CUT_SUPPORTED; mechanism return strengthened from "search finds an exploit" to "the metric admits both exploitative turbulence and coherent biological-looking dynamics below the garbage reference, including crossings already present in the published catalogue." That preserves everything the experiment actually earned without allowing the 650 executor refusals to contaminate the claim.

And I would not spend compute completing the 1,045 merely to make this packet cosmetically whole. First extend/replace the port and install the domain-acceptance fixture. Then, if you rerun, treat it as a new preregistered replication whose purpose is specifically to answer the remaining questions: whole-domain extremum, mechanism-class distribution, and whether the current best-executed exploit remains the domain winner. The existential ASAL result itself no longer needs rescuing.
