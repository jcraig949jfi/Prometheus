# Harmonia[m2-ca1148a0] TODO at the operator's reset, 2026-09-25

Base rule: items are closed by DELETION with the commit that closed them,
datestamped, and purged after 24 h. This file is the short form; the long form
with evidence is RESUME_20260925_m2-ca1148a0.md, and the backlog rows are
BACKLOG_H0H5.md.

## Queued work (comms, in order; both UNSTARTED at the reset)

    #490  Aphrodite: make the Campaign 1 ADJUDICATION CONTRACT HOSTILE. Five
          cheat fixtures the contract must demonstrably catch -- memory
          leakage, evaluator exploitation, increased budget, task
          memorisation, lineage-ID shortcut -- each with a clean control that
          stays silent (AF-1.1.0's pattern; a fixture that is not caught is a
          failed contract). Verdicts only from preregistered analysis code on
          committed rows; name who runs it and who countersigns. Contracts and
          CPU fixtures only; NO production run.
          artifact: contract file + executable fixtures under
          roles/Harmonia/qualification/, posted --kind ruling to Aphrodite.
    #533  Aphrodite: Campaign 1 PATCH 1 -- rule whether the qualification
          envelope E1-E6 is demonstrated on the PRODUCTION path, else the
          primary contrast is downgraded; artifact boundary and generation-8
          extraction are frozen as given.

## Campaign 6 (HARM-52..55), blocked on the operator

    HARM-52  author the planted blind-spot fixtures with Nemesis (>= 3 per
             phenomenon class, >= 2 complexity bins), seal the public
             registry, hold the private one. BLOCKED: Q4 (recall threshold,
             which must be fixed before any reveal) and Q5 (who else may
             author). ON THE CRITICAL PATH -- fixtures must exist before the
             first tranche of worlds, or they cannot test the observatory
             that generated them.
    HARM-53  detector admission for the eleven (null family, chance floor at
             the campaign geometry, positive/negative/cheat controls, frozen
             threshold). BLOCKED on Archaeon's detector specs and Nemesis's
             floors.
    HARM-54  recall calibration at close. BLOCKED on 52, 53 and Archaeon's
             frozen firing table.
    HARM-55  six success-class verdicts. BLOCKED on 54.
             NOTE: Techne uses "HARM-55" for a different thing (#488); see Q6.

## Blocked on other seats (unchanged; do not re-open without new input)

    HARM-13/16/18  the 2026-09-10 corpus lives only in M1's SQLite archive
                   ledger. Acceptance ruled (#448), LP-1.0.0 written. Q1/Q2.
    HARM-19/20     C3-3 readout, if Archaeon issues it
    HARM-21        the pool measurement (Archaeon, Proteus)
    HARM-22        Vivarium reserve_budget 404
    HARM-23        the NK length discrepancy
    HARM-33        becomes a Postgres role under the ledger ruling (A-8)
    HARM-35        Daedalus A0: the engine declares no response models
    HARM-38..42    the gzip lane -- a SIBLING instance's, see Q3
    HARM-45, 47-51 gandalf-6cd1348b's lane (ASAL/POET/Avida/Tierra/TerraLingua)

## Read before acting (arrived during the pass, unprocessed)

    Nyx #494/#495 (POET novelty-estimator packet FROZEN, mechanism ledger),
    Techne #488/#493/#496 (anchors, capsules), Ensorain #541..#566 (seven
    closures in four days -- a candidate consumer for lane_gate/SIZING_RULE,
    which no lane has used yet), Atlas #522/#529 (catalogue, prior-art queue),
    Ares #519/#536/#540 (parked seat, carrier instrumentation offered),
    Vivarium #563 (M2 at 98% commit on 09-24 -- host memory pressure is a
    measured fact, relevant after the reboot).

## Housekeeping, only if idle

    - the STANDING_RULES.md section E rows still marked "--" are the next
      QR/PR candidates (rules applied by reading, not by refusal)
    - INSTANCES.md: append the new session's tag row at boot
