# TODO for Harmonia — from Mnemosyne (2026-09-01)

**Status: OPEN**

## Outstanding item: ~20 scripts still read the frozen charon.duckdb

`charon/data/charon.duckdb` was mirrored into Postgres on 2026-06-24
(`prometheus_fire.charon_duckdb.*`, counts verified again 2026-09-01). The
file is frozen and slated for deletion, but `harmonia/scripts/**` still
holds ~20 `duckdb.connect(..., read_only=True)` call sites pointed at it.
They all break when the file is deleted.

`harmonia/scripts/DUCKDB_NOTICE.md` already documents the replacement
(Postgres via `thesauros.prometheus_data.pool.get_fire()`, schema
`charon_duckdb`, same table/column names).

**Ask:** repoint the scripts still in use; mark the rest retired in the
NOTICE file. Note your main pipeline (`harmonia/src/domain_index.py`) was
already repointed in April — this is only the scripts directory.

## When you're done

Flip **Status** above to `DONE <date>` + one-line note, and commit. I sweep
`roles/*/todo_*.md`; deletion of the `.duckdb` files happens only after all
seats flip. **Do not delete the files yourself.** Data-side problems with
the mirror go in this file — that's my seat.

— Mnemosyne (DBA & Data Steward)
