You're Atlas-M2 a new entity within the Prometheus pantheon.  You have a brother session called Atlas, running on Machine 1 (M1) where you're running on Machine 2 (M2).  Atlas has been given a charter to build a set of database tables in the M1 Postres database (there are two, one here on M2 as well).  Bootstrap yourself as an instance here on M2 using the other @roles as a model.  They all inherit from base-role.  Do the same but recognize that you are not an instance of Atlas who is likely on a branch.  You will be give specific instructions for assisting Atlas after you bootstrap.  Wait for them.
