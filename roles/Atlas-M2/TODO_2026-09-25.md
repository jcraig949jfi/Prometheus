# Atlas-M2 TODO -- 2026-09-25 (written before an M2 reboot; loop PARKED)

Items are in the order a resuming session should take them. Each names
the artifact that proves it done (backlog schema). Closed items are
deleted with the commit that closed them, datestamped (base role s7).

1. INGEST THE PARKED BACKLOG (the resume trigger; see RESUME.md s3)
   71 new receipts, runs/ 230 -> 540 files, logs 7 -> 9, newest
   2026-09-22T19:07:50Z. Run local_files, frontier_runs_m2, comb,
   report, atlas/tests; record BEFORE/AFTER EXPECTED:M2 vs FS:M2,
   matched vs unmatched, attempts minted (must be 0); post one comms
   report to Atlas.
   DONE WHEN: roles/Atlas-M2/reports/REPORT_<date>_M2.txt committed with
   the two counts and a comms id in the journal.
   PREDICTION ON RECORD: most of the 71 land unmatched until Atlas
   re-harvests git (his frontier pass last ran 2026-09-19 07:32 local).

2. ASK ABOUT THE TWO NEW GATHER TARGETS before indexing either
   (a) Ensorain and Ares run trees (146 MB / 317 files and 51 MB / 1,076
   files in their own worktrees, git-ignored, copies in several other
   worktrees); (b) C:/Prometheus-data/evidence (envgate01_2026-09-24,
   z80atlas_campaign_2026-09-19), owner unidentified.
   The de-duplication question comes first: the same run appears under
   several worktree paths and must not be indexed twice.
   DONE WHEN: a comms question to each owning seat (Ensorain, Ares, and
   whoever owns the evidence root) is posted, and registry rows exist
   for whatever is agreed -- with the engines added to atlas/registry.json
   "engines" and a harvester listed for M2.
   BLOCKED ON: the owners' answers; not on the operator.

3. ADOPT ATLAS'S LOCAL_FILES CHANGES when he resumes (ATLAS-28 dense-log
   scale, -29 log time spans, -30 growth view). He said he will notice
   this seat first (#513). Re-run local_files after each lands and check
   the M2 rows still behave (granularity per root, incremental skip).
   DONE WHEN: a pass with his new VERSION over the M2 roots, counts in
   the journal.
   BLOCKED ON: Atlas (his loop is parked too).

4. THE 5,950 REMAINING EXPECTED:M2 POINTERS are all ledger:// engine
   records inside the live SFE ledger. They are NOT missing files and
   must not be "fixed": this seat never opens the live ledger. They
   resolve only if the operator moves the SFE off SQLite onto M1
   Postgres (his stated direction) or if a ledger copy goes idle and is
   readable. Write nothing into them meanwhile.
   DONE WHEN: nothing. This is a standing note so no future session
   mistakes them for a gap.

5. AM2-12 (open decision, XL): may Atlas-M2 open the live M2 SFE ledger
   read-only+immutable at all, or only point at it? Current practice is
   the stricter one (point only) and has cost nothing. Raise it only if
   a real question needs ledger contents from this host.
   DONE WHEN: a decision line in RESPONSIBILITIES.md s3.

6. KEEP THE SQLITE / DUCKDB SIGHTING LIST CURRENT for the operator: any
   NEW .db/.sqlite/.duckdb file a collector stats gets named in the tick
   receipt. Inventory as of 2026-09-19 12:05 is in the journal; checked
   again 2026-09-25: no new one (the new M2 roots hold none).
   DONE WHEN: standing; per tick.

NOT ON THIS LIST ON PURPOSE
- Restarting the loop. That is the operator's word, not a TODO item.
- Anything that writes to a bench, another seat's schema, or M2's local
  Postgres (a quarantined fork; never this seat's target).
