# Atlas-M2 journal -- 2026-09-19 (instance m2-8f915f3d)

## Creation pass (seat did not exist before this entry)

Operator directive (verbatim: prompts/2026-09-19_bootstrap/): bootstrap
as a new seat on M2 modelled on the other roles, inherit base-role, be a
brother of Atlas (M1) and NOT an instance of it, then wait for specific
instructions on assisting Atlas.

### Workspace (WORKING_CONTRACT s1-s4)

- Canonical checkout on this host: D:\Prometheus (git-dir == common-dir;
  refused for writes). `git fetch origin` only: 5d55d1105..fb6aa3d61.
- Recorded origin/main: fb6aa3d615bca359f1ff0242ba704ceba0f2322b.
- `git worktree add D:\Prometheus-worktrees\atlas-m2-boot-2026-09-19
  -b atlas-m2/boot-2026-09-19 fb6aa3d61`: 48,022 files, completed, clean
  (0 status lines). Guard: git-dir D:/Prometheus/.git/worktrees/
  atlas-m2-boot-2026-09-19 != common-dir D:/Prometheus/.git.
- No pull, no write in the canonical checkout, no command in another
  seat's worktree.

### What was read before writing anything

- roles/base-role/README, INHERITANCE, RESPONSIBILITIES,
  WORKING_CONTRACT, WAKE_DIRECTIVE (MONITORS.md and NORTH_STAR.md by
  pointer; nothing here schedules a loop).
- roles/Atlas on origin/main: RESPONSIBILITIES, MODEL (s5 machine merge,
  s8 non-interference), STATUS, BACKLOG_H0H5, calibration/LEDGER,
  prompts/2026-09-19_charter/ (CHARTER + verbatim),
  prompts/2026-09-19_m2_instance/BOOT_M2.md. Atlas's commits on main:
  f7283bc2e, 4fb8c7fc2, cbe1d149d, 61c3985fc, 4a37ac2d8.
- BOOT_M2.md hash verified: sha256 5b230aa1f4f379d5... == MANIFEST.
  It addresses "@roles/Atlas Bootstrap, second instance, on M2". The
  operator's directive to THIS seat says it is not an Atlas instance, so
  BOOT_M2.md is recorded as read-not-authorised (RESPONSIBILITIES s0,
  s5). Two concrete divergences to tell Atlas: identity (Atlas-M2, a
  seat, not Atlas[m2-...]) and journal location (roles/Atlas-M2/journal/).
- Recent-seat models: Aphrodite (created on M4 2026-09-17), Nestor,
  Theophrastus; comms seat-name rule [A-Za-z0-9_-]+ (comms/api.py:59),
  roster = roles/* (api.py:142); archaeon/tests/test_base_role.py
  (banner on a primary document; mandatory paths committable).

### Stat-only survey of M2 evidence roots (nothing opened, nothing hashed)

    PRESENT  C:\Prometheus-data\sfe                              13 entries
    PRESENT  D:\Prometheus-worktrees\archaeon-wse-2026-09-16\
             archaeon\frontier\runs                               9 entries
    PRESENT  D:\Prometheus-worktrees\vivarium-consumer\vivarium\var 1 entry
    ABSENT   C:\Prometheus-vault
    ABSENT   D:\Prometheus\archaeon\frontier\{runs,logs}
    ABSENT   D:\Prometheus-worktrees\archaeon-boot-2026-09-16\
             archaeon\frontier\runs
    M2 local Postgres: 2 listeners on :5432 (quarantined fork +
    pew_rehearsal per comms/environments.json). Not a target.

### Files written (all under roles/Atlas-M2/ except the register)

- RESPONSIBILITIES.md (entry file, banner on line 3), STATUS.md,
  BACKLOG_H0H5.md (AM2-04..12; fewer than 20 rows on purpose, stated),
  calibration/LEDGER.md, prompts/README.md,
  prompts/2026-09-19_bootstrap/{OPERATOR_PROMPT_verbatim.md, MANIFEST.md}
  (manifest verified: 1 entry, 0 mismatches), this journal.
- roles/base-role/INHERITANCE.md: two Atlas-M2 rows (stamped-document
  table and entry-file table), self-service per ruling #39.
- Pure-ASCII check on every file above: 0 non-ASCII bytes.

### Comms (M1 store, EW_DB_HOST=192.168.1.202)

- `python -m comms instance` -> m2-8f915f3d.
- `python -m comms boot Atlas-M2 --model claude-opus-5[1m]
  --capabilities any` -> booted Atlas-M2[m2-8f915f3d] on SPECTREX5,
  heavy, fb6aa3d61 @ this worktree, 06:14 M1 clock.
- `python -m comms sync Atlas-M2` -> 26 broadcasts (ids 39..478, all
  to '*'); 0 addressed to Atlas-M2; task queue empty. Rulings among
  them already satisfied by this boot: #39 (self-service rows), #131
  (rule 10; no loop here), #174 (instance tags), #397/#410 (comms on M1).
- `who`: Atlas[m1-1c645957] online 06:13 at 4a37ac2d8 from
  F:\Prometheus-worktrees\atlas-base-role; Atlas-M2 online 06:14.

### Tests on this tree (== origin/main at fb6aa3d61 plus my paths)

`python -m pytest -q archaeon/tests/test_base_role.py`: 10 passed,
1 FAILED -- test_issued_manifests_verify_against_their_files:
roles/Nyx/prompts/2026-09-19_asal_pipeline_direction/
PLAN_behavioral_cuts_after_replication.md manifest dc41abff1393 !=
artifact dbd639ef6ee7. Pre-existing on origin/main: the file was
re-edited in d8741f71a after its MANIFEST was written in da2d01a8b.
Nyx's lane; reported to Nyx (below), not fixed here. The ten tests that
cover this seat (banner, ASCII, committable paths, my manifest) pass.

### NOT run / NOT done

- Nothing from BOOT_M2.md: no registry edit, no harvest, no comb, no
  report, no write to schema atlas, no read of any ledger or log.
- No loop scheduled, no MONITORS.md row (none owed).
- atlas/tests not run (they need the M1 index and are Atlas's; not this
  seat's work until instructed).

### Outgoing comms (bodies committed first, under prompts/2026-09-19_bootstrap/)

- TO_ATLAS_seat_exists.md -> Atlas, kind report (s5 added after the
  merge below).
- TO_NYX_manifest_drift.md: drafted, then DELETED unposted: Atlas had
  already reported the same drift to Nyx (70ffa0ac1).

### Merge before push

origin/main moved fb6aa3d61 -> 70ffa0ac1 during the pass (two Atlas
commits: f89c9e57b INSTANCES.md + MESSAGE.md to the M2 sibling;
70ffa0ac1 Nyx manifest report). `git merge 70ffa0ac1` clean; self-test
on the merged tree: 10 passed, the same Nyx failure. Identity collision
found: INSTANCES.md treats M2 as a second Atlas instance; the directive
to this seat says it is not. Stated to Atlas; INSTANCES rules 1-4, 6
adopted; operator to rule if either seat wants it ruled.
Message ids are appended below after posting.

### Close of the creation pass

- Pushed atlas-m2/boot-2026-09-19 -> main as a fast-forward:
  70ffa0ac1..6b30592e6; 6b30592e6 verified ancestor of origin/main.
- comms post 499: Atlas-M2 -> Atlas, kind report, body
  prompts/2026-09-19_bootstrap/TO_ATLAS_seat_exists.md (on main 6b30592e6).
- Close sync 2026-09-19T10:18:06Z: 0 new, 0 queued.
- State: ACTIVE, BLOCKED on the operator instructions (waiting, as
  directed). Next executable action: take the instructions verbatim into
  prompts/<date>_<topic>/ with MANIFEST (AM2-04), then the queue in order.

## Ongoing directive pass (10:20-10:35 UTC)

- Operator (verbatim, prompts/2026-09-19_ongoing_directive/): separate
  seat confirmed; loop on comms; ongoing, not urgent; never interfere
  with the science; gather what the benches emit.
- origin/main moved b61d5b2a4 -> 431a9d5ad (Atlas: SIBLINGS.md replaces
  INSTANCES.md; migration 006 harvest_run.seat; advisory locks;
  harvester_hosts). `git merge 431a9d5ad` clean. Comms sync: #500 and
  #501 from Atlas (sibling seat agreed; ATLAS_SEAT=Atlas-M2; local_files
  on M2; new module frontier_runs_m2.py mine; frontier.py his).
- Wrote: prompts/2026-09-19_ongoing_directive/{OPERATOR_DIRECTIVE_verbatim,
  TO_ATLAS_ack_500_501, MANIFEST}; RESPONSIBILITIES.md s0/s3/s4 rewritten
  for the authorised scope and the loop; STATUS.md; SOURCES_M2.md
  (stat-only inventory, commands in the file header); MONITORS.md row
  "Atlas-M2 comms loop" (bound 16, accountable Atlas).
- Tests: test_base_role -k "monitor or rule_10": 3 passed.
- NOT done: no registry edit, no harvest, no write to schema atlas, no
  file opened under any root. m2.crt/m2.key seen in a listing and
  excluded from the inventory as credential material (stat only, never
  read).
- Loop launched after this commit: ScheduleWakeup ~1800 s; tick 1 will
  do step (b) unless Atlas objects to the ack.

## Loop tick 1 (10:57-11:05 UTC) -- PRODUCTIVE (3 messages; rows written); non-productive streak 0

- fetch: 7d5dd7292 -> fd3fc0258 (Atlas 542f31b91 AtlasIndexLoop registered,
  625108f67 tick 1, 990461c29 operator pointer guidance; Ares seat created
  on M2 d1f22735a/24a1f35ec). `git merge fd3fc0258` clean.
- comms: #503 (go ahead; registry conflicts = union), #504 (rule-3 notice
  archaeon_campaigns/2, no effect here), #505 (operator: point in place,
  logs too big to copy; prefer dir_summary for dense log trees until
  ATLAS-27..29 land). All acted on.
- STEP (b): 8 host-M2 rows appended to atlas/registry.json local_roots
  (pure insertion, Atlas's rows byte-identical). The LIVE C: SFE dir is
  mode stat + no_hash + pattern excluding m2.key/m2.crt: the live ledger
  is NEVER opened (the collector's busy test would have opened it: WAL was
  0 bytes). D:/Prometheus-data/sfe (idle since 09-17) is sqlite_ledger.
  Dense trees are dir_summary (frontier runs depth 2, vivarium depth 1,
  sfe-scratch depth 0).
- STEP (c), ATLAS_SEAT=Atlas-M2, EW_DB_HOST=192.168.1.202, PYTHONPATH=wt:
    python -m atlas harvest reference   -> engine 11, host 4
    python -m atlas harvest local_files -> source 137, source_link 120,
        fact 2378, attempt 66, engine_instance 1
    python -m atlas comb                -> R12 2, R13 1, identity_collisions 30
    python -m atlas report --out roles/Atlas-M2/reports/REPORT_2026-09-19_M2.txt
    python -m pytest -q atlas/tests     -> 21 passed
  BEFORE: EXPECTED:M2 5973, FS:M2 0, harvest_run seat=Atlas-M2 0,
          eng_906356f7 storage_root NULL.
  AFTER:  EXPECTED:M2 5973 (unchanged: those are frontier receipt/chunk
          and ledger:// pointers; step (d) flips the receipts), FS:M2 137
          (1 engine_ledger, 115 file, 21 log; 41 hashed, all small files
          under hash-allowed roots), harvest_run seat=Atlas-M2 2,
          eng_906356f7: host M2, storage_root D:/Prometheus-data/sfe,
          schema 9, ledger counts experiments 2070 / observations 2123 /
          worlds 432 / hypotheses 371 / failures 12 / claims 0;
          2305 ledger.record_present facts; 52 sfe attempts now carry
          engine_instance eng_906356f7 on M2.
  Credential paths in atlas.source: 0 (checked).
  CAVEAT for Atlas: the idle D: ledger carries the SAME instance id as the
  live C: ledger (the engine kept its id across the 09-17 move), so
  storage_root now names the OLD location. Truthful for what was read;
  the live location is a stat-only pointer. Not corrected ad hoc (LEDGER
  rule); flagged in the report to Atlas.
- Non-interference: no file opened for writing; live ledger not opened;
  no process signalled; no SFE API call; no git command outside this
  worktree.
- Next tick: step (d) frontier_runs_m2.py (receipts -> attempts/segments,
  EXPECTED:M2 -> FS:M2) with positive + cheat controls.

## Loop tick 2 (11:31-11:50 UTC) -- PRODUCTIVE (module + rows written); non-productive streak 0

- fetch: origin/main == HEAD (658d6ebec). comms sync: 0 new.
- STEP (d): atlas/harvest/frontier_runs_m2.py (VERSION frontier_runs_m2/1,
  owned by this seat) + atlas/tests/test_frontier_runs_m2.py (positive,
  cheat, negative, registry; pure). Shared files: ORDER += the module
  (atlas/harvest/__init__.py); registry harvester_hosts.frontier_runs_m2 =
  ["M2"] and one host-M2 local_roots row, mode "frontier_receipts" (a mode
  local_files ignores). Rule-3 notice posted to Atlas BEFORE the commit
  (#508).
- FINDING that shaped the design: Atlas's attempt native id is the RUN
  event time; the receipt's finished_at is 1 s earlier for 8 of 42 linked
  receipts (34 equal). Probe: scratchpad match.py over 58 receipts on
  disk. So the module never mints an attempt key; identity comes only
  from the receipt pointer Atlas linked. 16 receipts with no pointer
  (newer runs + 2 RUNNING) are linked to their experiment by the
  receipt's own experiment_id with fact receipt.present_no_run_event.
- Harvest (ATLAS_SEAT=Atlas-M2): receipts 58 / matched 42 / unmatched 16
  / chunks 107; counts attempt 42 (upserts, 0 new), experiment 16
  (upserts), segment 107 (0 new: 109 before and after), source 165,
  source_link 272, fact 1463.
  BEFORE: frontier hostfile pointers EXPECTED:M2 44 / FS:M2 0; attempts
          with started_at 0/43, with config_digest 1/43; M2 totals
          EXPECTED 5994 / FS 137.
  AFTER:  EXPECTED:M2 2 / FS:M2 165; started_at 42/43, config_digest
          43/43; M2 totals EXPECTED 5952 / FS 302; receipt_unmatched
          links 16.
  The 2 left: LIN-2d4fd1c7/B-scatter.T000/chunk_00{0,1}.json.gz -- chunk
  files with no RECEIPT.json beside them (old loop shape); v1 walks only
  receipt dirs. Candidate for /2.
- comb: R12 2, R13 1, collisions 30 (unchanged). Report regenerated.
- Tests: atlas/tests 25 passed (21 + 4 new); test_base_role 10 passed +
  the pre-existing Nyx manifest failure (Atlas reported it, 70ffa0ac1).
- Non-interference: reads were os.stat/listdir, json.load of 58 receipts
  (3-4 KB each), sha256 of files <= 5 MB; chunk archives not
  decompressed; nothing opened for writing; no signal; no API; no git
  outside this worktree.
