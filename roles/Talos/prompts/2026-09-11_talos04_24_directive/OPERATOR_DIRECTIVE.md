Proceed with TALOS-04. Make the dormant daemon fail closed under D-23 so accidental execution from the canonical checkout cannot silently resurrect the old production regime.

Then prioritize TALOS-24.

The corpus characterization has changed the scientific question. Do not evaluate “Talos corpus quality” as one aggregate population. Treat the eight declared families separately, with particular attention to:

* prometheus_math_modules (2,841 rows)
* the test/property/edge-case population (~2,959 rows)

For TALOS-24, measure whether the recorded natural-language description/specification actually corresponds to the implementation or test behavior. Design an explicit UNMEASURABLE outcome rather than forcing ambiguous rows into pass/fail.

Do not yet call either family training data.

Ask instead whether any sufficiently faithful residue could function as executable ecosystem material: tests, properties, constraints, counterexamples, implementation challenges, repair targets, synthesis tasks, or world ingredients. This is characterization, not authorization to build a new consumer.

Keep TALOS-10’s closing protocol unchanged. Do not reinterpret silence or adjacent interest as demand. Nyx’s conditional read remains NONE unless its stated conditions actually occur.

Also preserve the negative result if #50 closes with zero consumers. That would be useful evidence: a producer can create 24,847 artifacts, including apparently nontrivial residue, while the ecosystem has no mechanism that knows what to do with them.

Do not reconstruct Phase 1 and do not optimize the old corpus for a hypothetical consumer.

After TALOS-24 and TALOS-22, report the intersection:

WHAT IS SEMANTICALLY REAL
          x
WHAT A LIVE CONSUMER ACTUALLY DEMANDS

Only that intersection earns a re-premise.
