# Aphrodite journal -- 2026-09-23

Operator ruling "S1 AUTHORIZED": write and commit AMENDMENT 12 first, then run
S1 -> S2 -> S3 -> S4 without returning while gates pass. RunPod and Azure CPU
acceleration forked from clean SHA cf601fa3e, engineering only, $3 per provider.

## S1 -- whole-program identity. OUTCOME: S1_FAIL twice. RETURNED to operator.

AMENDMENT 12 (287d208ea) was frozen before any code: three identities
(source / structure / behavior), safe rewrites R1-R4 only, behavior = the
whole-program denotation over a frozen battery B1 under the domain
D_NONNEG_v1, audited by an independent battery B2. Gate G-S1.1..7.

RUN 1 (d72595bb0, S1_GATE_RUN1): collapse 163/163, refinement, no-target and
old fixtures PASS. FAILED: separation (1/1,155 mutants), audit (403 false
merges in 6,304 classes), conformance (4,501 mismatches).
  - The Tier-3C compensating pair (`acc - v*v` / `first - acc` vs `acc + v*v` /
    `acc + first`) collapses in BEHAVIOR and stays distinct in STRUCTURE, with
    no rewrite rule involved, which is exactly what the operator asked for.
    Every mechanically built negation conjugate of every catalog witness
    collapses as well.
  - REAL ENGINE DEFECT FOUND: the emitted artifact never applied the 10^40
    ceiling to the OUTPUT, so the searcher said FAIL where the artifact
    returned a 41-digit number. The standing 21,600 sweep never produced an
    output-only overflow, so it stayed GREEN. This is the FIFTH instance of the
    INVARIANT 6 family. I suspected it while reading the emitter before
    measuring; the gate confirmed it.
  - LATENT DEFECT IN AMENDMENT 10: body-level identity probed only
    non-negative accumulators, so it merged `v + |acc|` with `v + acc`.
    Whole-program identity separates them exactly where the accumulator goes
    negative (fixture: init 0 - first).

ADDENDUM 1 (d72595bb0): B1 v2 (exhaustive short-input ceiling cross), emitter
v2 (output guard; v1 kept byte-identical for Tier 3A-3C reproducibility), and a
FRESH audit battery B2 v2 (10,000 inputs, new seed). It pre-committed: a second
coverage failure means return to the operator, not a second repair.

RUN 2 (S1_GATE_RUN2): collapse 163/163, separation 0/1,155, refinement,
conformance (21,600 + 2,851,828 whole-program comparisons, 0 mismatches),
no-target and old fixtures all PASS. FAILED: audit, 179 false merges among
7,338 classes (20,872 programs).
  DIAGNOSIS (all 25 recorded cases): every one is FAIL-vs-VALUE on a SHORT
  input with a value ABOVE the ceiling by more than 1. The ceiling band's only
  value above C is C+1, and the programs cross the ceiling at small offsets
  (v - 1 > C needs v >= C+2, first + 1, last // 1, ...). So the band covers
  the threshold, not a margin beyond it. This is the same coverage family as
  run 1, narrower. Per ADDENDUM 1 I did NOT repair it a second time.

WHAT I WOULD PROPOSE (not done): add C+2, C+3, C+97, 2C, 10^41 and C-3 to the
band, keep the exhaustive cross, and judge the result on a third fresh audit
battery. The more principled alternative: stop claiming a closed battery can
certify equivalence at the ceiling, and treat ceiling-edge behaviour
explicitly -- e.g. declare the domain as inputs whose values are at most C
(the generators' support is <= 97), and keep the ceiling as a failure
disposition that the conformance gate tests, not an identity-bearing region.
That changes the DOMAIN, which is a scientific choice, which is why it goes to
the operator.

## Frozen and coded, NOT RUN (S1 has not passed)
  AMENDMENT 13 (S2, keyed-order common random numbers + paired selection) and
  AMENDMENT 14 (S3 donor derivation by LGG over whole-program classes, S4
  transplant with S5-S7), both frozen at 4a6bbf55d, before their code.
  fair.py, s2_gate.py, tier3d.py and run_s3s4.py are committed unrun.
  DISCLOSURE: while checking the plumbing I ran two single recipients
  (a PRISTINE one on tc_rel_sum_minus_last and a positive-control one on
  tc_new_summod_times_first) under a "smoke" label, AFTER every design was
  frozen. They are not results and change nothing.

## Acceleration (engineering only)
  Azure CPU (aphrodite/accel-azure-cpu-2026-09-23 @ 96503f81a): a process-pool
  backend reproduces all 480 Tier-3C rows with 0 mismatches and conformance
  GREEN; 430 s on 3 workers vs 1,197 s serial. The cloud run is scripted with a
  $3 budget guard and NOT executed: there is no az CLI or credential on M4.
  RunPod (aphrodite/accel-runpod-2026-09-23 @ bfbc5859e): an exact algorithmic
  evaluator (fasteval) with 0 mismatches over 1,157,192 evaluator pairs, all
  480 search cells and the full end-to-end rows; ~45.7x on search and ~11.8x
  end to end on M4 (contended). The pod run is scripted and NOT executed: no
  RunPod key on M4.

## Process hygiene
  All pool workers were verified gone by PID after each run.

## 2026-09-24 -- ADDENDUM 2 (domain D_TASK_T3_v1) and S1 run 3. RETURNED again.

Operator ruling: option (b), identity over the valid task-input domain; the
ceiling stays execution semantics (conformance), internal overflow on a valid
input still counts as behavior. ADDENDUM 2 frozen at 18ab82c50, coded at
3c70322fa.

RUN 3 (e337593e8): collapse 199/199 (including the three pairs reclassified as
equivalent in-domain), separation 0/1,628 mutants plus all 7 in-domain
near-neighbours (two of them exercise the ceiling INTERNALLY on valid inputs),
refinement, conformance 21,600 + 7,833,930 (valid + boundary battery, emitter
v2) with 0 mismatches, no-target, old fixtures: all PASS.
AUDIT FAIL: 13 false merges among 3,445 classes. Every one is a genuine
in-domain difference: accumulators that grow geometrically or doubly
exponentially (last*(acc+1) vs acc*last, nested pow, v + acc*acc from
different inits) cross 10^40 one step apart on 1-6 of 10,000 valid audit
inputs. S2 refused to run (chain exit 1). Nothing after S1 has run.

DIAGNOSTIC (not a gate): over the donor's member space M = H1 x H2 x FINAL
(151,920 programs, 7,808 classes), 74 classes are false-merged, the same
phenomenon. BUT every meta-development family's class (all 8; md_f and md_h
have no members in M by design) is audit-clean: 0 false merges in the classes
the campaign actually uses.

The structural point: a finite battery cannot certify equivalence of two
programs whose only difference is the step at which a fast-growing
accumulator crosses a fixed ceiling, because that step depends on the exact
input values. Any fixed battery leaves such pairs, and any fresh audit finds
a few. This is the same attractor the operator named, now inside the domain.

## 2026-09-24 (cont.) -- ADDENDUM 3, S1-local PASS, S2 PASS, S3 YES, S4 YES

ADDENDUM 3 (a2ba379aa, frozen before the certifier): GLOBAL_BEHAVIOR_IDENTITY
= FAIL permanently; behavior_id is a provisional bucket; campaign-relevant
classes need certificates (fresh B_CERT 20,000 + threshold-adversarial A(S)
that brackets each member's own ceiling-crossing length).

S1-LOCAL GATE: all 16 Tier-3D witness classes + member sets certified, 0
splits; conformance 21,600 + 321,392, 0 mismatches.
CAMPAIGN_RELEVANT_IDENTITY = PASS. Certifier power (reported): 13/13 run-3
false merges split; 15/20 recorded member-space examples split (the records
hold only each class's first 6 members, so the splitting member may be
missing).

S2_PASS (93 s): identical-content libraries now score identically (Tier 3C:
13,479 vs 51,018); order and arm invariance; charge conservation; the
variance of paired differences is 39% below that of unpaired ones.

S3: generator qualification rejected all three gcd-shaped families (md_d,
md_h, tc_new_gcdshift), as in Tier 3C. OBSERVE 3/3, VALIDATE 3/5 (md_e, md_f,
md_g): S6 held with exactly the minimum. Three observed classes, all
certified. D4 produced ONE schema: (acc + {H}). D6 paired selection over 24
cells: SCHEMA_0 saving 94,073 (lower95 64,604) vs UNCHANGED; MEMORISE -2,700.
  ENDOGENOUS_ABSTRACTION = YES. Selected library sha 4fed9489d956...
  HOSTILE NOTES: (1) under keyed order the donor observed the ADDITIVE
  sum-of-squares form directly, so S1's compensating-factorisation collapse
  was present but NOT load-bearing this run -- a counterfactual LGG over the
  observed bodies alone also yields (acc + {H}). (2) The derived library is
  CONTENT-IDENTICAL to the positive control. The claim is that the donor
  DERIVED it, not that it found a different one.

S4 PLUMBING INCIDENT: the first S4 attempt crashed in the difficulty pilot.
Recipient.fresh() wipes one GLOBAL marker directory, and parallel workers
deleted each other's workspaces. No result was written. Fixed with a
per-worker marker directory (the reset semantics inside a process are
unchanged), then resumed from the FROZEN S3 artifact with every library hash
re-verified. Nothing was re-derived.

S4 (1,204 s, 7 workers): S7 admitted 7 families (5 unseen-body, 2 related;
only tc_new_gcdshift was lost, to G2).
  ABSTRACTION_TRANSPLANT = YES -- all eight conditions.
  Pooled paired saving vs PRISTINE 167,818 charges per recipient (lower95
  154,844; 112 cells). DERIVED 16/16 on every admitted family; PRISTINE
  0-1/16 on every unseen-body family. False positives: 0.39% of hits.
  SHAM_7 drew (acc - {H}) and also solved negmod and sumdiv 16/16; DERIVED
  still beat it narrowly (34,180 vs 34,294; 29,189 vs 32,688).
  negmod (acc - (v % last)), a NON-additive body, was solved by the additive
  schema through NEGATION CONJUGATION: (0, acc + (v % last), first - acc).
  This is the equivalence S1 formalised, found by the recipient's search.
  CONDITION 7 CAVEAT: the frozen test compares WHOLE-PROGRAM classes, so even
  the two RELATED families count as "unobserved" (their finals differ). The
  substantive version holds on its own: all five unseen-body families show
  an effect.
  MEMORISE (sensitivity) is far cheaper on RELATED families (1,452 vs 26,157)
  and fails every unseen-body family. The memorise-versus-generalise
  trade-off, measured.
  ECONOMICS: donor meta-cost 6,916,141 charges; mean saving 167,818 per
  recipient per family; break-even 41.2 descendants < horizon 64 ->
  economically positive.
  C4 certificates: no bucket match contradicted (condition 7 not
  inconclusive). Conformance GREEN after. All PIDs verified gone.

WHAT THIS DOES AND DOES NOT ESTABLISH. It answers the operator's open clause
for THIS engine: the improver derived the reusable abstraction from its own
certified successes, selected it on families it never observed, and the
derived artifact transferred to five unseen-body families under hostile
evaluation, beating pristine and the sham distribution. It is ONE bounded
meta-generation on a synthetic DSL. The catalog was designed by the seat
(the composition is declared in AMENDMENT 14). The positive control certified
family admission (a declared bias that acts equally on every arm). It is not
Campaign 1 evidence and says nothing about LLM substrates.

## 2026-09-24 -- OPERATOR REVIEW OF S4 (recorded verbatim in substance)

  S4 = ACCEPTED.  ENDOGENOUS_ABSTRACTION = YES.  ABSTRACTION_TRANSPLANT = YES.
  BOUNDED_RSI = NOT YET ESTABLISHED.
The evidence is the whole causal chain (derived before transplant, selected
under paired evaluation, frozen, moved across a clean membrane, improving
fresh recipients on 5 unseen-body families at equal expressivity). The
economics are a bonus, not part of the proof.
On the reviewer questions:
  Q1: partly a funnel (two additive OBSERVE families, a designer who knew the
      target). The next catalog's mechanism mix must be generated
      independently of any target schema.
  Q2: tests both the donor and the observation set.
  Q3: accepted this time on the 5/5 unseen-body substance; condition 7 MUST be
      rewritten before reuse -- no grandfathering.
  Q4: a principled narrowing. The failure that would matter is a donor class
      splitting under the adversarial battery; that did not happen.
  Q5: the admission bias is real; S4 is not reversed. Next campaign: family
      admission by treatment-blind reachability under the common base grammar.
Next campaign, ONE question: does the inherited G1 abstraction help produce a
NEW, semantically distinct abstraction that improves a fresh G2 relative to
G1? The catalog is treatment-blind and deliberately outside the additive
neighbourhood.

## 2026-09-24 -- AMENDMENT 15 (G1 -> G2 recursion assay): STOPPED AT THE CATALOG

Frozen aaae70067 (operator choices C1-C4); coded 6370e77e3. There was no RunPod
credential on M4 (RUNPOD_API_KEY unset, no key file), so there was no canary
and the run used M4 with the reference engine.
Conformance GREEN (21,600 + 803,480, 0 mismatches). The treatment-blind Tier-3E
catalog was generated, 28 families, sha 25a2535cbba1.
G2 generator qualification: 11 / 28 families qualified.
  OBSERVE  3/7  (mod_a, mul_c, powr_c)
  VALIDATE 2/7  (add_c, sub_d)   <-- below the frozen minimum of 3
  TRANSFER 6/14
Frozen s3 stop rule: fewer than 3 qualified VALIDATE -> STOP.
  BOUNDED_RECURSIVE_SELF_IMPROVEMENT = NO (the catalog cannot support the
  assay). No donor ran; no L2 exists; nothing was transplanted.

HOW TO READ THIS NEGATIVE: it is an UNTESTABLE-CATALOG stop, not evidence
about recursion. Uniform draws from G4 produce many degenerate or
ill-conditioned programs -- e.g. `1 % (acc - v)`, `pow(v, 0 * acc)`,
`acc % pow(1, v)`, gcd-shaped bodies -- and their development sets cannot
separate the target from wrong classes (mean surviving wrong classes up to
16 per draw). The mechanical filters F1-F4 did not exclude them, and
qualification then removed them, as it is designed to. The catalog was NOT
redrawn: redrawing after seeing qualification would be selection on outcome.
Any retry needs a new preregistration (for example a qualification-aware
generator, or a larger K fixed in advance).

## 2026-09-24 -- AMENDMENT 16 four-hour campaign: RETURNED EARLY (time)

T0 18:35:51Z. Prereg 46e477d81; code 5c74e4c58. CLOUD_UNAVAILABLE ($0).
E4 S1_NECESSITY = INCONCLUSIVE: WHOLE recovered 3/3, BODY_ONLY 1/3. BODY_ONLY
failed on the two genuinely adversarial sets (F1, F2); F3 was not fully
adversarial -- my prereg flaw.
FOUNDRY: wave 1 (112 draws) took 62 min and accepted ~6% (A 3/56, B 4/56;
mul/fdiv/gcd/powr 0 in both). No catalog could complete, and no E1/E2 could
finish before the reserve. Stopped at 19:50:46Z; the process tree was killed
and verified gone by PID. Catalogs A/B = FOUNDRY_INCOMPLETE_TIME; E1/E2
UNTESTABLE (time); ceiling probe UNTESTABLE. Defects: foundry throughput
mis-budgeted from a solo timing; no checkpointing (wave-1 evaluations lost);
Q2 calibration could be made exact-fast by bitmask. Report:
pivot/APHRODITE_AMENDMENT16_CAMPAIGN_REPORT_2026-09-24.md.
