# Operator directive, 2026-09-18 (chat, to Aphrodite[harry1-29db2c34])

The operator's own words, verbatim:

    This is great.  Let's build a research library about RSI.  Build
    additional toys to find the damage boundaries of swarms, model it,
    collect research, follow the news, working theories, etc.  can small
    weak models actually be used?  Let's ask all the questions like
    this.  It's ok if they stay unanswered:

Followed, in the same message, by an external review of the seat's
review packet (APHRODITE_RSI_REVIEW_2026-09-17.md), relayed by the
operator. It is committed verbatim beside this file as
RELAYED_REVIEW_verbatim.md (it is not pure ASCII: kept byte-faithful
rather than normalised; the MANIFEST hashes it). The reviewer is not
named in the message.

Seat reading (Aphrodite's, not the operator's):
- Operator commission, this pass: an RSI research library (sources,
  news, working theories, models, an open-questions register that may
  stay open), and new toys locating the DAMAGE BOUNDARIES of swarms,
  each with a model predicting the boundary before measurement.
- "can small weak models actually be used?" is logged as a question
  family, not assumed answered.
- The review PROPOSES a charter (its last paragraph) and a first real
  experiment (RSI-1, the transplant test). Adopting a charter is the
  operator's act (APHRODITE-01/08); the seat records it as PROPOSED and
  drafts the RSI-1 design without freezing it.
- Reviewer's factual claims are checked, not inherited (notably "a
  paper posted this week demonstrates benchmark-poisoning attacks
  against several self-modifying coding-agent designs").

Addendum, same session (operator, verbatim, mid-pass):

    We have 5060 GPUs on M1 and M2  and we can build experiments to run
    on runpod but I'd rather focus on the thought work if that makes
    sense

Seat reading: priority is the thought work (library, models, theories,
questions, designs). Toys stay small and CPU-only, as analytic props.
Experiments needing a real model are written as designs that name a
host (M1/M2 RTX 5060, or RunPod) and are not launched by this seat.
