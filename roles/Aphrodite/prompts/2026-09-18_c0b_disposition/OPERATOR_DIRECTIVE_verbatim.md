Campaign 0B is accepted as an informative stress-test failure. It does not reopen Campaign 0 or invalidate the qualified typical-lineage assay. It establishes a specific power boundary: the frozen population-average assay is well calibrated but is not an instrument for transfer concentrated in rare jackpot lineages.

Campaign 1 execution remains NOT AUTHORIZED.

Proceed with the following:

1. Preserve the Campaign 0 / 0B assay byte-for-byte. No repair, tuning, threshold movement, or retroactive reinterpretation.
2. Split Campaign 1 into two independent scientific endpoints:
    * PRIMARY: typical evolved improvers transfer.
    * SECONDARY: evolution produces rare transferable discoveries under a fixed discovery procedure.
        A secondary success must never rescue or alter the primary verdict.
3. Design and preregister Campaign 0C to qualify the SECONDARY endpoint before any GPU experiment. Use a strict screen -> sealed confirm structure. Screening may nominate candidates only; confirmation must use fresh tasks/seeds/evaluations unavailable to the screen.
4. In 0C, vary both jackpot prevalence and effect magnitude. Include null plus approximately 1%, 2%, 5%, 10%, and 20% carrier prevalence, multiple effect sizes, family-correlated jackpot structure, and at least one winner’s-curse/adversarial selection world. Freeze top-k/screen width, confirmation rule, multiplicity handling, and lineage count before execution. Measure false-positive rate, recovery probability, prevalence estimation, winner’s-curse bias, and conditional effect recovery.
5. Do not formulate the secondary endpoint as “any lineage succeeds.” Define a fixed rare-discovery estimand whose meaning does not drift simply because more lineages were sampled. Report separately:
    * fraction of lineages producing screen-qualified candidates;
    * fraction independently confirming;
    * confirmed held-out effect and interval.
6. Keep delta = 3 points as the target Campaign 1 equivalence margin. Do not widen delta merely to make a 32-lineage campaign convenient. If compute forces 32 lineages, narrow the supported claims instead.
7. Retain 64 lineages as the target design. A 32-lineage reduced design may claim only what the qualification supports. Sixteen lineages is not a Campaign 1 configuration.
8. Before choosing model/task workload, run a bounded throughput benchmark on the intended M1/M2 host path using representative short procedural tasks and the actual evolutionary/evaluation loop. Measure rather than extrapolate:
    tokens/task, wall time/eval, evaluations/generation, GPU utilization, lineage cost, and projected full-campaign wall time.
    This benchmark authorizes no Campaign 1 evolution.
9. Aphrodite may request design/interface work now from:
    * Archaeon: sealed task-generator contract and anti-leakage requirements;
    * Harmonia: metering, fixed-compute, multiplicity, and adjudication contract;
    * Vivarium: sandbox/isolation and execution contract.
        They may prepare contracts and fixtures, but no Campaign 1 production run is authorized.
10. Update the Campaign 1 design packet after 0C and the throughput benchmark. The next operator packet should contain:
    * 0C preregistration and verdict;
    * primary and secondary estimands side by side;
    * measured compute economics rather than the current 1–217 day assumption range;
    * exact claims supported at 32 versus 64 lineages;
    * frozen delta;
    * proposed task/model/sandbox combination;
    * explicit kill/resize rules before GPU execution.

Do not launch Campaign 1 until that packet is reviewed.
