# LOCAL ENGINE AMENDMENT 16 -- FOUR-HOUR RECURSION CAMPAIGN (MASTER
# PREREGISTRATION). FROZEN AND COMMITTED BEFORE ANY NEW FAMILY IS GENERATED
# AND BEFORE ANY SCIENTIFIC RESULT.

Dated 2026-09-24. Applies "APHRODITE -- AMENDMENT 16 / FOUR-HOUR RECURSION
CAMPAIGN" (operator). Campaign 1 remains FROZEN and UNRUN. No G3.

AMENDMENT 15 stands exactly and is NOT redrawn:
    BOUNDED_RECURSIVE_SELF_IMPROVEMENT = NO under AMENDMENT 15
    INTERPRETATION = UNTESTABLE_CATALOG (no donor ran)

--------------------------------------------------------------------------
0. ENVELOPE
--------------------------------------------------------------------------

T0 = 2026-09-24T18:35:51Z. HARD WALL = T0 + 4 h = 22:35:51Z. RESERVE starts
22:20:51Z: stop workers, verify PIDs, destroy cloud resources, receipts,
final report, commit and push. No stage begins if its conservative ETA
(measured durations x 1.5, else the estimate stated here) would cross the
reserve. Cloud ceiling $6 total, canaries $1 per provider; before each paid
launch, price x maximum lifetime is summed against the ceiling.
CLOUD STATUS AT T0: CLOUD_UNAVAILABLE (RUNPOD_API_KEY unset, no key file;
no Azure CLI or credential). Not polled again. Everything runs on M4 with the
reference engine; process pools change throughput only. Wall-clock under
concurrent stages is marked CONTAMINATED; every endpoint is charge-based.

--------------------------------------------------------------------------
1. EXPERIMENT 0 -- CATALOG FOUNDRY (repairs only the family supply)
--------------------------------------------------------------------------

SEEDS, frozen now:
    CATALOG_A  "APHRODITE/A16/CATALOG_A/v1"   (E1)
    CATALOG_B  "APHRODITE/A16/CATALOG_B/v1"   (E2)
    CATALOG_C  "APHRODITE/A16/CATALOG_C/v1"   (E3 only; never substituted)
Catalog C's candidate stream is drawn from the depth-3 grammar G5 (s4). It
is generated and hashed with A and B, and QUALIFIED ONLY IF E3 RUNS.

CANDIDATE DISTRIBUTION (the AMENDMENT 15 sampler): per stratum OP in {add,
sub, mul, fdiv, mod, gcd, powr}, the draws are ('fold', init uniform in H1,
body uniform among the grammar's bodies with top-level OP that mention acc
and v, final uniform among the FINAL_SPACE finals that mention acc). Draws
are taken without replacement at the behavior_id level (a repeated behavior
is skipped and does not count as a draw). 32 draws per stratum, in seeded
order.

QUALIFICATION -- uses ONLY these four criteria, checked in this order:
    Q1 the witness is representable in the common base grammar (true by
       construction);
    Q2 the family generator passes the existing G2 qualification
       (AMENDMENT 11 s3, unchanged);
    Q3 the witness artifact (emitter v2) passes the hostile MetaTribunal;
    Q4 PRISTINE headroom: <= 8 of 16 PRISTINE pilot recipients qualify
       (label "A16-<cat>-pilot").
It never inspects G1, any G2 artifact, a positive control or a donor result.
A stratum accepts the FIRST 4 qualifying draws in draw order, or fewer once
its 32 draws are exhausted. (For throughput, draws may be evaluated in
parallel. Acceptance is always decided by draw order.) The accepted pool is
frozen and hashed before role assignment.

ROLES: within each stratum, the accepted families are shuffled by the
independent seed "APHRODITE/A16/ASSIGN/<cat>/<OP>", then take roles in the
fixed order OBSERVE, VALIDATE, TRANSFER, TRANSFER (truncated when fewer are
accepted). Operator diversity is therefore mechanical.
QUOTAS: >= 3 OBSERVE, >= 3 VALIDATE, >= 2 TRANSFER. Otherwise that catalog
is CATALOG_UNTESTABLE, is not replaced, and its experiment reports
UNTESTABLE.
TRANSFER admission: the accepted TRANSFER families (Q2-Q4 already hold).

--------------------------------------------------------------------------
2. EXPERIMENTS 1 AND 2 -- G4 RECURSION ASSAY ON CATALOGS A AND B
--------------------------------------------------------------------------

Each is AMENDMENT 15 s2, s4-s7 verbatim, with that catalog in place of Tier
3E and with "T3E" replaced by "A16-<cat>" in every cell label and sham seed:
DONOR_G1 (from L1) and DONOR_P (from PRISTINE); EXACTLY 8 paired replicates;
donor-local realized coverage; class certificates; the full TRACE;
SEMANTICALLY_NEW by body mechanism keys; L2 by the lowest-replicate rule;
arms L2 / L1 / PRISTINE / L1+SHAM_0..7 / L1+P_SCHEMA; R1-R5 unchanged; n = 8
never changed. Each is adjudicated independently, and the two are never
pooled.
    E1_BOUNDED_RSI, E2_BOUNDED_RSI in {YES, NO, UNTESTABLE}
    REPLICATED_BOUNDED_RSI = YES iff both are YES; if exactly one is YES, the
    positive is kept as a fossil and REPLICATION = NO.
Order: E1 then E2. Catalog B is frozen before E1 starts. No parameter is
tuned from E1.

--------------------------------------------------------------------------
3. EXPERIMENT 4 -- S1 NECESSITY (cheap, independent; runs beside E1/E2)
--------------------------------------------------------------------------

Three FORCED observation sets, each containing a compensating factorisation:
    F1 the Tier-3D S3 observations with md_b's program replaced by its
       certified compensating member (0, acc - (v * v), first - acc);
    F2 the Tier-3C recorded observed programs, verbatim;
    F3 the Tier-3A recorded observed programs, verbatim.
Procedures, on identical observations:
    BODY_ONLY  -- LGG over the observed BODIES only (S1 structural forms);
    WHOLE      -- member enumeration in PRISTINE coverage + certification +
                  LGG over member bodies (the S3 procedure).
Each derived candidate set is then run through the AMENDMENT 13 paired
selection on the Tier-3D VALIDATE families (md_e, md_f, md_g), 8 cells each
labelled "A16-E4-val/<F>" and shared by both procedures.
RECOVERED = the selected library carries a schema whose instantiation
mechanism keys equal G1 MECHANISMS.
    S1_NECESSITY = SUPPORTED      if WHOLE recovers on all 3 and BODY_ONLY on
                                   none;
                   NOT_SUPPORTED  if BODY_ONLY recovers on as many as WHOLE;
                   INCONCLUSIVE   otherwise.
E4 can never modify E1-E3.

--------------------------------------------------------------------------
4. EXPERIMENT 3 -- REPRESENTATIONAL-CEILING PROBE (conditional, exploratory)
--------------------------------------------------------------------------

Runs only if (1) E1 and E2 both reached valid donor adjudication, (2) neither
established recursion, (3) both failures are failures to produce or select a
SEMANTICALLY_NEW abstraction (R1), not instrument or validity failures, and
(4) at least 75 minutes remain before the reserve when (1)-(3) are known.
GRAMMAR G5 (versioned "g5-depth3-v1", hashed before use): bodies are G4
bodies plus prim(a, b) with a in BODY_ATOMS and b a G4 depth-2 non-atom body.
Every arm, G1 and PRISTINE alike, gets G5 as its fallback; no arm gets an
exclusive primitive. Before any search: equal expressive power (every arm
desugars into G5); at least one reachable SEMANTICALLY_NEW non-G1 schema is
exhibited; search/artifact conformance GREEN on G5 programs. The exploratory
budget is 4 replicates per donor, and the E1/E2 criterion is applied
unchanged.
    G4_REPRESENTATIONAL_CEILING = SUPPORTED     (G5 passes R1 where both G4
                                                 experiments failed R1)
                                = NOT_SUPPORTED (G5 also fails R1)
                                = UNTESTABLE    (preconditions or time unmet)
A G5 positive is a new result, never a correction of a G4 negative.

--------------------------------------------------------------------------
5. GLOBAL GATES AND THE REPORT
--------------------------------------------------------------------------

GLOBAL_BEHAVIOR_IDENTITY = FAIL, permanently; campaign-local certificates for
every causal class. G1 conformance GREEN before each experiment and after.
PIDs verified gone after every pool. Final report:
APHRODITE_AMENDMENT16_CAMPAIGN_REPORT_2026-09-24.md, one section per
experiment. Effect estimates are never combined across experiments. Stop
after the report.
