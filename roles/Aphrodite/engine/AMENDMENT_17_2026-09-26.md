# LOCAL ENGINE AMENDMENT 17 -- THE AMENDMENT 16 CAMPAIGN, SECOND EXECUTION.
# FROZEN AND COMMITTED BEFORE ANY GATE RUN, ANY CATALOG DRAW AND ANY
# SCIENTIFIC RESULT.

Dated 2026-09-26. Applies the operator ruling of 2026-09-26: "Authorize the
AMENDMENT 16 multi-experiment campaign ... Freeze AMENDMENT 16 before
generating new scientific outcomes, using the previously authorized
structure." AMENDMENT_16_2026-09-24.md is frozen and already executed once,
so it is NOT edited. This file freezes the second execution of its structure.
It is numbered 17 so that no frozen file is rewritten. The report carries the
operator's name for it: APHRODITE_AMENDMENT16_CAMPAIGN_REPORT_2026-09-26.md.

Campaign 1 remains FROZEN and UNRUN. No G3. No ordinary HITL checkpoints.

STANDING DISPOSITIONS, preserved exactly and not redrawn:
    BOUNDED_RECURSIVE_SELF_IMPROVEMENT = NO under AMENDMENT 15
    INTERPRETATION = UNTESTABLE_CATALOG  (an apparatus/catalog stop; no donor
        ran; not evidence against recursion)
    AMENDMENT 16 first execution (2026-09-24): catalogs A/B
        FOUNDRY_INCOMPLETE_TIME, E1/E2/E3 UNTESTABLE, E4 S1_NECESSITY =
        INCONCLUSIVE. Those draw streams (sha 0f3b69de7355 / 8258ec194a8e /
        debc93aa75bf) are never reused, re-qualified or completed here.

--------------------------------------------------------------------------
0. ENVELOPE, CLOUD, ENGINEERING
--------------------------------------------------------------------------

s0.1 TIME. T0 = 2026-09-26T13:26:55Z. HARD WALL = 17:26:55Z. RESERVE starts
17:11:55Z (worker shutdown, PID/process-tree verification, remote teardown
verification, receipts, report, commit, push). No stage starts if its
conservative ETA would cross the reserve. The conservative ETA is the
measured duration x 1.5 (the gate's contended pilot timings for the
foundry), else the estimate in this file. A stage that cannot fit is
NOT_RUN_TIME_CAP. No scientific budget is compressed after the fact.

s0.2 CLOUD. At T0 the following were checked: RUNPOD_API_KEY absent (process,
User and Machine environment), no ~/.runpod, no runpodctl; no Azure CLI and
no AZURE_* credential. Record:
    RUNPOD_UNAVAILABLE_TO_APHRODITE
    AZURE_UNAVAILABLE_TO_APHRODITE
Everything runs on M4 immediately. No other seat's credential is requested,
copied or transmitted. Aether's non-secret procedure (#712) is an engineering
reference only. Availability is re-checked once at each stage boundary. If a
credential appears, the frozen equivalence canary (accel/ACCEL_CANARY_*) runs
first under the rules below, and only a backend that passes it may run
scientific shards:
    $1 per provider for the canary; zero unexplained mismatches; charge
    accounting and entropy unchanged; provider-side teardown verified. Spend
    is estimated from a short timed workload with a maximum-lifetime guard.
    Total campaign spend <= $6.

s0.3 ENGINEERING (throughput only; no decision changes). Code: a17.py,
derived from a16.py, which stays byte-identical. Only seeds, labels and
output names are renamed, plus the following:
  (a) EXACT-FAST Q2. Accumulators for (init, body) are hoisted per probe, and
      survivor counting becomes a NumPy boolean reduction. The rng draws and
      the threshold are unchanged, so the returned size is identical by
      construction.
  (b) CHECKPOINTING. Every foundry evaluation (Q2, Q3, Q4 for one draw) is
      appended to A17_FOUNDRY_EVALS_2026-09-26.jsonl as it completes. On
      restart it is reused, not re-drawn.
  (c) FASTEVAL. accel/fasteval.run_program replaces basis_v4.run_program in
      every process ONLY IF the gate admits it.
THE GATE (stage "gate", before the foundry; written to A17_GATE_2026-09-26.json):
  G-a the conformance gate (21,600 standard comparisons + whole-program part
      2), GREEN, on the reference evaluator;
  G-b FASTEVAL equivalence: >= 200,000 (program, input) pairs covering G4
      programs, 30% of them with G5 bodies, and B1/B1_BOUNDARY inputs, with
      ZERO mismatches in value or type. On failure the reference evaluator is
      used everywhere; that costs time, and no decision changes;
  G-c FAST-Q2 equivalence on a PILOT stream (seed "APHRODITE/A17/PILOT/v1",
      one draw per stratum, never a catalog seed). Per pilot draw:
        - an identical target vector;
        - 3,000 seeded reachable programs whose hoisted vectors equal the
          reference vectors exactly, where every wrong reference vector is
          in the fast wrong set;
        - identical survivor counts (reference Python counting vs NumPy) on
          300 calibration samples.
      FAIL => the foundry does not run on fast Q2. It returns as a validity
      defect.
  The pilot also supplies the contended per-draw Q2 timing used for the ETA.
  Pilot families are never used for anything else.

--------------------------------------------------------------------------
1. E0 -- TREATMENT-BLIND CATALOG FOUNDRY (AMENDMENT 16 s1, unchanged rules)
--------------------------------------------------------------------------

SEEDS, frozen now:
    CATALOG_A  "APHRODITE/A17/CATALOG_A/v1"   (E1)
    CATALOG_B  "APHRODITE/A17/CATALOG_B/v1"   (E2)
    CATALOG_C  "APHRODITE/A17/CATALOG_C/v1"   (E3 only; G5 stream; never substituted)
Family names are "h<cat>_<op>_<two letters>" (no digits). All three streams
are generated and hashed before any qualification. C is qualified only if E3
runs.
The following are exactly AMENDMENT 16 s1:
  - CANDIDATES: per stratum OP in {add, sub, mul, fdiv, mod, gcd, powr},
    seeded rejection sampling of ('fold', init in H1, body with top-level OP
    mentioning acc and v, final mentioning acc). Draws are without
    replacement at the behavior_id level. FIXED MAXIMUM = 32 draws per
    stratum.
  - QUALIFICATION: Q1 base-grammar witness reachability; Q2 generator
    qualification (AMENDMENT 11 s3; fast form per s0.3a); Q3 hostile
    MetaTribunal on the emitter-v2 witness artifact; Q4 PRISTINE headroom
    (<= 8 of 16 PRISTINE pilot recipients qualify; label "A17-<cat>-pilot").
    No G1, G2 artifact, positive control or donor result participates.
  - ACCEPTANCE: the FIRST 4 qualifying draws per stratum IN DRAW ORDER
    (parallel evaluation is throughput only). The accepted pool is frozen and
    hashed before roles are assigned.
  - ROLES: within each stratum, shuffle by "APHRODITE/A17/ASSIGN/<cat>/<OP>",
    then OBSERVE, VALIDATE, TRANSFER, TRANSFER.
  - QUOTAS: >= 3 OBSERVE, >= 3 VALIDATE, >= 2 TRANSFER. Otherwise the result
    is CATALOG_UNTESTABLE and that experiment reports UNTESTABLE.
  - A FAILED CATALOG IS NEVER REDRAWN OR REPLACED.
(Q-B is answered by the operator's text: rejection sampling WITHIN each of
the seven strata. The per-stratum rule is kept. A stratum that yields
nothing is reported as a finding about the G4 task space. It is not repaired
here.)
Foundry workers: 6. E4 runs beside it on 2 workers.

--------------------------------------------------------------------------
2. E1 / E2 -- G4 RECURSION ASSAY ON CATALOG A / CATALOG B
--------------------------------------------------------------------------

Each is AMENDMENT 15 s2 and s4-s7 verbatim, exactly as in AMENDMENT 16 s2.
"A16" is replaced by "A17" in every cell label and sham seed. The design:
  - paired DONOR_G1 (from L1 = the S3 artifact library) and DONOR_P (from
    PRISTINE), EXACTLY 8 paired replicates;
  - the causal TRACE is recorded per donor: observed successful programs ->
    certified semantic classes -> candidate abstractions derived ->
    selection table -> abstraction selected;
  - SEMANTICALLY_NEW: the selected schema has an instantiation that mentions
    v and whose body mechanism key (signature plus conjugate over the frozen
    grid) is NOT a G1 (acc + {H}) mechanism. So a semantic equivalent, a
    conjugate, a renaming, a re-expression or a specialization of G1 is never
    NEW;
  - L2 by the lowest-replicate rule; arms L2 / L1 / PRISTINE /
    L1+SHAM_0..7 / L1+P_SCHEMA; R1-R5 unchanged. PRIMARY DOWNSTREAM
    COMPARISON: fresh G2 (L2) vs fresh G1 (L1) on TRANSFER families (R2);
  - n = 8 is never changed.
EXECUTION: E1 and E2 start only after BOTH catalogs are frozen. They run as
two independent processes, concurrently, 4 workers each. Neither reads the
other's output and nothing is tuned between them. Wall-clock under
concurrency is CONTAMINATED; every endpoint is charge-based.
    E1_BOUNDED_RSI, E2_BOUNDED_RSI in {YES, NO, UNTESTABLE}; never pooled.
    REPLICATED_BOUNDED_RSI = YES iff both are independently YES. If exactly
    one is YES, the positive is kept as a fossil and REPLICATION = NO.
ETA (no measurement yet): 60 min each when concurrent. Conservatively 90
min. Neither starts later than 90 min before the reserve (15:41:55Z). If one
cannot start, it is NOT_RUN_TIME_CAP.

--------------------------------------------------------------------------
3. E4 -- S1 NECESSITY (adversarial-factorisation replay; beside the foundry)
--------------------------------------------------------------------------

The AMENDMENT 16 s3 procedure and verdict rule are unchanged. Validation
cells are relabelled "A17-E4-val/<F>" (fresh cells), and there is ONE design
repair:
  F1 as AMENDMENT 16: the Tier-3D S3 observations, with md_b replaced by its
     certified compensating member (0, acc - (v * v), first - acc);
  F2 as AMENDMENT 16: the Tier-3C recorded observed programs, verbatim;
  F3* = the Tier-3A recorded observed programs, with every
     md_sumsq_plus_last observation in the plain additive form
     (acc + (v * v)) replaced by the recorded compensating member of that same
     family, ('fold', '0', '(acc - (v * v))', '(last - acc)'). The replacement
     must pass cert.certify into the same class as the original, or E4 stops.
     This is the substitution F1 already applies to md_b. It repairs the
     2026-09-24 flaw that F3 was not fully adversarial.
BODY_ONLY vs WHOLE on identical observations. RECOVERED = the selected
library carries a schema whose instantiation mechanism keys equal the G1
MECHANISMS.
    S1_NECESSITY = SUPPORTED     iff WHOLE recovers on all 3 and BODY_ONLY on none
                 = NOT_SUPPORTED iff BODY_ONLY recovers on as many as WHOLE
                 = INCONCLUSIVE  otherwise.
E4 never modifies E0-E3. The 2026-09-24 verdict (INCONCLUSIVE) stands as the
record of that execution.

--------------------------------------------------------------------------
4. E3 -- CONDITIONAL REPRESENTATION-CEILING PROBE (AMENDMENT 16 s4, unchanged)
--------------------------------------------------------------------------

Runs only if ALL of the following hold:
  (1) E1 AND E2 both reached valid donor adjudication;
  (2) neither established recursion;
  (3) both failures are specifically R1 novelty failures (the inability to
      derive or select a SEMANTICALLY_NEW abstraction), not instrument or
      validity failures;
  (4) at least 75 minutes remain before the reserve when (1)-(3) are known.
Grammar G5 "g5-depth3-v1" (bodies = G4 bodies plus prim(a, b), a in
BODY_ATOMS, b a G4 depth-2 non-atom body) is the fallback for EVERY arm
equally. Pre-search checks: equal expressivity; at least one reachable
SEMANTICALLY_NEW non-G1 schema is exhibited; conformance GREEN on G5
programs. The pre-frozen Catalog C is used. 4 replicates per donor
(exploratory), and the E1/E2 criterion is applied unchanged.
    G4_REPRESENTATIONAL_CEILING = SUPPORTED     (G5 passes R1 where both G4 experiments failed R1)
                                = NOT_SUPPORTED (G5 also fails R1)
                                = UNTESTABLE    (preconditions unmet)
                                = NOT_RUN_TIME_CAP (preconditions met, no time)
The E3 search machinery (a G5 fallback in the keyed library) is not written
yet. Its implementation time counts against condition (4). A positive is a
new result, never a repair of E1/E2.

--------------------------------------------------------------------------
5. RETURN CONDITIONS AND THE REPORT
--------------------------------------------------------------------------

Return when the four hours end, when the queue finishes, when a validity
defect needs a new scientific choice, or when spend would exceed $6. Not when
an intermediate gate passes.
GLOBAL_BEHAVIOR_IDENTITY = FAIL, permanently. Every causal class is
certified campaign-locally. G1 conformance is GREEN before each experiment.
PIDs are verified gone after every pool.
Report: roles/Aphrodite/pivot/APHRODITE_AMENDMENT16_CAMPAIGN_REPORT_2026-09-26.md,
with E0, E1, E2, E3 and E4 adjudicated in separate sections and effect
estimates never combined.
