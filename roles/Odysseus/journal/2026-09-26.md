# Odysseus journal 2026-09-26

## Charter (instance ubu001-a96af0b0)

- Operator direction then charter in chat, verbatim at
  prompts/2026-09-25_distributed_brain/ and prompts/2026-09-26_charter/
  (MANIFESTs verify). Arc: shared RAM pool -> slow circuit simulation
  retaining hundreds of GB -> pause/play/rewind + microtest in playback
  -> the brain itself sharded across machines -> "Yes" with R1-R5.
- Seat pushback given in chat before adoption, recorded here: network
  RAM is ~1000x the latency and 100-200x less bandwidth than local RAM,
  so compute moves to the shard (only cut edges travel); WiFi multicast
  is unreliable; #pragma pack does not fix `long` (4 vs 8 bytes); the
  OS pagefile is lost on reboot, so donated capacity is a capped mmap
  file instead. Operator kept UDP (R5); adopted.
- Overlap check before claiming the lane: `git grep -i aeron|multicast`
  -> none relevant; replay/rewind grep -> Aether owns replay identity for
  its own physics (named in RESPONSIBILITIES s1), no generic layer exists.

## Brain v0 (branch odysseus/brain-v0-2026-09-26, base d0da25735)

- bd76253c6: charter record + odysseus/DESIGN.md (D1-D12, prior art per
  decision) + odysseus/tests committed RED before any implementation
  (pytest collection failed: no modules). Order is in git history.
- Implementation: odysseus/brain/{model,shard,wire,frames,player,
  transport,node,cluster,__main__}.py, stdlib only.
- First full run: 70/71. The failure was MY TEST: it asserted seek(41)
  from 39 goes forward from the cursor, but keyframe 40 is cheaper (1
  tick vs 2); the player was right. Test corrected to three cases that
  each exercise one branch of the cost rule (ledger row). Then 71/71 on
  three consecutive runs (12.1-14.2 s).
- Stress (process mode, scratchpad, not committed):
  `python3 -m odysseus.brain local --run <dir> --neurons 4000 --shards 6
  --ticks 200 --keyframe 25 --drop 0.4 --mode process`
  -> per shard ~3,000-3,140 datagrams dropped (18,462 total), 3,158-3,331
  NAKs sent, 2,162-2,529 retransmits; wall 57.56 s; maxrss 25.8 MB.
  `verify` -> root_ok true. Independent check against reference_run:
  0/200 tick roots mismatch; 195,477 spikes in the run. Run dir 4.1 MB.
  Same with --drop 0: wall 2.87 s, 3 NAKs, verify root_ok true.
- NOT run: Windows (no Windows host here); two hosts; multicast;
  anything larger than 4000 neurons.
- Windows test delegated to Cyclops (comms #679, prompts/2026-09-26_windows_test/).
- Review packet: odysseus/pivot/ODYSSEUS_BRAIN_V0_REVIEW_2026-09-26.md
