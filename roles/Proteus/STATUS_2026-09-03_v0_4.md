# Proteus — V0.4 reversibility crucible status, 2026-09-03

**Verdict: `NOT_QUALIFIED_DIRECTIONAL_MUTATION_PRIOR_REMAINS`.** Plus one non-verdict
classification: `HISTORICAL_V0_LENGTH_FAILURE_RECLASSIFIED_AS_JOINT_GEOMETRY`. Packet:
`roles/Proteus/PROTEUS_V0_4_REVERSIBILITY_CRUCIBLE_REVIEW_PACKET.txt`. Built, measured, stopped.
Campaign 1 remains blocked. No world touched, no adapter, no selection, no repaired result.

## What cleared

- **The tape ratchet is gone, by proof.** Over all 2,044 valid structural states, cases where a
  fitting shrink was blocked: v0.3 = 510, v0.4 = 0. Every tape edge reversible to 1e-15, and the
  model is cross-checked against the live operator.
- **No structural coordinate exceeds NC5** at any of five cohorts after correction. The V0.3
  tape ratchet of +1.18 log2 words at cohort 128 is now −0.17.
- **The historical length failure was geometry.** V0.4 reproduces the old drift (+0.027 at
  cohort 8, −0.017 at cohort 128 in instructions per generation) and so does NC5, which is
  symmetric by construction (+0.041 and −0.016). Three grammar revisions chased the shape of the
  state space, and the second of them introduced a real ratchet.
- **Diffusion collapse stays cleared.** Concentration matches the geometry reference.
- **Instrument integrity.** Identical cross-runtime digest on three runtimes, atomic audit
  identity with a demonstrated staleness detector, dual Holm that aborts on disagreement.

## What blocks qualification

One content coordinate, `class_halt_yield` at cohort 128: delta −0.0191 against the length-matched
null, z = 3.53, Holm-significant, sign persists. It does not replicate at the other four cohorts
(sign flips at two), it excludes zero against the second content null at no cohort, and I have no
mechanism — my best candidate predicts the opposite sign.

Three defensible moves would have flipped this to a pass: correcting multiplicity across cohorts,
requiring agreement between the two content nulls, or requiring replication. None was
preregistered, so none was applied. **The question for review is whether the rule should be
amended for V0.5, not whether this number should be waived.** The cheapest next measurement is an
independent replication of that single cell under a fresh seed, deliberately not run here.

## Disclosed reductions

The v0.3 tape rule is no longer a live code path (v0.3 and v0.4 differ only inside one operator
body; keeping both would need the conditional the brief forbids). It remains exactly modelled in
`nc5.transition_graph("v0_3")`. The V0.3 diffusion census did not record tape size, so that one
comparison is one-sided.

## Standing finding for scrutiny

The length kernel is **not reversible**: 16,172 asymmetric directed-edge pairs, all
OPERATOR_DESIGN, from unequal operator weights. It produced no detectable drift against NC5 at
these horizons. Reported, not repaired — the brief authorized one change and it went to the tape.
