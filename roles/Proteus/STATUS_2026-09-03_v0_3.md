# Proteus — V0.3 neutrality crucible status, 2026-09-03

**Verdict: `NOT_QUALIFIED_DIRECTIONAL_MUTATION_PRIOR_REMAINS`.** Packet:
`roles/Proteus/PROTEUS_V0_3_NEUTRALITY_CRUCIBLE_REVIEW_PACKET.txt`. Built, measured, stopped.
No campaign, no World-0, no fitness, no selection, no repaired result.

## What the crucible established

- **One directional prior survives every control**: `config_log2_tape_words`, +1.18 log2 words
  beyond the symmetric bounded null at cohort 128 and +0.48 at cohort 256. Mechanism named: the
  half-tape rule in `op_config_perturbation` blocks shrinking and never blocks growing. It is the
  residue of my own v0.1/v0.2 fix, one level out from where the V0 battery looked. Left unrepaired.
- **The length ratchet was geometry.** A symmetrized reflected walk drifts +18 instructions from
  the minimum and −19.6 from the cap. Against that control, V0.3 length drift is indistinguishable
  from geometry at all five start sizes — the coordinate three preregistered V0 runs failed on.
- **The content ratchet is gone.** NOP-share drift falls from +0.133 under grammar v0 to at most
  +0.0299 raw, surviving no null comparison.
- **The space does not collapse.** At 10,000 neutral mutations, occupancy matches the geometry
  reference (top share 0.390 vs 0.383, entropy 3.80 vs 3.80) while cumulative unique classes rise
  72 → 274. Shape: islands around one large recurrent hub.
- **Structure is not a probe artifact.** Six independent ensembles agree at Jaccard 0.9886–0.9993.
- **Replay is exact across runtimes.** Identical digest on CPython 3.12.10 Windows, 3.11.9
  Windows and 3.12.3 Linux; artifacts byte-identical.

## Defects I found in my own instruments, all disclosed in the packet

1. The Holm correction was inert and would have published twenty surviving coordinates; one survives.
2. The preregistered content null freezes length, a wrong-population comparison in the
   length-changing cohorts; both nulls are now reported and the length-matched one decides.
3. NC4 could not represent organisms whose configuration had drifted (crash, fixed before any
   coordinate was read).
4. NC1's configuration half was implemented after the V0.3 arm ran, before any config coordinate
   was examined.
5. The V0 quarantine audit never covered `export.py`; re-running it here failed on a `base64`
   import. The V0 packet's "string layer PASS" was true of the code when it ran, not of the code
   it shipped.
6. An opcode-prior figure in `ARCHITECTURE.md` said "0..6" where the correct set is "0..20".

## What needs review

The single question: is the tape-growth prior disqualifying, and if so, is the fix one more
authorized grammar change or a redesign of `config_perturbation`'s bounds handling? Every previous
generation of this grammar carried exactly one directional prior, and each fix moved it rather
than removed it. That pattern is the thing to decide about, not the individual coordinate.
