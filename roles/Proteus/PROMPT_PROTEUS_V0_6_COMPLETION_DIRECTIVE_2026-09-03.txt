================================================================================
PROMETHEUS / PROTEUS V0.6
DIRECTIVE: COMPLETE THE FROZEN ADJUDICATION -- NO NEW SCIENCE
2026-09-03
================================================================================

Proteus,

Interim packet No. 2 is accepted as an accurate statement of the current
state.

G1-G3 are accepted as cleared for purposes of continuing the preregistered
V0.6 analysis.

The n=12,000 failure is NOT being treated as a failed scientific result and
does not count against V0.6. The gate behaved correctly: the frozen precision
criterion failed before any substantive quantity was computed, the threshold
was not changed, the failure was preserved, and additional measurement effort
was committed before the second production attempt.

Likewise, the Metropolis-control error and truncated-state analysis defect are
important findings rather than reasons to restart the pass because both were
found before production adjudication and their corrections are documented.

DO NOT redesign anything else.

Your job now is simply to finish the experiment you preregistered.

-------------------------------------------------------------------------------
1. FREEZE THE EXPERIMENTAL SURFACE
-------------------------------------------------------------------------------

From this point until the V0.6 final packet:

    no grammar changes
    no operator changes
    no weight changes
    no manifest changes
    no new hypotheses
    no new targeted cells
    no new seeds except where already preregistered
    no behavioral-coupling substitute
    no marginal-battery rerun
    no Campaign 1 activity
    no World Foundry interaction
    no SFE integration work
    no interpretation-driven additional experiments

Do not improve the experiment while observing it.

Finish the frozen analysis.

If another instrument defect is discovered, fail closed, preserve the evidence,
and report it rather than improvising around it.

-------------------------------------------------------------------------------
2. COMPLETE THE PREREGISTERED ADJUDICATION IN ORDER
-------------------------------------------------------------------------------

Complete and preserve the outputs for:

    stationary solve
    independent stationary-solver agreement
    empirical occupancy check
    reversible controls
    K_A current map
    K_B current map
    measurement-noise threshold
    pooled current map, if permitted
    detailed-balance statistics
    entropy production
    structural-region decomposition
    operator decomposition
    zero-reverse transitions
    spanning-tree fundamental-cycle affinities
    offline operator-reweighting counterfactual

Apply the frozen branch table literally.

Do not choose a more interesting interpretation if the registered branch is
less interesting.

The final headline must be exactly one of the preregistered outcomes or an
explicit instrument-failure outcome if a prerequisite gate fails.

-------------------------------------------------------------------------------
3. KEEP K_A AND K_B VISIBLE
-------------------------------------------------------------------------------

Do not allow pooling to conceal instability.

For every major adjudicated quantity for which it is meaningful, report:

    K_A
    K_B
    pooled estimate, if permitted
    absolute difference
    relevant measurement/noise threshold

Especially preserve this distinction for:

    stationary distribution
    material current edges
    max |J|
    total |J|
    entropy production
    dominant cycle affinities
    attribution conclusions

The reviewer should be able to see whether the claimed phenomenon exists in
both independently measured kernels rather than merely in their average.

-------------------------------------------------------------------------------
4. THE FINAL QUESTION IS NOT MERELY "IS J NONZERO?"
-------------------------------------------------------------------------------

V0.6 must answer the scientific question in the preregistration first.

But the final packet must also give Prometheus enough information to answer the
engineering question that follows:

    DOES THE MEASURED NONEQUILIBRIUM STRUCTURE MATERIALLY COMPROMISE
    PROTEUS AS A NEUTRAL PLAYER SOURCE?

Do NOT invent a new acceptance threshold for this question.

Do NOT classify yourself as qualified.

Instead report the quantities necessary for external review to make that
decision.

If authored current survives, distinguish clearly between:

    microscopic circulation with negligible observable consequence

and

    circulation capable of producing meaningful structural preference over
    repeated mutation / generation trajectories.

Where existing V0-V0.6 evidence can answer this without a new experiment,
summarize it.

Where it cannot, say:

    OPERATIONAL_SIGNIFICANCE_NOT_YET_ADJUDICATED

Do not manufacture a post-hoc test.

This distinction matters because:

    detailed-balance violation != automatically unusable player generator

but also:

    zero marginal drift != neutral dynamics

V0.6 should leave us with the evidence required to decide what, if anything,
must happen before integration.

-------------------------------------------------------------------------------
5. ATTRIBUTION MUST IDENTIFY THE MECHANISM, NOT JUST THE OPERATOR
-------------------------------------------------------------------------------

If the final branch is:

    NOT_QUALIFIED_AUTHORED_NONEQUILIBRIUM_CURRENT

then identify as far as the frozen evidence permits:

    which operator(s) contribute
    which state-space regions carry the current
    which transitions dominate entropy production
    whether zero-reverse transitions dominate
    whether the effect arises from operator weighting
    proposal asymmetry
    validity/rejection mechanics
    storage boundaries
    or interactions among these

Do not repair any mechanism.

We want the specimen intact.

If attribution cannot distinguish these possibilities, preserve:

    FULL_SPACE_CURRENT_SOURCE_UNRESOLVED

rather than telling a plausible story.

-------------------------------------------------------------------------------
6. PRESERVE THE FAILED INSTRUMENT HISTORY
-------------------------------------------------------------------------------

Do not clean up V0.6 so that it appears to have worked on the first attempt.

The final packet must retain:

    n=12,000 precision failure
    pilot-support sampling mistake
    discarded-compute persistence defect
    incorrect plain-Metropolis control
    truncated-space closure assumption
    fixes and commit ordering
    evidence that substantive quantities were unavailable before G3 passed

These are part of the result.

One of the important products of this pass is the record of how an apparently
reasonable microscope generated believable wrong answers and how the gates
caught them.

-------------------------------------------------------------------------------
7. PREPARE THE PLAYER FOUNDRY FOR EXTERNAL CONSUMPTION -- DOCUMENTATION ONLY
-------------------------------------------------------------------------------

Do NOT integrate with Harmonia yet.

Do NOT touch the SFE.

However, assuming this can be done without changing experimental behavior,
make sure the final packet identifies the existing public consumption surface
of Proteus clearly enough that Harmonia can eventually use it.

Document, rather than redesign:

    available player families / generators
    stable identifiers
    manifest/schema version
    deterministic seed contract
    configuration parameters
    generated-player artifact format
    runtime requirements
    validation/audit entry points
    expected failure responses
    replay procedure
    version/hash information required to reproduce a player
    any existing registry or dictionary through which consumers enumerate
        available player types

If a canonical player registry/dictionary already exists, name its exact path
and schema.

If one DOES NOT exist, say so explicitly.

Do not build one during V0.6 merely because integration will eventually want
one. That becomes an external integration decision after this experiment
closes.

The goal is for Harmonia eventually to be able to answer:

    "What players can I ask Proteus for?"
    "How do I request one deterministically?"
    "What artifact do I receive?"
    "How do I verify what generated it?"
    "How do I replay it?"

without reading Proteus internals.

-------------------------------------------------------------------------------
8. FINAL PACKET
-------------------------------------------------------------------------------

When all frozen analysis is complete, STOP.

Create:

    PROTEUS / PLAYER FOUNDRY V0.6 -- FINAL EXTERNAL REVIEW PACKET

It must be self-contained for a reviewer who has not read V0-V0.5 or either
interim packet.

At minimum include:

    A. final one-line V0.6 verdict
    B. G1-G3 evidence
    C. stationary-solver gate
    D. reversible-control gate
    E. K_A versus K_B reproducibility
    F. material-current definition and threshold
    G. current results
    H. detailed-balance result
    I. entropy-production result
    J. cycle-affinity result
    K. attribution result
    L. operator-reweighting counterfactual
    M. operational-significance evidence / unresolved status
    N. complete failure chronology
    O. what V0-V0.6 collectively taught us about the wrong microscopes
    P. current qualification state
    Q. exact blockers remaining before Campaign 1
    R. consumer-facing Player Foundry interface/registry documentation
    S. artifact paths, commits, hashes and reproduction commands
    T. compute/time accounting

Also include a compact verdict ledger showing which historical Proteus claims
are:

    ACCEPTED
    RECLASSIFIED
    FALSIFIED
    SUPERSEDED
    STILL UNRESOLVED

Do not begin V0.7.

Do not repair the Foundry.

Do not qualify yourself.

Do not begin integration.

Return the final external-review packet and wait for adjudication.

-------------------------------------------------------------------------------
9. RESEARCH PRESERVATION
-------------------------------------------------------------------------------

Preserve the complete V0.6 research trail in the repository, including:

    directives/prompts
    preregistration and addenda
    failed production artifacts
    successful production artifacts
    transition counts
    analysis outputs
    control outputs
    attribution outputs
    logs
    hashes
    commit identities
    final review packet

The final packet should point to these rather than requiring reconstruction
from chat history.

Nothing important discovered during this pass should exist only in the
conversation.

================================================================================
REVIEW POSITION
================================================================================

The most important fact in Interim Packet No. 2 is not that G3 eventually
passed.

It is that Proteus attempted to answer a question with insufficient precision,
detected that fact BEFORE seeing the answer, spent more compute rather than
moving the threshold, and then obtained the preregistered precision.

That is the behavior we want from the instrument.

Now finish the measurement.

No victory lap.
No repair.
No integration.
No V0.7.

Bring back the specimen and the final packet.

-- External review
================================================================================
