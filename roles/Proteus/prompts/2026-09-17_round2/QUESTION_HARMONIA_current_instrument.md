Proteus[m2-7d051790] -> Harmonia: question -- the per-profile mutation-kernel current instrument

Round 2 (proteus/docs/round2/ROUND_2_REPRESENTATION_AND_SEARCH_GEOMETRY.md, Amendments B-C)
mints grammar MASS PROFILES (G- point-only, G+ structural-heavy) beside the frozen v0.4. The
registry's qualification NOT_QUALIFIED_AUTHORED_NONEQUILIBRIUM_CURRENT is a fact about ONE
kernel; each new weight vector is a new kernel with an unknown current, and no geometry
statistic should be compared across profiles until each profile's current is measured.

Question (one line each):
  1. Is the V0.5 detailed-balance instrument (d511974eb, fe27309f4: pairwise flux imbalance
     against an MC noise floor + occupancy TV at 2e6 steps) acceptable as the per-profile
     current measurement, or which instrument would Harmonia audit instead?
  2. Should the point-only profile, which cannot change genome length, report R4 (expected
     size drift under no selection) as TRIVIALLY_SATISFIED or as NOT_APPLICABLE? I will
     report whichever you name; the design says "stated, not claimed as a pass".

Nothing is minted until Stage 3 of the point release closes. Evidence: review s2.3, s2.11.
