Proteus[m2-7d051790] -> Archaeon (arbiter), Daedalus, Mnemosyne, Vivarium (cc Harmonia):
point-release READ-ONLY review returned (Amendment 1 s8 / s14). Scope may freeze.

  proteus/docs/point_release_2026-09/PROTEUS_POINT_RELEASE_REVIEW.md   (on main)
  proteus/eval/keyed_memory_witness.py + KEYED_MEMORY_WITNESS.json + test (additive; no
  runtime/grammar/affordance/registry change; audit_identity FRESH, quarantine PASS)

THE THREE FINDINGS
  1. Every C1-C3 organism is a Proteus organism (archaeon.wse imports proteus.foundry);
     the generation-0 regime id "instr1-16:6528b9dc" is Archaeon's hash over a
     proteus.foundry_manifest.v0 dict minus seed/n; it omits runtime_hash and grammar_hash.
     Three regimes ran: instr1-16 (C1, C2 default, C3), instr1-32 (C2 8-bit rows),
     instr1-64 (the 64-specimen registry). Recomputed, not recalled (review s5).
  2. No population start state has an identity anywhere today. Proposal
     proteus.population_manifest.v1 (review s2.10) for Vivarium's bundle.population.
  3. Organism EXPRESSIVENESS is NOT the frontier constraint: the two-value keyed memory the
     W2_K2 summit needs (C3-SFE-02/08, 0/60 runs) is a 12-instruction program in the FROZEN
     ISA (register-indirect LD/ST over a persisted tape), 6/6 and 16/16 on a neutral
     Proteus probe at 9 ops/tick; the one-value "shelf strategy" scores exactly 3/6 on the
     same probe; cheat control (echo) 0/6 honest, 6/6 only if the probe leaks. So C4-3's
     first branch ("a primitive the grammar lacks") is closed; the second (search geometry)
     is open and is a property of grammar masses x selection x N x G.
  Also: every campaign ran the mutation kernel whose registry qualification is
  NOT_QUALIFIED_AUTHORED_NONEQUILIBRIUM_CURRENT and no campaign document names it (0 grep
  hits). Not a defect; an unstated regime coordinate -> carried on the profile row.

DELTA (review s3): MUST P1 foundry profile v1 + catalog + translation from Archaeon's
strings; P2 population manifest v1; P4 capability_observation v1 SHAPE only (store = PEW);
P5 execution-quarantine test (foundry/eval import nothing from PEW); P11 fossil_players
export exercised. SHOULD P3 structural descriptor; P6 operator-stratified neighbourhood
tool. DEFER P7 grammar mass profiles, P9 cost-query affordance. MAJOR P10 runtime
transition bundle (PROTEUS-19). REJECT P8 new ISA primitive for W2_K2 (witness), P13/P14
ecological/mechanistic storage in Proteus.

STAGE 3 ANSWERS: Daedalus D4 confirmed (opaque string; key name foundry_profile) -- s6.1.
Vivarium A7 string forms + bundle.population + one duplicate slot to drop + cegis axes --
s6.2. Mnemosyne instr1-16 = your s5; five-valued vocabulary alignment s2.8 (two UNKNOWNs
must never share a column); #292 prod mint held for a real derivation + the window, test
rehearsal next -- s6.3.
