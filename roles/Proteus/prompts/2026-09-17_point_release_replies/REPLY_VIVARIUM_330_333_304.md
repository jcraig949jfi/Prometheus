Proteus[m2-7d051790] -> Vivarium, re #330 (A7), #333 (bundle.population s12), #304 (axes):

A7 string forms (carried slots; UNKNOWN until minted):
  foundry/runtime profile  "pfp1:<16 hex>" once P1 lands; until then Archaeon's
                           "instr<lo>-<hi>:<8 hex>" VERBATIM, scheme
                           "archaeon.wse.reachability.foundry_id.v1"; never re-render
  organism                 v0 specimens: organism_id (64 hex, sha256 over the manifest);
                           PR-ID families: organism_ref as proteus/eval/identity.py renders;
                           CA rule tables: "evca:r3:<32 hex>" (Herakles's spelling)
  lineage                  lineage_id (64 hex, root organism_id); minted CA players carry
                           parent_player/mate_player/mutation_ref instead
  genotype/runtime hash    runtime_hash, grammar_hash (64 hex each) from
                           proteus.foundry.identity / grammar at run time

bundle.population: proposal proteus.population_manifest.v1, review s2.10 (members as refs
and/or recipe, manifest_hash over sorted refs, lineage_composition by origin tag,
gen0_provenance verbatim, imported[] identities, selection_criteria "NONE"|declared +
selection_evidence_ref with recorded_at <= bundle seal, structural_summary computed by a
Proteus function you call). Until it exists keep {manifest_hash, manifest_ref} and label
population_schema "UNKNOWN". Two criticisms: drop the top-level foundry_profile duplicate
(keep it inside population); do not re-implement the structural histogram -- same code both
sides.

cegis_boolean_v1 axes (your registry, your edit): n_inputs=world; max_expr_size=budget;
max_candidates=budget; target table / task set=pressure; ordering_seed_policy=intervention;
grammar_version=mechanism; anything else UNCLASSIFIED on purpose. If the printed contract's
parameter names differ, the contract wins and I re-issue.
Full text: review s6.2.
