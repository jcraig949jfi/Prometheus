# Proteus — V0.5 equilibrium and confirmation crucible status, 2026-09-03

Packet: `roles/Proteus/PROTEUS_V0_5_EQUILIBRIUM_CONFIRMATION_REVIEW_PACKET.txt`.
Measured. Not repaired. Stopped. Campaign 1 remains blocked.

## Three results

- **`V0_4_CONTENT_DISCOVERY_NOT_REPLICATED`.** The halt/yield effect reversed sign under an
  independent seed: V0.4 −0.0191 (z 3.53), V0.5 +0.0118, one-sided p in the claimed direction
  0.9716. The discovery is retained in the record, not erased.
- **Zero of 350 cells survive** the global (coordinate × cohort) Holm family. Two cells would have
  been declared under V0.4's within-cohort rule and are rejected by the correct family — the
  architecture change caught two would-be discoveries prospectively, on live data.
- **`STRUCTURAL_KERNEL_NONREVERSIBLE_AUTHORED_CURRENT_DETECTED`.** On 124 enumerated states with
  50,000 live mutations each: max |J| 2.45e-04 against a Monte-Carlo floor of 4.16e-05, 166 of 506
  pairs above the floor, stationary entropy production 9.97e-03 nats/step with zero one-way edges,
  3-cycle affinities to 1.21 nats. 165 of the 166 material pairs are OPERATOR_WEIGHTING; boundary
  and validity geometry contribute nothing above the floor.

Ordinary gates give `NO_REPLICATED_MARGINAL_DIRECTIONAL_PRIOR_DETECTED`, which is explicitly not
permission to launch while authored current stands.

## The finding that matters

Four passes of marginal drift testing could not see this. Detailed balance is a far stronger
condition than zero drift, and it fails: the current is real, it is authored by operator weights I
chose, and it does **not** move coarse structural occupancy at two million neutral steps
(occupancy TV 0.0198 against a sampling floor of about 0.019). Whether nonequilibrium mutation
physics violates the neutrality requirement is reserved to external review.

## Instrument failures found in this pass

1. V0.4's analytic kernel differs from the measured one by median TV 0.0162, max 0.1661.
2. Cross-runtime replay is no longer byte-identical: CPython 3.12 changed `sum()` to compensated
   summation, so 3.11 differs at 1e-14 relative. No numerical replay tolerance was preregistered.
3. My preregistration made two kernel classifications non-exclusive; both fit, resolved toward the
   stronger one.
4. The preregistered full-space sensitivity arm (300 samples/state) **fails its own frozen
   agreement tolerance** and did not converge. The truncation question is inconclusive, not
   negative.

## Next measurement, not run

A properly powered full-space kernel run, roughly two orders of magnitude more sampling. And,
separately, `genome_length` at cohort 256 against NC5 is negative in both seeds (−6.53 then
−12.80) and survives within-cohort but not global correction — the natural candidate for the next
preregistered confirmatory test, deliberately not chased here.
