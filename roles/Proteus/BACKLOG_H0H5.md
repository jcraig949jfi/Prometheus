# Proteus backlog (schema: roles/Archaeon/prompts/2026-09-10_backlog/00_BACKLOG_SCHEMA.md)

Currency: 2026-09-18 (instance m2-7d051790: rows 42-48 added for Campaign 6 Axis O/T, proteus/docs/campaign6/; 2026-09-17: 29/30/36 CLOSED under the repair order (proteus/docs/repair_2026-09/PROTEUS_REPAIR_RECEIPT.json); rows 29-36 added from the point-release review and 37-41 from Round 2 (proteus/docs/round2/); rows 29-36 added from the point-release review, proteus/docs/point_release_2026-09/PROTEUS_POINT_RELEASE_REVIEW.md s3; first filing 2026-09-16 by m2-67f3bd16; PROTEUS-01 closed 09-16). Priority order. The first five are
what the seat starts next. Items closed by a commit are deleted with the SHA in the journal, not
left as green rows. `TODO.md` (2026-09-04) remains the narrative list; every T-item there has a
row here and the two are reconciled at each session close.

    PROTEUS-01 | CLOSED 2026-09-16 (same day as filed): Herakles had already built the library half (herakles.evca.derive, dbc41fd2f); Proteus shipped only the PR-ID identity + join (proteus/eval/rule_table_identity.py) and replied to THEO-REQ-003/005 -- row kept one day so the closure is visible, then deleted | ENGINE | beta | S | none | proteus/eval/rule_table_identity.py, proteus/tests/test_rule_table_identity.py, roles/Proteus/prompts/2026-09-16_replies/REPLY_THEOPHRASTUS_REQ003_005.md
    PROTEUS-02 | Implement Retention V0 as code (reservoir, EvaluationRecord store, deterministic eviction, three guards) so RETENTION_AND_MEASUREMENT_V0.md stops being a contract nobody runs (TODO T3) | ENGINE | beta | M | none | proteus/retention/ + tests incl. an entry that MUST be evicted and an eviction that touches a pointer it must not; contract amended IMPLEMENTED with the SHA
    PROTEUS-03 | Measure the alias-differential bound width: widen the probe ensemble and re-measure the CONFOUNDED rate (1/343 today) or state acceptance at the current width with the reason (TODO T6) | EVIDENCE | beta | S | none | proteus/v0_7/RESULT_ALIAS_BOUND_WIDTH.json with the rate, the ensemble, and the decision
    PROTEUS-04 | Add still_solves_metered (verdict, ops) beside still_solves so a kind can charge the check budget in VM ops without re-implementing the predicate (Nyx #190) | H1 | beta | S | Vivarium (names the kind sketch first; the interface is drawn by its consumer) | proteus/eval/shrink.py + test asserting ops == 8 x (nodes + 6) at n=3
    PROTEUS-05 | Wire the temporal-program interface v0 (DELAY grammar, compiler with latches from r13 down, temporal_table oracle, temporal_spec/evaluate, six fixtures) exhaustively at (n=2,L=4) and (n=3,L=3) | H0 | 1.1 | M | Vivarium declares the kind before it is useful; buildable now | proteus/eval/temporal.py + tests; correctness_scope() states (n, L); TEMPORAL_PROGRAM_INTERFACE_PROPOSAL.md marked IMPLEMENTED
    PROTEUS-06 | Exercise PEW_EXPORT.md against the live service: write proteus.organism and proteus.descent rows for the frozen registry and read them back (TODO T5) | EVIDENCE | beta | S | Mnemosyne (namespace + token) | roles/Proteus/prompts/<date>_pew_export/ prompt; proteus/integration/RESULT_PEW_EXPORT.json with round-trip ids
    PROTEUS-07 | Register Proteus in the fleet roster (TODO T4, overdue since 2026-09-02) by writing the prompt to the owner of scripts/portfolio_monitor.py rather than editing fleet infrastructure | TOOLS | program | S | owner of scripts/portfolio_monitor.py (unknown; finding the owner is the first step) | committed prompt under roles/Proteus/prompts/ and the roster row on main
    PROTEUS-08 | Grammar v1 with NOT as a primitive under a NEW grammar_version (never a change to v0): compile NOT as XOR-with-ONE still, but as a first-class node in the enumerator and the universe DP, with the parity fixture per grammar version | H1 | 1.0 | S | none | proteus/eval/boolean.py GRAMMAR_VERSION_V1 + test_boolean_grammar_v1.py; universe table row for v1
    PROTEUS-09 | Compile/evaluate parity fixtures PER GRAMMAR VERSION and PER ARITY, exhaustive over 2^n assignments for every expression to a stated node count (today: v0 n=3 to the 16 named; v0 n=4 to node count 4) | H1 | beta | S | none | proteus/tests/test_parity_matrix.py with the (grammar, n, max_nodes, count) table asserted
    PROTEUS-10 | B1 library beta: the 4-input population artifact `proteus.boolean4.v0` (hand-built ID0..ID3 and BLIND controls, registry-disjoint refs), so beta candidates have a declared interface | H1 | beta | S | Harmonia sizes beta from BOOLEAN_UNIVERSE_TABLE.json first | proteus/eval/BOOLEAN4_POPULATION.json + tests (no frozen organism_id collides)
    PROTEUS-11 | B1 library 1.0: expose the universe DP as a library call the kind may use to REFUSE an unsolvable target at its cap (an exact "no program of size <= S exists" is a result, not a timeout) | H1 | 1.0 | S | Vivarium (whether the kind wants a refusal path) | proteus/eval/boolean_universe.py::minimal_size(table, n) + test; kind-side wiring is Vivarium's
    PROTEUS-12 | B1 library 1.1: component leaves in the universe DP and the enumerator cost recurrence, so a component library's claimed effect (smaller minimal size) is computable exactly before any run | H1 | 1.1 | S | none | universe table emitted with a declared component set; test that a component moves minimal sizes exactly as the DP predicts
    PROTEUS-13 | Apply the PR-ID organism_ref convention to Boolean programs: every compiled program carries artifact_manifest(FAMILY_PROGRAM, ...) and organism_ref; the population artifact lists refs, not bare hashes | H1 | beta | S | none | proteus/eval/boolean_population.py refs + test that refs are stable across hosts (the cross-host fixture pattern)
    PROTEUS-14 | Per-task ordering fixture for Vivarium's ordering_seed_policy: prove a per-task seed reaches all 2^n inputs over a task set and the first-witness rule is unchanged (ordering moves WHICH case is first, never what a mismatch is) -- n=3 done in test_case_ordering.py; extend to n=4 and to the K=8 case | H1 | beta | S | none | test_case_ordering.py rows for n=4; BOOLEAN_UNIVERSE_TABLE.json already carries the union numbers
    PROTEUS-15 | Z3 parity with Techne: the 256-function (n=3) and a sampled 4-input parity fixture where the truth-table oracle is the reference, plus the counterexample-validation hook Techne asked for | TOOLS | beta | M | Techne (states the hook signature) | proteus/eval/Z3_PARITY_FIXTURE.json + test; hook in proteus/eval/boolean.py
    PROTEUS-16 | The Player Foundry's own ladder: a one-page statement of what V0..V0.7 established and what the next rung would have to show (composition that changes an observable beyond the meter floor), so the lane has a rung to fail at rather than a list of chores | program | program | S | none | proteus/LADDER.md with the measured floor numbers cited by RESULT file
    PROTEUS-17 | Retention parameter tuning (R_MAX 4096, K_MIN 3, control quota 256) -- deferred by directive until there is something to tune against (TODO T14) | ENGINE | 1.0 | S | a running retention (PROTEUS-02) and a first experiment | do not start; row exists so the deferral is visible
    PROTEUS-18 | Glue beyond concat.v0 (A->B control flow, conditional and world-conditioned activation) -- deferred by directive until A+B proves it necessary; A+B has not (TODO T13) | ENGINE | 1.1 | M | directive | do not start; row exists so the deferral is visible
    PROTEUS-19 | Runtime transition bundle: fix the affordances.py LDC docstring (T9) and add per-instruction ordered activation tracing (T10) TOGETHER, re-stamping runtime_hash once and re-fossilising the 64 specimens | ENGINE | 1.0 | XL | operator decision NEW: "authorise ONE runtime transition that re-stamps every frozen specimen; Harmonia's fossils are re-keyed in the same commit" | proteus/foundry/ change + new AUDIT_IDENTITY.json + registry re-emitted; Harmonia notified before, not after
    PROTEUS-20 | Refusal-guard exclusion for proteus/audits/audit_identity.py and quarantine.py: both are in the auditor's own covered-file list, so guarding them moves the tree digest (WORKSPACE_COMPLIANCE (d)) | TOOLS | program | XL | operator decision NEW: "accept the two exclusions as recorded, or bundle the guard into PROTEUS-19" | the decision recorded in DECISIONS.md and WORKSPACE_COMPLIANCE amended
    PROTEUS-21 | `random` in proteus/v0_6/equilibrium.py (T8): reaches only stationary_empirical(), two non-adjudicated call sites; fixing it moves two published numbers | EVIDENCE | program | XL | operator decision NEW: "permit a version that moves the two V0.6 stationary_empirical numbers" | equilibrium.py on SplitMix64 + the two numbers re-emitted with supersession annotations
    PROTEUS-22 | The A6 neutrality hard gate (T7) is still not passed after three preregistered runs -- state whether it CAN pass under a kernel with authored current (V0.5 says the current is real) or retire the gate as mis-specified | EVIDENCE | program | XL | operator decision NEW: "is A6 a gate on the kernel or on the marginals; V0.5 measured that they differ" | a one-page ruling request with the V0.5 numbers; no fourth run before the ruling
    PROTEUS-23 | SFE specimen content-identity gate (T11, Harmonia I-CLIENT-GATE-UNENFORCED): Proteus's side is closed (specimen_gate.py); the shared primitive is Daedalus's | ENGINE | beta | S | Daedalus | row tracks the dependency; nothing to build here
    PROTEUS-24 | PEW fossil typed identity fields (T12: registry_identity, entry_id, segment ids, composition topology, ablation relation) | EVIDENCE | 1.0 | S | Mnemosyne | row tracks the dependency; the block Proteus emits is closure packet section 12
    PROTEUS-25 | TODO.md reconciliation: fold T1-T14 into this file's rows with their SHAs and mark TODO.md as the narrative companion, so there is one queue | program | program | S | none | TODO.md header amended with the pointer; every T-item names its PROTEUS-NN row

    PROTEUS-27 | Mint the first derived rule-table players for Theophrastus's 16-cell stencil once the derivation records arrive; commit the ledger rows with the receipt (ruling #268) | ENGINE | beta | S | Theophrastus (records or a script path) | proteus/mint/RULE_TABLE_MINTS.jsonl rows + receipt naming each mint_id
    PROTEUS-28 | PEW fossil_players rows for minted rule tables: post one mint through Mnemosyne's route and read it back by player_id (supersedes the T5 wait for this family) | EVIDENCE | beta | S | Mnemosyne (route + namespace + token; prompt posted 2026-09-16) | proteus/integration/RESULT_MINT_ROUNDTRIP.json with the mint_id compared
    PROTEUS-26 | Cross-family descent record (a family-agnostic successor to proteus.lineage_record.v0, whose required resource_budget/runtime_hash are VM facts): parents, operator + params, child, semantic owner's verification, one record_id -- so a CA rule derivation and a program mutation sit in one lineage space as PR-ID put them in one identity space | EVIDENCE | 1.0 | M | Mnemosyne (fossil_players row shape) and Herakles (evca_derived_rule_v1 stays the source) | proteus/contracts/descent_record.schema.v1.json + adapter from evca_derived_rule_v1 and from lineage_record.v0 + tests

    PROTEUS-29 | CLOSED 2026-09-17 (foundry_profile.v1 + catalog landed; 3/3 Archaeon strings reproduce; 10 tests; row kept one day, then deleted) -- was: Point release P1: proteus.foundry_profile.v1 (regime = foundry manifest minus seed/n + runtime_hash + grammar_hash + grammar_version + affordance_hash + registry source_qualification), id "pfp1:<16hex>", FOUNDRY_PROFILE_CATALOG.md/json with the three regimes that ran and the translation from Archaeon's instr<lo>-<hi>:<8hex> strings; test recomputes Archaeon's three strings from the catalog dicts | EVIDENCE | beta | S | Stage 3 close (scope freeze) | proteus/contracts/foundry_profile.schema.v1.json + proteus/eval/foundry_profile.py + proteus/eval/FOUNDRY_PROFILE_CATALOG.json + test
    PROTEUS-30 | CLOSED 2026-09-17 (population_manifest.v1 landed; registry regenerates from recipe; 12 tests; row kept one day, then deleted) -- was: Point release P2: proteus.population_manifest.v1 (members refs and/or recipe, manifest_hash over sorted refs, lineage_composition, gen0_provenance verbatim, imported[], selection_criteria/selection_evidence_ref with recorded_at <= seal, structural_summary) emitted from generate()/registry; cross-host hash fixture; negative (composition != count refused) and cheat (duplicate member refused) | EVIDENCE | beta | M | Stage 3 close; Vivarium bundle.population consumes it | proteus/contracts/population_manifest.schema.v1.json + emitter + tests
    PROTEUS-31 | Point release P3: per-organism structural descriptor v1 (pure function of the manifest: instructions, words, static opcode-category counts, static_reachable count, persist, tape_words, n_regs, code_writable) + the population aggregate Vivarium calls; per-organism rows only for published artifacts/elites | TOOLS | beta | S | none | proteus/eval/structural_descriptor.py + test (descriptor of the 64 specimens committed)
    PROTEUS-32 | Point release P4: proteus.capability_observation.v1 SHAPE only (five-valued verdict, measurement_refs[]; TRUE/FALSE need >= 1 ref, CONFLICTING >= 2, NOT_MEASURED none) + the PEW vocabulary alignment table; NO store in Proteus | EVIDENCE | beta | S | Mnemosyne accepts or amends the alignment (review s2.8) | schema + validator + positive/negative/cheat fixtures
    PROTEUS-33 | Point release P5: extend proteus/audits/quarantine.py with the execution-quarantine check (proteus.foundry and proteus.eval import nothing from evidence_wiki/ew/requests/httpx/psycopg; no public generator/grammar/lineage function takes a capability/phenotype/evidence/score/fitness parameter); cheat = planted import in a scratch copy fails | TOOLS | beta | S | none | quarantine.py + test
    PROTEUS-34 | Point release P6: operator-stratified neighbourhood tool (children per named grammar operator, seeded, with the operator's realized share) so a C3-SFE-02-style anatomy reports useful/neutral/destructive PER OPERATOR; Proteus never sees the world -- Archaeon runs it | TOOLS | beta | S | Archaeon wants it for C4-3 branch 2 | proteus/eval/neighbourhood.py + test on the 64 specimens (counts only, no scoring)
    PROTEUS-35 | Point release P9 (DEFERRED): cost-query affordance (brief R1's permitted "cost query"; the ISA has none). Runtime transition -> bundle with PROTEUS-19; do not start until a world that charges a variable price exists | ENGINE | 1.1 | M | a world with variable prices + PROTEUS-19 | row exists so the deferral and its condition are visible
    PROTEUS-36 | CLOSED 2026-09-17 (ROUNDTRIP_OK on 127.0.0.1:8377 namespace test; player_id evca:r3:004f005f005f005f005fff5f005fff5f; row kept one day, then deleted) -- was: Namespace "test" rehearsal of the fossil_players mint route (Mnemosyne #292): one rule-table child derived through herakles.evca.derive, minted with rule_table_mint into a SCRATCH ledger (not proteus/mint/RULE_TABLE_MINTS.jsonl), registered with namespace test, read back by player_id; report player_id + mint_id | EVIDENCE | beta | S | none (token present on M2) | proteus/integration/RESULT_MINT_ROUNDTRIP_TEST.json

    PROTEUS-37 | CLOSED 2026-09-17 (specimens arrived 1df3b99d7; structure + 857 knockouts delivered proteus/round2/ANATOMY_L0_*; row kept one day) -- was: Round 2 L0 anatomy: structural descriptors, motif census, probe-touched addresses and ABLATION SETS for the 11 C3-SFE-03 delay-general readers vs 11 matched W0 solvers; separation statement with the re-labelling chance floor; Proteus computes structure only, Archaeon lesions on the delay family | EVIDENCE | beta | M | Archaeon publishes the manifests (prompt posted 2026-09-17); PROTEUS-31 | proteus/round2/ANATOMY_L0_RESULT.json + rows + the ablation-set manifests
    PROTEUS-38 | Round 2 L3: grammar mass profiles G- (point-only restriction) and G+ (structural-heavy) as NEW grammar_versions with their own grammar_hash; per-profile R4 size-drift measurement and per-profile detailed-balance current (V0.5 instrument or Harmonia's); v0.4 byte-identical | ENGINE | beta | M | Stage 3 close; Harmonia answer on the instrument (question posted 2026-09-17) | proteus/foundry/grammar_profiles.py + MUTATION_GRAMMAR.md rows per profile + RESULT_PROFILE_CURRENT_*.json
    PROTEUS-39 | Round 2 L2 admission gate (design Amendment A): pre-registered two-condition gate for any new ISA primitive; document the gate and the one deferred candidate (cost query) -- no primitive before the gate is met; any admission = runtime transition inside PROTEUS-19 as a NEW runtime version | program | 1.0 | S | PROTEUS-37/38 evidence | proteus/docs/round2/L2_ADMISSION_GATE.md with the two conditions and their falsifiers
    PROTEUS-40 | Round 2 L4 design only: proteus/contracts/WORLD_INTERFACE_v1_PROPOSAL.md -- per pressure class (channels, delayed observation, resource accounting, peers, persistent/ephemeral objects, partial observability) state whether the ABI changes or the world does it under A1; expected: one candidate (cost query) | program | 1.1 | S | none | the proposal document; no code
    PROTEUS-41 | Round 2 noise-floor rule: every geometry statistic reported across profiles carries the same-profile re-keyed-seed floor beside it (C3-SFE-07); implement as a required field in the P6 stratifier output so it cannot be omitted | TOOLS | beta | S | PROTEUS-34 | stratifier output schema with `noise_floor` required + test that a table without it is refused

    PROTEUS-42 | Campaign 6 Axis O step 2: adopt Representation B (archaeon/campaign5/repb vm_b/gen_b/grammar_b) under proteus/repb/ byte-identical with hash, tests, and a catalog profile row carrying the C5-03 qualification caveat (D5-008 post-hoc); an INSTRUMENT, never in DEFAULT_FOUNDRY_MANIFEST | ENGINE | beta | M | none (D5-015 default yes) | proteus/repb/ + tests + FOUNDRY_PROFILE_CATALOG row + READ_LEDGER entry
    PROTEUS-43 | Campaign 6 Axis O step 3: proteus.graph_organism.v1 runtime (nodes = v0 affordance kinds minus positional jumps + ROUTE/CALL-RETURN routing; edges; dormant = unconnected), generate/validate/replay, new runtime hash, v0 untouched; quarantine audit extended to the graph runtime | ENGINE | 1.0 | L | operator decision NEW (PROTEUS-19 class): open ONE new runtime profile for Campaign 6; v0 frozen as control | proteus/graph/ + tests (replay byte-identical; dormant nodes cost 0 ops; A1 channel ABI unchanged so W0..W7 run it)
    PROTEUS-44 | Campaign 6 Axis O step 4: proteus.graph_grammar.v1 over connectivity (NODE_ADD/REMOVE/KIND/PARAM, EDGE_ADD/CUT/RETARGET, SUBGRAPH_COPY/COPY_ATTACH/MOVE, CROSSOVER_SUBGRAPH, STATE_SLOT_PERTURB) with pre-registered masses, R4 neutrality run, current DETECTOR run (Harmonia #412), lineage_record.v1 with explicit edit lists (child == parent + edits asserted) | ENGINE | 1.0 | M | PROTEUS-43; Harmonia battery | proteus/graph/grammar.py + MUTATION_GRAMMAR_GRAPH.md + RESULT_R4_GRAPH.json + RESULT_CURRENT_GRAPH.json
    PROTEUS-45 | Campaign 6 Axis T (organism side): proteus.behavior_fingerprint.v1 for v0 AND graph organisms (meter observable minus timings, per-channel I/O summary, state written/read sets, RND draws; graph: node execution counts, route decisions, CALL depth, dormant/component counts) with the 40/40 reproducibility proof and a cheat control (a fingerprint that leaks the world reward is refused by schema) | TOOLS | beta | M | none for v0 (buildable now); PROTEUS-43 for graph fields | proteus/eval/fingerprint.py + schema + tests + RESULT_FINGERPRINT_REPRO.json
    PROTEUS-46 | Campaign 6 step 5: expressiveness witnesses (keyed memory, delay reader) hand-written on the graph substrate + per-operator neighbourhood anatomy of those witnesses under graph_grammar.v1 vs v0.4 on the same programs -- the C3/C4 controls re-measured on the new geometry BEFORE any world; falsifier: same cliff => second exhausted substrate | EVIDENCE | 1.0 | M | PROTEUS-43/44 | proteus/graph/WITNESSES.json + NEIGHBOURHOOD_GRAPH_VS_V04.json + tests
    PROTEUS-47 | Campaign 6 escalation support (protocol steps 1-9, organism side): freeze bundle emitter (manifest + lineage + checkpoint + replay seed + runtime/grammar hashes) + per-node knockout sets + P6 neighbourhood + matched fresh controls under the same profile, as ONE callable that detectors invoke; Proteus never knows fixture locations | TOOLS | 1.0 | M | PROTEUS-34, -43 | proteus/eval/freeze_bundle.py + tests (bundle replays byte-identical; a bundle missing any of the nine fields is refused)
    PROTEUS-48 | Campaign 6 Stage 3: post the Axis O scope and collect: world ABI stays A1 channels (Archaeon/Daedalus); R4 + current battery for a graph grammar (Harmonia); fingerprint fields carried per evaluation (Vivarium/Mnemosyne); operator decision on PROTEUS-43 | program | program | S | none | replies filed under roles/Proteus/prompts/2026-09-18_campaign6/

XL rows (the operator's queue, derivable): PROTEUS-19, -20, -21, -22, -43 (one new runtime profile for Campaign 6).
