================================================================================
PROMETHEUS INCUBATOR
PROTEUS - PLAYER FOUNDRY - V0.5 EQUILIBRIUM AND CONFIRMATION CRUCIBLE

Issued by James, 2026-09-03, as external adjudication of the V0.4 reversibility
crucible packet
(roles/Proteus/PROTEUS_V0_4_REVERSIBILITY_CRUCIBLE_REVIEW_PACKET.txt).

Committed verbatim as authoritative. It amends the V0 kickoff brief
(cacf303f...), the first addendum (4a9fe0cb...), the V0.3 brief (78ed8284...)
and the V0.4 brief (da514fda...). Where this file and any earlier document or
later summary disagree, this file wins as the latest instruction. Character set
preserved as issued. sha256 of this file is recorded in the commit message that
lands it, and is the frozen public seed root for the V0.5 fresh-seed
replication.
================================================================================

PROTEUS / PLAYER FOUNDRY V0.5 — EQUILIBRIUM AND CONFIRMATION CRUCIBLE

You are Proteus.

V0.4 has received external adjudication.

Its verdict remains historically unchanged:

NOT_QUALIFIED_DIRECTIONAL_MUTATION_PRIOR_REMAINS

Its second classification is ACCEPTED and must become permanent evidence:

HISTORICAL_V0_LENGTH_FAILURE_RECLASSIFIED_AS_JOINT_GEOMETRY

Do not alter either result.

V0.5 is NOT a grammar-repair pass.

NO mutation operator, weight, VM rule, manifest bound, initialization rule, probe, runtime behavior or configuration rule may change.

The active V0.4 grammar is frozen.

V0.5 has two purposes:

1. independently confirm or kill the isolated V0.4 halt/yield content discovery under a preregistered confirmatory rule; and
2. determine whether the non-reversible mutation kernel carries authored probability currents that marginal drift tests cannot see.

The second question is now the more fundamental one.

⸻

0. FREEZE THE HISTORICAL LESSON

Preserve the entire V0 -> V0.4 sequence.

Record explicitly:

V0 observed apparent genome-length directionality without a joint geometry null.

V0.1/V0.2 attempted to repair that directionality.

V0.2 introduced an actual tape-size ratchet while suppressing an apparent length effect.

V0.3 detected the tape ratchet.

V0.4 removed it and introduced NC5.

V0.4 then demonstrated that the original genome-length effect is reproduced by a symmetric-by-construction joint structural null:

HISTORICAL_V0_LENGTH_FAILURE_RECLASSIFIED_AS_JOINT_GEOMETRY

Do not amend the old verdicts.

The point is not that the old measurements were fabricated.

The point is that their instrument could not distinguish geometry from prior.

Treat this sequence as permanent calibration evidence.

⸻

1. ZERO GRAMMAR CHANGES

V0.5 must use the exact V0.4 mutation grammar.

Assert all relevant hashes before execution.

Any grammar change invalidates this brief.

There is no authorized repair in this pass.

⸻

2. PREREGISTER ONE GLOBAL MULTIPLICITY ARCHITECTURE

V0.4 exposed an ambiguity:

the final qualification decision searches for a surviving effect across multiple coordinates AND multiple cohorts, while multiplicity was corrected inside cohorts.

Resolve this prospectively.

Before generating any V0.5 measurement, define the statistical family corresponding to the actual decision rule.

Preferred implementation:

hypothesis cell = coordinate × cohort

and use one global step-down Holm procedure over all confirmatory cells belonging to the same qualification claim.

A defensible hierarchical procedure may be substituted if and only if its family structure and alpha allocation are frozen before data generation.

Do not choose the procedure based on which one passes.

Include synthetic fixtures proving:

* zero discoveries;
* one discovery;
* several discoveries;
* discoveries distributed across multiple cohorts;
* a case where within-cohort correction passes something that global correction does not.

Two independent implementations of the final multiplicity calculation should agree bit-for-bit or adjudication aborts.

⸻

3. DEFINE DISCOVERY VS CONFIRMATION

V0.4's isolated result:

halt/yield opcode-class share
cohort = [the exact frozen V0.4 cohort]
delta = -0.019 against its primary length-matched null
z = 3.53
Holm-significant under V0.4 rules
persistent sign

is a DISCOVERY.

It is not waived.

It is not confirmed.

V0.5 will provide its first independent confirmatory test.

Before generating the new seed, freeze:

* exact coordinate;
* exact cohort;
* exact primary null;
* exact effect direction;
* exact horizon;
* exact persistence criterion;
* exact confirmation alpha;
* exact replication criterion.

A successful confirmation requires, at minimum:

same effect sign
AND
significance under the preregistered confirmatory test.

Report effect size and uncertainty whether it confirms or fails.

Do not require the magnitude to equal V0.4.

Do not run additional seeds if it fails.

Do not stop early if it confirms.

One independent confirmatory dataset means one dataset.

⸻

4. DO NOT TURN NULL AGREEMENT INTO A RESCUE RULE

V0.4 reported that the halt/yield effect is absent against a second content null.

Do not create a retrospective rule saying:

BOTH NULLS MUST AGREE

unless that rule has an independently justified scientific meaning.

Instead assign every tested coordinate:

PRIMARY NULL
SENSITIVITY NULL(S)

before V0.5 execution.

The primary null determines qualification.

Sensitivity nulls test robustness and are reported beside it.

A contradiction between primary and sensitivity nulls is epistemically important and must be discussed, but does not automatically become pass or fail.

This prevents NULL SHOPPING in either direction.

⸻

5. REPEAT THE COMPLETE BATTERY UNDER A FRESH SEED

Do not measure only the discovered halt/yield cell.

Generate one independently seeded V0.5 replication of the full V0.4 battery using the same grammar, cohorts, horizons and instruments.

Reasons:

1. preserve the ability to detect another effect rather than only attempting to rescue the known one;
2. obtain an independent estimate of the false-discovery landscape;
3. permit global multiplicity across the actual qualification family.

Freeze the seed derivation before execution from a public hash that did not exist for the purpose of choosing this outcome.

Report V0.4 and V0.5 separately.

Do not pool them until all independent results have been shown.

If pooling is later reported, label it secondary.

⸻

PART II — THE NON-REVERSIBLE KERNEL

6. THE NEW QUESTION

V0.4 reports:

16,172 asymmetric directed-edge pairs

in the genome-length mutation kernel, attributed to operator design and unequal operator weights.

No marginal structural coordinate showed detectable drift beyond NC5.

That does NOT establish dynamical neutrality.

A Markov process may possess zero marginal drift while carrying persistent circulation.

The new question is:

Does the authored mutation kernel generate nonzero probability current through the interior of the valid structural state space?

Equivalently:

At stationarity, is the system merely distributed non-uniformly because of geometry, or is probability continuously circulating around directed cycles because of operator design?

Do not assume reversibility is required.

Measure it.

⸻

7. BUILD THE STRUCTURAL MARKOV KERNEL

Where computationally tractable, enumerate the complete structural state space relevant to:

* genome length;
* tape size;
* any other manifest coordinate needed because it changes structural mutation validity.

For every state i and state j compute or measure:

P(i -> j)

under the exact active V0.4 grammar.

Include self-loops produced by rejected/no-op proposals.

The transition model must be checked against the live mutation implementation.

Randomly sample transitions from the live operator and compare empirical frequencies with the enumerated kernel.

Abort on unexplained discrepancy beyond frozen numerical tolerance.

⸻

8. COMPUTE THE STATIONARY DISTRIBUTION

For each communicating class relevant to the initialized population, estimate or solve:

π = πP

Report:

* communicating classes;
* transient states;
* recurrent classes;
* stationary mass;
* spectral information if practical;
* mixing-time diagnostics or bounds if practical.

Do not assume the stationary distribution is uniform.

A non-uniform π can arise purely from state-space geometry.

The question is not:

IS π UNIFORM?

The question is what produces it.

⸻

9. MEASURE PROBABILITY CURRENT

For every connected state pair i,j compute:

J(i,j) = π(i) P(i,j) - π(j) P(j,i)

Under detailed balance:

J(i,j) = 0

for every pair.

Report:

* number of edges with nonzero current;
* total absolute current;
* maximum edge current;
* current as a function of genome length and tape size;
* interior current separately from boundary current;
* operator attribution for edges contributing to current.

Do not collapse this to a single pass/fail scalar.

Produce a machine-readable current map.

⸻

10. MEASURE CYCLE CURRENTS

Pairwise asymmetry alone may hide or exaggerate the dynamical effect.

Identify directed cycles carrying net current.

At minimum characterize:

* short cycles;
* highest-current cycles;
* whether currents form coherent directional circulation;
* which mutation operators provide each leg.

If exact cycle decomposition is computationally excessive, use a principled sampled or basis-cycle decomposition and state its ceiling.

The important distinction is:

ASYMMETRIC EDGE

versus

SUSTAINED NONEQUILIBRIUM CIRCULATION.

⸻

11. ENTROPY PRODUCTION

If mathematically well-defined for the finite kernel, compute or estimate the stationary entropy-production rate:

Σ = sum_{i,j} π(i)P(i,j)
    log[(π(i)P(i,j)) / (π(j)P(j,i))]

with appropriate handling of zero reverse probabilities.

Report infinities or disconnected one-way transitions explicitly rather than smoothing them away.

A positive entropy-production rate is evidence that the mutation process is a nonequilibrium Markov process.

Do not call positive entropy production a cognitive or evolutionary effect.

It is a property of the authored mutation physics.

⸻

12. BUILD A REVERSIBLE REFERENCE WITHOUT CHANGING V0.4

Construct an analytical/reference kernel from the same structural state space and, as far as possible, the same local move support, but satisfying detailed balance by construction.

This is a CONTROL ONLY.

It does not replace the active grammar.

Do not tune it to make V0.4 look abnormal or normal.

Use it to separate:

STATE-SPACE GEOMETRY

from:

AUTHORED NONEQUILIBRIUM CURRENT.

Where possible preserve local connectivity while symmetrizing paired transition mass.

State exactly what invariant distribution the reference targets.

⸻

13. TEST WHETHER CURRENTS MATTER TO OCCUPANCY

Run long neutral trajectories under:

V0.4 active kernel
reversible reference kernel

from matched initial conditions.

Compare:

* genome-length occupancy;
* tape occupancy;
* joint structural occupancy;
* first-passage times;
* recurrence times;
* transition-direction frequencies;
* mixing behavior.

A current may exist without materially changing coarse marginals.

Report that honestly.

The purpose is not to demand zero current blindly.

The purpose is to determine whether the Foundry contains authored dynamical direction that the existing observables miss.

⸻

14. CLASSIFY ASYMMETRY SOURCES

Every significant asymmetric transition/current should be attributed where possible to:

PUBLISHED_BOUNDARY
MANIFEST_VALIDITY_GEOMETRY
OPERATOR_WEIGHTING
OPERATOR_STEP_SIZE
OPERATOR_IMPLEMENTATION
MULTI-OPERATOR_PATH
UNKNOWN

Keep boundary/validity geometry separate from authored operator design.

The fact that a mechanism is understood does not make it neutral.

⸻

15. DO NOT REPAIR NON-REVERSIBILITY

Even if the source is obvious:

DO NOT change weights.

DO NOT Metropolis-correct the kernel.

DO NOT add reverse operators.

DO NOT rebalance insertion/deletion.

DO NOT alter step sizes.

V0.5 measures the active V0.4 physics.

Any repair requires later external authorization.

⸻

16. AUDIT IDENTITY

Retain the V0.4 exact-tree audit rule.

Every claimed PASS must bind to the exact source identity being classified.

No stale audit result may survive a covered source change.

⸻

17. V0.5 CLASSIFICATION

Evaluate in this order.

G1 — INSTRUMENT VALIDITY

If replay, null construction, multiplicity, transition-kernel/live-code agreement, or audit identity fails:

INSTRUMENT_INSUFFICIENT_TO_ADJUDICATE

G2 — CONFIRMATORY HALT/YIELD TEST

If the preregistered V0.4 discovery independently confirms:

CONFIRMED_CONTENT_DIRECTIONAL_EFFECT

and qualification fails.

If it does not confirm:

V0_4_CONTENT_DISCOVERY_NOT_REPLICATED

Do not erase the V0.4 discovery.

G3 — COMPLETE FRESH-SEED BATTERY

If another coordinate survives the preregistered V0.5 global qualification family and required confirmation rules:

NOT_QUALIFIED_DIRECTIONAL_MUTATION_PRIOR_REMAINS

G4 — PROBABILITY CURRENT

Report whether the active structural mutation kernel satisfies detailed balance.

If not, report stationary currents and their sources.

Do NOT automatically fail solely because detailed balance is violated unless this criterion was frozen before execution.

Instead produce one of:

STRUCTURAL_KERNEL_REVERSIBLE_WITHIN_TESTED_PRECISION
STRUCTURAL_KERNEL_NONREVERSIBLE_NO_MATERIAL_CURRENT_DETECTED
STRUCTURAL_KERNEL_NONREVERSIBLE_AUTHORED_CURRENT_DETECTED

The final qualification consequence of the third classification belongs to external review unless a hard rule is independently frozen before measurement.

G5 — FINAL

Only if the ordinary directional-effect gates clear, report:

NO_REPLICATED_MARGINAL_DIRECTIONAL_PRIOR_DETECTED

This is NOT yet permission to launch if authored probability current has been detected.

External review must rule on whether nonequilibrium mutation physics itself violates the Foundry neutrality requirement.

⸻

18. OUTPUT

Produce:

PROTEUS_V0_5_EQUILIBRIUM_CONFIRMATION_REVIEW_PACKET.txt

Failure modes first.

Include:

1. frozen V0.4 identities;
2. V0.5 preregistration;
3. global multiplicity definition and tests;
4. primary/sensitivity null map;
5. independent halt/yield replication;
6. full fresh-seed battery;
7. V0.4 vs V0.5 effect comparison;
8. complete structural transition kernel;
9. live-operator/kernel validation;
10. asymmetric-edge census;
11. stationary distribution;
12. probability-current map;
13. cycle-current analysis;
14. entropy production;
15. reversible reference definition;
16. matched long-run trajectory comparison;
17. asymmetry-source attribution;
18. every replicated directional effect;
19. every nonreplicated V0.4 discovery;
20. every unexplained current;
21. claims and ceilings;
22. reproduction commands;
23. compute/storage accounting.

⸻

19. STOP

After producing the packet:

STOP.

No grammar repair.

No Campaign 1.

No world exposure.

No adapter.

No selection.

No repeated fresh seeds.

The V0 sequence has already shown why a visible slope is not enough.

V0.5 asks whether an invisible circulation can fool us too.
