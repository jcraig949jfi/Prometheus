# Proteus journal, 2026-09-18, instance m2-7d051790 (continuing session; worktree proteus-boot-2026-09-17)

## 1. G5 remint over the canonical digest (Archaeon #389/#400/#420; operator relay in chat)

Sync 05:49Z: 9 new, 2 queued (#389, #400 both the same ask). Merged origin/main (0cddaca8e).
Defect: the mint bound sha256 of the declaration's RAW checkout bytes; this repo converts line
endings on checkout, so the same content hashed a3f9816f (CRLF) / 3c2ebfd9 (LF). Archaeon
published population_digest sha256:7f03cc82... with its rule (sorted compact JSON minus
generated_at / wall_s / the digest keys). Change (proteus/eval/emit_c4_starting_population.py):
canonical_digest() implements the published rule; load_declaration() REFUSES a declaration whose
published digest does not recompute under its own rule; the mint writes
declaration_canonical_digest (== published, recomputed, not copied) and keeps declaration_digest
(raw bytes of THIS checkout, 329da847...) beside it with a note; the inner population manifest
now binds the canonical digest too, so population_manifest_id is checkout-invariant:
    population_manifest_id  bbb1862f8bde58d89094f620ea4666bb408e6ddb4756804150293d974a139998
    manifest_hash           2dfc1c5d... (UNCHANGED: the 57 members did not change)
    declaration_canonical   sha256:7f03cc8282b4b1e2d47ebf541d053c7e640732cc44b7ab20b82e3812d3e808c1
Test added: CRLF and LF renderings of the same declaration hash differently raw (the defect) and
identically canonically; a content change moves the canonical digest; volatile keys do not.
One test edit of mine shipped a literal newline inside a string (my heredoc-replace wrote it);
caught at collection, fixed by line. Suite 364 passed / 2 skipped.
Also arrived: Harmonia #412 RULING on #341 (current instrument) -- read after this remint.

## 2. Campaign 6 directive (operator, chat; verbatim + MANIFEST under roles/Proteus/prompts/2026-09-18_campaign6/)

Sync 18:20Z: 13 new, 0 queued -- Campaign 4 COMPLETE (NO_CONDITION_SELECTED; C4-03
REPRESENTATION_BLOCKED: the interpreter is total; C4-04 addressing damage; C4-08
ROBUST_WITHOUT_MECHANISM), Campaign 5 COMPLETE and ACCEPTED (Phase A OLD_SUBSTRATE_EXHAUSTED;
Phase B BOUNDARY_CREATED_NO_DISCOVERY_GAIN; operator: "the failure has moved upstream" to the
searchable program structure / grammar; Representation B handed to Proteus as an instrument).
Read both campaign reports in full (ledgered). Harmonia #412: V0.5 current instrument admitted
as DETECTOR, not as absence instrument until a positive control.
Wrote proteus/docs/campaign6/PROTEUS_CAMPAIGN6_AXIS_O_SCOPE.md: the 09-17 stand
("expressiveness is not the constraint") STANDS and is now joined by C4/C5's measurement that
the flat genome + syntactic grammar is an exhausted SEARCH structure -- its units of variation
are positions, not parts. Proposal (design only): proteus.graph_organism.v1, a NEW runtime
where structure is connectivity and the grammar acts on connectivity (subgraph copy/attach,
edge cut/retarget, dormant nodes first-class); v0 frozen as the control substrate;
Representation B adopted verbatim as a second instrument; behavior_fingerprint.v1 for both;
freeze-bundle emitter for the escalation protocol. Neutrality risks and falsifiers written
(same cliff on the graph grammar => second exhausted substrate; stop). Backlog rows 42-48;
PROTEUS-43 (one new runtime profile) is the operator decision. No code, no mint.
