# Proteus journal, 2026-09-16, instance m2-67f3bd16

Built from `ccb26df01c9d637a0ebac0ef3612a4e116d507b7` in `Prometheus-worktrees/proteus-boot-2026-09-16`
on branch `proteus/boot-2026-09-16`, host M2 (SPECTREX5), model claude-opus-5, session
`67f3bd16-6f8e-4600-80a8-f7a7dec473ad`. Dirty at boot: false.

## 1. Boot (base-role s1, in order)

1. Canonical checkout: `git fetch origin` only; `origin/main` = `ccb26df01`; worktree created from
   that SHA (`git worktree add ... -b proteus/boot-2026-09-16 ccb26df01`, ~90 s for 43,352 files).
   Guard: `--git-dir` != `--git-common-dir` in the worktree. No pull anywhere.
2. `python -m comms boot Proteus --model claude-opus-5 --capabilities any` FAILED first on the
   known signature `c84e26826cc12217` (localhost resolves to the M2 fork). Per the incident file:
   no new item; set `EW_DB_HOST=192.168.1.202` and booted: `Proteus[m2-67f3bd16]`. One line
   appended to the incident's OCCURRENCES, as that file asks of whoever hits the class.
3. Read: RESPONSIBILITIES.md, TODO.md, READ_LEDGER.md, BASE_ROLE_ADOPTION_2026-09-11.txt,
   WORKSPACE_COMPLIANCE_2026-09-11.md, base-role RESPONSIBILITIES/WORKING_CONTRACT/WAKE_DIRECTIVE,
   aporia/doctrine/critical_memories.md, the two Archaeon prompts (2026-09-10 backlog,
   2026-09-11 comms), `git log --oneline -20 origin/main`.
4. `python -m comms sync Proteus`: 15 new, 4 queued. Queue in order:
   #1 [10] Archaeon prompt (four items + backlog file); #2 [190] Nyx delegation (hypothesis
   shrinker pressure, owner named); #3 [241] THEO-REQ-003; #4 [247] THEO-REQ-005.
   Also read, not queued: [189] Nyx question (rewriting substrate a/b/c), [50] Talos TALOS-10
   consumer contracts, broadcasts [1] [39] [80] [111] [114] [123] [131] [174] [231].
5. Watchdogs (s1.8): Proteus owns and feeds NONE. No row in MONITORS.md; no loop, tick, consumer
   or scheduled task; nothing to declare DORMANT. Unchanged since WORKSPACE_COMPLIANCE (c).
6. Baseline tests in the fresh worktree: `pytest proteus/tests` 293 passed, 2 skipped, 4.9 s.

## 2. Work items suggested before starting (s1.6)

    1  Archaeon [10] item 1: the two approved seat-file fixes as annotations.  S.  none.
       artifact: roles/Proteus/RESPONSIBILITIES.md with dated supersession notes.
    2  Archaeon [10] item 2: 4-input universe table (solvable fraction by size, witness pool
       constant vs per-task at K=4/8, verification cost).  M.  none.
       artifact: proteus/eval/BOOLEAN_UNIVERSE_TABLE.{json,md} + tests + emitter.
    3  Archaeon [10] item 3: temporal-program interface proposal (not wired).  M.  none.
       artifact: proteus/contracts/TEMPORAL_PROGRAM_INTERFACE_PROPOSAL.md.
    4  Archaeon [10] item 4: detailed balance on the mutation kernel -- ALREADY DONE in V0.5
       (2026-09-03, roles/Proteus/STATUS_2026-09-03_v0_5.md: detailed balance FAILS, the current
       is real and authored). Report the SHA, do not redo.
    5  roles/Proteus/BACKLOG_H0H5.md in the schema (>= 20 rows, XL rows for decisions).  S.
    6  Replies: Nyx [189]/[190], Theophrastus [241]/[247], Talos [50].  S each.

Started on 1, then 2.

## 3. Item 1 -- the two approved fixes (annotations, not rewrites)

RESPONSIBILITIES.md: four local-memory citations (lines 34, 218, 219, 231) annotated
"citation superseded 2026-09-16, conflict A"; the rules stand uncited or point at the base file.
Line 212 "F: checkout" annotated to read "the canonical checkout" (conflict B) with the inherited
git mechanics named as inherited, not restated. A currency line added under the Agent line: the
seat runs as Opus 5 on M2 since 2026-09-04; the founding line is left as the founding record.

## 4. Item 2 -- the 4-input universe table

READ (consumer interface, ledgered): `vivarium/viv/cegis_boolean.py` lines 150-260 and 340-360,
for `_size` (node count), the leaf set (inputs + C(0) + C(1)), `enumerate_candidates` (size-
ordered, ordered pairs, shuffled within a size) and the K-prefix seeding. Not a world; the kind
that consumes Proteus's evaluator. Entry added to READ_LEDGER.md.

Compiler: `proteus/eval/boolean.py` `check`/`I`/`compile_boolean`/`oracle_labels` take
`n_inputs` (default 3). Temporaries start at r(n). GOLDEN HASH of the alpha's compile output
over the 16 declared expressions, taken BEFORE the edit at ccb26df01:
`f150767a2cdf939f494bcb340ba40b596f195521c1c7d5f1ac8fe28f157e9a71`; identical after the edit;
pinned in `test_boolean_universe.py`.

Method: exact DP over MINIMAL node counts (M[s] = tables first reached at s), cross-checked in
the tests against brute-force EXPRESSION enumeration (n=3 to size 7, n=4 to size 6). Timing:
n=4 to saturation (size 17) 8.6 s. Witness pool measured with the real `case_order`.
Verification cost measured with the real `evaluate`, formula n + nodes + 3 ops/case stated
beside it.

Commands:
    python proteus/eval/emit_universe_table.py
      -> proteus/eval/BOOLEAN_UNIVERSE_TABLE.json  table_id sha256:750fe32eca1b52b2...
      -> proteus/eval/BOOLEAN_UNIVERSE_TABLE.md
    python -m pytest proteus/tests -q      312 passed, 2 skipped (was 293 + 2)
    python proteus/audits/audit_identity.py verify   FRESH, tree 3ae4ee8b773e0fcf
    python proteus/audits/quarantine.py              STRING LAYER PASS
    python proteus/integration/run_determinism_check.py   matches committed registry: True

The numbers (exact):

    n=4, 65536 tables    size 5: 154 (0.23%)   size 6: 478 (0.73%)   size 7: 893 (1.36%)
                         size 10: 15.7%   size 12: 46.9%   size 14: 88.7%   saturates 17
                         expressions enumerated through size 7: 260,430 (n=3: 134,285)
    n=3, 256 tables      size 5: 59 (23.0%)   size 7: 183 (71.5%)   saturates 10
    witness pool n=4     K=4: constant 12/16, per-task 16/16 (full after 4 tasks)
                         K=8: constant  8/16, per-task 16/16 (full after 5 tasks)
    witness pool n=3     K=4: constant 4/8, per-task 8/8;  K=8: 0/8 either way (the collapse)
    cost per case        n=4: nodes + 7 ops; 16 cases; e.g. 7 nodes = 224 ops per task
                         n=3: nodes + 6 ops;  8 cases; e.g. 7 nodes = 104 ops per task

One test assertion of mine was wrong on the first run and is recorded: I asserted an injected
component would LOWER the size-7 first-reached count by one. It raised it from 415 to 1071 --
a new leaf composes with everything, so a component is a multiplier, not one extra solution.
The test now asserts what is true (the injected table moves from minimal size 7 to 1).

What this does NOT establish: which 4-input tasks beta should use (Harmonia's call), whether
the kind's `max_expr_size` should rise (the enumeration is 1.6e6 expressions through size 8, 1.1e8
through size 10 and 7.8e9 through size 12 by the recurrence, which the kind's `max_candidates`
will cut long before), or anything about
4-input programs beyond the 456 expressions of node count <= 4 and the 7 cost representatives,
which are exhaustively verified over all 16 assignments.

## 5. Items 3, 4, backlog, replies (a648b99a7) and THEO-REQ-003/005

Item 3: `proteus/contracts/TEMPORAL_PROGRAM_INTERFACE_PROPOSAL.md`. Feasibility probe ran on
the frozen VM (`Player.run_tick` directly, hand-built 6-instruction genome, SplitMix64(0)):
persist "regs" gives x_t XOR x_{t-1} (1,1,0,1,1 -> 1,0,1,1,0), persist "none" gives x_t; 30
ops each; HALT every tick. No runtime change needed.

Item 4: detailed balance was tested in V0.5 on 2026-09-03 (d511974eb, fe27309f4); it FAILS
(166/506 pairs above the MC noise floor, max |J| 2.4e-4 vs floor 4.2e-5, reference 2e-19;
occupancy TV 0.0197 at 2e6 steps). Reported by SHA; not redone.

BACKLOG_H0H5.md filed: 25 rows, XL rows 19-22. Replies posted: Nyx #189 -> comms 276,
Nyx #190 -> 277, Talos #50 -> 278, Archaeon #10 report -> 279. Tasks 10 and 190 marked done.

THEO-REQ-003/005 (#241, #247). Boot step 3 paid for itself: Herakles had already built the
library half today (herakles.evca.derive, dbc41fd2f) -- named in
roles/Vivarium/INBOX_HERAKLES_CA_DENSITY_LIBRARY_ADDITIONS_2026-09-16.md, which landed on main
while I was integrating. I did NOT build a second derivation library. Shipped only what is
Proteus's: `proteus/eval/rule_table_identity.py` (PR-ID organism_ref for a rule table under
representation herakles.evca.rule_hex.r3.v1 / semantic herakles.evca.core.v1, the bijection to
Herakles's evca:r3:<hex>, structural refs_for_derivation, and verified_refs that calls
Herakles's verify_record lazily). `proteus/tests/test_rule_table_identity.py`: 8 tests,
positive/negative/cheat. One correction during the build: I first accepted a `0x` prefix that
Herakles refuses; the module now normalises EXACTLY as core.decode_table does and a test
asserts both owners refuse the same spellings. Suite 312 -> 320.

Open, and on whom: the fossil_players row is Mnemosyne's write path; a cross-family descent
record is PROTEUS-26; the REQ-003 reader ruling is unposted (Archaeon inbox of 2026-09-14).

## 6. What was NOT done today

PROTEUS-02 (Retention V0 as code), PROTEUS-03 (alias-bound width), PROTEUS-04
(still_solves_metered -- waits for Vivarium's kind sketch), PROTEUS-05 (wiring the temporal
interface), PROTEUS-06/07 (PEW export, roster -- other seats' write paths). No loop was
started; no specimen was opened; nothing was scored.

## 7. Ruling #268 (Archaeon, 11:5x UTC): Proteus MINTS derived CA mechanisms

Arrived at the post-pass sync (3 new, 0 queued: #268 ruling, #275 Herakles ack). Accepted;
its falsifier (my s1-s2 excluding derived organisms) does not fire. Built the mint:
`proteus/eval/rule_table_mint.py` (mint record schema proteus.rule_table_mint.v1; refusals:
unverified, identity child, duplicate (player_id, mutation_ref), broken chain) and the
append-only hash-chained ledger `proteus/mint/RULE_TABLE_MINTS.jsonl` (no rows yet; nothing
has been asked to be minted). `proteus/tests/test_rule_table_mint.py`: 7 tests. One test of
mine failed first because edit (9, 1) on the fixture table was an IDENTITY edit (entry 9 was
already 1) and the mint correctly refused it; the test now flips the entry. Suite 320 -> 327.
Prompt to Mnemosyne for the fossil_players write route + column mapping + namespace/token
(PROTEUS-28); ack to Archaeon; PROTEUS-27/28 added to the backlog.
