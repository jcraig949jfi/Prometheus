# Proteus journal, 2026-09-17, instance m2-7d051790

Built from `4a6457fbb14015cab3e94f66bc11fed6ac6527ea` in `Prometheus-worktrees/proteus-boot-2026-09-17`
on branch `proteus/boot-2026-09-17`, host M2 (SPECTREX5), model claude-opus-5, session
`7d051790-1272-4b23-b372-db319111bbcc`. Dirty at boot: false.

## 1. Boot (base-role s1, in order)

1. Canonical checkout: `git fetch origin` only; `origin/main` = `4a6457fbb` (canonical was 11
   behind; not touched). Worktree created from that SHA in 42 s (44,458 files). Guard: git-dir
   != git-common-dir. Yesterday's worktree `proteus-boot-2026-09-16` (c8ceaa585) verified clean
   and MERGED, removed, branch deleted locally (no remote branch existed) -- WORKING_CONTRACT s5.
2. `EW_DB_HOST=192.168.1.202 python -m comms boot Proteus --model claude-opus-5 --capabilities any`
   -> `Proteus[m2-7d051790]`. Concurrency check: `comms who` shows one live Proteus instance
   (this one); m2-67f3bd16 last synced 2026-09-16 08:15. No sibling worktree naming today's work.
3. Read: RESPONSIBILITIES.md, TODO.md, READ_LEDGER.md, BACKLOG_H0H5.md, yesterday's journal and
   replies MANIFEST, base-role RESPONSIBILITIES / WORKING_CONTRACT / WAKE_DIRECTIVE / MONITORS
   (grep: no Proteus row), aporia/doctrine/critical_memories.md, `git log --oneline -25 origin/main`.
4. `comms sync Proteus`: 11 new, 0 queued. Six carry Proteus asks: #292 Mnemosyne (first prod
   mint), #304 Vivarium (cegis_boolean_v1 axes), #327 Mnemosyne (instr1-16 identity), #328
   Daedalus (D4 profile by value), #330 Vivarium (A7 string forms), #333 Vivarium
   (bundle.population). #323/#324/#326 Archaeon campaign closes; #332 MNE-D1 ruled (nothing
   changes for Proteus today).
5. Watchdogs (s1.8): Proteus owns and feeds NONE; no MONITORS row; unchanged.
6. Baseline: `pytest proteus/tests` 327 passed, 2 skipped, 15.5 s (matches 09-16 close).
7. The operator's point-release directive + Amendment 1 (roles/Mnemosyne/prompts/
   2026-09-17_point_release/) found via the peers' messages, MANIFEST verified 3/3. PROMPT 3 is
   addressed to Proteus; Amendment s8 makes the scope freeze wait on Proteus's read-only review;
   s14 says "boot/review immediately". Taken as the first work item. Campaign 3 CLOSED at
   cb9135104, so its report was read as authoritative (section A).

## 2. Work items suggested before starting (s1.6)

    1  Point-release read-only review (Amendment s8 questions, Stage 0-2, Stage 3 replies).  M.
       artifact: proteus/docs/point_release_2026-09/PROTEUS_POINT_RELEASE_REVIEW.md.  DONE.
    2  Expressiveness witness for C4-3 branch 1 (does the frozen ISA express a keyed two-value
       memory?) with positive/negative/cheat/identity controls.  S.  DONE (inside item 1).
    3  Replies: Daedalus #328, Vivarium #330/#333/#304, Mnemosyne #327/#292.  S.  DONE (files;
       posted at close).
    4  PROTEUS-36 test-namespace mint rehearsal (#292).  S.  NOT STARTED (after the packet).
    5  PROTEUS-02 Retention V0 as code.  M.  NOT STARTED (below the directive in priority).

## 3. Stage 0 reads and what they established

READ (ledgered under 2026-09-17 in READ_LEDGER.md): the three campaign reports; evolve.py's
FOUNDRY dict and gen0/common_fill; reachability.foundry_id; FOUNDRY_C1/C2 definitions; the
three peer Stage 0/1 documents' Proteus-facing sections; PEW ontology vocabularies and the
fossil_players columns (005/006/013). NOT read: archaeon/wse/worlds.py, any world protocol.

Established (each recomputed, not recalled):
- archaeon.wse imports proteus.foundry {generate, grammar, lineage.descend, vm, prng}: every
  C1-C3 organism is a Proteus organism under runtime 73f110e2..., grammar v0.4 5043f5e1....
- `instr1-16:6528b9dc` = sha256(json.dumps(regime dict, sort_keys))[:8] over a
  proteus.foundry_manifest.v0 dict minus seed/n (Archaeon's rule). Recomputed: FOUNDRY_C1 ==
  FOUNDRY_C2 -> 6528b9dc (C1, C2 default, C3 via c3base); evolve.FOUNDRY -> instr1-32:199105b4;
  proteus DEFAULT_FOUNDRY_MANIFEST (the 64-specimen registry) -> instr1-64:97ce0af8.
  Proteus's own `foundry_identity(fm)` includes seed/n (a population id); a regime-level
  profile id excluding seed/n and INCLUDING runtime+grammar does not exist -> P1.
- grep of archaeon/campaign*/{DECISIONS,CAMPAIGN_REPORT}.md and archaeon/wse/*.py for
  USE_B | "authored current" | neutrality | equilibrium: 0 hits. Three campaigns ran the
  kernel whose registry qualification is NOT_QUALIFIED_AUTHORED_NONEQUILIBRIUM_CURRENT without
  naming it. Recorded as an unstated regime coordinate, not a defect.
- The grammar already contains every structural operator the directive s6 lists.
- The ISA already has multiple channels, persist policies, bounded tape, register-indirect
  LD/ST, RND, tick_budget, out_cap. No "cost query" affordance (brief R1 lists one).

## 4. The witness (proteus/eval/keyed_memory_witness.py)

Hand-written 12-instruction keyed memory (LDC/INQ/IN/ADD/JZ/ST/LD/OUT/HALT) on a neutral
two-channel probe, persist=tape, code_writable=false, tape 64, keys 0..15 at offset 48.

    keyed      6/6 two-key, 16/16 all-keys, 81 ops (9/tick), halt every tick
    one-value  3/6, 1/16 (the last-PUT shelf strategy; exactly half on two keys)
    inert      0/6, 0/16
    cheat      echo program 0/6 under the honest probe, 6/6 only when the probe leaks the
               expected value on the value channel
    identity   replay byte-identical; ops == 81

Verdict ISA_EXPRESSES_KEYED_MEMORY. Committed with the JSON and a 5-test file; the test
asserts the committed JSON matches the live runtime. One draft function was written wrongly
mid-file (a JZ offset landing on HALT) and replaced before any run; recorded here because the
first version never executed and so was never a wrong number.

Commands:
    python -m pytest proteus/tests -q               332 passed, 2 skipped
    python proteus/audits/audit_identity.py verify  FRESH, tree 3ae4ee8b773e0fcf (unchanged)
    python proteus/audits/quarantine.py             STRING LAYER PASS
    python proteus/integration/run_determinism_check.py   matches committed registry: True
    python proteus/eval/keyed_memory_witness.py     verdict: ISA_EXPRESSES_KEYED_MEMORY

## 5. The review

proteus/docs/point_release_2026-09/PROTEUS_POINT_RELEASE_REVIEW.md: Stage 0 inventory (s1),
the twelve Amendment-s8 answers (s2), the delta P1-P14 classified with MUST/SHOULD/DEFER/
MAJOR/REJECT and the eleven directive fields for each MUST (s3), Stage 2 self-critique with
KEEP/MODIFY/ADD/DROP (s4), the computed profile catalog draft (s5), Stage 3 criticism of
Daedalus/Vivarium/Mnemosyne/Archaeon (s6), declined directive items with reasons (s7),
conflicts and falsifiers (s8), what was not done (s9). Pure ASCII verified.

Headline answers: expressiveness is NOT the frontier constraint (witness); ship identity
(P1 profile, P2 population manifest), shapes (P3 descriptor, P4 capability observation) and
the quarantine test (P5); REJECT a new ISA primitive for W2_K2; DEFER mass profiles until P6
stratifies the neighbourhood per operator; MAJOR for the runtime transition (PROTEUS-19).

## 6. Backlog / ledger / replies

BACKLOG_H0H5.md rows 29-36 added (P1-P6, P9 deferred, test-namespace mint rehearsal).
READ_LEDGER.md: 2026-09-17 block. Replies committed under
roles/Proteus/prompts/2026-09-17_point_release_replies/ with MANIFEST; posted at close
(ids in section 8).

## 7. Operator, in chat, after the review was written (verbatim saved as
##    roles/Proteus/prompts/2026-09-17_round2/00_OPERATOR_MESSAGE_ROUND2.md)

"Proteus Round 2: Representation & Search Geometry" -- four lanes (frozen control profile;
minimal neutral expressiveness profile; structural-search profile; richer-interaction ABI
design only), anatomise the C3 delay-invariant readers as specimens first, a control matrix
per change, a rule ("do not design organisms to solve W2_K2 or delay tasks; design substrate
variants that change the classes of computation evolution can cheaply express"), and
"you have the final say". Taken up after this trajectory closed; see section 9.

## 8. Session close (first unit of work)

Commit 20bd3cfdc (review + witness + ledger + backlog + replies) on top of a merge of
origin/main 3f2920d6a (Daedalus SFE schema 9 candidate + SFE_INTERFACE_DELTA scope frozen
"by least-committal reversible default" for Proteus; Mnemosyne PEW candidate; Vivarium Stage 3
answers). Tests on the merged tree 332 passed / 2 skipped. Pushed HEAD:main -> 305876d00;
`git merge-base --is-ancestor 20bd3cfdc origin/main` true. Comms posts 336 (report to
Archaeon, Daedalus, Mnemosyne, Vivarium, Harmonia), 337 (Daedalus D4), 338 (Vivarium),
339 (Mnemosyne). Post-sync: 2 new, 0 queued -- #334 Daedalus SFE deploy window OPEN under
operator order (no Proteus write to 8811; none planned), #335 Vivarium Stage 3 answers (its
open Proteus ask is the population manifest proposed in the review). Review packet in chat
as one ASCII block.

## 9. Round 2 ruling (operator message, "you have the final say")

ADOPTED as "Proteus Round 2: Representation & Search Geometry" with three amendments:
  A  lane 2 (minimal expressiveness) re-sequenced behind anatomy + lane 3 and put behind a
     two-condition admission gate, because the candidates named (indexed state, indirect
     addressing, bounded registers) already exist in the frozen ISA and the motivating
     program was witnessed today at 12 instructions / 9 ops per tick. One deferred
     candidate: the cost query (brief R1; lane-4 motivated).
  B  lane 3 (structural search) = grammar MASS PROFILES (G- point-only restriction, G+
     structural-heavy) as new grammar_versions beside frozen v0.4 -- every operator the
     operator names already exists with a hashed weight; per-profile R4 and current
     measurement required; recombination control is a PAIR (profile: Proteus; mate policy:
     Archaeon).
  C  two control-matrix rows added: per-profile kernel current; same-profile re-keyed-seed
     noise floor beside every geometry statistic (C3-SFE-07).
Anatomy of the delay-invariant readers first, split along the firewall: Proteus computes
structure and ablation SETS; Archaeon lesions on the delay family. Design at
proteus/docs/round2/ROUND_2_REPRESENTATION_AND_SEARCH_GEOMETRY.md (sections 0-8 incl.
self-dissent). Operator message saved verbatim with MANIFEST under
roles/Proteus/prompts/2026-09-17_round2/. Delegation to Archaeon (specimens + mate policy)
and question to Harmonia (current instrument) written there and posted. Backlog rows
PROTEUS-37..41. Nothing minted; no run proposed for launch.

## 10. Repair order (operator, chat; verbatim at roles/Proteus/prompts/2026-09-17_repair_order/)

Section 3 assigned PROTEUS-29, -30, -36 and anatomy handoff support; no Lane 2 override; no
architecture change. Sync before: 6 new (SFE point release DEPLOYED #343 schema 9 live; PEW
DEPLOYED #344; Daedalus long-run finding + correction + "9.0.1 not today" #345/#347/#348;
Vivarium window plan #346), 0 queued, nothing addressed to Proteus, no specimens from Archaeon.

PROTEUS-29  proteus/eval/foundry_profile.py + FOUNDRY_PROFILE_CATALOG.json (catalog_id
            42e4db36db259a16...). Profile = regime (foundry manifest minus seed/n) + runtime_hash
            + affordance_hash + grammar {version, hash, WEIGHT VECTOR} + the registry's kernel
            qualification (read from PLAYER_REGISTRY.json, not typed). id pfp1:<16 hex>.
            Archaeon's rule reproduced byte-exactly (json.dumps sort_keys default separators,
            sha256[:8]): instr1-16:6528b9dc -> pfp1:625bc70456ebfa20 (C1/C2/C3);
            instr1-32:199105b4 -> pfp1:ecd4c13a4b7adf28; instr1-64:97ce0af8 ->
            pfp1:cff673bf3889d638 (registry). 10 tests: recompute; receipt join
            (archaeon/campaign2/C2-SFE-02/PREREG.json budget.foundry_id -- my first assertion
            used the wrong path, prereg["foundry_id"]; corrected to ["budget"]["foundry_id"]);
            seed/n invariance; every identity-bearing field changes the id; a moved grammar
            weight cannot alias (the Round 2 lane-3 guarantee, structural); cheats refused.
PROTEUS-30  proteus/eval/population_manifest.py + REGISTRY_POPULATION_MANIFEST.json
            (population_manifest_id 03a3af5d1dad771c..., manifest_hash 6acf3d993a3b55d6...,
            profile pfp1:cff673bf3889d638, 64 gen0). members + recipe must agree: the recipe
            REGENERATES the base population (registry: recipe_population_hash == manifest_hash).
            12 tests incl. the defect test: a harness-seeded organism labelled gen0 (C1
            L-008/L-030) is REFUSED "not produced by the recipe"; future-information selection
            evidence refused; organism_id must hash from its manifest.
PROTEUS-36  proteus/integration/run_mint_roundtrip_test.py against the LIVE M2 service
            http://127.0.0.1:8377 as agent Proteus, namespace test: derive (Herakles edit at
            entry 11) -> mint into a SCRATCH ledger (real ledger untouched, 0 rows) ->
            register inserted -> read back 0 mismatches over 8 mapped columns + producer.mint_id
            -> identical again duplicate_identical -> differing mutation_ref 409
            conflict_existing_row_differs, row unchanged. ROUNDTRIP_OK. player_id
            evca:r3:004f005f005f005f005fff5f005fff5f, mint_id sha256:3888f447ad0bd45f...
            Result JSON checked: 0 occurrences of token/bearer.
Anatomy     BLOCKED on input (Archaeon manifests, comms 340). structural_descriptor() is ready.
Gates       pytest 354 passed / 2 skipped (was 332); audit_identity FRESH 3ae4ee8b773e0fcf
            (runtime/grammar/affordances untouched); quarantine PASS; determinism True.
Receipt     proteus/docs/repair_2026-09/PROTEUS_REPAIR_RECEIPT.json (frozen identities, sha256
            of every artifact, acceptance row by row, not-done list).
Not done    no prod mint; no G-/G+ grammar profile minted (Harmonia #341 open; not required by
            the order -- the profile id already separates any weight vector from v0.4); no
            anatomy; no runtime change.

## 11. Slip, recorded (calibration ledger)

6c531d4a5 was pushed with 1 failing test on the MERGED tree:
test_workspace.py::test_every_writing_entry_point_is_guarded_or_explicitly_excluded flagged
proteus/integration/run_mint_roundtrip_test.py (my new writing entry point, no D-23 refusal
guard). Two causes, both mine: the pre-commit full-suite run reported 354 passed, so the guard
test did not fire then and I did not investigate why (a run-to-run difference in a test the
d884b3513 commit already calls cache-sensitive); and the push chain tested `tail`'s exit code,
not pytest's, so a red merged tree pushed. Fix: guard added (assert_not_canonical), suite 354/2
skipped and test_workspace 11/11 green on two consecutive runs; push chain now gates on pytest's
own status. WORKING_CONTRACT s5 was violated for one commit; origin/main was red for the minutes
between 168ffca3e and the fix commit for anyone running proteus/tests. Both SHAs kept.

## 12. Archaeon's delivery (via operator relay + main 1df3b99d7/1e17d14a4): G5 mint, anatomy, Q3

Merged origin/main (Daedalus 9.0.1 DEPLOYED 699ca0f9; Vivarium window C4-20260917-W1 CLOSED;
Archaeon c4 directive + launch gate RED + STARTING_POPULATION 57 distinct + SPECIMENS_FOR_PROTEUS).
- G5 mint: proteus/eval/emit_c4_starting_population.py -> C4_STARTING_POPULATION_MANIFEST.json
  over declaration digest sha256:a3f9816f... ; population_manifest_id a79458e3...,
  manifest_hash 2dfc1c5d..., profile pfp1:625bc70456ebfa20; 57/57 ids hash; 12 gen0 draws
  regenerate from seed 20260921; 45 imports with ancestries verbatim. 4 tests. The
  minted_by_proteus flag is in Archaeon's file; not touched (lane rule); Archaeon flips it.
- Gate defect found: launch_gate.g5_proteus reads o.get("ancestry") but the key is
  "ancestries" -> G5 can never go GREEN. Reported.
- PROTEUS-37 anatomy: proteus/eval/anatomy.py -> proteus/round2/ANATOMY_L0_{RESULT.json,
  ABLATION_SETS.json,.md}. Structure only; heldout_* stripped (test). 24 stats x 3 pairs,
  20,000 relabellings seed 20260921 (20.6 s). readers vs W0: tick_budget 267.6 vs 82.1
  (p 0.0062), has_conditional_branch 0.82 vs 0.33 (p 0.0213); none clears 0.05/24; persist
  regs-or-all constant 1.0 both groups. readers vs shelf: n_regs 5.7 vs 11.3 (p 0.0008).
  Ablation sets 195/198/464 knockouts (instruction->NOP, persist->none). 5 tests incl. the
  identical-groups cheat (floor_p == 1.0 everywhere).
- Q3 answered: USE_B forbids NEUTRAL-operator claims; selection-driven use is conditional on
  the kernel; Campaign 4 inherits a stated ceiling sentence (report s4); no relicensing.
