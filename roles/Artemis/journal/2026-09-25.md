# Artemis journal 2026-09-25

## Creation pass (instance ubu002-78a7bd7b, host ubu002)

- Directive: operator, in chat, verbatim at
  roles/Artemis/prompts/2026-09-25_creation/ (MANIFEST verifies, 2
  entries). The session chose the name (asked: a Greek god/goddess/hero
  from the original Deities & Demigods); candidates checked against
  roles/, agents/ and `git grep` in *.md: Artemis, Tyche, Nike all 0 hits.
- Workspace: `git fetch origin` in the canonical checkout
  /home/jcraig/Prometheus (read-only), origin/main =
  6ad5ec80ef31c60b1acc75cad47b0dd36e168100; `git worktree add
  /home/jcraig/Prometheus-worktrees/artemis-base-role -b
  artemis/base-role-adopt-2026-09-25 6ad5ec80e` (56876 files, 900 s
  budget). Guard: git-dir != git-common-dir.
- Read: roles/base-role/{README,INHERITANCE,WAKE_DIRECTIVE,NORTH_STAR}.md,
  RESPONSIBILITIES.md s1 and the constitutional rules, WORKING_CONTRACT.md
  s1-s3; the newest creation-pass exemplar roles/Cyclops/* (creation commit
  f4202098d). NOT read this pass: MONITORS.md in full (seat owns/feeds no
  loop), aporia/doctrine/critical_memories.md, base RESPONSIBILITIES s2-s7
  in full.
- Archaeology: `git grep -il artemis` -> 1 incidental hit (Lexis
  bibliography gpbib.bib); `git log --grep=artemis -i` -> 0. No prior seat.
- Host: ubu002, Ubuntu 26.04.1, 192.168.1.219, 4 cores, 7 GB, no GPU,
  207 GB free. Not in comms/api.py MACHINES, so the machine label is the
  hostname. No pip; `python3 -m venv` fails (no ensurepip). Installed via
  `sudo apt-get install python3-psycopg2 python3-pytest` (the only change
  to the host). Absent: gcc, make, docker, node, cargo, go, java, julia,
  lean, sage, gp, R, psql, nvcc, numpy. M1 5432 reachable.
- comms: sync Artemis (EW_DB_HOST=192.168.1.202) succeeded before the
  directory existed (program broadcast backlog, marked seen); second sync
  0 new, queue 0. After the files existed: booted
  Artemis[ubu002-78a7bd7b], heavy, claude-opus-5-5.
- Self-test `python3 -m pytest -q archaeon/tests/test_base_role.py`:
  10 passed, 1 skipped (no Task Scheduler on this host).
- Files: RESPONSIBILITIES.md (entry), WAKE.md, STATUS.md, TODO.md,
  BACKLOG_H0H5.md (provisional, 4 rows), calibration/LEDGER.md (empty),
  journal/, prompts/2026-09-25_creation/; two self-service rows in
  roles/base-role/INHERITANCE.md. ASCII except the verbatim prompt
  (UTF-8 apostrophes as received).
- Monitors owned or fed: none. Not run: any science.

## Host ruling (operator, same instance)

- Operator: this machine is not M1 (SKULLPORT); it is called ubu002.
  Artemis is likely the only agent here given its small memory, CPU and
  disk. Fleet: M1 (SKULLPORT), M2 (SPECTREX5), M3, M4, two laptops, and
  ubu002.
- Consequence: the "no M-number" TODO is closed -- ubu002 IS the name, and
  the comms hostname fallback already tags instances correctly. No edit
  to comms/api.py is needed or requested. RESPONSIBILITIES.md s0 and
  STATUS.md updated.
