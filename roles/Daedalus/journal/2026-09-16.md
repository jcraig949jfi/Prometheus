# Daedalus journal -- 2026-09-16 (instance m2-d6ecd70b)

First boot of this seat on M2 / SPECTREX5. Every prior session ran on M1.
Worktree `D:\Prometheus-worktrees\daedalus-boot-2026-09-16`, branch
`daedalus/m2-d6ecd70b-boot-2026-09-16`, base `ccb26df01` (origin/main).

## Boot

- `python -m comms` refused M2's local Postgres fork (incident
  c84e26826cc12217, occurrence line added); `EW_DB_HOST=192.168.1.202`
  and proceeded. Booted 11:41Z. Online at boot: Archaeon (m2-5c10f6f6)
  only. 13 unseen, 1 queued (#6, Archaeon #223).
- What arrived: Nestor #262/#263 (SKULLPORT rebooted 09-14 19:55; SFE and
  the Vivarium consumer stopped; restart is mine); Archaeon #223 (B1
  parity PASSED, cut over; delegation: `spec_hash`/`committed_seq`(/`spec`)
  on `/v2/read/observations`); Vivarium #224 (B1 recurrence live, scope
  617); Archaeon #225/#226/#229/#233/#237/#238 (S3..S7 + ARCH-46A closed
  synthetically, production not licensed, zero rows -- no engine action);
  Techne #231 (Z: share unreachable from M1); Harmonia #254/#256 (#215
  contract for 726275da STAGED at 3da6c2439; promote AFTER the restart;
  deploying first halts nobody, measured).
- A directive that reached me only as a commit message: `ccb26df01`
  (operator, 09-15 17:17) "M1 is handed to Nestor and SFE moves to M2
  carrying M1's engine.db" -- it added `--max-artifact-bytes 33554432` to
  the M2 launcher. No prompt, no comms post, no migrated ledger on M2.

## Measured before touching anything

    M1 8811   connect TIMES OUT from 192.168.1.191 (host pings, 5432 answers)
              -> the AtStartup task did not bring the engine back after the
                 09-14 reboot, or a firewall/profile change is dropping it;
                 cannot tell which from here. No SSH (22 times out), no SMB
                 (error 1702). M1 is not reachable for operations from M2.
    M2 8811   connection REFUSED. Watchdog log: engine dead since
              2026-09-14 05:33; ~600 "restart FAILED" lines; engine log:
              serve.py D-23 guard refusing the CANONICAL checkout
              (the tracked launcher served D:\Prometheus\...\Engine).
              A refusal exits before the port binds, so the probe never
              saw an engine and relaunched every 5 min for two days.
    watchdog  the tracked script was REPLACED MID-RUN at 11:40Z today by a
              `git pull` of the canonical checkout (log format changed
              from local-time to Z; state file v1 appeared; count 1 of 3).
              A supervisor whose code changes on someone else's pull is
              not pinned. That is now fixed by pinning a copy.

## M2 relocation (item 1) -- DONE, receipted, LIVE_VERIFIED

`deploy/relocate_m2.py` (imports the M1 tool's primitives; M2 names;
writes the launcher and the watchdog copy; re-points ONLY the task's
action; starts through the supervisor). Rehearsal 6/6 PASS, apply
11:48Z, receipt `deploy/M2_RELOCATE_2026-09-16/apply.json`.

    code     D:\Prometheus-worktrees\daedalus-sfengine, detached ccb26df01
             (sfe/*.py hash == CANDIDATE_BUILD 726275da; only deploy/ and
             tests/ differ from 8c53d04e6)
    data     D:\Prometheus-data\sfe\{engine.db, blobs, m2.crt, m2.key,
             backup\engine.db.pre-schema8-*.bak + the two 09-04 .bak}
    launcher D:\Prometheus-data\sfe\sfengine_m2.cmd
    watchdog D:\Prometheus-data\sfe\sfengine_m2_watchdog.ps1 (pinned copy,
             sha256 in the receipt); task action re-pointed; principal,
             triggers untouched; state/log/park now live beside it
    invariant engine_instance_id eng_906356f7fb1da180131f9290 identical in
             source, copy and the started engine; schema 4 -> 8 migrated
             in-store; canonical copies UNTOUCHED (removal = separate step)

Verification against the REAL service (not a scratch engine):
unit 481 passed (.venv-m2); harness 12/12; isolation 7/7; `/v2/health`
(B3) answers measured quantities; A6 journal dir
`D:\Prometheus-data\sfe\incidents`, not degraded. Harmonia's gate with
the staged 726275da contract: unmodified -> DRIFT on
`engine_instance_id` only (it pins M1's ledger; designed); instance id
rewritten to M2's -> CONFORMANT 68/68 routes, 30/30 GET scoping
(`gate_arm1_unmodified.txt`, `gate_arm2_m2_instance.txt`). The
candidate is LIVE_VERIFIED on the twin. It is NOT deployed on M1.

Pins: `deploy/DEPLOYED_BUILD_M2.json` (new); `CANDIDATE_BUILD.json`
notes the twin verification; MONITORS row 21 rewritten (my row), row 23
(SFEngine M1) annotated NOT ANSWERING; RUNNING_M1_VS_M2.md difference
sheet updated; tracked `deploy/sfengine_m2.cmd` carries a SUPERSEDED
header.

Live things touched on M2: the SFEngineM2Watchdog task ACTION; the
engine process (started); `D:\Prometheus-data\sfe` created. NOT touched:
M1, PEW on M2, the M2 local Postgres, the canonical checkout's files
(one untracked state file there is now stale and ignored).

## Held for the operator (item 4)

`ccb26df01` says SFE moves to M2 carrying M1's engine.db. I cannot do it
from here: M1's ledger (640 MB at `D:\Prometheus-data\sfe` on M1) is
unreachable from M2, and the move changes the production endpoint that
Vivarium's and Archaeon's configs and the contract's `base_url` name.
Questions that decide the shape, asked in chat: (1) restart M1's engine
now (someone on M1) or leave it down until the move; (2) who copies the
ledger and by what path (Z: is unreachable from M1 per Techne #231);
(3) the consumers re-point in the same window or after. The M2 side is
ready: the layout, launcher, supervisor and build are the ones the
migrated ledger would run on, and the staged contract's
`engine_instance_id` (eng_8a37a5d3) is ledger state, so it carries over
and `promote_candidate_contract.py` should read 0 after the swap --
that is the falsifier Harmonia named in #256 (a).

## Operator's ruling (12:0xZ) and the rest of the pass

"SFE ecosystem runs on M2 now; M1 runs a different ecosystem." Nothing
M1-side is mine any more. Asked one question (ledger comes over, or fresh
on M2's); proceeding on "comes over" (ccb26df01's text) until told
otherwise.

- `deploy/adopt_m1_ledger.py` (9bf7da675): the data-only swap; check mode
  refused an absent source and the twin's own schema-4 snapshot posing as
  the source (4 independent FAILs). Nothing applied; the landed dir does
  not exist yet.
- Queue #6 / Archaeon #223 (adb7952ff): spec_hash + committed_seq beside
  each scoped observation, `include_spec=true` for the spec; cheat control
  that no owner-shaped access leaks; 483 passed. New engine hash
  4dbcd3fd; NOT deployed (the staged contract names 726275da and the swap
  + promote go first). Contract unaffected. Reported #295.
- Consumers: Vivarium #288 accepted #270, withdrew (b), holds 5 rows, M2
  consumer prepared not launched; Archaeon #284 HOLD, no tick anywhere
  until my "step 4", redeploys on M2 with a new client (ARCH-51). The
  20:12Z stop on 09-15 was the engine leaving for the move.
- main: 2b21acf76, 9bf7da675, adb7952ff (merged forward twice: Mnemosyne,
  Herakles). Herakles de6adc493 reports the base-role self-test RED on
  main from M2 on a VivariumConsumerM2 registry row -- not mine, noted.
