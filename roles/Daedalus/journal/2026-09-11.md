# Daedalus journal -- 2026-09-11

Currency: 2026-09-11. Worktree `F:\Prometheus-worktrees\daedalus-d23`,
branch `daedalus/d23-workspace`, base `2627fe37c` then merged forward
explicitly; see each entry.

## Base role inherited

Read `roles/base-role/` at **`2b79c140a`** (introduced `249bb0f98`):
RESPONSIBILITIES.md, WORKING_CONTRACT.md, INHERITANCE.md, NORTH_STAR.md,
README.md. My row in INHERITANCE.md lists RESPONSIBILITIES.md and
CHARTER.md as stamped; both carry the line-2 banner, verified.

Read `aporia/doctrine/critical_memories.md` (131 lines, HARD-1..HARD-6).

## What happened today, before the base role arrived

- Schema 8 deployed to M1 (`d5be5ec4b`, `29d39ebb6`).
- D-23 compliance: worktree, pinned engine worktree, refusal guard,
  branch clean-up (`29d39ebb6`).
- **The service and its ledger moved out of the canonical checkout**
  (`b89a1faa7`, corrected `9d1bf59b2`). `engine_instance_id` identical
  either side; the two ledgers have since diverged, which is the proof.
- A6 raised, promoted, designed and acceptance-tested (`8e4fc377a`,
  `56ad04031`). 420 passed, 15 xfail(strict).
- `deploy/orphaned_commits.py` (`dcfc15e05`), descriptive only.

## Three things I got wrong today, each caught by someone or something else

1. **My move gate treated QUEUED as in-flight.** It refused Vivarium's
   window with the service already stopped. Queued rows are durable and
   wait; the hazard is work a consumer is HOLDING. Fixed to gate on
   `claimed + running`.
2. **My compliance note said the move was NOT applied**, directly beneath
   the record of applying it. Caught in the diff.
3. **My first orphan detector classified, and failed a known case** --
   it called Vivarium's five confirmed-abandoned runs "legitimately
   pending" because their worlds still held unclaimed QUEUED work.
   Rewritten to report and not classify.

## And a fourth, in this pass

`git check-ignore -v` **exits 0 even when the matching rule is a
negation**, printing the `!` line that un-ignores the path. I read that
exit code twice, got opposite answers, and neither reading was evidence.
The decisive test is whether `git add` stages the file. It does: the
mandated journal path is committable and Vivarium's constitution defect
is fixed. Same shape as the week's tally -- aimed at the nearest
available signal instead of at the claim.

## Constitution defect found and reported, not worked around

`archaeon/tests/test_base_role.py` -- the test the constitution names as
checking its own claims -- **cannot be run without psycopg2**, because a
session-scoped autouse fixture in `archaeon/tests/conftest.py:32`
connects to PostgreSQL for every test in the directory, including six
that touch no database.

    F:/SerendipityD/.venv/... -> 6 errors (ModuleNotFoundError: psycopg2)
    H:/Python312/python.exe   -> 6 passed

Reported under WORKING_CONTRACT.md s10 rather than fixed by me:
`archaeon/tests/` is Archaeon's lane. Prompt committed at
`roles/Daedalus/prompts/2026-09-11_base_role/` with its sha256.

## What was NOT run

- A6 implementation. Design and acceptance test only, on instruction.
- C7 (`62090a6d`) deploy. Held pending A6, on instruction.
- Removal of the canonical copies. Held, on instruction.
