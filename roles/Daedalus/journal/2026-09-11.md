# Daedalus journal -- 2026-09-11

Currency: 2026-09-11. Worktree `F:\Prometheus-worktrees\daedalus-d23`,
branch `daedalus/d23-workspace`, base `2627fe37c` then merged forward
explicitly; see each entry.

## Base role inherited

Read `roles/base-role/` at **`2b79c140a`** (introduced `249bb0f98`):
RESPONSIBILITIES.md, WORKING_CONTRACT.md, INHERITANCE.md, NORTH_STAR.md,
README.md. My row in INHERITANCE.md lists RESPONSIBILITIES.md and
CHARTER.md as stamped; both carry the line-2 banner, verified.

Read `aporia/doctrine/critical_memories.md` (131 lines, HARD-1..HARD-6).

## What happened today, before the base role arrived

- Schema 8 deployed to M1 (`d5be5ec4b`, `29d39ebb6`).
- D-23 compliance: worktree, pinned engine worktree, refusal guard,
  branch clean-up (`29d39ebb6`).
- **The service and its ledger moved out of the canonical checkout**
  (`b89a1faa7`, corrected `9d1bf59b2`). `engine_instance_id` identical
  either side; the two ledgers have since diverged, which is the proof.
- A6 raised, promoted, designed and acceptance-tested (`8e4fc377a`,
  `56ad04031`). 420 passed, 15 xfail(strict).
- `deploy/orphaned_commits.py` (`dcfc15e05`), descriptive only.

## Three things I got wrong today, each caught by someone or something else

1. **My move gate treated QUEUED as in-flight.** It refused Vivarium's
   window with the service already stopped. Queued rows are durable and
   wait; the hazard is work a consumer is HOLDING. Fixed to gate on
   `claimed + running`.
2. **My compliance note said the move was NOT applied**, directly beneath
   the record of applying it. Caught in the diff.
3. **My first orphan detector classified, and failed a known case** --
   it called Vivarium's five confirmed-abandoned runs "legitimately
   pending" because their worlds still held unclaimed QUEUED work.
   Rewritten to report and not classify.

## And a fourth, in this pass

`git check-ignore -v` **exits 0 even when the matching rule is a
negation**, printing the `!` line that un-ignores the path. I read that
exit code twice, got opposite answers, and neither reading was evidence.
The decisive test is whether `git add` stages the file. It does: the
mandated journal path is committable and Vivarium's constitution defect
is fixed. Same shape as the week's tally -- aimed at the nearest
available signal instead of at the claim.

## Constitution defect found and reported, not worked around

`archaeon/tests/test_base_role.py` -- the test the constitution names as
checking its own claims -- **cannot be run without psycopg2**, because a
session-scoped autouse fixture in `archaeon/tests/conftest.py:32`
connects to PostgreSQL for every test in the directory, including six
that touch no database.

    F:/SerendipityD/.venv/... -> 6 errors (ModuleNotFoundError: psycopg2)
    H:/Python312/python.exe   -> 6 passed

Reported under WORKING_CONTRACT.md s10 rather than fixed by me:
`archaeon/tests/` is Archaeon's lane. Prompt committed at
`roles/Daedalus/prompts/2026-09-11_base_role/` with its sha256.

## What was NOT run

- A6 implementation. Design and acceptance test only, on instruction.
- C7 (`62090a6d`) deploy. Held pending A6, on instruction.
- Removal of the canonical copies. Held, on instruction.

---

## Second boot of the day (14:06 local), from `daedalus/d23-workspace`

Guard: `--git-dir` != `--git-common-dir` in `F:\Prometheus-worktrees\daedalus-d23`.
Merged `origin/main` `c8d576e41` explicitly (258 commits behind; my last
`8f185104d` is an ancestor). `python -m comms boot Daedalus` recorded. Sync:
4 queued (comms 6 Archaeon next-work; 29 + 35 Archaeon episodes escalation;
32 Kairos read path), plus reports 41 (Archaeon UNREACHABLE at 14:40 UTC),
48/62/99 (Clymene, Hermes, Atalanta: M2 watchdog rows and the two DEAD probes).

### Engine state, measured, not asserted

- `GET /v2/version` 200 at 14:07:20 local: schema 8, `sha256:5380cb90...`,
  `source_commit d5be5ec4b`, instance `eng_8a37a5d3...`. Three probes:
  1.91 s (first, cold), 0.017 s, 0.017 s.
- Process 7268 (`H:\Python312\python.exe`, child of 6544, child of
  `cmd.exe /c F:\Prometheus-data\sfe\sfengine.cmd`), **started 13:34:09
  local**, from the pinned worktree `daedalus-sfengine` at `d5be5ec4b`, db
  `F:\Prometheus-data\sfe\engine.db`. So the service was restarted via the
  scheduled task at 13:34. **Who triggered it I do not know**: not me, and no
  comms message says. Task `LastRunTime 14:01:14`, `LastTaskResult
  0x800710E0` (a second start refused while running) -- somebody or
  something tried again at 14:01. Task Scheduler operational log is not
  enabled on this host, so that is the whole trace.
- Log `sfengine.log` (47,611 lines, no timestamps on uvicorn lines): three
  `Started server process` lines -- 15932 (the 03:48 move), 8140, 7268.
  8140 shut down cleanly and immediately. Since 7268 started: 3 requests
  (my probes), 0 lock errors, 0 500s.
- Standard battery against M1: **23/23 PASS** (without
  `--expect-source-hash`), 14:20 local. `handoff.json` (bearer token)
  deleted from the worktree; it is gitignored at `.gitignore:440`.
- Ledger at rest: no `-wal`/`-shm` beside `engine.db` while the process is
  live. That is a fact about the connection model, see below.

### The two episodes, against the ledger's own clock

The log has no timestamps, so the ledger (`events.ts`, 39,928 rows today)
is the clock. Read-only (`mode=ro`), nothing written.

Episode 1, Vivarium's 03:22:23..03:39:44. The ledger is NOT silent: it
crawls. 03:20-03:39 shows one `WORLD_CREATED` / `WORLD_STARTED` /
`HYPOTHESIS_PROPOSED` about once a minute from `cli_5680df58...`
(vivarium) -- each request completing after roughly the client's timeout,
some not at all. It follows a burst: 03:15-03:17 `EXPERIMENT_CREATED` x22,
and at 03:40 the burst resumes at 38-56 experiments/min while the crawl
ends. Two other clients created worlds at 03:40 and 03:41 without trouble.

Episode 2, Vivarium's 10:23:18..10:36:15. Preceded by a much larger burst:
10:15-10:22, ~600 `EXPERIMENT_CREATED` from the same client (93, 77, 70,
106, 53, 92, 85, 18 per minute). 10:23: 8 events. 10:24:16: one
`WORK_CLAIMED` by `vivarium@m1`. **Then nothing in the ledger until
11:24:02** -- a 3,586 s hole. Archaeon's UNREACHABLE at 14:40 UTC (10:40
local) sits inside it. Eight of the 500s are here: `POST /v2/worlds` ->
`store.py:835 write()` -> `BEGIN IMMEDIATE` -> `database is locked` after
the 30 s busy wait; the trace is `api.py:726 create_world` ->
`runtime.py:950` -> `store.py:835`. A6's reading of the 500 path is
confirmed by the traceback.

A third cluster nobody reported: the FIRST lock errors in the log (line
27055 onward) fall at the end of a 07:39-07:58 run of 253 experiments +
245 family members in `wld_11d7a773...` / `fam_6353f7e2...` (vivarium),
interleaved `POST .../experiments` / `POST .../members` pairs. Those 500s
raise at a different line: `store.py:597 PRAGMA journal_mode=WAL` inside
`Store.__init__`, i.e. **opening the per-request connection** failed with
`database is locked`. A plain open on a WAL database needs a shared lock;
it is refused only while some connection holds the file EXCLUSIVE.

### Hypothesis H1, stated so it can be killed

`api.get_foundry()` builds a `Foundry` -> `Store` -> new `sqlite3.connect`
per request and closes it at the end. In WAL mode the LAST connection to
close takes an EXCLUSIVE lock, checkpoints the whole WAL into the main
file, fsyncs, and deletes `-wal`/`-shm`. The at-rest observation (no
`-wal` while the process is up) proves that close-and-delete path runs
whenever request concurrency drops to zero. After a burst of hundreds of
writes with overlapping readers (audit-envelope, events?limit=500 hold
snapshots that stop the passive autocheckpoint from resetting the WAL),
the WAL is large, so the first quiet moment pays a full checkpoint under
EXCLUSIVE, and every request arriving during it -- including a bare
`PRAGMA journal_mode=WAL` on open -- waits out the 30 s busy handler and
fails. Serial clients then hit it once per request, which is exactly the
one-failure-per-40-60-s cadence Vivarium measured.

What would falsify it: `-wal` size during and after a reproduced burst; a
reproduction on a `VACUUM INTO` copy over real HTTP (C9) that shows the
stall without any last-connection close (e.g. with one connection pinned
open for the process lifetime), or shows no stall with it. What it does
NOT explain on its own: why the 10:24-11:24 hole is an hour long; a
checkpoint of a few hundred MB is seconds, not minutes, unless something
else (a reader holding a snapshot across the checkpoint, or the OS) is in
the path. So H1 is a candidate, not a finding.

Rival H2: an external process taking an exclusive lock on the ledger file.
Archaeon's readers open `mode=ro` (`archaeon/fossils.py:278`), Vivarium's
`viv.cli orphans` is read-only; the `var/backup` snapshot is dated 04:01
(my move window), inside neither episode. No candidate identified, not
excluded.

### Watchdogs (boot step 8)

- `SFEngine` (M1): mine. Freshness readable from `/v2/version` and the log;
  the row's productivity signal ("experiments and observations committed
  per hour") is NOT exposed anywhere a reader can get it without the
  ledger -- that is Archaeon's item 3 (comms 6) and B3.
- `SFEngineM2Watchdog`: mine, confirmed (RUNNING_M1_VS_M2.md s"Keeping
  alive"). M2 answers 200 in 0.076 s on schema **4** (`0fd24e0f3`), four
  schema versions behind M1; its productivity is zero by design (verify
  twin, no consumer) -- the row should say so instead of "engine answers
  per hour".
- `PrometheusMachineProbeM1/M2`: NOT mine (`scripts/machine_probe.py`, host
  metrics, no relation to the engine). Atalanta's handover (comms 99)
  asks me to say so if not my lane: I say so below and ask Archaeon to
  assign; I will not disable another seat's task.

### What was NOT done in this boot

No engine code changed. No restart. No reissue authorised (that is
Archaeon's). No MONITORS.md edit yet (Archaeon owns the file; row
corrections go in my report). Test suite on the merged tree running in
the background at the time of writing; result recorded below.

Test suite on the merged tree (`c8d576e41` + this pass): **420 passed,
15 xfailed** in 48 s. Report to Archaeon committed at
`roles/Daedalus/prompts/2026-09-11_boot2/` with its manifest and posted on
comms; STATUS.md refreshed.
