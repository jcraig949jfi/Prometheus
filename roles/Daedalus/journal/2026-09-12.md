# Daedalus journal -- 2026-09-12

Currency: 2026-09-12. Worktree `F:\Prometheus-worktrees\daedalus-d23`,
branch `daedalus/d23-workspace`, merged forward from origin/main at boot of
this pass (`97d5f8e3c` + later merges; each commit's carrier SHA below).

## The bite: C9 -> operational remedy, and the read-path handoff

1. **Archaeon handoff** -- comms 209, body committed at
   `roles/Daedalus/prompts/2026-09-12_archaeon_blind_tick/` with manifest:
   45 tick records since 09-11 07:12Z all `rows: 0`, `sfe db not found`,
   corpus hash = sha256(""); 4 WROTE_RANDOM rows. Repair named
   (ARCHAEON_SFE_DB / config.local.json now; B1 durably). Nothing of
   Archaeon's touched.
2. **B1** -- NOT issued: `read_grants` has no row for `cli_1029e9...` at
   11:52. Prerequisites verified on the deployed code: principal exists
   (Archaeon holds its token, #185); `_may_cross` (runtime.py:3065-3076)
   never consults read scopes, so a grant confers reading only; the only
   POSTs under /v2/read are owner-side scope management; membership will
   be `--name-prefix viv-`. Remaining blocker: Vivarium's seat has not
   synced since 09-11 18:27 (comms.agents); the consumer process is not
   the seat. I cannot issue it: I hold no owner token and must not.
3. **Storage move** -- rehearsal 11:52 (`deploy/LEDGER_MOVE_2026-09-12/
   rehearsal.json`, all checks PASS); window announced (comms 210) and
   taken right after the 11:57 Archaeon tick; apply 11:58:16-11:59:36,
   outage **80.2 s** (announced ~60; the 640 MB copy off the HDD took the
   difference). Receipt `apply.json`: engine.db / m1.key / m1.crt
   byte-identical; blobs 2,141 files; backup 2 snapshots; events 129,401
   at stop == destination; task action -> `D:\Prometheus-data\sfe\
   sfengine.cmd`; process reads `--db D:\...`; instance/hash/schema
   unchanged. One FAIL in the receipt is the tool's own read creating
   `-shm` after the stop (fixed in the tool: check before snapshot). The
   F: copy is untouched.
4. **Acceptance** -- same C9 shape on a copy on D: (`run3_D_asdeployed_
   after_move.json`): burst 20.5 s, producer p95 0.032 s max 0.125 s,
   reader p95 0.048 s max 0.053 s, 0 calls > 5 s, WAL max 247 KB, 0
   errors; live `/v2/version` 0.019-0.037 s and `/v2/openapi.json`
   0.019-0.021 s during the burst; D: avg write 6 ms. Before, on F::
   633.8 s, 2.674 / 15.154 s, 3.174 / 14.977 s, 53 calls > 5 s, live
   6.1 s. **STORAGE_REMEDY_CONFIRMED** on the operator's criterion;
   `ACCEPTANCE.md` in the move dir; the criterion is now a test
   (`test_sfe_c9_evidence.py::test_acceptance_after_move...`).
5. Runbook facts corrected (paths, log, interpreter chain); MONITORS
   SFEngine row names the volume; `orphaned_commits.py` default -> D:.

Live things touched: the SFEngine scheduled task action; the service
(stopped 80 s, restarted); `D:\Prometheus-data\sfe` created; a C9 copy at
`D:\Prometheus-data\c9`. NOT touched: the F: copies, Archaeon, Vivarium,
the deployed code, the build.

## The held bundle: A6 + B3 + C7 built as a CANDIDATE, deploy held on the contract

- **A6 implemented** (`sfe/attestation.py`, wired in the api middleware,
  `--incidents-dir`, `X-SFE-Request-Id`). Vocabulary kept as designed
  (CONFIRMED_EFFECT / CONFIRMED_NO_EFFECT / UNKNOWN_RECONCILABLE /
  UNKNOWN_UNRECONCILABLE) with the operator's rule made structural: the
  request path writes only intent / effected (after COMMIT returned) /
  refused; an intent with no outcome is UNKNOWN and never "failed";
  CONFIRMED from UNKNOWN only via `attest(rid, ledger_has_effect=...)` from
  a separate reader; a refusal the ledger contradicts is reported as a
  contradiction, ledger wins. Fails open. The 15 strict xfails of 09-11
  pass unmarked; the "no sink" live-evidence test flipped to "exactly one
  sink". New: `tests/test_sfe_a6_wiring_and_health.py` (8, incl. a real
  lock-timeout through HTTP: 500, refused line, ledger untouched).
- **B3** `/v2/health`: only measured quantities; `write_lock` = timed
  `BEGIN IMMEDIATE` in `store.write()` (that IS the acquisition wait);
  exempted from session scoping like `/v2/version`.
- One test-run accident: the fail-open test's POSIX path created
  `F:\nonexistent\...` at the drive root (my test, 12:09); removed; the
  journal now creates ONE directory level only, so a bad path degrades
  instead of building a tree.
- Candidate `8c53d04e6`, hash `sha256:726275da...`, pinned in
  `deploy/CANDIDATE_BUILD.json`; 469 passed, 0 xfailed. Harmonia's gate
  against a scratch engine of it: 0 removed, 1 added (`GET /v2/health`),
  scoping all match -> state 3 INCOMPLETE on the production ledger. **Deploy
  HELD** until the contract is regenerated (delegation to Harmonia, comms
  215) -- the schema-8 runbook's mandatory step; restarting into state 3
  would trip both wired consumers' gates.
- Archaeon restored its tick's eye via config.local.json (212) -- pointed
  at the F: path, which is now the frozen rollback; told them the one value
  to change (216). B1 still not issued at 12:26 (Vivarium synced 11:52;
  `read_grants` empty for the grantee).

## S1: attributable loop -- ancestry COMPLETE, B1 issued and verified, A6 witness built

- **C, ancestry: ANCESTRY_COMPLETE, no schema change.** `deploy/
  fossil_ancestry.py` performs the deterministic join returned fossil ->
  SFE (event entry_hash equality, spec.pew.encounter_id equality) -> queue
  row (PEW producer.queue.experiment_id; sfe_experiment_id and spec_hash
  equality) -> decision (source_evidence: policy, chosen_region,
  preregistration commit, corpus table) -> committed table row (git blob at
  the prereg commit) -> source fossil (obs/exp/pew ids; PEW world ==
  chosen_region; SFE observation in that world) and re-applies the
  preregistered rule. Canary: COMPLETE; a WROTE_RANDOM row: UNIFORM.
  8 tests incl. five cheat controls and a names/timestamps-never-consulted
  control. Finding with residuals: `deploy/ANCESTRY_2026-09-12/FINDING.md`.
  Encounter ids are not unique across queue rows (786 / 544 distinct), so
  the join never keys on them alone.
- **B, B1: ISSUED by Vivarium** (gnt_1ecdeae69f800240e03221ed, scope
  scp_1be32ffbe7c9bf3b29ec8d85 "archaeon-campaigns", granted_by == owner
  cli_5680df58, grantee cli_1029e9 "archaeon", not revoked). Engine-side
  verification `deploy/verify_read_grant.py` -> `deploy/B1_2026-09-12/
  verify_read_grant.json`: 592 worlds in scope, all viv- prefixed, none
  foreign; 24 owner viv- worlds not yet in scope (enumeration lag; the
  tool is idempotent, re-run extends); read-only by code: `_may_cross`,
  `claim_work`, `import_artifact` never read the scope tables; the only
  mutating routes under /v2/read are owner-side scope management. 2 tests.
- **A6 witness:** `deploy/reconcile_attestations.py` reads a dead
  process's journal files and the ledger's idempotency_keys; keyed intents
  settle to CONFIRMED_EFFECT / CONFIRMED_NO_EFFECT, unkeyed and ambiguous
  stay UNKNOWN_UNRECONCILABLE; verdicts go to reconciled.jsonl, the journal
  stays append-only. 2 tests. Limit stated: only keyed requests can be
  reconciled by this witness (14 idempotency keys on the whole ledger --
  consumers mostly do not send them; backlog A2).
- **A, held build:** unchanged; Harmonia offline since 09-11 15:25; the
  contract step is hers. Not bypassed.

## Next round (operator 13:35): maintenance bounded, then park

- Phase 1 held build: Harmonia still offline (comms.agents 09-11 15:25);
  delegation 215 stands; no deploy, no bypass.
- Phase 2 B1 recurrence: no owner-side recurrence implemented yet
  (Vivarium re-runs by hand; 221). Engine-side invariants verified without
  new code: membership is an enumerated table (`read_scope_worlds`, 592
  rows, `added_ts`), no rule; `add_scope_worlds` refuses non-owner worlds
  into `not_yours` (runtime.py:4165-4173) and is INSERT OR IGNORE
  (idempotent); grant idempotent (runtime.py grant_read); read grants are
  consulted only by /v2/read/* GETs (verify_read_grant.py code facts). Lag
  now 25 owner viv- worlds not in scope (617 total vs 592).
- Phase 3 A6 census: `deploy/A6_COVERAGE_2026-09-12/CENSUS.md`. 9 of 34
  mutating routes accept a key; 14 keys lifetime (0 Vivarium; Vivarium's
  only keys are 60 budget reservations, stored elsewhere); keyless A6 still
  covers refusals and slow-engine timeouts by code order; the uncovered
  shape is a process death mid-request; smallest follow-up = Vivarium keys
  world+experiment creation with its queue UUID. Not applied.
- Phase 4 S2: Archaeon (220) states no requirement; S1 rows carry created_seq
  window bounds for corpus re-derivation. No change.
