# Daedalus journal -- 2026-09-17 (instance m2-d6ecd70b)

## Overnight, by others: Campaigns 1-3 ran on THIS engine

Archaeon m2-411504ab ran C1 (closed 09-16 21:04, #323), C2 (closed 09-17
00:21, #324) and opened C3 (running) against eng_906356f7 at
192.168.1.191:8811 -- i.e. the operator resolved the #301/#314 ledger
conflict by using the M2 ledger. #315's hold stays until C3 closes; then
one post supersedes both. Engine errors across all three campaigns on
attempts of record: 0. Engine-side asks: #325 GET /v2/worlds/{wid}/
artifacts (405 today).

## Operator: point-release directive, then Amendment 1

Operator in chat: Campaign 3 running, change nothing; review the directive.
My comment (chat): the SFE prompt has ten sections, the evidence supports
four; engine was not the failure surface (measured); world objects as
engine services rejected on charter rule 1; QD route deferred; long-run
pagination/durability is where the real gaps are. Amendment 1 accepted the
narrowing (s5), the world-objects rejection (5F), the QD deferral (5G),
and imposed a migration freeze while C3 runs (s11). Verbatim copies +
MANIFEST: roles/Mnemosyne/prompts/2026-09-17_point_release/.

## Stage 0-2 written (read-only)

docs/point_release_2026-09/SFE_POINT_RELEASE_REVIEW.md. Evidence traced
to L-006, L-026, L-009, L-001, L-012, L-013, L2-021, L3-015, #325, the C3
readiness table, the A6 census and the deployed tree dd10c9074 (surveyed,
not recalled). Findings that shrank the delta: parent_world_id/fork_point
and checkpoint head_hash/state_hash ALREADY EXIST; list_observations and
list_experiments are UNBOUNDED; events is newest-100 only; the engine
contains zero occurrences of shelf/summit/corridor (verified by grep).
Delta: D1 logical_time, D2 typed termination, D3 generic sealed world
events, D4 manifest envelope, D5 artifacts list, D8 cursor pagination, D9
read semantics + capabilities, D10 long-run measurement, D11
restart/duplicate/checkpoint fixture (MUST); D6 fork diff, D7 labels
(SHOULD IF CHEAP); D12/D14/D16 DEFER; D13/D15 REJECT. Schema 8->9,
nullable columns only; +4 routes; one window after C3.

Not touched: the engine, its data, its task, the contract. Nothing
deployed.

## 9.0 and 9.0.1 shipped; the day in one paragraph (written 19:35Z)

Order received: implement + deploy. Stage 3 closed, SFE_INTERFACE_DELTA.md,
nine MUST items coded, schema 9 migrated on production in one window
(SFE_SCHEMA9_MIGRATION_RECEIPT.md), 511 tests. The long-run measurement
(SFE_LONG_RUN_REPORT.md) then found the engine paying for SQLite WAL
maintenance inside the request path: 5-13 s stalls, a 60 s POST on
production (Vivarium #366), 500s under two writers. Eight iterations, every
rejected one preserved under deploy/LONG_RUN_2026-09-17/; three real
mechanisms (per-request connections + autocheckpoint in COMMIT;
writer-blocking WAL resets; the A6 journal open/write/close per line in
the response path, found only by py-spy on the serving pid). 9.0.1
deployed 17:01:12Z (13.3 s outage, 13/13 identities, contract regenerated,
gate 0, qualification 18/18 engine checks). Review packets:
REVIEW_PACKET_SFE_POINT_RELEASE_2026-09-17.md and
REVIEW_PACKET_SFE_901_REPAIR_2026-09-17.md. Operator paused me at a clean
point.

## Archaeon's C4 launch gate (19:11Z) -- my items

Gate RED, correctly: my campaign-rate acceptance regime ran 331.8 s and
the defect it replaces first appeared ~840 s in. Launched the run that
answers it (30x1000 gens at 0.5 s/gen + paced reader, scratch engine on
port 9041, ~4.5 h, ends with the restart fixture). First minutes: 0 5xx,
WAL 8.9 MB plateau. P1 was never engine-gated (registration OPEN, cmp1-3
self-registered on this ledger today) -- minted cmp4-archaeon
cli_6354da5b5955a711dcc0b2af and placed the credential in Archaeon's
worktree (gitignored). P2 is Vivarium -> that id (Vivarium #372 corrected
the direction; the M2 ledger has one scope, zero grants). Posted #374.

While waiting: the two 8->9-only qualify shapes now N/A on a code-only
release; a day-rollover test for the persistent A6 handle found that the
COLD scan stopped at the first day file mentioning a rid, so an intent
before midnight + outcome after read "no intent record" -- fixed (scan
until both halves found). 512 tests. This moves HEAD's engine source hash
to f6a77c86; production stays 699ca0f9 -- not deploying into the C4 freeze
for a cold-scan reporting edge case. Backup runbook section added
(SQLite backup API, never a file copy). Landed on main 070672546.

## The G1 run failed -- and it was the disk (written 23:35Z)

Operator's closure order: own engine readiness, no commentary. The
4.5 h campaign-rate run was clean for 3 h 27 min (0 5xx, WAL pinned at
8.4 MB) and then the checkpointer's PASSIVE tick went 5 ms -> 26 s ->
never; a READ stalled 33 s; one 500 'database is locked' after a 33.3 s
wait. Ten minutes after the load stopped, with the engine dead, a 4 KB
write+fsync on D: took median 7 s / max 59 s; on C: under 1 ms. D: is a
Seagate ST8000DM004 -- an SMR drive -- and its own counters say
WriteLatencyMax 65,345 ms. Same class as C9 on M1 (2026-09-11), same
remedy: placement. Moved the production data dir to C:\Prometheus-data\sfe
with deploy/move_ledger_to_nvme_m2.py (rehearsal 11/11, apply MOVED,
13.0 s outage, identity unchanged, D: copy untouched as rollback). G1
relaunched on the NVMe from the PINNED worktree so the proof is against
699ca0f9, not HEAD. First 7 minutes on the NVMe: WAL 1.3 MB, checkpointer
in its 2 s cadence (on D: it sat in 50 ms fast mode for the whole run
because the WAL FILE never shrinks below the soft limit -- an amplifier to
fix in the next point release, not now).

C4-REH-1: Archaeon names the moment, I execute deploy/rehearsal_restart_m2.py
(refuses without live work; watchdog recovery). G3: the engine has no
operator grant path -- Vivarium issued gnt_2006a1ac to cli_6354da5b at
23:15:13Z; verified on the ledger; Archaeon's S3 read closes it.
