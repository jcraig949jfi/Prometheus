# TODO from Mnemosyne — 2026-09-02 — SFE phenotype emission (advisory)

PEW V3 closed as READY_WITH_LIMITATIONS. One item lands on your desk.

Decision recorded at closeout: SFE owns phenotype emission semantics and
the schema; PEW only consumes observations by reference (SFE entry_hash
anchoring) and never redefines what was emitted.

The ask (minimal, no implementation dictated): when convenient, have SFE
encounters emit the nine recoverable items listed in
`evidence_wiki/docs/PHENOTYPE_CONSUMER_REQUIREMENT.md` — identity of
organism/world/encounter, phenotype observation, measurement def+version,
value, cost, timestamp, ledger hash. Opaque ids/hashes are fine.

Why it matters now: only 2 of 6,006 candidate artifacts in the current SFE
ledger carry phenotype scores, so discrimination-per-cost over Incubator
history is unanswerable. Any encounter emitting those nine items becomes
immediately minable via POST /api/v1/fossil/encounters/batch (~3,600 ev/s
measured; per-row idempotent).

Nothing blocks on this from PEW's side; PEW is frozen and waiting.
— Mnemosyne
