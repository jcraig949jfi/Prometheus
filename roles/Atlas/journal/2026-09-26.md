# Atlas journal -- 2026-09-26

## Atlas[m1-a5680f90] -- bounded repairs authorized by the operator

Directive: two narrowly bounded recorder/index repairs only. It does NOT
restart the Atlas loop, Atlas-M2, roadmap generation, autonomous
prioritization or seat-directed prompting. When done: test, verify, report,
commit, push, park.

### ATLAS-37 -- Cosmos adapter: DONE (atlas/harvest/cosmos.py, cosmos/1)

- The MANIFEST gate runs BEFORE ingest. It checks the sha256 and row count of
  all 20 exported files (fact + edge jsonl for each of 10 stores) against the
  git blobs at origin/main. All 20 matched. One mismatch refuses the whole
  pass. Tests cover a tampered file (the cheat control), a row-count
  mismatch, a missing file, an empty manifest, and the committed export.
- A working-tree checkout does NOT match the MANIFEST, because autocrlf
  rewrites the line endings. The adapter and its test therefore read the git
  blobs.
- Model: one experiment and one attempt per store. The attempt's host is M2,
  DECLARED by Cosmos in #562. Campaigns cosmos/c0, c1 and c2 come from the
  store-name prefix, which is INFERRED and labelled so. The runtime ledgers
  (cwe.sqlite, receipts.jsonl) are EXPECTED:M2 pointers carrying the
  MANIFEST sha256. They are recorded, not checked from M1.
- Facts are kept PER STORE. Cosmos's fact_key is world-level and recurs across
  stores (1,630 keys recur, 92 of them with a different value), so collapsing
  on it would have lost real repeat measurements. The native method (it
  carries purpose=) is kept in value_json.
- Edges: 4,327 exported rows reduce to 3,096 distinct (src, dst, relation)
  triples. Migration 012 adds Cosmos's own words (phenomenon_verdict,
  candidate_law, CONTROL_OF, COORD_PRESERVING) rather than mapping them onto
  near-synonyms.
- A second pass left every count unchanged (idempotent). The per-store fact
  counts equal the MANIFEST exactly.
- Known limit, which Cosmos disclosed: a replicate-confirmed counterexample and
  an unconfirmed attack row look alike here. The confirmation stays in each
  store's adversary.json on M2. No re-export was requested (operator ruling:
  keep inferring, no interface requests).

### frontier/4 -- HELD

Archaeon has not answered the operator's producer-semantics question about
repeated BLOCKED_BY_SUPPRESSION events. There is nothing in comms and nothing
on main as of 2026-09-26 ~14:00Z. No aggregation code was written, and
frontier/3's 299,991 facts stand. Archaeon's registry is untouched.

### Before / after (atlas schema, M1)

                              before     after
  facts                      324,178   333,044   (+8,866 = 8,856 Cosmos + 10 derived)
    of which suppression echo 299,991   299,991   (unchanged; frontier/4 held)
  experiments                  2,043     2,053
  attempts                     1,732     1,742
  campaigns                       50        53
  edges                        2,398     5,494
  sources                    312,974   313,015
  engines with experiments         4         5   (cosmos)

Tests: 66 passed (atlas/tests + the base-role self-test).
Report: roles/Atlas/reports/REPORT_2026-09-26.txt. No roadmap, policy score
or portfolio was run, per the directive.

Remaining coverage gaps: Nestor C9, Ananke, Aether and Harmonia have no
adapter. The M2 runtime ledgers are visible only as EXPECTED:M2. Frontier
fact counts stay inflated until Archaeon answers.

Parked again: no loop, no watcher.
