# Atlas journal -- 2026-09-24

Instance: Atlas[m1-1c645957] (M1). Worktree atlas-base-role, branch
atlas/promotion-2026-09-24, base 0e89f7104.

## What the operator asked

Promotion: stop treating Atlas as primarily an index and start treating it as a
continuously learning research-policy layer -- three coupled jobs (an evolving
ontology of intelligence as a theory graph, a primitive inventory with
combination coverage, a portfolio allocator that emits directives), plus
learned prioritisation refitted against outcomes, a soup generator, a four-tier
cadence, and licence to challenge Prometheus's own shared assumptions. Verbatim
with MANIFEST in prompts/2026-09-24_promotion/.

## What I built

1. Migration 010: proposition, proposition_evidence, primitive, primitive_use,
   combination, experiment_score, policy_version, portfolio_update, blind_spot,
   vocab domains, and the view v_theory_frontier.
2. Three committed ledgers under roles/Atlas/theory/ so the reasoning is
   reviewable in git, not only in Postgres: 10 propositions (each with scope,
   confidence basis, confounds, implicated primitives and untested
   predictions), 20 primitives with axis rules, 7 blind spots.
3. harvest/theory.py (theory/2): loads the ledgers, then derives 4,099
   primitive_use rows by rule from axes the index already holds, and 190 pair
   combinations -- 89 TESTED, 85 UNEXPLORED, 16 SUGGESTED_BY_EVIDENCE.
4. atlas/policy.py: scoring (9 fields, weights in atlas.policy_version) and
   portfolio() per horizon. 46 proposals scored; 3 ISSUED updates, one per
   horizon.
5. `python -m atlas roadmap` -> reports/ROADMAP_2026-09-24.txt: theory graph,
   primitive coverage, combination coverage, blind spots, scores, portfolio,
   and the index coverage lag.
6. Charter rewritten (RESPONSIBILITIES.md), MODEL.md section 9, STATUS.md,
   seven backlog items (ATLAS-37..43), MONITORS cadence note.

## Defects I found in my own work today

- policy/1 did not discriminate: novelty was 0.000 for ALL 46 proposals,
  because it measured novelty against the CATALOGUED EXTERNAL corpus where
  almost every primitive pair is already tested. theory_impact also saturated
  at 1.0 for anything touching 4+ propositions. policy/2 measures novelty
  against PROMETHEUS coverage and weights each proposition by how unsettled it
  is. Both versions are kept; the /2 rationale names the /1 defect, and a test
  now fails if scores collapse.
- portfolio() emitted IDENTICAL output for all three horizons -- the horizons
  were decoration. Rewritten with real windows (10 / 100 / whole corpus) and
  horizon-specific directive builders; a cheat control now fails if two
  horizons issue the same directives or use the same window size.
- MICRO went quiet and I nearly reported that as quiet engines. It is not:
  the newest MODELLED experiment activity is 2026-09-19 08:55 while the newest
  indexed commit is 2026-09-24 07:09 -- a 4.9 day ADAPTER COVERAGE lag. Every
  horizon now emits a coverage-lag NOTE, and a control fails if the lag is
  over a day and MICRO does not say so.
- A test I wrote asserted that a primitive with no axis rule declares itself
  UNMEASURED in measured_by -- and it failed, correctly: measured_by describes
  how the primitive WOULD be measured, so five rule-less primitives read as
  merely untested. That is exactly the conflation the charter forbids.
  Migration 011 adds primitive.detection_status; the five now carry
  "UNMEASURED -- <reason>. Needs ATLAS-39." The distinction is now in the
  table, in the ledger and in the report, not in prose.

## Not mine, found while testing

archaeon/tests/test_base_role.py::test_issued_manifests_verify_against_their_files
fails on roles/Nyx/prompts/2026-09-19_asal_pipeline_direction:
PLAN_behavioral_cuts_after_replication.md manifest dc41abff1393 != artifact
dbd639ef6ee7. Pre-existing on origin/main (the file was edited in d8741f71a
after the manifest was issued in da2d01a8b); nothing of mine touches roles/Nyx.
I will report it to Nyx in comms rather than editing another seat's prompt.
All 10 other base-role checks pass; atlas/tests 49 passed.

## What I did NOT do

- Nothing was run, scheduled or commanded on any engine. No compute allocated.
- No proposal was promoted, no proposition was turned into a verdict, and no
  seat was told to do anything: the three portfolio updates are suggestions
  with their evidence attached, routed by name.
- The weights are a hypothesis. Until scored proposals actually run and
  theory_delta comes back (ATLAS-40), the ranking is untested.
- The loop stays PARKED. Today's work was operator-instructed, not a tick.

## Comms

- #557 to Nyx: the ASAL prompt manifest mismatch (pre-existing, not mine);
  reported rather than edited, since it is another seat's prompt directory.
- #558 to Cosmos, ack of #544: engine 'cosmos' registered with home_host null
  (I will not guess a host), adapter filed as ATLAS-37 with the ingest rules
  stated up front, and two questions -- which host executed C0, and whether
  the export distinguishes an INVALID attempt from a real negative.
- Four calibration rows added (policy/1 non-discrimination, identical
  horizons, quiet-window misreading, untested-vs-unmeasurable conflation).

Pushed f8df65681 to main; verified it is an ancestor of origin/main.
