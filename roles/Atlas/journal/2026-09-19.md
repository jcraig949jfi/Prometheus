# Atlas journal 2026-09-19 -- charter, addendum, schema, first populated pass

Instance Atlas[m1-1c645957] on M1 (SKULLPORT). Worktree atlas-base-role,
branch atlas/charter-2026-09-19 from origin/main 74b09076d.

## What happened

1. Charter received in chat; committed verbatim alone (4fb8c7fc2)
   before any survey. Four read-only survey agents mapped SFE, NPE, PEW +
   M1 Postgres, and Archaeon/Harmonia/other runners; findings committed
   as roles/Atlas/SOURCES.md.
2. Migration 001 applied (first model); reference + commits harvested.
3. Charter ADDENDUM arrived mid-build; committed verbatim alone
   (cbe1d149d). The model was reworked by migration 002 (not by editing
   001): three layers (attempt/segment = ran, fact OBSERVED, conclusion),
   typed edge table, host + engine_instance, first-class source +
   source_link, idea, defect, identity_collision, field_conflict, signal;
   003 vocab; 004 vocab (EXECUTION_OF); 005 identity correction (r16).
4. Collectors written and run: commits, archaeon_campaigns, frontier,
   npe (local branch), vivarium, pew, local_files; comb rules R01-R13;
   report. Full `harvest all` rerun was idempotent.
5. Non-interference checks made before touching machine-local data: SFE
   ledgers on M1 last written 2026-09-15 (D:) and 2026-09-11 (F:), nothing
   listening on :8811; opened only mode=ro&immutable=1. A LIVE service
   was found: F:/SerendipityD/var/deploy/foundry_service.py (pids 7988,
   8176) -- registered as engine serendipity.foundry.d, stat-only, no
   hashing, no ledger reads.
6. Controls: atlas/tests/test_atlas.py 16 passed (positive, negative,
   cheat; writes rolled back).
7. Six of my own errors caught and corrected in versioned passes; each
   is a row in calibration/LEDGER.md.

## Numbers (roles/Atlas/reports/REPORT_2026-09-19.txt)

11 engines registered (4 with experiments), 48 campaigns, 1,897
experiments, 1,592 attempts, 3,225 segments, 53 ideas, 11,994 source
pointers, 18,187 facts (5,859 RAN / 10,991 OBSERVED / 1,337 CONCLUDED),
402 conclusions, 1,055 edges (834 lineage), 217 defects, 30 collisions,
3 dangling edge endpoints, 1,400 machine-local sources, 6,069 EXPECTED
(M2) pointers, 47 commits only on M1-local branches, 182 open signals.
Review packet: roles/Atlas/reports/PACKET_2026-09-19.txt.

## What was NOT done

- No M2 evidence (prompt for the M2 instance committed).
- Harmonia rulers, Bellerophon, Herakles, Proteus, Ludus, archaeon/wse git
  ledgers, NPE QD cells, pm-data epoch mapping: registered, not harvested.
- No loop scheduled; nothing registered in MONITORS.md (ATLAS-21 first).
- No message sent to any operating seat; no interface change requested
  (ATLAS-26 is the operator's decision).
- Observed but not Atlas's lane: evidence_wiki/config.json carries a
  cleartext DB password (already noted in that file's own correction
  note; ATLAS-23 checks it is tracked).

## Integration (annotation)

Rebased onto origin/main before push; the three Atlas commits became
4fb8c7fc2 (charter), cbe1d149d (addendum), 61c3985fc (index v1), pushed
and verified ancestors of origin/main. The v1 commit message cites the
pre-rebase SHAs 4e063959f / 0eb0600c0, which are orphaned; this file and
BACKLOG_H0H5.md now cite the ancestors.

## Later 2026-09-19 (m1-1c645957): sibling instance, SerendipityD ruling

- Operator: an M2 Atlas instance is booting as its own instance ("a
  brother ... not to assume your role"); comms reported working from its
  side. At 10:13 UTC `comms who` listed only m1-1c645957 under Atlas and my
  inbox was empty -- recorded, not chased.
- roles/Atlas/INSTANCES.md written: shared keys, host-scoped roots,
  announced shared-code changes, tagged migrations and journals.
  Message to the sibling committed under prompts/2026-09-19_to_m2_sibling/
  and posted to Atlas in comms.
- Operator ruling: F:/SerendipityD "may have been repurposed ... Ignore it
  for now." Engine row serendipity.foundry.d -> IGNORED; its local root
  removed from atlas/registry.json; ATLAS-24 closed. The rows from the one
  stat pass stay as history.
- 10:17 UTC: the M2 instance is seat "Atlas-M2" (m2-8f915f3d), not an
  Atlas instance. #498 went to seat Atlas and did not reach it; a direct
  post to Atlas-M2 is refused ("unknown recipients ... roster is roles/*")
  because roles/Atlas-M2 is not on main yet. No message from Atlas-M2 has
  arrived. INSTANCES.md records the addressing.
- 10:18 UTC: #499 from Atlas-M2 (body on main 6b30592e6). It is a
  separate seat by operator directive; my INSTANCES.md was wrong. Replaced
  by SIBLINGS.md; calibration row added; reply posted to Atlas-M2.
- Operator: separate seats for git/messaging, ONE index on M1 Postgres,
  coordinated. Announced to Atlas-M2 first (#501), then: migration 006
  (harvest_run.seat, v_writers), ATLAS_SEAT env, advisory locks on
  migrate/flush/comb, registry harvester_hosts + CLI refusal. Tests: 20
  Atlas passed (incl. a cheat control proving a second session is refused
  the write lock); test_base_role still fails only on Nyx's pre-existing
  manifest drift (reported #497).
- Operator: loop on messages periodically, build the index with Atlas-M2,
  never interfere with the benches, not urgent. AtlasIndexLoop row filed
  in MONITORS.md (the base-role self-test rejected my first bound/seat
  format; fixed); tick procedure roles/Atlas/loop/TICK.md; loop launched.
- tick 1 (10:28 UTC): #502 from Atlas-M2 (ack; its loop, bound 16, me
  accountable) -> ack #503, rule-3 notice #504. No full harvest (<3 h).
  ATLAS-06 done: archaeon_campaigns/3 + classify.science_class; my first
  cut classed "WEAK POSITIVE ... failed residue" as FAILED (prose word);
  fixed before commit, test added. PRODUCTIVE.
- Operator: Atlas-M2 must POINT to M2 data as Atlas does on M1; logs are
  too voluminous to copy across machines and will grow substantially;
  "as long as we don't lose track of it"; the important pieces are
  centralised later by culling. Filed ATLAS-27 (loss tracking), -28
  (scale: incremental, no log hashing, granularity), -29 (log time spans
  -> attempt links), -30 (growth view); notice to Atlas-M2.
- tick 2 (11:31 UTC): #506 from Atlas-M2 (M2 roots landed: FS:M2 0->137,
  +2378 facts, 52 SFE attempts on eng_906356f7; asked what storage_root
  means) -> answered by rule in #507. Full M1 harvest (NPE branch moved):
  npe FAILED on a NaN inside a new CW01 result (jsonb rejects NaN) ->
  db._finite, npe/4 re-run 258 exp. ATLAS-27 done (local_files/4 loss
  tracking + storage_role); my first cut set eng_8a37's storage_root to
  its backup/ subfolder -- caught, fixed, migration 007 (claimed #507)
  reset the two M1 values. Tests 23 Atlas passed. PRODUCTIVE.
- ~11:40 UTC: operator -- "kick out of the loops ... I'll tell you to
  resume after some more research piles up." AtlasIndexLoop stopped after
  2 productive ticks; MONITORS row and STATUS set PARKED; Atlas-M2
  (accountable seat) told. Open backlog when resumed: ATLAS-28 (dense-log
  scale) first, then -29, -30, -05, -07.
