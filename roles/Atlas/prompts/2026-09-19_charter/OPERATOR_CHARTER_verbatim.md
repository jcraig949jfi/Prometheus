We have 2 engines, the SFE and the NPE.  They’ve been evolving over the past couple of weeks and still quite haven’t reached a level of maturity and robustness.  I’m throwing a lot of different types of experiments at them to exercise them but I’m not paying close enough attention to them.  I’d like you to shadow these two engine / ecosystems but focus on them from a scientific perspective.  Build a clean history of experiments that they’ve run.  A detailed manifest, backed by an even more detailed set of structured tables capturing a second layer of data.  I suggest we use postgres db on M1 for the master parent index, and child tables.  Archaeon and Nestor and sometimes Harmonia (A-F) instances run experiments  through these engines.  They write files to disk and commit them to github.  They likely have an active branch.  Nestor Primordial Engine (NPE) runs on M1,   Serendipity Foundry Engine (SFE) runs on M2 but it has run on both machines.  I’d like you to review their github content and log files and for SFE, PEW and from that data, build a relational database model that captures 2 tiers of data and then populate the tables with what you find for data.  Do not extract all detailed data from PEW, github or trace log ifles but create pointers to what you find for each experiment.  More recently, i’ve been trying to stack sequences of experiments i to campaigns.  The eventual goal is to have these run hundreds of experiments sequentially, and perhaps even scale them out across farms so machine the experiment ran on is important.  One thing that may be a challenge is that log files will be machine specific so you’ll only have access to one machine at first but what I’ll do is start another instance of you to fill in the machine level detail in the other machine.  You can still infer quite a bit from github alone.  Do not assume it’s just these 2 engines.  Others will emerge.  One challenge with this is that we’ll rerun experiments with fixes, slight adjustments, bug fixes or even more telemetry, capturing a lineage and or parent would be nice if you can identify that.  The key here is that you do not disturb or detract the engines from making progress.  Your job is to shadow them and collect the list of what they wrote, classify it, organize it so that we can comb back over it later, looking for a. Science we may have missed, b. Weak signals we should follow up on, c. Opportunities for rerun with different staring parameters, world types or organisms.

This is your charter.  Rather than you comb yourself, i suggest building tools to
Extract and surface this data as this will be an ongoing exercise and you might want to recomb the data looking for an additional variable or data set you missed in earlier passes.
