# Crius journal 2026-09-19 -- charter received; Campaign 0 built, tested, run

Instance m2-8d43bbf9 (continues from the 2026-09-18 second-boot entry).
Operator directive: the charter, verbatim in
prompts/2026-09-19_charter/CHARTER_CAMPAIGN_0.md (sha256 5c58c97d...).

## Sequence (commits on crius/base-role-adopt-2026-09-18, base 8bb1a28a0)

1. f8f2df6b0  charter committed verbatim with MANIFEST (verify: 1 entry, 0 mismatches).
2. de2ea2fb2  crius/DESIGN_C0.md: nine design challenges decided (C1-C9),
   frozen metric, controls A-J, partitions, predictions P1-P5. Committed
   BEFORE any code so the preregistration order is in git history.
3. 68cc85ff9  FREEZE: sandbox v0.1 (world, tasks, workspace, artifacts,
   vm, env, player, evaluate, baselines, receipts), configs/c0.json at
   config_hash 65fd4678cbcdd9c5, 11/11 s17 tests, baseline receipts
   crius/runs/baselines_c0. World fingerprint 57065ca240cee53d,
   partitions ece4bd004beb4710.
4. f4dae7a66  search/qualify/report; store-op cost units charged against
   the step budget in the VM (A2 in DESIGN_C0 s10); c0x.json EXPLORATORY
   variant (A3); baselines_c0x.
5. Campaign searches launched 12x: configs c0 and c0x, arms seeded and
   random, search seeds 1-3, 300 iterations, mu 8 lambda 24 (7200
   candidates per run), 8 workers each, 3 runs concurrent on M2.

## Numbers seen before the campaign searches (search suite, seed 101/102)

    player        effA   effF   succA  interA  interF  reuse_gain  blocks
    RANDOM        0.193  0.193    4   13311   13311        0.0      0
    HEURISTIC     2.844  2.844   47    1482    1482        0.0      0
    ENUMERATE     1.926  1.926   50    4139    4139        0.0      0
    ENUMERATE_VM  1.773  1.773   50    4424    4424        0.0      0
    CACHE_REUSE   5.784  1.859   50     114    4139     3987.9      0
    ADAPTIVE      5.078  1.839   50     114    4139     3906.2     27
  (seed 101 rows; seed 102 in crius/runs/baselines_c0/*.json)
  Positive control benefits (s17): CACHE_REUSE interactions 4139 -> 114;
  SCRAMBLED collapses it to 2.08; FULL transplant reproduces ACCUMULATED
  exactly; ARTIFACT transplant ~ CODE_ONLY; COMPUTE/STORAGE matched =
  CODE_ONLY. ADAPTIVE's blocks are net-negative at depth <= 3 (ablating
  all: remainder cost 10.0 -> 7.8).

## Smoke search (3 iterations, deleted, not a campaign run)

  Seeded arm best 2.50 vs seed 1.62: a crippled enumerator solving only
  depth-1 tasks (20/50), halting on the rest. On heldout_v1 it scored
  eff 3.00 vs CACHE_REUSE 2.97. The frozen ratio metric rewards
  abstention. Led to c0x.json (post hoc, EXPLORATORY).

## What was NOT done

- No tuning of world/metric/partitions in c0 after any search result.
- Python baselines' store units are not budget-charged (approximate,
  declared). CRIUS-20.
- No LLM in any loop; no comms calls from the sandbox; no integration.

## Campaign searches: results (all receipts under crius/runs/)

  12 runs x 300 iterations x (8+24) = 7208 candidates each; wall 49 s to
  ~15 min per run on M2 with 8 workers; qualification 7 candidates + 6
  baselines x 3 seeds per run. Table: crius/runs/CAMPAIGN_SUMMARY.md.
  c0 (preregistered): every run converges to a quitter; best solves
  3-21/50; seeded s1 4.34 = store-as-clock (32 empty blocks; ACC 2/50 vs
  FRESH 16/50 on held-out). c0x (EXPLORATORY): seeded bests are stride-2
  / mod-free enumeration orders, 50/50 on search seeds, 45-48/50 and
  BELOW the seed on held-out, 0 store use; random bests are hard-coded
  action scripts, 34-38/50 search -> 23-25/50 held-out. No searched
  candidate passes s13 criteria 1-7 on any seed. Full packet:
  roles/Crius/REVIEW_PACKET_C0_2026-09-19.md. Predictions scored there
  (P1 part lost, P2 form lost, P3 half, P4 held, P5 held); ledger rows
  added.
  Post-run instrument fix: report.py checklist guard 0 (competence kept,
  ACC successes >= FRESH) after c0 seeded s1 passed criterion 1 with
  collapsing competence; dated in report.py. Bulk receipts gzipped
  (339 MB -> 28 MB); readers accept .gz.

## Operator ruling: C0 CLOSED, C1 authorized under gate A-H

Ruling verbatim: prompts/2026-09-19_c1_ruling/ (MANIFEST; commit
ba02a9e7c). The hidden-scale world from the C0 packet was rejected as a
datum. Sequence of this pass:
  a9928654b  DESIGN_C1.md preregistration (RELAY world, competence-first
             fitness, sealed/rotating streams, FAIL sentinel + strict ACT,
             segment splice arm after checking Apollo's dispatch_merge
             record, gate A-H, predictions Q1-Q5) -- before any C1 code.
  97af44f88  FREEZE: C1 sandbox + controls + fixtures + 7 new tests (18
             pass) + gate A-H PASS on gate streams 301-303
             (crius/runs/gate_c1/GATE.md); config_hash 32dfb243be9fdec9;
             world 7c53db874324b532; generator 624728b00fdd3f2f.
Corrections recorded on the way (annotations, not rewrites):
  - C0 F1 mechanism: BLK_NEW ids grow monotonically (next_id never reuses
    freed slots) and the fossil's loop bound R2 = num_ops stops the
    enumeration once the id exceeds 5 -- abstention by a legitimate value
    channel, not a failure alias. The status-channel fix (ruling 5) stands
    on its own; the packet's F1 wording is superseded by
    tests/test_c1_integrity.py and DESIGN_C1 s10.
  - C0 F3 fossil (action script) depended on wrap-around ACT immediates;
    under strict ACT it collapses (38/50 -> 3/50). Recorded as its post-fix
    signature.
  - Gate D's first proxy ("cheap chain solves") counted enumeration luck;
    replaced by the explicit replay counter and TaskResult.success_in_block.
  - Positive control fixed three times while the gate failed on IT (live
    calibration map; witness rule for templates; depth-1 probe first):
    all dated in DESIGN_C1 s10. World/generator/metric untouched.
Gate numbers (PROCEDURE_REUSE_C1 on gate streams): ACC 50/49/50 vs FRESH
31/25/28; ARTIFACT_TRANSPLANT remainder 18/18/18 solved vs CODE_ONLY
3/5/5 at mean cost 20.8/28.5/~30 vs 957/924/884; ABLATION_ALL == CODE_ONLY;
blocks unchanged after transplant; template blocks invoked with >= 3
distinct arguments producing >= 3 distinct action sequences; TABLE_MEMO
replays 0, no transplant advantage; NOCAL solves 0 chains by invocation.
Searches launched: random/seeded/recombination x seeds 1-3, 300 its.

## C1 results (same day)

9 runs x 7208 candidates (random/seeded/recombination x seeds 1-3, 300
iterations, rotating streams). Qualification re-run with final-population
ranking (qualify.py fix: under rotation "best ever" is stream luck).
Sealed-stream results: PROCEDURE_REUSE_C1 47.1/26.96 FRESH, 46.7/50,
reuse_gain 29005, checklist 8/8 x 3 seeds; searched tops 27.6-32.0,
ACC == FRESH exactly, 0 blocks, 0 invocations, checklist 0/7 beyond
guard 0. Mechanism of the winners: stride-17 counter action generators
(pseudo-random walks with resets) that out-luck enumeration on chains.
Q1, Q2, Q4 held; Q3 half; Q5 lost (budgets allow luck). Packet:
roles/Crius/REVIEW_PACKET_C1_2026-09-19.md. Receipts gzipped (433 MB ->
26 MB). Recommendation: C1b with recalibrated budgets, else CRIUS-25.

## C1b (D1 isolation) under the operator GO

a78569dad GO verbatim; be339ac0f calibration rule + vector recorded before
search (budgets 2000/50/50/50; RANDOM 2.5/1.4/5.0 pct; pass 1 kept);
553ccff52 gate A-H PASS on c1b; 6364d6994 lineage tool + C1 readouts.
Nine C1b runs (random/seeded/recombination x 3 seeds, 300 its, 111-462 s
each), qualified with final-population ranking, LINEAGE.md per run.
Readout: 270 qualified rows -> 246 reuse_gain 0, 17 negative, 7 positive
(one family, stream-dependent sign: the record-id clock); blocks created
only by two random-arm block spammers (0/50); invocations reaching a
block 0; state-keepers not selected against (41/65 census rows above
contemporaries) but never load-bearing. Tops: compressed enumerators
19-21/50 sealed; two runs drifted below the seed. Decision per the GO:
frozen C1 family CLOSED; next CRIUS-25/D2 preregistration (CRIUS-30).
Calibration ledger: a draft-numbers row (two counts were written from
impression and corrected from the scripts before publishing).

## D2 authorized: Campaign 2 (accessibility frontier) preregistered and built

26658551e ruling verbatim (C1b ACCEPTED; frozen C1/C1b family closed;
D2 as an accessibility-frontier ladder). 565135bd0 DESIGN_C2.md before
code: ladder A-D over chain links L1-L6, central PARTS diagnostic,
paired streams + takeover check, metadata charge for empty objects,
gates A-H per rung, predictions R1-R6. Build commit: resource invariant,
post-success no-ops, last_primitive observation, typed procedures
(PREC_BEGIN/END, PINVOKE, substrate-maintained calibration artifact;
PSIM/PMATCH), per-rung opsets, paired streams + takeover log, PARTS
module and diagnostic, gate positive control per rung, configs c2a-c2d,
fixture F4 (C1b record clock), 11 new tests (29 pass). Deviations dated
in DESIGN_C2 s11 (A1-A7). Driver launched: per rung gate -> parts ->
baselines -> 9 searches -> qualify -> report -> lineage; a rung whose
gate fails is not searched.
Bugs found and fixed while building (all before any rung was gated):
typed opcodes routed into the store dispatcher; PMATCH FAIL taken as a
branch; PREC_BEGIN did not restart; planner loops burned the step budget
on an empty store (bounded to 8 objects); a test acted before loading
num_ops; the argument of a procedure is its first step's ABSOLUTE
position (my test expected relative).

## C2 rungs A-B run; rung C gated out; packet

Driver relaunched three times (takeover-log ordering; donor-pool name
shadowing the worker pool; calibration object corrupted by BLK_STATE_SET
from random programs; unbounded telemetry lists exhausting worker memory)
-- each a dated fix with a test where the substrate was involved. Rung A:
9 runs, no gradient, 0/9 below the seed (takeover check works; C1b's
collapse shape gone). Rung B: 9 runs, no gradient; PARTS: recorder-only
-0.001, invoker-only -0.002, pair +1.138 (7/10, +1.1 tasks); one
reproducible lineage E4 = record-id enumeration shifter (RECORD_IDS_ONLY
== TRANSPLANT on 3/3 streams). Rung C: PARTS cliff (planner-only -0.057;
+16.7 with procedures; full mechanism +15.97, 10/10); gate FAILED E/H
against the bytecode control (F/G pass); window-10 attempt recorded and
reverted; my driver had treated the failed gate receipt as a pass and ran
rung C searches ~20 min before I stopped them (deleted; ledger row).
Packet written with computed counts (a second draft-numbers ledger row:
two lineage counts corrected from scripts before publishing).
Decision requested on rung C (packet s9); my recommendation is (i).
