# Crius journal -- 2026-09-23 (terminal resolution round, Campaign 2)

Instance m2-8d43bbf9 on M2. Booted from the PARKED state (CRIUS-33) under the
operator's terminal-resolution directive (committed verbatim under
roles/Crius/prompts/). Times are UTC from receipts and `date -u`.

- Boot: fetch, no pull; origin/main a39bc1647; comms synced as Crius with
  EW_DB_HOST=192.168.1.202. Concurrency check: no other Crius instance.
- Step I: wrote crius/CRIUS_C2_TERMINAL_PREREG.md (cbf30a726) BEFORE any
  run: rungs, gates A-H exact, R1-R6 verbatim, meanings, the rung-C
  failure verbatim, prior evidence, the two-control separation with its
  validity condition, the disposition rule in checkable form, forbidden
  moves.
- Step II/III: instrument audit found one defect -- the invocation log
  recorded R0 as the argument for every block; typed PINVOKE carries its
  argument in `parg`, so the 09-19 H count for the typed control was on a
  loop register. Repaired in vm.invoke_block / artifacts.log_invocation;
  gate_c1.py GATE_VERSION 2 with --positive-control (E,H) and
  --causal-control (F,G); nine contrast fixtures in
  crius/tests/test_c2_terminal.py (a3f6a4be0). 39 tests green.
- Step IV: rung-C gate v2 clean re-run from c2c.json 416bbe8b9be34706:
  A-H all PASS separately; typed-control diagnostics reported non-gating
  (7661050d9). C-GATE TRUE.
- Path-evidence tool crius/c2_path.py + PATH.md for all rung A/B runs
  (f16d49322): recorder = neutral hitchhiker, invoker deleterious on
  arrival, no complete mechanism in 9/9 at B.
- Launched drive_c2cd.py (scratchpad) at ~08:1xZ: per rung gate v2 (skip
  only on all_pass true) -> PARTS -> baselines -> 9 searches -> qualify /
  report / lineage. Monitors re-armed at the 30-min cap throughout.
- Operator asked for a cut-and-paste ASCII interim packet: written,
  committed 6c39f9d1f, merged origin/main (Ares cycle-2 prereg only),
  pushed as cabe092b4 after crius/tests 39/39; comms synced (0 new).
- Wrote crius/c2_terminal.py (52c58d0d4, 0f0da6df1): mechanical scorer of
  the disposition rule from receipts. First draft counted a "positive
  step" from the search's own fitness -- wrong, because parents carry
  fitness from earlier paired streams and takeover ties count as
  takeover; replaced by PAIRED re-evaluation of parent vs child on the
  sealed streams, plus a neutral-edit null. Committed before rung D
  finished (the directive's "commit before decisive result").
- Rung C done 09:24Z: 9/9 null. Rung D gate v2 PASS (config
  7ab056c39e1821fd, identical witness values), PARTS identical to C,
  random/seeded arms byte-replicate rung C (same best ids per seed);
  recombination arm done 10:20Z: 3/3 null. Driver "C2CD ALL DONE",
  no failed step.
- Decisive readouts (all from scripts): 0/18 runs with a reproducible
  ACC > FRESH candidate; first complete mechanism None in 18/18; 19
  recorder/invoker steps re-evaluated paired, 9 "selectable" at +0.001
  fitness with 0 solved, inside the neutral-edit null; 1,771 PART-fragment
  children at rung D, 63 takeovers, 5 fragments in final-top ancestries,
  the only one that paid was `CONST R6, 4` (+1 task on 2 of 3 streams).
  Two "invocation without content" tops (32 blocks, 43/49 invocations,
  A = F = R = S). One under-floor RESET-sensitive row probed: carries
  nothing. R4 survives (the null); R5 lost.
- Disposition: CLOSED -- ACCESSIBILITY FRONTIER MAPPED (c2_terminal.py;
  crius/runs/C2_TERMINAL_DISPOSITION.json; fingerprinted receipt
  C2_TERMINAL_DISPOSITION_RECEIPT.json at 10:23:47Z, code 0f0da6df1).
- Deliverables: crius/CRIUS_C2_TERMINAL_REVIEW.md; C2_SUMMARY.md
  regenerated (c2_summary.py now prefers gate v2); receipts gzipped
  (candidates.jsonl.gz, qualify receipts .gz, SUMMARY.json plain);
  2 scorer regression tests (41/41 green); STATUS.md -> CLOSED, no resume
  pointer; roles/Crius/REVIEW_PACKET_C2_TERMINAL_2026-09-23.md.
- Process notes for the ledger: receipts say code_dirty_crius=true only
  because untracked run directories were being written (no tracked file
  modified). Interim packet printed "TABLE_MEMO FULL 1/1/?" for stream 303
  before the figure was read from the receipt (it is 0/0); the terminal
  review has the receipt value. No count entered a packet before a script
  printed it, except that one placeholder, which was marked as such.
- Forbidden moves not taken: no lowered floors, no E2/H2, no budget
  increase, no parts seeded into the population, no C3.
- Not my lane: Nyx manifest mismatch keeps archaeon/tests/test_base_role.py
  red (comms #516).
