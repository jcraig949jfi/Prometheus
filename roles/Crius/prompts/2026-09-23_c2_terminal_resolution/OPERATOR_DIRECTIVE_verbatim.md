You are Crius in Prometheus.

Resume from the parked state recorded in:

* roles/Crius/STATUS.md
* roles/Crius/journal/2026-09-19.md
* REVIEW_PACKET_C2_2026-09-19.md
* DESIGN_C2.md
* open decision CRIUS-33
* rung-C config crius/configs/c2c.json
* park receipt commit f2238721b, verified on origin/main

This is a terminal resolution round for Campaign 2 / the Accessibility Frontier.

Do not merely resume the old search. Your job is to determine whether Crius should be:

1. UNPARKED into a genuine continuation, because the accessibility gradient predicted by C2 exists under defensible controls; or
2. CLOSED, because the frontier has been adequately mapped and the proposed acquisition→representation→retention→addressing→invocation chain does not acquire an evolutionary gradient under this substrate/search regime.

Do not finish this round PARKED unless an external infrastructure failure makes the experiment literally uninterpretable. Scientific ambiguity is not a reason to park: resolve it experimentally where possible and make a disposition.

I. Reconstruct the exact claim before running anything

Read the C2 design, review packet, receipts, configs, tests, and relevant Crius history.

Restate, in Crius’s own round preregistration:

* what C2-A, C2-B, C2-C, and C2-D were intended to change;
* the exact definitions of gates A-H;
* the exact frozen definitions of predictions R1-R6, especially R4 and R5;
* the meaning of FRESH, ACCUMULATED, PARTS, typed artifacts/arguments, recorder/invoker components, and the two controls involved in CRIUS-33;
* the causal claim protected by F/G;
* the accessibility/economic claim measured by E/H.

Do not silently change any threshold or prediction.

Record the existing rung-C failure verbatim:

* E: 1/3 streams; 12% of FRESH cost versus frozen 20% floor.
* H: 1/0/1 blocks with >=3 distinct args versus floor 2.
* F/G: PASS under the retained causal control.
* C/D were not searched after that failed gate.
* Earlier rung-C receipts were deleted because they occurred under the failed gate and are not admissible evidence.

Also preserve the prior frontier evidence:

* C1/RELAY demonstrated that useful procedural reuse can exist independently.
* C1 did not demonstrate a measurable evolutionary gradient assembling the full reuse chain.
* C2 A/B did not reveal the desired gradient.
* PARTS showed a one-link valley at B and a larger cliff at C rather than a smooth easy path.

The question now is not “can reusable procedure work?”

It can.

The question is:

Has Crius supplied a substrate in which selection can actually reach, preserve, address, invoke, and ultimately recombine reusable procedure through an accessible evolutionary path?

II. Resolve CRIUS-33 experimentally

Adopt the following declared control separation unless inspection discovers a concrete implementation error that invalidates it:

Accessibility control — E/H

Measure E and H against the bytecode/block control.

Purpose: determine whether rung C genuinely makes creation/invocation and reusable argument-bearing parts more accessible than the lower-level block representation.

Causal control — F/G

Retain the typed control for F and G.

Purpose: preserve the stronger causal test that any advantage depends on the actual reusable procedural mechanism rather than representation convenience, aliasing, scalar/table leakage, or some cheaper substitute.

This is a declared deviation from the original single-control gate. It must be:

* written before the new C/D runs;
* justified mechanistically;
* versioned;
* hashed;
* treated as a correction to the instrument, not a post-hoc relaxation of evidence.

Do not lower the existing E/H numerical floors.

Do not weaken F/G.

Do not use one favorable control for every claim.

Before the expensive runs, build or extend deterministic fixtures demonstrating that each control is testing the intended contrast.

If the two-control interpretation is shown to be logically invalid — for example, because E/H and F/G cannot be separated without changing the estimand — then construct the stronger typed control necessary to restore a single interpretable comparison and rerun the gate.

Do not choose closure merely because the control repair requires implementation work.

III. Instrument audit before search

Run a short grounding suite before any evolutionary search.

At minimum verify:

* candidate and artifact accounting;
* mutation/duplication/splice operators;
* typed argument flow;
* artifact identity and non-aliasing;
* persistence/reset semantics;
* invocation accounting;
* recorder/invoker PARTS accounting;
* full-budget failure behavior;
* held-out procedural tests;
* transplant and ablation behavior;
* accumulated positive control;
* deterministic replay from receipt;
* paired-stream seed alignment;
* no hidden reward for merely allocating/storing an artifact;
* no scalar/table shortcut satisfying the procedural criterion.

Audit whether the previous E/H failure could have arisen from instrumentation rather than biology/search geometry.

Fix instrument defects.

Do not tune the world or evolutionary pressure to manufacture a pass.

Commit the instrument repair and preregistration before obtaining the decisive C/D result.

IV. Re-run the rung-C gate cleanly

Start rung C from a clean state using the canonical C configuration derived from:

crius/configs/c2c.json
hash: 416bbe8b9be34706

Run the preregistered gate with the corrected control treatment.

Report every gate A-H separately. Do not collapse them into a single score.

Three possibilities exist.

C-GATE TRUE

E and H pass their frozen floors against the accessibility control, while F and G continue to pass the causal control, and all other gates remain valid.

Then rung C is admissible.

Proceed immediately to the decisive C and D searches.

C-GATE FALSE

After the instrument/control repair, E and/or H still fail their frozen thresholds.

Do not tune thresholds.

Do not search D.

Perform enough diagnostic analysis to determine where the accessibility cliff lies and then proceed to the CLOSE disposition in Section VII unless a specific implementation defect invalidated the run.

INSTRUMENT INVALID

A concrete defect makes the gate uninterpretable.

Repair it, add a regression test, and rerun.

Do not classify a scientifically unfavorable result as an instrument failure.

V. If C passes, run C and D as the decisive experiment

Rungs C and D were withheld after the failed gate. This round may now search them only after C-GATE TRUE.

The purpose is not raw task performance.

The purpose is to test the frozen R4/R5 predictions about whether individually reachable creation+invocation and then recombinable recorder/invoker parts create a real accessibility gradient.

Run the frozen C/D experiments with:

* matched paired streams;
* exact budget accounting;
* frozen search budgets;
* held-out compositions;
* fresh versus accumulated comparisons;
* artifact transplant;
* code-only control;
* ablation;
* argument diversity;
* lineage/provenance tracing;
* intermediate PARTS frequencies;
* time-to-first complete mechanism;
* survival/persistence of partial structures;
* invocation frequency;
* recombination events producing functional descendants.

Retain failures and extinct partial lineages. They are evidence about the geometry.

Measure not only whether a complete reusable mechanism eventually appears, but whether incomplete components receive a selectable foothold before the complete mechanism exists.

This is critical.

A lucky complete program appearing after a long search is not evidence of an accessibility gradient.

VI. Distinguish existence from accessibility

Crius already knows that reusable procedure can exist.

Therefore the following are insufficient to continue the seat:

* a hand-written or seeded positive control works;
* RELAY still works;
* an evolved lineage eventually contains a complete mechanism without detectable selectable precursors;
* a complete artifact transfers but its PARTS were neutral/deleterious until final assembly;
* a rare success occurs only through an effectively simultaneous multi-edit jump;
* larger budgets eventually brute-force the mechanism;
* accumulated populations outperform fresh ones solely because of unrelated retained state;
* invocation is useful only after manually supplying the representation;
* typed objects make the task easier without producing an evolutionary path into reuse.

The evidence Crius needs is path evidence.

Ask:

Does the altered representation make useful incomplete machinery selectable early enough that evolution can assemble the full reuse chain incrementally?

Trace that causally.

VII. Hard disposition

At the end of this round, make exactly one of the following scientific dispositions.

UNPARK -> CONTINUE

This disposition is earned only if the preregistered evidence shows a genuine accessibility gradient.

At minimum:

* repaired rung C passes its frozen accessibility and causal gates;
* the decisive R4/R5 predictions survive their preregistered tests;
* partial recorder/invoker or equivalent components acquire measurable selectable value before complete reuse;
* the effect appears across the required paired streams rather than one anecdotal lineage;
* ablation removes the benefit;
* artifact transplant carries the relevant advantage beyond code-only transfer;
* held-out procedural tests rule out table/scalar memorization;
* lineage evidence shows incremental acquisition rather than an unexplained large jump.

If this occurs, do not immediately design C3.

Instead write the smallest possible next-question packet describing what has now become reachable and what unresolved mechanism deserves another campaign.

CLOSED -- ACCESSIBILITY FRONTIER MAPPED

Close Crius if, after a valid control/instrument repair:

* rung C still fails its frozen gate; or
* C passes mechanically but R4/R5 fail; or
* complete reuse occasionally appears but no selectable precursor gradient is observed; or
* PARTS remain a valley/cliff requiring effectively simultaneous assembly; or
* the apparent benefit disappears under causal ablation/transplant/held-out controls; or
* the only successful route requires hand-seeded components or representation privileges not available to evolution.

If closing, state the result positively and precisely:

Useful reusable procedural machinery exists, but under the tested C2 substrate and competence-first mutation/duplication/splice regime, evolution was not shown a defensible incremental accessibility path from ordinary candidates into acquisition→representation→retention→addressing→invocation reuse.

Preserve the measured frontier:

* where selection first loses the path;
* cost ratios;
* PARTS frequencies;
* required edit distances;
* argument-diversity behavior;
* which causal controls survived;
* which positive controls remained healthy;
* what representation changes were insufficient.

Then mark the seat CLOSED, not PARKED.

A future operator may cite the closure result, but do not leave a “try a larger budget / slightly different threshold / C3” resume pointer.

The only legitimate future reopening condition is new external evidence or a materially different substrate/mechanism that directly attacks the measured accessibility cliff. More search on the same geometry is not a reopening condition.

VIII. No rescue ladder

This round is explicitly forbidden from producing an endless sequence of substrate patches.

Do not:

* lower E/H floors;
* invent E2/H2 after seeing failure;
* increase budgets merely because success was absent;
* seed recorder/invoker parts into the evolutionary population;
* add task-specific primitives;
* redefine procedure to admit scalar/table transfer;
* change selection pressure after seeing the outcome;
* create C3 because C/D were disappointing;
* call a rare complete solution an accessibility gradient without lineage evidence.

One legitimate instrument repair is allowed.

One clean rung-C adjudication is required.

If admitted, one decisive C/D campaign is allowed.

Then disposition.

IX. Deliverables

Commit and push:

1. CRIUS_C2_TERMINAL_PREREG.md
2. the corrected control/instrument implementation and regression tests;
3. rung-C gate receipt with A-H individually reported;
4. if C passes, complete C/D receipts and lineage evidence;
5. CRIUS_C2_TERMINAL_REVIEW.md;
6. machine-readable disposition receipt;
7. updated roles/Crius/STATUS.md;
8. journal entry;
9. concise operator review packet.

The final report must begin with exactly one of:

CRIUS DISPOSITION: UNPARK -> CONTINUE

or

CRIUS DISPOSITION: CLOSED -- ACCESSIBILITY FRONTIER MAPPED

Then answer, in plain language:

1. What specifically unparked Crius?
2. Did rung C become genuinely accessible or merely executable?
3. Did partial reusable machinery obtain selectable value before completion?
4. Did R4 and R5 survive?
5. What causal interventions establish that the signal is procedural reuse?
6. Where is the measured accessibility frontier?
7. Why does the evidence justify continuation or permanent closure?
8. What result would have been necessary to reach the opposite disposition?

Run the science. Do not protect the seat.
