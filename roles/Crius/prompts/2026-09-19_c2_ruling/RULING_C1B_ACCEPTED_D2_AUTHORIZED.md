CRIUS — CAMPAIGN 1b RULING / D2 AUTHORIZATION

C1b is ACCEPTED.

Close the frozen C1/C1b family.

Principal result to preserve:

RELAY establishes that independently useful procedural reuse exists in the frozen world, but no measurable evolutionary gradient from ordinary programs to that mechanism was found under the tested VM, variation operators, search budget, seeds, streams, and two budget regimes.

Do not generalize this to universal inaccessibility.

The critical observation is Q-c:

State keeping itself is not strongly selected against. The missing gradient is in assembling a load-bearing causal chain from acquisition through representation, retention, addressing and later invocation.

Proceed to D2 / DESIGN_C2.md BEFORE code.

C2 OBJECTIVE

Do not ask merely whether an easier substrate can evolve RELAY.

Map the ACCESSIBILITY FRONTIER: determine which reduction in coordination depth first produces a reproducible ancestral gradient toward useful persistent computation.

Prefer a substrate ladder:

C2-A — current mechanism/control.

C2-B — typed artifact representation / argument port made locally accessible, invocation still coordinated.

C2-C — artifact creation and invocation each individually reachable by small mutations, without providing the complete reuse program.

C2-D — independently evolvable recorder/invoker parts plus generic recombination.

Exact design may differ, but preserve the ordered accessibility question.

PARTS DIAGNOSTIC

Make the proposed PARTS arm central.

Construct frozen partial mechanisms such as:

calibrator only
recorder only
invoker only
recorder + invoker
other naturally separable intermediates

Keep them out of the main evolutionary population.

For every partial, measure whether it has a reproducible selective advantage over its immediate ancestor before the complete mechanism exists.

Interpretation target:

parts individually non-beneficial
    -> coordination / fitness-valley problem
parts individually beneficial but fail to compose
    -> variation / recombination / linkage problem
ancestry develops gradient but stalls later
    -> localize the exact missing transition

Do not seed the complete hand-written mechanism into evolutionary search.

STREAMS

Use at least two paired common-random streams per candidate/parent comparison.

Add an independent takeover/check stream before weak improvements replace established competent ancestry if practical.

The goal is to prevent single-stream lottery replacement from masquerading as evolutionary dynamics.

RESOURCE ACCOUNTING

Preserve CRIUS-28 and CRIUS-31 exactly as historical fossils and regression fixtures.

Do NOT preserve free-empty-object accounting in the active C2 substrate.

A zero-payload record/block still consumes metadata, namespace and lifetime resource. Charge allocation accordingly or impose an equivalent explicit bound.

Fix the resource invariant, not the evolved behavior. Do not ban clocks, counters, IDs or other potentially useful persistent mechanisms.

GATES

Carry A-H forward.

Retain the positive causal requirement:

ARTIFACT_TRANSPLANT > CODE_ONLY
artifact ablation destroys that advantage

and verify it independently at every substrate rung whose semantics change materially.

PRIMARY READOUT

For each rung report:

first mutation distance / structural distance to useful reuse
selective value of each partial
frequency of persistent-state creation
frequency of useful invocation
ancestral trend toward load-bearing state
ACCUMULATED vs FRESH
artifact transplant and ablation
sealed-stream competence
recombination contribution

The most valuable C2 result may be a boundary, not a successful evolved workspace.

Question:

At what representational and variation granularity does reusable
computation become incrementally selectable?

That is the D2 result we want.

Proceed with CRIUS-25 / CRIUS-30 under this ruling.
