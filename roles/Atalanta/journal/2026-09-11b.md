# Atalanta journal 2026-09-11 (second pass) -- ATALANTA-01 ruled, ATALANTA-04 executed

Operator ruling (verbatim, in chat): "Disposition: RETIRE-AND-LIFT-ASSET.
Do not revive the daemon and do not reconnect it to Apollo yet. Your
useful output is the DEAD-GATING specimen, not another consumer loop.
Execute ATALANTA-04." Five questions were set, plus: treat the 354 as
provenance-limited and do not upgrade it; preserve the parser correction;
send the cross-seat recommendation to Archaeon; do not implement the
invariant outside this lane; and say at the end whether anything uniquely
useful remains, recommending clean retirement if not.

Workspace: D:\Prometheus-worktrees\atalanta-base-role, new task branch
atalanta/specimen-2026-09-11 from origin/main 05b1134e6. Guard passes
(git-dir differs from git-common-dir). Host SPECTREX5 = M2.

## What happened, in order

1. comms sync before work: 2 new. Archaeon's ack (#65) says the findings
   from this seat's #47 were absorbed and acted on: the pre-worktree step
   is now explicit in the base contract, comms FAILS CLOSED against a
   database with no comms schema and names EW_DB_HOST, the three M2
   scheduled tasks are registered, and -- the one that changes this pass
   -- UPSTREAM LIVENESS IS NOW BASE RULE 9. ATALANTA-03 and -05 are taken
   by Archaeon.
   Read rule 9 in full BEFORE drafting anything, so the proposal would be
   the smallest REMAINING invariant rather than a restatement of a rule
   that already exists. This changed the deliverable substantially.

2. Read the alarm path in source: daemon.py:547-552. The branch emits an
   event and a log_work row and then falls through to save_state. No
   return, no exit, no parked flag, and the condition is `>=`. PREDICTED,
   before touching any data, that the alarm must therefore have fired on
   every tick from 50 to 354 inclusive = 305 times.

3. Traced where the alarm went: emit_log_work (daemon.py:434-445) ->
   scripts/session_telemetry.py:164 -> agora_persist.log_intelligence_stage
   -> the table agora.intelligence_outputs. Realised this is a SECOND
   RECORDING CHANNEL that might have survived, since this host has no
   filesystem residue.

4. Queried the canonical store (EW_DB_HOST=192.168.1.202, read-only):

       atalanta_upstream_not_found   354
       atalanta_self_audit_null      305   <- prediction 305, observed 305
       atalanta_startup                3
       atalanta_shutdown               1

   The prediction survived. That establishes from a MECHANISM, not from a
   count, that the counter never reset and therefore that all 354 ticks
   were null.

5. First timeline query used `started_at` and returned min == max on 305
   rows. Wrong field: it is a module-global session start. Switched to
   `finished_at`, which is per-row, and got the real seven-day timeline.
   Recorded as LEDGER L-12 and as a caveat that travels with the handover.

   Life 2026-05-23 04:28:00 to 2026-05-30 12:10:49 = 7 d 07:42:48.
   First alarm 2026-05-24 04:09:50. Alarm window 6 d 08:00:58 = 86.5% of
   the agent's operational life. 48.4 ticks/day against a nominal 48.0.
   3 startups, 1 clean shutdown; the last row of every kind shares a
   timestamp, so the process was killed.

6. Fleet query on the same table: every *_self_audit_null stage.
   pheme 305, atalanta 305, polyhymnia 23, talos 8 = 641 alarm rows, all
   success=false, none ever queried. Before running it I had DERIVED
   Polyhymnia at about 201 from its MONITORS prose; measured 23. LEDGER
   L-10.

7. Verified the same emit-and-fall-through alarm in all four May-template
   daemons: atalanta:547, pheme:495, polyhymnia:442, talos:615, identical
   threshold 50, identical `>=`, none stops.

8. Checked whether Apollo was actually dead during Atalanta's window, to
   test the P47 autopsy's "consumer of a dead producer". git log over
   apollo/ for 2026-05-23..05-31: 12 commits, including two passing
   gauntlets over organism populations. Apollo was alive and working.
   BOUNDED the claim honestly: the committed apollo/run_* directories
   date from 2026-06-09, so whether Apollo persisted parseable runs THAT
   WEEK is not established, and the specimen says so rather than
   overclaiming.

9. The machine probes. The registry lists both as DEAD; on this host the
   M2 one is State Ready, PT5M, last run 12:02:32 today, result
   0x80070002, next run 12:07:31, 0 missed. A live instance of this
   agent's own failure shape, at 288 failures/day.
   Eliminated "script missing": scripts/machine_probe.py exists at the
   exact invoked path. Hypothesised the Windows Store App Execution Alias
   (the PATH pythonw.exe is a 0-byte reparse point, measured, and a real
   interpreter exists elsewhere, measured) -- then TESTED it with a direct
   CreateProcess on the bare name, which SUCCEEDED, exit 0. Hypothesis
   falsified by its own author. Reported as CAUSE UNRESOLVED with two
   eliminated hypotheses and the discriminating test named for the owner.
   LEDGER L-11.

10. Census over all 30 MONITORS rows, enumerated programmatically:
    18 unrouted, 9 routed to a status file or "the operator", 3 where
    something automatically acts.

11. Wrote the invariant as base rule 10, with the load-bearing detail that
    the counter must key on the rule-8 PRODUCTIVITY signal and never on
    emission -- because Atalanta emitted a well-formed artifact on all 354
    dead ticks and would score 354/354 productive under an emission-keyed
    bound.

12. Built the reference implementation and its controls inside this seat's
    own lane only (roles/Atalanta/reference/, imported by nothing):
    negative (live and bursty producers never park), positive (parks AT
    the bound, one notification), CHEAT (a loop that really writes a file
    every tick still parks, plus a companion test asserting the naive
    emission-keyed instrument scores 354 dead ticks as productive), brake
    (a parked loop does not tick or re-notify), and the Atalanta replay
    (354 -> 50 ticks, 305 -> 1 notification). 9 passed in 4.06 s.

13. Salvage assessment against a UNIQUENESS test, not a quality test.
    aggregate_primitive_signals: apollo/scripts/inspect_population.py:69-75
    already counts primitive frequency, against the real object model, on
    the real population, with an LLM/non-LLM stratification Atalanta never
    had. The composite-chain counter: six lines of standard bigram and
    trigram counting resting on a premise nothing ever tested. The DR
    template's discipline: already base rule 4. Conclusion: nothing
    uniquely useful remains; clean retirement.

14. Found one thing that is NOT this seat's and handed it over: the
    telemetry table holds 15,495 rows for the whole May fleet and is a
    surviving primary source for any dead agent whose gitignored residue
    is gone. Handed to the Necropolis Keeper via the Archaeon report, with
    the regenerable script and the started_at caveat. Not pursued here.

15. Updated the seat state to match the ruling: MONITORS row -> RETIRED by
    ruling (annotated, not deleted); charter carries a DISPOSITION block;
    STATUS says retirement recommended; backlog carries a dated status
    block and its superseded intro is annotated rather than rewritten;
    calibration ledger gained L-10 through L-13.

## What was NOT run

- The daemon. Still never started, in any mode, on any host.
- Any write to another seat's file, row, task or process. The census is
  inspect-only; the two machine probes were measured and not triggered,
  re-registered or disabled.
- The invariant, anywhere outside roles/Atalanta/reference/. The operator
  said do not implement it outside this lane and it is not implemented
  outside this lane.
- The discriminating test on the machine probes' 0x80070002. Named for
  the owner instead.
- No edit to engine/ledger/AGENT_AUTOPSIES.jsonl. The P47 correction is
  filed as an annotation in the specimen and reported; the autopsy record
  belongs to Aporia and to the auditor's independence rule.

## Numbers

    registered loops examined                  30 of 30 (enumerated)
    loops with no alarm route                  18
    loops where an alarm makes something act    3
    May-template daemons with an unstoppable    4 (verified in source)
      alarm
    alarm rows in the canonical store         641 (all success=false)
    Atalanta null ticks                        354 (dual-recorded)
    Atalanta alarm firings                     305 (predicted 305)
    fraction of its life spent alarming       86.5%
    Apollo commits during that window           12
    reference controls run                       9 passed
    files changed outside this seat's tree       2 (its own MONITORS row,
                                                   its own charter)
