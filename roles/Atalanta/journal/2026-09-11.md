# Atalanta journal 2026-09-11 -- reanimation as a seat and base-role adoption

Operator directive (verbatim, in chat): "You're @agents\atalanta\ You were
an agent back in May but haven't been seated since. Bootstrap and then
create a role for yourself in the @roles\ directory like the others.
Adhere to the base-role concept. Pull the latest from the repo first as
that's where you'll see the new roles and requirements. Don't do anything
other than this bootstrap and registration except remind me what you did
when you were active"

Host: SPECTREX5. This is NOT M1 (where the May daemon ran) and NOT the F:
canonical checkout the other adoption passes used today. That matters for
two things below: the runtime residue and the comms resolver.

## What happened, in order

1. Ran `git pull` in the canonical checkout D:\Prometheus, on the
   operator's instruction and BEFORE reading the working contract. This
   is a D-23 violation on two counts (s1 no mutating git operation in the
   canonical checkout; s3 never pull). Inspected afterwards: a
   fast-forward of main to 8714b2709, no conflict, no file loss; two
   pre-existing untracked evidence_wiki paths (.gitignore, derived/) were
   there before and were left alone. Recorded in calibration/LEDGER.md
   L-09 rather than omitted. The operator's instruction and D-23 s3 are
   in direct conflict; that is reported to Archaeon, not resolved here.

2. Read agents/atalanta/CHARTER.md (full), then the base role from the
   fetched tree: README.md, NORTH_STAR.md, RESPONSIBILITIES.md,
   WORKING_CONTRACT.md, INHERITANCE.md, MONITORS.md. Read
   roles/Talos/RESPONSIBILITIES.md and journal, roles/Pheme/STATUS.md,
   and the backlog schema as the pattern for an old seat's adoption pass.

3. Guard: D:\Prometheus IS the canonical checkout (git rev-parse --git-dir
   equals --git-common-dir, both `.git`). Refused to work there. Created
   the seat worktree:
       git fetch origin
       git worktree add -b atalanta/base-role-adopt-2026-09-11 \
           D:/Prometheus-worktrees/atalanta-base-role 8714b2709ffa3f1a5d55781d476c3eba4c97a898
   Given a 900 s budget per WORKING_CONTRACT s3 (the 39,284-file checkout
   is exactly the operation that must not get a short timeout). Completed
   in one pass, exit 0, 39,284 files, status clean. Guard after:
   git-dir D:/Prometheus/.git/worktrees/atalanta-base-role differs from
   git-common-dir D:/Prometheus/.git.

4. comms: `python -m comms sync Atalanta` FAILED with
   psycopg2.errors.UndefinedTable: relation "comms.messages" does not
   exist. Diagnosed rather than worked around: on this host the Evidence
   Wiki resolver (evidence_wiki/config.json db_host "localhost") reaches
   a LOCAL prometheus_fire whose only schemas are ew, public,
   information_schema. The canonical comms store is M1. With
   EW_DB_HOST=192.168.1.202 the sync succeeded at 15:10 UTC: 2 new, 0
   queued, queue length 0 -- Archaeon broadcast #1 (comms live; base role
   applies; sha256 402c3445443d1be5) and Archaeon ruling #39
   (INHERITANCE rows are self-service; sha256 6b1e9ec05abbabbc). Both are
   broadcasts to `*`; neither is addressed to this seat. It failed loudly
   rather than silently reading the wrong database, which is the right
   behaviour, but no committed document names the override. Filed as
   ATALANTA-05 and reported to Archaeon.
   Boot recorded after roles/Atalanta/ existed (same asymmetry Talos
   reported: `comms boot` refuses a seat with no roles/ directory while
   `comms sync` accepts it): booted 2026-09-11 on SPECTREX5,
   claude-opus-5[1m], heavy, capabilities any, status blocked, at
   8714b2709 in the seat worktree.

5. Archaeology. Sources read, all at 8714b2709 unless stated:
   - git log over agents/atalanta and scripts/atalanta_loop_launch.bat:
     exactly ONE commit in the agent's history, e6b3746f0
     (2026-05-23 04:43:27 -0400, Aporia, "three new agents for typed-DR
     substrate production": Hypatia D-track, Atalanta E-track, Pheme
     demand voicer). No commit has touched the agent since.
   - git grep "atalanta" over all tracked .md/.py/.json/.jsonl/.bat: 40
     files. Load-bearing ones: engine/ledger/AGENT_AUTOPSIES.jsonl (the
     P47 autopsy), engine/queues/BACKLOG.jsonl (AUTOPSY-ATALANTA DONE at
     line 92, PROF-Atalanta PARKED at line 660),
     engine/queues/PROF_TRIAGE.jsonl (binding AT-COST),
     engine/queues/CONSUMPTION.jsonl (the autopsy batch record),
     engine/shadow/WORKLOG.jsonl + REVIEWS.jsonl (Elenchus reviewed pass
     P47 UNDERCLAIMED -- on the Coeus null in the same batch, not on
     Atalanta), pivot/COMPONENT_DOSSIERS_2026-06-24.md (the dossier and
     the blank HITL line at 179), pivot/agent_roster_2026-05-{21,26,28}.md,
     aporia/docs/STATUS_2026-06-15_reset.md, the 06-23 disposition plan.
   - engine/necropolis/ROSTER.jsonl on the necropolis/foundation worktree
     (D:\prometheus-necropolis-wt): Atalanta is a roster subject,
     apparent_family "autopsy:DEAD-GATING",
     proposed_investigation_status UNQUEUED.
   - comms inbox: two broadcasts, ever. No prompt, delegation, INBOX file
     or D-nn decision has ever addressed Atalanta.

6. Re-measured the cause rather than repeating the dossier's "unverified
   claim":
   - daemon.py:63-67 hardcodes APOLLO_RUN_ROOTS to apollo/runs,
     apollo/runs_v2, apollo/organism_runs. Tested each: ABSENT, ABSENT,
     ABSENT. find_active_apollo_root (daemon.py:222-226) therefore
     returns None on every tick and the scan never runs.
   - Apollo's real output at this SHA: apollo/run_v2d2b/checkpoints/,
     apollo/run_branch_c*/ , apollo/run_branch_c_xover/novel_discovery.jsonl.
   - CORRECTION to the dossier's schema claim: `primitive_sequence` is
     still a live key in current apollo/src (genome.py, compiler.py,
     map_elites.py, primitive_types.py, ablation.py, health.py). The key
     survived; the CONTAINER diverged (pickles and jsonl, not per-run
     JSON with run_id/completed_at/organisms). Revival cost is a reader,
     not a redesign. Annotated, not silently rewritten (LEDGER L-07).
   - Talos's charter independently names apollo/runs as one of its five
     corpus streams and marks it "never existed". Two agents from the
     same week were gated on the same phantom path. Filed as ATALANTA-16.

7. Residue census on THIS host: agents/atalanta/ contains exactly four
   files (.gitignore, CHARTER.md, __init__.py, daemon.py 27,930 bytes).
   No state/, no artifacts/, no logs/, no events.jsonl, no atalanta.pid;
   .gitignore lists all of them, so none was ever committed. schtasks
   shows no task matching atalanta. The 354 artifacts and the state.json
   quoted by the autopsy and the dossier exist on M1 only, if at all. The
   354 figure is therefore SINGLE-SOURCED and not re-verifiable from here
   (LEDGER L-06); it is quoted with that grade attached and never as a
   re-measurement. Preserving the residue is ATALANTA-02 and needs a seat
   with M1 access.

8. Wrote roles/Atalanta/: RESPONSIBILITIES.md (banner line 3; entry
   file), STATUS.md, BACKLOG_H0H5.md (22 rows; ATALANTA-01 and -19 are
   the XL operator rows), ARCHAEOLOGY_2026-09-11.md (22 items: 0
   STILL_LIVE, 7 NEEDS_REPREMISE, 5 PARKED, 8 SUPERSEDED, 0 TRANSFERRED,
   2 RETIRED), calibration/LEDGER.md (9 rows), this journal, and
   prompts/2026-09-11_seat_adoption/REPORT_ARCHAEON_old_seat.md with its
   MANIFEST. All pure ASCII (verified with grep -P '[^\x00-\x7F]').

9. Annotated agents/atalanta/CHARTER.md: the inheritance banner plus a
   dated adoption block naming the superseded parts. Insertion only --
   22 insertions, 1 deletion and that deletion is the title line the
   block was inserted after. No line of Aporia's design was rewritten.

10. Added this seat's own rows (self-service per Archaeon ruling #39):
    two in roles/base-role/INHERITANCE.md (stamped document, entry file),
    one in roles/base-role/MONITORS.md (AtalantaPrimitiveHunterLoop,
    state DEAD / present-not-active, with its input, its absent freshness
    source, its unrouted alarm and its productivity signal of 0
    dispatches in 354 ticks). Both files re-checked pure ASCII after the
    edit.

## What was NOT run

- The daemon. Not once, in any mode, including --once and --dry-run. The
  operator said set up only, and base rule 8 says a loop whose input has
  never existed is not a monitor.
- No Type-E query, no dispatch, no enqueue to any queue.
- No write to techne/registry/ (a standing hard stop, unchanged).
- No repair of daemon.py: no D-23 guard, no config-driven upstream path.
  Both are on the backlog (ATALANTA-06, -07) and both are blocked on
  ATALANTA-01, because repairing a tool that may be retired is work done
  before the decision that governs it.
- No edit to any file outside agents/atalanta/ and roles/Atalanta/,
  except the two INHERITANCE rows and the one MONITORS row the base role
  requires a seat to add for itself.
- No fix for the three unregistered M2 scheduled tasks (see below). They
  are Mnemosyne's, Daedalus's and the unclaimed machine probe's; lane
  discipline says report, not repair.
- The Necropolis investigation of this agent. That is the Keeper's lane
  and this seat is its subject; the conflict is declared in STATUS.md.

## The merged-tree test

Merged origin/main 5b9ddd540 (Ludus's adoption pass) explicitly by named
SHA per D-23 s3, then ran archaeon/tests/test_base_role.py on the merged
tree, BEFORE pushing.

First run: 2 failed, 6 passed.

- test_monitor_registry_rows_carry_every_column FAILED, and it was MY
  defect: the row I had just added carried 9 columns, not 10, because I
  merged "dormancy threshold" and "alarm route" into a single cell. The
  self-test caught a base-role violation written by the seat that had
  spent the afternoon writing up an agent that died of an unrouted
  alarm. Split into two columns; the alarm-route cell now says NONE
  ROUTED and why. Re-run: passes.
- test_every_enabled_prometheus_scheduled_task_on_this_host_is_registered
  FAILED and is NOT mine: MnemosyneEvidenceWikiWatchdogM2,
  PrometheusMachineProbeM2 and SFEngineM2Watchdog are enabled on this
  host and have no MONITORS row. Verified pre-existing: all three names
  are absent from roles/base-role/MONITORS.md at origin/main. This also
  identifies the host by property rather than label -- SPECTREX5 is M2.
  Reported to Archaeon as FINDING 4 and left for Mnemosyne, Daedalus and
  whoever claims the probe.

Second run: 1 failed, 7 passed -- the one failure being the pre-existing
M2 registry gap above. Pushed with that failure standing and named, not
with it unmentioned.

## Numbers on this pass

    commits touching agents/atalanta, ever          1 (e6b3746f0)
    days the agent ran                              about 7 (05-23 to 05-30)
    ticks                                           354   (single-sourced, M1)
    artifacts                                       354
    UPSTREAM_NOT_FOUND artifacts                    354   (100.0 percent)
    Type-E queries dispatched                       0
    primitives proposed                             0
    primitives registered by Techne                 0
    operator prompts ever addressed to this seat    0 (before today)
    old-queue items classified                      22
    of those STILL_LIVE                             0
    files created in roles/Atalanta/                6 + 2 in prompts/
    files changed outside this seat's own tree      2 (both base-role registries)
