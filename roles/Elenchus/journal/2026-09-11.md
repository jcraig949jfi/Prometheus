JOURNAL -- Elenchus -- 2026-09-11

> Inherits roles/base-role/RESPONSIBILITIES.md and WORKING_CONTRACT.md (operator, D-23, 2026-09-11); this file adds to them and may not contradict them.

First journal entry this seat has ever written. It did not have one before today;
the base role requires it, so the absence is itself recorded here rather than
backfilled. Nothing below is reconstructed: everything is from this session.

PASS 1 -- BOOT AND FIRST COMMISSIONED REVIEW (base ad94bf2eb)

Booted into the canonical checkout and refused to work there. The canonical
checkout was on branch vivarium/v0-2026-09-05, whose upstream is gone, 302 commits
behind origin/main, with 20+ modified tracked files belonging to at least six
seats. Cut a worktree instead:
  worktree F:/Prometheus-worktrees/elenchus-review, branch
  elenchus/review-2026-09-11, base_sha ad94bf2eb, clean.
Heartbeat written as Elenchus/M2 with branch and base_sha in status_json.

Commissioned work: TECHNE-38, the adversarial review of
techne/acquisition/GAP_FIXTURE_SDP.json case C_illcond_1e10, from
roles/Techne/prompts/2026-09-10_unblock/ELENCHUS.md. Prompt hash verified after
the fact against the MANIFEST beside it:
  sha256 2e0ef26269e0c2fd35c3f8a2b13e0d6acd29990cd928c2de6333110e4783568b  matches.

What was run (cvxpy 1.9.2; CLARABEL, SCS, SCIPY, HIGHS, OSQP installed):
  - feasibility of the declared point X = I/m: trace 1.0 exactly; min eigenvalue
    of (I/30 - 1e-6 I) = 0.03333233 > 0, so STRICTLY feasible, Slater holds, and
    the feasible set is compact. Techne's load-bearing claim survived.
  - closed-form optimum, which the fixture never computed:
    p* = eps*trace(C) + (1 - m*eps)*lambda_min(C) = 18250.35288865741,
    verified by constructing X* = eps*I + (1-m*eps) v_min v_min^T, which attains
    trace(C X*) = 18250.35288886 at trace X* = 1.0.
  - reproduction as posed: CLARABEL infeasible_inaccurate, SCS unbounded. Both
    are therefore provably wrong answers, not readings of a degenerate problem.
  - reposings: variable shift alone still fails both. Objective rescaling by any
    norm succeeds. CLARABEL rel err by normaliser: lambda_max 2.6e-4, Frobenius
    2.8e-4, max|C_ij| 5.9e-4, trace(C) 1.9e-3, round 1e10 2.6e-4, all "optimal",
    ~50 ms.
  - 10-seed family at spread 1e10: rescaled CLARABEL solves all 10. UNSCALED,
    CLARABEL solves seeds 4 and 5 to 9 significant digits and reports "optimal";
    the other 8 scatter across five distinct failure modes. seed=7 is hardcoded
    in the fixture and never reported in its output.
  - spread sweep 1e4 to 1e14: rescaled CLARABEL optimal throughout, accuracy
    plateauing at ~3.2-3.6 significant digits from cond 1e6 upward.
  - SCS after rescaling reports status "optimal" and returns 52618 against 18250,
    188% wrong, at every spread from 1e6 to 1e14. The fixture's failure criterion
    is status-only and would score that a PASS.

Verdict filed: ELEN-TECHNE-38, METHOD-FLAW, invalidates-claim. Not a capability
gap; a purchase may not cite it. Record appended to engine/shadow/REVIEWS.jsonl
(31 lines, all valid JSON, checked).

Integration: origin/main moved twice during the work (ad94bf2eb -> c13867fcc ->
aee89ff8b). Fetched and merged each named SHA explicitly, never pulled; first push
was rejected as non-fast-forward, second landed. Before pushing, confirmed the two
review target files were byte-identical between base and tip, so the review was
not filed against a ghost. Pushed at 96a22e736 and verified it is an ancestor of
origin/main.

NOT RUN, and it matters: whether MOSEK solves the unscaled instance. No licence
here. It is not load-bearing -- the free path solves it once scaled -- but the
counterfactual stays untested and is carried as ELEN-14.

PASS 2 -- INHERITING roles/base-role (base 0da63fa17)

Read at SHA 2cfc66aed: roles/base-role/RESPONSIBILITIES.md, WORKING_CONTRACT.md,
INHERITANCE.md, then aporia/doctrine/critical_memories.md (131 lines, HARD-1..6),
then the only prompt addressed to this seat (Techne's, already executed above).
The Elenchus row in INHERITANCE.md is "RESPONSIBILITIES.md (already)", not a stub,
so no lane section is owed. My seat file differs from the version I booted on by
exactly the two stamping lines and nothing else.

D-23 housekeeping done: previous task branch verified an ancestor of origin/main,
then worktree removed and branch deleted. Remote branch never existed (the push
was HEAD:main), so the remote delete correctly errored and nothing was orphaned.
New worktree F:/Prometheus-worktrees/elenchus-baserole, branch
elenchus/base-role-2026-09-11, base_sha 0da63fa17, clean.

Work done this pass: the seat infrastructure the base requires and this seat never
had -- this journal, STATUS.md, BACKLOG_H0H5.md (22 items) -- plus dated
supersession annotations on the two documents whose git instructions contradict
D-23. Both annotations keep the original text visible beneath them; nothing was
silently rewritten.

THE FINDING OF THIS PASS, which nobody has recorded before: the loop this seat
exists to shadow is dormant. engine/shadow/WORKLOG.jsonl last grew on 2026-09-01
at pass 2026-09-01T00:00Z-P177 and is unchanged on origin/main since, checked
against origin/main directly rather than a local checkout. 187 passes stand
unreviewed and three reviews stand unanswered. A shadow reviewer with no loop to
shadow is a fact the program should know; it is ELEN-03 and goes to Aporia and the
operator as a committed prompt.

DEFECT HIT WHILE FILING THIS ENTRY, NOT MINE, ALREADY REPORTED
`git add roles/Elenchus/journal/2026-09-11.md` was refused: .gitignore line 259 is
`journal/`, which silently ignores the dated journal directory the base role requires
of every seat. Vivarium found the same thing today and filed it to Archaeon at
aee89ff8b (roles/Vivarium/prompts/2026-09-11_journal_gitignore/). No duplicate prompt
from this seat; this entry seconds it, with a second independent occurrence as
evidence that it hits every seat and not just one. .gitignore is not this seat's file
to change. Interim: this journal is committed with `git add -f`, which is recorded
here so that the force-add is visible rather than silent.
