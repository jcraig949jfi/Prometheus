# Vivarium journal 2026-09-16

## Pass: first boot on M2 -- consumer found dead 35.9 h, store guard, THEO-REQ-004/-006, dead-man, M2 prepared (m2-fce3fe0b, 11:4x-12:4x UTC)

Built from ccb26df01 in D:\Prometheus-worktrees\vivarium-boot-2026-09-16,
branch vivarium/boot-2026-09-16, dirty no. Landed on main by fast-forward
(the SHAs below); every push verified `merge-base --is-ancestor`.

### Boot
- Canonical checkout refused (git-dir == git-common-dir at D:\Prometheus);
  worktree created from origin/main ccb26df01 (43,352 files, ~4 min).
- `comms boot Vivarium` REFUSED first (WRONG_ENVIRONMENT: localhost on M2 is
  the fork, signature c84e26826cc12217) -> EW_DB_HOST=192.168.1.202, booted
  as Vivarium[m2-fce3fe0b]; occurrence line added to Hermes's incident file.
- Sync: 2 new (#262/#263 Nestor: M1 reboot 09-14 19:55 local), queue 8.

### The consumer (measured, UTC, canonical store)
- vivarium@m1 last heartbeat 2026-09-14 23:53:44Z; pid 13460 gone; no park;
  stranded 0; queued 5 (Archaeon, 09-15 03:27:10Z .. 20:12:09Z); completed
  579 / failed 79 / cancelled 492; last row 8bc6b162 done 23:27:07Z.
- M1 8811 and 8377: connect times out; 22/5985/5986 closed; 445/5432/8799
  open. M2 8811 LIVE (started 11:48:31Z by Daedalus's relocation):
  eng_906356f7, hash 726275da, schema 8. M2 8377 LIVE (mnemosyne-ew).
- Receipt receipts/CONSUMER_DEATH_2026-09-14_M1_REBOOT.md; report #265 to
  Archaeon + Daedalus (55a26268a). Archaeon #267 had measured the same
  numbers independently. Daedalus #270 RULING: production = ledger
  eng_8a37a5d3 wherever it runs; twin never promoted; hold the 5 rows.

### The hazard found while measuring (C12)
- `viv.cli status` on M2 with no VIV_DB_HOST reached the fork
  (7681719240261676752) and failed only on `viv.worker_heartbeat does not
  exist`; `run` applies migrations at start and would have created it.
- Fix a6d1ba114: viv/db.connect() -> comms.identity.require(); schema
  `viv` pinned to prometheus-canonical; VIV_DB_ENVIRONMENT honoured only
  off-production; import failure refuses; connection closed on refusal.
  tests/test_db_identity.py 7/7. Live: the same command now prints the
  REFUSED block with the signature.

### THEO-REQ-004 (#246) -- 6d21bd5ef, a9f81c7d6
- Cause: executor declared `_truncated` only when the bound bit; at exactly
  64 wrong the validator (correctly) refused an undeclared vector at its
  ceiling. Fix: ca_density.py and cegis_boolean.py declare both ways.
  Neither the library's `>` nor the validator's rule moved.
- success_mask_hex added (core.pack_mask_hex; parity refusal against
  classify()'s correct_mask_hex; unpack round trip). Fixture 4f211943 ->
  3655c564, arithmetic byte-identical (stated in test_wp0f_fixtures.py).
- One existing test (test_ca_cellwise_criterion) now pops the declaration
  channel the way executors.run does. Reply #280; `done 246`.
- Merged Herakles's dbc41fd2f mid-pass (library half; same encoding chosen
  independently) -- conflict in the incident file resolved keeping all
  occurrence lines (e908e4498).

### THEO-REQ-006 (#248 / Herakles #273) -- 6358acea9
- {"count": k} entries; refused at the executor's entry with n_cells; 15
  controls including the cheat that exact-count != Bernoulli(k/N).

### C11b dead-man + M2 preparation -- f249ae21c
- viv/deadman.py: verdicts LIVE/BUSY/DEAD/PARKED/STOPPING/NEVER; BUSY via
  tasklist pid on this host; rule 9 engine identity before launch; rule 10
  bound 3 -> park + self-disable + one post. 13 controls.
- Live probes (observe-only, --no-launch, scratch var dir):
    12:13:06Z  vivarium@m1 DEAD age 130,751.6 s; upstream UNREACHABLE at
               https://192.168.1.202:8811/v2/version; NOT relaunched (1 of 3)
    12:13:14Z  same row; upstream https://192.168.1.191:8811 -> WRONG_ENGINE
               observed eng_906356f7 expected eng_8a37a5d3; NOT relaunched (2 of 3)
- deploy/vivarium_consumer_m2.cmd, vivarium_deadman_m2.cmd, prepare_m2.py.
  Precondition functions exercised live: store OK (queued 5, environment
  prometheus-canonical), PEW OK (uptime 354.6 s -- it had restarted since
  my 11:5x read at 632.8 s; Mnemosyne's), engine WRONG_ENGINE, secrets
  ABSENT (no config.local.json in this worktree).
- VIV_PEW_BASE_URL env override in viv/db.py.
- MONITORS.md: consumer row annotated DEAD; rows VivariumConsumerM2
  (PREPARED, NOT LAUNCHED) and VivariumDeadmanM2 (REGISTERED DISABLED once
  prepare_m2 --register runs) added. Currency line updated.

### Test runs (all against the canonical server, VIV_DB_HOST set)
- after the guard: 542 passed / 41 skipped; after REQ-004: 553; after the
  library merge + parity: 554 (herakles 166); after REQ-006: 569; after the
  dead-man: 582 passed / 41 skipped, 46.4 s. Skips are the live SFE/PEW
  tiers (the M1 engine is down).
- Push discipline: main moved three times during merge/test; the final
  push (3cf3ffa9d) followed a 47-test targeted run after a 2-file merge
  from Mnemosyne outside vivarium/; the full suite had run at the previous
  merge point. Said here so it is not read as a full run.

### Not done
- Any launch. Any cancel. #186, #190, #202, #215, #240, #247 untouched
  (queue order after the reboot work). prepare_m2 --register not yet run
  (runs after this pass's SHA is on main so the pin names it).
- Whether M1's engine was written after 09-15 20:12Z (Daedalus's question;
  M1's receipts).

### prepare_m2 run (12:21:22Z, pinned to 3aa05e08b) -- receipts/PREPARE_M2_2026-09-16.json
- worktree D:\Prometheus-worktrees\vivarium-consumer CREATED detached at
  3aa05e08b (43,430 files), clean, not canonical. Launchers written to
  D:\Prometheus-data\vivarium\ (sha256s in the receipt). Tasks registered:
  VivariumConsumerM2 Ready (on demand), VivariumDeadmanM2 DISABLED.
- preconditions: worktree OK, store OK (queued 5), PEW OK (uptime 330 s),
  engine WRONG_ENGINE (twin), secrets ABSENT -> ready_to_launch false,
  exit 1, by construction. Nothing launched.
- The pinned dead-man launcher run once by hand (what the task would run):
  12:22:28Z consumer NEVER + WRONG_ENGINE, NOT relaunched, 1 of 3;
  state file var\deadman-vivarium@m2.state.json written. That count is
  live state: a later productive tick resets it.
- Operator gate written: prompts/2026-09-16_m1_reboot/OPERATOR_GATE_TOKENS_TO_M2.md.
- Closing sync 12:22:58Z: 2 new (#283 Proteus re REQ-003/005 -- informational;
  #284 Archaeon HOLD confirmed on main), 0 queued; queue length 6 (#186,
  #190, #202, #215, #240, #247). Posted #288 (ack to Daedalus/Archaeon/
  Harmonia) and #289 (REQ-006 reply); done #246, #248, #273.

### 12:3x-12:5x UTC: operator topology ruling; Herakles #294
- Operator (chat): Postgres and Redis shared on M1; every other service on
  one machine only -- the SFE ecosystem on M2 now; no farm yet. Recorded
  in STATUS. Ledger question (a)/(b) and the token gate asked in chat;
  proceeding on (a).
- #294 (Herakles): base-role self-test red. My two MONITORS rows had a
  parenthetical seat name and a head the task matcher could not read ->
  fixed; 11/11 from M2 with the host set. The 11 setup errors without
  EW_DB_HOST are archaeon/tests/conftest.py's autouse fixture reaching
  the quarantined fork -- the guard refusing it is correct; reported to
  Archaeon (not edited). Reply file REPLY_HERAKLES_294_BASE_TEST.md.
- Re-probed 12:48Z: M2 engine still eng_906356f7; no M1 data dir landed
  on M2; no config.local.json in the pinned worktree.

### ~13:0x UTC: operator ruling -- Q1 (a), Q2 copy both tokens, no minted identity
- Recorded verbatim in prompts/2026-09-16_m1_reboot/OPERATOR_RULING_LEDGER_A_TOKENS_COPIED.md.
- Probed for a copy path from M2: \192.168.1.202\F$ / D$ / named shares all
  False; `net view` error 5 (access denied). The copies are the operator's
  hand. Nothing was read.
- Armed a watch for the two launch preconditions (token file present in
  the pinned worktree; /v2/version reports eng_8a37a5d3); on both, run
  prepare_m2 --register and enable the dead-man.

### 13:1x-14:4x UTC: D2 closed (admission-time VALUE validation)
- Kind.value_checker; ca_density.payload_problems shared by admission and
  run(); the `"ic_density_set": null` payload that lost 24 rows is refused
  by spec.validate, by enqueue (no row), and by loop stage 2 on a pre-D2
  row (before BUILD). 18 controls (positive x3, negative x11, cheat x3).
  Tests that expected the executor's EvcaError through executors.run now
  assert the admission refusal AND the executor's own, both paths.
- vivarium suite: 599 passed / 41 skipped. archaeon/tests on this tree: 1
  failure, environmental and pre-existing (test_the_configured_sfe_ledger
  _exists_on_this_host: no engine.db / archaeon config.local.json in this
  worktree), identical with my change stashed.
- Launch watch re-armed 14:16Z (30 min windows); at 14:16Z neither the
  token file nor eng_8a37a5d3 on M2 had appeared.
