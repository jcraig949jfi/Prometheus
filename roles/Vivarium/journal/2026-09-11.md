# Vivarium journal -- 2026-09-11

> Inherits roles/base-role/RESPONSIBILITIES.md and WORKING_CONTRACT.md
> (operator, D-23, 2026-09-11).

First journal file for this seat. The base role requires one per pass;
this seat had none, which is itself the first thing the inheritance
surfaced.

## Pass: inherit roles/base-role

Read at `2cfc66aed`, verified unchanged since that SHA
(`git diff --stat 2cfc66aed origin/main -- roles/base-role` is empty).

    worktree_path  F:/Prometheus/.claude/worktrees/vivarium-campaign-e1-e6-e16
    branch         worktree-vivarium-campaign-e1-e6-e16
    base_sha       c13867fcc  (equal to origin/main at read time)
    dirty          no
    canonical      no (git-dir != git-common-dir)

Inheritance register: my row is not a STUB. `RESPONSIBILITIES.md` and
`CHARTER.md` both exist and both carry the line-2 banner, so I owe no
lane section.

## What the inheritance changed for this seat, in order of force

**1. WORKING_CONTRACT s8 is a live non-compliance, not a to-do.** The
consumer I restarted at 04:38 today runs against the engine without the
four-state conformance gate. Archaeon's half is on main
(`archaeon/conformance.py`); mine is not written. It was already an
operator directive in
`roles/Vivarium/INBOX_ARCHAEON_CONFORMANCE_WIRING_2026-09-11.md` and is
now also an inherited contract clause, and the next newly issued corpus
(C3-3) waits on it. `grep -rn conformance vivarium/` returns nothing.

**2. Doctrine s2: "A verdict ships in the same commit as its rows."** I
shipped the orphan classification this morning as counts inside two
inbox files. The 119 classified rows live in a session scratchpad and
are not committed. By the inherited definition that verdict is currently
an assertion. This is the cheapest item on the list and the one I should
not have needed telling.

**3. Journal and STATUS.** Neither existed. This file is the first half;
`STATUS.md` is owed.

**4. Section 4 changes the FORM of my escalations.** My RESPONSIBILITIES
says a stranded row is reported and never resolved by inference. The
base agrees but no longer lets "report it" be the end of the pass: I owe
the prompt that would unblock it, addressed to the seat that owns the
blocker, committed under `roles/Vivarium/prompts/` with its sha256.

**5. Two controls per change.** New obligation: a positive control and a
cheat control where the change is measured. My recent test files carry
negative controls (`tests/test_library_leak.py` is built around them);
"cheat control" is a stronger and different requirement and I do not
yet meet it by default.

## Compliance items I checked and passed

* No hardcoded drive letters in `vivarium/viv/*.py` or `config.json`
  (grep clean). Committed prose does carry host paths; so does
  WORKING_CONTRACT s1, which declares the path the operator's convention.
* Long-running process runs from a pinned detached worktree,
  `F:/Prometheus-worktrees/vivarium-consumer`, advanced today by an
  explicit logged command after tests (519 passed, 1 skipped) -- s6.
* Push-then-verify was used on both of today's commits (s4).
* Commit by explicit paths with a message file on both (s5).

## Conflict I cannot resolve myself

WORKING_CONTRACT s2: "Never place a worktree under the canonical
checkout's own directory". This session's authoring worktree is
`F:/Prometheus/.claude/worktrees/vivarium-campaign-e1-e6-e16` -- under
the canonical clone. It is harness-assigned, not chosen. The canonical
guard still passes correctly (git-dir != git-common-dir, so it is a real
worktree and not the main one) and no mutating git operation has touched
`F:/Prometheus` itself. Flagged for an operator ruling; backlog C5
already carries the related note that a `.claude/worktrees/` path is
prunable.
