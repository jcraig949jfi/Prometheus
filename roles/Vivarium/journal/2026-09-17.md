# Vivarium journal 2026-09-17

## Pass: point-release Stage 0/1, READ-ONLY (m2-fce3fe0b)

- Operator: Campaign 3 running, change nothing; reviewed the point-release
  directive in chat; Operator Amendment 1 narrowed scope and named my first
  two deliverables. No watch armed, nothing registered/launched/migrated;
  HOLD #315 stands (C3 is writing to eng_906356f7 in practice).
- Read: campaign1/2/3 ledgers + reports (execution-layer entries), c2 runner
  + c2base + c3base, wse/reachability row identity, my own migrations, spec,
  runner, loop, pew, request. Merged origin/main into this worktree to read
  current files (my worktree only).
- Wrote roles/Vivarium/point_release/VIVARIUM_EVIDENCE_INVENTORY.md (Stage 0
  + Stage-1 candidates classified) and IDENTITY_TRANSLATION_CONTRACT.md v0.1
  (section-D table with OWNER / CARRIER / UNKNOWN / N/A / ABSENT, the join,
  seven ambiguities for Stage 3).
- Key findings: one measured replay defect in three campaigns (L2-021) and it
  was a keying gap; attempt/step/replay live in archaeon/campaign2/runner.py;
  "experiment_id" names two different objects across the seats (A1);
  event_id has three owners and no join (A6); foundry/lineage/organism slots
  wait on Proteus (A7).

## Pass 2: Stage 1/2 direction after Campaign 3 (READ-ONLY; design + drafts)

- Read archaeon/campaign3/CAMPAIGN_REPORT.md (527 lines): 0 summits, shelf =
  one-value memory, ladder builds delay invariance, takeover = mechanics,
  two geometry claims dead; machine: 1 resume (slot 09, 29 steps), 0 full
  recreations, POSITIVE_CONTROL_FAILED twice (03, 06) on a fixed-constant
  gate; L3-016/029/039 block future runs.
- Wrote roles/Vivarium/point_release/: EXPERIMENT_TRANSACTION_MODEL.md,
  START_BUNDLE_SCHEMA.md, INTERVENTION_RECEIPT_SCHEMA.md,
  PREREQUISITE_GATE_RECEIPT.md, TERMINATION_ENVELOPE.md, PEW_OUTBOX_DESIGN.md,
  PRODUCTION_DESCRIPTOR.md, STAGE2_SELF_CRITIQUE.md (3 MODIFY, 3 ADD applied).
- Migration DRAFTS 006-009 under vivarium/migrations/drafts/ (not globbed by
  apply_migrations); check_drafts.py applied them to a throwaway schema and
  exercised 15 trigger cases: 15/15 ok, schema dropped. Production schema
  untouched; nothing deployed, registered or launched.
- The one semantic change proposed: BUDGET_EXHAUSTED / HORIZON_REACHED become
  COMPLETED-and-censored instead of FAILED, for attempts opened after the
  window only; old rows are backfilled UNKNOWN, never parsed.

## Pass 3: Stage 3 -> implementation -> scope freeze -> window plan

- Implemented the whole track on throwaway schemas (attempts/steps, NEW ATTEMPT,
  bundle, receipts, envelope, outbox + deliverer to PEW's /api/v1/events,
  descriptor + restart gate, conditional engine labels); synthetic
  qualification passed all eight s16 answers (receipts/QUALIFICATION_SYNTHETIC).
- Rehearsed the window on a COPY of the real viv rows (rehearse_window.py):
  1,155 old rows byte-identical over their columns; 1,150 attempts backfilled
  UNKNOWN; 0 steps fabricated; only new column bundle_declared (all NULL);
  terminal rows still frozen; throwaway dropped. Receipt WINDOW_REHEARSAL.
- Wrote deploy/window.py (receipted, refusing steps; runs only under
  --confirm) and SCOPE_FREEZE_AND_WINDOW_PLAN.md. Found that Archaeon's five
  HELD rows (#284) have not_before NULL -- a restart would claim them. Added
  `viv.cli hold` (not_before only, event logged, 3 controls) and a restart
  refusal on unheld foreign rows.
- Daedalus #345 (0.13% engine stalls 5-13 s): consumer timeout is 60 s;
  transport failure -> attempt FAILED/ENGINE_TRANSPORT -> NEW ATTEMPT over
  replayed steps; in-place retry named OUT of scope.
- main 7b9471917; comms #346 (freeze + window request + asks to Archaeon,
  Daedalus, Mnemosyne). Production untouched; consumer down; window NOT open.

## Pass 4: window C4-20260917-W1 (operator PRE-CAMPAIGN-4 REPAIR ORDER s2)

- 13:46Z backup (24.0 MB, pg_restore-verified) -> 006-009 promoted and applied
  to production viv (1,155 rows unchanged over 28 old columns; 1,150 attempts
  backfilled UNKNOWN; 0 steps fabricated; frozen rows refuse live). Archaeon's
  five rows held to 2027 (viv.cli hold). Pinned worktree advanced; deliverer
  task registered; identities vivarium / vivarium-test bootstrapped once on
  eng_906356f7; pew_token made an OPTIONAL descriptor key.
- 13:52Z dead-man ENABLED -> consumer up, restart receipt ok. s13 fixture at
  the deployed SHA: 16/16.
- s14 canary, nine runs. Ten production defects, each fixed with an acceptance
  test that reproduces the engine's real behaviour (the doubles had been more
  permissive than the engine): dead-man LIVE-for-15-min after a kill; engine
  SESSION_MISMATCH for a relaunched process (session key held host-locally,
  adopted); no ATTEMPT_TERMINATED on release; WORK_NOT_CLAIMABLE on a
  completed work item (claim is a keyed step); 409 on re-posting an
  observation whose step result never landed (recovery by content); 4xx
  classed as the halt class ENGINE_TRANSPORT (now ENGINE_REJECTED); deliverer
  parking on "no credential" (now a HOLD); schema-9 page objects iterated as
  dicts; a transport-failed row frozen with no re-attempt (migration 010);
  Idempotency-Key collision across rows of one design (engine key scoped to
  the row).
- 15:50Z a REAL 60 s engine stall on POST observation (write landed): the
  consumer parked per rider #136 and paged Daedalus (#365); evidence sent
  (#366); cleared; the row recovered under 010 in attempt 3 with 12
  observations in ONE world (one RECOVERED by content).
- READINESS_DISPOSITION.md: QUALIFIED_FOR_CAMPAIGN, DEPLOYED_AND_QUALIFIED,
  caveats named. MONITORS rows for the three M2 tasks (Harmonia #356).
- Comms: #346 freeze, #355/#361/#366 to Daedalus, #362 to Harmonia.
