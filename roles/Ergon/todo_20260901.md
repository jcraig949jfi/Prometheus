# TODO for Ergon — from Mnemosyne (2026-09-01)

**Status: OPEN — first step is on MNEMOSYNE, not you; read before acting**

## Context: kill_taxonomy was never migrated, and that's my defect

`prometheus_fire.kill.taxonomy` is empty NOT because the data was thin:
`mnemosyne/migrate_m2.py:231-248` (`load_kill_taxonomy`) reads DuckDB's
`hypothesis_queue` instead of the real source `forge/v3/kill_taxonomy.db`
(SQLite), logs "needs manual mapping", and returns 0 as a success. The 21
kill records + 10 negative_dimensions have never left SQLite. Found in the
2026-09-01 database inventory; logged as a Mnemosyne-owned defect.

## Your outstanding item (BLOCKED on me — sequence matters)

1. **Mnemosyne (me):** fix the loader and migrate the 21 kills + 10
   negative_dimensions into `prometheus_fire.kill.*`, with provenance.
   Awaiting James's go-ahead; nothing for you to do yet.
2. **Ergon (you), after I flip my step to DONE here:** repoint the
   kill-taxonomy consumers off SQLite to Postgres:
   - `ergon/tensor_executor.py:33,131` — the hypothesis pre-filter gate
     (`check_hypothesis_against_kills`) currently reads SQLite live
   - `ergon/autonomous_explorer.py:30`
   - `forge/v3/autonomous_explorer.py:22` and the module itself,
     `forge/v3/kill_taxonomy.py:6,11,19,120,136`
   Until both steps land, SQLite stays authoritative for kills — do NOT
   dual-read or hand-copy rows.

## When you're done

Flip **Status** above to `DONE <date>` + one-line note (commit SHA), and
commit. I sweep `roles/*/todo_*.md`. The SQLite file is deleted only after
your flip, verified row-for-row against Postgres first.

— Mnemosyne (DBA & Data Steward)
