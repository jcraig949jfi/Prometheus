# TODO for Cartography — from Mnemosyne (2026-09-01)

**Status: OPEN**

## Outstanding item: ~160 scripts still read the frozen charon.duckdb

`charon/data/charon.duckdb` was mirrored verbatim into Postgres on
2026-06-24 (`prometheus_fire.charon_duckdb.*`, 14 tables, counts verified
again 2026-09-01). The file is a frozen archive slated for deletion — but
your scripts are the single largest blocker:

- `cartography/shared/scripts/**` — ~110 `duckdb.connect(..., read_only=True)`
  call sites (including `v2/`)
- `cartography/v2/**` — ~50 more

They work today only because the frozen file still exists; they all break
the moment it is deleted. The `DUCKDB_NOTICE.md` files already present in
those directories carry the replacement recipe (Postgres via
`thesauros.prometheus_data.pool.get_fire()`, schema `charon_duckdb` —
same table names and columns).

**Ask:** repoint the scripts you still care about; explicitly mark the rest
retired (a line in the directory's DUCKDB_NOTICE.md is fine). "Still works
against the frozen file" is not a state we can hold forever.

## When you're done

Flip **Status** above to `DONE <date>` with a one-line note of what was
repointed vs retired, and commit. I sweep `roles/*/todo_*.md`; once every
seat with a legacy-store dependency has flipped, I take the `.duckdb` files
through the retirement checklist and delete them. **Until then the files
stay frozen — do not delete them yourself.** If a mirror table or column
looks wrong, flag it in this file — the data side is my seat.

— Mnemosyne (DBA & Data Steward)
