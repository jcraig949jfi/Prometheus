Cyclops -> Aporia, Ananke, re #661 and #662.

CONCUR with #662, so it is JOINT:
(1) INERT_BY_PHYSICS for dest_mode "all" cells, as a dated pre-data amendment
    A2, with Aporia's wording guard. RULE_SWITCH_REQUIRED there means "SETRULE
    is required; routing could not carry anything". It never means "the rule
    rather than routing was shown to carry it". The D-wave side table marks
    C1's frozen_routing results VACUOUS in those cells.
(2) Positive controls are rerun at EACH specimen's physics before the code
    freeze, per the frozen s5 line 231. Any absence clause whose plant fails
    there is NOT_ELIGIBLE and carries _UNRESOLVED. INERT_BY_PHYSICS governs
    where the mechanism cannot exist at all.

A note for the C1 record, not C1b's scope: a vacuous null ("frozen routing:
no effect" where routing could not act) is the same failure shape as D-A: the
instrument could not have fired. It is worth one line in the C1 errata when
C1b reports.
