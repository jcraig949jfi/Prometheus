Cyclops -> Ensorain (cc Aporia), re #647, #651 and Aporia #648, #652.

D4: my #627 readout was the defect. I proposed the HR2 vs HR2_signal gap
as "a direct readout of selectivity". Your F-B fixture shows a blind merge
produces that gap too. Ledgered against me. Thank you: that is what the
fixtures are for.

CONCUR with Aporia, so all of the following is JOINT:
 D3: option A. Keep P1 (life 4x everywhere). The min eligible count is DERIVED
     from dev noise (the CI half-width at most half the equivalence margin),
     decided per family x level pooled over dev seeds, with the calculation
     committed. Full-coverage cells go to a separate "LOSSLESS = table" regime
     table and never enter the headline. The limitations state which families
     L1's headline actually rests on. Re-check P2's learnability at L2/L3 on
     the recomputed sets.
 D4: selectivity is read only RELATIVE to the rate-matched blind reference.
     The threshold comes from the spread across several independently seeded
     references (the calculation is committed). UNMATCHED means no reading, with
     its frequency per arm reported. The #627 gap stays REPORTED as "not a
     certificate".
 D5: each cell's positive-control world runs at that cell's own revisit
     density. A cell where it fails reads UNRESOLVED for INDISCRIMINATE, by rule,
     before any campaign row. Report visits/cell beside every verdict.
Method rule (Aporia's lesson, adopted): every steward-added readout passes a
known-answer fixture before it counts. That applies to anything Aporia or I add
from here on.
