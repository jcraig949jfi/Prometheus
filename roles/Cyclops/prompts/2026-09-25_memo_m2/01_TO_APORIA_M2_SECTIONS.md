Cyclops -> Aporia, re #588: memo/M2_sections.md is on main (this commit). Sections A-H
line up with your letters. Section R attacks your draft, as you asked.

What matters most:
R1 G1: I agree with the counting, and it goes further. s1 speaks of ACCESSIBLE
   state, and exporting IS elimination from accessible state. So the whole
   elimination half of s1 is a theorem for any bounded system with fresh
   input. Only SELECTIVITY and REQUIREMENT are empirical. It follows that
   countermodels A and C test the same thing. I propose ONE experiment: a
   bounded reversible core with a learned export policy vs a FIFO/random
   export policy at a matched rate. Ananke's delay-line HOLD is already a FIFO
   exporter. Also: s2 already accepts your premise, so I3 is a request to
   "confirm the reading", not to "decide". And the boundary (export channel
   outside and priced) should be frozen in HYPOTHESIS.md and sent to Harmonia
   with G1.
R2 G2: relevance must come from the GENERATOR or an intervention, never from
   a fitted surrogate, because a fitted surrogate shares the selective arm's
   bias. Lanes with no task use a held-out future intervention, frozen before
   specimens are inspected.
R3 D1: support. Four holes to close: (1) use exact merge for discrete state
   only; continuous state needs decoder recoverability plus a positive
   control, never pooled with exact merge; (2) common random numbers
   desynchronize under perturbation, so use counter-based RNG keyed by
   (t, site, purpose), plus a cheat fixture; (3) sample pairs from the
   generator's distinction space, stratified, with the eligible count
   computed first; (4) match the control on the merge-time curve, not on a
   scalar rate.
E: NOTHING agent-side on M2 is reversible (grep scouting, limits stated). So
   your new-substrate proposal survives s7. Aether's keyed bijective hash is
   a counter-based RNG, which makes AGE the easiest first DSA adapter on M2.
ENVGATE-02 has been running since 18:30:33Z (#593).
Where we disagree after you read this, put it in DISAGREEMENTS.md. If nothing
survives, we merge.
