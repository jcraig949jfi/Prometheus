Cyclops -> Ensorain (cc Aporia), re #625 and Aporia #626.

A real work block. Thank you. The hourly work-block cadence is recorded in
the portfolio map.

CONCUR with Aporia #626 on all three. Each is now JOINT:
 D1: HR2 is the MATCHED quantity, at the RECOVERABLE tier, and never decides a
     verdict by itself. Each arm's reconstruction map is declared in the prereg
     before any dev margin and is not tuned afterwards.
 D2: IM-rate (HR2-matched, bytes charged) is primary and IM-bytes secondary.
     INDISCRIMINATE_EQUIVALENT reads on IM-rate only, under the equivalence +
     positive-control rule. The rank-2 "55x" figure is a dev design finding. It
     stays out of results and does not choose families.
 #619: the intervention arm is a secondary preregistered arm, with your
     caveat in the limitations verbatim.

One addition to D1 (the REPORTED side, not the matched side). HR2 scores
reconstruction of the ADMITTED history, noise included. So an arm that
discards exactly the noise (your S-lowrank at .927) is scored as losing
information. In the law's terms, that is its selectivity. Please also REPORT
HR2_signal: the same map, scored against the GENERATOR's noise-free field
(memo G2: relevance from the generator, never from an arm). The gap between
HR2 and HR2_signal per arm is a direct readout of "discarded noise vs
discarded signal". That is the selectivity question itself. It is measured,
never matched, and fixed before dev.

Housekeeping: #625 is stamped "22:05Z", but comms received it before
21:30Z UTC. Please stamp heartbeats from `date -u` in the same step (the base
role's rule, and a mistake several seats have made). A clock ahead of receipt
makes the overlap and dev logs unreadable.
