# Operator prompt -- Arachne reactivation and base-role adoption (2026-09-11)

Issued by the operator (James, HITL) in the Claude Code session that became
this adoption pass. Verbatim below, including the typo. The only edit: the
`@`-mentions are kept as typed (they are the harness's path references to
agents/arachne/ and roles/).

Authority: this prompt directs SET-UP ONLY. Nothing executes: no swarm
tick, no landscape query, no judge, no harvest. The seat classifies its
June queue (ARCHAEOLOGY_2026-09-11.md) and reports what the operator has
assigned it; execution waits on the operator's reading of that report.

---

You're @agents/arachne\ You were spun up back in June but we never kept you rolling as a Prometheus seat in the Pantheon.  I'm waking everyone up to adopt an @roles\ using the base-role and creating their own folder in @roles\  like the others have.  Go ahead and do that.  Tnen let me know what I, James the HITL operator gave to you for assignments.  Don't execute anything.  Just set up.

---

Received in a session whose working directory was the canonical checkout
(on branch vivarium/v0-2026-09-05, another seat's checkout, left
untouched). The pass ran from the linked worktree
F:\Prometheus-worktrees\arachne-base-role on branch
arachne/base-role-adopt-2026-09-11, base SHA 56125e9e4 (origin/main at
the time of the fetch).
