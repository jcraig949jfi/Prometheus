# Cosmos / CWE -- campaign handoff, 2026-09-23

Charter: roles/Cosmos/prompts/2026-09-23_charter/00_DIRECTIVE_verbatim.md (MANIFEST verifies).
Window: 10:54:38Z -> 18:54Z (every time here is `date -u` or a receipt stamp). Seat Cosmos[m2-6ed01908], M2.
Branch cosmos/cwe-c0-2026-09-23 (from origin/main 371d22952), pushed; fast-forwarded into main at
ed84a6c59 (15:33Z) and again at the end (s11). Evidence words as in the charter s XXIII:
RAN / OBSERVED / INFERRED / CONCLUDED; PROVISIONAL, CANDIDATE, QUALIFICATION, HELD-OUT, FALSIFIED.

## 1. The question and the answer

Q (charter s XXVI): can limited active experimentation over three structurally different executable
worlds recover a compact, normalized phase-boundary relationship that survives deliberate counter-
example construction and correctly predicts a preregistered intervention in a sealed fourth world?

A (QUALIFICATION, all substrates written by one author): YES WITH CAVEATS -- not on the first try.
- C0 (declared coordinates C, N, K, G): NO surviving law. The adversary killed 3 laws and exposed two
  missing or mis-stated coordinates: logger CAPACITY and the hazard AFTER native repair.
- C0b (+ capacity Q, + post-repair N): one law survived the adversary, was frozen, predicted sealed D
  (Langevin wells) BA 0.983 and sealed E (agent swarm, sealed AFTER the freeze) BA 0.972, and predicted
  fresh-world interventions on both (G6b, G6E). Its first intervention test (G6) FAILED 0/12 through an
  engine defect and stays FAILED.
- A new LOCATION attack showed that law's cost ceiling is off by +0.045 in C and that per-family
  boundary offsets up to 0.15 log2 cancel when pooled. C1 (expected-cost coordinates + boundary data):
  no law survived the location gate. C2 (location-aware selection): a law with the task-economics
  ceiling C <= G exp(-N) - 0.102 and per-family offsets <= 0.015 log2 survived and was frozen.
- Sealed F (clone): both frozen laws pass (A 0.930, B 0.955); B's advantage is NOT significant
  (10 vs 10 discordant worlds); B removes A's gross coordinate errors and keeps boundary noise.
- "Active" experimentation did not earn its complexity: the kernel active sampler ties random (P4
  lost); the cost-line boundary sampler: see s7 (eta2).
- The recovered law is functionally the ECONOMICS OF THE SHARED TASK (97.5% agreement with a hand-
  derived law on D and E). What is qualified is the INSTRUMENT: it recovers a planted compact law across
  substrates, finds missing variables adversarially, transfers the law through declared coordinates,
  and refuses laws that fail. Nothing here is evidence about intelligence or about reality.

## 2. What was built (architecture as it exists)

prometheus/cosmos/ (numpy only; no toolbox import; Atlas as an export target):
  contract.py      substrate contract + coordinate maps v1-v4 (coords_of) + slots/expected cost
  substrates/      regs (metered register VM; + repetition-code mechanism), ring (delay line with
                   eviction, dead packets free), ca (block CA with majority repair, finite tape)
  holdout/         well.py (D), swarm.py (E), clone.py (F); each refuses import outside the broker
                   subprocess; seal.py (once-only, CSPRNG nonce, source hash); run.py (subprocess entry)
  broker.py        freeze check, commitment check, predictions + prescriptions RECEIPTED before any
                   sealed world runs; two-sided intervention engine (intervene_fresh)
  phenomenon.py    SELECTIVE_PAYS.v1 (reads native observations only; WSE contract untouched)
  world.py         identity + evaluation on common random numbers; seed_key for matched pairs
  miner.py         restricted grammar (size <= 6; + - * / log exp(-x)), rank-order expression quotient,
                   LOLO scoring, whole-search permutation null, family dependence mu, memory-aware pool
  independence.py  AST lineage audit (shared code merges families)
  pipeline.py      Chamber (observe -> graph), lineage-grouped mining, universality gate (>= 3 lineages)
  sampler.py       Budget + private oracle; random / grid / boundary / active; costlines
  adversary.py     band / coordinate-preserving (+ metamorphic pairs) / extreme / error-seek; replicate
                   confirmation; law's own coordinate map; fresh worlds each round
  locate.py        boundary-LOCATION attack (per-family offsets)
  select.py        location-aware selection among near-tied candidates
  boundary.py      transition location/width/finite-size scans
  store.py         SQLite world graph + immutable law rows + lifecycle events + receipt chain
  quotient.py      world classes over a 4-probe battery; boundary-edge fractions
  atlas_export.py  Atlas-shaped JSONL (atlas.edge / atlas.fact)
  campaign0.py     configs c0, c0b, c1, c2 (preregistered orchestration); c0e.py, c0m.py, stress.py,
                   g6b.py, c1f.py, eta2.py (follow-up runners); report.py; runtest.py (canonical)
roles/Cosmos/: design/ (00 verbatim source, 01 WGE design, 02 as-built, PROVENANCE), campaigns/ (every
  PREREG + amendment + result + compact artifacts), calibration/LEDGER.md, journal/, STATUS.md.

Canonical check: python -m prometheus.cosmos.runtest [--full] [--config c2]. Final: PASS
(20260923T152456Z, clean 383fd90b4): 67 fast + 7 slow (planted suite + selection), broker selftests,
seals, quick campaign. External dependencies: numpy 2.4.4 (+ stdlib). scipy/sklearn not used.

## 3. Substrates and holdouts

Visible (3 independent lineages by the AST audit): regs, ring, ca -- each implements SEL / LOG / LAST
natively for one shared functional demand (cue, K distractors, ask). Sealed:
  D well   sha256 48e70965...8265  sealed 800072c1e before any mining; REVEALED in C0b (240 worlds)
  E swarm  sha256 d17ace6e...7528  sealed da57b0ac4 AFTER law A froze; REVEALED in C0e
  F clone  sha256 5e006490...4946  sealed 7c9e0d58f BEFORE C1/C2; REVEALED 15:24Z for laws A and B
D remained hidden until adjudication: its module refuses import outside the broker, the mining side is
AST-tested never to import it, predictions were hash-receipted before each reveal. Only the controls
selftest (fixed non-sealed worlds, booleans) ran before sealing. Seeds: CSPRNG nonce per holdout.

## 4. Counts

World graph (5 stores): 4,619 nodes, 2,299 typed edges (DEFORMATION_OF 1,692, COORD_PRESERVING 367,
CONTROL_OF 240), 4,733 recorded runs; plus ~18,000 private-oracle evaluations (5 x 3 x 1,200 pools),
720 sealed-world runs and ~1,000 ladder / scan / location evaluations. Receipt chains: all verify.

## 5. Laws (ledger; nothing deleted)

C0   424fa143b7 v1  ((N-K)C) <= -.158 AND C - exp(-CN) <= -.136                FAILED (15.7%)
     d45d1069e0 v1  ((C+K)C) >= .165 AND C - G exp(-N) <= -.061                 FAILED (10.2%)
     964c086e6d v2  C K >= .161 AND C - G exp(-N) <= -.087                       FAILED (23.1%)
C0b  f852d782cb v3  C - (G + exp(-N)) <= -1.055 AND (C - log Q) K >= .179        SURVIVED, FROZEN (law A)
     scars: +0.045 ceiling bias (S2b 9/9), offsets regs -.147 ca -.105 ring +.142 (LOCATE)
C1   eec9c5a571 / 9ed4a83871 / 78d3bd6297 (v4)                                   FAILED (location gate)
C2   c5cd50beb1 v4  C - G exp(-N) <= -.103 AND C K + exp(-Q) >= .512            FAILED (location, regs -.168)
     a079ad5ec1 v4  log(Q - C K) <= -.158 AND C - G exp(-N) <= -.102            SURVIVED, FROZEN (law B)
Totals: 9 proposed, 7 FAILED, 2 frozen and surviving. (The hour-4 status said 10 / 8: wrong; ledger wins.)

## 6. Holdout predictions and interventions (all receipted before reveal)

                 law  BA     5-NN   majority  Brier (clim)   intervention
  D (well)       A    0.983  0.841  0.500     0.029 (0.103)  G6 FAIL 0/12 (engine defect);
                                                             G6b PASS dir 12/12, precision 0.155 log2
  E (swarm)      A    0.972  0.769  0.500     0.031 (0.174)  G6E PASS dir 10/12, precision 0.318 (const 1.25)
  F (clone)      A    0.930  0.841  0.500     0.045 (0.155)  --
  F (clone)      B    0.955  0.833  0.500     0.042 (0.155)  G6F PASS dir 11/12, precision 0.201 (const 0.71)
G6b's preregistered magnitude window would also pass a constant prescription (10/12): precision against
the best constant is the evidence (added as a gate from G6E on).

## 7. Sampler efficiency
C0: active 0.790 vs random 0.788 (B=30), 0.807 vs 0.831 (B=60): active NOT earned.
eta2 (cost lines vs random, budget matched, 3 seeds): LINES 0.767 vs RANDOM 0.946 oracle BA;
  location tie (.364 vs .376). Cost lines NOT earned. C2 used lines on top of random data, so C2's
  success is not yet attributed to selection vs lines (ablation c2abl, s12).

## 8. Planted truths, controls, attacks (G3 and the adversary)
Planted suite (7/7 on the final miner): positive and interaction laws recovered functionally; substrate
artifact not promoted; broken universality -> NONE; null world -> NONE; shared-code artifact ->
INSUFFICIENT_INDEPENDENCE/NONE; location-aware selection prefers the well-located ceiling.
Adversarial findings: capacity (ring n=3 evicts the logger cue); repair (ca r=3); mechanism (a
repetition-coded register flips 43/300 verdicts; the law tracks them with mechanism-level coordinates,
BA 0.961); location bias invisible to the contradiction rule; per-family biases that cancel when pooled;
ring's "dead memory is free" coordinate defect (offset tracks N, corr 0.99).

## 9. Defects found in Cosmos's own instrument (all recorded in campaigns/ and calibration/LEDGER.md)
I1 adversary evaluated a v2 law with v1 coordinates; I2 G1b noise-blind (now CRN); I3 G2/G7 hidden for
killed laws; G6 prescription engine assumed one flip (band law); G6/G6b gate lacked a chance floor;
S2 ladder resolution coarser than the criterion; repeated adversary rounds re-fired identical attacks;
null pool crashed twice (Windows pipe, worker MemoryError); pool-row leak caught before the prereg run;
base_rate_D label; single-atom direction bug (inverse predictions); Hour-1 status timestamp estimated.

## 10. Claims
JUSTIFIED (QUALIFICATION scope): CWE can build mechanically distinct executable worlds, record controlled
transformations as a typed graph, certify a phenomenon from native observations only, recover planted
laws and refuse artifacts / broken universality / null worlds, find missing coordinates adversarially,
seal holdouts with engineered separation, freeze laws, and predict sealed substrates and fresh-world
interventions better than nonparametric transfer -- on these substrates, with this author.
NOT JUSTIFIED: any statement about intelligence, memory in general, or reality; that the law is
"universal" (it is the shared task's economics expressed in declared coordinates); that active sampling
helps; that law B is better than law A on F (not significant); that independence is more than
code-level (one author wrote every substrate and the sealed ones knowing the law's form); that the
location gate is independent of location-aware selection (same criterion, different bases).

## 11. Next campaign (recommendation) and resume point
Recommended C3: (a) a substrate written by a DIFFERENT author (another seat) and sealed by them -- the
only way past the one-author ceiling; (b) move from SELECTIVE_PAYS to the charter's functional-memory
certificate (conditional MI + ablation), where the law is NOT a restatement of a hand-written economy;
(c) replace the kernel active sampler or retire it; (d) Atlas harvest of atlas_export JSONL.
Resume: worktree Prometheus-worktrees/cosmos-base-role, branch cosmos/cwe-c0-2026-09-23; stores under
C:/Users/James/cosmos_runs/{c0_main_run2_f31339054, c0b_21fd1b2cc, c0m, c0s, c1_run2, c2_7b14ec99e,
eta2}; run `python -m prometheus.cosmos.runtest --full` first. No sealed universe remains unspent.
