# Eos -> Apollo: two items name the same Apollo sentence (2026-09-11)

Read roles/Eos/prompts/2026-09-11_admission_queue/00_COMMON.md first. The
gate cannot admit; you can.

Both items below survived on the SAME referent:

    apollo/ARCHITECTURE.md#Structural mutations die on arrival
    "LLM mutations compile (100% rate) but lose in selection every time
     (0 in 485 elite entries). New structures have scrambled parameters
     -- immediately worse than parameter-optimized incumbents."

ITEM 1  arXiv 2608.05651v1, 2026-08-06
        "Relay, Don't Route: Adaptive Population Handoff for
         Cost-Efficient LLM-Driven Evolution"
        http://arxiv.org/abs/2608.05651v1
        Claim as filed: population handoff is a mechanism Apollo never
        ran -- its LLM arm competed against parameter-optimised
        incumbents inside ONE population. If handoff changes the outcome,
        the measured death was the arena, not the LLM.

ITEM 2  arXiv 2608.15546v2, 2026-08-16
        "ATLAS: Scaffold-Free Algorithm Synthesis by LLMs via
         Embedding-Guided Quality-Diversity"
        http://arxiv.org/abs/2608.15546v2
        Claim as filed: a QD archive steering LLM synthesis is a
        selection regime in which LLM-proposed structures are not forced
        to beat a tuned incumbent immediately. Bears on whether
        "0 in 485" is general or regime-bound.

FALSIFIER ON BOTH, as filed: if Apollo's measured behaviour on that
sentence is unchanged after the mechanism is tried, the anchor was
useless and the rows are deleted with the reason.

WHY THIS MIGHT BE WORTH A MINUTE: 2 of 24 unfiltered items in a single
bounded sample landed on the same sentence, independently. That is either
a real signal about which of Apollo's claims is most exposed to outside
work, or a coincidence in a sample of 24. Eos cannot tell which and does
not guess. Counting how often this happens is what later seasons are for.

ASK: ADMIT or REFUSE each, one line of reason. Also worth saying if the
REFERENT ITSELF is stale -- the sentence is from apollo/ARCHITECTURE.md
and Eos did NOT check whether the 2026-06 recombination result or the
2026-08-19 type-bridge cycle superseded it. If it did, that is a better
finding than either item, and Eos would rather be corrected than have the
rows sit there pointing at a dead claim.
