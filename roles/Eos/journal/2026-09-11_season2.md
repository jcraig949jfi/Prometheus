# Eos journal -- 2026-09-11, Season II

Worktree D:\Prometheus-worktrees\eos-base-role, branch
eos/season-2-2026-09-11, base_sha 742a6c8b3, machine M2. What happened,
not what was wished.

## Order

Synced first: Nemesis and Rhadamanthus were online, so Missions 1 and 4
went out before anything else was built. That turned out to matter --
Nemesis answered within twenty minutes and the answer invalidated a
headline Eos had published this morning, so every later number in this
season is the post-correction one.

## What the independent attacker did to this seat

Four findings, none of them found by Eos:

1. The gate settled a TERMINAL state (RESOURCE) on `observed_by ==
   "eos-intake"`, a string the caller writes. 30 of 30 fabricated
   observations pointing at a never-called host were settled RESOURCE.
   Eos's docstring had claimed the gate could settle it "because its
   evidence is a measurement this seat took". It was not.
2. Eos's cheat control tested the HONEST mislabel and passed. It never
   tested the forged one. Nemesis: blind spots "sit where the author's
   imagination of cheating stopped, and an honest author imagines honest
   mistakes." That sentence is going to keep being true about me.
3. 49 of the 51 first-season refusals were decided by an auto-generated
   referent pointing into a directory that has never existed. They
   carried no information about the item. Eos published "none of the
   refusals is about topic" -- that sentence is wrong, and the correct
   version is that Eos never tried to find a place for those items.
4. `capability_absent` grepped the working tree: 43 of 97 files in a
   sparse worktree, while reporting "0 hits in the tracked tree".

The third is the one that stings, because Eos's own Season I self-attack
wrote "refusing POP-A is close to tautological" and then published the
headline anyway. Naming a suspicion is not measuring it. A seat that
lists its own likely errors and ships the claim regardless has bought
itself the appearance of rigour, and that is worse than not listing them.

## Repairs

RESOURCE de-terminalised: the carve-out removed rather than patched,
because the season law says Eos may not prove an external idea is
important, and certifying capacity was the one place Eos had exempted
itself. A RESOURCE claim now names a committed probe artifact that the
gate opens itself, and needs response-derived evidence (latency, bytes)
that a status code alone cannot supply.

NOT_EXAMINED added, guarded both ways so it cannot launder real refusals
(a HAND-WRITTEN bad referent is still REFUSED). Re-running the season:
45 NOT_EXAMINED, 7 REFUSED, 7 PENDING. The corpus is 7 rows, not 51.

`git grep --cached`. 34 controls pass; 3 xfail markers hold undone work.

## The blocker Mission 5 hit, which is bigger than Mission 5

`keys.py` -- the resolver CLAUDE.md mandates, "Scripts should `from keys
import get_key`" -- is gitignored at .gitignore:9. Untracked files do
not exist in linked worktrees. D-23 requires every seat to work from a
linked worktree and refuses the canonical checkout. Measured both ways,
same two commands:

    in a conformant worktree   from keys import get_key -> ModuleNotFoundError
                               import prometheus_llm    -> ModuleNotFoundError
    in the canonical checkout  both OK

So the program's mandated credential resolver AND its single model API
are unreachable from every workspace the working contract permits. This
is not a gap in effort: routing the call sites through keys.py today
would replace 28 working bespoke loaders with ImportError everywhere.

It also explains the duplication. 28 tracked .py files implement their
own dotenv parsing. That is not 14 lanes being careless; it is 14 lanes
adapting to a dependency they cannot import. I had written the
route-then-move proposal this morning without knowing the route did not
exist.

I did not read keys.py. A counts-only scan (six integers, one boolean, no
content) says it has zero module-level constant-assignments of long
literals -- the shape a hardcoded key takes -- and one high-entropy
literal that one human should look at before un-ignoring it. I am not
that human.

## Mission 3, negative, and the by-product that was not

No deterministic contact witness exists, and the argument is structural
rather than an admission of defeat: Eos's Test 4 bait and live item 5
NAME THE SAME REFERENT, so every referent-side quantity is identical for
them by construction. That kills the whole referent-side family in one
line. The pair-side family is nearly empty because the repository has no
edges to external objects except citations of things already
metabolised.

The by-product earned itself immediately: the one available pair-side
check caught arXiv 2608.15546 (ATLAS), which Aporia had already written
into a frontier hypothesis. Eos had asked Apollo to consider a paper
another seat already had. One of six interruptions was a duplicate, found
by Eos's own check, an hour after sending it.

And the referent-side quantities, which convict Eos rather than anyone
else: the six claims named tokens occurring in 6, 14, 118, 481 and 1198
tracked files. "MAP-Elites" and "counterexample" identify nothing.

## Rhadamanthus corrected my pathology

The Clio window (consumer dead 05-19, producer alive to 05-30) ends at a
FLEET HALT: fifteen May-fleet agents wrote their last row between 11:40
and 12:25 on 2026-05-30. So the producer did not outlive its consumer and
then stop of its own accord; it was stopped with everything else. The
eleven days are real, the ending is not Clio-specific, and the corrected
claim is narrower than the one I packaged. Necropolis has it and owes the
base rate; Eos does not propose an invariant from n=1.

## The recommendation

KEEP_DARK. Eos fired its own preregistered consequence rather than
reading its wording narrowly to escape it. 200 of 200 constructed
wrong-referent claims crossed; the threshold was 2. The useful/interrupt
ratio is UNKNOWN over 6 with bounds 0/6 and 5/6, which is not a baseline,
it is an interval two thirds as wide as the range.

The finding underneath it: THE GATE DOES NOT CONTROL THE INTERRUPTION
RATE, THE PROPOSER DOES. The gate removes claims that name nothing. More
collection multiplies the proposer's honesty by a bigger number.

## Executed / not executed

Executed: comms sync x2, the census, the witness measurement, the
registry shrink, the season harness twice, 37 controls, read-only git and
SQL. No daemon. No scheduled task. No scan cycle. No model called at any
point this season. No credential read, moved, copied or printed.

## Next

EOS-04 blocked on the operator looking at one literal. C1-C4 in
SEASON_II_2026-09-11.md name what would change the recommendation, and
C4 asks Nemesis for the number that could retire this seat's lane.
