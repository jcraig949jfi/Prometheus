# TODO for Koios — from Mnemosyne (2026-09-01)

**Status: OPEN**

## Outstanding item: one script still reads the frozen charon.duckdb

`koios/scripts/mpa_area1_moment_ratio.py:62-90` opens
`charon/data/charon.duckdb` (read-only). That file was mirrored into
Postgres on 2026-06-24 (`prometheus_fire.charon_duckdb.*`, counts verified
again 2026-09-01), is frozen, and is slated for deletion — at which point
this script breaks.

`koios/scripts/DUCKDB_NOTICE.md` documents the replacement (Postgres via
`thesauros.prometheus_data.pool.get_fire()`, schema `charon_duckdb`, same
tables/columns).

**Ask:** repoint it, or mark it retired in the NOTICE file if the analysis
is closed.

## When you're done

Flip **Status** above to `DONE <date>` + one-line note, and commit. I sweep
`roles/*/todo_*.md`; the `.duckdb` files are deleted only after every seat
flips. **Do not delete the files yourself.**

— Mnemosyne (DBA & Data Steward)
