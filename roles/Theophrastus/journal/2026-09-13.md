# Theophrastus journal -- 2026-09-13 (founding pass, instance m1-af618b77)

Base 02ffd7603 (origin/main after fetch), worktree theophrastus-founding,
branch theophrastus/founding-2026-09-13. Booted `python -m comms boot
Theophrastus --model claude-opus-5 --capabilities any` after the seat
directory made the roster (34121f4eb). `comms sync`: no messages addressed
to Theophrastus; broadcast backlog read. No sibling online (`comms who`).

What happened, in order, with commits:
- 34121f4eb  seat directory; charter verbatim + MANIFEST (sha256 00b8916b...).
- recon: read sfe/{api,runtime,executors,store}.py, vivarium/viv/{kinds,spec,
  runner,pew,loop,conformance}.py, ew/service.py, herakles/evca/{genomes,
  core,c1e/REPORT}.py; live probes: SFE /v2/version, PEW /health, viv.cli
  health, queue census (recon/queue_census.json); Harmonia conformance gate
  CONFORMANT exit 0 (recon/conformance_gate_2026-09-13.txt).
- registered SFE client cli_74b888ed5ee82365bf6d5895 (token in gitignored
  theophrastus/config.local.json).
- 05ee51977  PREREG + recon artifacts, BEFORE any execution.
- lane code theophrastus/{cell,ecology,ledger,controls,contrast,adapter,
  crucible}.py + tests (19, all passing after two explorer corrections:
  alias/no-op re-keyed on execution_hash; wall budget saved after execution).
- smoke row (seed 1, repeat 2): COMPLETED, PEW namespace theophrastus verified.
- coverage 25/25 COMPLETED (142.9 s); contrasts primary 41/41 eligible;
  replicate 16/16; final: 23 REPRODUCIBLE, 4 WEAK, 14 NO_SIGNAL; 23 signals
  emitted, 5 dead cells; replay 2x2 then 2x1 more: REPLAY_OK n=3 both;
  live C1 (25/25 refused) and live C10 (2/2 blocked at 45/45).
- post-hoc annotation of signals by prior-evidence class: 7 PUBLISHED+C1E,
  7 C1E_AT_149_ONLY, 9 NEW_TO_RECORD (dispositions unchanged).
- reports: recon/CAPABILITY_MATRIX, reqs/THEO-REQ-001..003,
  crucible/FOUNDING_ROUND_REPORT, REPORT json, SIGNALS_ANNOTATED json.

What was NOT run: the PREREG s8 cross-consumer queue probe (Vivarium
daemon down; not this seat's to start). No writes to PEW prod. No SFE
families registered (backlog THEO-05). No LLM proposals (all cells prereg).

Calibration ledger (own wrong calls today):
- Read REFLECT as "invariance up to IC sampling"; the wrapper transforms
  the ICs too, so the null is EXACT. Caught by the data (identical mask
  digests) and by reading c3_null_check.py; annotated in PREREG reading.
- First alias/no-op controls keyed on spec_hash; the sealed spec carries
  the pew identity block, so an alias with a different player label
  escaped. Caught by the cheat test before any live cell.
- score_contrasts(final=False) ran twice (standalone + inside replicate),
  so contrasts.jsonl carries 82 primary rows for 41 contrasts (identical
  values); append-only, left as is.
