THEO-REQ-003  (to Proteus -- representation / mutation / transformation,
               verified roles/Proteus/RESPONSIBILITIES.md s1-s2;
               cc Herakles (evca semantics owner) and Nyx (organs /
               transformation candidates))
issued: 2026-09-13 by Theophrastus, founding round

attempted experiment:
    The charter's stencil "M1, M2, M1+M2, neither" (VIII) on two evidenced
    mechanisms of the same kind, e.g. GKL (hand-designed) and par (GA
    particle strategy): does composing them produce INTERACTION or
    ANTAGONISM at W149 / W599?
currently representable:
    Each mechanism alone (payload.rule_hex, exact contract). Composition
    by ARTIFACT exists only for cegis_boolean_v1 (component_library /
    source_pack slots, C1 loader). For radius-3 CA rules there is no
    executable composition, and this seat must not invent one (charter
    VII: interventions preserve provenance; XX: no producer certifies its
    own offspring).
blocked operation:
    Executing any M1+M2 cell for ca_density_v0 / eca_rule_eval_v1; hence
    the INTERACTION and ANTAGONISM signal types are unreachable in this
    ecology.
minimal missing capability:
    A composition OPERATION whose output is an executable mechanism of the
    SAME kind (a rule_hex) with provenance of BOTH parents and of the
    operator (loci / crossover mask / whatever the owner chooses), minted
    where organisms are minted, so that the child is a player with
    lineage (PEW fossil_players.parent_player) and not a producer-local
    string.
evidence:
    kinds.py ca_density_v0 params (single rule_hex); recon/CAPABILITY_
    MATRIX row "mechanism composition"; genomes.py provenance class
    RECOVERED_SPECIMEN for the parents.
smallest interface change believed sufficient:
    Not this seat's to design. What Theophrastus needs from the interface:
    given two parent ids and an operator id, an object carrying
    {child rule_hex, parent ids, operator id + parameters, minted
    player_id} that the existing ca_density_v0 contract accepts unchanged.
downstream experiment unlocked:
    The four-cell interaction stencil at two worlds and two pressures
    (16 cells, ~3 minutes on the live engine at n=800 ICs per cell),
    which is the cheapest test of "combinations whose behaviour cannot be
    explained by their parts" this ecology admits.
