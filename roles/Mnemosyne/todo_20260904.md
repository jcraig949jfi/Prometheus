# Mnemosyne TODO — opened 2026-09-04 (PEW closure lane)

Open items surfaced by the PEW closure lane. Full context:
`evidence_wiki/CLOSURE_PACKET_2026-09-04.txt` (section refs below).
State/hygiene backlog that predates this lane lives in `mnemosyne/STATE.md`
("Open questions / hygiene backlog") — not repeated here.

Status key: OPEN | BLOCKED(who) | OPERATOR | DONE

---

## A. Mine to drive (PEW-owned)

- [ ] **A1 — Cross-host M1 closure.** OPEN. Packet §4/§13: closure V0 is
  LIVE on M2 but M1 is `LIVE_NOT_QUALIFIED`. M1 must apply
  `evidence_wiki/migrations/008_closure_v0.sql` and redeploy its PEW service
  so its fossils get attestation and it exposes /identity, /packets, /constraints.
  M1's Claude is dark; I can reach 192.168.1.202:5432 to apply 008, but the
  M1 service redeploy needs M1's watchdog/operator. Coordinate when M1 is back,
  or apply 008 + trigger M1 redeploy remotely.

- [ ] **A2 — Send the cross-component requests.** OPEN (awaiting James's
  go-ahead, offered 2026-09-04). Packet §12: R-SFE-1, R-SFE-2 to Daedalus;
  R-PRO-1, R-PRO-2, R-PRO-3 to Proteus.

- [ ] **A3 — PHENOTYPE_CONSUMER_REQUIREMENT.** OPEN. Carried from the
  2026-09-02 TODO; Daedalus did not touch it (this window was closure only).
  `evidence_wiki/docs/PHENOTYPE_CONSUMER_REQUIREMENT.md`.

## B. Operator-gated (needs James / admin; I track + prep)

- [ ] **B1 — Credential rotation + cleartext purge.** OPERATOR. Packet §3.
  Override mechanism SHIPPED (env > config.local.json > config.json). Still to
  do (operator, M1+M2 simultaneously): choose new db_password + auth_token +
  per-machine tokens; place in each host's config.local.json/env; delete the
  cleartext from config.json; purge git history. Treat existing tokens as
  compromised (they lived in git).

- [ ] **B2 — Scope inbound 5432.** OPERATOR. Packet §3(iv). M1's
  192.168.1.202:5432 accepts config.json creds from any LAN peer, bypassing
  ew.write_log attribution. Scope to specific peer IPs, or close + re-point
  M2's service first (closing as-is breaks the M2 deploy). Needs an M1-side
  firewall edit.

## C. Cross-component (BLOCKED on others; track for when they deliver)

- [ ] **C1 — R-SFE-1 engine-verified anchor.** BLOCKED(SFE/Daedalus). When SFE
  provides anchor verification (verify endpoint or signed token), wire it,
  flip `sfe_anchor_verified` true on verified writes, and un-pin closure gate
  C4. Until then the wrong-but-real-event gap stays pinned + honest (§6/§7).

- [ ] **C2 — R-SFE-2 sfe_engine_identity.** BLOCKED(SFE/Daedalus). Closes the
  STILL_AMBIGUOUS "which of two engines" (§5).

- [ ] **C3 — R-PRO-1 composition provenance block.** BLOCKED(Proteus). Add
  typed columns/edges (composition_id, topology, serialization_digest,
  ablation_id) once Proteus fixes the names → COMPOSITION_COMPLETE (§11).

- [ ] **C4 — R-PRO-2 exact_action / exact_input / exact_output_digest.**
  BLOCKED(Proteus). → REPLAY_COMPLETE (§6).

- [ ] **C5 — R-PRO-3 joinable measurement definition/version.**
  BLOCKED(Proteus). Makes `outcome` a joinable metric, not a bare token.

- [ ] **C6 — registry_identity, entry_id.** BLOCKED(upstream). STILL_MISSING
  fossil identities (§5); need SFE/registry to supply.

---

Done in this lane (for the record): server-attested /identity + per-fossil
attestation; packet read; constraint-transfer + errata V0; credential override;
deploy-aware watchdog (CT-SFE-1 deploy-lag fix). All LIVE on M2, batteries green.
