# Mnemosyne journal - 2026-09-16 - instance m2-9c10ae00

Built from ccb26df01c9d637a0ebac0ef3612a4e116d507b7 in
Prometheus-worktrees/mnemosyne-boot-2026-09-16 on mnemosyne/m2-boot-2026-09-16
(linked worktree; git-dir differs from git-common-dir). Host SPECTREX5 (M2).
Booted Mnemosyne[m2-9c10ae00], model claude-opus-5, heavy.

Times below are local (-0400) unless marked Z.

## Boot (07:40-07:50)

- fetch only in the canonical checkout; origin/main ccb26df01; worktree
  created from it (43,352 files, unbounded timeout, clean, 0 dirty).
- `comms boot` from M2 without EW_DB_HOST hit the M2 fork and the store
  guard REFUSED (WRONG_ENVIRONMENT, signature c84e26826cc12217). Known
  class; occurrence line added; booted with EW_DB_HOST=192.168.1.202.
- Inbox: 22 messages carried since 2026-09-11, 4 unanswered from after my
  last session (Techne #194, Techne #231 broadcast, Theophrastus #239,
  Agora #258); queue #3 = #239. Later in the pass: Proteus #286 ack, #287
  delegation (queue #4).

## What I found before touching anything (step 8, watchdogs first)

    M1 PEW 8377      HTTP health times out from M2; TCP 8377 refused/filtered;
                     5432 answers. Cannot tell service-dead from firewalled.
    INDEPENDENT      ew.read_log in the canonical store: the M1 watchdog
    ORACLE           searched every 5 min, 400 rows in 48 h, LAST ROW
                     2026-09-15 17:11:01 -0400, then nothing. 22 minutes
                     after the operator's commit 985a3f760 ("M1 is being
                     handed to Nestor and the SFE ecosystem moves to M2").
                     Daedalus 2b21acf76 places the M1 engine stop at
                     ~20:15Z the same evening. Nobody was alerted: the row's
                     alarm route was "none yet".
    M2 PEW 8377      answering 200 from D:\Prometheus\evidence_wiki -- the
                     CANONICAL checkout -- with base_sha "" and
                     main_worktree false in its own receipt (git absent
                     from the S4U task context; ew.workspace._git() returned
                     ""; "cannot tell" read as "not canonical"). search.ready
                     false forever: ModuleNotFoundError sentence_transformers.
                     Fronting the quarantined fork. uptime 336 s: started at
                     07:40 by the OLD watchdog's deploy-follows-HEAD branch.
    M2 watchdog log  2026-09-11 12:41 -> 2026-09-14 05:34: three
                     `restart FAILED`, zero ok lines, no alarm; 48 lines total.
    M2 fork store    last read 2026-09-05 02:45; last write 2026-09-11 06:31
                     (harmonia, namespace test). NO READER for 11 days.
    venvs on M2      no interpreter had sentence_transformers; .venv-m2 also
                     lacked rank_bm25 (found later as a search 500).

Decision taken and why (reversible; the fork is untouched): the M2 PEW
now fronts the CANONICAL store. The 2026-09-04 "M2 fully independent"
ruling rested on M1 being the ecosystem's home; on 09-15 the operator
moved SFE to M2 carrying M1's engine.db. A PEW on M2 fronting a stale fork
would verify anchors against a world the engine no longer runs. A fork-
serving instance is one env change on another port if wanted. The
canonical STORE stays on M1 (Nestor's machine now): moving it is the
operator's fork-vs-migrate call, open since 09-04 (MNE-D1 below).

## Changes shipped (all pushed; every SHA verified ancestor of origin/main)

    33af26c1c  ew.workspace fails CLOSED when git does not answer
               (workspace_known in every receipt; 4 tests; the real
               canonical path exercised with and without git on PATH:
               REFUSED / REFUSED / REFUSED-unknown / ADMITTED-known).
               One watchdog for both machines: ew_watchdog_m2.ps1 is a
               wrapper that sets env (EW_DB_HOST, PROMETHEUS_ENV, machine,
               interpreter) and file names (MNE-35). Rule-10 bound
               (MNE-36): nonproductive_ticks, reset ONLY on an ok line;
               at 12 the loop parks (record + comms post + `parked`
               ticks) until the file is removed by hand; 3 new tests,
               9/9. Techne #194: the two live-qualification scripts skip
               unless EW_LIVE_QUALIFICATION=1 and write under derived/;
               evidence_wiki/derived/ gitignored at the root.
    2ba8825b9  the watchdog creates derived/: the first tick from a fresh
               pinned worktree wrote NOTHING (exit 0, 2.7 s, no log, no
               state) -- Add-Content into a missing directory under
               SilentlyContinue. Test fixture no longer pre-creates it.
    6b4aee022  the wrapper finds .venv-m2 through the common git dir:
               the first pinned tick started the service "via python"
               (the Windows Store shim) because the venv sits beside the
               canonical clone, not beside the worktree.
    1e0708472  PEW qualified FROM M2 against the canonical store with the
               M2 engine: 17/17 12/12 19/19 14/14+1SKIP 16/16. S2 gate
               keyed by ENVIRONMENT (registry) not machine; cheat run
               naming the fork FAILS S2. requirements.txt written.
    ed74db21c  THEO-REQ-001: `ecology` jsonb containment selector on
               GET /fossil/encounters; client fossil methods; 7/7 with
               positive/cross/negative/three cheats.
    3fed05954  Proteus #287: migration 013 (mate_player, family,
               representation_version, semantic_version) applied to the
               canonical store (6009 rows before/after); mapping stated in
               docs/PROTEUS_MINT_WRITE_CONTRACT.md; Proteus write identity
               issued (R-5, sha256 only in git, value out of band on M2);
               round trip 7/7. Tracker R-6 for Agora #258.
    (this)     journal, STATUS, MONITORS rows, backlog, comms bodies,
               results of the deployed-service reruns.

## Deployment on M2 (D-23 s6)

    pinned worktree  D:\Prometheus-worktrees\mnemosyne-pew, detached,
                     advanced 33af26c1c -> 2ba8825b9 -> 6b4aee022 ->
                     2b874bbc4 -> 569a675f7, clean at each step
    scheduled task   MnemosyneEvidenceWikiWatchdogM2 action re-pointed
                     08:06 to the pinned wrapper (was the canonical
                     checkout's script; it raced me once: relaunched the
                     fork service at 08:04 after I stopped 6160)
    interpreter      D:\Prometheus\.venv-m2 (+torch cpu 2.14.0,
                     sentence-transformers 6.0.1, rank-bm25 0.2.2)
    config.local.json (untracked, pinned worktree): canonical_db_host,
                     sfe_db_path, sfe_verify_url/cacert/token -- the token
                     minted on the M2 engine for client pew-m2-service
                     (sha256 854b3f974fa1d2c7), never displayed
    first ok line    08:10:30 -- 14 h 59 min after the last M1 one
    proof of store   canonical read_log has machine=M2 agent=watchdog at
                     08:10:30.835; the fork has 0 reads today
    final state      569a675f7, workspace_known true, search ready 4.6 s
                     after start, batteries 17/17 12/12 19/19 14/14+1 16/16
                     and both checks 7/7 against the DEPLOYED service

## Not run, and said so

- Nothing on M1: no process, task or log on M1 can be attested from M2.
  The M1 watchdog, backup and restore-verify rows are DORMANT / UNLOCATED
  in MONITORS.md with the measured timestamps, not guessed states.
- lineage P gate SKIP: no peer engine reachable (M1 8811 times out).
- The M1 pin still runs the pre-bound script; MNE-36 is enforced on M2 only.
- Test rows written to the canonical store today: batteries (namespace
  test, as always), ecology check (1 encounter, test), minted-player check
  (1 player, test). The 2026-09-15 10:46 write_log rows by
  V1-writepath-test / fake M2-M4 agents are somebody's pytest run of the
  old live scripts; not cleaned (namespace test; history is append-only).
- Decision I did NOT take: deleting or repurposing the M2 fork database.

## Defects reported to other lanes (comms bodies in comms_out/)

- Archaeon: archaeon/workspace.py is_main_worktree fails OPEN when git
  yields nothing (same defect I fixed in my copy); a scheduled-task
  context without git on PATH admits the canonical checkout. Constitution
  defect per WORKING_CONTRACT s10 -- the reference guard is the one that
  fails.
- Operator (via backlog XL rows): MNE-D1 where the canonical store and its
  backup live now that M1 is Nestor's; MNE-D2 the 2026-09-04 fork ruling
  is superseded in practice -- confirm or reverse.

## Operator directive received mid-pass (chat, ~08:35 local), verbatim

    "Probably noticed that we started up on M2.  M1 is running a
    different ecosystem for now.  We are going to run thenSFE ecosystem
    on M2 now."

Read as: the SFE ecosystem (engine, PEW, its consumers) runs on M2; M1 is
Nestor's. MNE-D2 confirmed. MNE-D1 (the canonical STORE, which also holds
comms for both ecosystems) is still the operator's call; recommendation
unchanged.

## Calibration (my own wrong calls today)

- I stopped the fork service before re-pointing the scheduled task; the
  old task relaunched it 90 s later. Order is: re-point, then stop.
- The first pinned tick "succeeded" (exit 0) and wrote nothing; I read
  exit 0 as a tick for about a minute. Silence is not health, including
  from my own instrument on the first run.
- A stray `cat > "$TMP/..."` in a commit command blocked on stdin for
  5 min; nothing was committed. Boot transient, no repository effect.
