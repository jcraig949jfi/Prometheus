# Mnemosyne journal - 2026-09-11

Built from 96a22e736513d2323867d1bf587214d4e08ac7f3 in
F:\Prometheus-worktrees\mnemosyne-baserole on mnemosyne/baserole-adopt.

## D-23 workspace compliance (earlier today)

Moved PEW off the canonical checkout. Task worktree mnemosyne-d23 from base
2627fe37c, integrated at 7c36278f2 and bc3c39eee; both verified ancestors of
origin/main. The service, the daily backup and the weekly restore-verify all
ran out of F:\Prometheus and now run from the pinned worktree
F:\Prometheus-worktrees\mnemosyne-pew, detached at bc3c39eee, clean.
Record: roles/Mnemosyne/D23_COMPLIANCE_2026-09-11.md.

What the move cost and taught:

- THE WATCHDOG HAD NO SINGLETON GUARD. Its health probe timed out at 5s, but
  PEW has answered health in 7-15s under host load, so a slow probe read as
  "service is down" and started another service. Three ew.service processes
  were live at once, contending for port 8377 and the connection pool, each
  duplicate making the next probe slower. The watchdog was manufacturing the
  outage it exists to prevent. Fixed: 20s probe, and a listener/process check
  before starting anything. Verified by firing the task three times against a
  healthy service: exactly one process. Same class as Daedalus's R-F.
- THE BATTERIES ASSUMED TREE-LOCAL RUNTIME STATE. SFE's var/engine.db exists
  only where the engine runs, so a worktree has none. Now PEW_SFE_DB, then
  tree-relative, then the canonical location. Reading the canonical checkout
  is permitted; mutating it is not.
- SKIP IS NOT PASS AND NOT FAIL. SFE was briefly down mid-migration (Daedalus
  moving his engine the same day). Gates that cannot exercise their mechanism
  for want of a dependency now report SKIP, excluded from all_pass. An absent
  dependency is not a broken mechanism.
- I EXERCISED RULE 7 RATHER THAN WORKING AROUND IT. The pinned worktree went
  dirty because I hand-copied the fixed watchdog into it for immediate effect.
  Recorded the diagnostic, destroyed the worktree, recreated it at bc3c39eee.

Canonical clean-up: deleted five of my own untracked scratch copies (four
byte-identical to main, one a stale pre-guard indexer), claimed
ops/restore_verification.json, and deliberately left eight tracked files that
show as modified only because the canonical HEAD is a branch that no longer
exists on origin. Removed the stale sfe_pristine worktree from a session temp
path; deleted local branch mnemosyne/evidence-wiki-v0.

Verification after the move, against the pinned worktree: pew 16/16, seam
12/12, closure 19/19, lineage 15/15, h0h5_refs 16/16.

## Adopting roles/base-role

Read the base at 2cfc66aed (RESPONSIBILITIES, WORKING_CONTRACT, INHERITANCE).
My row in the register is "RESPONSIBILITIES.md (already)" -- stamped, not a
stub, so I owe no lane section.

Three artifacts the base requires that I did not have, created this pass:
roles/Mnemosyne/STATUS.md, this journal, and BACKLOG_H0H5.md in the schema at
roles/Archaeon/prompts/2026-09-10_backlog/00_BACKLOG_SCHEMA.md.

Four lines in my own seat file appear to contradict the base. They are
reported to the operator for a ruling rather than rewritten by me, because
the directive asked for them quoted, and because one of them
("I don't do science") is a scope question I should not settle alone.

## Not run

X5 witness round trip (no witness exists yet). Cross-machine PEW
qualification from M2 itself. The re-index of cs-c3-2's remaining 43 rows
(still queued or running in Vivarium).

## Second pass (from 14:06 local): the watchdog's mirror image, and three items closed

Merged origin/main c8d576e41 then ce7e6208 by named SHA; pushed e301547dd
(verified ancestor of origin/main); advanced the pinned worktree to it and
the watchdog started pid 15616. Report to Archaeon: comms #119. Talos
TALOS-10 answered NONE: comms #124.

What the instrument showed before any work was chosen:

- Host rebooted 13:33:56 local. The watchdog's restart at 14:06:10 bound the
  port about four minutes later (cold import of torch and the embedding
  model), so "restart FAILED" at 14:06:22 was wrong: the start succeeded.
- Before the reboot, 10:46-11:06 local, the singleton guard I wrote in the
  morning logged "already present; NOT starting another" against a service
  that was present and not answering. Apollo's #21 (search timeout 60 s x3,
  health empty at 11:41Z) is that state. Three hours of it.
- First hybrid search after cold start: 80 s. Every later one: 0.01 s.
  Loading happened inside a request thread with no lock; concurrent first
  callers each started a load; the threadpool filled and the sync health
  endpoint starved behind them. The hub was consulted unauthenticated on
  every load even though the weights were cached.
- http://localhost costs 2.1-2.7 s per request; 127.0.0.1 costs 0.08 s.

MNE-31 closed (0653e64e1): model warmed at startup under a lock, hub
offline when cached; /health async with search.ready; battery gate E14;
watchdog rewritten to probe the property, log last-success, restart a
present-but-dead service after 3 ticks, and give a fresh start 300 s.
Tests: 6/6 against tests/_fake_pew.py on port 8378 (cheat and positive
controls both fire). Two test-harness lessons cost forty minutes: a
leftover fake on the port answered for every later test (SO_REUSEADDR on
Windows permits a double bind; the fake now refuses it, the fixture clears
the port), and a process started by PowerShell inherits its stdout pipe,
so a captured tick never reached EOF (no pipes now).

MNE-32 closed (b08a4f0de): per-agent scoped tokens; Kairos read-only,
tracker R-4; 6/6 tests. MNE-33 closed (a36a8234d): Hermes's guard, 3/3
tests; the M2 watchdog names m2-local-fork.

Battery defect found on the redeploy and fixed (this commit): E11 409'd
because real_sfe_run() anchored on the WORLD's latest event rather than the
experiment's OBSERVATION_RECORDED event, and because every battery read the
canonical checkout's engine.db, a stale copy (mtime 03:48) of the ledger
the engine moved off-tree today. Both batteries now resolve the ledger
PEW_SFE_DB > config sfe_db_path (config.local.json, untracked) > tree.
After the fix: 17/17, 12/12, 19/19, 14/14+1 SKIP, 16/16.

Rule kept: the pinned worktree does not run batteries. A run there rewrote
five tracked result files; restored them and ran from the task worktree,
where the results are committed beside this entry.

Not run: MNE-01 rewrite (next), MNE-34 indexing (next), the M2 property
probe port (MNE-35), the rule-10 BOUND enforcement in the script (MNE-36).
