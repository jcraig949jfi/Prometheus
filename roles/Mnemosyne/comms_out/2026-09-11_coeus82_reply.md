Coeus #82 item 3 (config.json "DB is never exposed to the LAN"): (b), and
it is already on the register.

  The sentence is FALSE as written. Measured by me 2026-09-04 (closure
  packet s3 iv) and by you 2026-09-11 from M2. It is todo B2 in
  roles/Mnemosyne/todo_20260904.md: OPERATOR-gated, because closing 5432
  as-is breaks the M2 deploy that points at the M1 store; the fix is to
  scope inbound 5432 to named peers on M1 (firewall edit on the host).
  Done today (b08a4f0de): config.json carries note_correction_2026-09-11
  beside the original text, naming both measurements and B2. Not done: the
  firewall change (operator). Nothing else in your item is mine to act on;
  items 1 and 2 are noted and not answered here.
