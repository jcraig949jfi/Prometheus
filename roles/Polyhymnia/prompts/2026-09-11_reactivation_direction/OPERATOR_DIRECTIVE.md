OPERATOR DIRECTIVE, verbatim (James, HITL operator, in chat, 2026-09-11)

Received by the Polyhymnia seat after the adoption receipt (bfea90d28).
Reproduced exactly; the em-dash and the curly quotes in the original are
kept as the operator typed them below, so this file is NOT pure ASCII
by design (base-role ASCII rule applies to roles/base-role/, not to a
verbatim operator prompt). The seat's reading follows the rule.

----------------------------------------------------------------------

POLYHYMNIA — REACTIVATION DIRECTION, 2026-09-11

Do not restart the old daemon.

The old premise was backwards: Polyhymnia accumulated tensor-shaped material before Prometheus had demonstrated a consumer for it. The result — one scour, 47 integrating ticks, 250 null ticks, 163 unanswered approval requests — is evidence against that architecture, not against representation scavenging itself.

Re-premise the seat.

Polyhymnia is no longer “the one tensor.” Polyhymnia becomes the REPRESENTATION SCAVENGER for the Prometheus 2.0 ecology.

MISSION

Search aggressively for unusual ways information can be represented, compressed, decomposed, indexed, transformed, or exposed to organisms/worlds.

The fringe remains the point.

Candidate material may come from mathematics, optimization, theorem proving, program synthesis, information theory, neural representations, scientific computing, signal processing, compression, databases, dynamical systems, old Prometheus machinery, or mechanisms extracted from external solver/tool architectures.

Do not prove that these representations are useful. Do not promote them because they look clever. Produce testable representation candidates that downstream selection can kill, retain, combine, mutate, or metabolize.

The consumer contract comes first.

POLY-01:
Before rebuilding any persistent tensor or daemon, identify the smallest existing Prometheus 2.0 consumer interface through which one representation candidate can actually alter an experiment.

Prefer an SFE world/organism boundary if one exists. Do not invent a large new infrastructure layer merely to give Polyhymnia somewhere to write.

Then construct ONE end-to-end probe:

source/mechanism
    -> representation candidate
    -> consumer
    -> measurable behavioral difference
    -> deterministic observation

A null result is acceptable. An unused artifact is not.

Polyhymnia should eventually become a producer capable of continuously feeding many representation families into the experimental ecology, but continuous production is authorized only after at least one real consumption path exists.

Keep the Omnitensor and HARD-3 signature-keyed tensor conceptually separate.

For the historical 11.5 MB tensor body:
archive it as archaeological residue with SHA-256 and enough provenance to recover it later. Do not treat it as the canonical representation substrate for 2.0 and do not spend time rehabilitating it yet.

Also complete POLY-03/POLY-05 housekeeping where safe.

Return with:

1. the proposed consumer interface;
2. one minimal representation-to-consumption experiment;
3. what existing code can be reused rather than rebuilt;
4. what would constitute evidence that this new Polyhymnia premise is itself wrong.

Do not build the perpetual daemon yet.

The daemon earns resurrection when something is hungry enough to eat what it produces.

----------------------------------------------------------------------

Seat's reading (not the operator's words):

- POLY-XL-02 is DECIDED: the seat is re-premised as the representation
  scavenger. The old "one tensor" thesis is SUPERSEDED, not dead: its
  failure is evidence against accumulate-before-consumer, not against
  scavenging.
- POLY-XL-01 is DECIDED: archive the body as residue with sha256 and
  provenance; do not rehabilitate.
- "Execute nothing" is lifted for: the archive, POLY-03, POLY-05, and
  the POLY-01 consumer search + ONE probe. It stands for: any daemon,
  any continuous production, any new infrastructure layer.
- The deliverable is the four-part return, in chat as a paste block and
  as committed files.
