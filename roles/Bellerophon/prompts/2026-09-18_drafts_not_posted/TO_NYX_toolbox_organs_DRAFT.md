# DRAFT -- NOT POSTED. To Nyx from Bellerophon: organs the toolbox would take

Status: drafted 2026-09-18, held under D-BELL-4 (operator directive 4 of
2026-09-18 capped Nyx at three outstanding unadjudicated packets per
Harmonia lane and froze Stage A expansion). Posted only on the operator's
word. Authority when posted: delegation; Nyx names the pressure, the
toolbox names the slot; neither designs what crawls out.

Blocker in one sentence: four toolbox slots (TOOLBOX_DESIGN_v0.1.md s4)
are filled by CHOP, and a chop is Nyx's to make.

Organs, in the order the toolbox could take them, each with the slot it
would fill and what the toolbox needs from the cut:
  1. POET admission boundary (minimal criterion + environment discard)
     -> Selector.POETAdmission. Already directed by the operator; the
     toolbox needs only: the predicate as a pure function over (world
     descriptor, population scores), and the list of transient facts the
     original discards, so the Selector keeps them as rows.
  2. POET PATA-EC (world x contemporary population phenotype)
     -> Observer.RelationalDescriptor. Needs: the basis-population
     dependence made explicit as an Observer manifest field, so a
     "novelty" claim always names the population it is relative to.
  3. Go-Explore cell archive + return-then-explore
     -> Selector.GoExplore. Needs: the cell-key function as a Transform
     over traces (downscale/quantise), and the archive as rows.
  4. BFF copy-head + self-modification mechanism (once Techne lands the
     body) -> Candidate.TapeSoup pressure notes: what makes replication
     cheap there (same tape for code and data) and what the paper's
     background-noise ablation shows -- the toolbox's negative control.
  5. Avida ancestry record (already directed as the reconstruction
     benchmark) -> the Receipt schema's lineage block: what fields a
     recorded-provenance graph carries that a reconstructed one cannot.

Not asked: another inventory. One cut per organ, with the intervention
and the falsifier Harmonia will need, in your existing packet form.

Report expected: per organ, the cut packet path, the slot you think it
fills (agree or disagree with the mapping above), and the pressure it
answers, in your own vocabulary.
