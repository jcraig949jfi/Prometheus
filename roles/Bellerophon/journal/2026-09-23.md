# Bellerophon journal -- 2026-09-23

Instance Bellerophon[m2-9e74888e], SPECTREX5 (M2), model claude-opus-5-5, session 9e74888e.
Worktree D:/Prometheus-worktrees/bellerophon-post-campaign-forensics, branch
bellerophon/post-campaign-forensics-2026-09-23, base f17292833 (merge-base with origin/main a39bc1647).

## Boot (11:10Z-11:15Z)

- Canonical checkout D:/prometheus is on main (clean); work happens in the worktree above.
- `comms boot` recorded (EW_DB_HOST=192.168.1.202). `comms sync`: 0 new, queue length 0.
- Read: roles/base-role/RESPONSIBILITIES.md (boot, doctrine), aporia/doctrine/critical_memories.md,
  seat RESPONSIBILITIES.md, STATUS.md (pointer), and the directive
  prompts/2026-09-23_post_campaign_forensics/00_OPERATOR_DIRECTIVE_verbatim.md in full.
- Directive blob sha256 ecfd746d...96796 recorded in a new MANIFEST.md beside it (there was none).
- Monitors: none owned or fed (unchanged).
- Campaign pid 9372 not running. No resume/halt/finalize decision in decisions.jsonl: the campaign ran
  from one start to one stop; checkpoint restoration never happened in this campaign.

## Work items (boot step 6)

1. Evidence freeze -> EVIDENCE_MANIFEST.md + receipts/RUNDIR_DIGEST_ROOT.json. DONE.
2. Phase 1 census from raw files -> receipts/CENSUS.json. DONE (first pass).
3. Replay identity -> receipts/REPLAY_sample36.json. DONE: 39/39 runs, every file byte-identical.
4. Flag adjudication (Phase 3) -> receipts/FLAG_AUDIT*.json, GEOM_AUDIT_*.json, TRACED_*.json. RUNNING.
5. Independent adversarial audit (subagent, blind to my list) + snapshot reconciliation (subagent). RUNNING.
Blocker for Phase 6 (grounding round): Phases 3-5 must finish first (directive order).

## Findings so far (exploratory; forensic reading of historical data)

- Census: 63,247 runs, all dirs present, ids contiguous, no duplicates; 49,412 families, 39,613 with ONE run;
  31,196 promoted (63%): promotion (score >= 2) barely discriminates. 49 exact (vec, seed) duplicate pairs
  (97 verification runs); late-stage matched controls reuse the batch's first seed (up to 22x).
- Replay: the frozen harness regenerates runs bit-for-bit from config.json (39/39 incl. interventions).
- REACHED_UNDER_ENDOGENOUS_NOT_EXTERNAL: the reverse over the same 9,589 matched pairs is 1,596 (vs 393);
  exposure-matched first runs 443 endo-only vs 1,554 external-only (sign p 6e-15). 261/393 flags had
  more treatment runs (promotion exposure). The flag reads exposure; the opposite association holds.
- REACHED_INCREMENTAL_NOT_ATOMIC: INCREMENTAL 'solver' is partial credit; on a common exact ruler forward 59
  = reverse 59, first runs 65 vs 59 (p 0.65). The flag reads the ruler difference.
- REPRODUCTIVE_MACHINERY_RAISED_BENEFICIAL_DENSITY: gain distribution over 10,563 runs is centred at 0
  with the negative tail twice the positive; smoke audit of 6 flagged specimens: an IDENTICAL "mutant"
  scores 'better' 15-90% of the time (unpaired inputs), paired beneficial density ~0-4% and paired gain ~0.
  Full audit (493 flagged + 493 controls) running.
- REPRODUCTIVE_ARCHITECTURE_RESPONDED_TO_TASK: 579/600 SEEDED_HYBRID (both triggers seeded in); baseline
  span is a tick-0 event (98.5%), 38% of them fidelity < 0.5. Task-ON vs NEUTRAL first-run compression
  576 vs 344 (p 2e-14): an association on the wrong descriptor (max PC), to be regrounded (G5).
- First replication events: 14,003/15,072 RANDOM-init endogenous runs record one, 9,271 at fidelity ~0:
  first_replication records COPY_EVENTs. An in-place LDIR sweep (S=T) marks the whole window written
  and births a zero tape into an empty cell under ENDOGENOUS_COPY (positive control r000001, tick 0, fid 0.047).
- Traced replay (byte provenance + executing-PC region; tracer checked against stored summaries) of the
  first 5 spontaneous runs: genuine own-code self-copy, chains 46-444 generations, variants transmitted.
  Full spontaneous set (544) + 300-run baseline running.
