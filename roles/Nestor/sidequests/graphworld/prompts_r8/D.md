# D -- R8 SCIENCE. ANOMALY HUNTER. RUN IT.

Worktree F:/Prometheus-worktrees/nestor-r8-d. `git fetch origin && git merge --ff-only origin/nestor/sidequest-graphworld-2026-09-14` FIRST -- you need the committed GATE_MAP_R8.json or admission refuses everything.

OPERATOR EXECUTE ORDER: the build is over. Default action is RUN THE SCIENCE. Do not build infrastructure.
A defect interrupts science ONLY if it prevents execution, corrupts evidence, violates a preregistered
invariant, or makes the observation uninterpretable. Everything else -> residue/PC ledger, and continue.
C1 did not land, so cross-lane GPU work is refused at admission. Do not work around it, do not discuss it,
do not let it block CPU science. GPU exactness is OFF the list this round.

Read: SWARM_R8 s9 (BETA sweep), s11 (retrospective side threads); BOOT_R8 s4 (D block).

## Priority order -- start at 1 now

1. **CHEAP MINIMUM DISCRIMINATORS.** Fast, aimed, CPU. Start here; they are the highest yield per second.
2. **THE BETA SWEEP (PC 1789518268676-0), specific to AP-02.** G2 and G3 both landed, so it is authorised.
   Preregister BEFORE running: the beta values, the stopping rule, and the primary response variables.
   Measure at minimum: beta, fraction of possible charge actually paid, genome bytes, train fitness, held
   fitness, verdict. **Do not tune BETA after seeing the curve.**
   It does not resolve all anti-prior PASSes; it is one sweep on one cell.
3. **B-R5-1 residual anomalies** if capacity remains.
4. H4 saturation audit / H6 descriptor robustness (s11) -- cheap, little new simulation, good companions to a
   compute-heavy thread. H6: a claim that reverses under an alternate descriptor family becomes
   DESCRIPTOR_DEPENDENT; no cherry-picking the descriptor that preserves the story.

## Invariants (preregistered -- these DO stop you)

- **No open-ended campaigns.** Bounded experiments with a stated stopping rule.
- CANDIDATE_N binds at admission: 32 / 4 / 8, structurally balanced.
- Telemetry is WRITE-ONLY with respect to science: no telemetry field enters an eligibility check, control,
  discriminator or verdict. An anomaly in telemetry is FILED as an anomaly; it never relabels a verdict.
- Nothing failing before ELIGIBILITY is a hypothesis kill (P1).
- Every receipt declares `residue` (list or the token NONE).

Restart your worker after ANY harness edit. Never git write with a live RowWriter. Never pipe `bus inbox`.
Report to A: what ran, what the sweep showed, what persisted, what failed, what survived the controls.
