# B -- R8 SCIENCE. EXPLOIT / REPLICATION. CONDITIONAL LANE.

Worktree F:/Prometheus-worktrees/nestor-r8-b. `git fetch origin && git merge --ff-only origin/nestor/sidequest-graphworld-2026-09-14` FIRST -- you need the committed GATE_MAP_R8.json or admission refuses everything.

OPERATOR EXECUTE ORDER: the build is over. Default action is RUN THE SCIENCE. Do not build infrastructure.

Read: SWARM_R8 s6 (B-R5-1 replication and independent failure mode); BOOT_R8 s4 (B block).

## Your trigger

**Run the FROZEN B-R5-1 recipe IF AND ONLY IF code publishes a valid SECOND SURVIVED world.** G is resolving
the four PENDING cells now (Route B bound first, then Route A behind a feasibility precommit). If a cell
becomes SURVIVED, code publishes the replication trigger and A will tell you. Until then you are IDLE.

**Correct idleness is a result.** Round 7's B correctly did almost nothing and reported it cleanly. Do not
invent substitute work to look busy, and do not evolve a new candidate to have something to show.

## If the trigger fires

B-R5-1: w13 train128_held64, 16-byte int4a4, progress 1.591, CI [1.139, 1.827]. It remains CANDIDATE.

- Run the FROZEN recipe, **untuned to the new world**, frozen BEFORE the new-world result is read.
- **Evolving a new candidate is not replication.** Tuning B-R5-1 to a second world and calling it
  replication is explicitly prohibited.
- Required outputs: candidate score, world floor, float baseline, progress-above-floor, observation-use
  control, exact oracle results, per-family results, equal-search-budget accounting.
- Instrumentation displacement (no second host exists): frozen candidate bytes, fresh process, fresh
  worktree, independently generated seed manifest, and an independently implemented or independently routed
  evaluator against an exact reference. Record explicitly: **if the original harness contained a bug, how
  likely is this path to contain the same bug?**
- A second Claude lane is NOT an independent failure mode and may not be claimed as one.
- Per R8: the five-element displacement supports CANDIDATE -> REPLICATED. It does NOT support promotion.
  Promotion waits for genuinely independent hardware.

CANDIDATE_N binds at admission: 32 / 4 / 8, structurally balanced. Every receipt declares `residue`.
Never git write with a live RowWriter. Never pipe `bus inbox`. Report to A either way, including idleness.
