# R -- R8 SCIENCE. PRIOR PREDICTOR. PREDICT, DO NOT EXPERIMENT.

Worktree F:/Prometheus-worktrees/nestor-r8-r. `git fetch origin && git merge --ff-only origin/nestor/sidequest-graphworld-2026-09-14` FIRST.

OPERATOR EXECUTE ORDER: the build is over. Default action is RUN THE SCIENCE. Do not build infrastructure.

Read: SWARM_R8 s8 (anti-prior); BOOT_R8 s4 (R block).

## Your job, and only your job

A has published the r8 candidate cells (code-drawn, seed 20260921, binding-eligible via G2's pre-check).
**Post a prior for each candidate cell BEFORE assignment seals.** For every cell:

    P(PASS), expected direction, expected mechanism/failure, timestamp, predictor_id

Seal them with `anti_prior.seal(...)` in your own lane. Then stop and wait.

## Why this round is different, and why your predictions are finally at risk

All four round-7 assignments failed for CELL-CONSTRUCTION reasons -- the pressure could not bind, the oracle
could never fire, or both arms returned identical values. Your confident-failure calls were therefore never
put at genuine risk, and after two rounds the calibration question is still untested. G2 landed this round,
so every candidate cell has been verified by CODE to bind, resolve, fire and differ. Your predictions are now
falsifiable. Predict accordingly -- across the range, not only where you are confident.

## Invariants (these DO stop you)

- **PREDICT ONLY. NEVER EXPERIMENT.** You do not run jobs, you do not evolve, you do not measure.
- **Never see an outcome before your prediction seals.** If you have seen a result for a cell, say so and
  do not predict on it.
- Priors NEVER enter scientific scoring. They are scored for CALIBRATION only, descriptively, at close.
- Small N stays small N. No grand inference from a handful of cells.
- You are not told arms, ranks or quantiles while the round is live. Do not ask; do not infer from traffic.

Never pipe `bus inbox`. Report to A when every candidate carries a sealed prediction, with the count.
