# H -- R8 SCIENCE. JUDGE / REPLAY / RULE CONSISTENCY. RUN IT.

Worktree F:/Prometheus-worktrees/nestor-bld-h. `git fetch origin && git merge --ff-only origin/nestor/sidequest-graphworld-2026-09-14` FIRST -- you need the committed GATE_MAP_R8.json or admission refuses everything.

OPERATOR EXECUTE ORDER: the build is over. Default action is RUN THE SCIENCE. Your G2 is landed and in the
published gate map; do not revisit gate machinery, do not add gates, do not refine checkers unless a checker
actually prevents an experiment from running or invalidates its evidence.

Read: SWARM_R8 s8 (anti-prior), s15 (interpretation rules); BOOT_R8 s4 (H block).

## Priority order -- start at 1 now

1. **INDEPENDENT REPLAY of live r8 results as they land.** You are the judge: re-derive verdicts from the
   committed rows, not from the experimenter's summary. Disagreement is a finding, not an embarrassment --
   report it as a DISPUTE and it is preserved, never overwritten.
2. **RULE CONSISTENCY.** Every verdict-class receipt satisfies EVIDENCE_N_v1 (32 / 4 / 8, structurally
   balanced) and its predicate was pinned BEFORE the run. A verdict on an unpinned predicate is not a result.
3. **Calibration bookkeeping** for the anti-prior, descriptively only, at close: binding-eligible candidate
   count, prediction distribution, assigned ranks/quantiles, observed outcomes. Small N stays small N.
4. H3 tie-stability is NOT prepared (its tie-aware gate is D's unbuilt PC) -- do not build it. Skip.

## Invariants (preregistered -- these DO stop you)

- **HELD is a distinct scientific state.** Never read it as "almost SURVIVED" and never as replication.
- **Thresholds are not relaxed because survivor density turned out to be low.** The 5 HELD cells may be
  analysed descriptively for sensitivity only; any threshold change needs a NEW preregistered experiment.
- A PASS whose arms cannot discriminate is VACUOUS and must be reported as such.
- Nothing failing before ELIGIBILITY is a hypothesis kill (P1).
- Arms, ranks, quantiles and priors stay conductor-only while the round is live.
- **LLM lanes do not score themselves.** You judge others' evidence, not your own worth.

Restart your worker after ANY harness edit. Never git write with a live RowWriter. Never pipe `bus inbox`.
Report to A: which observations survived the controls, which did not, and every dispute you raised.
