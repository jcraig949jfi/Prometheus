# C -- R8 SCIENCE. DISTANT-QD / ANTI-PRIOR. RUN IT.

Worktree F:/Prometheus-worktrees/nestor-r8-c. `git fetch origin && git merge --ff-only origin/nestor/sidequest-graphworld-2026-09-14` FIRST -- you need the committed GATE_MAP_R8.json or admission refuses everything.

OPERATOR EXECUTE ORDER: the build is over. Default action is RUN THE SCIENCE. Do not build infrastructure.
A defect interrupts science ONLY if it prevents execution, corrupts evidence, violates a preregistered
invariant, or makes the observation uninterpretable. Everything else -> residue/PC ledger, and continue.

Read: SWARM_R8 s8 (anti-prior); BOOT_R8 s4 (C block).

## Priority order -- start at 1 now

1. **TRUE ANTI-PRIOR DRAWS, from BINDING-ELIGIBLE CELLS ONLY.** G2 landed, so the cell-binding pre-check is
   live: a cell enters the pool only if CODE verified the pressure can bind, the discriminator resolves, the
   oracle can fire, and the control can differ from the experimental arm in principle. A has published the
   r8 candidate list (seed 20260921). Draw from it and RUN.
   This matters because ALL FOUR round-7 assignments failed for CELL-CONSTRUCTION reasons -- the predictor's
   confident-failure calls were never put at risk. This round they are.
2. **Optionally ONE distant-QD draw** if capacity remains.

## Invariants (preregistered -- these DO stop you)

- **Never read `pm:prior:*`.** Arm, rank, quantile and prior are conductor-only while the round is live.
  Round 7 had three disclosure lapses. Do not name an arm in any record another lane can read.
- **No hand-selected redemption experiments.** Code chooses; you run what code drew.
- A PASS whose arms cannot discriminate is VACUOUS and must be reported as such -- r7 AP-03 returned exactly
  31.71875 in all 64 runs of both arms, IQR 0.0. Check resolving power before claiming anything.
- CANDIDATE_N binds at admission: 32 runs / 4 families / 8 per family, structurally balanced. Never "32 x 4".
- Nothing failing before ELIGIBILITY is a hypothesis kill -- it is INSTRUMENT_FAILURE, IMPLEMENTATION_DEFECT,
  RUN_INELIGIBLE, REPAIR_REQUIRED or INDETERMINATE (P1).
- Every receipt declares `residue` (list or the token NONE). NONE is admissible and counted.

Restart your worker after ANY harness edit. Never git write with a live RowWriter. Never pipe `bus inbox`.
Report to A: which cells ran, under which worlds and pressures, what discriminated, what survived controls.
