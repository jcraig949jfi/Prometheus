# E -- R8 SCIENCE. WATCHMAKER / TRANSFER INSTRUMENT. RUN IT.

Worktree F:/Prometheus-worktrees/nestor-r8-e. `git fetch origin && git merge --ff-only origin/nestor/sidequest-graphworld-2026-09-14` FIRST -- you need the committed GATE_MAP_R8.json or admission refuses everything.

OPERATOR EXECUTE ORDER: the build is over. Default action is RUN THE SCIENCE. Do not build infrastructure,
do not improve telemetry, do not repair latent defects. A defect interrupts science ONLY if it prevents
execution, corrupts evidence, violates a preregistered invariant, or makes the observation uninterpretable.
C1 is closed: do not discuss it unless a preregistered experiment actually reaches GPU execution.

Read: SWARM_R8 s7 (Clause B: investigate the sham); BOOT_R8 s4 (E block).

## Priority order -- start at 1 now

1. **H1 SHAM RESPONSE CURVE.** Preregister the frozen sham strengths/forms BEFORE running:
   identity / no scramble; partial feature permutation; full feature permutation; matched random replacement
   preserving gross scale; and at least one further structure-destroying control that preserves nuisance
   statistics where possible. Then run recipients across that ladder.
   Question: does recipient performance vary SYSTEMATICALLY with destruction of donor structure?
   Outcomes, all legitimate: (A) decreasing with destruction -- sham behaving as intended; (B) INCREASING --
   the transfer machinery may be measuring initialisation / regularisation / diversity rather than structure;
   (C) non-monotonic -- possible new mechanism. **None of these may be called "transfer"** until a structural
   discriminator supports it.
2. **Recalibrate any CHANGED control before applying it to a live pair.** A verdict on an uncalibrated
   instrument is not a result. If you change sham or control semantics, the changed instrument is
   recalibrated first.
3. B-R5-1 residual anomalies if capacity remains.

## Invariants (preregistered -- these DO stop you)

- The round-7 Clause B FAIL STANDS and is not relabelled. The hardened control did its job.
- **NO transfer matrix.** Not this round.
- Report the 0/40 planted-negative result as an INTERVAL: one-sided 95% upper bound 1 - 0.05^(1/40) =
  **<= ~7.2%**, never "0%".
- CANDIDATE_N binds Clause B at admission: 32 runs / 4 families / 8 per family, structurally balanced.
- Dry-run `envelope.admit` before posting a predicate, AND dry-run the receipt guard on a SYNTHETIC receipt.
  A round-6 Clause B pair ran clean and was then refused by the guard at receipt time.
- Every receipt declares `residue` (list or the token NONE).

Restart your worker after ANY harness edit. Never git write with a live RowWriter. Never pipe `bus inbox`.
Report to A: what ran, the sham ladder and its response, what survived the controls.
