# Builder brief, lane H, round 5 phase P-BUILD (delivered to the live H session)

You are Nestor-H, the MEASUREMENT builder, round 5 P-BUILD. You stay in F:/Prometheus-worktrees/nestor-bld-h.
- First: `git fetch origin` then `git merge --ff-only origin/nestor/sidequest-graphworld-2026-09-14`.

Read:
- SWARM_R5.md (your items are s3 H-R5-1..4; s1 O5 and O8 bind them);
- prompt 19 s4.4, s11, s12.

STAGE: SMOKE-stage build, HARD CAP 2 h wall from this brief.

Order:
1. H-R5-1 automatic receipt guard:
   - all nine checks of prompt 19 s4.4, plus campaign_stage, plus the CANDIDATE_N/BASELINE_N sample rule;
   - a machine-readable refusal reason per check;
   - a test per failure case.
2. H-R5-2 sealed anti-prior ledger:
   - predictor-only write;
   - the experimenter's read is denied until its receipt is filed;
   - fields prior_p_pass, prior_expected_direction, prior_expected_mechanism, predictor_id, prediction_ts;
   - prediction_ts must predate assignment;
   - code assignment by seeded draw among prior_p_pass <= 0.2;
   - a descriptive calibration table at close.
3. H-R5-4 EXPECTED_REFUSAL_OBSERVED classifier: a campaign label only, the judge untouched.
4. H-R5-3 F12 progress axes by rule: boundary_resolution, instrument_gain, error_metabolism credited to the refuter
   (O8), own-claim refutations 0.

Coordinate with F (job envelope fields, campaign_stage) and G (seed schema field names) before coding.

At 1 h 45 m post "H BUILD STATUS". At 2 h stop and post "H R5 BUILD DONE" with shas. Items not done become
PRODUCTION_CANDIDATE notes.

Tests gate commits; push via ops.push; journal H.md.
