Operator message to Nestor-A[m1-449a9e76], 2026-09-14 ~14:25, in chat
(verbatim). This committed file is authoritative over any later summary.
It answers decisions D1-D4 of ROUND2_PREP_BACKLOG_2026-09-14.md.

----------------------------------------------------------------------

1. Domain Lanes or Cohorts?
Convert the lanes into Cohorts.
Do not ask a single LLM to context-switch between "exploit" and "falsify" internally—it will default back to its conversational, crowd-pleasing mean. Hardcode the budget split into the very identity of the agents by mapping them directly to the 4 slots:
 Lane B (The Hill Climbers - 40%): Pure exploitation. They take the highest-fitness cells from the ledger and try to optimize them across any domain.
 Lane C (The Anti-Prior - 25%): Pure falsification. Their system prompt strictly forbids them from chasing high-fitness scores. They must attack the LLM consensus.
 Lane D (The Anomaly Hunters - 20%): Serendipity. They only read from the ⁠ANOMALY⁠ queue and attempt to resolve weird edge cases.
 Lane E (The Watchmakers - 15%): Tooling and Instrumentation.
By making them cross-domain cohorts, they are forced to look at the intersection of soup, brain, and lingua, which is exactly where the serendipitous architectural discoveries usually hide.
2. Contract Benchmarks & Baselines
Start with Option A (Compression) using the existing MAP-Elites baseline.
Do not block Round 2 waiting on baselines and harnesses that don't exist yet.
 The target: Can they beat the MAP-Elites baseline (or achieve parity) with a smaller byte-footprint, fewer parameters, or a tighter symbolic representation?
 The evolution: Assign Lane E (The Watchmakers) the explicit task of building the Domain A → B transfer harness and a basic Transformer baseline as part of their Round 2 tooling mandate. Once Lane E commits those tools, you trigger Option B (Transfer) for Round 3.
3. CHIMERA-0 vs. Contract-Driven Cells
Contract-driven cells only.
CHIMERA-0 is the integration target—the final boss. If you aim for end-to-end integration now, the agents will write massive, brittle monolithic scripts that fail catastrophically and trigger the TTL before yielding any scientific state.
Focus entirely on expanding the QD (Quality Diversity) ledger. We want a populated database of robust, isolated, and compressed mechanisms. Integration comes later once you have a library of proven primitives.
4. The Gate (F1–F6)
Hard agree. Gate F1–F6 must be locked in before booting.
F1–F6 represent the foundational laws of physics for your simulation (heartbeats, strict logging, immediate commits, and isolated warming). You cannot run a TTL-enforced evolutionary simulation without a reliable heartbeat and a persistent ledger.
F7–F15 (stream workers, auto-scoring, epochs) are the evolutionary pressures. It is perfectly fine to roll those out dynamically during the test launches as the swarm begins to stabilize.
Lock in F1-F6, assign the Cohort identities, and let's see what happens to the Anti-Prior lane when it's forced to survive outside of its conventionality bias.
