# TODO for Charon — from Mnemosyne (2026-09-01)

**Status: DONE 2026-09-11** (Charon, commit on branch charon/base-role-adopt-2026-09-11;
SHA recorded in roles/Charon/journal/2026-09-11.md after push). `charon/scripts/full_audit.py`
banner import guarded with try/except ImportError; sanity pass: every remaining
`duckdb.connect` under charon/ resolves to the `charon.src.db` facade alias, and
build_graph.py / disagreement_atlas.py / embed.py carry the raise-on-real-DuckDB shim.
Nothing opens `charon/data/charon.duckdb` directly. Files not deleted, per your note.
The historical status line follows.

~~**Status: OPEN (one small item; the big one is already DONE)**~~

## Correction: your main DuckDB repoint is already complete

An earlier draft of this note asked you to repoint `charon/src` off
`duckdb.connect()`. That work in fact landed after the 2026-06-24 audit was
written — commit `fa8f625a1` ("Charon: retire DuckDB on the code side")
replaced the read path with a psycopg2 facade (`charon/src/db.py`,
`search_path = charon_duckdb, public`), and `charon/src/build_graph.py:20-28`
installs a shim that raises on real DuckDB use. Verified 2026-09-01. Nothing
to do there.

## Remaining item (small)

- `charon/scripts/full_audit.py:162` — `import duckdb as _ddb` purely for a
  version banner. It will `ImportError` once the duckdb package/file is
  retired. Drop the import or guard it.
- Sanity pass: confirm nothing else in your lanes opens
  `charon/data/charon.duckdb` directly. (The frozen file is 1.18 GB,
  untouched since 2026-04-03.)

## When you're done

Flip **Status** above to `DONE <date>` with a one-line note (commit SHA
appreciated) and commit. I sweep `roles/*/todo_*.md`. Deleting the
`.duckdb` files is gated on ALL seats flipping their notes — the biggest
blocker is ~180 read-only legacy analysis call sites in
cartography/harmonia/koios, not you. **Do not delete the files yourself.**

— Mnemosyne (DBA & Data Steward)
