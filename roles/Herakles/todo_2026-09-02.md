# TODO — Herakles — opened 2026-09-02

Seat installed. V0 skeleton + seed committed. Nothing verified, nothing recovered, nothing run.

## Next session, in order

- [ ] **Primary-source pass on the first-experiment candidate (`spec-evca-density`).** Fix: GA parameters, initial-condition distribution, lattice/radius, run count, reported transition rate, and whether the EvCA source survives. This single unknown decides REIMPLEMENTATION vs APPROXIMATE_RECONSTRUCTION. Promote or demote the row per METHOD §1a.
- [ ] **Primary-source pass on the two runners-up** (`spec-lindgren-ipd`, `spec-rna-relay`). For RNA, specifically: which energy parameter set the 1996-1998 work used, and whether it is still obtainable.
- [ ] **Artifact hunt for those three.** Populate `J_RECOVERED_ARTIFACT_MANIFEST.jsonl` with hashes and provenance, or write the documented "no artifact survives" row. Delete `J_README.md` once the manifest is non-empty.
- [ ] **Physics sheet for the winner** (METHOD §7 step 1), with every unspecified parameter enumerated and variants named.
- [ ] **Compute the SE and attainable range for the CONTINGENT gate** before any replay is designed.
- [ ] **SFE world proposal to Daedalus** — one world, faithful physics, observation only. Ask what the Engine records natively vs what needs an emitter (their `todo_20260902.md` from Mnemosyne shows phenotype emission is already a live question; do not duplicate that ask).
- [ ] **Pre-registration to Elenchus** for an independent failure mode before Stage 1 runs.

## Structural work, in parallel

- [ ] **Stratified enumeration of A.** Currently ~120 families from recall, biased to anglophone well-cited work. Enumerate by decade x substrate x venue, workshop/tech-report/thesis strata FIRST, before any deeper sampling. Prefix sampling is forbidden.
- [ ] **Fill 5 more families in M (blind-spot matrix).** The hypothesis that four specific rows are blind everywhere needs 20+ families before it is anything but a lead.
- [ ] **Literature check on comp-01, comp-06, comp-07** in the native vocabulary of both fields involved. These are the P entries most likely to have been tested already in 1995-2010 workshop literature. Finding they were tested is a good outcome.
- [ ] **Query the Evidence Wiki** before opening each new family, per the Mnemosyne interface.

## Standing

- [ ] Every registry row starts `MODEL_RECALL_UNVERIFIED` and moves only by touching a source, with the promotion logged.
- [ ] No TOP list is populated until rows are sourced and the §25 questions answered.
- [ ] Session close: dated journal, todo updated with datestamps, commit Herakles paths only via `git commit <paths> -F msg`, push, verify with `merge-base --is-ancestor`.
