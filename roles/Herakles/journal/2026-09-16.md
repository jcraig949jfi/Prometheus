# Herakles journal, 2026-09-16 (instance m2-5dfd8a81)

Receipt: built from ccb26df01 (origin/main at 11:3x UTC) in
D:/Prometheus-worktrees/herakles-boot-2026-09-16, branch
herakles/boot-2026-09-16, model claude-opus-5-1m, harness session
5dfd8a81-bcf8-416c-bdb2-67e0b74b9f19, machine SPECTREX5 (M2). The
canonical checkout (D:/Prometheus, git-dir == git-common-dir) was refused;
no mutating operation was run there. Worktree add of the full tree
completed under a 900 s budget without a timeout.

## Boot

- Read order followed: base RESPONSIBILITIES + WORKING_CONTRACT, then
  BOOTSTRAP, RESPONSIBILITIES, CHARTER, METHOD, the 2026-09-11 Archaeon
  prompt (hash 6039f09a... re-verified against its MANIFEST from the
  origin/main blob), todo_2026-09-03, `git log -20 origin/main`, and the
  sibling commits on roles/Herakles and herakles since 09-11 (one:
  Theophrastus 57e15dc34, the player-id question).
- `python -m comms boot Herakles` on M2 first REFUSED with
  WRONG_ENVIRONMENT (M2's local Postgres is not the canonical store;
  incident class c84e26826cc12217, known). Re-run with EW_DB_HOST=
  192.168.1.202: booted. Recorded here so the next M2 boot does not
  rediscover it. (Candidate BOOTSTRAP capability-inventory line.)
- `comms sync`: 10 new, 4 queued; queue length 5 (#9 open from 09-11,
  then #241, #246, #247, #248). Read, no act: #174 (instance-aware comms),
  #231 (Techne, Z: share not reachable from M1; my large-binary location
  is a configured path and nothing of mine is on Z: today), #249 and #252
  (Theophrastus round-2 packet and THEO-14: my C1-e numbers are consumed
  and re-derived bit-exactly by another seat; no request to me beyond
  the REQs), #255 (Harmonia's C3-3 preflight NO-GO; addressed to
  Archaeon and me for information; my c3_null_check is not in its scope).
- Watchdogs (boot step 8): this seat owns and feeds NO standing loop,
  monitor, shadow or validator; MONITORS.md has no Herakles row and needs
  none. Stated, not skipped.
- `comms who`: Archaeon and Daedalus online on M2; Vivarium, Theophrastus,
  Harmonia, Nyx offline; Proteus never_booted.

## Work items suggested at boot (step 6), then taken in queue order

1. #9 item 4, cellwise synchronisation criterion      DONE
2. #248 / #246 / #247 / #241 library halves            DONE
3. #242 player-id ruling                               DONE
4. #9 item 3, ca_stream_v2                             BLOCKED (Vivarium)
5. L-7 rule search                                     NOT STARTED

## What happened, with numbers

Item 4. `cellwise_synchronisation_match` in herakles/evca/core.py: a cell
counts if it flips between T and T+1 and equals the ring's majority at
T. Exact identity `fraction_all_cells_sync == synchronisation_score`
asserted on 14 tables x 2 samples. Expectations written into the code
BEFORE any run (commit dbc41fd2f), then measured (6c9c55bfb,
sync_floor_2026-09-16.json, N = 149, 100 ICs seed 20260910, 298 steps):

    20 random tables   mean of means 0.2274, range [0.0565, 0.3072],
                       sd of means 0.0489 (SE of the mean 0.011)
    constants          0.0000 exactly (no cell flips)
    blinker, random    0.5305 (analytic majority share 0.5327; SE 0.002)
    blinker, k ones    max(k, N-k)/N to 1e-12 for 8 values of k (cheat)
    blinker, uniform   1.0000
    genomes            maj/exp/par/particle2/GKL 0.0000; particle1 0.0034

PREDICTION LOST. I wrote "about 0.27 in a NARROW interval, spread about
0.005". Measured spread 0.049, ten times wider, because the flip fraction
is a rule property (0.113 to 0.569 across the 20 tables; r = 0.99 with
the mean); and the mean is 3.5 SE below the independence value 0.266
because flip and in-phase are anticorrelated across tables (r = -0.79).
Annotated beside the prediction in core.py, in the test, and in the
CRITERIA row. Consequence: the floor is a band and an organism is quoted
against its top (0.31 at this geometry), never against the mean.

particle1: mean 0.0034 with sd 0.0334 across ICs is one IC of 100 with
about 100 of 149 cells still flipping at T = 298 (flip fraction 1/149
averaged over ICs). A row. Not replicated. In the TODO.

REQ-006. `make_ics(exact_count=k)`: seeded uniform permutation of k
ones; refused with `density`; third ensemble with its own stream; default
ensemble byte-identical to before (asserted). Position-uniformity control
at 4000 ICs and its cheat (unshuffled base) in the tests.

REQ-004. Library keeps `witness_truncated = n_wrong > limit`; the
completeness of a length-64 witness is decidable from `n_incorrect` on
every row. Added `correct_mask_hex` (whole per-IC mask, packbits
big-endian) and `unpack_mask_hex`; existing digests verify against the
unpacked mask. The validator boundary is Vivarium's; position filed in
their inbox.

REQ-005 / REQ-003. herakles/evca/derive.py: derive_edit, derive_flip,
derive_crossover (+ one-point and uniform masks), derive_transform,
verify_record. Record = child hex, content-derived child player id,
parents by content id, operator, canonical params, route-derived
derivation_id, identity flag. Mints nothing. Owner of minting unruled
(Archaeon); the library is done under either answer.

#242. Ruled: player id = evca:r3:<canonical hex>, derived from content,
nobody mints; PEW column mapping given; existing rows not re-keyed.

Tests: 166 herakles (was 137); Vivarium ca_density/cellwise/transform
and Theophrastus suites 103 pass against the changed library.

## Not run

- Item 3 (ca_stream_v2): kinds.py has no ca_stream kind at ccb26df01.
- L-7 search; particle1 replication; mean_flip_fraction survey.
- No PEW row written; no kind contract edited.

## Close

Pushed to main as a fast-forward on the third attempt (Techne fe60cab7e
and Proteus 2fe8d6855 landed between my fetches; each merged explicitly
by SHA and re-tested, 280 pass each time): dbc41fd2f, 6c9c55bfb,
bfe8f0bd8, merges 3f53bace3 / da45e68ad / 4b7837a8e; origin/main
4b7837a8e; every SHA verified an ancestor after the push.

comms: #271 ruling to Theophrastus (player id, reply-to 242); #272 report
to Theophrastus, Vivarium, Nyx, Proteus (library side of the four REQs);
#273 delegation to Vivarium (three kind fields, validator boundary); #274
report to Archaeon (the packet, reply-to 9). Queue items 241, 246, 247,
248 marked done; #9 stays OPEN for item 3.

Closing sync brought #268: Archaeon's ruling that PROTEUS mints derived
CA mechanisms and Herakles owns the operator's semantics, delivered as a
pure function + tests in its own commit before any wrapper. That is
derive.py at dbc41fd2f, already on main. My player-id reply's "the
submitting seat writes the PEW row" paragraph is superseded and
annotated; ack posted with the interface Proteus consumes.

comms self-tests (comms/tests) were NOT green from this worktree: 1
failed, 3 errors, all database-connection shaped without EW_DB_HOST in
the pytest environment; not my lane, not chased, recorded.

Next executable action: L-7 recovery search for the 1995 synchronisation
rule (author-site PDF first), then the particle1 5-seed replication.

## Operator, in chat, after the close (2026-09-16 ~12:05 UTC)

"Probably noticed that we started up on M2. M1 is running a different
ecosystem for now. We are going to run the SFE ecosystem on M2 now."

Read for this seat: no code or record of mine binds to a host (grep of
herakles/ and roles/Herakles/ for 192.168, localhost, EW_DB_HOST, db_host
in .py: none). comms still resolves to M1 via EW_DB_HOST until Hermes or
Archaeon migrates it. X-5 (Vivarium kind fields) now needs the Vivarium
consumer on M2. Any future Herakles run against the engine gates on the
M2 instance id (Daedalus journal 2026-09-16: eng_906356f7..., port 8811,
D:/Prometheus-data/sfe). No run is pending; the next action, L-7, is a
recovery and needs no engine.

Post-close: base-role self-test RED on the merged tree at 427d8da37 from M2
(11 setup errors without EW_DB_HOST; 3 failures with it, all on Vivarium's
VivariumConsumerM2 registry row). Reported to Archaeon, cc Vivarium:
roles/Archaeon/INBOX_HERAKLES_BASE_ROLE_TEST_RED_ON_M2_2026-09-16.md.
