# Clymene journal -- 2026-09-11 (adoption pass)

> Inherits roles/base-role/RESPONSIBILITIES.md and WORKING_CONTRACT.md (operator, D-23, 2026-09-11); this file adds to them and may not contradict them.

Workspace: base_sha 8714b2709ffa3f1a5d55781d476c3eba4c97a898, branch
clymene/base-role-adopt-2026-09-11, worktree
Prometheus-worktrees/clymene-base-role, dirty=false at boot, host M2
(SPECTREX5). Model claude-opus-5[1m], tier heavy.

## What happened, in order

1. The operator's directive arrived in chat (verbatim in
   prompts/2026-09-11_adoption/OPERATOR_DIRECTIVE.md): bootstrap,
   create a role under roles/, adhere to the base-role concept, do
   nothing else except report what the agent did when it was active.

2. Read agents/clymene/ (README, manifest, source, registry, log,
   three reports). Read roles/base-role/ in full: README, NORTH_STAR,
   RESPONSIBILITIES, WORKING_CONTRACT, INHERITANCE, MONITORS. Read
   roles/Pheme/ as the pattern for a seat created on an adoption pass.

3. WORKSPACE. `git rev-parse --git-dir` equalled `--git-common-dir` in
   the Prometheus checkout on this host: it is the main worktree, so
   working there is refused (WORKING_CONTRACT s1). `git fetch origin`
   (never pull) gave origin/main = 8714b2709, identical to local HEAD.
   `git worktree add ... -b clymene/base-role-adopt-2026-09-11
   8714b2709` from the recorded SHA, unbounded timeout budget (900 s;
   39,284 files updated, completed). archaeon.workspace.receipt()
   confirms main_worktree=False.

   NOTE for the record: I ran `git pull` once at the very start, before
   reading WORKING_CONTRACT, because the operator's message said to.
   It reported "Already up to date" and changed nothing (HEAD before
   and after = 8714b2709). It was still a forbidden operation in the
   canonical checkout and it is recorded here rather than omitted.
   Every subsequent git operation was an explicit fetch plus a named
   SHA.

4. COMMS. `python -m comms who` failed with relation "comms.agents"
   does not exist. Cause, measured: the evidence_wiki resolver on this
   host reaches a LOCAL prometheus_fire at ::1 holding only the ew and
   public schemas. The canonical queue is on M1. Probed M1 from here:
   5432 and 8377 both OPEN. With EW_DB_HOST pointed at the M1 host the
   canonical database answers (23 schemas including comms, agora, viv;
   comms.agents 12 rows; max message id 45). Did NOT run `comms init`
   locally, which would have created an empty comms schema in the wrong
   database and made every future post vanish into something that
   answers healthy. Logged as calibration/LEDGER.md CLY-CAL-004.

5. `comms boot Clymene` refused first with ValueError: unknown agent
   'Clymene' -- the roster is roles/*, and roles/Clymene did not exist
   yet. This is precisely the behaviour Talos recorded in
   INHERITANCE.md ("comms boot refuses a seat whose roles/<Seat>/ is
   not on the tree it runs from while comms sync accepts it"). Created
   the seat directory first, then booted successfully.

6. ARCHAEOLOGY of the March agent. Sources: commit eb17886fa
   (2026-03-23, creation) and the four later commits touching
   agents/clymene (7e9719cb0 03-24, 8af6ba9e5 03-27, 9e189d683 04-01,
   b674a9976 04-03); data/clymene.log (647 lines, four run dates);
   data/last_run.txt; data/vault_registry.db (40 rows); configs/
   manifest.yaml (26 repos / 20 models / 8 datasets); the three
   committed hoard reports; agents/pronoia/README.md and logs;
   aporia/docs/program_audit_2026-06-10.md;
   aporia/docs/STATUS_2026-06-15_reset.md;
   pivot/COMPONENT_DOSSIERS_2026-06-24.md;
   pivot/agent_portfolio_and_monitoring_2026-05-12.md;
   engine/ledger/AGENT_AUTOPSIES.jsonl; and the vault on this host.
   Findings in RESPONSIBILITIES.md section 0; classification in
   QUEUE_ARCHAEOLOGY_2026-09-11.md.

   Numbers, so they are in one place: four hoard cycles over nine days
   (2026-03-22 to 2026-03-31); 26 of 26 repos, 14 of 20 models, 0 of 8
   datasets; 2 gated-model 403 failures that still occupy the vault as
   55 KB stubs counted as models by every report; three hoard reports
   whose delta across eight days was 8 upstream hashes and 10 MB; 164
   days dormant; 51 GB on M2; 0 of 26 repository trees are git clones;
   0 code consumers of the vault path.

7. THE NEAR MISS. Checked vault freshness with `git -C
   vault/repos/<name> log -1 --format=%cs` and got 2026-09-11 for all
   26 trees, which reads as "archive is current". It is not: none of
   those directories is a git repository, so git walked up to the
   enclosing Prometheus checkout and reported that repository's HEAD,
   26 times. Caught by testing for .git directly (0 of 26 have one).
   calibration/LEDGER.md CLY-CAL-003. This would have been the first
   false line in this seat's first status file.

8. Wrote roles/Clymene/: RESPONSIBILITIES.md (entry file, banner
   stamped), STATUS.md, QUEUE_ARCHAEOLOGY_2026-09-11.md,
   BACKLOG_H0H5.md (provisional and says why it is below the schema
   floor), calibration/LEDGER.md (4 rows), journal/2026-09-11.md,
   prompts/2026-09-11_adoption/ with MANIFEST (verified, 0 mismatches).

9. Added this seat's OWN rows, per Archaeon's ruling of 2026-09-11
   (comms message 39, seen on this pass): two rows in
   roles/base-role/INHERITANCE.md (stamped-document table and
   entry-file table) and one row in roles/base-role/MONITORS.md
   (ClymeneHoardCycle, DORMANT since 2026-03-31 with its cause named).
   Nothing else in either file was changed; both remain pure ASCII.

10. TESTS on the merged tree (branch == origin/main + this seat's
    files): `python -m pytest archaeon/tests/test_base_role.py -q` ->
    7 passed, 1 failed. The failure is
    test_every_enabled_prometheus_scheduled_task_on_this_host_is_registered
    and is PRE-EXISTING, not caused by this commit: three enabled M2
    tasks (MnemosyneEvidenceWikiWatchdogM2, PrometheusMachineProbeM2,
    SFEngineM2Watchdog) have no registry row. This seat's only
    MONITORS change is an addition, and the test asserts that host
    tasks minus registered names is empty -- adding a name can only
    shrink that difference. PrometheusMachineProbeM2 returns
    2147942402 (0x80070002) on every fire, the same defect the registry
    already records as DEAD for its M1 twin. Reported to Archaeon,
    Mnemosyne and Daedalus rather than fixed in another seat's lane
    (prompts/2026-09-11_adoption/REPORT_M2_UNREGISTERED_TASKS.md).

## What was NOT run, and why

- agents/clymene/src/clymene.py was not run, in any mode, not even
  `status`. The operator's directive was bootstrap and registration
  only; separately, the script imports PyYAML and PyYAML is not
  installed on this host, so it is not known to run here at all.
- No acquisition, no clone, no download, no vault write, no vault
  delete. 51 GB of gitignored artifacts on a shared disk is a
  destructive surface and this seat holds no acquisition authority.
- No scheduled task created, none disabled, none claimed.
- No file outside roles/Clymene/ and the two base-role registries was
  changed.
- The full-repository `grep -r` for host addresses was killed after it
  began walking the 51 GB vault; replaced with `git grep` over tracked
  files. Recorded because the first command was the wrong instrument,
  not because it found anything.

## Receipt

    assigned      1 (the operator's adoption directive)
    completed     1
    failed        0
    artifacts in  agents/clymene/* (read-only), roles/base-role/* (read),
                  vault/ on M2 (read-only, stat and .git presence only)
    artifacts out roles/Clymene/ (8 files), 2 INHERITANCE rows,
                  1 MONITORS row, 1 comms boot, 1 comms sync receipt,
                  1 comms report
    controls      none applicable: this pass measured state and made no
                  claim that a control could falsify. The one
                  measurement that could have been wrong (vault
                  freshness) was checked against the property (.git
                  presence) rather than the command's output.
    cost          one session, no compute, no network acquisition
    outcome       seat created, base role adopted, March queue
                  classified, 0 items executable
    blocker       an operator decision (CLY-01): is Clymene chartered,
                  and what happens to the 51 GB vault
    next action   none without that decision
    enables       this enables the operator to decide the vault's
                  disposition against measured numbers instead of a
                  remembered mission, and enables Mnemosyne to test
                  whether an acquisition-provenance ledger belongs in
                  the evidence substrate (CLY-04)
