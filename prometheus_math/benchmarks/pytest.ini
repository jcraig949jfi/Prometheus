[pytest]
# Local pytest config for the benchmarks directory.
#
# Make pytest collect `bench_*` callables from `bench_*.py` files,
# in addition to the usual `test_*` prefix used in tests/ subdirs.
python_files = bench_*.py test_*.py
python_functions = bench_* test_*
python_classes = Bench* Test*
