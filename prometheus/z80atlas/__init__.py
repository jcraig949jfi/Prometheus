"""z80atlas -- the 72-hour Z80 x Atlas combinatorial campaign harness (Bellerophon, 2026-09-19).

A frozen, autonomous producer/consumer search over a declared factor grammar: world physics x organism
representation x reproductive physics x pressure coupling x spatial/ecological structure x accessibility
manipulations x Atlas transplant axes. No HITL, no LLM in the loop, no post-result threshold changes, no new
experiment semantics during execution: Python instantiates, mutates, combines, schedules, promotes and retires
experiments ONLY from the preregistered grammar (grammar.py, hashed at campaign start).

Core principle: reproduction is an evolvable, EXECUTED behaviour. Under an ENDOGENOUS treatment an organism persists
only by executing writes that cause descendants; the population manager never reproduces for it (world.py guards
this and the observatory counts it). Matched EXOGENOUS controls always exist.

Modules: vm (Z80-like byte-addressable substrate), tasks (task worlds + accessibility manipulations), world (soup /
grid / niches / physics / pressure / migration), grammar (frozen factors + sampling), geometry (mutational topology,
damage cliffs), observatory (records, serendipity, anti-cheat, triggers), runner (one experiment), scheduler (the
72-hour campaign), controls (positive controls), campaign (CLI).
"""
