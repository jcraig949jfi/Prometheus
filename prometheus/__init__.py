"""prometheus -- the Prometheus Worlds Kernel package root (D-BELL-1, operator 2026-09-18).

Only `prometheus.toolbox` lives here today. Nothing in this package runs an LLM, adjudicates a
claim, or executes an experiment on import.
"""
