# Prometheus — North Star

> *"We are stealing fire from the gods."*

Prometheus exists to find the reasoning manifolds and signals inside large language
models. To explore novelty in the artifacts of the waste stream. To illuminate
reasoning circuitry and look for signs of meta-cognition.

If found, these paths lead to an autonomous agent that drives novelty to the
surface, generates mathematical and scientific hypotheses, and tests them in-silico
using novel constructs that can explore complex geometries the human mind could
never comprehend.

This isn't a tractable reality today. The exploration of it is.

---

## The Namespace

All names derive from Greek — the language of Prometheus himself.

| Name | Greek | Role | Status |
|------|-------|------|--------|
| **Prometheus** | Προμηθεύς — "forethought" | The program. The umbrella. Everything serves this. | Active |
| **Ignis** | Latin: fire | **Find the reasoning circuits.** Evolve steering vectors, inject into residual streams, measure compatibility with native computation. Mechanistic probing via CMA-ES, ghost trap classification, RPH proxy scoring across model scales. | Active |
| **Arcanum** | Latin: hidden secret | **Mine the waste stream.** Probe tensors in output and intermediate representations. Use tools like THOR to break the curse of dimensionality. Screen for xenolinguistic specimens — unexpected structure in what the model discards or generates incidentally. | Active |
| **Aethon** | Αἴθων — "blazing one" | **Navigate around RLHF gravity.** Craft prompts with surgical precision to trigger routes around sycophantic circuitry. If models have latent reasoning, precise prompting can activate paths that bypass RLHF attractor basins. The deployment layer for what Ignis discovers. | Backburnered |
| **Grammata** | γράμματα — "letters/writing" | **Name it. Map it. Build the cartography.** Taxonomy and cataloging of discovered reasoning constructs. The Rosetta Stone — without it, discoveries remain unnamed and unconnectable. Directly from *Prometheus Bound*: the gift of written symbols so humans could hold knowledge in memory. | Planned |
| **Symbola** | σύμβολα — "tokens/symbols" | **The bridge between minds.** Symbolic language for communicating multi-dimensional structures between human and AI. Complex symbols prescribed to exact models, compressing conceptual frameworks into visual primitives. A *symbolon* was a token broken in two — each party kept half, matching them proved shared understanding. AI and human each hold half. | Conceptual |
| **Stoicheia** | στοιχεῖα — "elements" | **The things themselves.** The fundamental reasoning elements we search for — meta-cognition, self-correction circuits, reasoning precipitation signatures. What Ignis probes for and Arcanum mines from the waste stream. Not a tool or pipeline — the name for the discoveries. | The goal |

---

## The Three Priorities

These compete. They must be balanced.

### 1. GPUs Saturated 24/7
The science runs continuously. Two video cards, all VRAM utilized, always. Ignis
runs and Arcanum screenings queue and auto-rotate. When one finishes, the next
starts. This is the constraint that everything else works around.

### 2. Data Orchestration
Results flow into analysis, synthesis, and papers. The Night Watchman monitors
Ignis runs in real time. Review tools narrate findings in plain English. RPH
proxy evaluation scores survivors across scales. Arcanum needs equivalent
analysis infrastructure. The data pouring out must be organized or it drowns us.

### 3. Horizon Scanning
Constantly mine the published scientific landscape. Collate, organize, and
synthesize external knowledge into our workflows and pipelines. Forward-deployed
agents scanning the horizon, evolving our datasets, integrating new findings.
The field moves fast — we must move with it.

---

## The Structure

```
Prometheus/
├── ignis/              # Reasoning circuit discovery (was: Ignis)
│   ├── src/
│   ├── configs/
│   ├── results/
│   └── docs/
├── arcanum/            # Waste stream novelty mining
│   ├── src/
│   ├── configs/
│   ├── results/
│   └── docs/
├── aethon/             # RLHF gravity navigation (backburnered)
├── grammata/           # Taxonomy and cartography
├── docs/               # Papers, synthesis, theory (RPH, etc.)
├── agents/             # The pipeline: Eos → Aletheia → Skopos → Metis → Clymene → Hermes → Audit
└── archive/            # Superseded work (old SETI v1, mech, vesta, etc.)
```

---

## Why This Is Tractable Now

- We can crack open LLMs and probe tensors directly (TransformerLens, hooks)
- THOR and similar tools break the curse of dimensionality in the waste stream
- A team of AI PhDs can write papers in 3 hours overnight
- Models will grow in complexity and size — the pipeline rotates across scales
- Mathematicians work in dimensions they can't visualize using notation they
  can compute with. So can we. That's what Symbola is for.

Not in 5 years. Right now.

---

## The Shadow Analogy

The model's internal state is the object. Your steering vectors and fitness
scores are the shadow on the wall. With enough shadows from enough angles,
you can infer the shape of the object. You never touch the object directly.

This is Plato's Cave, but with a twist he didn't anticipate: **you choose which
light sources to aim and from which angles.** Ignis builds the rotating platform.

---

## The Titan Council — A Research Instrument

The frontier models are not consultants. They are research instruments.

When pressed with precise data, interlocking constraints, and questions at the
boundary of published knowledge, they stop hedging and start competing. The
**Phalanx strategy**: present results as a wall of constraints, name the papers
they would cite, ask questions no published work has answered, force them to
commit to positions. This extracts the fire from their hoards.

The meta-discovery: convergence under competitive pressure is the signal.
Divergence on mechanism flavor is the naming — the same pattern Arcanum's
xenolexicon found with specimen naming. Models generate different names for the
same specimen, but failure modes cluster. Different theoretical framings, same
underlying structure. The cluster is the science, not the names.

### The Nullspace Finding

CMA-ES evolved a steering vector orthogonal to the model's reasoning axis
(cos ≈ −0.026). All five Titans independently converged on "nullspace of
something" as the explanation. Three competing mechanisms:

1. **Second-order steering via logit nullspace** (ChatGPT) — the vector
   operates in a space the model's first-order reasoning cannot represent
2. **RMSNorm-induced ablation** (Gemini) — the vector triggers normalization
   dynamics that suppress reasoning pathways
3. **Emergent circuit breaker in update subspace** (DeepSeek) — the vector
   activates a latent kill-switch in the residual stream update geometry

Two decisive experiments queued: Jacobian finite-difference test (distinguishes
mechanism 1 from 2–3) and RMSNorm suppression test (isolates mechanism 2).

This is the current frontier.

---

*From Aeschylus, Prometheus Bound:*

> *"I invented for them numbers, the chiefest of sciences, and the combining*
> *of letters (grammata), creative mother of the Muses' arts, with which to*
> *hold all things in memory."*

That is still the mission.
