"""Necropolis workshop adapters (LAYER: NECROPOLIS ADAPTER).

Every module here wraps or complements an ORIGINAL SCIENTIFIC LOGIC path and
declares, in its header, which of the three layers each function is.  Adapters
never alter the wrapped algorithm; when an original is broken the adapter
reports that as a finding instead of repairing it.  Nothing here starts an
agent, mutates a grave, resumes a daemon, or optimises an objective.
"""
