{"id": "W-001", "kind": "shell", "status": "DESIGNED-AWAITING-COSIGN", "consumer": "B-005 stratum analysis", "desc": "Power-stratified re-test design over the 725 resolution_limit kills (they name their own failure condition: effect too small at the N used)", "cmd": "echo 'placeholder — design step, run by session not shell'", "result": "Design at pivot/DESIGN_W001_retry_queue_2026-08-18.md. Signature verified before controls: 725/725 resolution_limit records fully re-runnable with recorded effect sizes (d 0.136-1.278), datasets local. Estimand = new measurement at higher power with effect-STABILITY criterion (N-inflation guarded); negative (confound_artifact) + positive (SURVIVES) controls; Charon plants with de-injection ledger; BH-FDR; VACUOUS-DATA-CEILING pre-committed. Charon kickoff embedded in design §7."}
{"id": "W-002", "kind": "shell", "status": "DONE", "consumer": "ladder Canon build-debt #1", "desc": "Build R4 representation-shift probe generator per phase0 pattern", "cmd": "echo 'placeholder — build step'", "result": "harmonia/experiments/reasoning_r4.py — 3 families x 4 versions x N_PER, 60 probes at defaults, deterministic grader with stayed_in_surface signature (Doctrine #2: failure emits direction), self-tests pass incl. brute-force verification of all counting formulas. Standalone; wiring into the phase0 suite is Harmonia A call (consumer)."}
{"id": "W-003", "kind": "shell", "status": "QUEUED", "consumer": "engine telemetry / LAW 1", "desc": "Consume remaining 17 DR reports; emit one typed consumption record each", "cmd": "echo 'placeholder — consumption pass'"}
{"id": "W-004", "kind": "shell", "status": "QUEUED", "consumer": "irreplaceable data", "desc": "Backup job: pg_dump fire+sci + robocopy F: corpus to Z:\\ (DECISION 2)", "cmd": "echo 'placeholder — needs Z: path confirmed'"}
{"id": "W-005", "kind": "shell", "status": "DONE-HEALED", "consumer": "M4 reporter truthfulness (James's inbox)", "desc": "Verify M4 healing WITHOUT access: next email should show (a) no Restore-Redis, (b) Session-model activity line, (c) deterministic tone; Pages state.json generated_at should move off 2026-06-24. If unchanged after 2 cycles, M4 is not pulling -> James at the box is the only remaining path.", "cmd": "echo 'needs M4 session or manual check of next brief email'", "result": "All three signals confirmed on first post-fix cycle (15:45 UTC 08-18): (1) docs/state.json committed with generated_at 15:44:44 — first fresh state since 06-24, dashboard unfrozen after 8 weeks; (2) brief dropped Restore-Redis and carries the Session-model activity line (James received it); (3) PG-bus read WORKS — 43 agents parsed, 1 ALIVE (Pronoia, the one real daemon). M4 healed entirely via repo, zero physical access."}
{"id": "W-006", "kind": "shell", "status": "QUEUED", "consumer": "M4 DR-pipeline visibility (the email's Watch-this section)", "desc": "M4's DR tracker watches Pythia's ledger and reports '0 dispatched in 24h' while 20 reports were fired+consumed from M1 via the manual dispatcher. Wire the dispatcher to emit the event M4 watches (agora/bus DR event or Pythia ledger append) so firing from any station is visible to the reporter.", "cmd": "echo 'design step — locate the ledger metis reads, append from dispatcher'"}
