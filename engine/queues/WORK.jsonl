{"id": "W-001", "kind": "shell", "status": "QUEUED", "consumer": "B-005 stratum analysis", "desc": "Power-stratified re-test design over the 725 resolution_limit kills (they name their own failure condition: effect too small at the N used)", "cmd": "echo 'placeholder — design step, run by session not shell'"}
{"id": "W-002", "kind": "shell", "status": "QUEUED", "consumer": "ladder Canon build-debt #1", "desc": "Build R4 representation-shift probe generator per phase0 pattern", "cmd": "echo 'placeholder — build step'"}
{"id": "W-003", "kind": "shell", "status": "QUEUED", "consumer": "engine telemetry / LAW 1", "desc": "Consume remaining 17 DR reports; emit one typed consumption record each", "cmd": "echo 'placeholder — consumption pass'"}
{"id": "W-004", "kind": "shell", "status": "QUEUED", "consumer": "irreplaceable data", "desc": "Backup job: pg_dump fire+sci + robocopy F: corpus to Z:\\ (DECISION 2)", "cmd": "echo 'placeholder — needs Z: path confirmed'"}
{"id": "W-005", "kind": "shell", "status": "QUEUED", "consumer": "M4 reporter truthfulness (James's inbox)", "desc": "Verify M4 pulled the pg_redis/metis fixes before its next brief cycle; if bus still fails from M4 after the method fix, repair M4's db.toml/creds (M4-local work)", "cmd": "echo 'needs M4 session or manual check of next brief email'"}
