# Loop Cycle 010 — 2026-08-21 (third relay of round 3; residue only)

**Relay note for James:** this paste was substantially the same round-3 content as cycles
008/009 — R5 relational, existential elimination, operator plasticity, epistemic objective
were all already executable. THREE items were genuinely new and are now built (8 tests;
suite 87 green).

1. **Legality-gated paired transition** — their sharpest R5 form. The relation gates
   ADMISSIBILITY: perturb b in one world and the synchronized step becomes illegal; the pair
   is STUCK at exactly the perturbation step (property-swept). Strictly stronger than
   cycle-009's value-changing guard, because "run twice and diff" cannot even be posed —
   there is no second run, only a blocked joint execution.
2. **Capacity theorem as a property** — for ANY finite palette, a Γ exhausting it exists;
   Hypothesis sweeps arbitrary palettes; the unbounded allocator is unaffected. Their
   |namespace| < ∞ ⟹ ∃Γ statement, executable.
3. **Cross-realization transfer** — the macro-cache killer that symbolic held-out cannot
   deliver. Same higher-level operator, different internal primitives (ints → mod-7 →
   polynomials). The cache captured the REALIZATION and is silently wrong in the new domain
   (returns the int answer under mod-7); the schema captured the OPERATOR and instantiates
   correctly in a third carrier it never saw. Schema order matters, so "these primitives are
   useful" is insufficient.

**Nothing needed retracting this cycle** — the earlier builds held under restatement, which
is itself mild evidence the fold-ins were faithful rather than convenient.
