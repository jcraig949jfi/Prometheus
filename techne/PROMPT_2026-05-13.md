# Techne — Next-Step Prompt (2026-05-13)

**Paste-target:** Techne agent session. Self-contained — agent has memory; prompt gives direction.

---

## Strategic context

Substrate-volume-and-quality remains the gate per `pivot/strategic_pivot_2026-05-11_substrate_volume_first.md`. LoRA training stays paused indefinitely. Substrate-shaped pipeline pilot fires today (3 substrate-shaped prompts: DR-001 / DR-007 / DR-231) alongside 17 standard prompts; dispatcher is running in background as of 2026-05-13. Aporia has authored a 20-claim starter batch using the `claim_v1` schema sketch at `aporia/meta/queue/claim_stack_aporia_starter_2026-05-13.md` — design-time inputs you can validate against the formal schema when shipped.

Per the 2026-05-11 adjudication ticket `T-2026-05-11-aporia-to-techne-claim-stack-design-adjudication` (P1-high, APPROVED with 2 mods), claim_v1 design ships unchanged except for two structural modifications:
- **Mod 1:** `source_report` field accommodates both hand-authored and mined provenance (no field-addition needed; descriptive flexibility only).
- **Mod 2:** LearnerRecord extension adds `verifier_outcome_class` enum (`decisive_verified | decisive_contradicted | decisive_inconclusive | verifier_transient_failure | verifier_permanent_failure`) to distinguish verifier reliability issues from real signal.

## Your scope this week

**You will NOT:**
- Trigger LoRA training, Mahler scans, or any --writeable substrate generation (existing stand-down per strategic_pivot).
- Open the v4.0 contract-change window for Wave 1 primitive registration until substrate-shaped pilot evidence is in.
- Burn cycles on Phase-3 P2 hardening pile.

**You will:**

### Track 1 — Ship claim_v1 schema + validator + runner stubs (PRIMARY)

Per the Day 1-2 plan in `pivot/claim_stack_design_2026-05-11.md` §7:

1. **Ship `techne/contracts/substrate_block_schemas/claim_v1.json`.** JSON Schema 2020-12. Fields per the design doc §1 with the two adjudication-required modifications applied. Add `claim_v1` to the 6 existing substrate_block schemas in `_common_definitions.json` reference family. All fields validated; bare-noun coordinate names (`rank` / `complexity` / etc) rejected at `claim_text` per HARD-5 enforcement; citation regex enforces arXiv/DOI format; minimum claim_text length 20 chars per the design.
2. **Extend `aporia/scripts/validate_substrate_blocks.py`** to recognize and validate claim blocks against `claim_v1.json`. Add `claim` to the parser's tag map; route validated claim blocks to `aporia/docs/staged_substrate_blocks/<date>/claim.jsonl` like the other 6 schemas. Same arXiv-verify + cross-reference checks where applicable.
3. **Ship `prometheus_math/substrate_generation/tier_1_claim_runner.py`** with the 7 verifier function stubs (citation_audit / catalog_lookup / mpmath_compute / sympy_factor / triangulation / substrate_self_check / manual_review) returning `inconclusive` + `not_yet_implemented` flag. Integration with existing `learner_enrichment.enrich()` adapter; add the three claim-specific extension fields (`claim_id`, `claim_category`, `actual_verdict`) plus the `verifier_outcome_class` per Mod 2. Per-claim execution emits 1-3 LearnerRecords (claim / falsify / promote-or-errata phases).
4. **Smoke-test against `aporia/meta/queue/claim_stack_aporia_starter_2026-05-13.md`.** Aporia's 20-claim starter batch is design-time input; Techne's validator + runner stubs should accept it cleanly OR surface schema-shape mismatches as findings. Document divergence in a short response ticket to Aporia.

### Track 2 — Wire the four most-leveraged verifiers (per Day 2 of claim_stack design)

Once stubs ship and Aporia's batch validates cleanly:

1. **`citation_audit`** — wrap `validate_substrate_blocks.py:_arxiv_head_check` + `_arxiv_withdrawal_check` (already built). Verified if citation resolves and is active; contradicted if withdrawn; inconclusive on network failure (retry once before marking; per Mod 2's verifier_outcome_class semantics).
2. **`catalog_lookup`** — wrap `prometheus_math.catalog_consistency.run_consistency_check` plus per-catalog adapters (LMFDB / OEIS / Mossinghoff / arXiv).
3. **`mpmath_compute`** — for numeric claims like Mahler measures. Aporia's CLAIM-calibration-mahler-00001 is the smoke target: compute Lehmer polynomial Mahler measure at dps=30, compare to 1.17628081825991750... within 1e-25 tolerance. Should return `verified`.
4. **`substrate_self_check`** — meta-claims about substrate behavior. Dispatch to a small table of known invariants. Aporia's batch has no substrate_self claims (those are Techne's territory per Q5 of the adjudication); but smoke-test on a stub invariant to confirm dispatch works.

The remaining three verifiers (`sympy_factor`, `triangulation`, `manual_review`) are stub-completable but lower-priority for this week.

### Track 3 — Ship training_anchor v1.1 (bs_coverage approval)

Per `T-2026-05-11-aporia-to-techne-bs_coverage-schema-approved-as-v1.1`: backwards-compatible v1.1.0 patch adding optional `bs_coverage: List[str]` field. Change `_schema_version` from `const: "1.0.0"` to `enum: ["1.0.0", "1.1.0"]`. Existing blocks (emitting 1.0.0) validate unchanged; new blocks emit 1.1.0 with the optional field; Ergon's ingester switches from regex-heuristic to explicit field when v1.1 is detected. ~10 LOC change to `training_anchor_v1.json` + a CHANGELOG entry.

### Track 4 — Process today's substrate-shaped pipeline pilot output (when it returns)

Today's dispatcher is firing 3 substrate-shaped prompts (DR-001 / DR-007 / DR-231) alongside 17 standard prompts. When the pilot returns (estimated wall-clock 35-50 min):

1. Run `python aporia/scripts/parse_substrate_blocks.py --batch-dir aporia/docs/deep_research_batch_2026-05-13`. Should extract substrate_blocks from the 3 pilot reports specifically; standard reports have no substrate_block YAML.
2. Run `python aporia/scripts/validate_substrate_blocks.py --staged-dir aporia/docs/staged_substrate_blocks/2026-05-13`. Report block validity rate.
3. Compare against pilot success criteria from `techne/PROMPT_2026-05-11_substrate_first.md`: ≥80% block validity + ≤50% reviewer time vs narrative-only equivalents at `aporia/docs/deep_research_batch_2026-05-11/{01,07}_*.md`.
4. File pilot-result ticket back to Aporia inbox: `T-2026-05-13-techne-to-aporia-substrate-shaped-pilot-result` with empirical metrics.

If pilot passes (≥80% validity, ≤50% reviewer time): migrate all 4 tier templates to substrate-shaped variants in `build_deck_from_queue.py`. Build `ingest_substrate_blocks.py` (the previously-deferred Day-4+ work). Approve claim-mining pipeline integration per `T-2026-05-11-aporia-to-techne-claim-mining-pipeline-proposal`.

If pilot is conditional (50-80% validity): schemas need refinement. Iterate one cycle; defer migration.

If pilot fails (<50% validity): revert to narrative-only; keep arXiv-verify as standalone post-hoc audit. Mining-pipeline proposal stays doc-only.

## Bounded resources

- File ownership: `techne/`, `aporia/scripts/` (shared with Aporia), `prometheus_math/substrate_generation/`.
- No compute escalation, no API budget, no kernel contract changes.
- Estimated total work: 4-6 days across Tracks 1-4. Track 1 is Day 1-3; Track 2 is Day 2-4; Track 3 is ~30 min; Track 4 is reactive (waits for pilot return).

## What to file when work lands

- Track 1 schema + validator + runner: append `techne/CHANGELOG.md` v3.2.0 entry. File `T-2026-05-13-techne-to-aporia-claim-stack-runner-ready` to Aporia inbox.
- Track 2 verifiers wired: append CHANGELOG; file readiness ticket to Aporia.
- Track 3 v1.1 schema: append CHANGELOG v3.1.1 (patch); file readiness ticket to Aporia + Ergon (ergon's ingester switches consumption path).
- Track 4 pilot result: file structured-metrics ticket back to Aporia.

## What you should ask James about

Nothing this week. All work is within file ownership + uses existing primitives + no contract changes.

## Pattern carryover

The `derive_kill_signature` factorization-leak catch from 2026-05-11 and the Aporia BS-coverage Q-form catch (Ergon's smoke test) are both instances of the generic-dim-to-specific-instance-via-code-level-audit pattern (memorialized in `feedback_generic_to_specific_audit.md`). Apply the same posture for Tracks 1-4: ship the generic-level infrastructure, expect the audit-at-consumption-time to surface specific instances that reshape the design.

End of prompt. Acknowledge receipt, then proceed with Track 1.
