{
  "runner": "docker",
  "image": "prometheus-fossil-i386:bookworm",
  "workdir": "upstream",
  "probe": [
    {
      "name": "gcc",
      "cmd": "gcc --version | head -1"
    }
  ],
  "build": [
    {
      "name": "compile the 1983 K&R sources natively 32-bit (i386 world: ILP32 as the code assumes; -Dvax selects the little-endian byte order the source keys on the vax/pdp11 symbols; -std=gnu89 -fcommon -w; no source change)",
      "cmd": "gcc -Dvax -std=gnu89 -fcommon -w -o $BODY/build/compact compact.c tree.c 2>&1 | grep -iE '\\berror' | head -5; gcc -Dvax -std=gnu89 -fcommon -w -o $BODY/build/uncompact uncompact.c tree.c 2>&1 | grep -iE '\\berror' | head -5; test -x $BODY/build/compact && test -x $BODY/build/uncompact && echo built"
    }
  ],
  "runs": [
    {
      "name": "round trip a 200 KB text sample; report the ratio",
      "cmd": "cd $BODY/build && awk 'BEGIN{for(i=0;i<4000;i++) printf(\"the quick brown fox %d jumps over the lazy dog %d\\n\", i, i*7)}' > sample && cp sample s.txt && ./compact s.txt; ls -l s.txt.C | awk '{print \"compacted bytes:\", $5}'; ./uncompact s.txt.C; cmp s.txt sample && echo ROUNDTRIP OK && echo original bytes: $(wc -c < sample)",
      "expect": {
        "exit": 0,
        "stdout_contains": [
          "ROUNDTRIP OK"
        ]
      }
    }
  ],
  "tests": [],
  "test_kind": "TECHNE",
  "test_classification_if_none": "TECHNE_SMOKE_HARNESS_PASS",
  "classification_if_ok": "RUNNABLE_CONTAINER",
  "notes": "The loser runs -- in the i386 world (batch 06), which replaces batch 05's per-run apt-get gcc-multilib + -m32 -static accommodation. Unmodified on x86-64 it segfaults (K&R implicit-int malloc truncates pointers). compact(1) exits 1 after a successful run (1983 convention), so the round trip is judged by cmp. Ratio printed beside the byte count for the lineage comparison."
}
